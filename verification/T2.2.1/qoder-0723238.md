# T2.2.1 Qoder implementation verification

## Fixed state

- Task: `T2.2.1 Reader & Workbench Polish`
- Base: `af424a1` (the ZCode functional seam commit is included in this
  Qoder worktree)
- Delivery implementation: `072323854c75ca43fa8bce1a03b27622be65430f`
- Delivery head: `16f61ca7a8b0dedb7bd583b3bc2ea19e5ad9a265` (escape-key and
  run-state assertions)
- Branch: `agent/qoder/T2.2.1-reader-workbench-qml`
- Worktree: `G:/CODEX/New Manga.worktrees/T2.2.1-qoder`
- Shell: PowerShell
- Interpreter: `G:/CODEX/New Manga.task-envs/T1.1.1-impl-py312/Scripts/python.exe`
- `PYTHONPATH=src`; `PYTHONDONTWRITEBYTECODE=1` for pytest commands;
  `QT_QPA_PLATFORM` was unset; pytest cache provider disabled.

## Scope delivered

- Added the shared `ChapterPicker.qml` Popup using the existing bookshelf
  models and slots only.
- Enabled Reader and Workbench context entry points while preserving
  standalone/no-context safety.
- Added accessible labels, token palette/geometry, and dismiss/copy controls
  to the existing command-error surface without changing error production.
- Added a `Tokens.bgCanvas` webtoon canvas wrapper while preserving the
  existing Flickable tile, natural-height, and scroll-offset logic.
- Added QML behavior and source-contract tests, including mutation guards for
  picker enablement, command-error accessibility, and webtoon ground.

## Verification

| Command | Result | Exit |
|---|---|---:|
| `python -m pytest tests/ui_shell tests/workbench/test_qml_workbench.py tests/reading_export -q -p no:cacheprovider -rs` | 278 passed, 0 failed, 0 skipped | 0 |
| `python -m pytest tests -q -p no:cacheprovider -rs` | 1209 passed, 6 skipped, 1 known environment failure, 1 warning | 1 |
| `python -m compileall -q src tests` | clean | 0 |
| `python -m bootstrap.app --smoke-test --data-root G:/CODEX/New Manga.task-envs/T2.2.1-qoder-smoke-20260922` | clean bootstrap smoke | 0 |
| `git diff --check` before delivery commit | clean | 0 |

Full-suite skip reasons: six OpenSSL-dependent network tests reported
`openssl unavailable`. The single failure is the pre-existing
`tests/providers/test_registry_readiness.py::test_no_model_runtime_is_installed_in_this_environment` assertion because `torch`
is installed in this venv; it is not caused by T2.2.1. The complete pytest
output was captured in the task run and the failure is intentionally not
reported as a product pass.

## Acceptance mapping

- Reader picker: covered by `tests/ui_shell/test_qml_shell.py` and the
  standalone Reader no-bookshelf test.
- Workbench picker and stale-book refresh: covered by the shell picker test;
  ZCode's functional seam tests cover the existing ViewModel refresh and
  navigation handoff.
- Command-error dismissal/accessibility: covered by
  `tests/workbench/test_qml_workbench.py`; the source mutation guard fails if
  the accessible label is removed.
- Webtoon canvas ground: covered by the production-source contract plus the
  existing webtoon scroll/restore test.
- No prohibited Python, bootstrap, storage, provider, dependency, or page
  changes were made.

## Receiving notes

Codex must review `16810c5..16f61ca` (or the final merge-equivalent diff),
rerun the focused tests after any review fix, then integrate serially. Do not
merge `master` from this worktree. The full-suite torch probe remains an
environment blocker for a clean exit, not a reason to hide or rewrite the
test.
