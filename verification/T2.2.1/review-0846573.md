---
task_id: T2.2.1
reviewer: Codex
author: Qoder (Codex-controlled recovery)
base_commit: af424a1
reviewed_head: 08465739ae92d7175a77d00053abe598bb150bae
decision: approved
---

# Review：T2.2.1 Reader & Workbench Polish

## 范围与依据

Reviewed `af424a1..0846573` against `doc/tasks/T2.2.1.md`, the approved
T2.2.1 plan, the existing bookshelf/navigation ViewModels, the Reader and
Workbench QML call paths, and the fixed author Handoff. The diff contains
only the authorized QML, QML tests, Handoff, and verification paths. The
ZCode functional seam was separately reviewed in
`verification/T2.2.1/review-df637df.md`.

## Architecture

PASS. `ChapterPicker.qml` consumes the already-published
`bookshelfViewModel.bookListModel` / `chapterListModel` and calls only
`selectBook`, `enterTranslation`, and `enterReading`. No database, file,
provider, service, bootstrap, schema, or new page is constructed. Reader and
Workbench remain four-page shell children, and the no-ViewModel guards remain
safe. The command-error surface still reads `commandErrorText` and calls
`clearCommandError`; its close action does not alter run state. The webtoon
change adds a canvas-ground Rectangle around the existing Flickable without
changing tile geometry, width fit, or scroll persistence.

## Verification

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| Reader/Workbench/QML affected suite | `PYTHONPATH=src G:/CODEX/New Manga.task-envs/T1.1.1-impl-py312/Scripts/python.exe -m pytest tests/ui_shell tests/workbench/test_qml_workbench.py tests/reading_export -q -p no:cacheprovider -rs` | PowerShell, Python 3.12, `0846573` | PASS — 278 passed, EXIT=0 | `verification/T2.2.1/qoder-0723238.md` |
| Full suite | `PYTHONPATH=src ... -m pytest tests -q -p no:cacheprovider -rs` | Same PowerShell/venv, implementation head | Known environment limitation — 1209 passed, 6 OpenSSL skips, torch probe failed, EXIT=1 | `verification/T2.2.1/qoder-0723238.md` |
| Python compile | `... -m compileall -q src tests` | Same venv | PASS, EXIT=0 | `verification/T2.2.1/qoder-0723238.md` |
| Bootstrap smoke | `... -m bootstrap.app --smoke-test --data-root G:/CODEX/New Manga.task-envs/T2.2.1-qoder-smoke-20260922` | Same PowerShell/venv, fresh data root | PASS, EXIT=0 | `verification/T2.2.1/qoder-0723238.md` |
| Diff hygiene | `git diff --check af424a1..0846573` | Qoder delivery worktree | PASS, EXIT=0 | delivery worktree |

The six skips are individually reported as `openssl unavailable`. The sole
failure is the pre-existing `test_no_model_runtime_is_installed_in_this_environment`
probe because torch is installed in the chosen environment; it is not a
T2.2.1 regression and is not counted as a product pass.

## Findings

在记录范围内未发现问题。P0/P1/P2 findings: none.

## Spec

PASS. The Reader picker, Workbench empty-state picker, dismissible and
accessible command-error surface, and webtoon canvas consistency each have
executable evidence. Escape dismissal and preservation of `runStatus` are
covered. No command palette, glass effect, top navigation, Region renderer,
theme persistence, storage migration, provider/runtime, dependency, or
packaging scope was introduced.

## 结论与复审

The fixed Qoder head `0846573` is approved for Codex serial integration. The
full-suite torch probe remains an environment limitation and must remain
visible in integration evidence; it must not be silently converted to PASS.
Post-integration focused/full/compile/smoke verification is still required
before T2.2.1 may be marked `VERIFIED_COMPLETE`.
