# DSH external review — deduplicated candidate findings (scoring input)

Range `c3dabc8..5f3c115` (7 ZCode-window integrations). Sources: guidance lens (GL), bug&correctness lens (BL), security lens (SL), historical lens (HL), comment lens (CL), author verification (DSH).

| ID | Source(s) | File:line | One-line defect | DSH verification |
|---|---|---|---|---|
| D1 | BL-2, HL, DSH | `src/infrastructure/importing.py:165-174` | pdfium `BGR` buffer mapped to `Format_RGB888` → red/blue swapped for every PDF page; wrong pixels persisted into Managed Copy + source_hash | reproduced: red PDF → centre pixel (0,0,255) |
| D2 | BL-1, SL-1, GL-1, HL, DSH | `src/infrastructure/sqlite/library.py:432-457` + `src/application/maintenance/trash.py:137-147` | `purge_pages` omits 5 FK-dependent tables (media_artifacts, pipeline_run_targets, pipeline_tasks, step_runs, step_result_candidates; no ON DELETE, `PRAGMA foreign_keys=ON`) → deterministic `IntegrityError` for any processed page, **after** the managed file was already unlinked → partial delete + `restore_batch` yields a live page with a missing file; purge can never succeed | reproduced on real schema + storage |
| D3 | BL-4, HL, CL(claim), DSH | `src/infrastructure/imaging/webtoon_tiles.py:99-108,234-262` + `src/ui/qml/reader/ReaderView.qml:245-254` | tile PNG saved from the overlap-inclusive decode window while rows declare `content_height` → non-first tiles are letterboxed and repeat the previous band's rows; contradicts the module's own "displayed band stays [content_top,content_bottom)" and AC-WEBTOON-004 | code + two lenses' measurements (400x832 vs declared 800) |
| D4 | BL-3 | `src/ui/qml/reader/ReaderView.qml:231-235` + `webtoon_tiles.visible_tiles` + `viewmodel.py:330/337` | `requestTiles` receives display-space `contentY` but the grid interprets page pixels → visible bands stay un-URL'd (blank bands) whenever viewport width ≠ page width; `pagePixelWidth/Height` exposed but unused | code reading (pagePixelWidth unused confirmed) |
| D5 | BL-5, HL, DSH | `src/ui/viewmodels/reader/viewmodel.py:112/128/246/252/258` + `ReaderView.qml:110-121` | page-turn slots never call `_rebuild_tiles()` and the toolbar prev/next buttons are not `vertical`-gated → tiled webtoon shows the previous page's pixels while progress advances | code + QML verified |
| D6 | BL-10, GL-1(scope), HL | `src/application/maintenance/trash.py:141-143` | purge deletes only `managed_original_ref`; mask/clean/translated files (and their rows for F-D2's fix) are never deleted → violates documented deletion scope (`doc/04_USER_FLOW.md:1677-1688`, `doc/07_NON_FUNCTIONAL_REQUIREMENTS.md:1065`) | code reading |
| D7 | BL-7, SL-3, CL(claim) | `src/application/maintenance/trash.py:39-49,87-105` | manifest written non-atomically (`write_text`) and read with unguarded `json.loads`; DB and manifest are two media with no transaction → truncation permanently breaks trash; the DB-first window hides soft-deleted pages with no batch to restore | code reading |
| D8 | BL-8, HL, DSH | `src/ui/viewmodels/bookshelf/viewmodel.py:370-371` | `@Slot(str, list)` without `result=` → metatype is `void`, QML receives `undefined` and the summary map is dropped; every sibling slot declares `result=` | metatype + precedent verified |
| D9 | BL-9, HL(R-004) | `src/application/importing/documents/service.py:142-147,162-166,191` | after a failed page the loop breaks: remaining pages are neither imported nor reported (module docstring says the remainder is reported); an empty document is reported as `skipped_duplicates` | code reading |
| D10 | DSH | `src/infrastructure/importing.py:126` | `import pypdfium2` sits outside the `try` → a missing optional dependency raises a bare `ImportError` instead of the typed `DocumentDecodeError` the module promises | code reading |
| D11 | GL-2, BL-13 | `src/application/tasks/service.py:486` + `src/infrastructure/pipeline/assembly.py:30-41` + `src/bootstrap/app.py:508` | docstring says `clean_probe` is "injected at assembly (AC 2)" and TASK-039 AC ② is ticked, but no production injection exists → render-only commands still plan `BLOCKED(missing_clean_artifact)`; already registered as a follow-up slice (TASK-040) | code reading |
| D12 | CL-1 | `src/infrastructure/imaging/webtoon_tiles.py:14-18,186-188` | docstring claims peak memory ≈ one tile because `QImageReader.setClipRect` decodes only the band; probe: `supportsOption(ClipRect) == False`, and the repo's own test (`tests/reading_export/test_webtoon_tiles.py:274-277`) says "setClipRect does not help — the handler allocates the whole image" → the memory contract behind the tiling (and TASK-042's premise) is false; the oversize BLOCKED is architectural, not a Qt limit | comment lens probe + repo test |
| D13 | CL-5, BL(TASK-020 R-004 open) | `src/infrastructure/imaging/webtoon_tiles.py:189,289-295` | comment claims the cache key is "source hash + tile index + geometry" but the key uses path+size+mtime-less metadata → an in-place same-size rewrite reuses stale tiles | code reading |
| D14 | CL-9 | `src/application/maintenance/trash.py:6-8` + `library.py:296-310` | module docstring claims "reader/imports hide soft-deleted rows through their existing filters"; the import side does not filter `deleted_at`, so a soft-deleted page's hash still blocks re-import (reported `skipped_duplicates` while the chapter shows nothing) and still occupies `source_order` | code reading |

Below-threshold / registered-only (not sent to scoring): no `deleted_at IS NOT NULL` guard on purge (BL-6 ≙ SL-2, 65); `tile_file` concurrency semantics + non-atomic PNG write (BL-11, 70); render pointer write outside the step-commit CAS (BL-12, 70); unbounded `TileGrid` partition (SL-4, 45); `clear()` glob deletion (SL-5, 30); `file_pages_skipped` dead variable; tile cache dir naming vs `doc/03 §18` (GL-10); `document_hash` computed then `del` (TASK-023 R-001, open P3).

Verified-holding (no issue): single-writer invariant (D2's sibling item (a)); trash **user-source-file** containment (`resolve()+relative_to`, escape tests pass); R-001 batch isolation; `_clean_available` probe=None equivalence; `commit_step` optimistic CAS; render composition-base CAS; SQL parameterisation; import never overwrites/deletes user files; no command execution / unsafe deserialisation / secrets; QML has no DB/file access; no schema/migration/dependency/deletion violations; no new skips.
