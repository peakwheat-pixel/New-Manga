---
id: T3.2.1-REPAIR-13
title: Restore chapter image import and readable QML controls
kind: implementation
status: done
approval: approved_by_user
suggested_owner: ZCode
owner: ZCode
reviewer: DeepSeek Harness (independent non-author Review)
depends_on: []
base_commit: 8738c41d7215920935e6fa067d3740e69280cdc6
branch: agent/zcode/T3.2.1-repair-13
worktree: G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13
delivery_head: aed009405fd523c52fa45e604afe40f01cef3d45
handoff_commit: 13ae23f65e963cf1d5aea19634572627e5e709db
evidence_head: 13ae23f65e963cf1d5aea19634572627e5e709db
review_report_commit: ccd26a4b4ef19a33ad91841602535614705213bd
integration_commit: 6fe7455d0927ae0b4328b77f117893d373797d92
review_decision: APPROVE_WITH_CONDITIONS
---

# T3.2.1-REPAIR-13：恢复章节图片导入并提高 QML 控件可读性

## 来源与目标

> **当前登记状态（2026-09-25，Codex 集成完成）**：产品 Delivery=`aed009405fd523c52fa45e604afe40f01cef3d45`；最终 Handoff/evidence head=`13ae23f65e963cf1d5aea19634572627e5e709db`；DeepSeek Harness Review=`ccd26a4b4ef19a33ad91841602535614705213bd`，原结论 `APPROVE_WITH_CONDITIONS`，C1/C2 条件均已满足。Codex 将固定任务差异集成于 `6fe7455d0927ae0b4328b77f117893d373797d92`，集成后 UI/QML 套件 `162 passed`，Task=`done`。C2 补记把修正前空白数写为 12；Codex 按固定 base 复算为 13（5+3+5），但三份日志所有尾随空白均已清除，完整 post-C2 `git diff --check` 为 exit 0。父 T3.2.1 Full Release Gate 仍 `OPEN`，父 AC3 仍 `BLOCKED`。

历史授权与状态快照只说明当时的恢复点；当前有效授权分三段：

1. **初版授权**：书架导入入口/章节绑定、无效状态提示与四页面按钮可读性修复及其限定测试、verification。
2. **R13-1 延续授权**：仅 `ExportWindow.qml` 与 `ChapterPicker.qml` 中相关控件的按钮/选择控件对比度修复。
3. **F-13-2 导出链路授权 + Review 接受 F-13-4 并入**：`ReaderView.qml` 一行 `open()`→`show()` 修复及可见性回归守卫；Reviewer 接受同一真实导出链路上的 `ExportWindow.qml` 一行 `id: exportOutputPath` 修复。该段以本机固定包完成实际两页 ZIP 导出验证。F-13-4 已登记为本切片范围，不扩展到其他导出逻辑。

**证据卫生条件（已完成）**：F-1 的 JPEG 截图已在 C2 evidence-only commit `13ae23f` 更名为 `.jpg`，并更新 Handoff/验证记录中的两处链接；F-3 三份 RED pytest 日志尾随空白已清除。产品代码与 AC 结果未在 C2 中改变；Codex 已完成独立范围核对与集成，见 [Codex 集成验证](../../verification/T3.2.1/repair-13/integration-6fe7455.md)。DSH Review 报告未重写。

用户在固定 Build 3 的真实 Sandbox 交互中报告：点击导入无法选择图片、按钮文字与底色难以区分、因此无法继续 Reader 导出流程。Codex 将原始截图、固定包哈希、启动记录和 stderr 存入
[`manual-observations.md`](../../verification/T3.2.1/ac3-interactive-20260925/manual-observations.md)。

已核验的代码线索：原始 stderr 有
`BookshelfView.qml:23: TypeError: Cannot read property 'currentChapterId' of undefined`；
`BookshelfView` 通过 `detailArea.chapters` 访问 `BookDetailPanel` 内部的 `chapters` ID，
该 ID 没有公开为外部属性。Reader 截图则显示没有选择阅读章节；当前 Export 按钮因此
处于 disabled 状态，导出实现尚未被有效测试。

目标是修复章节图片导入的真实 UI 路径，令无效/未选章节状态提供清楚反馈，并使全局按钮
启用、禁用、聚焦等状态的文字可辨；随后用有效导入页面复测 Reader 的 Export 入口和真实
文件输出。不得预设导出实现有缺陷；若有效前置条件下 Export 仍失败，先记录证据并向
Codex 提交范围扩展请求。

固定 base `8738c41d7215920935e6fa067d3740e69280cdc6` 正是本次 Build 3 的源码 HEAD；
其产品代码/打包/打包测试与 Codex 候选 `188d72a9d144b8cab16f77757abc46318ac8e5e6`
差异检查为零。新 Task branch/worktree 由 Codex 从该 base 创建，开始时工作区干净。

## Acceptance Criteria

- [x] **R13-AC1 — 导入入口无静默失败**：无书/无选中章节时，“导入”要么禁用并说明如何创建/选择章节，要么提供明确章节选择；任何状态下不得因 `chapters` 跨组件 ID 访问而产生 QML TypeError。本机直接运行固定 delivery GUI EXE 时的 stderr 中不再出现该错误。DeepSeek Harness 同意 PASS。
- [x] **R13-AC2 — 图片导入闭环**：在已有作品与选中章节时点击“导入”打开多文件选择器；选择两张固定合成 PNG 后，页面记录成功写入 Managed Copy、章节页数更新为 2，Workbench 可显示并选择两页。确认 fixture 源文件 hash 未变。DeepSeek Harness 同意 PASS；逐字节副本核验有人工证据，自动覆盖度较低。
- [x] **R13-AC3 — 按钮可读性**：Bookshelf、Workbench、Reader、Settings 和相关对话框中的按钮标签与底色可辨；至少检查 enabled、disabled、checked/highlighted、keyboard-focus 状态。保留现有 Graphite Atelier 色彩方向；提交对照截图及所用文字/底色值，不能只凭源码声称改善。DeepSeek Harness 同意 PASS；截图 JPEG 扩展名已在 C2 evidence-only follow-up 更正。
- [x] **R13-AC4 — Export 有效状态复测**：导入后从 Reader 用户可见操作打开 ExportWindow，完成实际两页 ZIP 导出。Delivery `aed0094` 修复 F-13-2（`ReaderView.qml` 的 `open()`→`show()`）与 Reviewer 接受并入的 F-13-4（`ExportWindow.qml` 输出路径 TextField 增加 `id: exportOutputPath`）；Review 确认有判别性 RED/GREEN 守卫、窗口可见及两页归档证据，判定 PASS。
- [x] **R13-AC5 — 固定包本机复验与交接**：按用户裁决直接运行固定 delivery EXE，以新临时目录通过 `--data-root <path>` 隔离数据；保留 OS/build、delivery SHA、启动命令、stdout/stderr、截图及归档检查。DeepSeek Harness 接受本机完整 AC1–AC4 流程为子任务验证完成；开发机覆盖差异保留在记录中，**不替代或改写**父 T3.2.1 的 Windows/Sandbox Release Gate。
- [x] **R13-AC6 — 范围与 Gate**：DeepSeek Harness 对 `8738c41..aed0094` 完成代码/测试范围 Review；C2 evidence-only head=`13ae23f` 将 JPEG 截图改名并清理三份 RED 日志；`git diff --check 8738c41 13ae23f`=exit 0，`git diff aed0094 13ae23f -- src tests packaging` 为空。Codex 在 `master` 核对并集成 62 个任务路径，未触碰父 Gate 判定。C2 补记声称修正前 12 处，但固定比较 `8738c41..66a13f8` 实测为 13 处（日志行数 5+3+5）；现有 head 已清理全部尾随空白，计数差异已记入集成证据，不影响 post-C2 检查结论。父 Gate 的 AC3 继续 BLOCKED；父级 AC5=NOT_RUN、AC6/AC7=PARTIAL / NOT_RUN、AC8/AC9=NOT_RUN、R-014～R-016=OPEN。

## 允许修改范围

- `src/ui/qml/Main.qml`、`src/ui/qml/theme/**`、`src/ui/qml/shell/**`：只处理跨页面按钮/调色板可读性，不重做视觉设计系统。
- `src/ui/qml/bookshelf/BookshelfView.qml`、`src/ui/qml/bookshelf/BookDetailPanel.qml`、`src/ui/qml/bookshelf/ChapterList.qml`：只处理章节选择、图片导入目标绑定和必要的用户反馈。
- **Codex 于 2026-09-25 授权的 R13-1 延续范围**：`src/ui/qml/windows/ExportWindow.qml` 与 `src/ui/qml/common/ChapterPicker.qml`，仅修正相关按钮/选择控件显式 `palette.buttonText` 与浅色原生控件表面低对比的问题；不得调整其他窗口样式、token、bootstrap 或 Qt Controls 全局样式。该延续已在 delivery `c0aba79` 交付。其后 Review 将 F-13-4 的单行 `id: exportOutputPath` 修复接受并入，见下一段登记。
- **第三段 F-13-2 授权与 F-13-4 Review 范围接纳**：`src/ui/qml/reader/ReaderView.qml` 仅将 ExportWindow 实例上的 `Component.onCompleted: open()` 改为 `show()`，保留销毁行为，并在已授权测试路径添加真实窗口可见性/无 ReferenceError 守卫。执行该授权的真实两页导出流程时发现 F-13-4：`src/ui/qml/windows/ExportWindow.qml` 输出路径 TextField 仅有 `objectName` 而无 `id`，点击导出时报 ReferenceError。DeepSeek Harness 将其作为同文件、一行、直接阻断 AC4 的最小修复接受并入本切片；代码变更仅增加 `id: exportOutputPath`，新增 RED/GREEN 守卫。除此以外未扩大 Reader、ExportWindow 或导出流程逻辑范围。
- tests/ui_shell/** 与 tests/reading_export/test_qml_contract.py：仅新增/调整本 Task 所需的 UI 状态与入口回归检查。
- `verification/T3.2.1/repair-13/**`：完整测试、本机固定包 GUI 运行记录与截图、构建日志及 manifest；除下方明确列出的 Review C2 evidence-only 文件外，不得覆盖既有证据。旧 Sandbox attempt 记录保持原样并作为历史证据。
- `doc/handoffs/T3.2.1-REPAIR-13-<delivery-short-sha>.md`：一个新 Handoff。
- **Codex 于 2026-09-25 为关闭 Review 条件 C2 授权的单一 evidence-only follow-up**：仅可将 `verification/T3.2.1/repair-13/screenshots/10-export-window-export-complete.png` 更名为 `.jpg`，清除 `discriminating-pre-repair.log`、`f13-2-pre-repair-red.log`、`f13-4-pre-repair-red.log` 的行尾空白，并仅更新 `verification/T3.2.1/repair-13/local-verification.md` 与现有 Handoff `doc/handoffs/T3.2.1-REPAIR-13-aed0094.md` 中指向该截图的链接。不得改动 Review 报告、交付代码、测试、命令输出内容或其他文件；交付后记录新 evidence head，产品 `delivery_head=aed0094` 不变。

## 禁止范围

- `src/application/**`、`src/domain/**`、`src/infrastructure/**`、`src/bootstrap/**`、
  Python 业务逻辑、`packaging/**`、依赖、Schema/迁移；任何未列明的 `src/**` 或
  `tests/**` 路径。
- `doc/STATUS.md`、`doc/REBASELINE_PLAN.md`、`doc/00_INDEX.md`、其他 Task、旧/既有
  Handoff、任何 DeepSeek Harness Review 报告；这些由 Codex/Reviewer 各自维护。
- 不修改 Reader/export service 实现来掩盖尚未满足的章节/页面前置条件；超出范围前先由
  Codex 更新本 Task 并登记新 base/scope。
- **Owner 集成禁令（交付期）**：ZCode 不合并到 `master`；主线集成仅由 Codex 在独立 Review 和本次用户委托后执行。REPAIR-13 的固定任务差异由 Codex 集成于 `6fe7455`；这不代表父 T3.2.1 Release Gate 通过。

## 测试要求

| 场景/AC | 计划命令或手工步骤 | 前提/环境 | 实际结果 | 证据 |
|---|---|---|---|---|
| R13-AC1/AC2 | 固定包本机 GUI 中查看选择提示；真实文件选择器导入两张 fixture；核对 Managed Copy、DB 页数并在 Workbench 显示两页；运行规定 pytest | delivery `aed0094` build8；隔离 `--data-root` | PASS；Review 同意（AC2 的副本逐字节校验有人工证据，自动覆盖度较低） | `verification/T3.2.1/repair-13/local-verification.md`、探针输出、pytest 日志 |
| R13-AC3 | 检查四页面与两个对话框 enabled/disabled/checked/focus，保存截图与色值 | build8 固定包本机 GUI | PASS；Review 同意 | `local-verification.md`、探针采样与 `screenshots/08-reader-chapter-picker.png`、`screenshots/09-export-window.png` |
| R13-AC4 | Reader 用户可见操作打开 ExportWindow 并完成两页 ZIP 导出 | build8，delivery=`aed0094` | PASS；Review 同意。F-13-2 与 F-13-4 均有修前 RED/修后 GREEN 守卫；归档包含预期两页 | `local-verification.md`、两份 RED 日志、ZIP hash 与截图 |
| R13-AC5 | 最终 delivery 固定包、全新隔离数据根、完整 AC1–AC4 GUI 操作与正常退出 | Windows 10.0.26200 x64 开发机，build8 EXE SHA-256=`8b2343ddb295e5b69699851bc10701311ed285ce3b52b911593538e87a7bc6e1` | PASS（按用户批准的本机验证口径；不关闭父级干净环境 Gate） | `local-verification.md` build8 增量、build8 log/hash/manifest、GUI logs、screenshots、ZIP hash |
| R13-AC6 | 固定 base 至 evidence head 路径/范围核对，执行完整及受限 diff-check | base=`8738c41`，delivery=`aed0094`，evidence=`13ae23f` | Review 无越权；62 个任务路径已由 Codex 集成；完整 `git diff --check 8738c41 13ae23f`=0；delivery→evidence 的 `src tests packaging` diff 为空 | [Review 报告](../../verification/T3.2.1/repair-13/review-report-dsh.md)、[C2 补记](../../verification/T3.2.1/repair-13/diff-check-full-post-c2.md)、[Codex 集成验证](../../verification/T3.2.1/repair-13/integration-6fe7455.md) |

测试输出须保存完整命令、日期、环境/Python 路径、固定 SHA、原始 stdout/stderr 和 exit code；skip 逐项说明。固定包本机 GUI 流程必须直接运行 EXE，不得以源码启动或 --smoke-test 代替；数据通过全新的 --data-root <path> 目录隔离。

## 依赖、风险与阻塞

- Owner=ZCode，按本 Task 的用户指定承担 QML/UI 实现；Reviewer=DeepSeek Harness，必须非作者。Codex 负责 Task
  注册、父 Gate 状态与后续集成。
- 作者恢复点仍为 `G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13`，
  branch=`agent/zcode/T3.2.1-repair-13`，delivery=`aed009405fd523c52fa45e604afe40f01cef3d45`，
  最终 Handoff/evidence head=`13ae23f65e963cf1d5aea19634572627e5e709db`；原始固定 base=`8738c41d7215920935e6fa067d3740e69280cdc6`。作者 worktree 保留，未跟踪 DSH prompt/run 与重复报告未清理。
- 用户提供的实际复现证据：`verification/T3.2.1/ac3-interactive-20260925/`。使用其中
  fixture 时须核对并保持其 manifest hash。
- 父 T3.2.1 Gate 仍 `OPEN`，父 AC3 仍 `BLOCKED`；本切片的 AC4 PASS 不改变父级 AC3/AC5/AC6–AC9、R-014～R-016。
- Review 原结论为 `APPROVE_WITH_CONDITIONS`；C1/C2 均已满足，Codex 集成后本 Task=`done`。父 Gate 仍 `OPEN`，父 AC3 仍 `BLOCKED`。

## 交付与运行记录

- 提交链：首段 Delivery=`2f18e78` / Handoff=`df95ad1`；R13-1 Delivery=`c0aba79` / Handoff=`efe9c85`；最终 Delivery=`aed009405fd523c52fa45e604afe40f01cef3d45`；Handoff/evidence head=`13ae23f65e963cf1d5aea19634572627e5e709db`；Codex integration=`6fe7455d0927ae0b4328b77f117893d373797d92`。原始 base=`8738c41d7215920935e6fa067d3740e69280cdc6`。
- 独立 Review 报告：[review-report-dsh.md](../../verification/T3.2.1/repair-13/review-report-dsh.md)，报告登记 commit=`ccd26a4b4ef19a33ad91841602535614705213bd`，固定审查 base=`8738c41`、delivery=`aed0094`、原始 evidence tip=`66a13f8`。Codex 采信结论 `APPROVE_WITH_CONDITIONS`；C1/C2 条件已核实满足，Task=`done`，integration=`6fe7455d0927ae0b4328b77f117893d373797d92`。
- F-13-4 已由 Reviewer 接受并入本切片；Codex 已将第三段授权登记为 F-13-2 + F-13-4 和本机两页导出验证。C2 evidence-only 修正=`13ae23f`，产品 Delivery 保持 `aed0094`。
- C2 前 Codex 复算 `git diff --check 8738c41 66a13f8` 为 exit 2，共 13 条诊断（5+3+5）；C2 补记中的“12 处”及第三份日志“6 行”计数不一致，已在 [集成验证](../../verification/T3.2.1/repair-13/integration-6fe7455.md) 记录。C2 后 `git diff --check 8738c41 13ae23f` exit 0；JPEG 截图改名为 `.jpg`，仅两处链接更新，文件 blob 不变。
- Reviewer 与 Codex 集成后复跑均为 `162 passed, exit 0`；任务代码只改授权 QML 与两份回归测试，未改 Python 业务逻辑、export service、packaging、依赖、Schema、bootstrap 或 token 值。证据、逐项 AC 结论及限制见 Review 报告与 [Codex 集成验证](../../verification/T3.2.1/repair-13/integration-6fe7455.md)。
- 父 Gate 仍 `OPEN`；父 AC3=`BLOCKED`、AC5=`NOT_RUN`、AC6/AC7=`PARTIAL / NOT_RUN`、AC8/AC9=`NOT_RUN`、R-014～R-016=`OPEN`。子 AC5 的本机验证裁决只适用于 REPAIR-13，不替代父级环境要求。REPAIR-14 仍 `proposed / NOT_RELEASED`，未创建 worktree、未启动实现。
- 原始两次 DSH 401 派发记录仍保存在 `verification/T3.2.1/repair-13/review-dispatch-record.md`；它们是历史事实，本次最终 delivery 已由新的独立 Review 覆盖审查。

## 初版实施指令（历史，不再是恢复指令）

本节保留初版范围与 Sandbox 路径的历史描述。当前交付和验证状态以上方登记及最终 Handoff 为准。

## Codex 授权的 R13-1 延续指令（2026-09-25）

本节记录 R13-1 的既有授权及交付；该延续已在 `c0aba79` 完成。本节后续命令为已执行的历史记录，不构成当前新增授权。

Codex 已批准的新增代码路径仅为：

- `src/ui/qml/windows/ExportWindow.qml`
- `src/ui/qml/common/ChapterPicker.qml`

只处理截图 `08-reader-chapter-picker.png` 与 `09-export-window.png` 中原生浅色控件表面上的按钮文字低对比；不得改其他窗口、tokens、bootstrap、样式引擎、Python、依赖或 Schema。实现后在同一探针中更新并保存这两个状态的截图和对比度采样。

继续验证原有尚未证明的行为：通过 Bookshelf 导入按钮实际打开文件选择器并选入两张固定 fixture；随后进入 Workbench，确认两页可以显示并选择。现有探针通过 ViewModel 直接调用导入方法，且 `11-workbench.png` 是空状态，不能作为上述 UI 路径的 PASS。Reader 章节选择和导出窗口也应记录用户可见操作；本地 ZIP 两页结果只作为本地探针证据，不替代固定包 EXE 本机 GUI 验证。

完成后提交新的 Delivery 和 Handoff，保留本 Task 原始 base=`8738c41d7215920935e6fa067d3740e69280cdc6` 及前序提交，并以原始 base 到最终 Delivery 重新核对完整 diff。固定包构建或本机固定包 GUI 验证若受环境阻塞，逐项如实记录 `BLOCKED`。此前 Windows Sandbox attempt 已是历史记录，不需重试。完整 `git diff --check <base> <delivery>` 当前会因 `discriminating-pre-repair.log` 中保留的原始测试输出尾随空格而非零；不得静默修改或丢弃原始证据，也不得把受限路径的 diff-check 写成完整命令通过。若完整检查仍非零，保持 AC6 未通过并报告原始输出。DSH 401 修复前不要重复无效派发；凭证可用后由 DeepSeek Harness 对固定 base 与最终新 Delivery 独立 Review，未获报告前不得标 `approved` 或合并。

## Codex 授权的 F-13-2 延续指令（2026-09-25，已执行）

历史恢复点：worktree `G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13`，branch `agent/zcode/T3.2.1-repair-13`，原始作者 tip `c47e6dab3c7f4573ee024b2ceffa4ce28d16e2c8`；原始固定 base=`8738c41d7215920935e6fa067d3740e69280cdc6`。后续固定 delivery=`aed0094`、Handoff/evidence tip=`66a13f8`。

唯一新增生产代码范围是 src/ui/qml/reader/ReaderView.qml:62：将 ExportWindow 实例 Component.onCompleted: open() 改为 Component.onCompleted: show()，保留现有 onVisibleChanged 销毁逻辑。Handoff 将故障文件误标为 ExportWindow.qml；固定 delivery 源码与本机 stderr 已证明调用位于 ReaderView.qml。测试只可落在现有授权范围 tests/ui_shell/** 或 tests/reading_export/test_qml_contract.py；验证必须覆盖 Reader 点击导出后窗口 visible 且无 ReferenceError。不得修改其他 Reader/Export 行为、Python/export service、bootstrap、packaging、依赖或 Schema。

以上内容是 F-13-2 实施时的历史指令；后续 Review 条件 C1/C2 已满足，REPAIR-13 固定任务差异由 Codex 集成于 `6fe7455`，Task=`done`。父 Gate 仍 OPEN，父 AC3 仍 BLOCKED，父 AC5 仍 NOT_RUN；此子 Task 集成不代表父 Gate 通过。
