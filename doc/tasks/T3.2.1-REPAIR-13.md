---
id: T3.2.1-REPAIR-13
title: Restore chapter image import and readable QML controls
kind: implementation
status: in_progress
approval: approved_by_user
suggested_owner: ZCode
owner: ZCode
reviewer: DeepSeek Harness (independent non-author Review)
depends_on: []
base_commit: 8738c41d7215920935e6fa067d3740e69280cdc6
branch: agent/zcode/T3.2.1-repair-13
worktree: G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13
delivery_head: 2f18e784b7a4bdcc71672a949550594e7958846e
handoff_commit: df95ad1ed1f12ff3211831c14051319347055480
review_report_commit: null
integration_commit: null
---

# T3.2.1-REPAIR-13：恢复章节图片导入并提高 QML 控件可读性

## 来源与目标

用户在固定 Build 3 的真实 Sandbox 交互中报告：点击导入无法选择图片、按钮文字与底色难以区分、因此无法继续 Reader 导出流程。Codex 将原始截图、固定包哈希、启动记录和 stderr 存入
[`manual-observations.md`](../../verification/T3.2.1/ac3-interactive-20260925/manual-observations.md)。

已核验的代码线索：原始 stderr 有
`BookshelfView.qml:23: TypeError: Cannot read property 'currentChapterId' of undefined`；
`BookshelfView` 通过 `detailArea.chapters` 访问 `BookDetailPanel` 内部的 `chapters` ID，
该 ID 没有公开为外部属性。Reader 截图则显示没有选择阅读章节；当前 Export 按钮因此
处于 disabled 状态，导出实现尚未被有效测试。

目标是修复章节图片导入的真实 UI 路径，令无效/未选章节状态提供清楚反馈，并使全局按钮
启用、禁用、聚焦等状态的文字可辨；随后用有效导入页面复测 Reader 的 Export 入口和真实
文件输出。不得预设导出实现有缺陷；若有效前置条件下 Export 仍失败，先记录证据并向
Codex 提交范围扩展请求。

固定 base `8738c41d7215920935e6fa067d3740e69280cdc6` 正是本次 Build 3 的源码 HEAD；
其产品代码/打包/打包测试与 Codex 候选 `188d72a9d144b8cab16f77757abc46318ac8e5e6`
差异检查为零。新 Task branch/worktree 由 Codex 从该 base 创建，开始时工作区干净。

## Acceptance Criteria

- [ ] **R13-AC1 — 导入入口无静默失败**：无书/无选中章节时，“导入”要么禁用并说明如何创建/选择章节，要么提供明确章节选择；任何状态下不得因 `chapters` 跨组件 ID 访问而产生 QML TypeError。本机直接运行固定 delivery GUI EXE 时的 stderr 中不再出现该错误。
- [ ] **R13-AC2 — 图片导入闭环**：在已有作品与选中章节时点击“导入”打开多文件选择器；选择两张固定合成 PNG 后，页面记录成功写入 Managed Copy、章节页数更新为 2，Workbench 可显示并选择两页。确认 fixture 源文件 hash 未变。
- [ ] **R13-AC3 — 按钮可读性**：Bookshelf、Workbench、Reader、Settings 和相关对话框中的按钮标签与底色可辨；至少检查 enabled、disabled、checked/highlighted、keyboard-focus 状态。保留现有 Graphite Atelier 色彩方向；提交对照截图及所用文字/底色值，不能只凭源码声称改善。
- [ ] **R13-AC4 — Export 有效状态复测**：导入成功后，在 Reader 明确选择有页面的章节；Export 按钮应可用并打开 ExportWindow。执行一次 ZIP/CBZ 导出，确认目标文件存在且包含预期两页。若在这些有效前置下按钮或导出仍失败，不扩展至 Python/export service；将最小复现和证据交 Codex 决定新范围。
- [ ] **R13-AC5 — 固定包本机复验与交接**：在本 Task 固定 delivery 源树上重建包，记录原始完整构建日志、依赖版本、文件 manifest 和 EXE SHA-256；在当前 Windows 主机上将 `NEWMANGA_DATA_ROOT` 指向全新的临时数据目录，直接无参数启动固定 delivery 的 GUI EXE（不从源码/venv 启动，不使用 `--smoke-test`），通过真实 GUI 流程完成 R13-AC1～AC4 并正常退出。记录主机 OS/build、EXE SHA-256、隔离数据根、启动方式、原始 stdout/stderr、截图及导出归档检查。旧 Sandbox 尝试只保留为历史证据，不再是本子任务的执行要求。本机结果仅用于关闭 REPAIR-13 子切片，不满足或替代父级 T3.2.1 的现有干净 Windows / Sandbox Release Gate。交付 Handoff 后由 DeepSeek Harness 对固定 base/delivery 独立 Review。
- [ ] **R13-AC6 — 范围与 Gate**：`git diff --check <base> <delivery>` exit 0；逐项审查变更路径只在下方授权范围。AC3 父 Gate 在全流程复验前继续 `BLOCKED`；AC5=`NOT_RUN`、AC6/AC7=`PARTIAL / NOT_RUN`、AC8/AC9=`NOT_RUN`、R-014～R-016=`OPEN` 不由本 Task 改写。Full Gate 和最终独立 Review 通过前，禁止合并任何相关分支到 `master`。

## 允许修改范围

- `src/ui/qml/Main.qml`、`src/ui/qml/theme/**`、`src/ui/qml/shell/**`：只处理跨页面按钮/调色板可读性，不重做视觉设计系统。
- `src/ui/qml/bookshelf/BookshelfView.qml`、`src/ui/qml/bookshelf/BookDetailPanel.qml`、`src/ui/qml/bookshelf/ChapterList.qml`：只处理章节选择、图片导入目标绑定和必要的用户反馈。
- **Codex 于 2026-09-25 授权的 R13-1 延续范围**：`src/ui/qml/windows/ExportWindow.qml` 与 `src/ui/qml/common/ChapterPicker.qml`，仅修正相关按钮/选择控件显式 `palette.buttonText` 与浅色原生控件表面低对比的问题；不得调整其他窗口样式、token、bootstrap 或 Qt Controls 全局样式。该延续从当前作者 tip `33f2ff71f07d00378e738bce4a7d9c8ed9041555` 开始，需提交新的 delivery 与 Handoff，并以原固定 base `8738c41d7215920935e6fa067d3740e69280cdc6` 对最终完整切片重新 Review。
- `tests/ui_shell/**` 与 `tests/reading_export/test_qml_contract.py`：仅新增/调整本 Task 所需的 UI 状态与入口回归检查。
- `verification/T3.2.1/repair-13/**`：完整测试、本机固定包 GUI 运行记录与截图、构建日志及 manifest；不得覆盖既有证据。旧 Sandbox attempt 记录保持原样并作为历史证据。
- `doc/handoffs/T3.2.1-REPAIR-13-<delivery-short-sha>.md`：一个新 Handoff。

## 禁止范围

- `src/application/**`、`src/domain/**`、`src/infrastructure/**`、`src/bootstrap/**`、
  Python 业务逻辑、`packaging/**`、依赖、Schema/迁移；任何未列明的 `src/**` 或
  `tests/**` 路径。
- `doc/STATUS.md`、`doc/REBASELINE_PLAN.md`、`doc/00_INDEX.md`、其他 Task、旧/既有
  Handoff、任何 DeepSeek Harness Review 报告；这些由 Codex/Reviewer 各自维护。
- 不修改 Reader/export service 实现来掩盖尚未满足的章节/页面前置条件；超出范围前先由
  Codex 更新本 Task 并登记新 base/scope。
- 不合并到 `master`，不宣称父 T3.2.1 Release Gate 通过。

## 测试要求

| 场景/AC | 计划命令或手工步骤 | 前提/环境 | 实际结果 | 证据 |
|---|---|---|---|---|
| R13-AC1/AC2 | 本地真实组装探针 + `pytest tests/ui_shell tests/reading_export/test_qml_contract.py -q -p no:cacheprovider -rs`；还需直接启动固定包 EXE，点导入按钮打开文件选择器、选择两张 fixture，并在 Workbench 显示/选择两页 | 作者本地 Python 3.12 / PySide6 6.11.2；固定包本机 GUI 复验待执行 | PARTIAL：本地探针确认 hint、选章后导入两页、Managed Copy 与源 hash；但导入调用直接经 ViewModel，未验证文件选择器 UI；Workbench 截图为空状态，未证明可显示/选择两页；此前 Sandbox attempt BLOCKED，当前要求的固定包本机 GUI 流程尚未运行 | `probe-ui-states.stdout.log`、`screenshots/06-pages-imported.png`、`screenshots/11-workbench.png`、`sandbox-attempts.md` |
| R13-AC3 | 四个一级页面及相关对话框检查 enabled/disabled/checked/focus；记录状态、文字/底色与可读性；在固定包 EXE 本机 GUI 中复验 | 作者本地真实组装探针；固定包本机 GUI 复验待执行 | FAIL / 未完成：主页面采样有对比度数据，但截图 08 Reader ChapterPicker、09 ExportWindow 中按钮文字仍低对比；已授权 R13-1 修复范围，修复后的固定包状态尚未验证 | `screenshots/01–13/`、`probe-ui-states.stdout.log` |
| R13-AC4 | 选中含两页的 Reader 章节，确认 Export 可用、窗口打开、执行 ZIP/CBZ 并检查归档条目；在固定包 EXE 本机 GUI 中完成用户可见操作 | 作者本地真实组装探针；固定包本机 GUI 复验待执行 | PARTIAL：本地探针生成含两页 ZIP；Reader 章节和输出路径由探针设置，未完成固定包本机 GUI 复测；CBZ 未单独运行 | `probe-ui-states.stdout.log`、`screenshots/07-reader-active.png`、`screenshots/09-export-window.png`、`screenshots/10-export-after.png` |
| R13-AC5/AC6 | 固定 delivery build、manifest/hash；固定包 EXE 本机 GUI 实际交互；`git diff --check <base> <delivery>`；逐条核对范围及受保护路径 | Windows 主机；固定 Python 3.12.3 packaging venv 仅用于构建；固定 base/delivery SHA；运行时使用临时 `NEWMANGA_DATA_ROOT` | PARTIAL / NOT_RUN：固定 delivery build exit 0、EXE SHA 与 manifest 已记录；新的固定包本机 GUI 流程尚未运行；此前 Sandbox attempt BLOCKED（历史记录）；DSH Review dispatch 401，未生成报告；完整 diff-check 非零（raw 判别日志有尾随空格），`src/tests` 子集检查 exit 0；路径范围检查无越界 | `build5-delivery.raw.log`、`build5-delivery-exe.sha256`、`build5-delivery-artifact-manifest.sha256`、`sandbox-attempts.md`、`review-dispatch-record.md`、`discriminating-pre-repair.log` |

测试输出须保存完整命令、日期、环境/Python 路径、固定 SHA、原始 stdout/stderr 和 exit
code；skip 逐项说明。固定包本机 GUI 流程必须直接运行 EXE，不得以源码启动或 `--smoke-test` 代替；使用临时 `NEWMANGA_DATA_ROOT` 隔离真实用户数据。

## 依赖、风险与阻塞

- Owner=ZCode，按本 Task 的用户指定承担 QML/UI 实现；Reviewer=DeepSeek Harness，必须非作者。Codex 负责 Task
  注册、父 Gate 状态与后续集成。
- 固定 recovery point 为 `G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13`，
  branch=`agent/zcode/T3.2.1-repair-13`，base=`8738c41d7215920935e6fa067d3740e69280cdc6`。
- 用户提供的实际复现证据：`verification/T3.2.1/ac3-interactive-20260925/`。使用其中
  fixture 时须核对并保持其 manifest hash。
- 父 T3.2.1 Gate 仍 OPEN。AC3 当前 `BLOCKED`；任何本切片 Review 结论都不能将其直接
  改为 PASS。REPAIR-7/9/10/11/12 或本分支在全 Gate 与最终独立 Review 完成前均不得
  合并到 `master`。

## 交付与运行记录

- 首次 Delivery=`2f18e784b7a4bdcc71672a949550594e7958846e`；Handoff=`df95ad1ed1f12ff3211831c14051319347055480`；后续 DSH 派发失败记录 tip=`33f2ff71f07d00378e738bce4a7d9c8ed9041555`。当前 ZCode worktree 在该 tip 干净。`review_report_commit=null`：DSH turn 1 因 API key 401 失败，未生成 Review；记录见 `verification/T3.2.1/repair-13/review-dispatch-record.md`。Delivery 尚未批准或集成。
- Codex 依据 AC3“相关对话框”要求，将 R13-1 两个 QML 文件纳入本 Task 的受限延续范围；继续工作固定起点为 `33f2ff71f07d00378e738bce4a7d9c8ed9041555`。旧 delivery、Handoff、Sandbox 与 DSH 失败记录均保留。
- 父 Gate：AC3=`BLOCKED`；AC5=`NOT_RUN`；AC6/AC7=`PARTIAL / NOT_RUN`；
  AC8/AC9=`NOT_RUN`；R-014～R-016=`OPEN`。
- 2026-09-25 用户将 Owner 从 Qoder 改为 ZCode。原 Qoder branch/worktree 保留在固定 base，未修改；
  Codex 新建 ZCode branch/worktree。2026-09-25 Delivery 后进入 `in_review`；因 DSH 401 未获审查，随后
  Codex 授权 R13-1 受限延续，状态回到 `in_progress`。Reviewer、父 Gate 与全量 T3.2.1 状态不变。
- 2026-09-25 用户将 R13-AC5 的验证方式从 Windows Sandbox 改为本机固定包 GUI 验证：在当前 Windows 主机上直接启动固定 delivery EXE，以全新临时 `NEWMANGA_DATA_ROOT` 隔离数据，完成真实 GUI 全流程并记录运行证据。旧 Sandbox attempt 保留为历史证据，不覆盖、不作为本子任务当前验收要求。此变更只调整 REPAIR-13 子任务；父 T3.2.1 Full Release Gate 的现有干净 Windows / Sandbox 要求、OPEN 状态及 AC3=`BLOCKED`/AC5=`NOT_RUN` 均保持不变。

## 可直接转发给 ZCode 的实施指令

请在 `G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13` 的
`agent/zcode/T3.2.1-repair-13` 分支实施 `T3.2.1-REPAIR-13`，固定 base 为
`8738c41d7215920935e6fa067d3740e69280cdc6`。这是实际 Build 3 的源码树；先核对
branch、HEAD、工作区状态与 common directory，再阅读本 Task 以及
`verification/T3.2.1/ac3-interactive-20260925/manual-observations.md`。

只修复 Bookshelf 的章节图片导入 QML 接线/无效状态反馈和全局按钮可读性，按允许路径
编写必要回归测试。禁止修改 Python 业务逻辑、export service、打包实现、依赖、Schema、
Task/STATUS/Plan/Index 或任何 Review 报告。点击导入必须在无选章和有选章两种状态下都有
明确结果；有选章时实际导入两张固定 fixture，确认 Managed Copy 页数/源 hash；修复后在
所有一级页面检查按钮 enabled/disabled/checked/focus 可读性并保存截图。随后在同一有效
页面前置下测试 Reader Export 是否启用、打开导出窗口并产出包含两页的归档。若 Export
仍失败，只记录最小复现并交 Codex，不自行改出界模块。

按 R13-AC5 在固定 delivery 上重建包并保存完整原始日志、manifest 与 SHA-256；设置全新临时
`NEWMANGA_DATA_ROOT`，直接无参数启动固定包 GUI EXE，在本机完成真实 GUI 回归。不得从源码/venv 启动或使用 `--smoke-test`。运行 `python -m pytest tests/ui_shell` 和本 Task 规定的
Reader QML contract 检查；另运行 `git diff --check 8738c41d7215920935e6fa067d3740e69280cdc6
<delivery>` 并逐条核对所有改动路径。提交固定 Delivery、新 Handoff 和完整证据后交
DeepSeek Harness 对固定 base/head 独立 Review。父 AC3 在完整复验前保持 BLOCKED；最终
独立 Full-Gate Review 前严禁合并任何相关分支到 `master`。

## Codex 授权的 R13-1 延续指令（2026-09-25）

本节覆盖此前交付中发现的验收缺口。继续前先核对 ZCode worktree 的 branch、HEAD、clean status 与 common directory；期望 branch=`agent/zcode/T3.2.1-repair-13`、HEAD=`33f2ff71f07d00378e738bce4a7d9c8ed9041555`、工作区干净。不得 reset、stash、改写或丢弃现有 delivery、Handoff、verification 与 DSH 失败记录。

Codex 已批准的新增代码路径仅为：

- `src/ui/qml/windows/ExportWindow.qml`
- `src/ui/qml/common/ChapterPicker.qml`

只处理截图 `08-reader-chapter-picker.png` 与 `09-export-window.png` 中原生浅色控件表面上的按钮文字低对比；不得改其他窗口、tokens、bootstrap、样式引擎、Python、依赖或 Schema。实现后在同一探针中更新并保存这两个状态的截图和对比度采样。

继续验证原有尚未证明的行为：通过 Bookshelf 导入按钮实际打开文件选择器并选入两张固定 fixture；随后进入 Workbench，确认两页可以显示并选择。现有探针通过 ViewModel 直接调用导入方法，且 `11-workbench.png` 是空状态，不能作为上述 UI 路径的 PASS。Reader 章节选择和导出窗口也应记录用户可见操作；本地 ZIP 两页结果只作为本地探针证据，不替代固定包 EXE 本机 GUI 验证。

完成后提交新的 Delivery 和 Handoff，保留本 Task 原始 base=`8738c41d7215920935e6fa067d3740e69280cdc6` 及前序提交，并以原始 base 到最终 Delivery 重新核对完整 diff。固定包构建或本机固定包 GUI 验证若受环境阻塞，逐项如实记录 `BLOCKED`。此前 Windows Sandbox attempt 已是历史记录，不需重试。完整 `git diff --check <base> <delivery>` 当前会因 `discriminating-pre-repair.log` 中保留的原始测试输出尾随空格而非零；不得静默修改或丢弃原始证据，也不得把受限路径的 diff-check 写成完整命令通过。若完整检查仍非零，保持 AC6 未通过并报告原始输出。DSH 401 修复前不要重复无效派发；凭证可用后由 DeepSeek Harness 对固定 base 与最终新 Delivery 独立 Review，未获报告前不得标 `approved` 或合并。
