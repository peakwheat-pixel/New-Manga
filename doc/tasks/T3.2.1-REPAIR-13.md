---
id: T3.2.1-REPAIR-13
title: Restore chapter image import and readable QML controls
kind: implementation
status: in_progress
approval: approved_by_user
suggested_owner: ZCode
owner: ZCode
reviewer: DeepSeek Harness (independent non-author Review)
depends_on: []
base_commit: 8738c41d7215920935e6fa067d3740e69280cdc6
branch: agent/zcode/T3.2.1-repair-13
worktree: G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13
delivery_head: c0aba799168d10ab20c63efa7d8c4ff0a44ed6cd
handoff_commit: efe9c8575d9027fae94c40cfb4cda382bb79f6c2
review_report_commit: null
integration_commit: null
---

# T3.2.1-REPAIR-13：恢复章节图片导入并提高 QML 控件可读性

## 来源与目标

用户在固定 Build 3 的真实 Sandbox 交互中报告：点击导入无法选择图片、按钮文字与底色难以区分、因此无法继续 Reader 导出流程。Codex 将原始截图、固定包哈希、启动记录和 stderr 存入
[`manual-observations.md`](../../verification/T3.2.1/ac3-interactive-20260925/manual-observations.md)。

已核验的代码线索：原始 stderr 有
`BookshelfView.qml:23: TypeError: Cannot read property 'currentChapterId' of undefined`；
`BookshelfView` 通过 `detailArea.chapters` 访问 `BookDetailPanel` 内部的 `chapters` ID，
该 ID 没有公开为外部属性。Reader 截图则显示没有选择阅读章节；当前 Export 按钮因此
处于 disabled 状态，导出实现尚未被有效测试。

目标是修复章节图片导入的真实 UI 路径，令无效/未选章节状态提供清楚反馈，并使全局按钮
启用、禁用、聚焦等状态的文字可辨；随后用有效导入页面复测 Reader 的 Export 入口和真实
文件输出。不得预设导出实现有缺陷；若有效前置条件下 Export 仍失败，先记录证据并向
Codex 提交范围扩展请求。

固定 base `8738c41d7215920935e6fa067d3740e69280cdc6` 正是本次 Build 3 的源码 HEAD；
其产品代码/打包/打包测试与 Codex 候选 `188d72a9d144b8cab16f77757abc46318ac8e5e6`
差异检查为零。新 Task branch/worktree 由 Codex 从该 base 创建，开始时工作区干净。

## Acceptance Criteria

- [ ] **R13-AC1 — 导入入口无静默失败**：无书/无选中章节时，“导入”要么禁用并说明如何创建/选择章节，要么提供明确章节选择；任何状态下不得因 `chapters` 跨组件 ID 访问而产生 QML TypeError。本机直接运行固定 delivery GUI EXE 时的 stderr 中不再出现该错误。
- [ ] **R13-AC2 — 图片导入闭环**：在已有作品与选中章节时点击“导入”打开多文件选择器；选择两张固定合成 PNG 后，页面记录成功写入 Managed Copy、章节页数更新为 2，Workbench 可显示并选择两页。确认 fixture 源文件 hash 未变。
- [ ] **R13-AC3 — 按钮可读性**：Bookshelf、Workbench、Reader、Settings 和相关对话框中的按钮标签与底色可辨；至少检查 enabled、disabled、checked/highlighted、keyboard-focus 状态。保留现有 Graphite Atelier 色彩方向；提交对照截图及所用文字/底色值，不能只凭源码声称改善。
- [ ] **R13-AC4 — Export 有效状态复测**：导入成功后，在 Reader 明确选择有页面的章节；Export 按钮应可用，点击后 ExportWindow 必须真实显示，并执行一次 ZIP/CBZ 导出、确认归档包含预期两页。当前固定包复验在窗口显示前被 F-13-2 阻断；Codex 已冻结一行修复范围：src/ui/qml/reader/ReaderView.qml 中 Component.onCompleted: open() 改为 Component.onCompleted: show()。修复前后均不得改 Python/export service；修复后要在固定包本机真实 GUI 中重新验证窗口显示和两页归档。
- [ ] **R13-AC5 — 固定包本机复验与交接**：按用户裁决在当前 Windows 主机直接运行固定 delivery GUI EXE，以新建临时目录通过 --data-root <path> 隔离数据；不得从源码/venv 启动，也不得以 --smoke-test 代替真实 GUI。记录 OS/build、delivery SHA、隔离数据根、实际启动命令、stdout/stderr、截图和导出归档检查。Handoff 所载本机运行已为 AC1–AC3 及阅读链路提供实际包内证据，但 F-13-2 阻断 AC4 导出窗口，因此本 AC 的完整 AC1–AC4 流程目前为 PARTIAL；修复后必须在新的临时数据根重跑完整流程并正常退出。此方法只替代 REPAIR-13 子任务的 Sandbox 路径，不替代或改写父级 T3.2.1 的 Windows/Sandbox Release Gate。独立 Review 以原固定 base 对修复后的最终 delivery 执行。
- [ ] **R13-AC6 — 范围与 Gate**：记录 git diff --check <base> <delivery> 完整结果并逐项核对允许路径。当前 base 到 c0aba79 的完整检查 exit 2，原因是保留的 discriminating-pre-repair.log 有 5 处尾随空格；不得改写该原始判别证据，也不得将受限路径检查冒称完整 PASS。此项保持未通过，后续交付需照实报告。父 Gate 的 AC3 继续 BLOCKED；父级 AC5=NOT_RUN、AC6/AC7=PARTIAL / NOT_RUN、AC8/AC9=NOT_RUN、R-014～R-016=OPEN 均不由本 Task 改写。Full Gate 和最终独立 Review 通过前，禁止合并任何相关分支到 master。

## 允许修改范围

- `src/ui/qml/Main.qml`、`src/ui/qml/theme/**`、`src/ui/qml/shell/**`：只处理跨页面按钮/调色板可读性，不重做视觉设计系统。
- `src/ui/qml/bookshelf/BookshelfView.qml`、`src/ui/qml/bookshelf/BookDetailPanel.qml`、`src/ui/qml/bookshelf/ChapterList.qml`：只处理章节选择、图片导入目标绑定和必要的用户反馈。
- **Codex 于 2026-09-25 授权的 R13-1 延续范围**：src/ui/qml/windows/ExportWindow.qml 与 src/ui/qml/common/ChapterPicker.qml，仅修正相关按钮/选择控件显式 palette.buttonText 与浅色原生控件表面低对比的问题；不得调整其他窗口样式、token、bootstrap 或 Qt Controls 全局样式。该延续已在 delivery c0aba79 交付，最终完整切片须从原固定 base 8738c41 重新 Review。
- **Codex 于 2026-09-25 授权的 F-13-2 一行修复范围**：src/ui/qml/reader/ReaderView.qml，仅将 ExportWindow 实例上的 Component.onCompleted: open() 改为 Component.onCompleted: show()；保留 onVisibleChanged 现有销毁行为，不改其他 Reader、ExportWindow 或导出流程逻辑。Handoff 将故障位置误标为 ExportWindow.qml:62；固定 delivery c0aba79 的源码表明实际调用在 ReaderView.qml:62。允许在下列已授权测试路径内添加对真实 ExportWindow 可见性/无 ReferenceError 的回归守卫。
- tests/ui_shell/** 与 tests/reading_export/test_qml_contract.py：仅新增/调整本 Task 所需的 UI 状态与入口回归检查。
- `verification/T3.2.1/repair-13/**`：完整测试、本机固定包 GUI 运行记录与截图、构建日志及 manifest；不得覆盖既有证据。旧 Sandbox attempt 记录保持原样并作为历史证据。
- `doc/handoffs/T3.2.1-REPAIR-13-<delivery-short-sha>.md`：一个新 Handoff。

## 禁止范围

- `src/application/**`、`src/domain/**`、`src/infrastructure/**`、`src/bootstrap/**`、
  Python 业务逻辑、`packaging/**`、依赖、Schema/迁移；任何未列明的 `src/**` 或
  `tests/**` 路径。
- `doc/STATUS.md`、`doc/REBASELINE_PLAN.md`、`doc/00_INDEX.md`、其他 Task、旧/既有
  Handoff、任何 DeepSeek Harness Review 报告；这些由 Codex/Reviewer 各自维护。
- 不修改 Reader/export service 实现来掩盖尚未满足的章节/页面前置条件；超出范围前先由
  Codex 更新本 Task 并登记新 base/scope。
- 不合并到 `master`，不宣称父 T3.2.1 Release Gate 通过。

## 测试要求

| 场景/AC | 计划命令或手工步骤 | 前提/环境 | 实际结果 | 证据 |
|---|---|---|---|---|
| R13-AC1/AC2 | 在固定包本机 GUI 中无选书/选章查看提示；通过真实文件选择器导入两张固定 fixture；核对 Managed Copy hash、DB 页数并在 Workbench 显示和选择两页；运行规定 pytest | delivery c0aba79 固定包；Windows 10.0.26200 x64 开发机；隔离数据根通过 --data-root 指定 | PASS（作者证据）：本机包实际打开原生文件选择器、导入两图、Managed Copy hash 一致、DB 两页、Workbench 显示并选中两页 | verification/T3.2.1/repair-13/local-verification.md、pytest-ui-shell-qml-contract.log、probe-ui-states.stdout.log |
| R13-AC3 | 在 Bookshelf、Workbench、Reader、Settings 和相关对话框检查 enabled/disabled/checked/focus，并留截图、色值与可读性记录 | 固定 delivery c0aba79 的包内 GUI；同一隔离数据根 | PASS（作者证据）：本机包按钮文字可辨，ChapterPicker 和 ExportWindow 的对比度修复已覆盖；独立 Review 尚未完成 | local-verification.md、local-verification.md、probe-ui-states.stdout.log、screenshots/08-reader-chapter-picker.png、screenshots/09-export-window.png |
| R13-AC4 | 经 Reader 用户可见操作打开 ExportWindow，实际导出并核对两页 ZIP/CBZ；修复后在固定包本机 GUI 重跑 | c0aba79 固定包运行时证据；固定源码定位 ReaderView.qml:62 | BLOCKED（F-13-2）：点击“导出…”时 stderr 报 ReaderView.qml:62 ReferenceError: open is not defined，窗口未显示。源码探针直接调用 controller 产出的两页 ZIP 不能替代 UI 窗口验证。新授权的一行修复尚待 ZCode 实施和固定包复验 | local-verification.md、外部运行日志 G:\CODEX\repair13-local-verify\gui-stderr.log；此前 probe-ui-states.stdout.log |
| R13-AC5 | 在最终 delivery 上重建固定包，以全新 --data-root 目录启动 EXE，完成 AC1–AC4 并正常退出；保存构建、环境、stdout/stderr、截图及归档检查 | Windows 10.0.26200 x64 开发机（含 Python/venv/开发 PATH）；固定包 c0aba79 的 EXE SHA-256 为 18eac441cdedac9d51d9e3396e21c129d589183b106d4a688d14c73aceaf464d；运行时直接启动包内 EXE | PARTIAL：用户批准的本机验证方式已执行；AC1–AC3 和 Reader 阅读链路有证据，AC4 在导出窗口打开处阻断。完成一行修复后须重建最终包并全流程复验。此环境不是干净 Windows 覆盖；父级 Gate 不变 | build6-final-delivery.raw.log、build6-final-delivery-exe.sha256、build6-final-delivery-artifact-manifest.sha256、local-verification.md |
| R13-AC6 | 对原 base 到最终 delivery 执行完整 git diff --check，并逐项核对差异路径 | 固定 base 8738c41 与 delivery c0aba79 | NOT PASS：完整检查 exit 2，discriminating-pre-repair.log 中 5 处尾随空格；证据原样保留。路径核对无超范围改动。DSH Review 两次均 401，未生成报告 | discriminating-pre-repair.log、review-dispatch-record.md、dsh-review-run-2.log |

测试输出须保存完整命令、日期、环境/Python 路径、固定 SHA、原始 stdout/stderr 和 exit code；skip 逐项说明。固定包本机 GUI 流程必须直接运行 EXE，不得以源码启动或 --smoke-test 代替；数据通过全新的 --data-root <path> 目录隔离。

## 依赖、风险与阻塞

- Owner=ZCode，按本 Task 的用户指定承担 QML/UI 实现；Reviewer=DeepSeek Harness，必须非作者。Codex 负责 Task
  注册、父 Gate 状态与后续集成。
- 固定 recovery point 为 `G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13`，
  branch=`agent/zcode/T3.2.1-repair-13`，base=`8738c41d7215920935e6fa067d3740e69280cdc6`。
- 用户提供的实际复现证据：`verification/T3.2.1/ac3-interactive-20260925/`。使用其中
  fixture 时须核对并保持其 manifest hash。
- 父 T3.2.1 Gate 仍 OPEN。AC3 当前 `BLOCKED`；任何本切片 Review 结论都不能将其直接
  改为 PASS。REPAIR-7/9/10/11/12 或本分支在全 Gate 与最终独立 Review 完成前均不得
  合并到 `master`。

## 交付与运行记录

- 首次 delivery=2f18e78 / Handoff=df95ad1 保留为历史记录；最终现有 delivery=c0aba799168d10ab20c63efa7d8c4ff0a44ed6cd，Handoff=efe9c8575d9027fae94c40cfb4cda382bb79f6c2，作者分支最新 tip=c47e6dab3c7f4573ee024b2ceffa4ce28d16e2c8。review_report_commit=null：对 base 8738c41 到 delivery c0aba79 的两次 DSH 派发均因 DeepSeek API key 401 失败，未生成 Review；记录见 verification/T3.2.1/repair-13/review-dispatch-record.md。当前 delivery 未批准或集成。
- Codex 已冻结 F-13-2 的唯一代码改动为 ReaderView.qml:62 的 open() 改 show()，从作者 tip c47e6da 继续；保留父 Task 原始 base 8738c41。提交修复、回归守卫、本机固定包全流程证据及新 Handoff 后，以 base 8738c41 到最终新 delivery 重新进行独立 Review。当前 c0aba79 不再是最终 Review head。
- 父 Gate：AC3=`BLOCKED`；AC5=`NOT_RUN`；AC6/AC7=`PARTIAL / NOT_RUN`；
  AC8/AC9=`NOT_RUN`；R-014～R-016=`OPEN`。
- 2026-09-25 用户将 Owner 从 Qoder 改为 ZCode。原 Qoder branch/worktree 保留在固定 base，未修改；
  Codex 新建 ZCode branch/worktree。2026-09-25 Delivery 后进入 `in_review`；因 DSH 401 未获审查，随后
  Codex 授权 R13-1 受限延续，状态回到 `in_progress`。Reviewer、父 Gate 与全量 T3.2.1 状态不变。
- 2026-09-25 用户将 REPAIR-13 子任务的验证方式从 Sandbox 改为本机固定包 GUI 验证。Handoff 记录的实际启动参数为 --data-root G:\CODEX\repair13-local-verify\data，而非 NEWMANGA_DATA_ROOT 环境变量；执行时必须使用全新的隔离数据目录。AC1–AC3 和 Reader 阅读步骤已在本机包完成，F-13-2 阻断 AC4 窗口打开，因此完整 AC1–AC4 流程及 child AC5 仍为 PARTIAL，修复后需重建包复验。旧 Sandbox 记录作为历史证据保留。父 T3.2.1 Full Release Gate 的 Windows/Sandbox 要求、OPEN 状态、AC3=BLOCKED 与 AC5=NOT_RUN 不变。

## 初版实施指令（历史）

初版实施及原 Sandbox 方案已由 delivery 2f18e78 / Handoff df95ad1 取代；实际交付与验证结果见后续 Handoff。其后 R13-1 已在 c0aba79 交付。所有继续工作按本文末尾的 F-13-2 授权执行；本机包运行必须用 --data-root <path> 隔离数据，不使用 NEWMANGA_DATA_ROOT 环境变量。

## Codex 授权的 R13-1 延续指令（2026-09-25）

本节记录 R13-1 的既有授权及交付；该延续已在 c0aba79 完成，当前恢复点和继续范围以本文末尾的 F-13-2 指令为准。

Codex 已批准的新增代码路径仅为：

- `src/ui/qml/windows/ExportWindow.qml`
- `src/ui/qml/common/ChapterPicker.qml`

只处理截图 `08-reader-chapter-picker.png` 与 `09-export-window.png` 中原生浅色控件表面上的按钮文字低对比；不得改其他窗口、tokens、bootstrap、样式引擎、Python、依赖或 Schema。实现后在同一探针中更新并保存这两个状态的截图和对比度采样。

继续验证原有尚未证明的行为：通过 Bookshelf 导入按钮实际打开文件选择器并选入两张固定 fixture；随后进入 Workbench，确认两页可以显示并选择。现有探针通过 ViewModel 直接调用导入方法，且 `11-workbench.png` 是空状态，不能作为上述 UI 路径的 PASS。Reader 章节选择和导出窗口也应记录用户可见操作；本地 ZIP 两页结果只作为本地探针证据，不替代固定包 EXE 本机 GUI 验证。

完成后提交新的 Delivery 和 Handoff，保留本 Task 原始 base=`8738c41d7215920935e6fa067d3740e69280cdc6` 及前序提交，并以原始 base 到最终 Delivery 重新核对完整 diff。固定包构建或本机固定包 GUI 验证若受环境阻塞，逐项如实记录 `BLOCKED`。此前 Windows Sandbox attempt 已是历史记录，不需重试。完整 `git diff --check <base> <delivery>` 当前会因 `discriminating-pre-repair.log` 中保留的原始测试输出尾随空格而非零；不得静默修改或丢弃原始证据，也不得把受限路径的 diff-check 写成完整命令通过。若完整检查仍非零，保持 AC6 未通过并报告原始输出。DSH 401 修复前不要重复无效派发；凭证可用后由 DeepSeek Harness 对固定 base 与最终新 Delivery 独立 Review，未获报告前不得标 `approved` 或合并。

## Codex 授权的 F-13-2 延续指令（2026-09-25）

当前恢复点：worktree G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13，branch agent/zcode/T3.2.1-repair-13，作者 tip c47e6dab3c7f4573ee024b2ceffa4ce28d16e2c8；原始固定 base 仍为 8738c41d7215920935e6fa067d3740e69280cdc6。继续前核对 branch、HEAD、clean status、common directory；不得 reset、stash、清理或覆盖已有 delivery、Handoff、日志、截图或其他 worktree。

唯一新增生产代码范围是 src/ui/qml/reader/ReaderView.qml:62：将 ExportWindow 实例 Component.onCompleted: open() 改为 Component.onCompleted: show()，保留现有 onVisibleChanged 销毁逻辑。Handoff 将故障文件误标为 ExportWindow.qml；固定 delivery 源码与本机 stderr 已证明调用位于 ReaderView.qml。测试只可落在现有授权范围 tests/ui_shell/** 或 tests/reading_export/test_qml_contract.py；验证必须覆盖 Reader 点击导出后窗口 visible 且无 ReferenceError。不得修改其他 Reader/Export 行为、Python/export service、bootstrap、packaging、依赖或 Schema。

在新固定 delivery 上重建 GUI 包；使用新的空临时数据目录，通过 --data-root <path> 启动 EXE，完成实际文件导入、Workbench 两页选择、Reader 选择章节、点击导出窗口并生成包含两页的归档，正常退出。保存原始构建日志、EXE hash、manifest、环境、stdout/stderr、截图与归档检查，新增 Handoff。运行规定回归测试，并从原始 base 到新 delivery 记录完整 diff-check；现有 discriminating-pre-repair.log 的尾随空格证据不作静默修改。提交后等待 DSH 凭证恢复，由 DeepSeek Harness 对原始 base 到最终新 delivery 独立 Review；父 T3.2.1 Gate 仍 OPEN，AC3 仍 BLOCKED，AC5 仍 NOT_RUN，REPAIR-13 分支不得因此合并 master。
