---
id: T3.2.1-REPAIR-3
title: Frozen-path test discrimination and AC7 evidence-scope alignment
kind: implementation
status: ready
approval: approved_by_user
suggested_owner: Antigravity
owner: Antigravity
reviewer: DeepSeek Harness (independent non-author Review)
depends_on: []
base_commit: ffff7c688df07165c6e801c0c9572e9c4910cb9e
branch: agent/antigravity/T3.2.1-repair-3
worktree: G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-3
delivery_head: null
handoff_commit: null
integration_commit: null
---

# T3.2.1-REPAIR-3：修复 R-008 判别力并统一 AC7 口径

## 来源与目标

DeepSeek Harness 对 REPAIR-2 的独立重审结论为 `changes_requested`：reviewed product head `7d687f51567d4f947fee9b3f6eff4bc95e2b6413`，evidence head `ffff7c688df07165c6e801c0c9572e9c4910cb9e`；Review commit `4613338815406be0fdf2240cb1e5715a619f61f7`，branch `agent/deepseek/T3.2.1-review-7d687f5`。该 Review 发现 R-008 的 frozen-path 测试在删除生产分支后仍通过，并发现 AC7 在 Handoff 中写 `PASS`、在 Task 中写 `PARTIAL / NOT_RUN`。R-013 的 Task“完全覆盖”表述也与 AC6/AC7 未完成状态冲突。

本切片只修复这组测试判别力和验收口径问题。它不表示 T3.2.1 已通过 Review 或可集成；REPAIR-2 Review 中的其他 findings、AC3 clean-Windows blocker 与 AC6 未完成项继续保持原状态，等待 Codex 单独处置。

## Acceptance Criteria

- [ ] `test_frozen_qml_path_resolution` 使用与源码 fallback 不同的隔离目录，例如 `tmp_path / "meipass"`；创建 `ui/qml/Main.qml`，设置 frozen 属性并 reload 被测模块，断言 `_RESOURCE_ROOT` 与 `QML_PATH` 指向该目录。
- [ ] 基线下该测试通过；在临时/变异副本中删除 `src/bootstrap/app.py` 的生产 `sys.frozen` / `_MEIPASS` 分支后，同一测试失败。不得把变异写入正常交付 commit。
- [ ] 将变异 diff、基线与变异运行的原始输出、命令、PowerShell/Python 环境、完整被测 SHA 和两次 exit code 保存到 `verification/T3.2.1/repair-3/r008-mutation.log`。基线为 PASS，删除分支后的变异运行必须为非零退出。
- [ ] `doc/tasks/T3.2.1.md` 与新 Handoff 对 AC7 使用一致的 `PARTIAL / NOT_RUN` 口径：可说明源码级探针/测试结果，但明确发布包级 Lock、current/pinned Revision 与人工确认内容保护尚无可接受的干净 Windows 证据。不得把源码级结果写成发布包级 PASS。
- [ ] 将 Task 测试表中的 AC6 与 AC7 拆成各自状态，并改正与未完成 AC 冲突的“完全覆盖”表述；AC6 保持 `PARTIAL / NOT_RUN`，AC3 保持 `NOT_RUN`。
- [ ] 提交新的固定实现 head 和 Handoff；将新 Task 标为 `in_review`，逐项记录本切片 AC、变异 artefact 路径及尚未处理的 Review findings。

## 允许修改范围

- `tests/packaging/test_frozen_runtime_contract.py`
- `verification/T3.2.1/repair-3/**`
- `doc/tasks/T3.2.1-REPAIR-3.md`
- `doc/tasks/T3.2.1.md`：仅为 AC6/AC7 分项状态、相关证据范围文字和新 Handoff 指针所需的最小修改。
- `doc/handoffs/T3.2.1-REPAIR-3-<short-head>.md`

## 禁止范围

- 不修改 `src/**`、`packaging/**`、依赖、业务代码、Schema、用户数据或产品包；这次修复限于测试与证据/Task 文案。
- 不在正常源树中保留生产分支变异；变异只在临时副本或临时 worktree 执行。
- 不把 R-014～R-016 或其他 Review 项记为已解决；不改变 AC3=`NOT_RUN`、AC6=`PARTIAL / NOT_RUN`。
- 不修改 `doc/STATUS.md`、`doc/REBASELINE_PLAN.md`、主线历史；这些由 Codex 维护。
- DeepSeek Harness 对新固定 head 的独立复审通过、Codex 核对完整 Review 状态和 Release Gate 前，任何人不得把该分支集成到 `master`。

## 测试要求

| AC / 场景 | 命令或步骤 | 前提/环境 | 完成标准 | 证据 |
|---|---|---|---|---|
| R-008 基线 | `python -m pytest tests/packaging/test_frozen_runtime_contract.py -k test_frozen_qml_path_resolution -q` | 任务 worktree；记录 PowerShell、解释器路径、delivery SHA | PASS；断言路径来自独立 fake `_MEIPASS` 目录 | 原始日志与 `r008-mutation.log` |
| R-008 变异 | 在临时副本删除 frozen 分支，再运行同一 pytest 命令 | 临时副本与原工作树隔离 | 测试失败、exit code 非零；清楚记录变异 diff 和输出 | `verification/T3.2.1/repair-3/r008-mutation.log` |
| AC7 文档一致性 | 对照 Task、Handoff、clean-windows-safety 的作用域声明 | 新 Handoff 与对应固定 head | 三处均不宣称发布包级 AC7 PASS；AC7 为 `PARTIAL / NOT_RUN` | 新 Handoff、Task、既有日志 |
| 改动卫生 | `git diff --check ffff7c688df07165c6e801c0c9572e9c4910cb9e <delivery-head>` | 固定 delivery head | exit 0 | Handoff 引用的原始日志 |

测试日志须记录 shell、Python 解释器、命令、完整被测 head、实际输出与 exit code。未执行项标为 `NOT_RUN`，不得以计划命令代替证据。

## 依赖、风险与阻塞

- 前置代码/证据快照固定为 base `ffff7c688df07165c6e801c0c9572e9c4910cb9e`；现有产品实现仍为 `7d687f51567d4f947fee9b3f6eff4bc95e2b6413`。
- 真实脱机干净 Windows 尚未验证，AC3 继续是发布 blocker；AC6 全业务主流程也仍为 `PARTIAL / NOT_RUN`。
- R-014（Build 2 独立构建日志与 smoke 进程证据）、R-015（类型标注）、R-016（测试口径差异）及 Review 报告中的其他遗留项不在本切片内，不能因为 R-008/AC7 修复而推断整体 approved。

## 交付与运行记录

- Handoff：待交付，路径 `doc/handoffs/T3.2.1-REPAIR-3-<short-head>.md`。
- Review：待 DeepSeek Harness 对新固定 head 独立非作者复审；须记录 decision 与 reviewer commit/session。
- 实际测试：尚未执行；当前状态 `ready`，未自动启动。
- 集成：`integration_commit: null`；Review 与 T3.2.1 全量 Gate 均通过前不得集成。
