---
id: M2-D1
title: Headless & Qt Coupling Audit
kind: research
status: done
approval: approved_by_user
suggested_owner: DeepSeek Harness
owner: DeepSeek Harness
reviewer: Codex (independent non-author Review and integration)
depends_on: [GOV-001]
base_commit: 307a940265fc325076fe77698750392531f8fce1
branch: agent/deepseek/M2-D1-headless-qt-coupling-audit
worktree: G:/CODEX/New Manga.worktrees/M2-D1-deepseek-audit
delivery_head: 0a27a54c9048fa1d3149ef4641a5d67fcb20849a
handoff_commit: 6f2cbb3002b9ab6db048d6464fb83c696fe746ce
review_report_commit: 41f05713673400dbd4c9592a44fff0bc35579cb8
integration_commit: 100abfbb51e2d59d0b0dccbb0cb6aa15ba9f6e02
---

# M2-D1 — Headless & Qt Coupling Audit

## 来源、状态与目标

[AD-001](../AD-001_TARGET_DESKTOP_ARCHITECTURE.md) 已接受未来 React + TypeScript + Tauri + Python Core 目标；[GOV-001](GOV-001.md) 已完成登记与独立 Review。用户现授权**只读研究/审计**，不授权迁移实现。代码审计固定 base 为 `master` @ `307a940265fc325076fe77698750392531f8fce1`。Codex 登记提交只改变本 Task 和规划导航；Owner 开工前须核对 worktree 的实际 HEAD、该 base 的祖先关系及注册范围，不能把登记提交当作代码变化。

目标：逐目录识别当前 Python Core 与 PySide6/QML、Qt imaging/rendering、bootstrap 和 SQLite 写入的耦合，判断可复用边界与 headless 入口可行性，并交付有证据的 `KEEP / DECOUPLE / REPLACE / LEGACY / UNVERIFIED` 矩阵。分类是研究结论，不直接决定替代技术或释放实现。

本 Task 已作为只读研究集成完成，未释放迁移实现。它与 T3.2.1 Release Gate 并行，因为它只读源码且写入独立研究路径。T3.2.1 仍是唯一 Active 产品 Task；Full Gate=`OPEN`、AC3=`BLOCKED`、REPAIR-13=`ready`。M2 仍 `PLANNED / NOT_RELEASED`（实现）。

## Acceptance Criteria

- [x] **D1-AC1 — 六棵目录完整盘点**：逐一审计 `src/domain`、`src/application`、`src/ports`、`src/infrastructure`、`src/bootstrap`、`src/ui`。记录各目录的 PySide6/Qt 直接、延迟及间接依赖，含 QObject、QImage、QPdfWriter，以及 QML 与 Python ViewModel 的接口。无命中也要给出实际命令与结果，不能省略目录。→ 证据：`verification/M2-D1/02-required-rg.log`（必跑 `rg`，286 行命中，exit 0）、`03-supplementary-searches.log`（A1/A2 列出模块级 19 行与惰性 16 行 Qt 导入；C1/C2 给出 QML 接口）、`07/08-probe-P4/P5-*.log`（逐模块导入图）；报告 §2.1/§2.2/§2.4。
- [x] **D1-AC2 — 重点路径与权威边界**：追踪 Qt compositor、Qt layout、font handling、Webtoon tile decoding、image/document importing、bootstrap engine coupling、SQLite connection/writer/transaction 路径；对每项给出源文件/行、调用链、运行入口与状态权威。区分 UI 投影与持久化写入，核对 Python Core 单一持久化写者意图。→ 证据：`04-writer-and-entry-searches.log`（W1/W2/W4/W5/W6/W7/W8）；报告 §2.3/§2.4/§2.5/§2.6 与矩阵 M2D1-04/06/10~15/19/20/23。
- [x] **D1-AC3 — Headless 可行性**：给出当前无 QGuiApplication/QQmlApplicationEngine 的 Python 入口是否可装配/运行的静态与实际证据；如不能安全验证，列为 `UNVERIFIED` 并说明阻塞。Qt imaging/rendering 替代技术只列待决问题，不在此选择或改造。→ 证据：`05-probe-P1-core-import.log`（Core 102 模块在无 PySide6 环境全部导入成功，exit 0）、`06-probe-P2-P3-entry.log`（生产入口与装配根导入 exit 1）；运行时可行性照实记 `UNVERIFIED`（本环境无 PySide6）；报告 §4 与矩阵 M2D1-22。
- [x] **D1-AC4 — 可审查矩阵**：研究报告对每个有意义的耦合点逐项标 `KEEP`、`DECOUPLE`、`REPLACE`、`LEGACY` 或 `UNVERIFIED`，说明分类理由、对 headless 的影响、证据、风险、可复用条件和后续候选切片。`REPLACE` 仅表示需后续决策的候选，不授权重写。仅有 grep 命中不能构成分类证据。→ 证据：报告 §3 的 26 项矩阵（KEEP 9 / DECOUPLE 11 / REPLACE 3 / LEGACY 2 / UNVERIFIED 1）与 §6 候选切片。
- [x] **D1-AC5 — 只读范围与交接**：源码、测试、打包、依赖、Schema、历史 Task/Handoff/Review/Verification 零修改；只新增/更新下列授权研究路径。固定 Delivery 与 Handoff，记录命令、shell/Python 路径、HEAD、原始输出、exit code、NOT_RUN/UNVERIFIED 项；`git diff --check <base> <delivery>` exit 0，由 Codex 对固定 base/head 做非作者 Review 后才能集成。无 React/Tauri/IPC 初始化。→ Owner 范围与 `diff --check` 见 `verification/M2-D1/11-delivery-scope-and-diff-check.log`；Codex 非作者 [Review](../reviews/M2-D1-0a27a54.md) `41f0571` 为 `approved`，集成 merge=`100abfb`，受保护路径零修改。

## 审计起点与方法

先按 [AD-001](../AD-001_TARGET_DESKTOP_ARCHITECTURE.md) 的 `MIGRATION_AUDIT_REQUIRED` 边界和六棵目录查找。已观察到的候选入口包括 `src/application/export/pdf_qt.py`、`src/infrastructure/rendering/qt_compositor.py`、`src/infrastructure/rendering/qt_layout.py`、`src/infrastructure/rendering/font_catalog.py`、`src/infrastructure/imaging/webtoon_tiles.py`、`src/infrastructure/importing.py`、`src/bootstrap/app.py` 与 `src/ui/viewmodels/**`；这是定位线索，不是穷尽清单或预先分类。

必跑起点命令：

```powershell
rg -n 'PySide6|QObject|QImage|QPdfWriter|sqlite3|connect' src/domain src/application src/ports src/infrastructure src/bootstrap src/ui
```

补充搜索须覆盖 import、反射/延迟加载、QML/Qt 对象生命周期、影像/字体/布局/瓦片的调用方、SQLite `open_database`/connection/repository/transaction 路径。逐条复核实际代码与调用链，并区分源码事实、推断和未验证项。可做安全的只读/隔离临时数据探针；探针不得访问真实用户库，必须记录环境、命令、原始输出和 exit code。不要因命令返回零命中就声称整个目录无耦合。

## 允许修改范围

- `doc/tasks/M2-D1.md`：Owner 回填 checkpoint、结果与交接；Codex 负责发布与最终集成状态。
- `doc/research/M2-D1_HEADLESS_QT_COUPLING_AUDIT.md`：新研究报告和分类矩阵。
- `verification/M2-D1/**`：仅本 Task 新建的原始搜索、探针、环境、差异及覆盖证据；不得覆盖既有证据。
- `doc/handoffs/M2-D1-<delivery-short-sha>.md`：一个新 Handoff，引用固定 Delivery。

`src/domain/**`、`src/application/**`、`src/ports/**`、`src/infrastructure/**`、`src/bootstrap/**`、`src/ui/**` **只读**。上列可写路径不授权修改共享接口、Schema 或依赖。

## 禁止范围

不得修改 `src/**`、`tests/**`、`packaging/**`、`requirements*`、`AGENTS.md`、`doc/REBASELINE_PLAN.md`、`doc/STATUS.md`、`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/09_COLLABORATION.md`、其他既有 Task/Handoff/Review 或 `verification/**` 旧证据。不得创建 `frontend/**`、`src-tauri/**`、`package.json`、`Cargo.toml`，不得实现 React/Tauri/IPC、修改 Python Core/SQLite Schema/QML、关闭或重定义 T3.2.1 Gate。发现需修复或新契约时仅在报告中记录，交 Codex 冻结后续范围。

## 验证计划与 Gate

| 场景 | 命令 / 步骤 | 前提 | 当前结果 | 预期证据 |
|---|---|---|---|---|
| 固定点与保护 | `git rev-parse HEAD`、`git status --short`、`git diff --name-only <base> <delivery>`、`git diff --check <base> <delivery>` | Owner 独立 worktree；source base `307a940` | **PASS（固定点）**：HEAD=`bbeb489`、base 为祖先、注册提交未触碰代码、六目录存在；交付范围与 `diff --check` 见 `verification/M2-D1/01-…log` 与 `11-delivery-scope-and-diff-check.log` | `verification/M2-D1/` 的完整输出与 exit code |
| 六目录搜索 | 上述必跑 `rg`，另按调用链追加定向搜索和源码阅读 | 记录 shell、HEAD、命中与零命中 | **PASS**：必跑 `rg` 286 行命中 exit 0；17 组补充检索；逐模块导入图 160/181 通过、21 个 Qt 阻塞 | 搜索原始输出、覆盖清单和报告行号 |
| Headless 入口 | 静态装配图；如安全则在隔离临时数据根做无 GUI 探针 | 不触及用户库/真实模型；不写仓库源码 | **静态 PASS + 运行时 `UNVERIFIED`**：Core 102 模块无 Qt 导入成功（exit 0）；生产入口/装配根 exit 1（`No module named 'PySide6'`）；本环境无 PySide6，无 GUI 运行未验证 | 命令、环境、stdout/stderr、exit code；不能验证则 `UNVERIFIED` |
| 非作者 Review | Codex 比较固定 base/head、矩阵与原始证据，覆盖 Architecture 与 Verification | Delivery + Handoff 冻结 | **PASS**：独立 [Review](../reviews/M2-D1-0a27a54.md) `41f0571`，`decision: approved`；P2/NOTE 仅为记录的精度限制 | 固定 Review 报告；批准仅针对研究交付 |

完成 Gate：D1-AC1～AC5 有逐项证据；六棵目录、指定 Qt/SQLite/headless 问题均有分类或明确 `UNVERIFIED`；所有修改仅在 allowed paths；Codex 非作者 Review `approved`，集成后再标 `done`。源码未改时，产品测试可记 `NOT_RUN`，不得写成 PASS。

**Owner 自检（2026-09-25）**：D1-AC1～AC4 = PASS（证据见上表与报告 §2/§3/§4）；D1-AC5 = Owner 侧 PASS、Review 侧 NOT_RUN；产品测试 = `NOT_RUN`（源码零修改；本环境未安装 PySide6，Qt 相关套件不可执行）；headless 运行时 = `UNVERIFIED`。

## 依赖、风险与阻塞

硬依赖 GOV-001=`done`，AD-001 已在 `master` 登记。Owner=DeepSeek Harness，Reviewer/Integrator=Codex，禁止自审。DSH CLI 在前次 GOV-001 审查尝试中返回过 `AUTH 401`；这不改变本 Task 的授权，但若当前运行环境仍失败，Owner 应在 DSH Web 或可用会话中认领并提交，不能让 Codex 冒充 DSH 审计。正式开始时以本 worktree 的实际状态为准，错误或漂移先记录并交 Codex。

## 交付与运行记录

- Codex 于 2026-09-25 核对 `G:/CODEX/New Manga`：`master` @ `307a940265fc325076fe77698750392531f8fce1`；主工作树 1 tracked dirty + 35 untracked entries 均为 `PRE_EXISTING_WORKTREE_CHANGE`，未触碰。六棵审计目录真实存在。
- Codex 创建 Owner branch=`agent/deepseek/M2-D1-headless-qt-coupling-audit`，worktree=`G:/CODEX/New Manga.worktrees/M2-D1-deepseek-audit`，最初从固定代码 base `307a940` 创建；发布登记后须快进至包含本 Task 的主线注册提交，并重新确认干净状态。
- 注册时 Delivery/Handoff/Review/Integration 均尚无，Task 为 `ready`；该项是发布时的历史 checkpoint，最终结果见下方集成记录。T3.2.1 Full Gate 与 M2 实现授权状态不由本 Task 改变。
- Codex 于 2026-09-25 从 Owner worktree 尝试 `dsh --profile headless --json` 派发本审计；session=`session-0955a334-3cab-4b05-85e1-44bf5a4b937c`，exit=`1`，错误代码=`AUTH`，在审计启动前结束。Owner worktree 随后 status 空、HEAD=`504ed57772131140fa2d6f0aeef16d858d415c2d`，无 Delivery 或证据。Task 保持 `ready`，可由 DeepSeek Harness Web 会话按本冻结范围认领；不得把此次 CLI 尝试记为审计 PASS。
- **2026-09-25 DeepSeek Harness 认领并执行（DSH Web 会话，未走 CLI，故无 §6.1 session 口径）**：开工核对 `pwd`=`G:/CODEX/New Manga.worktrees/M2-D1-deepseek-audit`、branch=`agent/deepseek/M2-D1-headless-qt-coupling-audit`、HEAD=`bbeb4892746795be4bca92f4e5dda91a77c422a3`、worktree status 空、`git-common-dir`=`G:/CODEX/New Manga/.git`、`307a940` 为 HEAD 祖先、`307a940..HEAD` 只含 2 个注册提交且仅改 `doc/**` 与新增本 Task。
- 方法：先执行 Task 指定必跑 `rg`（`verification/M2-D1/02-required-rg.log`，286 行命中 exit 0），再做 17 组补充检索与写者/入口检索；随后在隔离临时数据根（`NEWMANGA_DATA_ROOT`/`LOCALAPPDATA` 指向 `%TEMP%` 生成目录，跑完即删）用 `python -B` + `PYTHONDONTWRITEBYTECODE=1` 运行 6 个探针（P1/P2/P3/P4/P5/P6，含 2 次探针自身缺陷的 `ATTEMPT1-failed` 记录）。探针后实测 `src/**` 与 `tests/**` 内 `__pycache__` = 0，源码与测试零写入；真实用户库/数据根从未访问。
- 关键结果：Core 102 模块（`domain`+`ports`+`application`）在**未安装 PySide6** 的解释器中全部导入成功（exit 0）；Qt 阻塞模块按模块名计 21 个（infrastructure 4 + bootstrap 1 + ui 16），其中 `ui/models/tasks/projection.py` 与 `ui/viewmodels/workbench/region_canvas.py` 按文件路径可加载、仅被包 `__init__` 重导出拖累（P6）；生产入口与该环境下的装配根导入 exit 1（`No module named 'PySide6'` @ `src/bootstrap/app.py:36`）。
- 交付：`doc/research/M2-D1_HEADLESS_QT_COUPLING_AUDIT.md`（26 项矩阵：KEEP 9 / DECOUPLE 11 / REPLACE 3 / LEGACY 2 / UNVERIFIED 1）、`verification/M2-D1/**`（11 个证据文件 + 2 个探针脚本）、新 Handoff。写集合严格限于本 Task 授权路径；`src/**`、`tests/**`、打包、依赖、Schema、T3.2.1 与历史证据零修改；未创建任何 React/Tauri/IPC 产物。Task 进入 `in_review`，等 Codex 非作者 Review。
- **2026-09-25 Codex 非作者 Review / 集成**：Review `41f05713673400dbd4c9592a44fff0bc35579cb8` 对 Delivery `0a27a54`、Handoff tip `6f2cbb3` 给出 `approved`（Architecture 与 Verification 均通过，R-001～R-006 为非阻断精度限制）；Codex 从 `master`=`bbeb489` 串行 merge `100abfbb51e2d59d0b0dccbb0cb6aa15ba9f6e02`，核对受保护路径零修改及 `git diff --check` exit 0 后将 Task 收敛为 `done`。产品测试仍 `NOT_RUN`，Qt 平台/生产无 GUI 运行仍 `UNVERIFIED`；本集成不释放 M2 实现。

## 可直接转发给 Owner 的指令

请在 `G:/CODEX/New Manga.worktrees/M2-D1-deepseek-audit` 的 `agent/deepseek/M2-D1-headless-qt-coupling-audit` 分支执行 `M2-D1`，先核对实际 HEAD、工作树、Git common directory，确认固定代码 base=`307a940265fc325076fe77698750392531f8fce1` 是祖先且已包含本 Task 的注册提交。仅审计 `src/domain`、`src/application`、`src/ports`、`src/infrastructure`、`src/bootstrap`、`src/ui`，先执行 Task 中的 `rg -n 'PySide6|QObject|QImage|QPdfWriter|sqlite3|connect' ...`，再逐处追踪 Qt imaging/rendering/font/Webtoon/importing、bootstrap 与 SQLite writer 调用链和无 GUI 入口。交付 `doc/research/M2-D1_HEADLESS_QT_COUPLING_AUDIT.md` 的 `KEEP/DECOUPLE/REPLACE/LEGACY/UNVERIFIED` 证据矩阵、`verification/M2-D1/**` 原始命令/环境/exit 记录、新 Handoff 与固定 Delivery；未验证项照实标记。源码、测试、打包、依赖、Schema、T3.2.1、历史证据只读；不建 React/Tauri/IPC。交 Codex 对固定 base/head 非作者 Review，未获 approved 前不集成或宣称迁移实现获授权。
