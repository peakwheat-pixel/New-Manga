---
id: T3.2.1-REPAIR-12
title: Correct REPAIR-11 evidence and pin the verification record
kind: implementation
status: ready
approval: approved_by_user
suggested_owner: Antigravity
owner: Antigravity
reviewer: DeepSeek Harness (independent non-author Review)
depends_on: []
base_commit: 962239521afbfec72025ab01941f89e08116ffc4
branch: agent/antigravity/T3.2.1-repair-12
worktree: G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-11
delivery_head: null
handoff_commit: null
integration_commit: null
---

# T3.2.1-REPAIR-12：更正 REPAIR-11 证据并固定验证记录

## 来源与目标

本 Task 承接 DeepSeek Harness 对 REPAIR-11 Delivery `962239521afbfec72025ab01941f89e08116ffc4` 的独立 Review `4f2f8a07e5b8119834cdc4cfd0fd0aa5c62ac4cc`，结论为 `changes_requested`。报告在 Reviewer 提交的 `verification/T3.2.1/repair-11/review-9622395.md`；其固定 Handoff `941dbc89e139a2e24e6b1d17c900e363995decfc` 中的下一 Task 指令列出两项 P1（F-11-1/F-11-2）及两项记录瑕疵（O-11-1/O-11-2）。Reviewer 报告和旧 Handoff 均只读。

固定新 base 为 REPAIR-11 的 Owner Delivery `962239521afbfec72025ab01941f89e08116ffc4`，不是含旧 Handoff 的 `941dbc89…`。已核验：9622395 是 941dbc89 的父提交；9622395 树中没有 `doc/handoffs/T3.2.1-REPAIR-11-9622395.md`，该旧 Handoff 在 941dbc89 树中。以 9622395 起新 Task 分支可在不改写旧 Handoff 的前提下，修复 `verification/T3.2.1/repair-11/build-evidence-binding.log` 并满足对新交付树的全树无命中检查。旧 REPAIR-11 分支及其 Handoff 必须保留。

本 Task 只更正可由原始 REPAIR-9 证据和 `packaging/build.ps1` 直接核实的内容，补齐 scope-audit 的固定对象记录，并生成新 Handoff。它不重新判定 AC3、AC5、AC6/AC7、AC8/AC9 或 R-014～R-016。

## Acceptance Criteria

- [ ] **R12-AC1 — F-11-1 SHA 更正**：在 `verification/T3.2.1/repair-11/build-evidence-binding.log` 中，把 Sandbox `NewManga.exe` 的错误 SHA-256 更正为 `4b6f4734415c2add670c2889b22ea107e28345ca24f4a1c86840d866b775e3e6`，并精确引用只读来源 `verification/T3.2.1/repair-9/build1-manifest.sha256:1` 与 `verification/T3.2.1/repair-9/sandbox-run.log:35`（可附 `build-manifest-diff.log:8` 交叉核对）。Build 1 manifest 与 Sandbox SHA 相等的结论只能绑定这些原始值。
- [ ] **R12-AC2 — F-11-2 命令更正**：逐条对照 base 中 `packaging/build.ps1` 的真实调用，修正 `build-evidence-binding.log` 所记命令。Closure 检查调用须与脚本的 `& $PythonExe $closureScript --dist-dir $artifactDir` 一致；manifest 调用须与 `& $PythonExe $manifestScript --root $artifactDir --output $manifestFile` 一致；PyInstaller 调用也须逐字匹配脚本行。不得保留脚本不存在的参数或把推测命令记作实际调用。脚本只读，不改 `packaging/**`。
- [ ] **R12-AC3 — O-11-1/O-11-2 固定对象**：修正 `verification/T3.2.1/repair-11/scope-audit.log` 中两处 `<Delivery>` 占位符和受保护路径检查中的动态 `HEAD`；都改为本轮实际、完整的固定 Delivery SHA，并列出对应命令和结果。检查针对哪个 SHA，就在日志中写哪个 SHA。
- [ ] **R12-AC4 — 全树错误值清零**：针对固定新 Delivery 执行 `git grep -n -i '4f940bbd' <new-delivery>`；输出必须无命中（Git grep 无匹配的预期退出码为 1）。结果应能复核，但保存的验证记录不得再次包含会被该全树 grep 命中的连续搜索字面值；可记录拆分构造 pattern 的 PowerShell 命令及实际传给 Git 的参数、输出与退出码。新 Handoff 只记录正确 SHA 和权威来源，不复制错误值。
- [ ] **R12-AC5 — 固定对象、范围与空白**：Delivery 的 `base..delivery` Owner 变更仅限 `verification/T3.2.1/repair-11/**` 与一个新 Handoff；`src/**`、`tests/**`、`packaging/**`、依赖与 Schema 零差异。`git diff --check <base> <delivery>` 与受保护路径 `git diff --exit-code <base> <delivery> -- src tests packaging requirements.txt requirements-dev.txt` 均 exit 0，原始命令、固定 SHA、输出和 exit code 入库。旧 Handoff、Reviewer 报告、REPAIR-7/9/10 产物保持不改。
- [ ] **R12-AC6 — 新 Handoff 与 Gate 诚实性**：提交新的 REPAIR-12 Handoff，逐项列出 F-11-1/F-11-2/O-11-1/O-11-2 的处置、固定 Delivery/Handoff、实际命令和证据。父任务状态保持：AC3=`NOT_RUN`（发布阻断项）、AC5=`NOT_RUN`、AC6/AC7=`PARTIAL / NOT_RUN`、AC8/AC9=`NOT_RUN`、R-014～R-016=`OPEN`；本 Task 不作其有效性判断。Delivery 固定后交 DeepSeek Harness 对固定 base/head 独立复审。Review `approved` 且 Codex 完成 T3.2.1 全量 Release Gate 与最终独立 Review 前，严禁合并 REPAIR-7/9/10/11/12 或任何相关分支到 `master`。

## 允许修改范围

- `verification/T3.2.1/repair-11/**`：仅修正 F-11-1/F-11-2/O-11-1/O-11-2，并新增本 Task 的验证记录；不得覆盖或改写 Reviewer 报告。
- `doc/handoffs/T3.2.1-REPAIR-12-<delivery-short-sha>.md`：一个新 Handoff。
- Task 注册文件及项目导航/状态由 Codex 在 `master` 维护，不同步进 Owner 分支；全树 grep 检查因此只针对本 Task base 派生的 Owner 分支树。

## 禁止范围

- `src/**`、`tests/**`、`packaging/**`、依赖清单、Schema/迁移、产品/QML 行为。
- `doc/STATUS.md`、`doc/REBASELINE_PLAN.md`、`doc/00_INDEX.md`、任何 Task 文件（由 Codex 管理）、旧 REPAIR-11 Handoff、任何 Reviewer 报告。
- REPAIR-7/9/10 证据/交付产物的修改、覆盖或重命名；它们只可作为只读原始来源引用。
- 父任务 AC3、AC5、AC6/AC7、AC8/AC9、R-014～R-016 的关闭、豁免或有效性判断；合并任何相关分支到 `master`。

## 测试要求

| 场景/AC | 计划命令或手工步骤 | 前提/环境 | 实际结果 | 证据 |
|---|---|---|---|---|
| F-11-1 / R12-AC1、AC4 | 搜索全树禁用 SHA 前缀；核对正确 SHA 与 REPAIR-9 manifest/Sandbox 原始日志 | 固定 Delivery tree；旧 REPAIR-11 Handoff 不在新分支历史中 | NOT_RUN | `verification/T3.2.1/repair-11/repair-12-verification.log` |
| F-11-2 / R12-AC2 | 逐字对照 `git show <base>:packaging/build.ps1` 中 PyInstaller、closure、manifest 调用 | `packaging/**` 只读 | NOT_RUN | 同上 |
| O-11-1/O-11-2 / R12-AC3 | `git diff --name-only <base> <delivery>` 与固定 delivery 的受保护路径命令，核对审计日志没有占位符或动态 `HEAD` | 完整 SHA 已固定 | NOT_RUN | `scope-audit.log`、`diff-check.log` |
| 空白与路径 / R12-AC5 | `git diff --check <base> <delivery>`；`git diff --exit-code <base> <delivery> -- src tests packaging requirements.txt requirements-dev.txt` | 同一 Git common directory | NOT_RUN | `verification/T3.2.1/repair-11/diff-check.log` |

## 依赖、风险与阻塞

- Owner=Antigravity；Reviewer=DeepSeek Harness，必须非作者。来源 Review 固定为 commit `4f2f8a07e5b8119834cdc4cfd0fd0aa5c62ac4cc`，结论 `changes_requested`。
- Recovery worktree 复用 `G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-11`。Codex 从固定 base `962239521afbfec72025ab01941f89e08116ffc4` 在该干净 worktree 建立新分支 `agent/antigravity/T3.2.1-repair-12`；原 `agent/antigravity/T3.2.1-repair-11` 指针保留在 Handoff tip `941dbc89e139a2e24e6b1d17c900e363995decfc`，不移动、不改写。
- `9622395` 是有意选择的 pre-Handoff base。若从 `941dbc89` 派生，旧 Handoff 自身就含全树 grep 禁止的错误值，而本 Task 禁止修改旧 Handoff；因此无法同时满足范围与无命中门禁。
- 父 T3.2.1 Gate 仍开放；任何本切片 Review 结果都不更改用户列出的 AC/R 状态。

## 交付与运行记录

- Recovery point：`G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-11`；旧分支/旧 Handoff 保留。
- 固定 base：`962239521afbfec72025ab01941f89e08116ffc4`。
- 新 Owner branch/worktree：`agent/antigravity/T3.2.1-repair-12` / `G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-11`。
- Delivery / Handoff / Review：尚无。
- 父 Gate 状态：AC3=`NOT_RUN`；AC5=`NOT_RUN`；AC6/AC7=`PARTIAL / NOT_RUN`；AC8/AC9=`NOT_RUN`；R-014～R-016=`OPEN`。

## 可直接转发给 Antigravity 的实施指令

请在 `G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-11` 的新分支 `agent/antigravity/T3.2.1-repair-12` 上实施 **T3.2.1-REPAIR-12**，固定 base 为 `962239521afbfec72025ab01941f89e08116ffc4`。Codex 已核验该 base 是 REPAIR-11 Delivery；旧 REPAIR-11 分支仍固定于 `941dbc89e139a2e24e6b1d17c900e363995decfc` 且保留，不要切回或改写旧 Handoff。任务登记文件在主工作区 `G:/CODEX/New Manga/doc/tasks/T3.2.1-REPAIR-12.md`；先核对当前 branch/HEAD/status/common directory 与该 Task 的 base，再开始编辑。

只改 `verification/T3.2.1/repair-11/**` 中本 Task 指定的三份已有审计/证据日志，并新增验证记录；另新增一个 REPAIR-12 Handoff。更正 Build 1 Sandbox exe SHA 为 `4b6f4734415c2add670c2889b22ea107e28345ca24f4a1c86840d866b775e3e6`，引用 `repair-9/build1-manifest.sha256:1` 与 `repair-9/sandbox-run.log:35`；逐字对照 base 的 `packaging/build.ps1` 修正 closure/manifest/PyInstaller 命令；将 `scope-audit.log` 中两处 `<Delivery>` 与动态 `HEAD` 换成固定实际 SHA。旧 REPAIR-11 Handoff 与 Reviewer 报告只读，不得将其 cherry-pick 或同步进本 Task 分支。

固定新 Delivery 后核验并记录：`git grep -n -i '4f940bbd' <new-delivery>` 无匹配（预期 exit 1；为避免验证日志自我命中，日志中的命令可用拆分 pattern 的 PowerShell 形式记录，但须说明实际参数等价）；Build 证据日志中的命令逐字匹配 `packaging/build.ps1`；`git diff --check 962239521afbfec72025ab01941f89e08116ffc4 <delivery>` exit 0；`git diff --exit-code 962239521afbfec72025ab01941f89e08116ffc4 <delivery> -- src tests packaging requirements.txt requirements-dev.txt` exit 0。逐项核对变更只在允许范围，完整记录 SHA、命令、原始输出和 exit code，提交新 Handoff 后交 DeepSeek Harness 独立复审。AC3/AC5/AC6/AC7/AC8/AC9 与 R-014～R-016 保持本 Task 前述状态；Review approved 且 Codex 完成全量 Release Gate 与最终独立 Review 前，严禁合并任何相关分支到 `master`。
