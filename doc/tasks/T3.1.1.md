---
id: T3.1.1
title: Unify Reading Progress and Export History into SQLite
kind: implementation
status: done
approval: approved_by_user
suggested_owner: Antigravity
owner: Antigravity
reviewer: DeepSeek Harness (approved @ 4e1eb3a)
depends_on: [T2.2.1, TASK-057]
base_commit: e771179
branch: agent/antigravity/T3.1.1-storage-sqlite
worktree: G:/CODEX/New Manga.worktrees/T3.1.1-antigravity-storage-sqlite
delivery_head: 899bd3c99752ce435da25e41166bafa1c2963abc
review_commit: 4e1eb3ab829eea438437e6289418c20393950dcb
integration_commit: 0c70e44
---

# T3.1.1：阅读进度与导出历史统一进入 SQLite

## Release Gate

**Decision：PASS TO START（2026-09-22）。** 本 Task 已由用户授权，范围、基线、
允许路径和独立复审门槛已冻结；本 Gate 只释放实现，不代表产品代码已经修改或
验收通过。实现从主线治理头 `e771179` 开始，Antigravity 为实现 Agent，Codex
担任 Lead / Architect / Integrator，
DeepSeek Harness 必须对固定 delivery head 做独立非作者 Review。

前置核对：

- T2.2.1 已在产品头 `fe9fca0` 完成集成；Release Gate 的主线治理基线为 `e771179`。
- TASK-057 已集成于 `38d6eaa`，其后续复审与恢复证据已存在；本 Task 复用现有
  migration backup/restore 能力，不接线 backup UI，也不处理 TASK-057 的 R-012。
- TASK-028 是已冻结的 Library/Page/Region 设计，不是本 Task 的实现授权；本 Task
  只承接 ReadingProgress 与 ExportHistory。
- 主工作区既有 dirty/untracked 路径均为用户内容，必须保持原样，不得清理、覆盖或
  纳入本 Task 的产品提交。

## 来源与目标

来源：D03 §29 `ReadingProgress`、D03 §31 `ExportHistory`、D08
`AC-READ-002`/`AC-EXPORT-002`、REBASELINE_PLAN T3.1.1，以及当前代码证据：

- `src/application/reading/ports.py` 的 `JsonProgressDocumentStore`；
- `src/application/export/ports.py` 的 `JsonHistoryDocumentStore`；
- `src/application/reading/service.py` 的 `ReadingProgress` 全字段与
  `(book_id, chapter_id, mode)` 语义；
- `src/application/export/service.py` 的 `ExportRecord` 全字段、repeat 依赖的
  `scope_snapshot_json` 与 `render_profile_snapshot_json`；
- `src/bootstrap/app.py` 当前以 `reading_progress.json` 和 `export_history.json`
  装配生产服务；
- `src/infrastructure/sqlite/schema.py` 当前最高 Schema 为 v3。

目标：在现有 SQLite 数据库中新增最小 v4 持久化面，使阅读进度和导出历史在重启
后可恢复、事务失败可回滚，并对现有 JSON 做一次安全导入；导入后 SQLite 是唯一
生产写入真值，遗留 JSON 保留为只读恢复材料。

## 冻结的实现边界

- 复用现有 `ProgressDocumentStore` / `HistoryDocumentStore` 的应用层调用形状，
  优先新增 SQLite adapter，避免无必要地改写 `ReadingService`、`ExportService`
  的业务语义。
- 新增一个位于 v3 之后的 additive migration。不得编辑 v1/v2/v3 SQL；迁移必须
  复用现有 `MigrationRunner` 的 checksum、事务、新 Schema 拒绝和迁移前备份语义。
- ReadingProgress 至少完整保存 `progress_id`、`book_id`、`chapter_id`、`mode`、
  `last_page_id`、两个 scroll offset、`progress_percent`、`last_read_at`、
  `total_read_seconds`、`updated_at`，并保持 Original/Translated 与 Webtoon
  offset 独立。
- ExportHistory 至少完整保存 `ExportRecord` 的全部字段，包括 repeat 所需的两个
  JSON snapshot 和失败/取消的 `detail`；SQLite 中 JSON 仍以文本快照保存，不写入
  凭据或文件二进制。
- 遗留 JSON 只导入一次：缺失文件视为空；合法文件在一个受控事务中导入；已有但
  损坏或结构非法的文件不得删除、覆盖或静默标记为成功，必须保留原文件并给出
  可诊断失败。导入标记使用现有 metadata 能力或等价的最小 Schema v4 机制，必须
  防止重启重复导入。
- 导入成功后不得再写两个 JSON 文件；原文件保留且内容/mtime 不应被生产写路径
  改写。数据库写入必须是短事务，失败时既不留下半行数据，也不破坏遗留文件。
- Bootstrap 必须把同一生产 SQLite 数据库接入两个 adapter；测试必须走真实生产
  装配路径，不能只证明独立 fake 可用。

## Acceptance Criteria

- [x] **AC1 Schema/迁移**：v3→v4 和空库 v4 均可迁移；迁移前备份、checksum、
  单迁移事务、失败回滚及旧程序面对新 Schema 的拒绝行为均有证据；v1/v2/v3
  migration 文本无修改。
- [x] **AC2 ReadingProgress**：所有字段 round-trip；同一
  `(book_id, chapter_id, mode)` 不产生重复行；Original/Translated、Webtoon
  `last_page_id`/`scroll_offset_y`、阅读时长与重启恢复均有测试。
- [x] **AC3 ExportHistory**：所有字段 round-trip；completed/skipped/cancelled/
  failed 记录、最新优先历史、repeat 使用原 snapshot、非法/缺失 JSON 不破坏既有
  记录均有测试。
- [x] **AC4 遗留导入**：合法 progress/history JSON 各只导入一次；导入后重启不重复；
  缺失、损坏、错误顶层结构和单行非法数据的行为可诊断；遗留文件不被删除或覆盖。
- [x] **AC5 生产装配**：`assemble_services`/`assemble_engine` 使用 SQLite adapter；
  生产路径不再把两个 JSON store 作为写入真值；现有 Reader/Workbench 行为测试
  仍通过。
- [x] **AC6 原子性与数据安全**：注入 DB 写入失败、导入失败、重启和并发/锁冲突时，
  不丢已有数据库行、不破坏遗留 JSON、不写用户源文件、Managed Copy、Lock、
  current/pinned Revision 或 Pipeline schema。
- [x] **AC7 范围与证据**：无新增依赖；无 QML/UI、Provider、Pipeline graph、
  backup UI 或打包改动；提供 Handoff、DeepSeek Harness 独立 Review、固定 head
  验证日志和 Codex integration evidence。

## 允许修改范围

- `src/infrastructure/sqlite/**`：仅 T3.1.1 的新 migration、progress/history
  adapter 及必要的 metadata/import 辅助。
- `src/application/reading/ports.py`、`src/application/export/ports.py`：仅为
  adapter 兼容性或明确的持久化错误语义所需的最小调整；服务业务语义不扩展。
- `src/bootstrap/app.py`：仅生产数据库 adapter 的装配与既有服务注入。
- `tests/reading_export/**`、`tests/storage/**`、`tests/core/**`：本 Task 的迁移、
  adapter、遗留导入、生产装配与回归测试。
- `doc/tasks/T3.1.1.md`、本 Task Handoff、`verification/T3.1.1/**` 及经 Codex
  同步的 `doc/STATUS.md`、`doc/REBASELINE_PLAN.md`、`doc/00_INDEX.md`、
  `doc/tasks/README.md`、`docs/superpowers/plans/2026-09-22-t3-1-1-unify-storage-into-sqlite.md`。

## 禁止范围

- 不改 v1/v2/v3 migration，不重做 TASK-028 的 Library/Page/Region，不重开 TASK-057。
- 不改 QML/UI/UX、Provider/credential、Pipeline run graph/handler、模型、导入器、
  Managed Copy、用户源文件、Lock/current/pinned Revision 语义。
- 不删除或就地重写遗留 JSON，不新增依赖，不修改 `AGENTS.md` 或其他 Agent 的
  未授权分支历史，不在主线直接实现产品代码。
- 发现必须修改共享契约、Schema 外部实体或产品取舍时，先停在 Task 中登记，交
  Codex/用户裁决，不在实现分支静默扩大。

## 测试要求

| 场景/AC | 计划命令或手工步骤 | 前提/环境 | 实际结果 | 证据 |
|---|---|---|---|---|
| AC1–AC4 | `PYTHONPATH=src <venv python> -m pytest tests/reading_export tests/storage -q` | PowerShell；T1.1.1-impl-py312 / Python 3.12.3 | 220 passed，exit 0 | [integration-focused.log](../../verification/T3.1.1/integration-focused.log) |
| AC5 | 真实 `assemble_services`/`assemble_engine` probe，重启同一 data root | delivery + independent Review evidence | 4/4 probes passed；Review approved | [review-899bd3c.md](../../verification/T3.1.1/review-899bd3c.md) |
| AC6 | migration/import/SQLite failure injection + source/managed-copy invariant probe | delivery + independent Review evidence | 通过；R-004 并发多实例风险登记为后续 P2 | [review-899bd3c.md](../../verification/T3.1.1/review-899bd3c.md) |
| 回归 | `PYTHONPATH=src <venv python> -m pytest tests -q`、`python -m compileall -q src tests`、bootstrap smoke | PowerShell；T1.1.1-impl-py312 / Python 3.12.3 | core 49 passed；compileall/smoke exit 0；未重复全量套件 | [integration evidence](../../verification/T3.1.1/integration-899bd3c.md) |

## 独立复审与交付门槛

1. Antigravity 在固定基线 `e771179` 的独立 worktree 实现并提交 delivery head。
2. Antigravity 先完成自测与 Handoff，但不能以自测替代复审。
3. DeepSeek Harness 在不参与实现的独立 worktree/上下文中，绑定 base/head 审查
   Architecture、Verification、迁移/导入数据安全和允许路径；结论必须为 `approved`
   才能进入集成。`changes_requested` 必须回到 Codex 返修并重新复审。
4. Codex 只在 Review 通过后执行集成，复跑固定验证，记录实际 shell、venv、命令、
   collected/passed/skipped/failed、已知环境限制及 `git diff --check`。
5. 只有 Handoff、独立 Review、集成验证和 STATUS/Plan 台账均回填后，才能把本 Task
  改为 `done / VERIFIED_COMPLETE`；完成后 T3.2.1 仍须先通过自己的 Release Gate，
  不得因本 Task 完成而自动释放实现权限。

## Review 处置与集成记录

- Delivery：Antigravity `899bd3c99752ce435da25e41166bafa1c2963abc`，基线
  `e771179beaa92b7c592a1986a32ffbf1b6302566`；Handoff commit
  `81c319705afd2be2abc168414310164acdce9d90`。
- Independent Review：DeepSeek Harness 在独立 worktree 审查，Review commit
  `4e1eb3ab829eea438437e6289418c20393950dcb`，结论 `approved`；Codex 仅在该结论
  后集成。
- **R-001（P2，accepted）**：v4 `reading_progress` 与 `export_history` 实际为无
  外键表；“外键关联、级联删除”是 delivery report 的措辞错误。按当前任务的
  数据保留边界接受无 FK 设计，避免遗留孤儿记录导入失败；本 Task 不引入级联删除。
- **R-002（P2，defer）**：重复 `progress_id` 的手工/损坏 JSON 仍可能抛出原始
  `sqlite3.IntegrityError`，但事务会回滚、遗留文件不变，正常 writer 生成 UUID 不
  触发该路径。为保持已审查 delivery head 不变，本次不返修；后续 legacy-import
  hardening 必须在写入前校验 ID 唯一性并抛出带文件/条目上下文的
  `LegacyInvalidRecordError`，再单独 Review。
- **R-003（P2，accepted with evidence correction）**：delivery Probe 3 只证明
  JSON 文件仍存在，不能单独表述为“tracking filesystem”。本次以 DeepSeek Harness
  的独立 call-level monitor（14 个 mutation API、0 次命中）作为不修改遗留文件的
  集成证据；原 delivery report 的实质内容保持不变（仅按本次要求清理行尾空白），正确解释登记在此处及
  [integration evidence](../../verification/T3.1.1/integration-899bd3c.md)。
- **R-004/R-005/R-006（P2，defer）**：分别为多实例并发写覆盖、既有迁移 checksum
  pending-only 策略和未使用的遗留 JSON factory/import；均非本 delivery 引入，登记
  后续维护，不阻塞本 Task 集成。
- Product merge：`0c70e44`；Review evidence merge：`cbed31b`；Handoff merge：
  `0ed71a8`。主线未纳入作者/Review worktree 的未授权历史改写。

## 交付与运行记录

- Release Gate：[T3.1.1 Release Gate](../../verification/T3.1.1/release-gate-e771179.md)。
- Handoff：[T3.1.1 Antigravity delivery](../handoffs/T3.1.1-antigravity-delivery.md)，
  head `81c3197`。
- Review：[review-899bd3c](../../verification/T3.1.1/review-899bd3c.md)，`approved`，
  commit `4e1eb3a`。
- Integration：[integration-899bd3c](../../verification/T3.1.1/integration-899bd3c.md)，
  product merge `0c70e44`。
- 当前状态：`done / VERIFIED_COMPLETE`；T3.1.1 产品代码、独立 Review、集成验证
  和治理台账均已回填。R-002 及 Review 其余 P2 follow-up 已明确登记，不构成当前
  release blocker。
