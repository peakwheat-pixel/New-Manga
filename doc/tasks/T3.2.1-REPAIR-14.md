---
id: T3.2.1-REPAIR-14
title: Restore persisted Bookshelf entries after application restart
kind: implementation
status: ready
approval: approved_by_user
suggested_owner: Antigravity
owner: Antigravity
reviewer: DeepSeek Harness (independent non-author Review)
depends_on:
  - T3.2.1-REPAIR-13
base_commit: 4ab6f58c06cb0691413e2630cb48a145c18c383a
branch: agent/antigravity/T3.2.1-repair-14
worktree: G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-14
implementation_release: RELEASED
integration_commit: null
---

# T3.2.1-REPAIR-14：恢复应用重启后的书架作品列表

## 来源与目标

来源为 REPAIR-13 最终 Handoff 与独立 Review 登记的 F-13-3：应用重启后 SQLite 中仍有作品，但书架列表显示为空。该问题尚未在 REPAIR-13 两次新建 data-root 的 GUI 会话中复现；需要使用同一个持久化 `--data-root` 完成正常退出和再次启动的复现。

当前代码线索：`src/ui/viewmodels/bookshelf/viewmodel.py` 的构造函数创建空 `BookListModel`，`refreshBooks()` 才会从 `LibraryService.list_books()` 加载条目；QML 下当前搜索未发现启动时调用 `refreshBooks()` 的路径。这是待验证的根因线索，不是已确认结论。Task 目标是确认真实数据根/SQLite 中作品存在时，重新启动应用即可显示这些作品，不触碰作品数据或 Schema。

Task 已由 Codex 登记为 `ready` 并释放给 Antigravity。依赖 REPAIR-13 已于 `6fe7455` 集成并完成。固定实现基线为当前 mainline HEAD `4ab6f58c06cb0691413e2630cb48a145c18c383a`；独立分支/worktree 和下列路径边界已冻结。实施前先按 R14-AC1 在隔离 data-root 复现并记录；若证据不支持当前根因线索，应停止修复推断并回报 Codex。

## Acceptance Criteria

- [ ] **R14-AC1 — 可重复复现**：固定一个临时 `--data-root`，创建至少一本作品、正常退出、使用相同 data-root 重启；确认 DB 有作品但初始书架为空，或如未复现则记录实际启动序列和 DB 证据并停止修复推断。
- [ ] **R14-AC2 — 根因与最小修复**：给出启动时 `BookshelfViewModel`、QML 生命周期、bootstrap、LibraryService 和 SQLite 查询路径证据；在冻结范围内修复初始化/刷新问题，不改 Schema、持久化格式或用户作品数据。
- [ ] **R14-AC3 — 判别性回归**：新增测试在修复前固定 base 上失败、修复后通过，覆盖持久化 repository 重启/重新装配后的书架初始模型；原有 bookshelf、bootstrap 测试通过，并保存完整命令、环境、stdout/stderr、exit code。
- [ ] **R14-AC4 — 本机固定包验证**：若本 Task 影响打包 GUI 路径，在同一个全新隔离 data-root 下运行固定 EXE 两次（首启创建作品后正常关闭，第二次启动验证书架卡片及作品详情可见）；确认没有重复作品、删除或数据重写，保存 EXE hash、启动记录、截图和 DB 行数证据。该证据不改写父 T3.2.1 Gate。
- [ ] **R14-AC5 — 范围、Handoff 与 Review**：仅修改冻结允许路径；完成独立非作者 Review，未通过前不集成；父 T3.2.1 Gate 状态不变。

## 冻结允许修改范围

- `src/ui/viewmodels/bookshelf/viewmodel.py`：只允许为初始书架模型 hydration / refresh 实施最小修复。
- `tests/ui_shell/test_bookshelf_viewmodel.py`、`tests/core/test_bootstrap.py`：只增加该缺陷的回归覆盖。
- `verification/T3.2.1/repair-14/**`：复现、测试与固定包验证证据。
- `doc/handoffs/T3.2.1-REPAIR-14-<delivery-short-sha>.md`：一个新 Handoff。
- 任何 QML 生命周期、bootstrap 装配或其他路径变更须先由 Codex 单独登记 scope change；不得假设路径已授权。

Antigravity 只能写入以上冻结路径（其中 Handoff 仅一个新文件）；不得修改 Task、STATUS、Plan 或索引等治理文件。

## 禁止范围

- 不修改 `src/domain/**`、`src/application/**`、`src/ports/**`、SQLite Schema/迁移、Managed Copy/导入行为、依赖、打包实现、REPAIR-13 文件或历史证据。
- 不处理章节页数标签未刷新、R-13-2/3/4 或其他书架视觉问题；不扩大为数据恢复/迁移功能。
- 不改写、不清理用户 data-root；仅使用本 Task 新建的隔离临时目录。
- 不修改父 T3.2.1 Release Gate 的 `OPEN`、父 AC3 `BLOCKED`、REPAIR-13 状态或任何 Release Evidence。
- 不合并到 `master`；不得声称父 Gate 通过或本 Task 已完成。
- 不修改 QML 视觉契约、依赖、打包实现；不修改 SQLite Schema、Managed Copy 或导入实现。

## 测试要求

| 场景/AC | 计划命令或手工步骤 | 前提/环境 | 实际结果 | 证据 |
|---|---|---|---|---|
| R14-AC1 | 计划：隔离临时 data-root 首启建书、正常退出、相同路径二次启动；只读核对 DB 与初始 UI | Windows 固定包；Task 释放后记录 EXE/base SHA | NOT_RUN | 无 |
| R14-AC3 | 计划：`pytest tests/ui_shell/test_bookshelf_viewmodel.py tests/core/test_bootstrap.py`，另提供修复前 RED 记录 | Task 释放后固定 venv / base | NOT_RUN | 无 |
| R14-AC4 | 计划：固定 EXE 在相同隔离 `--data-root` 下执行两次 GUI 流程 | Windows；如打包路径受阻则逐项 `BLOCKED` | NOT_RUN | 无 |

## 依赖、风险与阻塞

- Owner=Antigravity，因当前可见候选在 Python `BookshelfViewModel` 的初始模型加载；UI/生命周期若经证据证实属于 QML，则暂停并提交 Codex scope/Owner 调整请求。
- Reviewer=DeepSeek Harness，必须独立于实现作者。
- REPAIR-13 依赖已由 Codex integration `6fe7455` 满足。Task 已于 2026-09-25 由 Codex 释放为 `ready`；实现基线=`4ab6f58c06cb0691413e2630cb48a145c18c383a`，branch=`agent/antigravity/T3.2.1-repair-14`，worktree=`G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-14`。Owner 可在上述冻结路径内开始工作。
- `src/ui/viewmodels/bookshelf/viewmodel.py` 是当前线索，不代表根因已经证实；若 DB、data-root 或 bootstrap 装配证据不支持该假设，应先报告并停止代码修改。

## 交付与运行记录

- 2026-09-25：登记 F-13-3 新 Task，Owner=Antigravity，初始状态 `proposed / NOT_RELEASED`；来源见 ZCode worktree Handoff `G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13/doc/handoffs/T3.2.1-REPAIR-13-aed0094.md` 与 [独立 Review](../../verification/T3.2.1/repair-13/review-report-dsh.md)。
- 2026-09-25：Codex 释放 Task 为 `ready`，固定 base=`4ab6f58c06cb0691413e2630cb48a145c18c383a`，branch=`agent/antigravity/T3.2.1-repair-14`，worktree=`G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-14`；冻结允许路径与禁止范围见上文。父 Gate `OPEN`、父 AC3 `BLOCKED` 未变。
- Delivery：尚无。Review：尚无。Integration：尚无。
