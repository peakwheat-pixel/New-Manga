---
id: T3.2.1-REPAIR-10
title: Repair packaging-test path independence and evidence hygiene
kind: implementation
status: ready
approval: approved_by_user
suggested_owner: Antigravity
owner: Antigravity
reviewer: DeepSeek Harness (independent non-author Review)
depends_on: []
base_commit: eeb095ea94cdf0d477bcb09ab7c761883b06f833
branch: agent/antigravity/T3.2.1-repair-10
worktree: G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-10
delivery_head: null
handoff_commit: null
integration_commit: null
---

# T3.2.1-REPAIR-10：修正空白卫生与证据纪律

## 来源与目标

本 Task 从 REPAIR-9 的 Handoff/tip
`eeb095ea94cdf0d477bcb09ab7c761883b06f833`（包含产品修复 Delivery
`d2bf6a8621c0c89c077e181e3e7bec9ac7cf098f` 与其 Handoff）继续。Codex 在该
固定恢复点以当前任务环境运行 `pytest tests/packaging`，未设置外部
`PYTHONPATH`，结果为 **3 failed, 9 passed, exit 1**；三个失败均为
`ModuleNotFoundError: No module named 'bootstrap'`，发生于
`tests/packaging/test_frozen_runtime_contract.py`。完整命令、环境和错误已在本次
Task 登记前实测；这是待修复基线，不是交付验收结果。

目标仅为关闭该测试路径依赖问题（F-3(b)）、保持交付的空白卫生，并将完整、可复核的
测试原始输出入库。不得改动 REPAIR-9 的产品实现或扩大测试范围。本次独立 Review
须覆盖 REPAIR-10 在固定 base 之后的 Owner 变更；Review `approved` 只覆盖本切片，不
代表 T3.2.1 发布 Gate 通过。

## Acceptance Criteria

- [ ] **AC1 — F-3(b) 路径独立**：在固定 Windows/Python 3.12.3 packaging venv 中，
  明确确认环境变量 `PYTHONPATH` 未设置，运行 `python -m pytest tests/packaging`，
  得 **12 passed, 0 skipped, 0 failed, exit 0**。修复限于
  `tests/packaging/**`，不得要求调用者注入外部 `PYTHONPATH`、改变依赖或修改生产代码。
- [ ] **AC2 — 完整测试证据**：将上述运行的完整原始 stdout/stderr 与最终 exit code
  存入 `verification/T3.2.1/repair-9/packaging-tests.log`；日志头记录日期、worktree、
  固定 base、delivery head、PowerShell、Python 解释器绝对路径与版本、pytest 版本、
  `PYTHONPATH=UNSET` 和精确命令。若实际 skip 数不为 0，逐项记明测试名与原因；不得
  只保存摘要或末尾几行。
- [ ] **AC3 — 空白卫生**：`git diff --check eeb095ea94cdf0d477bcb09ab7c761883b06f833 <delivery>`
  exit 0；原始命令、输出和 exit code 存入
  `verification/T3.2.1/repair-9/diff-check.log`。
- [ ] **AC4 — 禁止路径与作者范围**：
  `git diff --exit-code eeb095ea94cdf0d477bcb09ab7c761883b06f833 <delivery> -- src requirements.txt requirements-dev.txt`
  exit 0。Owner 自 Codex release-sync commit 之后的变更仅限本文件列出的
  `tests/packaging/**`（只处理 F-3(b)）、`verification/T3.2.1/repair-9/**` 与一个新
  Handoff；Codex 预先写入的任务注册/状态同步提交不属于 Owner 变更。提交前记录
  `git diff --name-only <owner-start> <delivery>` 并逐项核对允许路径。Task/STATUS、
  `src/**`、依赖、Schema、`packaging/**`、既有 REPAIR-7/8 产物、任何 Review 报告及
  原始 `verification/T3.2.1/repair-5/reference-check.log` 不得由 Owner 修改。
- [ ] **AC5 — 交接与 Gate**：提交固定 Delivery Head 和新 Handoff；逐条报告 AC、命令、
  结果及证据。随后交 DeepSeek Harness 对固定 base/delivery 独立复审。除非该 Review
  `approved` 且 Codex 已完成 T3.2.1 全量 Release Gate 闭环，否则严禁将 REPAIR-7/9/10
  分支合并到 `master`。

## 允许修改范围

- `tests/packaging/**`：仅修复 F-3(b) 所述、未设置外部 `PYTHONPATH` 时
  `bootstrap` 无法导入的问题；不改动其他测试目标或断言。
- `verification/T3.2.1/repair-9/**`：只新增本 Task 的完整原始测试日志、diff-check
  日志及必要的简短索引；保留现有证据，不覆盖/改写 REPAIR-9 原始构建与 Sandbox 日志。
- `doc/handoffs/T3.2.1-REPAIR-10-<delivery-short-sha>.md`：Owner 新 Handoff。
- 本 Task、`doc/STATUS.md`、计划与总索引由 Codex 维护；Owner 无权修改。

## 禁止范围

- `src/**`、`packaging/**`、`requirements.txt`、`requirements-dev.txt`、其他依赖清单、
  Schema/迁移、业务行为、`doc/STATUS.md`、任何 Task 文件（包括本文件）。
- 所有 REPAIR-7/8 Task、Handoff、verification 产物；任何 DeepSeek Harness 或其他
  Reviewer 报告；原始 `verification/T3.2.1/repair-5/reference-check.log`。
- 覆盖或修订 REPAIR-9 既有 raw logs、Sandbox 记录、manifest 或历史交付 Handoff。
- 合并到 `master` 或宣称 T3.2.1 全量 Release Gate 已通过。

## 测试要求

| 场景/AC | 计划命令或手工步骤 | 前提/环境 | 实际结果 | 证据 |
|---|---|---|---|---|
| F-3(b) / AC1 | PowerShell 中移除当前进程的 `PYTHONPATH` 后，运行 `& 'G:/CODEX/New Manga.task-envs/T3.2.1-packaging-py312/Scripts/python.exe' -m pytest tests/packaging`；预期 `12 passed, 0 skipped, 0 failed, exit 0` | worktree 固定 delivery；Python 3.12.3 / pytest 9.1.1；不设置外部 `PYTHONPATH` | NOT_RUN | `verification/T3.2.1/repair-9/packaging-tests.log` |
| 空白卫生 / AC3 | `git diff --check eeb095ea94cdf0d477bcb09ab7c761883b06f833 <delivery>` | 同一 Git 仓库；固定 base 和 delivery | NOT_RUN | `verification/T3.2.1/repair-9/diff-check.log` |
| 路径保护 / AC4 | `git diff --exit-code eeb095ea94cdf0d477bcb09ab7c761883b06f833 <delivery> -- src requirements.txt requirements-dev.txt`；另核对 Owner 变更文件列表 | 固定 base；区分 Codex release-sync 与 Owner commits | NOT_RUN | Handoff / 完整命令记录 |

## 依赖、风险与阻塞

- 固定恢复点 branch=`agent/antigravity/T3.2.1-repair-9`，worktree=
  `G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-9`，tip/base 为上述
  `eeb095e…`；该分支工作区经 Codex 核验干净。REPAIR-10 使用新分支和新 worktree，
  不在 REPAIR-9 checkout 上续写。
- 新 Owner 分支由 Codex 从固定 base 创建；Codex 的 Task/STATUS/Plan 导航同步提交与
  Owner 实现提交分开。`base_commit` 指实现来源基线；作者范围只审查 Codex 同步提交之后
  的 Owner commits。Full base-to-delivery 命令仍按 AC3/AC4 原样执行。
- T3.2.1 全量 Gate 未闭环：AC3 仍为 `NOT_RUN` 且是发布阻断项；AC5、AC6/AC7 和
  R-014～R-016 继续开放。REPAIR-9 Handoff 中 smoke/packaging 声明不等同于全流程 AC3
  PASS，须由独立 Review 检查且不能替代全量 Gate。
- DeepSeek Harness Reviewer 必须非作者；固定 delivery 后若内容再变化，必须重新复审。

## 交付与运行记录

- Recovery point: `eeb095ea94cdf0d477bcb09ab7c761883b06f833`。
- REPAIR-9 Product Delivery: `d2bf6a8621c0c89c077e181e3e7bec9ac7cf098f`；Handoff/tip:
  `eeb095ea94cdf0d477bcb09ab7c761883b06f833`。
- Owner branch/worktree: `agent/antigravity/T3.2.1-repair-10` /
  `G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-10`（Codex 创建；base 上方
  的注册同步提交由 Codex 所有，不授权 Owner 修改其中 Task/STATUS/Plan 文件）。
- Delivery / Handoff / Review：尚无。
- AC3 remains `NOT_RUN`; AC5=`NOT_RUN`; AC6/AC7=`PARTIAL / NOT_RUN`;
  R-014～R-016 remain open.

## 可直接转发给 Antigravity 的实施指令

请在 `G:/CODEX/New Manga.worktrees/T3.2.1-antigravity-repair-10` 的
`agent/antigravity/T3.2.1-repair-10` 分支实施 **T3.2.1-REPAIR-10**。固定恢复点与
`base_commit` 为 `eeb095ea94cdf0d477bcb09ab7c761883b06f833`，即 REPAIR-9 的
Handoff/tip；不要在 REPAIR-9 worktree 上直接续写。先阅读本 Task、该 base 上的
`doc/handoffs/T3.2.1-REPAIR-9-d2bf6a8.md` 和 `verification/T3.2.1/repair-9/**`。

仅处理 F-3(b)：让 `tests/packaging` 在不设置外部 `PYTHONPATH` 时能导入源码并完整通过。
Owner 可写范围仅为 `tests/packaging/**`（仅此 F-3(b)）、
`verification/T3.2.1/repair-9/**`（新增完整测试原始日志和 diff-check 日志，不覆盖历史
原始证据）及一个 `doc/handoffs/T3.2.1-REPAIR-10-<delivery-short-sha>.md`。不得改
`src/**`、`packaging/**`、依赖、Schema、Task/STATUS 文件、REPAIR-7/8 产物、任何
Review 报告或原始 reference-check.log。不得扩大测试断言或产品范围。

在 PowerShell 明确移除当前会话 `PYTHONPATH` 后，用固定解释器
`G:/CODEX/New Manga.task-envs/T3.2.1-packaging-py312/Scripts/python.exe` 运行
`-m pytest tests/packaging`；目标是 **12 passed, 0 skipped, 0 failed, exit 0**。将
日期、worktree、base、delivery、shell、解释器与 pytest 版本、`PYTHONPATH=UNSET`、精确
命令、完整 stdout/stderr 和 exit code 原样写入
`verification/T3.2.1/repair-9/packaging-tests.log`。逐条记录 skip 原因（目标口径为
0 skipped）。运行 `git diff --check eeb095ea94cdf0d477bcb09ab7c761883b06f833
<delivery>` 并保存完整命令/输出/exit code；运行
`git diff --exit-code eeb095ea94cdf0d477bcb09ab7c761883b06f833 <delivery> -- src
requirements.txt requirements-dev.txt`，须 exit 0。另从 Codex 注册同步提交检查 Owner
变更文件列表只落在允许路径，且不改注册同步文件。提交固定 Delivery Head 与新 Handoff，
逐项报告 AC 和未跑项，然后交 DeepSeek Harness 对固定 base/head 独立复审。Review
`approved` 且 Codex 完成 T3.2.1 全量 Release Gate 闭环前，严禁合并任何相关分支到
`master`。
