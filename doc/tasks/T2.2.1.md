---
id: T2.2.1
title: Reader & Workbench Polish
kind: implementation
status: verified_complete
approval: approved_by_user
suggested_owner: ZCode
owner: ZCode
ui_owner: Qoder
reviewer: Codex
depends_on:
  - T1.1.2
  - T1.2.1
  - T2.1.1
base_commit: 16810c55c28ada960420d8053f47f29b1f71b8f0
branch: agent/zcode/T2.2.1-reader-workbench-polish
worktree: G:/CODEX/New Manga.worktrees/T2.2.1-zcode
integration_commit: fe9fca0
delivery_head: 08465739ae92d7175a77d00053abe598bb150bae
review_commit: 125fff744e914d1df18c77195b160bd06a2f8e38
---

# T2.2.1：Reader & Workbench Polish

## Release Gate

**VERIFIED_COMPLETE.** This Task was released from the integrated baseline
after T1.1.2, T1.2.1, and T2.1.1. ZCode delivered the functional seam,
Qoder delivered the QML/UI implementation, Codex completed non-author review,
and Codex integrated the preserved commit chain at product code head
`fe9fca0`; later governance commits record the evidence pointers.
Evidence: [Qoder Handoff](../../doc/handoffs/T2.2.1-qoder-0723238.md),
[Review](../../verification/T2.2.1/review-0846573.md), and
[Integration](../../verification/T2.2.1/integration-aad510b.md).

The existing production seams are sufficient for the planned picker: the
bootstrap already publishes `bookshelfViewModel` and `navigationViewModel`,
and the bookshelf ViewModel already owns book/chapter models plus the existing
`selectBook`, `enterTranslation`, and `enterReading` slots. A new Python seam
is not pre-authorized; if a concrete test proves one is required, stop that
portion and let Codex record a scope decision before changing it.

The implementation base is `16810c55c28ada960420d8053f47f29b1f71b8f0`.
ZCode's functional branch/worktree is
`agent/zcode/T2.2.1-reader-workbench-polish` /
`G:/CODEX/New Manga.worktrees/T2.2.1-zcode`. Qoder's QML branch/worktree is
`agent/qoder/T2.2.1-reader-workbench-qml` /
`G:/CODEX/New Manga.worktrees/T2.2.1-qoder`; both start at the same base.
ZCode and Qoder work serially when a test file overlaps; neither may merge
`master`.

## 来源与目标

This is Roadmap order 5 in [`REBASELINE_PLAN.md`](../REBASELINE_PLAN.md), after
T2.1.1. The product mapping requires chapter selection in the Reader toolbar
(`doc/05_UI_MAPPING.md` §§37–42), a Workbench context entry that remains an
honest empty state without context (`D05 §62`), and a non-blocking dismissible
error surface (`D05 §54`). The T2.1.1 non-author review deferred the missing
webtoon canvas ground to this Task; the paged and webtoon viewers must share
`Tokens.bgCanvas` without changing the existing tiled reading behavior.

## Acceptance Criteria

- [x] Reader `readerPickChapter` opens an accessible, dismissible chapter picker; selecting a valid book/chapter uses the existing navigation/reader path and updates the displayed chapter without creating a page or bypassing existing state handling.
- [x] Workbench `workbenchPickContext` opens the same or an equivalent picker from the no-context empty state; selecting a valid chapter enters the existing Workbench context. Standalone/no-ViewModel loading remains safe and honest.
- [x] The command-error surface remains bound to `commandErrorText`, is reachable by keyboard/accessibility tooling, and its dismiss action calls `clearCommandError`, hides the surface, and leaves run state unchanged. A mutation test demonstrates the guard is discriminating.
- [x] The production webtoon `Flickable` uses `Tokens.bgCanvas`, matching the paged viewer, while width-fit, natural height, tile serving, and saved scroll offset behavior remain intact.
- [x] No new top-level page, command palette, glass effect, top navigation, Region six-state renderer, theme persistence, storage migration, bootstrap seam, Schema, provider/runtime, or dependency change is introduced.
- [x] Delivery includes implementation tests, fixed author Handoff, reproducible verification evidence, independent Codex Review, and Codex integration evidence.

## 允许修改范围

ZCode may modify only the smallest existing functional/test paths needed after
the seam audit; the expected default is tests only because the current
ViewModels already expose the required slots:

- `tests/ui_shell/**`, `tests/workbench/**`, `tests/reading_export/**` for
  functional regression and QML harness coverage;
- `src/ui/viewmodels/**` only if a concrete failing test proves an existing
  seam cannot express the AC and Codex records the scope decision first;
- `verification/T2.2.1/**` for reproducible logs and author evidence;
- `doc/handoffs/T2.2.1-*.md` for the fixed delivery Handoff.

Qoder may modify only:

- `src/ui/qml/common/**`, `src/ui/qml/reader/**`, and
  `src/ui/qml/workbench/**` for the picker, command-error surface, and
  webtoon canvas ground;
- `tests/ui_shell/**`, `tests/workbench/test_qml_*.py`, and
  `tests/reading_export/test_qml_contract.py` for QML behavior coverage;
- `verification/T2.2.1/**` for QML evidence;
- `doc/handoffs/T2.2.1-*.md` only when assigned by Codex.

Codex alone may update this Task, `doc/STATUS.md`,
`doc/REBASELINE_PLAN.md`, and `doc/00_INDEX.md` for governance, review, and
integration records.

## 禁止范围

- `src/bootstrap/**`, `src/ports/**`, SQLite Schema/migrations,
  repositories/domain/application behavior, provider/runtime/settings
  infrastructure, dependency/lock files, and top-level QML shell redesign.
- `doc/contracts/**`, `doc/design/**`, D01–D08, other Tasks' evidence, or
  another agent's dirty files/worktrees.
- New top-level pages, E command palette, C glass, D top navigation, Region
  six-state renderer, theme persistence, storage migration, packaging, and
  unrelated Reader/Workbench feature work.
- Source files, Managed Copy contents, databases, model caches, `git reset
  --hard`, `git clean -fd`, history rewriting, force push, or merging
  `master` from an author worktree.

## 测试要求

| 场景/AC | 计划命令或步骤 | 前提/环境 | 当前结果 | 证据 |
|---|---|---|---|---|
| Picker/no-context, Reader/webtoon, command-error | `PYTHONPATH=src <venv python> -m pytest tests/ui_shell tests/workbench/test_qml_workbench.py tests/reading_export -q -p no:cacheprovider -rs` | PowerShell, `G:/CODEX/New Manga.task-envs/T1.1.1-impl-py312/Scripts/python.exe`, integrated `fe9fca0` | 278 passed, 0 skipped, EXIT=0 | [`integration-aad510b`](../../verification/T2.2.1/integration-aad510b.md) |
| Compile | `<venv python> -m compileall -q src tests` | Same PowerShell/venv | PASS, EXIT=0 | [`integration-aad510b`](../../verification/T2.2.1/integration-aad510b.md) |
| Bootstrap smoke | `<venv python> -m bootstrap.app --smoke-test --data-root G:/CODEX/New Manga.task-envs/T2.2.1-integration-smoke-20260922` | Fresh isolated data root | PASS, EXIT=0 | [`integration-aad510b`](../../verification/T2.2.1/integration-aad510b.md) |
| Full suite | `PYTHONPATH=src <venv python> -m pytest tests -q -p no:cacheprovider -rs` | Same PowerShell/venv | 1209 passed, 6 OpenSSL skips, 1 known torch probe failure, EXIT=1 | [`integration-aad510b`](../../verification/T2.2.1/integration-aad510b.md) |

Each executed row must record total/pass/fail/skip counts, every skip reason,
shell, interpreter path, and exit code in `verification/T2.2.1/`.

## 依赖、风险与阻塞

T1.1.2, T1.2.1, and T2.1.1 are integrated and verified. The main risks are a
picker that mutates the wrong book selection, a no-context QML null dereference,
an error bar that looks dismissible but does not clear the ViewModel, and a
webtoon fix that changes tile/scroll behavior. Any Python seam, bootstrap,
contract, or persistence requirement is a scope blocker until Codex records a
new decision.

## 交付与运行记录

- Release Gate: [`verification/T2.2.1/release-gate-ef1a9b8.md`](../../verification/T2.2.1/release-gate-ef1a9b8.md)
- Plan: [`docs/superpowers/plans/2026-09-22-t2-2-1-reader-workbench-polish.md`](../../docs/superpowers/plans/2026-09-22-t2-2-1-reader-workbench-polish.md)
- Handoff: [`T2.2.1-qoder-0723238`](../../doc/handoffs/T2.2.1-qoder-0723238.md).
- Review: [`review-0846573`](../../verification/T2.2.1/review-0846573.md), approved.
- Integration: [`integration-aad510b`](../../verification/T2.2.1/integration-aad510b.md), product head `fe9fca0`.

## 下一任务建议

Next Roadmap item: T3.1.1 Unify Storage into SQLite. It is not started by this
Task; Codex must create its own Release Gate and fixed recovery point before
any storage implementation.
