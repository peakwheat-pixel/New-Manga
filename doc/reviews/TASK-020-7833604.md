---
task_id: TASK-020
reviewer: ZCode 子 agent（窗口授权同体审查，结论仅 approved_subagent/changes_requested）
author: ZCode（窗口 W3 Owner）
base_commit: a3c36b1（Task 元数据 base=e96b3eb 之上为窗口授权与 W1/W2 集成提交 `5832894`，写集合与本切片不相交，已核对）
reviewed_head: 7833604
decision: changes_requested
---

# Review：TASK-020（Webtoon 分块处理与阅读）

Review 模板口径：Reviewer 不能是变更作者。本报告为**窗口内子 agent 同体审查**（09_COLLABORATION.md §6 临时条款），结论只能为 `approved_subagent` 或 `changes_requested`，`approved_subagent` 不等同跨 Agent 独立批准；T1 后 Codex + DeepSeek Harness 外部 post-hoc 复审义务不受本结论影响。

## 范围与依据

- **固定对象**：base=`a3c36b1`、reviewed_head=`7833604`（实现）、docs head=`ddcf884`（Handoff/Task/verification，均为白名单文档路径）。分支 `agent/zcode/TASK-020-webtoon-chunked-reading`，worktree `G:/CODEX/New Manga.worktrees/TASK-020-zcode`，实审时 head=`ddcf884`、工作区干净。
- **diff 口径**：`git diff a3c36b1..7833604` 含 W1/W2 集成提交；**本切片红线核对以实现提交 `7833604` 的实际写集合为准**（`git show 7833604 --stat`）：`src/infrastructure/imaging/__init__.py`（新建）、`src/infrastructure/imaging/webtoon_tiles.py`（新建 334 行）、`src/ui/qml/reader/ReaderView.qml`（+49/−4 中的一部分）、`src/ui/viewmodels/reader/viewmodel.py`（+115）、`tests/reading_export/test_webtoon_tiles.py`（新建 313 行），共 5 文件 +808/−4，与 Handoff 声明一致。
- **依据**：doc/tasks/TASK-020.md（AC 4 条 + 允许/禁止范围）、doc/handoffs/TASK-020-7833604.md、09_COLLABORATION.md §6、AGENTS.md、05_UI_MAPPING.md §20.3/§40、03_DATA_MODEL.md §5、06_TRANSLATION_PIPELINE.md §67～70、08_ACCEPTANCE_CRITERIA.md AC-WEBTOON-001～005。
- **未审到的部分**：`ddcf884` 的文档 diff（Handoff/Task/verification）只核对了与实跑证据的一致性，未做文档质量审查；W1/W2 集成提交内容不在本切片审查范围（已确认其写集合与本切片不相交）。

## 隔离要求声明

09_COLLABORATION.md §6 第 6 条：环境不支持并行两个独立 sub-agent（单线程子 agent）。已按该条兜底执行**两遍相互隔离**的检查：第一遍仅以"仓库记录标准 + Fowler baseline"视角逐 hunk 审查（不看 AC 完成度），第二遍仅以"Task AC → D 条文 → STATUS 裁决"来源顺序逐条对照（不复用第一遍结论）。两轴分别成节、不合并、不跨轴排名。此偏差与替代做法在此显式声明。

## Standards（executed）

**记录在案标准逐条核对（硬性项）：无违规。**

1. **AGENTS.md「QML 不直接访问数据库、文件或模型」**：`ReaderView.qml` 改动只消费 ViewModel 属性/槽（`tilesActive`/`tiles`/`requestTiles`/`saveScrollOffset`/`scrollOffsetY`），tile 图片以 ViewModel 生成的 file URI 加载，QML 无任何文件系统/存储访问。hunk：ReaderView.qml 238-274（tilesHost/Repeater/delegate）。
2. **白名单（TASK-020.md 允许修改范围）**：本切片 5 文件全部在 `infrastructure/imaging/**`（明示新建）、`ui/viewmodels/reader/**`、`ui/qml/reader/**`、`tests/reading_export/**` 内；未触碰 bootstrap/app.py、ViewerPanel.qml、其他 workbench QML、Schema、依赖清单、其他 Task（红线核对见「验证」表）。
3. **02 技术架构 §架构方向 / §16（依赖方向 UI → Application → … → Infrastructure）**：`src/ui/viewmodels/reader/viewmodel.py` 生产 import 仅 `time/typing/PySide6.QtCore/application.*/ui.viewmodels.export`，**零 infrastructure import**——tile 能力以 `tile_factory: Callable` 注入，UI 层不认识 infrastructure 类型，方向合法且比 Handoff 表述更干净。守卫 `tests/core/test_architecture.py`（`UI_FORBIDDEN={"infrastructure"}` 扫描 `src/ui` 全树）+ `tests/providers/test_ports_contract.py` 实跑 14 passed。
4. **D05 §40（按宽适配；禁止按固定高度强制缩放整个超长图）**：tile delegate 高度 `modelData.height * tilesHost.width / modelData.pageWidth` 为纯等比换算（ReaderView.qml 249-251），`contentHeight` 为 tile 几何总高，无固定高度压缩。§40 的 `last_page_id`/`scroll_offset_y` 保存复用既有 ReadingService 逻辑，未动。
5. **D05 §60（业务规则不进 QML/ViewModel）**：`_rebuild_tiles`/`requestTiles`/`clearTileCache` 只做材料化与 URL 填充，几何来自 `infrastructure.imaging`（TileGrid），进度/Region/修订仍全部在 ReadingService——「ViewModel 无业务规则」边界保持（存在 R-001 的行为缺陷，但缺陷属材料化调度而非业务规则下沉）。
6. **协作协议 §6 临时条款 3（不得新增 skip/xfail）**：本切片 diff 零新增；`a3c36b1..7833604` 中 grep 命中的 `pytest.importorskip`/`skip_reason` 均属 W2 已集成交付 `tests/providers/test_full_chain.py`（TASK-033，非本切片写集合）。

**Fowler smell baseline（判断项，仓库未记录处；每条为判断项非硬性违规）**：

- **possible Speculative Generality**（webtoon_tiles.py 256-260 + 178-181）：`TiledPageRasterizer.tile_file` 向 `TileCache` put 文件路径成本，但生产路径从不 `get`（文件定位走 `_file_keys` 与磁盘存在性）；`_trim` 淘汰 entry 既不删 PNG 也不阻止 `tile_file` 命中磁盘——TileCache 在 rasterizer 语境中退化为装饰性记账（见 R-002）。
- **possible Speculative Generality**（viewmodel.py 323-337）：`pagePixelWidth`/`pagePixelHeight` 属性当前无任何 QML 消费者（QML 用每行 `modelData.pageWidth`），属未接线的公共面（并入 R-005）。
- **possible dead conditional**（ReaderView.qml 244）：Repeater `model: visible ? model.tiles : []` 的 unqualified `visible` 解析为 Repeater 自身属性、恒为 true，条件分支冗余且误导（行为无害：`tilesActive=false` 时 `model.tiles==[]` 兜底；见 R-005）。
- formatter/linter：仓库无 pyproject/ruff/mypy 等工具配置，工具强制项不存在，跳过。

**本轴小结**：硬性违规 0 项；baseline smell 3 处（R-002/R-005 及 pagePixel 未消费），最严重为 R-002（P3）。

## Spec（executed）

**来源顺序**：TASK-020.md AC → D03 §5 / D05 §20.3、§40 / D06 §67～70 / D08 AC-WEBTOON-001～005 → STATUS 用户裁决。

① **来源要求但缺失/部分完成的项**：

- **「按需解码」部分完成（R-001，P1）**。Task AC 第 2 条原文："长图按宽适配、**按需解码**、限制预取/内存"。Infrastructure 层 `ensure_viewport`（webtoon_tiles.py 265-274）按视口+有界预取正确按需（实证：`ensure_viewport(0,900)` 后仅 3/4 PNG 落盘）；但 `requestTiles`（viewmodel.py 356-362）对**每个未填 URL 的 row 无条件调用 `tile_file`**（359 行）再判断 `wanted_keys`——同场景实测 **4/4 tile 全部解码落盘**，即打开任意长 webtoon 页的首次滚动事件会同步解码整页全部 tile。URL 材料化按需、解码不按需：AC 原文语义被绕过，且 Handoff「解码峰值为单 tile 解码窗」仅在内存维度成立、总解码量维度失真。显示正确性与内存预算未破坏，故 P1 而非 P0。
- 其余 AC 交付项核对均为完成：一 Page 原则（tile 不进 Page 表/Progress，D06 §67 三个"不"全满足）；坐标回映射+边界去重（`test_region_boxes_survive_tile_boundaries_deduplicated` 从所属 tile 0 与 1 各回映射一次、集合去重为单元素，真实覆盖 AC 第 3 条语义）；清缓存不动真值（`test_tiled_reader_serves_viewport_tiles` 断言 clear 后 `scroll_offset_y==1234.0`、`last_page_id=="p0"`）；重启恢复（`test_tiled_reader_restores_saved_scroll_offset` 两会话 + QML 几何立即恢复）；按宽适配（等比换算）。
- **BLOCKED 如实登记，未记 PASS**：`test_oversize_page_geometry_and_decoder_limit_record` 保持"几何 PASS（50 tiles 恰好划分/回映射/视口）+ 像素解码 `pytest.raises(OSError, match="tile decode failed")` 表征断言"结构；`oversize-record.json` 记录 BLOCKED 口径与解锁条件（流式 PNG 依赖需批准 / Qt 上游修复）。符合窗口红线"不得把 BLOCKED 记为通过"。1600×200000 fixture 授权验证在像素维度 BLOCKED 的登记与探针数字（40000px=256MB ok、50000px=305MB fail，rgb32 换算自洽）与 Handoff 一致。

② **来源未要求的行为（scope creep）**：未发现。`pagePixelWidth/Height` 未消费属性与 QML 冗余条件算 Standards 轴 smell（R-005），非行为越界。

③ **看似实现但实现有误的项**：R-001（上述）；另 R-004（P3）：`_cache_key`（webtoon_tiles.py 290-296）为 sha256(path|size|几何|index)，**不含内容 hash/mtime**——Handoff「键=源 hash+几何+序号」「源变更自动失效」的表述强于实现（同路径同尺寸内容替换时 tile 缓存 stale）。Managed Copy 下源文件替换通常伴随路径/Revision 变化，风险低，登记不改判。

**本轴小结**：1 项部分完成（R-001，P1），1 项表述偏差（R-004，P3），最严重为 R-001。

## Architecture（executed）

- **Qt import 延迟成立**：webtoon_tiles.py 模块级仅 hashlib/os/uuid/collections/dataclass/pathlib/typing；`QImageReader`/`QRect` 均在 `_probe_size`（222-229）、`_decode`（276-288）、`_qt_rect`（315-318）方法内 import——几何/缓存半区无 Qt 可单测（测试 1-7 无 Qt 通过，实证一致）。
- **依赖方向**：见 Standards 第 3 条，UI 层零 infrastructure import，factory 注入合法；守卫实跑 14 passed / exit 0。
- **分层与契约**：tile 缓存目录 `cache/webtoon-tiles/<uuid>` 属可重建数据，不进 Page 表/ArtifactRevision（D06 §67）；`clear()` 只删 `tile-*.png` 与内存账（webtoon_tiles.py 302-312），业务真值零接触（有测试）。未注入 `tile_factory` 时整图路径逐字节不变：QML 旧 Image 分支保留（`visible: !tilesHost.visible`、R-003 onStatusChanged 恢复逻辑原样），TASK-037 契约 `test_reader_webtoon_swaps_in_vertical_viewer` 单跑 1 passed、在 82 例套件中通过。
- **遗留（作者已如实登记，非本切片违规）**：bootstrap 生产装配点在白名单外未注入（`grep tile_factory src/bootstrap/app.py` 无命中）——生产 webtoon 当前走原整图路径，行为不变，待后续切片。

**本轴小结**：0 项违规；1 项已登记遗留（装配点，移交 Codex）；最严重项为无（P0/P1 归零）。

## Verification（executed）

作者证据与实跑一致性：`verification/TASK-020/full-suite-runs.log` 5×「764 passed、EXIT=0」与本次实跑完全一致；oversize 探针数字与 Handoff/test docstring/record.json 三处同口径。explains 764 = 754（W2 后基线）+ 10（本切片新测试）。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-001 | P1 | src/ui/viewmodels/reader/viewmodel.py:356-362（核心 359） | `requestTiles` 循环对每个未填 URL 的 row 无条件调用 `tile_file(index)` 后才判断 `wanted_keys`：首次滚动事件在 UI 线程同步解码**整页全部 tile** 并写盘，违背 Task AC 第 2 条「按需解码」；长页（可解码范围内）秒级 UI 冻结 + 磁盘写放大；Handoff「URL 只为可见窗口材料化」在解码维度失真。`test_tiled_reader_serves_viewport_tiles` 仅断言 URL 填充，无法暴露该行为 | 探针脚本（review 现场）：400×3000 页 4 tiles，仅请求视口 [0,900)：`ensure_viewport(0,900)` 后 3/4 PNG；复刻 requestTiles 循环后 `tile_file` 触发解码 [0,1,2,3]、目录 4/4 PNG | 将 wanted 判定前置：先取视口+预取的 index 集合，仅当 `row["index"]` 属于该集合时调用 `tile_file`；补一条"视口外 tile 不解码"的断言（如 `tile-N.png` 文件数） | open |
| R-002 | P3 | src/infrastructure/imaging/webtoon_tiles.py:256-260、178-181 | `TileCache` 在 rasterizer 中 put 后生产路径从不 get；`_trim` 淘汰 entry 不删 PNG、不阻止磁盘命中重建——三重缓存（内存 LRU/`_file_keys`/磁盘）并存其一装饰化 | possible Speculative Generality（Fowler baseline 判断项）；行为无害 | 二选一：让 `tile_file` 优先经 `self._cache.get` 定位，或 rasterizer 只保留磁盘+`_file_keys`、TileCache 留给纯 LRU 语义（当前测试 6/7 已覆盖） | deferred |
| R-003 | P3 | src/infrastructure/imaging/webtoon_tiles.py:321-325 | `default_cache_root()` 每次调用生成 uuid4 随机子目录且无清理机制：孤儿 tile 缓存目录随会话累积（磁盘泄漏），同页跨会话不复用重建成果 | 代码审读；bootstrap 未注入 factory，生产当前不触发 | 装配切片中改为确定性目录（如按源 hash）+ 会话/容量清理策略 | deferred |
| R-004 | P3 | src/infrastructure/imaging/webtoon_tiles.py:290-296 | `_cache_key` 无内容 hash/mtime，同路径同尺寸内容替换时 tile 缓存 stale；Handoff「键=源 hash…源变更自动失效」表述强于实现 | 代码审读；Managed Copy 下风险低 | 键中补 `st_mtime_ns` 或在 Handoff 措辞上收敛为「路径+大小+几何」 | deferred |
| R-005 | P3 | src/ui/qml/reader/ReaderView.qml:244；src/ui/viewmodels/reader/viewmodel.py:330、337 | Repeater `model: visible ? model.tiles : []` 的 unqualified `visible` 恒为 true（解析 Repeater 自身属性），条件冗余误导；`pagePixelWidth/Height` 无 QML 消费者 | possible dead conditional / possible Speculative Generality（baseline 判断项） | 绑定改 `tilesHost.visible` 或删条件；未消费属性移除或等装配切片接线 | deferred |

## 验证

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| 红线：写集合 | `git show 7833604 --stat`；`git diff a3c36b1..ddcf884 -- tests/reading_export/test_{qml_contract,viewmodels,reading,export}.py`（空输出） | worktree ddcf884 | PASS（5 文件全在白名单；既有 4 测试文件全区间零改动；零新增 skip/xfail——grep 命中属 W2 交付 tests/providers/test_full_chain.py） | 本报告「范围与依据」 |
| 红线：BLOCKED 口径 | 审阅 test_oversize_page_geometry_and_decoder_limit_record 结构（`pytest.raises(OSError)` + record.json） | head 7833604 | PASS（几何 PASS + 解码 BLOCKED 表征断言，无假通过） | tests/reading_export/test_webtoon_tiles.py:263-313 |
| 红线：旧路径回归 | `pytest tests/reading_export/test_qml_contract.py::test_reader_webtoon_swaps_in_vertical_viewer -q` | Python 3.12 task-env / ddcf884 | PASS（1 passed，exit 0） | 实跑输出 |
| 切片套件 | `python -m pytest tests/reading_export -q -p no:cacheprovider -rf` | 同上 | PASS：**82 passed / 0 skipped，exit 0** | 实跑输出（5.59s） |
| 全仓回归 | `python -m pytest -q -p no:cacheprovider -rs -rf`（Git Bash，openssl 可用） | 同上 | PASS：**764 passed / 0 skipped，exit 0**（6 条 TLS 真实执行，与作者 5×764 一致） | 实跑输出（28.98s）；verification/TASK-020/full-suite-runs.log |
| 分层守卫 | `python -m pytest tests/providers/test_ports_contract.py tests/core/test_architecture.py -q -p no:cacheprovider` | 同上 | PASS：14 passed / 0 skipped，exit 0（UI→infrastructure 禁令实测覆盖 src/ui 全树） | 实跑输出 |
| R-001 实证 | 探针脚本：`ensure_viewport(0,900)` vs 复刻 requestTiles 循环 | 同上 | ensure_viewport=3/4 PNG（按需正确）；requestTiles 循环=4/4 PNG（全量解码） | 本报告 Findings R-001 |
| oversize fixture 像素解码 | 套件内 `test_oversize_page_geometry_and_decoder_limit_record` | 同上 | BLOCKED（Qt PNG handler ≳300MB rgb32，几何 PASS；表征断言通过，未记 PASS） | 套件 82 passed 含本例 |

## 结论与复审

**decision = `changes_requested`**（仅 R-001 一项 P1 阻断项；R-002～R-005 均 P3 可 deferred）。

- **理由**：四轴中 Standards/Architecture/Verification 无阻断发现，写集合、断言保护、BLOCKED 登记与全量测试证据全部属实且优秀；但 Spec 轴 R-001（P1）实证 `requestTiles` 绕过 infrastructure 的按需解码机制、全页 tile 全量同步解码，直接触及 Task AC 第 2 条「按需解码」原文并带 UI 冻结风险。按 §6 第 8 条「P0/P1 未解决不能批准」，固定 head `7833604` 暂不可交 Codex 集成。
- **修复路径（作者后续切片内完成，仍在白名单内）**：viewmodel.py `requestTiles` 将 wanted index 集合判定前置（约 3 行改动），并在 `test_webtoon_tiles.py` 补一条「视口外 tile 不产生解码产物」断言；修订后按协议复审追加 disposition，不抹掉本报告记录。
- **剩余风险**：① 超大页像素解码 BLOCKED 依赖解锁（依赖变更需用户批准）；② 生产装配点 bootstrap 注入待后续切片（当前生产行为不变）；③ R-002/R-003 的缓存机制装饰性与孤儿目录问题在装配切片前潜伏不触发。
- 本结论为窗口内同体审查 `changes_requested`，不取代 T1 后 Codex + DeepSeek Harness 的强制外部 post-hoc 复审义务。
