---
task_id: TASK-012
reviewer: DeepSeek Harness
author: ZCode
base_commit: 2cceb1e734c5079870662b7a28da316e46444810
reviewed_head: ca5848b746210564a2503e8c5f59e0a2118e56a1
decision: approved
---

# Review：TASK-012 四页导航与书架 UI（ca5848b）

Reviewer 非本变更作者。被审内容固定于 `reviewed_head = ca5848b`（代码 + 视觉基线 + 验证证据）；`delivery_head = e6fe52a` 是 `ca5848b` 的子提交，仅含 Handoff 文档，不属被审范围。Review 期间只读访问 ZCode worktree，未修改任何生产代码或测试。

**接收方式**：`G:/CODEX/New Manga.worktrees/TASK-012-deepseek-review`，分支 `agent/deepseek/TASK-012-review`（基于 `ca5848b` 新建）。

## 范围与依据

**变更**：`git diff 2cceb1e ca5848b` = 38 文件、`+2721/−23`；`git diff --check 2cceb1e ca5848b --` **退出码 0**。内容为 `src/application/navigation/**`、`src/ui/{viewmodels,models,qml}/**`、`tests/ui_shell/**`、`doc/ui-baseline.md`、`doc/tasks/TASK-012.md`、`verification/TASK-012/**`，外加项目级状态文档（见 R-002）。

**依据**：D05 §2/§3.1/§7/§8/§9/§43.1/§60/§62；D08 `AC-NAV-001..003`、`AC-LIB-001`、`AC-CH-001..004`；Task 文件 `doc/tasks/TASK-012.md`；Handoff 与 author-verification。

**环境**：Windows `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`QT_QPA_PLATFORM` **未设置**，即默认 Windows 平台（按 Handoff 要求，QML 测试不可用 offscreen）。

## 四项重点核查（Handoff 指定）

| 重点 | 结论 |
|---|---|
| ① QML 无业务逻辑（D05 §60） | **PASS**。`AppShell.qml` 仅含布局与 `visible: navigationViewModel.currentPage === "<page>"` 绑定；四页常驻、只切 `visible`（符合 §3.1 与 AC-NAV-003 的"运行中任务不被切换打断"）。全 QML 目录检索未发现算术/循环计算/IO；所有事件处理器均为**单行转发**到 ViewModel（如 `shelfList.bookSelected(bookId)`、`shelf.selectBook(bookId)`）。决策集中在 `NavigationViewModel`/`BookshelfViewModel`/`NavigationService` |
| ② 导入只读源文件 | **PASS**。`viewmodel.py:348` `importFilesFromUrls(chapter_id, urls)` → `:356` `data_provider=lambda path=Path(url.toLocalFile()): path.read_bytes()`。`data_provider` 为**延迟调用**且只执行 `read_bytes()`，源码路径中无写入/移动/删除源文件的调用；与 TASK-007 的"源文件只读"契约一致 |
| ③ 空状态诚实性（D05 §62 / §7.1） | **PASS**。`BookCard.qml:14` `property string progress: "—"`；`BookDetailPanel.qml:55` `text: "进度：—"  // 阅读进度字段尚未落地（TASK-007），诚实显示`。占位符为破折号而非编造的 0%/章节名，且**在代码注释中注明原因**。`EmptyState.qml` 仅提供 title/description/actions 三个属性与布局，无逻辑。测试 `test_shelf_and_detail_empty_states`、`test_workbench_and_reader_are_honest_skeletons` 固化该行为 |
| ④ scope-change request 处理建议 | **建议批准，但作为独立的最小装配切片实施**（详见文末专节） |

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-001 | P2 | `doc/handoffs/TASK-012-ca5848b.md:54`；`verification/TASK-012/author-verification.md:23` | 两处把全仓回归记为 **296 passed**，实测为 **290 passed + 6 skipped**（差值恰为 6，即 skipped 被计入 passed）。6 项 skip 均为既有用例、与本 Task 无关 | 复跑 `…TASK-012-py312/python.exe -m pytest tests -q` → `290 passed, 6 skipped`，退出码 0；与 `verification/TASK-012/pytest-full-20260915.txt` 对照 | 更正为 `290 passed, 6 skipped` 并注明 skip 原因。**注意：这是本会话内第三次同一模式**（TASK-009 F-01、TASK-010 F-01、本次），建议 Codex 在协作协议或证据模板中统一规定"通过数与 skip 数必须分列" | open（非阻塞） |
| R-002 | P2 | `src/ui/viewmodels/__init__.py`（不在白名单）；`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/STATUS.md`、`doc/tasks/README.md`（均不在 Task 白名单） | 5 个文件落在 TASK-012 `allowed_paths` 之外。其中 `__init__.py` 是子包必需的模块说明文件；4 个项目级文档的改动内容为**登记 TASK-012 与 TASK-010 的释放/实施状态**（含"本次例外使用独立 worktree/branch"的如实说明），属项目状态维护的常规动作 | `git diff --name-only 2cceb1e ca5848b` 与 Task 白名单逐条比对；4 个文档的实际 diff 内容已逐行核对 | 内容恰当，无需回退。建议 Codex 二选一：①在 Task 模板/协议中**显式授权**"状态类文档（`00_INDEX`/`12_ROADMAP`/`STATUS`/`tasks/README`）属所有 Task 的默认允许路径"；②或在每个 Task 白名单中逐项列出。另建议白名单用 `src/ui/viewmodels/**` 覆盖包初始化文件 | open（非阻塞） |

**在记录范围内未发现 P0/P1 问题。** 四项重点核查均为 PASS；无越界生产代码改动，无 Schema/共享契约变更。

## 验证

| 场景 | 命令或手工步骤 | 环境 / commit | 结果 | 证据 |
|---|---|---|---|---|
| 必查 whitespace | `git diff --check 2cceb1e ca5848b --` | Git 2.52.0 | **PASS，退出码 0** | 无输出 |
| 任务命令 | `…TASK-012-py312/python.exe -m pytest tests/ui_shell -v` | Python 3.12.3 / PySide6 6.11.2，**默认 Windows Qt 平台** | **PASS，46 passed，退出码 0**（与要求一致） | 本次复跑输出；对照 `verification/TASK-012/pytest-ui_shell-verbose-20260915.txt` |
| 全仓回归 | `…TASK-012-py312/python.exe -m pytest tests -q` | 同上 | **290 passed, 6 skipped，0 failed，退出码 0** | 数字口径差异见 R-001 |
| 架构边界 | `git grep -E '^\s*(import\|from)\s+(infrastructure\|sqlite3)' ca5848b -- src/ui` | `ca5848b` | **PASS**：`src/ui` 未导入 `infrastructure`/`sqlite3`（仅导入 PySide6，属 UI 层允许） | 无命中 |
| 分层纯净 | `git grep -E '^\s*(import\|from)\s+(PySide6\|shiboken6\|sqlite3\|infrastructure)' ca5848b -- src/application/navigation` | 同上 | **PASS**：0 命中 | 无命中 |
| 范围 | `git diff --name-status 2cceb1e ca5848b` | 同上 | 生产代码全部在白名单内；5 个白名单外文件见 R-002 | 逐条比对 |
| 空状态 | 阅读 `BookCard.qml` / `BookDetailPanel.qml` / `EmptyState.qml` + 两项测试 | 同上 | **PASS**（诚实占位） | 见重点③ |
| 导入只读 | 阅读 `viewmodel.py:348,356` | 同上 | **PASS**（`read_bytes()` 延迟读） | 见重点② |
| QML 装载与行为 | `tests/ui_shell/test_qml_shell.py`（11 项） | 同上 | **PASS** | 复跑输出 |
| **应用入口启动 / 100%·150%·200% DPI 截图初验** | 未执行 | — | **NOT_RUN（BLOCKED）** | 应用装配（`Main.qml`/`bootstrap`）属已登记的范围变更请求，未实施；当前无法从入口启动完整应用 |
| 键盘 Tab/方向键全量矩阵 | 未执行 | — | **NOT_RUN** | Rail 首按钮 focus 已设；全量矩阵归 TASK-022 |
| 导入 FileDialog 原生交互 | 未执行 | — | **NOT_RUN** | 原生对话框无法无头驱动；QML→`importFilesFromUrls` 调用契约已由 python 侧覆盖 |
| 工作台/阅读器/设置真实内容 | 未执行 | — | **N/A** | 本切片仅 §62 空状态骨架；TASK-013/015/022 交付 |

## 三轴结论

**Spec**：`AC-NAV-001`（启动默认书架，服务层默认 + rail checked 绑定，双层测试）、`AC-NAV-002`（只有四个一级入口，`navigate()` 拒绝第五 Route + AppShell 恰四页四钮）、`AC-NAV-003`（切换保持上下文，四页常驻仅切 `visible`，往返后模型行数不变）、`AC-LIB-001` 与 `AC-CH-001..004`（CRUD 全部经 TASK-007 的 `LibraryService` 用例，QML Dialog 端到端）均有实现与测试对应；D05 §7/§8 的导入入口以 python seam 交付。`AC-LIB-002/003`（标签）按 D05 §8 归 TASK-022，Handoff 标 N/A 是准确的范围判断，不是遗漏。D05 §60（QML 无业务逻辑）、§62（空状态）、§7.1（不虚构阅读进度）三项专项要求均满足且被测试固化。

**Architecture**：分层方向正确——`NavigationService` 位于 application 层且**零 Qt/零 infrastructure 依赖**（已核验 0 命中）；`src/ui` 的 ViewModel/Model 只依赖 PySide6 与 application 用例，未触碰 `infrastructure`；QML 只做绑定与转发。QML 侧对 PySide6 6.11 的平台约束（`QQmlComponent.status` 为方法、Repeater delegate 不在 QObject 树、Loader item 为 JS ownership、QML 不能调用非 slot 的 python 方法、delegate 绑定需预转 ISO 字符串）已写入 `test_qml_shell.py` docstring 与 QML 注释，属**如实记录的平台事实**而非缺陷。唯一架构级瑕疵是 R-002 的 5 个白名单外文件（内容恰当，属授权口径问题）。

**Verification**：任务命令 `pytest tests/ui_shell -v` 在指定任务环境、默认 Windows Qt 平台下 **46 passed**，与要求完全一致；全仓回归 **0 failed**；架构边界与分层纯净度经独立 `git grep` 核验通过；三项重点（QML 无逻辑、导入只读、空状态诚实）由代码阅读 + 既有测试双重确认。**应用入口启动与 DPI 截图初验为 NOT_RUN（BLOCKED）**，原因已由作者如实登记为范围变更请求，未以推测代替。R-001 的通过数口径偏差属证据表述问题，不改变"0 failed"的事实。

## Scope-change request 处理建议

**请求内容**（Task 文件内已登记，本切片未实施）：`src/ui/qml/Main.qml` 改为加载 `shell/AppShell.qml`（约 3 行）；`src/bootstrap/app.py` 构造服务与 ViewModel 并 `setContextProperty`。

**建议：批准，但不要就地实施，而是作为一个独立的最小装配切片执行。** 理由与条件：

1. **必要性成立**：不裁决则该切片交付的完整 Shell **只能经测试装载、用户不可见**，"四页导航与书架 UI"的产品价值无法被观察或人工验收。这是当前唯一 BLOCKED 项（DPI 初验也因它而阻塞）。
2. **改动边界很小**：两个文件、无 Schema、无共享契约、无依赖变更；Task 文件已给出实施范围与行数量级，且承诺"不裁决不实施"，符合协议。
3. **必须补齐的授权要素**：批准时应在新的（或本 Task 的）范围变更记录中明确——**具体路径**（`src/ui/qml/Main.qml`、`src/bootstrap/app.py`）、**owner 与 reviewer**、**绑定 base commit**、以及**验收门槛**："能从入口启动完整应用" + 100%/150%/200% DPI 截图初验，从而使当前两项 BLOCKED 转为可验证。
4. **需 Codex 先行确认的前提**：装配链是否已就绪。`bookshelfViewModel` 依赖 `LibraryService`/`ImportImagesUseCase`（需要 `ImageDecoder`、`ManagedCopyStore`、`ImportPageSink` 的具体 adapter）与 `NavigationService`；SQLite 侧适配器已随 TASK-029 集成，但 Qt 图像解码与 Managed Copy 的生产装配是否已在主线存在，需 Codex 核实。**若依赖未齐，先装配会产生"能启动但功能不可用"的假可见性**，反而不如维持当前诚实骨架。
5. **不建议**把该装配并入 TASK-012 的既有 reviewed head：那会改变已被本报告批准的内容边界，使本次批准的适用范围失效；应作为后续提交与独立验收。

## 结论与复审

**`ca5848b` 可交 Codex 集成：decision = approved。**

- 无 P0/P1；四项重点核查全部 PASS；任务命令 46 passed 与要求一致；全仓回归 0 failed。
- R-001（通过数口径）与 R-002（5 个白名单外文件）均为 P2，内容不影响正确性，可在集成前后的文本提交中关闭，不需新的 reviewed head。
- **集成时须由 Codex 同时处置范围变更请求**（见上节），否则 TASK-012 交付物对用户不可见；该裁决不应被延后到任务 done 之后。

**剩余风险**：应用装配未完成 → 入口启动与 DPI 初验 `NOT_RUN/BLOCKED`；阅读进度依赖 TASK-007 后续字段（当前诚实显示"—"）；工作台/阅读器/设置的真实内容属 TASK-013/015/022；标签编辑入口按 D05 §8 属 TASK-022；键盘全量矩阵与原生文件对话框交互未验证。本报告不释放任何冻结任务（含 TASK-013/TASK-015），未修改任何生产代码。本报告事实仅适用于 `ca5848b`；分支后续变化（含 `e6fe52a`）不延用本批准。
