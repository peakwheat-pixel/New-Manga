---
task_id: TASK-023
reviewer: ZCode 子 agent（窗口授权同体审查；结论仅 approved_subagent/changes_requested，T1 后须 Codex + DeepSeek Harness 外部 post-hoc 复审）
author: ZCode（窗口插队项 Owner，与 Reviewer 同为窗口 ZCode 主体——按 STATUS「ZCode 全权窗口授权（2026-09-17）」§Review 定性执行，明确不等同跨 Agent 独立批准）
base_commit: cca8b09（Task 元数据 base=e96b3eb 之上为窗口授权与 W1-W3 集成提交，写集合与本切片不相交）
reviewed_head: 7fa9118（实现）；docs heads=8e713fa（Handoff/Task）+52445f4（全仓证据）
decision: approved_subagent
---

# Review：TASK-023（PDF/MOBI 导入路线）

固定 base=`cca8b09`、reviewed_head=`7fa9118`。实现 diff `git diff cca8b09..7fa9118`：8 文件，+605/-1（`requirements.txt` +1、`src/application/importing/documents/` 新子包 3 文件 +281、`src/infrastructure/importing.py` +94 纯追加、`tests/import_formats/` 新 2 文件 +227、`doc/tasks/TASK-023.md` 状态行——属 Task 白名单明示允许且 §3.5 允许 Owner 更新）。文档 diff `7fa9118..52445f4` 仅 Handoff/Task/verification 证据。Reviewer 实际读完整 diff、复用基线 `application/importing/images/{service,ports}.py`、`domain/pages/entities.py`、`infrastructure/importing.py` 全文、D01 §2 / D02 §8 §16 / D04 §8、STATUS U-1/U-2 与窗口条款，并实跑测试。

**两轴隔离声明（§6 第 6 条偏差声明）**：本 Review 在单线程子 agent 内执行，无法并行两个独立 sub-agent。替代做法：Standards 轴与 Spec 轴做**两遍相互隔离**的检查（第一遍仅对照仓库记录标准逐条审 diff，不读 AC；第二遍仅对照 Task AC 与 D 条文逐条审行为语义，不复用第一遍结论），未用一遍通读充当两轴；两轴分别成节、分别小结、不跨轴排名。

## 红线核对（逐条，全部 PASS）

1. **实现 diff 触碰面**：仅 `requirements.txt`（仅新增 `pypdfium2==5.13.0` 一行，`git diff cca8b09..7fa9118 -- requirements.txt` 实测）、`src/application/importing/documents/**`（新）、`src/infrastructure/importing.py`（`grep -c '^-[^-]'` = 0，纯追加，无既有行修改）、`tests/import_formats/**`（新）、`doc/tasks/TASK-023.md`（白名单明示允许）。✔
2. **bootstrap/Schema/AGENTS/其他 Task**：`git diff cca8b09..7fa9118 --stat --` 上述路径**零触碰**。✔
3. **无新增 skip/xfail**：`tests/import_formats/` 中 "skip" 命中均为 `DuplicatePolicy.SKIP` 业务语义与 `skipped_duplicates` 字段，非 pytest 标记；`-rs` 显示 0 skipped。✔
4. **AC 口径未抬高**：Task AC 1/2 勾选但描述明确"MOBI 光栅化 BLOCKED…未记 PASS"；AC 3（交付/Review/集成）未勾选；实现中 MOBI 记 `UNSUPPORTED_FORMAT`+BLOCKED detail，无任何"MOBI 已支持"表述。✔
5. **依赖变更注意事项**：`pip show pypdfium2` = **5.13.0**；requirements 仅此一行新增，无顺带升级/新增；Handoff 登记安装前后全仓各 764 passed/0 skipped exit 0；readiness 断言零命中无需更新（本 Review grep 复核：除新代码外 `pypdfium2|pdfium` 在 src/tests 零命中）。✔

## Spec（executed）

**隔离检查第一遍（仅对照 AC 与 D 条文，不使用 Standards 结论）**，逐条：

- **AC 1**（"复用 Managed Copy/Page 用例；PDF：pypdfium2 光栅化＋ImportDocumentsUseCase 复用 images 纪律；MOBI BLOCKED——typed UNSUPPORTED_FORMAT，不自行发明解析器，未记 PASS"）：`ImportDocumentsUseCase` 直接复用 images 的 `ManagedCopyStore`/`ImportPageSink`/`ImportReport`/`FailedImport`/`ImportedPage`/`ImportSource`（`service.py` L34-43 import 自 `application.importing.images`）与 `DuplicatePolicy`（同一对象，非副本）；光栅经 `DocumentRaster` 端口绑定 `PdfiumDocumentRaster`（pypdfium2）。MOBI：`_is_pdf` magic 检查后非 PDF 一律 `UNSUPPORTED_FORMAT`，detail 明示 "MOBI rasterisation is BLOCKED, TASK-023"（测试断言 `"MOBI" in detail`）；全代码无任何 MOBI 解析逻辑。✔
- **AC 2**（"页级 hash 去重＋order 顺延；INVALID_DOCUMENT/ENCRYPTED/UNSUPPORTED_FORMAT/COPY_FAILED 分列；取消保留已提交页并报告剩余；先导数据在后续失败时原样"）：去重按 `sha256(rendered.png)` 页级（SKIP 跳页、整文档全跳过计 `skipped_duplicates`、IMPORT_AS_NEW 重导，测试 `test_duplicate_pdf_skips_and_import_as_new_reimports` 覆盖三种路径且断言 raster hash 稳定）；`source_order`/`sort_order` 自 `max_source_order` 顺延（3 页 PDF 断言 [1,2,3]）；四类 reason 分列（L50-54）；取消逐页检查（每文件前+每页前），已提交页保留、剩余报 `pending_after_cancel`（测试：4 页文档提交 2 页后取消，`len(imported)==2`、`len(sink.pages)==2`、pending 非空）；损坏 PDF 导入失败后先导 2 页原样（`test_corrupt_pdf_fails_diagnosably_and_keeps_imported_data`）。✔
- **AC 3**：Handoff 已交付（`doc/handoffs/TASK-023-7fa9118.md`），本 Review 即其待办项，集成后 done——口径一致，无抬高。✔
- **D02 §8**（"导入器验证文件类型、可读性、重复项和页序；原始资源复制到项目受控目录；数据库只保存引用/哈希/尺寸/顺序"）：类型=magic+`UNSUPPORTED_FORMAT`；可读性=pdfium 打开/逐页渲染错误→typed `INVALID_DOCUMENT`/`ENCRYPTED`；重复项=页 hash；页序=文档页序→`source_order`。✔ **D04 §8**（保存"原始文件名/原始导入顺序/当前显示顺序/文件 Hash/图片宽高/Managed Copy 引用；用户原始文件不被修改"）：全部落位（`source_filename` 存原始文件名、页名仅作 Managed Copy 显示名；宽度/高度断言 1224×1584）。"文件 Hash"在文档场景按 AC 2 批准口径落为**页级** hash（PNG 渲染结果），文档文件级 hash 未持久化——见 R-004，属 AC 明示范围，非违规。✔ **D01 §2**：PDF/MOBI 导入解析为补充扩展能力，本切片在 U-2 批准的 PDFium 范围内实现 PDF，MOBI BLOCKED 如实登记。✔
- **来源未要求的行为（scope creep）**：无。渲染 scale=2.0 为 Handoff 已声明的实现默认非契约（D07 若有 DPI 契约后续显式绑定），`notes.txt` 走同一 `UNSUPPORTED_FORMAT` 路径属同一类型验证语义，非新增范围。
- **看似实现但有误**：未发现。Managed Copy 严格先于 commit（`store_original`→`add_page`，真实 `ManagedCopyStoreAdapter` 逐字节 temp+`verify_temp` hash 校验+uuid revision 路径，IMPORT_AS_NEW 同名页不冲突）；失败页永不进 sink。

**Spec 轴小结**：0 个 P0/P1/P2 发现；2 个 P3（R-004 及 R-003 的 Spec 侧覆盖观察）。AC 1/2 有实跑证据，MOBI 口径未抬高。

## Standards（executed）

**隔离检查第二遍（仅对照仓库记录标准逐条，不使用 Spec 结论）**：

- **AGENTS.md**：QML 不直接访问数据库/文件/模型——本切片零 QML/零 UI；数据保护——`PdfiumDocumentRaster.open(data)`/`render_page` 只消费 bytes 并向 Managed Copy 写入副本，**无任何对源文件/源的写路径**（测试 `test_pdf_import_materialises_one_managed_page_per_document_page` 断言 managed 副本 hash 与页 hash 一致，D04 §8"用户原始文件不被修改"由只读 bytes 入参结构保证）。✔
- **09_COLLABORATION**：结果 passed/skipped 分列+退出码（Handoff 与本报告均如此）；无 flaky/BLOCKED 记为通过；测试素材自制（`minimal_pdf` 手工构造多页 PDF，无许可顾虑，Handoff 已声明）。✔
- **02 §8/§16**：Managed Copy 五步纪律全部满足；§16 未列 `importing/` 系文档滞后，既有 `application/importing/images/` 已确立该结构且 Task 白名单（Codex 已按实际结构收紧）明示允许 `src/application/importing/**` 新增子包。✔
- **Task 允许/禁止范围**：见红线核对第 1、2 条，零越界。✔
- **Fowler baseline 判断项（仓库未记录标准处）**：
  - R-001 **possible Dead Code**：`service.py` L108-109 `document_hash = hashlib.sha256(data).hexdigest()` 后立即 `del document_hash`——计算后即弃，对大 PDF 是纯 O(n) 开销，`del` 语句迫使读者揣测意图（注释已解释但代码形态自相矛盾）。
  - R-002 **possible Duplicated Code（测试 double 低保真）**：`tests/import_formats/test_document_import.py` L67-74 的 `InMemoryPageSink` 不过滤 `chapter_id`，与既有 `tests/library/helpers.py` L85-96 同名 double（按 `page.chapter_id == chapter_id` 过滤）行为不一致；多章节测试场景下会跨章节误判重复。
- 工具已强制项（formatter/linter）跳过；仓库记录标准优先于 baseline，本轴无硬性违规。

**Standards 轴小结**：0 个 P0/P1/P2 发现；2 个 P3（R-001、R-002，均为判断项非硬性违规）。

## Architecture（executed）

- 依赖方向：`application/importing/documents/**` 实际 import 仅 stdlib + `application.importing.{documents,images}.*` + `domain.pages.entities`——grep 验证**零 infrastructure import**；`QML/UI → Application → Domain/Ports → Infrastructure Adapters` 无反向。✔
- 端口/适配器：`DocumentRaster`/`DocumentHandle` Protocol 在 application 层，`PdfiumDocumentRaster`/`_PdfiumDocumentHandle` 在 infrastructure 层，生产装配留待 bootstrap（白名单外，Handoff 已移交 Codex，与 TASK-020 `tile_factory` 同情形）。✔
- **pypdfium2 仅被 infrastructure import**：grep 验证实际 `import pypdfium2` 仅 `src/infrastructure/importing.py` L126（application/tests 命中均为 docstring 文字）；Qt（QImage/QBuffer）同样仅出现在 infrastructure（L139-141），application 层零 PySide6 import（`application/export/pdf_qt.py` 的 PySide6 为既有代码，不在本 diff）。✔
- 安全/持久化：无 Schema 变更、无网络、无 AI 依赖边界涉入；错误一律 typed 上浮，不静默吞异常（`close()` 的 `except Exception: pass` 有 `# pragma: no cover` 注释且语义为"关闭不得掩盖主流程异常"，可接受）。✔

**Architecture 轴小结**：0 个发现。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-001 | P3 | `src/application/importing/documents/service.py` L108-109 | 计算 `document_hash` 后立即 `del`——大 PDF 多一次 O(n) sha256 纯开销，`del` 形态令读者困惑 | `grep -n document_hash` 两行连排；行为无影响（测试全过） | 删除这两行，保留注释意图于模块 docstring | open（可随后续切片顺手清理） |
| R-002 | P3 | `tests/import_formats/test_document_import.py` L67-74（对照 `tests/library/helpers.py` L85-96） | sink double 不过滤 `chapter_id`，与既有 helpers 版行为不一致；未来多章节用例会跨章节误判重复/顺序 | 两处 `existing_source_hashes` 实现对比 | 对齐既有按 `chapter_id` 过滤的实现 | open |
| R-003 | P3 | `src/infrastructure/importing.py` L129-136 + `tests/import_formats/test_document_import.py` L195-215 | `ENCRYPTED` 的 pdfium 真实映射（error message 含 "password" 判断）未经真实加密 PDF 验证；现有加密测试为单元级假 raster（测试 docstring 已如实标注 "Unit-level"） | 全套 6 例无真实加密 PDF 素材（pypdfium2 不提供加密写出，补测需外部素材+许可留证） | 后续切片以真实加密样本补一条集成断言；当前 HANDOFF 声明与实际一致，不构成虚假证据 | open（deferred 候选） |
| R-004 | P3 | `src/application/importing/documents/service.py` L155-168（COPY_FAILED/页失败 break 路径） | 文档中途失败（非取消）时剩余页不进 `pending_after_cancel`（该字段仅报告取消），用户仅从 `failed` 一条得知文件失败；与 images 用例逐文件 continue 语义不同，信息完整性略低 | 失败 break 后无 pending 记录（对照 images `service.py` L94-96 continue）；AC 2"取消和失败不破坏已导入数据"仍满足 | 若后续需要与取消对齐，可将失败文档剩余页一并报 pending；当前为可接受语义 | open |

R-001~R-004 全部为 P3，不阻塞批准。没有 P0/P1/P2 发现；无模板占位行。

## 验证

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| 切片+library 回归 | `PYTHONDONTWRITEBYTECODE=1 python -m pytest tests/import_formats tests/library -q -p no:cacheprovider -rs -rf`（解释器 `G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`，worktree） | `52445f4`（含 7fa9118） | **46 passed / 0 skipped**，exit 0 | 本 Review 实跑输出 |
| 全仓 | `PYTHONDONTWRITEBYTECODE=1 python -m pytest -q -p no:cacheprovider -rs -rf` | 同上 | **770 passed / 0 skipped**，exit 0（Git Bash 口径 openssl 可用，6 条 TLS 真实执行；与 PowerShell 口径 764/6 差异仅此，与预期一致） | 本 Review 实跑输出 |
| 依赖版本 | `python -m pip show pypdfium2` | 共享 venv TASK-012-py312 | **5.13.0**，无其他包变动迹象 | pip 输出 |
| requirements 变更面 | `git diff cca8b09..7fa9118 -- requirements.txt` | `cca8b09..7fa9118` | 仅 +`pypdfium2==5.13.0` 一行 | git diff 输出 |
| 红线路径 | `git diff cca8b09..7fa9118 --stat`（bootstrap/Schema/AGENTS 等路径过滤）、`grep -c '^-[^-]'`（infrastructure 纯追加）、`grep skip/xfail` | 同上 | 全部符合红线（见上文逐条） | git/grep 输出 |
| 依赖方向 grep | `grep -rn pypdfium2\|pdfium src/ tests/`、`grep -rn PySide6 src/application/` | 同上 | pypdfium2 实际 import 仅 infrastructure；documents 子包零 infrastructure/Qt import | grep 输出 |
| 作者证据复核 | `verification/TASK-023/full-suite-runs.log`（5×770 passed/exit 0）、Handoff 依赖前后 764/764 对照 | `52445f4` | 与本 Review 实跑一致（770/0/0）；依赖留证完整满足 Task 注意事项 ②③ | `verification/TASK-023/full-suite-runs.log` L1-71 |
| `minimal_pdf` 合法性 | `test_pdf_import_materialises_one_managed_page_per_document_page`（3 页、1224×1584、hash=managed 副本逐字节）经真实 pypdfium2+真实 ManagedCopyStoreAdapter | `7fa9118` | PASS（生成器产出合法多页 PDF，测试通过即证） | 实跑 |
| 取消/失败/加密诊断断言真实性 | 阅读测试 L154-215 与 L111-126 | `7fa9118` | 断言真实存在且执行（cancelled 回调基于已提交页数确定触发；损坏 PDF→INVALID_DOCUMENT 且先导 2 页原样；MOBI/txt→UNSUPPORTED_FORMAT+detail 含 MOBI） | 阅读记录 |

四轴状态：Standards=executed、Spec=executed、Architecture=executed、Verification=executed。

## 结论与复审

**decision = `approved_subagent`**。理由：红线五项全部 PASS；AC 1/2 逐条有实跑证据且 MOBI 口径未抬高（BLOCKED/UNSUPPORTED，未记 PASS）；复用而非第二管线成立（同一组端口与 DuplicatePolicy 对象，去重/order/先拷贝后提交/取消语义与 images 用例一致）；架构方向零违规；全仓 770 passed/0 skipped exit 0 与作者证据吻合；全部 findings 为 P3，不阻塞。

剩余风险：①生产装配点在白名单外（`ImportDocumentsUseCase` 未接线 bootstrap/入口 UI，当前生产行为不变——已由 Handoff 移交 Codex）；②MOBI 光栅化 BLOCKED，解锁=用户批准 MOBI 解析依赖；③render scale=2.0 为实现默认，D07 DPI 契约若存在需后续显式绑定；④R-003 真实加密 PDF 集成断言缺失（单元级已覆盖 use case 映射路径）。

本结论按窗口条款**不等同**跨 Agent 独立批准；T1=2026-09-18 08:50 后须由 Codex + DeepSeek Harness 完成外部 post-hoc 复审，可推翻本结论及窗口内 `done`。复审追加新 head 与 findings disposition 时不得抹掉本记录；集成后的测试由 Codex 另行记录。
