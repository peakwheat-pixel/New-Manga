---
task_id: TASK-010
reviewer: DeepSeek Harness
author: ZCode
base_commit: 2cceb1e734c5079870662b7a28da316e46444810
reviewed_head: cd76d30fdc561eb8f22a849eeb5989473957dc5b
decision: approved
---

# Review：TASK-010 翻译约束、TM 与 Context（cd76d30）

被审范围固定 `2cceb1e..cd76d30`。其后 `ba98c32`（author-verification + Task 状态）与 `0f5ab2d`（Handoff）经核对为**纯文档提交，无源码变化**，不属被审内容。Review 未修改 TASK-010 分支、`master` 或任何白名单外生产代码；报告写于独立 review 分支。

**接收方式**：`G:/CODEX/New Manga.worktrees/TASK-010-deepseek-review`，分支 `agent/deepseek/TASK-010-review`（基于 `cd76d30` 新建）。ZCode worktree 仅作只读读取。

## 范围与依据

**固定对象**：`base_commit = 2cceb1e…`（release；实现基线为 `2bdfd6f`）、`reviewed_head = cd76d30…`。提交链：`cda5ab8`（认领）→ `0df119d`（约束实体/合并/候选）→ `26ec405`（TM 服务）→ `1998e96`（Context/SFX/Translate Step）→ **`cd76d30`**（TM 枚举正常化）。

**变更**：23 文件、`+2916/−2`（源码 12 + 测试 10 + Task 1）。`git diff --check 2cceb1e cd76d30 --` **退出码 0**。白名单过滤：**越界 0**（全部落在 `src/domain/constraints/**`、`src/application/translation/{knowledge,context}/**`、`tests/knowledge/**`、`doc/tasks/TASK-010.md`）。

**依据**：D03 §12~14、D06 §9~18/§85、D08 AC-SFX/CONSTRAINT/TM/TRANS、TASK-002 契约 §2/§3/§4/§7.2。

## 六项设计取舍核验

| # | 取舍 | 规格依据 | Reviewer 核验 |
|---|---|---|---|
| 1 | Fuzzy 阈值默认 `0.80`（可配置），`difflib.SequenceMatcher`，不引入 Embedding/RAG | D03 §14.4 / D06 §12 只要求「Exact + Fuzzy」、**未给数值** | **成立**。`tm.py:118` `DEFAULT_FUZZY_THRESHOLD = 0.80`，`:255` 允许调用方覆盖；`:297` 用标准库 `difflib`。无新增依赖（与 author-verification 的"仅标准库"一致）。缺规格数值 → 见 F-02 |
| 2 | 自动候选高置信阈值 `AUTO_ACTIVE_CONFIDENCE = 0.90`（可配置） | D03 §12.5 只定状态流转（高置信→active、低置信→pending），**未给数值** | **成立**。`candidates.py:26` 定义常量，`:93`/`:128` 用于判定；与 §12.5 的两态分流一致。缺规格数值 → 见 F-02 |
| 3 | TM 记录模型与 Store 协议置于 `application` 层（白名单无 `domain/tm`） | TASK-010 白名单无 domain TM 路径；沿用消费侧 port 惯例 | **成立且边界诚实**。`TranslationMemoryEntry`（`tm.py:43`）、`TranslationMemoryStore`(Protocol, `:87`)、`TranslationMemoryService`（`:112`）均在 application；SQLite 适配器明确留待后续持久化 Task，未越界实现 |
| 4 | Token 预算 `1 token ≈ 1 字符`，estimator 可注入 | 规格未定预算单位 | **成立**。`builder.py:155-156` 注释明确"first shipped budget unit… CJK-friendly, deterministic, dependency-free. Callers may inject a real tokenizer"，`:163` `estimator or _default_estimator` 支持注入 |
| 5 | Webtoon chunk 间文本重叠未实现（D06 §17 为"允许"非必须） | D06 §17 为许可性表述 | **成立**。`builder.py:284-285` 明确"window over Region reading_order, cut into budget-sized chunks. **Tiles are an image-processing concern and never appear here**"；`:297-309` 仅按预算切块、无重叠逻辑。与"Webtoon 以 Region 窗口而非 Tile 为上下文"的任务附加要求一致 |
| 6 | TranslateStep 不含 Planner 的 Lock/skip_lock 决策与真实 Provider 调用 | D06 §18 输入输出；TASK-002 §4 输出映射 | **成立**。`step.py:159` 以 `provider_call: Callable[...]` 注入调用；`:96`/`:145`/`:150` 三处 `OutputMappingError` 覆盖"越界输出/缺失输出"（TASK-002 §4 语义）；无 Planner 决策代码 |

**六项取舍全部与获批规格一致或属规格明示的许可范围，并已在 Handoff/author-verification 中如实声明。**

## 规格一致性核验

| 项 | 结论 |
|---|---|
| D03 §12.1 作用域层级 | **一致**。`ConstraintScope` = global/book/chapter；`merge.py:21-25` `_SCOPE_PRIORITY` 为 CHAPTER(3) > BOOK(2) > GLOBAL(1) |
| D03 §12.4 `normalized_key` | **一致**。`entities.py:23-29` `normalize_term` = NFKC → strip → casefold → 空白折叠；被 TM 复用为 Exact key |
| D03 §12.5 自动术语规则 | **一致**。`candidates.py` 按置信度分流 active/pending；rejected 键保留以免重复提议（AC-CONSTRAINT-004） |
| D03 §12.6 人工优先 | **一致**。`merge.py:45-50` `_origin_rank` = locked(3) > manual(2) > auto(1)；与 §12.6 的「人工锁定 > 人工未锁定 > 自动高置信度 > 自动待确认」逐级对应；`merge.py:85` 只取 `ACTIVE`，故 pending/rejected/disabled 永不成为正式约束（D06 §11） |
| D03 §13 / TASK-002 §2.1 | **一致**。`ConstraintRevision` 为 frozen 且带 `snapshot`/`change_reason`/`is_pinned`/`source_run_id`；`merge_constraints` 首行调 `require_current_revision_id()`，拒绝未 attach 的约束（对应"创建事务提交时 current 必须非空"） |
| D06 确定性 | **一致**。`merge.py:61-68` `_winner_key = (updated_at, constraint_id)`，同层同源时给出确定性 tie-break，合并结果与输入顺序无关 |
| D06 §85 SFX Policy Gate | **一致**。`gate.py:16-17` `SKIP_POLICY="skip_policy"`、`SFX_SKIP_REASON="sfx_skip"`；`:32` `decide_sfx_translation(region_type, sfx_policy)` 对 skip/manual 返回 skip_policy、translate 正常 |
| TASK-002 §3.2 冻结快照 | **一致**。`snapshot.py:13,69` 以 `MappingProxyType` 包装术语表；`EffectiveConstraintSnapshot` 提供 `lookup`/`terminology`/`do_not_translate`/`provenance`；测试覆盖"Run 中改术语不影响已冻结值" |
| Context Scope ≠ Write Scope | **一致**。`builder.py` 的 bundle 携带上下文范围，写范围单独界定；测试 `test_write_scope_only_contains_target_regions` |
| AC 覆盖（13 主责） | **可对应**。等测试证据见 author-verification 的 AC 表（constraint 4 项 + TM 4 项 + SFX/TRANS 若干），`tests/knowledge` 78 项经 Reviewer 复跑通过 |

## 三轴结论

**Spec**：13 个主责 AC 均有对应实现与测试；D03 §12.1/§12.4/§12.5/§12.6/§13、D06 §11/§17/§18/§85、TASK-002 §2.1/§3.2/§4 的关键语义逐条核对一致。六项设计取舍中，1、2 属"规格未给数值"的合理实现选择（已声明、可配置），3～6 属白名单或规格许可范围内的边界收缩（均已声明）。未发现范围扩张或未声明的语义自创。

**Architecture**：新增 `domain/constraints`（纯领域，无 sqlite3/PySide6）与 `application/translation/{knowledge,context}`（消费侧 port + 用例），分层方向正确；TM 的 Store 以 Protocol 定义在 application、持久化留给基础设施切片，边界诚实；`TranslateStep` 以注入的 `provider_call` 隔离真实 Provider，本切片不含 AI 调用与 Planner 决策。`git diff` 显示未触碰 `src/domain` 其他包、Schema、共享接口、依赖清单或 AGENTS。

**Verification**：`tests/knowledge` **78 passed**（与作者一致）；`tests/core/test_architecture.py` **4 passed**；全量 **322 passed, 6 skipped, 0 failed**（退出码 0，连续两次一致）；`tests/rendering` 单独 **56 passed**；`git diff --check` 退出码 0；白名单越界 0。唯数字口径偏差见 F-01。

## Findings

| ID | 级别 | 文件/行 | 问题 | 影响 | 复现 | 建议 | disposition |
|---|---|---|---|---|---|---|---|
| F-01 | P2 | `doc/handoffs/TASK-010-cd76d30.md:42`；`verification/TASK-010/author-verification.md:25` | 两处把全量记为 **328 passed**，但实测为 **322 passed + 6 skipped**（差值恰为 6，即 skipped 被计入 passed）。6 项 skip 与本 Task 无关，属环境限制 | 数字口径与项目"未实际执行的检查必须标记 NOT_RUN/BLOCKED/N/A、不得写 PASS"不符；后续引用会误判通过范围。无 failed，实现本身无缺陷 | 复跑 `PYTHONPATH=src python -m pytest tests -q`（322 passed, 6 skipped）；对照 Handoff/author-verification 的 328 | 更正为 `322 passed, 6 skipped`，并注明 6 项 skip 的原因 | open（非阻塞） |
| F-02 | P2 | `src/application/translation/knowledge/tm.py:118`（`DEFAULT_FUZZY_THRESHOLD = 0.80`）；`src/domain/constraints/candidates.py:26`（`AUTO_ACTIVE_CONFIDENCE = 0.90`） | 两个默认数值在获批规格（D03 §12.5/§14.4、D06 §12）中均无数值依据，仅由实现选定；虽可配置且已在 Handoff 声明，但文档侧没有记录该默认值及其可调范围 | 后续 Agent 无法从规格判断"0.80/0.90 是否为获批值"，也难以评估调参影响；属长期可维护性隐患而非行为缺陷（阈值可注入，测试用显式值） | 阅读两个常量定义；对照 D03 §12.5 与 §14.4 的原文（只有状态与匹配方式，无数值） | 由 Codex 在 D03 §12.5/§14.4 或 D06 §12 补登"默认阈值与可调范围"，或明确标注为"实现可调参数、非产品承诺"；不要求本 Task 改码 | open（非阻塞） |

P0 = 0，P1 = 0，P2 = 2（F-01、F-02），均不阻塞集成。

## 单独分析：`tests/rendering` 顺序依赖缺陷（按指令不计入本 Task 结论）

**背景**（引自 Handoff 与 author-verification）：base `2bdfd6f` 干净树全量曾现 `2 failed, 251 passed`，失败为 `tests/rendering/test_source_style.py::TestPixelAnalyzer` 两例；单独跑 `tests/rendering` 全过；作者判定为 pre-existing 顺序依赖并移交 Codex。

**Reviewer 独立尝试（7 种运行方式）**：

| # | 命令 | 结果 |
|---|---|---|
| A | `pytest tests/rendering -q` | 56 passed |
| B | `pytest tests/knowledge tests/rendering -q` | 134 passed |
| C | `pytest tests/rendering tests/knowledge -q` | 134 passed |
| D | `pytest tests -q`（默认顺序） | 322 passed, 6 skipped，**0 failed** |
| E | `pytest tests -q -p no:randomly` | 322 passed, 6 skipped，**0 failed** |
| F | `pytest tests/rendering/test_source_style.py::TestPixelAnalyzer -q` | 5 passed |
| G | `pytest tests -q`（第二遍） | 322 passed, 6 skipped，**0 failed** |

**分析结论：在本 Reviewer 环境（Python 3.12.3、pytest 9.1.1、pwsh、同一仓库）下该缺陷未能复现**——包含默认全量顺序、禁用随机化、模块前后互换与单类重跑，均无失败。因此**无法独立确认其触发条件**。可能的环境差异（作者使用 `task-envs/TASK-005-py312` 与 Git Bash；Reviewer 使用独立 venv 与 pwsh）尚未被证明是原因。**建议由 Codex 要求原报告者提供最小复现（精确命令、完整参数、收集顺序与失败断言输出），或改派独立的 Bug 分析任务**；在复现条件确认前，不建议据该现象修改 `tests/rendering` 或 `tests/knowledge`。按指令，此项**不作为 TASK-010 的 Review 结论依据**。

## 未执行项

| 项 | 状态 | 说明 |
|---|---|---|
| TM / 约束的 SQLite 持久化适配器 | **NOT_RUN（范围外）** | 白名单无基础设施路径；属后续持久化 Task |
| 真实 Provider 端到端翻译 | **NOT_RUN（范围外）** | 依赖后续 Provider 适配 Task；本切片仅编排与映射守卫 |
| Webtoon chunk 重叠策略 | **N/A（规格许可）** | D06 §17 为"允许"；已声明留待 Provider 适配层 |
| 模型 / 视觉 / 性能指标 | **N/A** | 本 Task 无模型推理、无渲染像素、无性能 AC；未以 Mock 冒充 |
| 6 项 skip（全量中的既有用例） | **NOT_RUN** | 与本 Task 路径无关；未计入通过数（见 F-01） |

## 结论与下一步

**`cd76d30` 可交 Codex 集成：decision = approved。**

- 六项设计取舍全部与获批规格一致或落在其明示许可范围内，且均已在 Handoff/author-verification 声明；13 个主责 AC 有实现与测试对应。
- 无 P0/P1；F-01（通过数口径）与 F-02（两个阈值缺规格登记）均为 P2，可在集成前后的文本提交中关闭，不需新的 reviewed head。
- 本批准覆盖 `2cceb1e..cd76d30`；未触碰共享文件与 Schema，回退为整分支 revert。

**集成时须由 Codex 完成**：核对 `cd76d30` 为当前 head、按协议 §6.6 串行集成并记录 `integration_commit`；（a）更正 F-01 的通过数口径；（b）处置 F-02（补登阈值或标注为可调参数）；（c）把本轮 decision 与 disposition 回填 `doc/STATUS.md`、`doc/tasks/TASK-010.md` 与 Handoff；（d）就 rendering 顺序依赖缺陷要求最小复现或另立分析任务。

**剩余风险**：持久化与真实 Provider 两条链路完全未验证（`tests/knowledge` 为纯确定性单元测试）；两个阈值尚无规格来源；`TranslationConstraint._revision_count` 是内存计数器，对象重建后归零，`revision_no` 的单调性依赖后续持久化实现在事务内重新校验（该责任已在 `ConstraintRevision` docstring 中声明，未列为 finding）。**本报告不释放任何冻结任务，未修改 TASK-010 分支、master 或白名单外生产代码。** 本报告事实仅适用于 `cd76d30`。
