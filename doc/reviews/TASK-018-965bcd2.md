---
task_id: TASK-018
reviewer: Codex
author: DeepSeek Harness
base_commit: 6f130ade18c6c83a42a40a16e7620af518f55e33
reviewed_head: 965bcd2308c367d3428f564d68edd68c73ac8545
decision: approved
---

# Review：TASK-018 尾项切片（R-104 关闭）

本文件是 TASK-018 第四轮（尾项切片）的独立 Review 记录。Reviewer=Codex（非作者），作者=DeepSeek Harness。前三轮记录见 [`doc/reviews/TASK-018-6c33e7f.md`](TASK-018-6c33e7f.md) 与 [`doc/reviews/TASK-018-5063315.md`](TASK-018-5063315.md)。

## 固定对象

| 项目 | 值 |
|---|---|
| base commit | `6f130ade18c6c83a42a40a16e7620af518f55e33`（master；`git merge master` 进入分支为 fast-forward） |
| 被审 head（delivery） | `965bcd2308c367d3428f564d68edd68c73ac8545` |
| 元数据 head | `c8c2a8e`（仅新增 Handoff） |
| 前次集成基线（仍有效） | `14b92e4`（首轮 `4d189ce` 亦有效） |
| decision | `approved` |

## 范围

变更 5 个文件，全部在允许范围内：`experiments/TASK-018/run_experiment.py`（+`assert_implementations_registered()`，导入期调用）、`experiments/TASK-018/test_route_gating.py`（+`StartupAssertionTests` 3 例）、`doc/research/TASK-018.md`（§9.3）、`verification/TASK-018/experiment-log.md`（§7.4）、`doc/tasks/TASK-018.md`（状态）。

`experiments/TASK-018/results/experiment.json`、`experiments/TASK-018/test_mask_protocol.py`、`doc/STATUS.md`、`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/tasks/README.md`、生产 `src/`、`tests/`、Schema、依赖清单、`AGENTS.md` **命中 0**；`git diff --check 6f130ad 965bcd2` 退出码 0。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-201 | P3 | `doc/tasks/TASK-018.md:93,98` | 只有 frontmatter 与优先级声明被改为 `in_review`；自称"唯一"的状态块仍写"`done`…本 Task 无未决 Review"，且仍把 R-104 剩余部分记为 **deferred**（与 §9.3/§7.4 的 fixed 冲突）。读者可能取到过期状态 | 全文检索：第 93 行 `当前状态（唯一）` 仍为 `done`；第 98 行 `R-104 剩余部分 … deferred` | 同步该状态块（或交由集成收口重写） | **closed in integration**（集成收口将状态块重写为尾项切片完成后的最终状态与 `integration_commit`；R-103 同类问题，非本次代码缺陷，无需作者返工） |

其余为观察项，非缺陷：守卫是单向的（只查"`implementation` 非 None 但未在 `FILLERS` 注册"，不查多余的 `FILLERS` 条目或 `implementation=None` 却已注册），与 Reviewer 原建议一致，且这两种情形都不会产生伪造成果；`run()` 不做运行期复检（仅导入期），当前仅测试会动态改 `ROUTES`，无实际影响。

## 验证（本 Reviewer 实际执行）

| 场景 | 命令或步骤 | 结果 |
|---|---|---|
| 协议自检 | `python -m unittest experiments/TASK-018/test_mask_protocol.py` | **12 passed / 0 skipped**，退出码 0；该文件未被改动 |
| 门控与探测 | `python -m unittest experiments/TASK-018/test_route_gating.py` | **16 passed / 0 skipped**，退出码 0（13 → 16，**通过数未减少**） |
| **反例（CLI 版，独立于作者脚本）** | 临时副本删除 `FILLERS` 的 `edge-bleed` 条目（route 仍声明 `implementation`），执行 `run_experiment.py --repeat 1 --output-dir <temp>` | **退出码 1**；`RuntimeError: route/FILLERS misconfiguration: edge-bleed declare an implementation but have no registered filler; refusing to start`；**写出 PNG = 0**；输出目录**未被创建**——确认为"启动即失败"，无中途 `KeyError` 窗口 |
| 守卫无误报 | 真实 harness `--repeat 1 --route simple-fill --output-dir <temp>` | 退出码 0；5 MEASURED |
| 确定性 | 整目录副本 `--repeat 3` vs 仓库已提交 `results/experiment.json` | 10 MEASURED + 20 BLOCKED；`routes`/`manifest_sha256`/`mask_sha256`/`output_sha256`/`sample_sha256`/`parameters`/`mask`/`protected_box_violations`/`blocked_stage`/`requirement_probes` **0 差异** |
| 数据未被覆盖 | `git diff 6f130ad HEAD -- experiments/TASK-018/results/`；`git status` | 空；工作区干净（JSON 工作区为 CRLF、blob 为 LF，`git ls-files --eol` = `i/lf w/crlf`，归一化后逐字节相同） |

## 结论

**`approved`**：固定 `reviewed_head=965bcd2` 可交 Codex 集成。R-104 的启动期一致性断言已按原建议实施，反例在 CLI 与导入两条路径上均经本 Reviewer 独立复现，且不误报；未改动任何已集成 measured 数据。唯一 P3（R-201）为任务文件状态块未同步，已定于集成收口内关闭。

剩余风险：学习型路线质量/性能仍 `BLOCKED`、Mask 内部结构损伤与真实 OOM/漫画样例仍 `NOT_RUN`；本项目仍无生产 Router（属 TASK-019，未释放）。本结论仅对该固定 head 有效。
