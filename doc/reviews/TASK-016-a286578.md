---
task_id: TASK-016
reviewer: Codex
author: DeepSeek Harness
base_commit: f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86
reviewed_head: a286578ad951fc0f3c391a08e724a1c0ecb10059
decision: changes_requested
---

# Review：TASK-016 OCR 与检测路线独立实验

本报告只审查 `f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86...a286578ad951fc0f3c391a08e724a1c0ecb10059` 的实现提交，并核对用户指定的元数据提交 `dfd11b9cf3264ef254074043e8f2de5c3f3cdd22`。`dfd11b9` 提供的 Handoff 已纳入审查；交付 worktree 在 `dfd11b9` 后的未提交改动不在本次范围内。

## 范围与依据

- 工作路径：`G:/CODEX/New Manga.worktrees/TASK-016-deepseek`
- Owner：DeepSeek Harness；Reviewer：Codex
- 必读：`doc/handoffs/TASK-016-a286578.md`、`verification/TASK-016/experiment-log.md`、`doc/research/TASK-016.md`
- 依据：`doc/tasks/TASK-016.md` Acceptance Criteria、`doc/09_COLLABORATION.md` 状态生命周期、用户提供的 TASK-016 Review 指令
- `git diff --check`：通过；固定实现 diff 仅涉及 TASK-016 白名单路径，未修改生产 `src/`、`tests/`、Schema/migration、依赖清单或 `AGENTS.md`。

## Standards

### 硬性标准

- **S-001 P2 — Task 元数据与交付记录不一致**：`dfd11b9` 中 `doc/tasks/TASK-016.md:67-69` 仍写 `Handoff：尚无`、`Review：尚无`、`实际执行/实验/测试：尚无`，但同一文件已是 `status: in_review`，且 `doc/handoffs/TASK-016-a286578.md` 与 `verification/TASK-016/experiment-log.md` 已存在。该记录违反 `doc/09_COLLABORATION.md §4` 对 Task 交付链和状态记录一致性的要求，影响审计可追溯性。建议把三项改为实际文件链接/结果，保留“待 Review、待集成”的真实状态。

### 判断性 smell

- **Repeated Switches（低严重度）**：`experiments/TASK-016/run_experiment.py:324-339` 在 probe-only 和执行分支重复按 provider 名称级联；当前实验规模尚小，不构成阻塞。若候选路线继续增加，再改为 provider→probe/runner 函数表。
- **Divergent Change（较弱）**：`run_experiment.py` 同时承载候选目录、Windows 资源探测、三个 Provider runner、结果协议和 CLI；当前仍在本 Task 最小范围内，不要求拆分。

## Spec

| ID | 级别 | 文件/行 | 触发与影响 | 建议 | 状态 |
|---|---|---|---|---|---|
| R-001 | P1 | `doc/tasks/TASK-016.md:31`；`experiments/TASK-016/run_experiment.py:31-56,368-372` | AC 明确要求“检测器作为 OCR 前置共同验证”。实现矩阵只有 `manga-ocr`、`paddleocr-korean`、`openai-compatible-vision` 三个 OCR/Vision 候选；DBNet/CTD/YOLO 只出现在研究表，未被探测、执行或纳入 15 条结果矩阵。因此检测器前置验证仅停留在文档层，AC1/AC2 不完整。 | 在本 Task 增加检测器候选的 probe/失败记录与协议验证，或经 Codex/用户明确将检测器实验拆为后续切片并把本 AC 标为 partial；不得把当前文档列表当作已验证。 | open |
| R-002 | P2 | `experiments/TASK-016/protocol.py:55-61`；`experiments/TASK-016/test_protocol.py:43-47` | `fallback_status` 对任意非空 `configured_routes` 返回 `FALLBACK_CONFIGURED`，测试只覆盖空集合和一个任意非空字符串，不能证明 route 名称有效或实际调用了已配置路线。失败路径当前仍为 BLOCKED，尚未造成生产风险。 | 校验配置项属于已知候选/已注册路由，或至少补充非法 route 负例并明确 `configured_routes` 已由上层完成白名单校验。 | open |
| R-003 | P2 | `experiments/TASK-016/run_experiment.py:34-52`；`verification/TASK-016/experiment-log.md:59-63` 对照 `doc/research/TASK-016.md:5,41-44` | 研究文档正确声明版本/许可均待核实，但结果候选对象和日志表写入具体版本与代码许可文本。当前可理解为“候选元数据”而非实测或合规结论，但若脱离研究文档阅读容易被误认作已确认事实。 | 保留候选值也可以，但应在输出字段/日志表头直接标注 `unverified_candidate_metadata`，并继续禁止据此修改依赖或作合规决策。 | open |
| R-004 | P2 | `experiments/TASK-016/run_experiment.py:127`；`verification/TASK-016/experiment-log.md:46-47` | Review 指令要求模型侧字段保持 `NOT_RUN`/`None`，但 `model_sha256` 使用 `NOT_AVAILABLE`。这对“未加载权重”的事实表达是诚实的，且不是质量/性能指标；若按字段字面执行该 Review 门槛，仍存在哨兵值不一致。 | 明确 `model_sha256` 属于环境元数据并允许 `NOT_AVAILABLE`，或在未运行结果中统一为 `NOT_RUN`；不要用样本 Hash 代替模型 Hash。 | open |

## Verification

环境：Windows；固定 Review 快照为 `dfd11b9`（包含 `a286578` 实现与 `dfd11b9` Handoff 元数据）；Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`）；`PYTHONDONTWRITEBYTECODE=1`；实验不加载 Qt，不设置 offscreen。

| 检查 | 命令 | passed | skipped | 结果 |
|---|---|---:|---:|---|
| 协议自检 | `$env:PYTHONPATH='experiments/TASK-016'; python -m unittest experiments/TASK-016/test_protocol.py -v` | 4 | 0 | PASS，退出码 0 |
| probe-only | `python experiments/TASK-016/run_experiment.py --manifest experiments/TASK-016/samples/manifest.json --output <tmp>.json` | N/A | N/A | PASS，退出码 0，15 条全部 `BLOCKED`；未联网、未下载 |
| 模型字段核验 | 检查 probe 结果记录 | N/A | N/A | PASS；`recognition_errors=NOT_RUN`，质量/延迟/RAM/VRAM 字段均未产生实测值；`model_sha256=NOT_AVAILABLE` 为未加载权重的环境元数据哨兵，见 R-004 |
| 范围核验 | `git diff --name-only f9edd688...a286578`、`git diff --check f9edd688...a286578` | N/A | N/A | PASS；实现 diff 仅在 TASK-016 白名单，check 退出码 0 |

未执行真实模型质量、性能、真实漫画样本和权威版本/许可确认：分别保持 `BLOCKED`/`NOT_RUN`，不作为 PASS 证据。

## 结论

`a286578` 当前 **不能批准集成**，decision=`changes_requested`。R-001 是阻塞性 AC 缺口；R-002～R-004 为非阻塞改进项。已确认：未以 Mock 冒充模型质量，probe-only 不联网/不下载；质量、坐标/顺序和性能字段保持 `NOT_RUN`/`None`，模型 Hash 诚实标为 `NOT_AVAILABLE`；且未越界修改生产代码、测试、Schema、依赖或 AGENTS。

TASK-016 保持 `in_review/changes_requested`，不标记 `approved`/`done`，不填写 `integration_commit`。修订后须提交新的 delivery head，由 Codex重新固定并复审；TASK-015、TASK-017～TASK-027 及其他冻结 Task 不因本 Review 变化。
