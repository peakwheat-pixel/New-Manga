---
task_id: TASK-053
reviewer: 独立子代理（ZCode 会话内新开子对话，与作者不同对话；窗口条款 §8.2 口径）
author: ZCode
base_commit: 8bf8da3f988cdeffce83ad251a15c22ce862dd1a
reviewed_head: 5eeebde
decision: approved_subagent
started_at: 2026-09-19 03:12 +0800
supersedes: doc/reviews/TASK-053-3b58736.md（approved_subagent，R-001 守卫复审）
---

# 复审：TASK-053（TASK-044 遗留收口 R-03 / R-04，R-001 守卫修订复审）

独立子对话复审（开始时间 2026-09-19 03:12，reviewer=独立子代理，与作者不同对话；首轮 approved_subagent 的 R-001 守卫复审）

> 编者注（作者落盘）：以下为独立子代理复审报告原文（结构保留）；作者仅添加 frontmatter 与本注。

## 固定对象

| 项 | 值 |
|---|---|
| worktree（只读） | `G:/CODEX/New Manga.worktrees/TASK-053-zcode`，全程未 commit/push，终检 `git status --short` 为空 |
| 分支 | `agent/zcode/TASK-053-run-files-leak` |
| 首轮被审 head | `3b58736`（报告 `doc/reviews/TASK-053-3b58736.md`，decision=`approved_subagent`，R-001 P2） |
| 修订 | `16340b5`（retry 守卫 + docstring + 新用例 + idempotent 用例构造修正 + 修订取证 ×5） |
| 被审新 head | `5eeebde`；`git diff 16340b5..5eeebde -- src tests` 为空（实证，= 修订 + 首轮报告落盘） |
| 修订取证 | `verification/TASK-053/revision-full-suite-run{1..5}.log`（881 passed / 0 skipped ×5，= 873 基线 + 8 新用例） |

## R-001 复核（逐处核对 diff + 判别力实验）

**`src/application/maintenance/trash.py`（+18 行）——修复属实：**

1. `retry_pending_purges()` 新增守卫：对每个 pending 条目先 `if self._batch_in_ledger(batch_id): continue`，位于 `_remove_pending_targets` 之前；被跳过条目不触碰 manifest（保留），`cleared` 只计真正清除数。与首轮 R-001 建议一致。
2. `_batch_in_ledger()` 语义正确：只查 manifest `batches` 段的 `batch_id`。batch 是否已出账唯一由 `_drop_batch` 决定，而 `_drop_batch` 只操作 manifest `batches` 段——守卫判定源与出账操作源是同一处，无第二真相源。
3. docstring 增补 Guard 段，记录理由（premature 条目 = 行删除未发生 ⇒ 删文件会孤儿化活页；real purge 以同 id 覆盖条目并完成清扫）。
4. 正常 purge 完成路径不受影响：`purge_batch()` 直接调 `_remove_pending_targets`（未变、不经过守卫），且调用前 `_drop_batch` 已出账——即便套守卫也不会命中。
5. `_record_pending_purge`/`_pending_purges`/`_remove_pending_targets` 均未改动。

**`tests/storage/test_run_files_leak.py`（+60/−1，删除行仅为 idempotent 用例内一个空行，无断言改动；无 skip/xfail）：**

- 新用例 `test_retry_skips_premature_entry_while_rows_are_still_live`：`FailingPages` 代理 store 且 `purge_pages` 必抛 OSError，经真实 `purge_batch` 构造 R-001 premature 状态（record 先于 purge_pages 落盘 ⇒ 条目已在、batch 仍在 ledger、行仍活）；断言 retry==0、条目保留、live 文件在、页行在，最后真实 purge 以同 id 覆盖条目并完成清扫。构造真实。
- `conftest.py` 仅暴露 `manifest_path`（与 `service` 的 manifest 同一文件路径），供第二个 `TrashService` 实例共享状态——只读辅助，无副作用。
- idempotent 用例构造修正：补 `_drop_batch(batch_id)`，使「条目残留时 batch 必已出账」与真实 R-04 流程一致。核对了不修正的后果：旧构造下 batch 仍在 ledger，新守卫会使其跳过并返回 0 ≠ 1——作者必须同步修构造，且修法正确。

**判别力实证（TEMP 一次性探针，已清理）**：`git archive 16340b5` 导出树 + 仅用 `git show 3b58736:src/application/maintenance/trash.py` 覆盖（守卫移除；3b58736→16340b5 的 src diff 仅此一文件，已核实），运行新测试文件：**`test_retry_skips_premature_entry_while_rows_are_still_live` FAILED（`assert 1 == 0`——无守卫时 retry 返回 1，即误删活页文件并清条目），其余 7 用例 PASS，EXIT=1**。守卫移除则该用例必失败，判别力声明成立；判别力完全由新守卫用例承载（修正后的 idempotent 用例在两棵树上均 PASS，属语义修正非判别件）。

## 反面场景与 F-7 交互（TEMP 语义探针 S2a–S2d，stub store 直驱 worktree trash.py，已清理）

| 场景 | 结果 |
|---|---|
| S2a：F-7 重建 batch（manifest 条目丢失）+ purge_pages 失败 + retry | **BLIND SPOT（见新 finding R-004）**：守卫未跳过 `rebuilt:` 前缀条目，cleared=1、活页文件被删、行仍在 |
| S2b：rebuilt batch + 真实 R-04（行删后文件删除失败）+ retry | PASS——合法条目不被守卫误跳过，retry 正常清除 |
| S2c：manifest 整文件损坏 | PASS——`pending_purges` 段与 `batches` 段同文件同损，「F-7 退化 + pending 段在 + batch 条目丢」唯一可达路径是 S2a 的窄窗口 |
| S2d：混合条目（1 premature 在账 + 1 合法出账） | PASS——守卫选择性正确：只清合法条目、保留 premature 条目、不触碰 premature 的活页文件 |

结论：无过度守卫（所有 batch 已出账的合法条目，包括 FlakyRemover 真实失败场景、idempotent 场景、rebuilt 场景，均不被误跳过）；F-7 交互除 R-004 一处残余盲区外可论证（S2b/S2c）。`_batch_in_ledger` 每条目重读 manifest（N+1 读）属维护入口低频操作，不构成问题。

## 复跑命令与结果

环境：venv `G:\CODEX\New Manga.task-envs\TASK-012-py312\Scripts\python.exe`（Python 3.12.3）；PYTHONDONTWRITEBYTECODE=1；`-p no:cacheprovider`；QT_QPA_PLATFORM 未设（输出确认空）。

| # | 命令 | 结果 |
|---|---|---|
| V1′ | 判别力树（16340b5 tests + 3b58736 trash.py，TEMP）定向新套件 | **1 failed（守卫用例）+ 7 passed，EXIT=1** ⇒ 守卫用例对 3b58736 树必失败 |
| V1 | 新 head：`pytest tests/storage/test_run_files_leak.py -v` | **8 passed / 0 skipped，EXIT=0** |
| V2 | 新 head：`pytest tests/storage tests/core -q` | **80 passed / 0 skipped，EXIT=0**（首轮 79 + 1 新） |
| V3 | 新 head 全仓：`pytest -q -rs -p no:cacheprovider` | **881 passed / 0 skipped / 1 warning（既有 mobi imghdr DeprecationWarning），EXIT=0**；与 `revision-full-suite-run{1..5}.log`（881 ×5）逐条一致 |
| 基线算术 | 873（首轮证实基线）+ 8（7 首轮 + 1 修订新增）= 881 | 成立 |

## AC ①–⑥ 判定（修订后全部维持 PASS）

- **AC ① PASS（维持）**：守卫只改 retry 入口，record-before-destroy 主修复面零改动。
- **AC ② PASS（维持）**：FlakyRemover 真实失败→retry 用例新旧树均 PASS；S2b/S2d 实证守卫不干扰合法 retry。
- **AC ③ PASS（维持）**：R-03 相关代码零改动。
- **AC ④ PASS（维持）**：trigger 面未动，全仓复跑含 `TestTriggerDiscipline` PASS。
- **AC ⑤ PASS（增强）**：判别力新增一层（守卫用例对 3b58736 必失败，V1′ 实证）；全仓 881 ≥ 873、×5 日志与独立复跑一致；既有断言零改动维持。
- **AC ⑥ PASS（按流程进度，不变）**：集成与 STATUS 台账行属集成后步骤。

## Findings

| ID | 级别 | 文件/行 | 触发条件与影响 | 建议 | 处理状态 |
|---|---|---|---|---|---|
| R-004 | P3 | `trash.py`（`_batch_in_ledger` 与守卫） | F-7 重建 batch 的 id 为 `rebuilt:…` 且**从不写入 manifest `batches` 段**（`list_batches` 虚拟重建），守卫查不到 ⇒ 「manifest 恰好丢 batch 条目 + 对 rebuilt batch 发起 purge + `purge_pages` 失败」时，premature 条目不被跳过，retry 会删除仍存活页的托管文件（用户源文件不受影响、可重导入）。非本修订引入（3b58736 上同样发生），是 R-001 的残余缺口而非回归；需三个非典型事件叠加，S2c 证明整文件损坏时 pending 段同损、唯一到达窗口极窄 | 若装配前继续收口：守卫同时按 (chapter_id, deleted_at) 匹配 `list_batches()` 中的 rebuilt 组，或在 docstring/Handoff 风险段声明该盲区；不阻塞本次 | open（建议随装配 Task 一并处理，与 R-003 同批） |
| R-002 | P3 | 探针文案 | 维持首轮判定（"pre-fix" 文案易误读），修订未涉及 | 维持首轮建议 | open（作者声明不阻塞） |
| R-003 | P3 | 端口/实现 | 三个入口仍无生产接线，维持首轮判定；接线时连同 R-004 一并处理 | 维持首轮建议 | open（已声明） |

## 结论

**R-001 守卫真实修复且被新用例钉住**（判别力树实证：守卫移除 ⇒ 守卫用例必失败）；`_batch_in_ledger` 语义正确、docstring 记录理由、`_remove_pending_targets` 主路径未动；无过度守卫（合法条目含 rebuilt 与混合场景均不被误跳过）；修订未引入任何回归，全仓 ×5 日志与独立复跑一致（881/0/EXIT=0）。AC ①–⑥ 全部维持 PASS 且判别力声明增强。唯一新发现 R-004 为 P3 残余盲区（前提比 R-001 更苛刻、非修订引入、不阻塞），与 R-002/R-003 同批留给装配前收口。

**decision: `approved_subagent`**
