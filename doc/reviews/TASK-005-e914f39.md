---
task_id: TASK-005
reviewer: DeepSeek Harness
author: Codex
base_commit: d65901b953e6fb26043344ed3d668520847eb295
reviewed_head: e914f39a0c721f717c45a01229cd6d0b82f0045e
decision: changes_requested
---

# Review：TASK-005 最小工程入口与架构守卫（e914f39）

本报告只覆盖 `d65901b..e914f39`。Review 期间未修改任何被审源码、测试、依赖、验证脚本或 Handoff；本报告是 Reviewer 新增的唯一文件。

## 范围与依据

**固定对象**：`base_commit = d65901b953e6fb26043344ed3d668520847eb295`、`reviewed_head = e914f39a0c721f717c45a01229cd6d0b82f0045e`；作者分支 `agent/codex/TASK-005-minimal-bootstrap`，后置元数据提交 `4c86c9e`（Task/Handoff）不属于被审范围。

**变更**：`git diff --stat d65901b e914f39` = 14 路径、`+734/−4`，全部位于 TASK-005 `allowed_paths`；TASK-006～TASK-027 零变化。

**Reviewer 环境（独立）**：Windows `10.0.26200`；Review venv `C:\Users\49745\AppData\Local\Temp\task005-review-venv`（Python `3.12.3`，由 `C:\Users\49745\AppData\Local\Programs\Python\Python312\python.exe` 创建）；另建 Core-only venv `task005-core-only-venv`。一次性 detached 检出 `%TEMP%/nm-task005-e914f39`（`git worktree add --detach ... e914f39`），审查后已移除。

**依据**：TASK-005 四条 AC 与「已批准设计：方案 A」；D02 §16（推荐源码边界）、D07 §83（可选依赖）、D08 `AC-OPTIONAL-001`；`doc/09_COLLABORATION.md`。

## Findings

| ID | 级别 | 文件/行 | 问题与触发条件 | 影响 | 复现证据 | 建议 | 状态 |
|---|---|---|---|---|---|---|---|
| R-001 | **P1** | `tests/core/test_architecture.py:26-27` | `ast.ImportFrom` 在 `node.module is None` 时被整体跳过（`modules = [node.module] if node.module else []`）。触发条件：`src/ui/*.py` 写 `from .. import infrastructure`（`..` = `src`，实际导入顶层 `infrastructure` 包）或 `src/domain/*.py` 写 `from .. import sqlite3` 等相对形式 | 守卫对一种**静态可检测**的合法 import 形式返回空结果，即架构守卫可被一行代码静默绕过，AC3「可运行守卫」的保证不完整。**当前被审代码无实际违规**，不影响 AC1/AC2/AC4 与当前功能正确性 | 独立探针（调用被审的 `find_forbidden_imports`）：`from .. import infrastructure` → `[]`（MISSED）、`from . import infrastructure` → `[]`（MISSED）；对照 `import infrastructure.x`、`from infrastructure import x`、`from ..infrastructure import x` 均 DETECTED | 在 `ImportFrom` 分支中当 `node.module is None` 时改查 `node.names` 的别名（保留 level 信息更佳），并补一个合成测试覆盖 `from .. import <forbidden>`；预计 1–3 行改动 | open |
| R-002 | P2 | `verification/TASK-005/author-verification.md:8,16` | 该文件表格把「被测生产/测试提交」标为 `b318bdd`，而 reviewed_head 是 `e914f39`。文件 L16 已主动说明原因（本文件与被测提交同批后置提交），但表格首列仍可能被读者或工具当作 reviewed_head 的证据 | 取证时可能把绑定 `b318bdd` 的 PASS 误引为 `e914f39` 的结论。实际 e914f39 的复跑结果在 Handoff，且本次 Review 已在 e914f39 上独立复跑通过，因此**不影响本次结论** | `git show e914f39:verification/TASK-005/author-verification.md`（L8、L16）；对照 Handoff 的固定 head 复跑行 | 在表格「被测提交」处直接标注 `reviewed_head` 复跑指针（或把固定 head 复跑结果并入本文件），避免两处证据边界靠正文说明维系 | open |
| R-003 | P2 | `verification/TASK-005/implementation-plan.md:3` | 计划首行引用 `superpowers:subagent-driven-development` / `superpowers:executing-plans` —— 本项目未定义这两个 sub-skill，仓库内也无对应说明 | 后续读者无法核验该指令来源，属项目外工具链引用；不影响构建、测试与验收 | `git show e914f39:verification/TASK-005/implementation-plan.md` 第 3 行；`git grep -n 'superpowers' e914f39` 仅此一处 | 删除该行，或改为说明「本地工作流工具，不属于本项目约定」 | open |
| R-004 | P2 | `src/bootstrap/app.py:21` | `QGuiApplication([sys.argv[0]])` 固定使用进程 `sys.argv[0]` 作为 Qt 应用名，而 `main(argv)` 的 `argv` 只传给 `argparse`；程序化调用 `main([...])` 时两者不一致 | 轻微：当前唯一调用路径是 CLI，无实际影响；但接口语义（`main(argv)`）与 Qt 参数来源不一致，后续若被程序化调用（例如测试或打包入口）会产生混淆 | `git show e914f39:src/bootstrap/app.py` L16-21 | 改为 `QGuiApplication([sys.argv[0], *(argv or ())])` 或明确注释「argv 仅用于 CLI 解析」 | open |

P0 = 0，P1 = 1（R-001），P2 = 3（R-002～R-004）。

## 验证

| 场景 | 命令或手工步骤 | 环境 / commit | 结果 | 证据 |
|---|---|---|---|---|
| 必需检查 1 | `git diff --check d65901b e914f39 --` | Git 2.52.0 / 工作区 | PASS，退出码 0 | 无输出 |
| 必需检查 2 | `git diff --stat d65901b e914f39` | 同上 | PASS | 14 files changed, 734 insertions(+), 4 deletions(−) |
| 必需检查 3 | `git diff d65901b e914f39 --`（逐文件阅读全部 14 路径） | 同上 | PASS | 结论见 Findings 与三轴 |
| 独立 venv 安装 | `py312 -m venv` → `pip install -r requirements-dev.txt` → `pip check` | Python 3.12.3；独立 venv | PASS | 安装退出码 0；`No broken requirements found.`；freeze 与 requirements-dev 闭包一致（仅多 `pip` 自身） |
| 必需检查 4（作者验证脚本） | `pwsh -NoProfile -File ./verification/TASK-005/verify.ps1 -BaseCommit d65901b... -ReviewedHead e914f39... -PythonExe '<Reviewer venv>'` | 一次性 detached 检出 `e914f39` | **PASS，退出码 0** | 6 条 PASS（见下） |
| AC2 强化验证（Core-only） | 第二个 venv 只装 `requirements.txt`，断言无 pytest、无 torch/transformers/onnxruntime，再跑真实 QML smoke | Python 3.12.3 Core-only venv | **PASS** | 依赖仅 `pip/PySide6_Essentials/shiboken6`；`heavy = []`；`import pytest` 失败（证明非 dev 环境）；`python -m bootstrap.app --smoke-test` 退出码 0 |
| 缺失 QML 诊断（独立复现） | 复制 `src/bootstrap` 到无 `ui/qml` 的隔离 `src`，设 `PYTHONPATH` 后运行 `--smoke-test` | Core-only venv | **PASS** | 退出码 1；stderr = `Failed to load QML: <隔离绝对路径>`，路径与预期一致；对照真实 `src` 退出码 0 |
| 架构守卫覆盖探针 | 调用被审 `find_forbidden_imports` 对 11 种 import 形式 | Review venv | **部分 FAIL** | 9 种 DETECTED；`from . import infrastructure`、`from .. import infrastructure` 两种相对形式 MISSED（见 R-001） |
| 越界检查（数据库/Provider/导航/打包） | `git ls-tree -r --name-only e914f39` 过滤 `sqlite\|repository\|provider\|migration\|schema\|pyinstaller\|\.spec$\|pyproject\|uv\.lock\|poetry` | `e914f39` | PASS | 仅命中 `verification/TASK-004/logs/pyinstaller_build.log`（TASK-004 历史日志，非本 Task 新增）；`src` 树只有 `bootstrap/`、`domain/__init__.py`、`ui/__init__.py`、`ui/qml/Main.qml` |
| 冻结范围 | `git diff --name-only d65901b e914f39` 对照 TASK-005 白名单 | 同上 | PASS | 14 路径全部授权；无 `doc/tasks/TASK-006..027.md` 变化 |
| 测试真实性 | 阅读 `tests/core/*`（无 mock、无 monkeypatch；subprocess 真实执行并以真实退出码/stderr 断言） | 同上 | PASS | `test_bootstrap.py`、`test_architecture.py` |
| 正常交互式窗口人工操作 | 未执行 | — | NOT_RUN | 本 Task 仅要求自动 smoke |
| PyInstaller / 干净 Windows VM | 未执行 | — | NOT_RUN | TASK-005 禁止打包配置；TASK-004 证据不替代本 head |
| SQL / Provider / 模型质量 / 性能 | 未执行 | — | N/A / NOT_RUN | 本 Task 无这些实现或获批阈值 |

作者验证脚本在 Reviewer venv 上的实际输出：

~~~text
PASS: base/head ancestry and 14 allowed paths
PASS: exact Core/dev dependency lock with no heavy AI or PySide6 Addons
No broken requirements found.
PASS: Python 3.12.3 environment is consistent and heavy AI dependencies are absent
============================= test session starts =============================
platform win32 -- Python 3.12.3, pytest-9.1.1, pluggy-1.6.0
collected 5 items
tests\core\test_architecture.py ...                                      [ 60%]
tests\core\test_bootstrap.py ..                                          [100%]
============================== 5 passed in 0.45s ==============================
PASS: core pytest and real QML smoke startup
PASS: 8 local Markdown links and balanced code fences
PASS: git diff --check d65901b953e6fb26043344ed3d668520847eb295 e914f39a0c721f717c45a01229cd6d0b82f0045e
~~~

## 三轴结论

**Spec（方案 A 与四条 AC）**

- AC1 依赖与入口：**PASS**。`requirements.txt`/`requirements-dev.txt` 是唯一锁定方式，逐项精确版本；无 `pyproject.toml`/`uv.lock`/Poetry；入口为 `PYTHONPATH=src` + `python -m bootstrap.app [--smoke-test]`，与规格一致。
- AC2 Core 无重型依赖仍可启动：**PASS（独立强化验证）**。Reviewer 另建只含 `PySide6_Essentials==6.11.2`、`shiboken6==6.11.2` 的 Core-only venv，无 pytest、无 torch/transformers/onnxruntime，真实 QML smoke 退出码 0；未批量生成空模块，只有 `bootstrap`/`domain`/`ui` 三个当前必需边界。
- AC3 架构守卫：**部分 PASS**。真实源码扫描无违规、两个合成违规测试真实有效、诊断含文件/行号/模块；但相对导入形式存在漏检（R-001），守卫保证不完整。
- AC4 交接与独立审查：**进行中且设计正确**。Handoff 绑定 base/head 并如实标注 NOT_RUN 项；未把作者自查写成独立批准。

**Architecture / Standards**

- 最小包边界符合 D02 §16 的定位（该节明确「只是目标边界示意……不为目录漂亮进行无收益的大规模移动」），未创建 Application/Ports/Infrastructure/Repository/Provider/数据库/四页导航/打包配置/CI。
- 依赖隔离达到 AC2 目标，且 `requirements-dev.txt` 通过 `-r requirements.txt` 复用 Core，未引入第二套锁定工具；`pyinstaller` 属 TASK-004 已获批版本，TASK-005 规格明确允许。
- 14 个变更路径全部在白名单内，未触碰其他 Task、AGENTS、STATUS。
- 唯一架构层面的实质问题是 R-001（守卫盲区）；R-003/R-004 属文档与接口一致性。

**Verification**

- 三项必需 git 检查全部执行并通过；作者 `verify.ps1` 在 **Reviewer 自己的独立 venv** 上退出码 0，5 个 core 测试真实执行（platform/版本行可见）。
- 测试**真实有效**：`test_bootstrap.py` 用真实 subprocess 与真实退出码/stderr 断言（非 mock），`QT_QPA_PLATFORM=offscreen` 真实加载 `Main.qml`；`test_architecture.py` 用真实文件 + `ast`，并以两个合成违规验证诊断格式。
- 缺失 QML 分支由 Reviewer 在隔离目录独立复现：非零退出 + 绝对路径诊断。
- AC2 通过 Core-only 环境独立强化验证。
- 守卫的覆盖边界经 11 种合成 import 形式探测，暴露 R-001。
- Mermaid / PyInstaller / 干净 Windows / 性能 / 模型质量：NOT_RUN 或 N/A，未以推测代替。

## 结论与复审

**`e914f39` 暂不可交 Codex 集成：decision = changes_requested。**

阻塞项为 R-001（P1）：架构守卫对 `from . import X` / `from .. import X` 形式的相对导入返回空结果，`src/ui/*.py` 中的 `from .. import infrastructure` 可静默绕过 UI 守卫。该缺口**不影响当前被审代码**（`src/domain`、`src/ui` 实测无违规），也不影响 AC1/AC2/AC4，但属于 AC3 核心交付的完整性缺陷，且修复成本极低（1–3 行 + 1 个合成测试）。

R-002～R-004 为 P2，可与 R-001 同批处理或明确 de-scope：R-002 建议把固定 head 复跑证据并入 `author-verification.md`；R-003 建议移除或改写外部 sub-skill 引用；R-004 建议统一 `argv` 与 Qt 应用名来源。

**复审要求**：修订后提供新的交付 head，重新绑定 `base_commit = d65901b`、`reviewed_head = <新 head>`，逐条给出 R-001～R-004 的 disposition（fixed / deferred + 理由）。复审将复跑 `verification/TASK-005/verify.ps1`、`git diff --check`、变更范围检查，并针对守卫补测相对导入形式。

**剩余风险**：静态 AST 守卫天然无法覆盖动态导入（`importlib.import_module`、`__import__`）等绕过方式；即使修复 R-001，它仍应被理解为「防止常见违规的护栏」而非安全边界，建议在守卫文档或 Task 中明确该定位。四个一级页面、业务流程、数据库、Provider、打包与发布均未实现，也未获本 Task 授权。
