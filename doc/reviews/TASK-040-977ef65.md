---
task_id: TASK-040
reviewer: Codex（非作者）
author: ZCode
base_commit: 40d97d5（开工 fast-forward 至 c5aa664；尾部又 merge master 84bdda7）
reviewed_head: 977ef65
metadata_head: 2afabf2（尾部另有 merge 004f00e 与证据 9b9c2da）
decision: approved
---

# Review：TASK-040（接通 TASK-039 的 `clean_probe` 生产注入 + F-12 口径修正）

Reviewer=Codex（**非作者**；作者=ZCode）。固定 `base=40d97d5`（开工 ff 至 `c5aa664`）、被审 `reviewed_head=977ef65`（元数据 `2afabf2`；分支尾部 `004f00e` = merge master `84bdda7`、`9b9c2da` = merge 后证据）。口径按 2026-09-18 更新后的 §6：**Architecture 与 Verification 必覆盖**，Standards/Spec 作为可选视角一并给出。

## 范围与依据

- **作者改动 = 4 文件 `+121/−5`**：`src/infrastructure/pipeline/assembly.py`（**白名单外——作者主动声明并请裁，见 R-01**）、`src/bootstrap/app.py`、`src/application/tasks/service.py`（**仅 docstring**）、`tests/core/test_bootstrap.py`（+2 例）；另有 `verification/TASK-040/**` 15 个证据文件与 Handoff。
- **白名单外命中 1 处**（`assembly.py`），其余全部在授权路径内；未改 Schema/migration、依赖清单、`AGENTS.md`、其他 Task。
- 未审到：MOBI（TASK-041）、webtoon（TASK-042/045）、purge（TASK-044）——按禁止范围排除。

## 独立复跑 vs 复用作者证据

| 结论 | 来源 |
|---|---|
| 定向 `tests/core tests/pipeline tests/providers` = **261 passed / 0 skipped** | **独立复跑**（本次，PowerShell + `TASK-012-py312`，exit 0） |
| 全仓 = **804 passed / 6 skipped**，6 条全为既有 `tests/network` `openssl unavailable` | **独立复跑**（作者同口径为 `810 passed / 0 skipped`＝其 shell 下 openssl 可用、6 条 network 转通过；**804+6 = 810，收集总数一致**） |
| **判别力**：新测试 + master `src` → **2 failed / 14 deselected**（接线断言 FAILED；生产对照 FAILED 于 `blocked:missing_clean_artifact != run:`） | **独立复跑**（在 `git archive master` 导出树中放入新测试运行） |
| `service.py` 仅 docstring、判定逻辑零改动；`tests/core` 零断言/零测试删除；`src`+`tests` 空白干净 | **独立核对**（逐条读 diff / `git diff --check` exit 0） |
| 其他调用方不受影响（`build_production_pipeline` 另有 2 个测试 helper 调用点，均不传新参数） | **独立核对**（`rg` 调用点 + 本次全仓通过） |
| 作者 5 次逐次全仓日志、merge 后 5 次日志、`pre-fix-discriminating-power.log` | **复用作者证据**（我复跑了聚合结果与判别力本身） |

## Architecture（必覆盖）

**`executed`**。

- **探针来源合规（AC ① 硬要求）**：`app.py` 的闭包**复用既有只读查询器** `SqlitePageArtifactLocator(conn).locate_current(page_id, ArtifactType.CLEAN)`，**未新造第二套 Clean 判定** ✓；判定语义为"该页存在 current Clean revision"（`current_revision_id` 非空且 revision 行存在），与 `ArtifactStepWriter` 的提交方对齐（Handoff 已写明）。
- **只可能放宽 BLOCKED→RUN，不可能放宽既有守卫**：探针仅在 `_clean_available` 的三分支之末参与，且为真时才把结果从 False 变 True ✓（TASK-039 语义原样，`service.py` diff 仅 docstring 已核）。
- **装配方向正确**：闭包在**装配现场**构造并注入，`application` 层未反向依赖 `infrastructure`（AST 守卫在本次全仓通过）✓。
- **成本**：每次规划判定多一次只读 DB 查询；规划器本就在做 DB 工作，量级可忽略（未测），**接受**并登记为观察项（R-04）。
- **白盒接线断言**：`services.pipeline._clean_probe is not None` 读私有属性——因本切片**禁止改 `service.py` 逻辑**（不能加公开访问器），且仓库已有同风格先例（`tests/core/test_bootstrap.py:259` 的 `_catalog/_store/_snapshots/_executor`）⇒ **接受**，但见 R-02。

**本面小结**：0 项缺陷；1 项 seam API 扩展待裁（R-01，**已授予**）、1 项白盒风格观察（R-02）。

## Verification（必覆盖）

**`executed`**。

| 场景 | 结果 |
|---|---|
| 定向三目录 261/0、全仓 804/6（skip 原因逐条列出） | **PASS**（与作者同口径数字可换算一致：804+6=810） |
| 判别力（新测试 × master src） | **PASS 2 failed / 14 deselected**，失败点与作者描述逐条吻合 |
| AC ② 接线断言存在且可通过 | **PASS**（`test_assemble_services_injects_production_clean_probe`） |
| AC ③ 生产对照 + 负例 | **PASS**（真 SQLite + 真 locator：无 Clean → `blocked:missing_clean_artifact`；种入 current Clean 后**仅该页**转 `run:`，兄弟页仍 blocked ⇒ 未把探针做成"永远有 Clean"） |
| 判定逻辑/断言/Schema/依赖/删除/skip 六项纪律 | **PASS**（service.py 仅 docstring；tests 零删除；0 Schema；依赖零改动；0 新增 skip） |
| 未独立复核 | 作者 10 份逐次日志（合并前后各 5）— **复用作者证据** |

**本面小结**：0 项阻断；1 项口径勘误（R-03）。关键路径（注入、接线断言、生产行为与负例、判别力）**均由我独立复跑**。

## Standards（可选视角）

**`executed`**。规范面：白名单/资产/证据纪律基本合规，**唯一命中为 `assembly.py` 越界**（作者已声明并请裁，见 R-01）；无新增 skip、无断言删除、`git diff --check` 在 `src`+`tests` 范围 exit 0。判断项：R-02（白盒断言）。

## Spec（可选视角）

**`executed`**。AC ①～⑥ 逐条核对：① 注入（复用既有查询）✓；② **接线断言**（本切片核心，让"接线漏做"不再可能悄悄通过）✓；③ 生产对照 + 判别力（含负例）✓；④ F-12 口径修正（`service.py` docstring 已改，TASK-039 AC ② 注记按 AC 允许的"记入 STATUS"选项处理，未回改已入档 Review）✓；⑤ 回归分列 ✓；⑥ Handoff + 待集成 ✓。**scope creep：无**（改动均可归到 AC ①～④）。

## Findings

| ID | 级别 | 位置 | 触发与影响 | 处置 |
|---|---|---|---|---|
| **R-01** | **P2（程序性）→ 本 Review 授予** | `src/infrastructure/pipeline/assembly.py` | 该文件**不在 TASK-040 白名单**（任务书把"装配处"写成 `src/bootstrap/app.py`，并禁止改 seam 本体），作者为其新增**可选形参** `clean_probe: Callable[[str], bool] \| None = None` + 透传。**实质**：`PipelineService` 的**物理构造点**确在 `assembly.py::build_production_pipeline`（`app.py:523` 只是调用方），白名单内注入只剩"跨模块写 `pipeline._clean_probe` 私有属性"一途——**更差** | **授予（granted）**。理由：①**加法式、默认保持**——新增参数默认 `None`，另外 2 个既有调用点（`tests/providers/test_full_chain.py:339`、`test_handlers_pipeline.py:241`）不传该参 ⇒ 修前装配**逐字节不变**；②未动 seam 的其他语义（diff 仅形参 + 透传 + docstring）；③**F-12 的证据位置本身就含 `assembly.py`**，修复面天然覆盖；④作者**主动声明偏差并给出回滚路径**，程序上正确。**同时登记我方的指认错误**（见下方"流程修正"） |
| R-02 | P3 | `tests/core/test_bootstrap.py:505` | 接线断言读私有属性 `_clean_probe`（白盒）。若将来改名/重构，断言会以"AttributeError"而非"接线缺失"失败 | **accepted**：本切片禁止改 `service.py` 逻辑（无法加公开访问器），且仓库已有同风格先例；建议后续在 `PipelineService` 加只读访问器（或在 `service.py` 允许范围内）后把断言改为公开 API 或行为式断言 |
| R-03 | P3 | Handoff 的口径 | 作者用 Git-Bash 继承 PATH 口径（`810/0`），我的是 `804/6`；总数一致、无屏蔽 | **accepted/记录**：已按既有惯例在集成记录中并列表述（同 TASK-036/042 一类） |
| R-04 | P3 | `assemble_services` 的探针 | 每次规划判定多一次只读 DB 查询 | **accepted（观察项）**：规划器本就有 DB 工作；如将来出现性能预算再实测 |

**流程修正（我方）**：TASK-040 的释放白名单把"装配处"指认为 `src/bootstrap/app.py`，而**物理构造点在 `src/infrastructure/pipeline/assembly.py`** —— 这是**我在释放时的核对遗漏**（我当时只 grep 了调用点，没有沿到构造函数）。作者按事实选择了最小侵入方案并声明。**改进**：今后释放涉及"装配/接线"的切片时，白名单必须包含**构造函数所在文件**（先 `rg "^def build_|^class .*Service"` 定位物理装配点），或明确写"装配函数所在文件待作者确认后可申请"。

## 结论

**decision = `approved`**（0 P0/P1；R-01 为程序性越界，**本 Review 明示授予**；R-02～R-04 均 P3 非阻塞）。`reviewed_head=977ef65` 可交集成。

理由：①**AC ② 的接线断言**是本切片的核心价值，已实现且我独立复现其判别力（修前 2 failed）；②探针**复用既有 locator**、语义正确、**未把探针做成"永远有 Clean"**（负例由生产路径断言）；③判定逻辑零改动（`service.py` 仅 docstring）；④F-12 的 docstring 口径已修正且未回改已入档 Review；⑤回归分列、无新增 skip、无断言删除；⑥越界一处已由作者主动声明并经我授予，附带回滚路径与流程改进。

**集成后**：**F-12 关闭**；TASK-040 置 `done`。

**剩余风险**：R-02（白盒断言脆性）、R-04（探针成本未测）；R-01 的授予使 `build_production_pipeline` 的**公开签名新增一个可选参数**——已确认 3 个调用点中 2 个不受影响、1 个（`app.py`）为本切片的有意接线。
