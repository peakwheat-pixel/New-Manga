---
task_id: TASK-041
kind: re-review
reviewer: Codex（非作者外部 Review —— 复审）
author: ZCode
first_review: doc/reviews/TASK-041-d114253.md（changes_requested；R-001 P1、R-002 P2、R-003～R-007 P3）
reviewed_range: d11425376fe482f782c4efc0ae08ae42a930b6f3..237e623c9586fb351be5a6eb9e48a3e975c9be85
reviewed_implementation_head: f75209623d80d738d1ecd787372baf5f0d9754be
reviewed_head: 237e623c9586fb351be5a6eb9e48a3e975c9be85
decision: approved
---

# 复审：TASK-041 修订（R-001～R-007）

Reviewer=Codex（**非作者**；作者=ZCode）。被审范围 `d114253..237e623`（实现 `f752096`、文档 `237e623`；分支内已 `git merge master` 合入首轮 Review 与探针）。首轮 [Review](TASK-041-d114253.md) 为 `changes_requested`；**本次 `decision = approved`** —— 7 条 finding 全部按 disposition 落实，且两条关键主张（反例消失、判别力）由我**独立复现**。按协议 §6「复审追加新 head 与 disposition，不抹掉旧记录」，首轮报告保持原样。

**执行口径（用户 `[$code-review]` 调用）**：四轴 Standards / Spec / Architecture / Verification（Standards/Spec 为可选视角，本会话无并行 sub-agent 通道，单遍执行并声明偏差）；本仓库 §6 强制项 Architecture + Verification 均 `executed`。

## 范围与依据

- **被审 diff = `d114253..237e623`**：23 文件 `+774/−59`、**0 删除**。生产面仅 2 文件：`src/infrastructure/importing.py`（±78：`_PAGE_IMAGE_RE` + 收集/排序 + `mobi8/` fail-closed + docstring 收窄）、`src/application/importing/documents/service.py`（±12：`_is_mobi` 口径 + 未注入文案）。测试 1 文件（`test_mobi_import.py`，9 → 12 例）。文档/证据：Handoff 修订节、`doc/tasks/TASK-041.md`、`doc/STATUS.md` +1 台账行、`verification/TASK-041/revision/**`（9 文件）。**未碰** Schema/migration、依赖清单、pipeline seam、`AGENTS.md`、其他 Task、`tests/import_formats/test_document_import.py` ✓
- **逐条 disposition（作者自述）**：R-001 fixed（收集收敛 + record 排序 + KF8 fail-closed）／R-002 fixed（**选 ②声明收窄**，AZW3 **BLOCKED** 登记）／R-003 fixed（`sys.modules["mobi"]=None` 驱动真实 import 守卫）／R-004 fixed（3 页中段坏图）／R-005 fixed（五个 fail-closed 用例加受控根不存在断言）／R-006 fixed（口径勘误入 Handoff）／R-007 fixed（未注入文案改写并同步断言）。
- **未审到**：真实 AZW3/KF8 容器与真实商业 DRM 样本（无获取渠道；本次修订已把 AZW3 明确收窄为 typed fail-closed，故不再是本 Task 的验证义务）。

## Standards（executed）

- **白名单**：改动路径全部在 Task 允许范围内，越界 0；`requirements.txt` 本次**零改动**（`mobi==0.4.1` 仍是唯一新增项）✓
- **分层**：`rg "import mobi|from mobi" src/` 仍仅命中 `src/infrastructure/importing.py`；`application → infrastructure` 仍 0 命中 ✓
- **断言纪律**：测试只增不减（+156/−? 净增 3 例）；`doc/tasks/TASK-041.md` 的 **AC ②③ 已按首轮 Review 结论回退为未勾选**并写明原因与待复审事项（不掩盖、不抢先回勾）✓；无新增 `skip`/`xfail` ✓
- **注释与实现一致（本 Task 的反面教材就是这条）**：`_PAGE_IMAGE_RE` 上方的三行注释、`MobiDocumentRaster` 类 docstring 的"Page semantics (R-001)"段、`_is_mobi` 的"dispatch only"段与代码逐条对应；旧的"文件名序即容器页序"表述**已删除**（不再有第二套说法）✓
- **smell baseline（判断项）**：possible Duplicated Code 仍在（PDB 魔数/偏移在 `service.py::_is_mobi` 与 `importing.py::_pdb_crypto_type` 各一份），但两处语义不同（分派 vs 头解析），且首轮已作判断项记录，**不重复报告**。

## Spec（executed）

| 首轮 finding | 级别 | 复审判定 | 我的独立验证 |
|---|---|---|---|
| **R-001** 页面清单=提取树全量图片按字典序 | P1 | **fixed ✓** | ①**复跑首轮探针**（`verification/TASK-041/review-d114253/probe_collect.py`）：注入含 `cover00001`/`HDimage00004` 的树 → `page_count` 由 **4 → 2**、顺序为 record 序（红 8×6 → 绿 9×7）；PROBE B 三例 fail-closed 仍零残留（[first-probe-rerun-on-revision.txt](../../verification/TASK-041/review-237e623/first-probe-rerun-on-revision.txt)）。②**我另写探针**（[reviewer_probe_revision.py](../../verification/TASK-041/review-237e623/reviewer_probe_revision.py)）覆盖 6 个用例：噪声树=2 页；**数值排序**（`image00009` 先于 `image00010`，字典序会反）；**镜像 `mobi8/`** → typed `INVALID_DOCUMENT`（detail 含 KF8）；**KF8-only**（无 mobi7）→ 同 typed；**不合规名**（`image0002.jpeg`/`image.jpeg`/`imagenote00003.jpeg`）**被排除**（1 页）——语义与 docstring 一致。③**判别力**：修订测试跑在 `d114253` src 上 → **3 failed / 9 passed，exit 1**，恰为三个行为修复用例（我复现，见 [discriminating-revised-tests-vs-d114253-src.txt](../../verification/TASK-041/review-237e623/discriminating-revised-tests-vs-d114253-src.txt)） |
| **R-002** docstring/`_is_mobi` 声称 AZW3 | P2 | **fixed ✓（选项②）** | 类 docstring 与 `_is_mobi` 均收窄为「KF7 picture MOBI」；KF8/AZW3 typed fail-closed 且 **BLOCKED 登记带解锁条件**（专门切片在真实 AZW3 样本上验证 KF8 树；双格式容器的 `mobi7/` 树为未验证降级转换、同不信任）——该口径同时写入 Task 修订记录与 STATUS 台账行 ✓ |
| **R-003** 适配器 `MISSING_DEPENDENCY` 分支零覆盖 | P3 | **fixed ✓** | 用例改为 `monkeypatch.setitem(sys.modules, "mobi", None)` 驱动适配器**自身** import 守卫（double 已删除），断言 detail 含 "unavailable" + 受控根不存在 ✓ |
| **R-004** 坏图页仅单页用例 | P3 | **fixed ✓** | 新增 3 页中段坏图：页 1 保留（`source_order == [1]`）、页 2 `INVALID_DOCUMENT`、页 3 `pending_after_cancel == ("mid.mobi (page 3+)",)`、`cancelled is False`，与 PDF 先例同口径 ✓ |
| **R-005** 断言映射不准（自称查受控目录，实际只查 `sink.pages`） | P3 | **fixed ✓** | DRM/截断/text-only/KF8/missing-binding 五个用例新增 `not managed_root.exists()`；我在探针中独立确认该断言**有区分力**（受控根在失败路径下确实不存在，若写入即失败）✓ |
| **R-006** 口径表述与实际不符 | P3 | **接受勘误 ✓** | 精确口径＝**Git Bash 会话内启动 `powershell.exe`**（继承会话 PATH、`openssl` 可用）→ 6 条既有 `tests/network` TLS 用例真实执行并通过，故 `0 skipped`；Reviewer 直接启动的 PowerShell 下为 `822/6`（本次修订 `831/0` ↔ 我 `825/6`），**总数一致、无屏蔽、无新增 skip**。该解释与我的实测完全吻合，**接受**，并已写入 Handoff 修订节（与 STATUS flaky 节 TASK-037 登记的口径差异同源） |
| **R-007** 未注入绑定仍写 "MOBI rasterisation is BLOCKED" | P3 | **fixed ✓** | detail 改为 "the MOBI payload was recognised but no MOBI binding was injected (MOBI support landed with TASK-041)"，测试断言同步更新为 `no MOBI binding was injected` ✓（我在定向用例中确认通过） |

**AC 回勾**：**AC ② 现判 PASS**（收集面/排序/重复策略在收敛后的范围内与既有文档导入一致，且有判别力）；**AC ③ 现判 PASS**（矩阵各行均触达真实分支 + 受控根断言）。二者由集成提交回勾。

**scope creep**：无。本次修订只动首轮 finding 指向的两处生产代码，未顺带改其他行为。

**新增观察（不阻塞，未要求本切片修）**：

| ID | 级别 | 位置 | 说明 |
|---|---|---|---|
| R-101 | P3 | `src/infrastructure/importing.py` 收集段 | 记录号相同的两个不同扩展名（`image00002.jpeg` + `image00002.png`）会被当成**两页**（我的探针用例 6 实测 `pages=2`）。真实 `mobi` 绑定对同一 record 只写一个文件（`image%05d.%s`，类型由该 record 决定），故**当前不可达**；若要更硬，可按 record 号建 dict（后者覆盖）。登记为观察项，不影响本次批准 |

## Architecture（executed）

- 依赖方向与端口面不变；新逻辑（收集/排序/KF8 判定）全部落在适配器内部，未把提取器的命名约定泄漏进 application 层 ✓
- **fail-closed 先于收集**：`mobi8/` 判定在页面收集之前执行，且失败路径 `shutil.rmtree(tempdir)` 后抛 typed（不遗留临时树）✓
- **临时资源**：`.mobi` 临时文件仍 `finally` unlink；提取树 owner 仍是 handle，`service.py:259` 的 `finally: handle.close()` 未变 ✓
- **收敛后的不变式（代码+注释+断言三者一致）**：页面 = `mobi7/Images/` 下 fullmatch `image%05d.<ext>` 的文件，按名字里的 record 号**数值**升序；cover/HD 永不成为页面；KF8 树永不导入 ✓（三点均有断言：噪声树用例、排序用例、KF8 用例）
- 残余风险：真实 KF7 容器的命名若与合成容器不同（如 4 位或 6 位 record 号），会被"排除"而非报错——PDB 的 record 计数字段为 2 字节、`%05d` 恒为 5 位，故该风险在格式层面不成立；已在 docstring 写明命名契约。

## Verification（executed）

口径：pwsh + `G:/CODEX/New Manga.task-envs/TASK-012-py312`、`PYTHONDONTWRITEBYTECODE=1`、`-p no:cacheprovider`；被审树 = 作者 worktree（只读）。

| 场景 | 命令 / 步骤 | 结果 | 证据 |
|---|---|---|---|
| 定向 | `pytest tests/import_formats tests/library -q -rs -rf` | **PASS** 68 passed / 0 skipped，exit 0 | [reviewer-tests-237e623.txt](../../verification/TASK-041/review-237e623/reviewer-tests-237e623.txt) |
| 全仓 | `pytest -q -rs` | **PASS** 825 passed / 6 skipped，exit 0（6 条 = `tests/network` `openssl unavailable`：`test_connection_tester.py:106`、`test_transport_tls.py:39/47/62/69/83`）；基线 `822/6`（首轮）⇒ +3 恰为新例 | 同上 |
| 首轮探针复跑（反例消失） | `python verification/TASK-041/review-d114253/probe_collect.py` | **PASS** `page_count` 4 → 2，record 序；PROBE B 零残留不变 | [first-probe-rerun-on-revision.txt](../../verification/TASK-041/review-237e623/first-probe-rerun-on-revision.txt) |
| 复审探针（6 用例语义） | `python verification/TASK-041/review-237e623/reviewer_probe_revision.py` | **PASS** 噪声树 2 页 / 数值排序 / 镜像 mobi8 typed / KF8-only typed / 不合规名排除 / 同 record 双扩展名 = 2 页（R-101） | [probe-revision-output.txt](../../verification/TASK-041/review-237e623/probe-revision-output.txt) |
| 判别力 | 修订测试 + `git archive d114253 src`（临时树） | **PASS（判别力）** 3 failed / 9 passed，exit 1 | [discriminating-revised-tests-vs-d114253-src.txt](../../verification/TASK-041/review-237e623/discriminating-revised-tests-vs-d114253-src.txt) |
| 作者留证核对 | 读 `verification/TASK-041/revision/**` | **PASS** 与其自述一致（`probe-collect-after-fix.txt` 与我的复跑逐字相符；`discriminating-pre-fix-d114253.log` 3 failed；定向 68/0；全仓 ×5 = 831/0） | 作者仓库内 |
| 真实 AZW3/DRM 端到端 | — | **NOT_RUN**（无样本；AZW3 已按 R-002 收窄为 BLOCKED） | — |

**独立复跑 vs 复用作者证据**：定向/全仓/首轮探针复跑/复审探针/判别力 = 我独立执行；作者全仓 ×5 的逐次日志、`pre-fix` 全表 = 复用（我核对其汇总行与我的实测一致，未重跑第 3～5 次）。**无冲突**。

## 结论

- **`approved`**：R-001（P1）已修且我独立复现反例消失；R-002～R-007 逐条落实或按裁定接受；无新增 P0/P1。AC ②③ 可在集成提交回勾。
- **集成**：由 Codex 按 §6.6 串行集成（先 `git merge master`，master 现为 `c3d9ed2`），置 TASK-041 `status=done` 并回填 `integration_commit`；在 STATUS 关闭 **TASK-023 遗留①**（MOBI 光栅化 BLOCKED，解锁条件＝用户批准 MOBI 解析依赖，已达成并由本 Task 交付）并登记 **AZW3/KF8 BLOCKED** 新遗留（解锁＝专门切片在真实 AZW3 样本上验证 KF8 树）。
- **剩余风险（登记，不阻塞）**：R-101（同 record 双扩展名，当前不可达）；真实 DRM 样本仍不可得（合成加密容器替代，首轮已如实登记）；AZW3 走 typed fail-closed 而非导入（产品层面为已知限制，已在 Task/STATUS 标注 BLOCKED）。
