---
task_id: TASK-050
reviewer: 独立子对话 Reviewer（非实现作者）
author: ZCode（handoff 5027998）
base_commit: 13561c7（= 切片起点 in_progress + master b30b06e）
reviewed_head: 0525f59
decision: approved_subagent
---

# Review：TASK-050 生产 Pipeline 的设置与 provider 绑定注入

独立复核完成：白名单、代码审读、测试/探针全部实际复跑（非采信 Handoff）。结论：**approved_subagent**，无 P0/P1；2 条 P2（Task 文档状态失真，集成回填时修正）、5 条 P3 备忘。固定 head `0525f59` 可交 Codex 集成。

## 范围与依据

- diff `13561c7..0525f59`（18 文件，+765/−12）；`doc/tasks/TASK-050.md`（AC、允许/禁止范围、测试要求表）；`doc/handoffs/TASK-050-5027998.md`；`verification/TASK-050/**`。
- 逐行审读：`src/application/settings/pipeline_defaults.py`（新）、`src/application/settings/errors.py`、`src/bootstrap/app.py` 相关段（`_load_pipeline_defaults`、装配注入、save 桥接）、`tests/core/test_pipeline_defaults_{service,assembly}.py`（新）、`verification/TASK-050/bindings_probe.py`。
- 对照的 infrastructure 契约（只读）：`SqliteSnapshotProvider.__init__/set_defaults/freeze`（src/infrastructure/sqlite/pipeline.py:763-840）、`build_production_pipeline`（src/infrastructure/pipeline/assembly.py:19-49）、`ProductionHandlers.as_mapping` + `_chain`（src/infrastructure/providers/handlers.py:172-220, 909-917）、`chain_from_binding`（src/infrastructure/providers/fallback.py:164-196）、`_redact`（src/infrastructure/sqlite/pipeline.py:748-760）。
- 未审到部分：主仓库（只读参照）未跑测试（避免写入共享工作区），基线 878 以 `G:/CODEX/New Manga/doc/STATUS.md:7` 台账记录印证。

## 白名单核对（全部 PASS）

- `git diff --numstat 13561c7..0525f59`：src/tests 仅 5 文件（errors.py +9、pipeline_defaults.py +139、app.py +75/−1、两个新测试 +177/+134，合计 534/1）——与 Handoff 声明逐字一致。
- `src/infrastructure/pipeline/**`、`src/application/editing/**`、`src/ui/**`：**零改动**（name-only grep 无命中）✓。
- Schema/migration、`requirements.txt`、`src/ui/qml/**`：零改动 ✓。既有测试文件零改动（tests 仅新增 2 文件）✓；无断言放宽、无新增 skip/xfail ✓。
- doc/STATUS.md 零改动——符合流程：AC⑥ 的台账行属集成后收尾（见 R-001）。

## 代码审读要点（均亲自验证）

1. **分层守卫**：`pipeline_defaults.py` 仅 import `collections.abc/dataclasses/typing/.errors`，无 infrastructure；AST 级守卫测试 `test_application_layer_does_not_import_infrastructure` 扫描整个 application 包（含动态 import），通过。infrastructure 的 import 只出现在 bootstrap 装配处（`from infrastructure.sqlite.pipeline import SqliteSnapshotProvider`），方向合法。✓
2. **容错分支**：`_load_pipeline_defaults` 全路径返回四键 dict——sqlite3.Error → `dict(empty)`；行缺失 → `dict(empty)`；坏 JSON/空串/非 dict → `{}`；ref 非 str → None。装配处 `defaults["settings"]` 等取键不会 KeyError（Handoff 自述的开发期修复"行缺失曾返回 {}"已在现树消除）。`dict(empty)` 为函数内新建，无跨调用共享可变状态。✓
3. **未配置安装**：全空解析 → `settings or None` 等注入 None → `build_production_pipeline` 透传 → `SqliteSnapshotProvider.__init__` 全 None 走 else 分支 `INSERT ... DO NOTHING`（pipeline.py:783-789）——与修前装配（不传四参→None）**同一条代码路径**。行已存在时 updated_at 不被触碰。✓（证据强度见 R-003）
4. **写面 RMW**：save/clear/section 均为 read→copy→四列全量 `_save(**kwargs)`；桥接 `SqliteSnapshotProvider(conn, **kwargs)` 收到非 None 参数必走 `set_defaults` 四列 upsert（`any(item is not None ...)` 对空 dict 也为真，幂等写回），无半列覆盖风险。跨调用者非原子已在 docstring 声明，单写线程（TASK-048）前提成立。✓
5. **known_steps**：`build_production_handlers` 返回 `ProductionHandlers(...).as_mapping()` 即 `dict[str, handler]`，键为 `STEP_DETECT="detect"`、`STEP_OCR="ocr"` 等字符串（handlers.py:100-101,172-184,219-220）；`frozenset(handlers)` 即合法 step 集。写面形状校验 `_PROVIDER_KEYS = ("provider_id","provider_profile_id","id")` 与 `chain_from_binding` 的读取键逐一相同（fallback.py:179），非空字符串/非空 mapping 主键的接受域一致。✓
6. **save 桥接与装配内实例共存**：两实例仅共享同一 conn 与同一行；装配内实例只在构造时投影一次、此后 `freeze` 只读；桥接实例用后即弃、无自有状态；同 conn 顺序事务（`with self._conn`）在单写线程模型下与既有写入语义相同。未发现状态或事务问题。✓
7. **凭据边界**：写面只收"名字/形状"（id 字符串或 naming mapping），无凭据字段通道；settings/bindings/policy 落库经 `_redact`（secret/password/token/api_key/apikey 键递归剥离，pipeline.py:748-760）；probe/测试零凭据文件读写。✓

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-001 | P2 | doc/tasks/TASK-050.md:34 | AC⑥ 含"独立子对话 Review + 集成 + STATUS 台账行"，本提交时点三者均未发生（`integration_commit: null`、STATUS 零改动、本 Review 即其对象），却已勾 `[x]`。Handoff AC⑥ 节自己写明"在本提交后进行"——勾选与披露自相矛盾，属状态提前失真，不影响代码正确性 | Task 文件 diff（AC⑥ 行 `[ ]`→`[x]`）+ frontmatter `integration_commit: null` | 集成回填时以实际完成状态为准；或现在改回 `[ ]` | open（归 Codex 集成收尾） |
| R-002 | P2 | doc/tasks/TASK-050.md:65-66 | "交付与运行记录"段新旧文本残留：同段先称"Handoff：…实际测试：见上方测试要求表"，又残留"原始登记：尚无。Review：尚无。实际测试：尚无（`ready`…）"；句尾残留"**实施尚未开始。**"与同句"实现并取证完毕"矛盾 | Task 文件 diff hunk（+ 行原文） | 集成回填时清理残留句 | open（归 Codex 集成收尾） |
| R-003 | P3 | Handoff AC①/"设计取舍 5" | "逐字节一致"为**设计推理 + 语义断言**，非字节级 diff 实测：依据是 None 注入与修前共用同一 DO NOTHING 代码路径（sqlite/pipeline.py:783-789）、`test_unconfigured_install_keeps_the_empty_fail_closed_bootstrap`（tests/core/test_pipeline_defaults_assembly.py:111-136）断言空行 `'{}'`+typed fail-closed、探针对照为配置态而非未配置态。结论可信，措辞强于证据 | 本报告"代码审读要点 3" | 备忘：后续如需更强证据可在探针加未配置态双树行内容 dump | open（备忘） |
| R-004 | P3 | src/application/settings/errors.py:28 | docstring 收尾 `"""` 顶格（模块内其余缩进一致），纯风格 | 文件原文 | 顺手修 | open（备忘） |
| R-005 | P3 | src/bootstrap/app.py:339,379,523,609-617 | 同一 `pipeline_defaults.settings_json` 列现有两个读取器：`_load_pipeline_settings`（供 TASK-019 provider_runtime）与 `_load_pipeline_defaults`（供 pipeline 注入）。容错语义一致、无漂移，但并存后续易分叉 | 两函数原文与调用点 | 备忘：后续可让 provider_runtime 复用四列读取器的 settings 键 | open（备忘） |
| R-006 | P3 | 本仓库 pytest 收集特性（非本切片引入） | 以**显式文件路径**跑 tests/core 下文件会 collection ERROR（Review 实测：两个新测试文件路径 → "2 errors during collection"）；必须以目录形式跑。判读 `discriminating-new-tests-vs-prefix.log` 时需区分：该 ERROR 来自修前树 `test_pipeline_defaults_service.py` 的 import 失败（目录+`-k` 形式，"24 deselected"），不是路径跑法伪影 | Review 复现实测（见验证表第 5 行） | 备忘：review/取证命令一律用目录形式 | open（备忘） |
| R-007 | P3 | src/application/settings/pipeline_defaults.py:105-120 | `save_settings_section` 的 section 名仅要求非空字符串、值任意 Mapping，无键字符集/嵌套约束，与 binding 校验严格度不对称（AC 未要求，不算缺陷） | 文件原文 | 备忘：TASK-022 设置页接入时再定约束 | open（备忘） |

## 验证（全部实际执行，环境：PowerShell + `G:\CODEX\New Manga.task-envs\TASK-012-py312\Scripts\python.exe`，不设 `QT_QPA_PLATFORM`，worktree `0525f59` 工作区干净）

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| tests/core 目录复跑 | `python -m pytest tests/core -q -rs -p no:cacheprovider` | 0525f59 | **PASS：30 passed, exit 0**（含 6 新用例；30 = 修前 24 + 6，与判别 log 自洽） | verification/TASK-050/review-rerun-core.log |
| 6 新用例收集确认 | `pytest tests/core --collect-only -q` | 0525f59 | PASS：6 条全部列出 | 同上 log + 会话记录 |
| 定向回归 | `pytest tests/core tests/providers tests/pipeline -q -rs` | 0525f59 | **PASS：273 passed, exit 0**（与 Handoff/Task 表一致） | 会话记录（未单独落盘，数字与 run-full 一致口径） |
| 全仓复跑 | `python -m pytest -q -rs -p no:cacheprovider` | 0525f59 | **PASS：884 passed, 1 warning, 0 skipped, exit 0**（= 主仓 STATUS 基线 878 + 6 新，自洽） | verification/TASK-050/review-rerun-full.log |
| AC③ 探针复跑 | `python verification/TASK-050/bindings_probe.py` | 0525f59 | **PASS：WRITE_FACE=ok / PROVIDER_BINDING_SNAPSHOT={'ocr': 'probe-local-ocr'} / STEP ocr -> completed code=None / RUN_FINAL=completed / ECHO_LANDED=1，exit 0** | verification/TASK-050/review-probe-rerun.txt |
| 显式文件路径反例 | `pytest tests/core/test_pipeline_defaults_{service,assembly}.py` | 0525f59 | 2 errors during collection（仓库既有收集特性，见 R-006；非实现缺陷） | 会话记录 |
| 证据链复核 | 读 discriminating log、probe-{pre,post}-fix-run{1,2}.txt、full-suite run{1..5}.log | — | PASS：数字/路径/退出码自洽（"24 deselected, 1 error"；pre/probe 与 post/probe 五字段与 Handoff 表逐项一致；run4 现存 884 passed；probe 脚本设计双树兼容，AttributeError→unavailable 分支与 pre-fix 输出吻合） | verification/TASK-050/**（既有） |
| 白名单/分层 | `git diff --numstat`、name-only grep、`test_architecture.py` 守卫 | 13561c7..0525f59 | PASS（见上两节） | 本报告 |

## 视角状态声明

- **Architecture：executed** —— 分层方向、注入契约（`build_production_pipeline` 五可选参、`SqliteSnapshotProvider` kwargs 语义）、存储写语义（四列全量 upsert / DO NOTHING）、凭据边界全部核过，无违规。
- **Verification：executed** —— 上表全部实际运行，无 NOT_RUN 项（真实端点 NOT_RUN 为窗口既定排除，Handoff 已声明，非本 Review 缺口）。
- **Standards：executed（要点）** —— 允许/禁止范围全过；无断言放宽/skip；QML 未触碰。剩余为 R-004 风格项（无记录标准管辖，Fowler baseline 下的琐碎项）。
- **Spec：executed（要点）** —— AC①②③④⑤ 逐条有测试或双树证据支撑，AC⑥ 部分后置（R-001）；未发现 scope creep（diff 未引入 AC 外行为）；未发现"看似实现但有误"项。

## 结论与复审

**decision: approved_subagent**（reviewed_head `0525f59`）。

- 理由：白名单零越界；核心机制（同一存储行、读面 None 注入保持原分支、写面复用 snapshot provider 持久化、known_steps 来自 handler 表）实现与声明相符且有测试/探针双级证据；全部复跑实测通过（30 / 273 / 884 / 探针五字段全绿，exit 均为 0）。
- 剩余风险：R-001/R-002 的 Task 文档状态失真须在集成回填时修正；R-003 表明"逐字节一致"属强措辞弱证据（结论本身可信）；RMW 非事务与单值绑定（无 scope 级解析）为已披露边界，属后续 Task 范围。
- 可交 Codex 集成；集成后按协议回填 Task/STATUS 并由 Codex 另行记录集成测试。本报告未做任何 git commit。
