---
task_id: TASK-045
reviewer: Codex（非作者外部 Review）
author: DeepSeek Harness
fixed_base: 1c171dcdeafc1cbffe6111d503dd0cd598e22dea
merge_base: 6ea3dd39d1bb2b61c49af2ead070f40b83a9b9b4
reviewed_head: 1171bc5eae390639c4caf9d8be5e27069dfba4de
implementation_commits: 602cca8a7bd2a5c850f289d2b73d8f4d6e1d4658+1171bc5eae390639c4caf9d8be5e27069dfba4de
branch_head: 04a700a60dd5231aef081e0f0b35d0ece8c8285a
decision: changes_requested
---

# Review：TASK-045（TASK-020/038 尾项 F-4/F-5/F-8/F-11/F-13/F-14 + TASK-042 R-01～R-04）

Reviewer=Codex（**非作者**；作者=DeepSeek Harness）。固定 `base=1c171dc`（Task 固定 base；开工 fast-forward 到 `6ea3dd3`，故实际 merge base = `6ea3dd3`）、被审 `reviewed_head=1171bc5`（实现两提交 `602cca8` + `1171bc5`）、分支 head `04a700a`（+ Handoff / Task 记录 / README）。

**decision = `changes_requested`** —— **R-001（P1）**：分块（tiled）webtoon 视图**在打开时（保存偏移为 0）与每次翻页之后都不会再请求瓦片**：delegate 全部 `source` 为空、同时整图回退 `<Image>` 因 `tilesActive` 为真被隐藏 ⇒ 用户看到**空白带**，直到发生一次滚动（`contentY` 变化）。这正是本 Task 要消灭的"分块视图看不到内容"，本次只修掉了 Repeater 那一半原因。按 §6.8，`1171bc5` **不集成**，退回作者；**TASK-045 保持 `in_progress`**，F-4/F-5/F-8/F-11/F-13/F-14 与 TASK-042 R-01～R-04 **均不登记关闭**。

**执行口径（用户 `[$code-review]` 调用）**：四轴 = Standards / Spec / Architecture / Verification，逐轴声明、分别报告、**不跨轴排名**。技能第 4 步要求 Standards/Spec 各由**并行 sub-agent** 运行；**本会话无 sub-agent 通道**（与 2026-09-18 用户取消双轴强制的原因一致），两轴由同一 Reviewer 单遍执行，**偏差在此声明**（不生造"两遍隔离"）。技能第 1 步（issue tracker）不适用：仓库无 tracker；按协议 §6.2 顺序取 Task 文件 AC → 其引用的 [TASK-042 Review](TASK-042-fc6c649.md) → STATUS 用户裁决。

## 范围与依据

- **被审 diff = `6ea3dd3..1171bc5`**：23 文件 `+1919/−38`、**0 删除**（`git diff --diff-filter=D` 空）。生产面 5 文件 `+180/−37`：`src/infrastructure/imaging/webtoon_tiles.py`（47/8 → 实为 77/19）、`streaming_png.py`（47/8）、`src/ui/viewmodels/reader/viewmodel.py`（24/4）、`src/ui/viewmodels/bookshelf/viewmodel.py`（7/2）、`src/ui/qml/reader/ReaderView.qml`（25/4）。测试 4 文件（`+721/−1`，唯一删除行是一处 import 行改写：`QQuickWindow` → `QQuickItem, QQuickWindow`，属**加强**非放宽）。证据 14 文件；文档提交 `04a700a` 含 Handoff、`doc/tasks/TASK-045.md`、`verification/TASK-045/README.md`。
- **受影响调用链**（实际读过）：`ReaderView.qml`（`webtoonScroll.onContentYChanged` / `tileScale()` / `tilesHost` + `Repeater` delegate）→ `ReaderViewModel.requestTiles(top, bottom, scale)` → `TiledPageRasterizer.ensure_viewport` → `tile_file` → `_decode(tile)` → `grid.decode_rect(tile)` + `StreamingPngReader.read_band` → `TileCache` / 磁盘瓦片文件。
- **未审到**：真机 GUI 屏幕级像素（作者如实声明未做截图）；AZW3 无关；`tests/network` 的 6 条 skip 与本切片无关。

## Standards（executed）

轴状态 **`executed`**。

- **白名单**：`src/` 改动恰为 5 文件、`tests/` 恰为 4 文件，均落在 Task 允许路径内；`src/ui/qml/` **仅** `reader/ReaderView.qml` ✓；未碰 Schema/migration、依赖清单、pipeline seam、`AGENTS.md`、其他 Task、`doc/STATUS.md` ✓；越界 0 ✓
- **断言纪律**：无新增 `skip`/`xfail`（diff 中 `skip` 命中全在作者的证据文本里）；既有断言零放宽——唯一删除的测试行是 import 加强；`tests/reading_export` 与 `tests/core` 的既有用例（TASK-020 几何/带宽、TASK-037 滚动、TASK-042 表征钩子）逐条仍通过 ✓
- **证据纪律**：全仓 `827 passed / 6 skipped`，我复跑 2 次逐次一致（`-rs` 逐条给出 6 个 `openssl unavailable`）；基线 `813/6` ⇒ +14 恰为新增 14 例 ✓
- **代码风格/一致性**：新增代码沿用既有形状（dataclass 化的 `TileSpec`、docstring 引用证据路径、`_TILE_CACHE_FORMAT` 显式版本化）；`import os` 已随 `os.stat` 一并移除，无死导入 ✓
- **smell baseline（判断项）**：① possible Mysterious Name —— `ReaderView.qml:265` 的 `visible &&`（见 R-003）；② possible Divergent Change —— 无（`webtoon_tiles.py` 本切片只改几何/缓存/注释三件相关事）。

**本轴小结**：0 项硬性违反、1 项判断项（R-003）。本轴最严重项 = R-003（P3）。

## Spec（executed）

轴状态 **`executed`**。来源=`doc/tasks/TASK-045.md` AC ①～⑦ + §追加范围 AC ⑧～⑪。

| AC | 判定 | 我的独立验证 / 依据 |
|---|---|---|
| ① F-4 落盘几何（选项①裁到内容带） | **PASS（我独立复现）** | 我**自写 PNG 编码器**造 60×1200 行号页（tile 500 / overlap 40），用生产 `TiledPageRasterizer` 落盘后用 Qt 解码逐行比对：文件高 **500/500/200 == 声明**、每块行 == 该页对应行、**拼接 == 整页 1200 行**；同一探针在 base `6ea3dd3` src 上 **540/240≠声明、拼接 1280≠1200** ⇒ 判别力成立（见 `verification/TASK-045/review-1171bc5/`） |
| ② F-5 翻页不显示旧图 | **PARTIAL → FAIL（生产路径）** | VM 侧成立（换页即重建、旧 url 清空）；QML 侧作者断言成立（delegate `source` 换页后清空、再请求换到新页文件）——但**该断言由测试自己调用 `vm.requestTiles()`**；我的 QML 探针**不调用**它，实测：打开（偏移 0）与 `nextPage()` 之后 sources 均为空且整图回退被隐藏 ⇒ **用户看到空白带**（**R-001，P1**） |
| ③ F-8 `result=` | **PASS** | `@Slot(str, list, result="QVariantMap")`；`test_document_import_slot_publishes_a_return_type` 用元对象断言 `returnType == QVariantMap` ✓（我复跑通过） |
| ④ F-11 坐标口径 | **PASS** | `requestTiles(top, bottom, scale=1.0)` 以 `floor/ceil` 做显示→页像素换算；QML `tileScale() = hostWidth / pagePixelWidth` 与 delegate 的 `height = content_height × hostWidth / pageWidth`（`ReaderView.qml:269-272`）**同一比值** ⇒ 口径自洽（读码 + 测试） |
| ⑤ F-13 注释 | **PASS** | `src/infrastructure/imaging/**` 内 `setClipRect` 出现 0 次；模块 docstring 改为"Qt handler 先分配整图"根因口径（我复跑作者 `comment_drift_check.py`，exit 0） |
| ⑤ F-14 缓存键 | **PASS** | 键 = `_TILE_CACHE_FORMAT` + `sha256(源内容)` + 页/块几何 + index；同尺寸原地改写用例成立（读码 + 作者用例通过） |
| ⑥ 回归与证据 | **PASS** | 定向 `tests/reading_export tests/core` **130 passed / 0 skipped**；全仓 **827 passed / 6 skipped ×（作者 5+1 次、我 2 次）**；逐目录 reading_export 106/93、core 24/23、ui_shell 46/46、workbench 51/51 |
| ⑦ Handoff/取证/非作者 Review | **PASS（本文件）** | Handoff 完整、证据入库、未验证项与"判别力不计入的 3 例守卫"如实声明 |
| ⑧（R-01）`zlib.error` typed | **PASS** | `_pump` 包成 `StreamingPngError("CORRUPT_DATA")`；用例覆盖结构合法/载荷损坏 → typed，且端到端经 VM 优雅退化（读码 + 用例通过） |
| ⑨（R-02）内存声明 | **PASS（口径已修正）** | 实测构成 = 常驻压缩源 + 2× 最大 IDAT 块 + O(带)；单 IDAT 400×10000 额外 24.1MB（23× 带）、1000 行分块 5.7MB（我复跑探针，数字一致） |
| ⑩（R-03）rewind 成本 | **PASS（文档化 + 实测；前提被推翻，见下裁定 ①）** | 我复跑 `rewind_cost_probe.py`：400×20000 全页 25 块 = **24 次 rewind**；1600×8000 单块 4.406s → 两块同调用 13.281s（多 8.875s）；脚本自述"quadratic in tiles，TASK-042 的说法仅在 overlap=0 成立" |
| ⑪（R-04）真实编码器 × 超大 | **PASS** | 我复跑 `memory_and_fixture_probe.py`：Qt 编码 1600×20000、`read_band(12000, 2000)` 与 Qt `constBits()` **identical=True**；Qt 编码件最大 IDAT = 8192（libpng 风格） |

**scope creep**：无。新增范围均在 §追加范围（AC ⑧～⑪）与 Task 白名单内。

**本轴小结**：10 项 PASS、1 项 PARTIAL/FAIL（AC ②→R-001）。本轴最严重项 = R-001。

## Architecture（executed）

轴状态 **`executed`**。

- **分层/依赖方向**：改动只在 `infrastructure/imaging`（纯 stdlib，无 Qt）、`ui/viewmodels`、`ui/qml/reader`；端口面（`tile_factory`、`DocumentRaster` 形状）未动；QML 仍不直接访问文件/DB（只消费 VM 暴露的行与 url）✓
- **持久化/缓存语义**：瓦片是**可重建像素缓存**，键含内容摘要 + 格式版本；`_TILE_CACHE_FORMAT="v2"` 使旧瓦片自然失效 ✓（但旧文件不清理，见 R-005）
- **资源生命周期**：`_in_flight` 保护重复解码；解码失败走 `OSError`/typed 路径；`ensure_viewport` 返回路径而非字节，QML 侧只读文件 URI ✓
- **边界缺陷（= R-001）**：渲染触发面**只有一个**（`onContentYChanged`）。VM 在打开/换页时把行重建为 `url=""` 并隐藏整图回退，却不重新物化；QML 也没有 `tilesChanged`/`visible` 触发。**这是"状态已就绪但无人驱动"的架构缺口**，不是纯视觉问题——可用对象树断言稳定复现。
- **可达性**：生产装配注入 `tile_factory`（TASK-038）⇒ webtoon 章节默认走分块路径 ⇒ 打开即空白可达（我的 QML 探针用真实 `ReaderView.qml` + 真实 VM 复现）。

## Verification（executed）

轴状态 **`executed`**。口径：pwsh + `G:/CODEX/New Manga.task-envs/TASK-012-py312`、`PYTHONDONTWRITEBYTECODE=1`、`-p no:cacheprovider`；被测树 = 作者 worktree（只读）。

| 场景 | 命令 / 步骤 | commit | 结果 | 证据 |
|---|---|---|---|---|
| 定向 | `pytest tests/reading_export tests/core -q -rs -rf` | `1171bc5` | **PASS** 130 passed / 0 skipped，exit 0（复跑 2 次） | [reviewer-tests-1171bc5.txt](../../verification/TASK-045/review-1171bc5/reviewer-tests-1171bc5.txt) |
| 全仓 | `pytest -q -rs` | `1171bc5` | **PASS** 827 passed / 6 skipped，exit 0（复跑 2 次）；6 条 = `tests/network` `openssl unavailable`（`test_connection_tester.py:106`、`test_transport_tls.py:39/47/62/69/83`） | 同上 |
| F-4 独立复核（自建页 + Qt 比对） | `python reviewer_probe.py`（section A） | `1171bc5` src | **PASS** 高 500/500/200、逐块行 == 页行、拼接 == 整页 | [reviewer-probe-delivered.txt](../../verification/TASK-045/review-1171bc5/reviewer-probe-delivered.txt) |
| F-4 判别力（base src） | 同上，`TASK045_SRC=<git archive 6ea3dd3 src>` | `6ea3dd3` src | **PASS（判别力）** 540/240≠声明、拼接 1280≠1200 | [reviewer-probe-base-6ea3dd3.txt](../../verification/TASK-045/review-1171bc5/reviewer-probe-base-6ea3dd3.txt) |
| F-5 生产触发（QML，**不**手动请求） | 同上（section B，真实 `ReaderView.qml`） | `1171bc5` | **FAIL（R-001）** 打开后 `tilesActive=True / host.visible=True`、host 内 3 个视觉子项、**sources=[]**、`readerPage.visible=False`；`nextPage()` 后同样全空；一次 `contentY` 变化后即出现 2 个新页文件 | 同 F-4 两个文件 |
| 作者三探针复核 | `python verification/TASK-045/{memory_and_fixture_probe,rewind_cost_probe,comment_drift_check}.py` | `1171bc5` | **PASS** 三次 exit 0，关键数字与作者留证一致（见 AC ⑨/⑩/⑪ 行） | [author-probes-rerun.txt](../../verification/TASK-045/review-1171bc5/author-probes-rerun.txt) |
| 真机屏幕像素 | — | — | **NOT_RUN**（作者已声明未做截图） | — |

**独立复跑 vs 复用作者证据**

- **我独立复跑**：定向与全仓（各 2 次）、F-4 的自建页逐行比对与 base 判别力、F-5 的 QML 生产触发探针、作者三探针（内存/rewind/注释）、`grep` 面（`requestTiles` 唯一调用点、`setClipRect` 计数、`application→infrastructure` 无关面）、测试删除面（`--diff-filter=D` 与逐行删除扫描）。
- **复用作者证据（未重跑）**：作者全仓 ×5 的逐次日志（我核对其汇总行一致，未重跑第 3～5 次）、`pre-fix-tests.txt` / `pre-fix-failure-summary.txt`（10 failed / 120 passed 的判别力全表）、`pre-fix-delegate-diagnosis.txt`（Repeater 遮蔽的隔离过程）、`full-suite-runs.log`、`baseline-master-6ea3dd3.txt`。
- **无冲突**：我测得的 130/0 与 827/6 与作者留证一致（同口径）。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-001 | **P1** | `src/ui/qml/reader/ReaderView.qml:249`（唯一 `requestTiles` 调用点）+ `src/ui/viewmodels/reader/viewmodel.py:289-322`（`_rebuild_tiles` 只清 url） | 打开 webtoon 章节且保存偏移为 0 时、以及每次翻页之后，**没有任何组件请求瓦片**：`tiles` 行存在但 `url=""`，delegate `source` 全空，同时 `readerPage.visible = !tilesHost.visible` = false ⇒ **空白带直到下一次 `contentY` 变化**。作者的 F-5 QML 断言看不出这一点，因为断言前它自己调用了 `vm.requestTiles()` | [reviewer-probe-delivered.txt](../../verification/TASK-045/review-1171bc5/reviewer-probe-delivered.txt) 的 B1/B3（`sources=[]`、`readerPage.visible=False`）与 B2/B4（滚动后出现新页文件） | 补一个真实触发：QML 侧连 `tilesChanged`/`stateChanged`（或在 `tilesHost` 变可见时）请求一次当前视口，或由 VM 在重建后按上次视口自行物化；并把 QML 用例改成**不手动调用 `requestTiles`** 断言"打开即服务 / 换页后自动服务新页" | **open** |
| R-002 | P2 | `src/infrastructure/imaging/webtoon_tiles.py:250-251` | 类 docstring 仍写 "``visible_tiles`` is ascending, so one ``ensure_viewport`` call needs **at most one rewind**" —— 与同文件方法 docstring（`:352-365`）、测试 docstring（`test_webtoon_tiles.py:589-598`）、实测（24 次 rewind）与作者 Handoff 关键技术点 5 **直接矛盾**。这正是本 Task 要修的"注释与实现不一致"（F-13）同类缺陷，且作者的 `comment_drift_check.py` 只覆盖 F-13/F-14 两项、不会发现它 | `rewind_cost_probe.py`（我复跑）：25 块全页 = 24 次 rewind；3 块用例 = 2 次 | 把类 docstring 改为实测口径（升序只保证"不向后跳"；`overlap>0` 时每个非首块各 rewind 一次、成本随块数平方），与 `:352-365` 一致 | **open** |
| R-003 | P3 | `src/ui/qml/reader/ReaderView.qml:265` | 同一行里 `visible` 仍是**未限定名**：在 Repeater 作用域内解析为 Repeater **自身**的 `visible`（`Item.visible`，默认 true）⇒ `visible &&` 是无条件真，残留自同一次遮蔽陷阱。今天无副作用（非分块模式下行清单为空），但读起来像是"仅当宿主可见才建 delegate" | 读码 + 我的探针（`host.visible` 为真；未分块时 `model.tiles` 为空） | 改为 `tilesHost.visible && rv.model` 或删掉该子句 | open |
| R-004 | P3 | `src/infrastructure/imaging/webtoon_tiles.py:374` | `keep_rows = min(tile.content_height, band_height - offset)` 会把**读取短于请求**的情形静默截短，写出比声明矮的瓦片文件（只有 `<= 0` 才报错）。当前几何下 `band_height - offset == content_height` 恒成立，属防御性容忍；但一旦读取契约变化，症状是"图短一截"，与 F-4 要消灭的症状同类 | 读码（`decode_rect` 与 `read_band` 契约） | 改成显式相等断言/`!=` 即抛 typed，或至少加注释说明该 `min` 只作防御 | open |
| R-005 | P3 | `webtoon_tiles.py:41-44`（`_TILE_CACHE_FORMAT`）+ `_cache_key` | 格式版本一升，旧键文件永不命中、也无人回收 ⇒ 缓存目录多一份一次性孤儿文件（可重建像素缓存、不触业务数据，量级有限） | 读码 | 在 Handoff/注释写明"升版会遗留旧文件，需手动或由清缓存入口回收"；或后续切片加一次目录清扫 | open |

## 待 Reviewer 裁定（4 项）

1. **AC ⑩ 的口径与 TASK-042 措辞（用户要求给动作）** —— **裁定：接受"文档化 + 实测"（选项 (a)），并由我（Codex）在集成时更正 TASK-042 的措辞**。理由：解码策略与生产 `overlap` 属 TASK-042 面且 `src/bootstrap/app.py` 不在本 Task 白名单，TASK-045 的 AC 只要求"写明 + 记入实测"，作者已做到且实测被独立复现。**最终口径（入 STATUS）**：`visible_tiles` 升序只保证"不向后跳"；生产 `overlap=64` 使**每个非首块各 rewind 一次并从 row 0 重扫**，全页扫掠为 **O(块数²)**（400×20000 = 24 次；1600×8000 两块多付 ≈8.9 s）；TASK-042 的"全页只需一次顺序扫描"**仅在 `overlap=0` 时成立**。**另附建议（非本 Task 阻塞项）**：F-4 裁剪后 overlap 对输出已无贡献，`overlap=0` 是零显示代价的单点改动 ⇒ 建议你批准一个解码面小切片处理（我不在本切片内改）。
2. **AC ⑨ 的构成式** —— **接受入库**：常驻压缩源 + 2× 最大 IDAT 块 + O(带)；R-02 的"≈3–4× 带宽"仅在分块 IDAT 下近似成立，单块编码时块项主导（我复跑探针数字一致）。集成时在 STATUS 记明该口径修正。
3. **F-8 是否需要 QML 入口** —— **本 Task 不要求**：F-8 的 AC 是"补 `result=` 并接线验证"，已达成；书架页要不要文档导入入口属新产品能力，**由用户另行裁决**，不阻塞本切片。
4. **Repeater 遮蔽缺陷的归属** —— **需更正历史表述**：分块视图自 TASK-020 起从未创建 delegate ⇒ TASK-020 的 Review/STATUS 中"QML webtoon viewer tiled 分支已交付"的表述**过强**；TASK-038 的"tile 在生产可达"亦只到"装配可达"而非"渲染可见"。集成时我按惯例**不改已入档 Review 正文**，以 STATUS 勘误行 + Review 尾注登记；本 Task 的修复不因此回滚。

## 结论与复审

- **不可集成**：`decision = changes_requested`。R-001（**P1**）是用户可见的空白分块视图，直接落在本 Task 的 F-5 目标里；其余 1×P2 + 3×P3 可随同一修订一并处理，其中 R-002 只需改注释。
- **已通过且无需返工**：F-4（我独立复现 + 判别力）、F-8、F-11、F-13、F-14、AC ⑧（typed 损坏载荷）、AC ⑨（内存构成）、AC ⑪（真实编码器超大页）、回归与证据纪律、白名单与断言纪律——这与 §6.8"只有 P0/P1 未解决才不能批准"一致：返工面**很小**（一个触发点 + 一处注释 + 三个可选清理）。
- **状态影响**：**TASK-045 保持 `in_progress`**；F-4/F-5/F-8/F-11/F-13/F-14 与 TASK-042 R-01～R-04 **不登记关闭**；`integration_commit` 待复审通过后由我执行（届时先 merge master `fe59360`）。本次 Review **未修改**作者分支、生产代码、Schema、依赖或其他 Task；复审证据在 `verification/TASK-045/review-1171bc5/`。
- **复审要求**：新 head + 逐条 disposition；R-001 须给出**不手动请求**的 QML 断言（打开即服务 / 换页后自动服务新页），且该断言对修前 `1171bc5` 失败；R-002 改注释后可复跑 `comment_drift_check.py` 校验。
