---
task_id: TASK-019
reviewer: DeepSeek Harness
author: Codex
base_commit: 36242fb00f9f432ec66cc3c33afc167578d0f341
reviewed_head: ab26601
metadata_head: b03cf57
kind: 尾项切片独立 Review（Standards S-1 / S-2 分层修正）
decision: approved
---

# Review：TASK-019 尾项切片（Standards S-1 / S-2 分层修正）

Reviewer=DeepSeek Harness（**非作者**），作者=Codex。固定对象：`base_commit=36242fb`（代码基线）、`reviewed_head=ab26601`（切片交付）、元数据/分支 head `b03cf57`。切片 diff 基准为其直接父提交 `0389eb4`（= 含 post-hoc Standards 附录的起点）。本 Review 在分支工作区**只读**执行：除本报告文件外未修改、未新增、未删除任何文件（`git status --short` 为空）；未 push、未合并 master、未释放其他冻结 Task。

**已集成基线不重审**：TASK-019 主体 `integration_commit=3755af9`（Review `doc/reviews/TASK-019-726baf5.md` approved）的结论不因本切片失效；本报告只审 `ab26601` 的分层修正及其对既有 640 项测试的行为不变性。

## 范围与依据

- 切片改动（`0389eb4..ab26601`）= **10 个文件**，全部落在 TASK-019 允许范围内：`src/application/translation/inpaint/{route_catalog,protection,router,step}.py`、`src/infrastructure/providers/{handlers,inpaint_routes,runtime}.py`、`tests/providers/{test_inpaint,test_handlers_pipeline,test_ports_contract}.py`；元数据提交 `b03cf57` = 3 个文件，同在允许范围（`doc/handoffs/TASK-019-*.md`、`doc/tasks/TASK-019.md`、`verification/TASK-019/**`）。两个提交**均无越界路径**。
- 动因依据（作者）：[`doc/reviews/TASK-019-standards-addendum.md`](TASK-019-standards-addendum.md) 的 **S-1**（记录在案标准违反：Inpaint Step 反向依赖 `infrastructure.providers.*`，违反 `doc/02_TECHNICAL_ARCHITECTURE_.md` §架构方向）与 **S-2**（同根因：路由策略与适配器同居 infrastructure）。任务口径来自 [Handoff `TASK-019-ab26601.md`](../handoffs/TASK-019-ab26601.md) 与 [取证 `layering-fix-ab26601.md`](../../verification/TASK-019/layering-fix-ab26601.md)。
- 本 Review 采用四轴：**Standards**（本切片的主体即 Standards 修复）、**Spec**（是否改变任何 AC 结论/是否 scope creep）、**Architecture**（依赖方向与边界）、**Verification**（作者记录是否可独立复现）。
- 未审到的部分：主体 AC 的实现细节与真实模型/端点层（已于 `726baf5` 审过、`3755af9` 集成；本环境相关项仍 `BLOCKED`/`NOT_RUN`，不因本切片变化）。

## 验证（本 Reviewer 实际执行，全部退出码 0 除注明外）

环境：Windows `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；CWD=分支工作区根；全部加 `-p no:cacheprovider`。历史树对照用 `git archive 3755af9 src tests pytest.ini | tar -x -C %TEMP%/task019-review-3755af9-full`（**只读**导出，不建 worktree、不改分支）。

| # | 命令 / 步骤 | 退出码 | 结果 |
|---|---|---:|---|
| V1 | `python -m pytest tests/providers -q -p no:cacheprovider -rs` | 0 | **111 passed / 0 skipped**（与作者一致） |
| V2 | `python -m pytest tests/pipeline tests/core tests/storage -q -p no:cacheprovider -rs` | 0 | **78 passed / 0 skipped**（与作者一致） |
| V3 | 全仓 `python -m pytest -q -p no:cacheprovider`，**9 次**（3 次 + 6 次两批） | 5×0 / 4×1 | 5 次 `641 passed, 6 skipped`；4 次 `1 failed, 640 passed, 6 skipped`。失败用例：`tests/reading_export/test_qml_contract.py::test_reader_webtoon_swaps_in_vertical_viewer` ×2、`tests/reading_export/test_viewmodels.py::test_start_export_stale_abort_surfaces_failure` ×1（另 1 次工具侧未捕获名称，与作者同一现象）。**0 次失败落在本切片改动路径** |
| V4 | **控制实验**：同一全仓命令在 `3755af9`（切片前）导出树运行 **4 次** | 3×0 / 1×1 | 1 次 `1 failed`，用例为**同一个** `test_viewmodels.py::test_start_export_stale_abort_surfaces_failure` → 该 flaky **先于本切片存在**，与本切片无因果 |
| V5 | 两个 flaky 用例隔离复跑 5 次（HEAD） | 0 | 每次 `2 passed` → 与"全仓串跑才偶发"的顺序/时序敏感特征一致 |
| V6 | **S-1 独立断言**（自写 AST 扫描，非复用作者守卫）：`src/application/**` 的 `Import`/`ImportFrom`（含相对形式）与 `importlib.import_module("infrastructure…")` 字符串 | — | HEAD：**静态 0 / 动态 0**；切片前 `3755af9`：**2 处**（`step.py:23`、`step.py:29`）→ 断言有判别力且已消除 |
| V7 | **S-2 断言**：全仓检索 `inpaint_router` | — | `src/`、`tests/` **0 命中**；仅历史文档（`doc/**`、`verification/**`）提及，属记录而非引用 |
| V8 | **判别力**：`git show 726baf5:src/application/translation/inpaint/step.py` 中 `^from infrastructure` | — | **2 行**；并把新守卫的判定逻辑（行前缀扫描）原样作用于 `3755af9` 导出树 → 2 offenders，作用于 HEAD → 0 offenders |
| V9 | **逐字搬迁核验**（自写 AST 源片段比对）：old `inpaint_routes.py` 的 8 个迁出符号 vs 新 `route_catalog.py`/`protection.py` | — | 全部 **IDENTICAL**（`RouteRecord`/`ROUTE_TABLE`/`route_record`/`provider_id_for_route`/`route_gate`/`describe_routes`/`learned_routes_declared`/`protected_pixel_violations`）；跨模块顶层名 **0 重名**（无重复定义）；**无符号丢失**（old 中不存在于任何新模块者：none） |
| V10 | **router 迁移核验**：`git diff 0389eb4:…inpaint_router.py HEAD:…router.py` + 21 个顶层符号逐一比对 | — | diff 仅 1 行（`route_gate` 导入目标）；21/21 符号 IDENTICAL；无新增/删除符号 |
| V11 | **行为不变性**：同一脚本在 HEAD 与 `3755af9` 导出树分别输出 `providers.status()`（12 Provider 状态/错误码、GPU、模型数、route policy） | 0 | 两份 JSON **IDENTICAL**（`ready` 2、`missing_dependency` 2、`not_configured` 4、`not_ready/PROVIDER_NOT_IMPLEMENTED` 4；GPU `unavailable`；`model_count=0`；allowed=`[brushnet, edge-bleed, simple-fill]`、color=`brushnet`） |
| V12 | **测试数增量归因**：`3755af9` 导出树跑 `tests/providers` | 0 | **110 passed** → HEAD **111 passed**，唯一增量 = 新守卫 `test_application_layer_never_imports_infrastructure`（单独收集确认，`--collect-only` 命中） |
| V13 | 行数归因：`git diff --numstat 0389eb4..ab26601` | — | `+235/−165`：新增 `route_catalog.py` 152 + `protection.py` 36（迁出代码的落点）、守卫 +22；`inpaint_routes.py` `−149/+9`（迁出 + 导入改向）、`step.py −5/+3`、`handlers.py 2/2`、`runtime.py 1/1`、测试导入同步 `1/1`、`8/6`。无来源不明的净增逻辑 |
| V14 | 边界：`git diff --name-only`（切片与元数据提交分别与允许范围正则比对）；`git diff --check 36242fb..ab26601` | 0 | 切片 10/10、元数据 3/3 全部在允许范围；越界 **0**；空白检查无输出。`36242fb..HEAD` 全范围内仅 `doc/STATUS.md`、`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/tasks/README.md` 4 个**集成方所有**的文档出现在范围中，其改动提交为 `d66c3c5`（集成收口）与 `46414df`/`7847750`/`c9eb65a`（释放/改派），**均非本切片** |

**skip 原因**：V1/V2 `0 skipped`；V3/V4 的 6 项 skip 全部为既有 `tests/network` 的 `openssl unavailable`（与本切片无关）。

## 重点确认（用户点名 ①~⑤）

| # | 结论 | 证据 |
|---|---|---|
| ① 搬迁是否逐字无损（router 仅 1 行导入变化、迁出代码无改写） | **是** | V9（8 个迁出符号全 IDENTICAL、无重名、无丢失）、V10（router 仅 1 行、21/21 符号 IDENTICAL）。迁出/迁入仅新增模块 docstring 与导入语句 |
| ② 行数/测试数增量是否只来自新增守卫 | **是** | V12（110 → 111，增量即守卫）、V13（`+235/−165` 与逐文件 numstat 完全对账；新文件行数 = 迁出代码 + docstring/导入，无新增逻辑）。**一处口径不精确**见 T-3 |
| ③ 是否仍有任何 application→infrastructure 依赖 | **否（0 命中）** | V6：静态与动态导入均为 0（我的 AST 扫描强于作者的行前缀守卫）；V7：`inpaint_router` 在 `src/tests` 0 命中 |
| ④ 是否有行为变化（对比 `3755af9` 的 Provider 状态集合） | **无** | V11：两份状态 JSON 逐字段 IDENTICAL；V2/V3 既有测试全绿；V1 110 项逐项不变（仅 +1） |
| ⑤ 未越界、未改依赖/Schema/seam/AGENTS | **确认** | V14：切片 10/10 与元数据 3/3 全在允许范围；`36242fb..HEAD` 中 `requirements*`/`pyproject.toml`/`*.sql`/`sqlite/schema.py`/`infrastructure/pipeline/**`/`application/translation/pipeline/**`/`AGENTS.md` **0 命中**；4 个集成方文档的改动属 Codex 集成职责、非本切片 |

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| T-1 | P2 | `tests/reading_export/**`（测试基础设施，非本切片代码） | 全仓串跑存在**低频顺序/时序敏感 flaky**，且**登记范围不完整**：既有登记只覆盖 `test_qml_contract.py::test_reader_webtoon_swaps_in_vertical_viewer`（TASK-017 `doc/reviews/TASK-017-protocol.md` R-007），我另外观察到同目录 `test_viewmodels.py::test_start_export_stale_abort_surfaces_failure` 以同样特征失败。影响：作者"全仓 5 次 1 次失败未捕获用例名"这一未决现象已被定位；也说明任何"全仓 N passed"记录在本环境不是 100% 稳定。**不构成本切片缺陷** | V3（HEAD 9 次：4 次失败 = 2×`test_reader_webtoon_swaps_in_vertical_viewer` + 1×`test_start_export_stale_abort_surfaces_failure` + 1 次未捕获）、V4（**切片前** `3755af9` 4 次中 1 次同一 `test_start_export_stale_abort_surfaces_failure`）、V5（隔离复跑 5×2 passed） | 把 `test_viewmodels.py::test_start_export_stale_abort_surfaces_failure` 并入既有 flaky 跟踪条目（建议 Codex/后续维护 Task），必要时给这两个用例加 `pump_until` 超时诊断 | open（移交 Codex 跟踪，不阻塞本切片） |
| T-2 | P2 | `src/infrastructure/providers/runtime.py::_route_policy` 与 `src/infrastructure/providers/handlers.py::_route_policy` | **Duplicated Code（判断项，Fowler baseline）**：两份代码解析同一 `settings["inpaint"]["route_policy"]` 契约，但默认值/错误行为不一致——非 mapping 时 runtime 回落 `DEFAULT_ROUTE_POLICY`、handlers 抛 `INVALID_INPUT`；`color_route` 缺省时 runtime 用 `brushnet`、handlers 用 `None`。影响：设置契约"同一输入两种解释"的漂移风险（当前无测试失败）。非本切片新增，但正处 S-2 主题旁边 | 阅读 `runtime.py` 与 `handlers.py` 两个 `_route_policy` 并排比对；V3/V11 证明当前行为一致 | 在 `application/translation/inpaint/router.py` 提供单一 `RoutePolicy.from_settings(settings, default=…)`，两处调用它（可另开尾项切片，不必阻塞本次集成） | open（判断项，非阻塞） |
| T-3 | P2 | `verification/TASK-019/author-verification.md:26`、`verification/TASK-019/changed-paths.txt:13`、`doc/handoffs/TASK-019-ab26601.md` / `verification/TASK-019/layering-fix-ab26601.md` 的"删除已迁出的 158 行" | **文档与最终树不一致（非阻塞）**：(a) `author-verification.md` §1 仍把 `src/infrastructure/providers/inpaint_routes.py / inpaint_router.py` 记为"route 表 + Router 决策"的所在地（该文件已不存在，代码现在 application）；(b) `changed-paths.txt` 是 `6c981c2` 时刻的快照，仍列出已被 `git mv` 掉的 `src/infrastructure/providers/inpaint_router.py`；(c) "158 行"是 `inpaint_routes.py` 的**合并** diff 规模，逐文件实为 `−149/+9`。影响：仅取证文本精度，**不推翻任何 AC 证据或 S-1/S-2 结论** | V9/V10（搬迁事实）、V13（numstat 对账）、`grep inpaint_router`（仅文档命中） | 刷新这两份文件（或在文件头标注"主体取证快照，路径已由 `ab26601` 迁移"），并把行数写成 `−149/+9`；属本切片允许路径内，可在集成时一并处理 | open（文档精度，非阻塞） |
| T-4 | P2 | `tests/providers/test_ports_contract.py:181-196` | **守卫强度与位置的判断项**：守卫是"行前缀扫描"，不覆盖 `importlib.import_module("infrastructure…")` 之类动态导入（当前实测 0 处，无现实缺口）；位置在 `tests/providers/` 是**受允许路径约束的正确选择**（`tests/core/` 不在 TASK-019 允许范围） | V6（我的 AST 扫描含动态导入，HEAD 为 0）、V8（旧树 2 offenders → 判别力成立） | 待 `tests/core/` 可改时，把该守卫迁入 `tests/core/test_architecture.py` 并复用其 `find_forbidden_imports`（AST 版）以覆盖动态形式；本次无需改动 | open（建议，非阻塞） |

**无 P0/P1 finding。** 在"记录在案标准"层面，本切片**未新增违反**，且消除了一条（S-1）。

## 三轴 / 四轴结论

- **Standards**：**通过**。S-1（记录在案标准违反）由本切片消除，且由独立 AST 扫描（V6）在修前/修后两棵树双向证明；S-2（判断项）按附录建议落地（策略/声明/保护规则入 application，光栅实现与 provider 留 infrastructure）。残留判断项 T-2/T-4 均为非阻塞建议，T-1/T-3 属测试基础设施与文档精度。
- **Spec**：**通过（不变）**。本切片不改变任何 AC 判定：AC-INPAINT-001~004、AC-RFULL-002/003/005、AC-FALLBACK、AC-GPU、AC-OPTIONAL、AC-MODEL 的 PASS 证据路径随导入改写仍成立（V1/V2/V3）；AC-RFULL-001 完整链仍为 `BLOCKED`（`color`/`term_extract`/`render` handler 不在允许路径），真实模型质量与端点类仍 `BLOCKED`/`NOT_RUN`——本切片没有把它们写成通过。无 scope creep：10 个改动文件全部服务于同一目标。
- **Architecture**：**通过**。依赖方向恢复 `QML/UI → Application → Domain/Ports → Infrastructure Adapters`：application→infrastructure 0 命中（含动态导入）；infrastructure→application 的新导入方向合法且与既有先例一致（如 `infrastructure/pipeline/assembly.py` → `application.tasks.service`、`infrastructure/importing.py` → `application.importing.images.ports`）。未触碰 pipeline seam 本体、Schema/migration、依赖清单、`AGENTS.md`、其他 Task。
- **Verification**：**通过（含一处作者未决项已闭合）**。作者 V1/V2/V3 的核心数字我全部独立复现（111 / 78 / 641+6skip），并额外给出：切片前树对照（V4，证明 flaky 先于本切片）、守卫判别力模拟（V8）、逐字搬迁 AST 比对（V9/V10）、状态集合字节级对照（V11）、测试数归因（V12）、行数对账（V13）。作者登记的"1/5 次失败、用例名未捕获"已被我定位为 `tests/reading_export` 既有两个 flaky（T-1），**与本切片无因果**。

## 结论与复审

**decision = approved。** 固定对象 `ab26601`（元数据 `b03cf57`）可交 Codex 集成：

1. S-1/S-2 修复真实、完整、零行为变更，且由独立方法（AST 扫描 + 符号级逐字比对 + 状态集合对照 + 修前/修后双向判别力）证实，不依赖作者自述；
2. 无 P0/P1 finding；4 条 P2 均为非阻塞（T-1 测试基础设施跟踪、T-2 可选收敛、T-3 文档精度、T-4 可选守卫增强）；
3. 边界合规：切片与元数据提交的路径全部在 TASK-019 允许范围内，未触碰依赖/Schema/seam/`AGENTS.md`。

**集成时需 Codex 一并处理（属集成方职责，不在本切片允许路径）**：`doc/STATUS.md` 当前仍记 `TASK-019：done`，而分支内 Task 文件为 `in_review`（尾项切片待审）——集成本切片后请把 STATUS/ROADMAP 的状态与 flaky 跟踪条目（T-1）同步更新；`doc/tasks/TASK-019.md` 的尾项切片段可随之收口为 `done`。

**剩余风险**：T-1（`tests/reading_export` 两个用例在串跑下低频失败，会间歇性污染"全仓 N passed"证据——建议后续 Task 处理，但与本切片解耦）；主体遗留的 R-1（prepare-only 并发窗口）/R-2（完整链 handler 归属）/R-5（`sfx_policy` 默认 `'skip'` 跨 Task 缺陷）/R-8（页级 Mask artifact 与 Region 一对多）状态不变，仍按 `doc/reviews/TASK-019-726baf5.md` 与取证 §5 跟踪。

本报告为固定 `ab26601` 的审查记录；如后续修订产生新 head，应追加新记录并给出各 finding 的 disposition，不抹掉本记录。
