---
task_id: TASK-034
reviewer: Codex（非作者独立 Review）
author: DeepSeek Harness
base_commit: b34b27e2bc7c1dbd4c3b15a91f08e158d966f365
slice_base: 34823b9
reviewed_head: 002889e
metadata_head: 132e776
decision: approved
---

# Review：TASK-034 尾项切片（AC ① 第 2 步 + 强制项 R-01）

Reviewer=Codex（**非作者**；作者=DeepSeek Harness）。固定 `base_commit=b34b27e`；本切片 diff base=`34823b9`（已集成的 AC ②③④⑤）；被审 `reviewed_head=002889e`（元数据 `132e776`）。

**执行口径（`code-review` 技能 + 协作协议 §6）**：四轴 = **Standards** + **Spec**（技能两轴，分别报告、不合并、**不跨轴排名**）+ **Architecture** + **Verification**。

**并行偏差声明（§6 第 6 条，强制披露）**：按 `code-review` 技能要求，本次**实际派发了两条独立 worktree 子审查线程**——Standards 轴（`client-new-thread:8ab61adc-abd3-4461-ace6-211644416d14`，标题「TASK-034 · Standards 轴（双轴 1/2）」）与 Spec 轴（`client-new-thread:1c7e88cb-7d78-4cee-98e2-c84e82d262f6`，标题「TASK-034 · Spec 轴（双轴 2/2）」），各自带独立 diff command、commit list 与完整 brief。**派发后 `list_threads` 未见任何新线程**（工作树未创建、线程未取到执行槽），判定为**排在本回合之后**——这是本环境**第三次**出现同一现象（前两次为 TASK-029 双轴、TASK-034 首轮）。故按 §6 第 6 条**兜底**执行：Standards 与 Spec 各做**一遍相互隔离**的检查（独立取材、独立成文），并在此显式声明偏差。**替代做法不等价于并行子代理**，故本报告的 Standards/Spec 两轴不应被解读为"已由独立 context 执行"。

## 范围与依据

- 被审 diff：`git diff 34823b9 132e776` = **17 路径**：`src/` **3**（`application/translation/inpaint/router.py`、`infrastructure/providers/runtime.py`、`infrastructure/providers/handlers.py`）、`tests/` 6、`doc/` 2、`verification/TASK-034/**` 6。越界 **0**；`schema|requirements|pyproject|AGENTS|src/ui/` 命中 **0**。
- 需求来源（Spec）：[doc/tasks/TASK-034.md](../tasks/TASK-034.md) AC ① + **本裁决**：[doc/reviews/TASK-034-ac1-route-policy-ruling.md](TASK-034-ac1-route-policy-ruling.md)（R-1～R-7、Option B、3 文件 `src/` 范围、强制项 R-01）+ [verification/TASK-034/route-policy-decision-request.md](../../verification/TASK-034/route-policy-decision-request.md)。
- **未审到**：真实端点/模型端到端（本 Task 无关，仍 `NOT_RUN`）；`src/ui/**`（不在允许范围）。

## Standards

轴状态：**`executed`**（隔离 pass 1：仓库记录标准 + Fowler baseline）。

- **`AGENTS.md` 范围与角色**：`src/` 改动恰为批准的 3 个文件，未扩展路径；未改 Schema/migration、依赖清单、pipeline seam 本体、`AGENTS.md`、其他 Task；Reviewer 与作者分离。**无硬性违反**。
- **[09 协作协议](../09_COLLABORATION.md) §6**：作者未自批、未自标 `done`（`status=in_review`）；**主动回抛与裁决措辞的差异**而不是迁就措辞（见 R-01）。**合规**。
- **[02 技术架构](../02_TECHNICAL_ARCHITECTURE_.md) 依赖方向**：新增 `handlers.py → runtime.py` 为 **infrastructure→infrastructure 同层**。作者在代码注释里声明"runtime 不 import handlers，故无环"——**我实测 `rg "handlers" src/infrastructure/providers/runtime.py` = 0 命中**，声明属实，无环。**合规**。
- **工具已强制项**：跳过。未新增 `skip`/`xfail`（全仓仍 6 条既有 `openssl unavailable`）。
- **Fowler smell baseline（判断项）**：
  - "**possible Duplicated Code**" — **已消除**：两份 `_route_policy` 与第三份字面量全部删除，`rg "_route_policy" src/` = 0 命中；`from_settings` 成为唯一实现。这正是本切片的目标。
  - "**possible Middle Man**"（新命中，判断项）：`handlers.py` 现在只是把 `run.settings_snapshot` 转交给 `RoutePolicy.from_settings`，而 `runtime.py` 的常量 `DEFAULT_ROUTE_POLICY` 被 `handlers.py` 反向引用——常量与其类型（`RoutePolicy`）分居两个层。建议（非阻塞，见 R-03）：若要彻底"一处定义"，常量可随 `RoutePolicy` 落在 application。**本切片不动属正确取舍**（裁决把常量钉在 runtime 并对齐其值）。
  - "**Primitive Obsession**"（残留，冻结）：`allowed_routes` 为字符串时逐字符展开、`requirements` 非 bool 被 `bool()` 强制 —— 属已登记的 R-07，本切片**正确地未扩张**（并有测试把该冻结行为显式锁定）。

**本轴小结**：**0 项硬性违反记录在案标准**；3 项 baseline 判断项（1 项已消除、2 项为已登记/frozen 或建议）。本轴最严重项 = 无（R-03 为 P3 建议）。

## Spec

轴状态：**`executed`**（隔离 pass 2：AC 原文 → 裁决 R-1～R-7 → 用户/STATUS 裁决）。

- **① 缺失/部分完成：无。** 逐条核对裁决要求：
  - **R-1**（唯一允许差异显式化）：`raw is None → return default`，装配传 `DEFAULT_ROUTE_POLICY`、运行传 `deps.route_policy`；测试以 `is` 断言**身份**相等（不是等值）✓
  - **R-2**（两处一致报错）：`from_settings` 抛 `ProviderInputError("inpaint.route_policy must be a mapping", stage="inpaint")`；**两条调用点各有回归**（装配 `build_provider_runtime(settings=root_with("simple-fill"))` 抛错；运行级策略非 mapping → Step `FAILED/INVALID_INPUT` 且未写出 clean artifact）✓
  - **R-3 / R-4 / R-5**：`allowed or default.allowed_routes`；`fallback_routes=()`；`requirements={}` + `bool(value)` 保留 ✓
  - **R-6 Option B**：`color_route=str(color_route) if color_route else None`；`DEFAULT_ROUTE_POLICY == RoutePolicy()`（allowed 两条基线、color None、requirements {}）✓；显式指定仍生效且并入 allowed（AC-INPAINT-004 正面要求有测试）✓
  - **R-7 冻结**：有专门测试锁定"字符串 `allowed_routes` 仍逐字符展开"= 未扩张 ✓
  - **范围**：`src/` 3 文件收敛（第三份字面量改复用常量）、**路由判定算法零改动**（`decide_route`/`acceptable_routes`/`preferred_route_order`/`route_gate`/`RouterFeatures` 未出现在 diff 中）✓
  - **R-02 登记**：Handoff §「必须记录的裁决后可观测变化」第 3 条 + 取证 §3.3 明写"装配路径由静默回落变为启动期报配置错误，用户需要知道" ✓ **登记充分**
  - **决策不变性**：作者给出四组实测 + 参数化测试；**我另以独立探针复核**（按 `b34b27e` 字面量重建旧默认）得同样结论 ✓
  - **强制项 R-01**：`requires_pyside6`/`_pyside6_available` 移入唯一命名 `reading_export_helpers`，两处显式导入；**全仓已无裸 `conftest` 导入**；**十种顺序全部 exit 0**（超出我要求的"两种参数顺序 + 一个伙伴目录顺序"）✓
- **② scope creep：无。** 所有改动可归到裁决的 R-1～R-6 与 R-01；未顺手改 R-05/R-06/R-07。
- **③ 看似实现但实现有误：无。** 我逐一核对 `from_settings` 与旧两处的语义差异，均落在裁决范围内；`__post_init__` 已负责"显式 color_route 并入 allowed_routes"，故新实现删掉了旧 handlers 里那段重复合并逻辑——**这是正确的去重**（我实测显式 `color_route='brushnet'` 后 `allowed` 确实含 brushnet 且无重复）。

**本轴小结**：**0 项缺失 / 0 scope creep / 0 实现有误**；裁决 R-1～R-7 与强制项 R-01 逐条达成。本轴最严重项 = 无（唯一差异是**裁决文本本身**的措辞，见 R-01，且不属切片）。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-01 | P3（**errata**） | [doc/reviews/TASK-034-ac1-route-policy-ruling.md](TASK-034-ac1-route-policy-ruling.md)（**裁决方文本**，非切片） | 我在裁决中写"四种组合裁决前后**均 BLOCKED**"，实测只有彩色 webtoon / 高复杂度两组 BLOCKED；**线稿 / 普通两组两侧都是 `RUN → edge-bleed`**（`preferred_route_order` 末尾固定追加依赖无关基线）。影响：裁决措辞失准；若不澄清，会被读成"要求线稿/普通也 BLOCKED"，那将是**判定算法**变更 | 作者实测（`ac1-decision-invariance.txt`）+ **我的独立探针**：`colour-webtoon/high-complexity = blocked↔blocked`、`lineart = route:edge-bleed↔route:edge-bleed`、`normal = route:simple-fill↔route:simple-fill`，四组 `decision`+`route` 前后一致 | **勘误已写入裁决文本**（新增「裁决勘误」节）：明确**不需要**任何算法变更，实质要求（选中路线未变）四组全部成立；作者的回抛与处置**正确** | **fixed（裁决文本已勘误）**；**不构成切片 finding** |
| R-02 | P3 | `src/application/translation/inpaint/router.py::from_settings` | `inpaint` **段本身**为非 mapping（如 `{"inpaint": "oops"}`）时被当作"键缺失"→ 静默返回 `default`。影响：与 R-2 同类的"配置写错被静默忽略"，但**不属于本切片的分歧面**（裁决前后两处调用点在此**一致**），且 **R-7 已把输入校验冻结** | 代码通读 + 测试 `test_missing_section_or_key_returns_the_explicit_default` 第三个断言**显式锁定**该行为并注明"before the ruling they agreed here" | 并入已登记的 **R-07（输入校验）** 一并裁决：是否让"段类型错误"也 fail-loud | **open（非阻塞）**；与 R-07 同一后续项 |
| R-03 | P3 | `src/infrastructure/providers/runtime.py:74`（`DEFAULT_ROUTE_POLICY`）↔ `src/infrastructure/providers/handlers.py`（反向 import） | 判断项：默认策略常量定义在 infrastructure 的 `runtime.py`，而其**类型** `RoutePolicy` 在 application；`handlers.py` 为复用常量引入 infrastructure→infrastructure 的反向引用。影响：无功能问题（**我实测无环**），但"一处定义"在**跨层**意义上仍不彻底 | `rg "handlers" src/infrastructure/providers/runtime.py` = 0；`handlers.py` 仅新增 `from infrastructure.providers.runtime import DEFAULT_ROUTE_POLICY` | 后续切片可考虑把常量随 `RoutePolicy` 放在 application（届时应同时是"单一来源"的最终形态）；**本切片按裁决不动，属正确取舍** | **open（P3 建议，非阻塞）** |
| R-04 | P3 | `verification/TASK-034/r01-collection-orders-{before,after}.log` | R-01 的证据强度**超出**裁决要求 | 我独立复现"修复前 `tests/reading_export tests/editing` → 2 collection errors"与"修复后两种参数顺序 + 伙伴目录全部 exit 0"；作者的十种顺序矩阵与我一致 | 无动作 | **accepted（超额达成）** |

**上一轮 findings 的延续（状态不变，本切片未扩张）**：R-05（`src/ui/viewmodels/export/viewmodel.py` 发布顺序）**deferred**；R-06（webtoon flaky 未复现）**deferred**；**R-07**（输入校验冻结）**仍 open 并已在本次承接 R-02**。

## 验证

环境：Windows `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；全部 `-p no:cacheprovider`。**下列均为我独立重跑**。

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| mandated 三套件 | `pytest tests/providers tests/core tests/reading_export -q -rs` | `132e776` | **PASS** 217 passed / 0 skipped，exit 0 | 本次重跑 |
| AC ① 聚焦 | `pytest tests/providers/test_route_policy_settings.py tests/providers/test_handlers_pipeline.py -q` | `132e776` | **PASS** 35 passed（22 + 13） | 本次重跑 |
| 全仓 | `pytest -q -rs` | `132e776` | **PASS** 706 passed / 6 skipped，exit 0；6 条 skip 全为既有 `tests/network` `openssl unavailable` | 本次重跑 |
| **R-01 顺序（独立复现）** | `pytest tests/reading_export tests/editing -q`（修复前/后） | `132e776` | **PASS** 修复后 91 passed / exit 0；反向亦 91 passed / exit 0（修复前该顺序为 2 collection errors，我于上一轮已独立复现） | 本次重跑 |
| **决策不变性（独立探针）** | 以 `b34b27e` 字面量重建旧默认，对四组特征各跑 `decide` | `132e776` | **PASS** 四组 `decision`+`route` 前后一致（彩色/高复杂度 = blocked↔blocked；线稿/普通 = route:edge-bleed↔route:edge-bleed） | 本次重跑 |
| AC-INPAINT-004 正面要求 | 显式 `color_route=brushnet` 后查看 allowed 与候选理由 | `132e776` | **PASS** `allowed` 含 brushnet 且无重复；候选理由由 `not in the configured route policy` 变为 `not implemented: …`（= 已被策略接受，仅因无实现而 BLOCKED） | 本次重跑 |
| 单入口 / 无残留 | `rg "_route_policy" src/`；`rg "from conftest import" tests/`；`rg "ROUTE_\*" handlers.py` | `132e776` | **PASS** 0 / 0（仅 docstring 提及同名字符串）/ 0；`STEP_INPAINT`、`Mapping`、`Any` 仍被使用（无遗留未用 import） | 本次审查 |
| 边界与空白 | `git diff --name-only 34823b9 132e776` + 禁止路径正则 | `132e776` | **PASS** 17 路径；`src/` 恰为批准 3 文件；`schema|requirements|pyproject|AGENTS|src/ui/` 命中 0 | 本次审查 |
| 真实端点/模型端到端 | — | — | **NOT_RUN**（与本 Task 无关） | — |

**四轴结论（逐轴声明，不跨轴排名）**

- **Standards：`executed`** —— 0 硬性违反；3 项 baseline 判断项（重复代码已消除；1 项 frozen；1 项建议）。本轴最严重项 = 无（R-03，P3）。
- **Spec：`executed`** —— 裁决 R-1～R-7 + 强制项 R-01 **逐条达成**，0 缺失 / 0 scope creep / 0 实现有误。本轴最严重项 = 无。
- **Architecture：`executed`** —— 唯一解释入口落在 application 的 `RoutePolicy` 上（正确归属）；两处私有副本与第三份字面量全部消除；infrastructure→infrastructure 引用**实测无环**；路由判定算法零改动；未触碰 Schema/依赖/seam。本轴最严重项 = R-03（P3，跨层常量归属建议）。
- **Verification：`executed`** —— 作者三项核心数字（217/0、R-01 顺序、706/6）我全部独立复现，并额外独立复核**决策不变性探针**与 **AC-INPAINT-004 正面要求**、单入口无残留、边界；**本轮未发现任何新增缺陷**（唯一 P3 是针对我自己裁决文本的勘误）。本轴最严重项 = 无。

**P0/P1：0 项。** 无未解决 P0/P1，无关键验证缺失（唯一 `NOT_RUN` 与本 Task 无关）。

## 结论与复审

**decision = `approved`**。`reviewed_head=002889e` 可交 Codex 集成。

理由：①裁决 R-1～R-7 逐条落地且**逐条有测试锁定**；②`from_settings` 确为唯一入口（两处私有实现与第三份字面量均已消除，`rg "_route_policy" src/` = 0）；③R-2 的装配期"静默→报错"已在 Handoff/取证充分登记，并有装配+运行两条回归；④Option B 的可观测变化与**决策不变性**由作者实测 + 我的独立探针双向确认，且**不需要**任何判定算法变更（R-01 勘误已澄清）；⑤强制项 R-01 不仅关闭，证据（十种顺序）**超出**要求，我亦独立复现修复前后；⑥范围合规、`src/` 恰为批准清单、禁止路径 0。

**本次批准使 TASK-034 的全部 AC 具备完成条件**（AC ① 第 2 步已交付、R-01 已关闭），故集成后本 Task 可置 `done`。

**剩余风险**：R-02/R-03/R-07（输入校验与常量归属，均 P3、已登记、非阻塞）；R-05/R-06（生产侧发布顺序、webtoon flaky，维持 deferred）。**并行子代理偏差**已在上文显式声明（第三次派发失败）。

复审追加新 head 与对应 findings disposition，不抹掉本记录。
