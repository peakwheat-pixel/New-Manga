---
task_id: TASK-033
reviewer: ZCode 子 agent（窗口内 Review；结论仅 approved_subagent/changes_requested）
author: ZCode（窗口 W2 Owner；本 Review 为窗口授权内的同体子 agent 审查，不构成 §6 第 1 条的跨 Agent 独立批准，T1 后须由 Codex + DeepSeek Harness 补外部 post-hoc 复审）
base_commit: a3c36b1
reviewed_head: 933819f
decision: approved_subagent
---

# Review：TASK-033（reviewed_head = 933819f）

Reviewer 声明：本 Review 在 ZCode 全权窗口（2026-09-17～2026-09-18 08:50）内执行，窗口内 Review 结论**只能**为 `approved_subagent` 或 `changes_requested`（09_COLLABORATION.md §6 第 2 条窗口修订）；窗口期满后必须由 Codex + DeepSeek Harness 补外部 post-hoc 复审（可推翻本结论）。

## 范围与依据

- 固定对象：worktree `G:/CODEX/New Manga.worktrees/TASK-033-zcode`，分支 `agent/zcode/TASK-033-full-chain-handlers`，实现 diff `a3c36b1..933819f`（10 文件，+1194/−15），文档 diff `933819f..e308f98`（Handoff/Task 状态/全仓 5 次运行日志）。
- 依据：`doc/tasks/TASK-033.md`（AC ①–⑧、允许/禁止范围、归属裁决 4 条、「单一写者」P0 要求）、`doc/handoffs/TASK-033-933819f.md`、`doc/09_COLLABORATION.md` §6、`doc/06_TRANSLATION_PIPELINE.md` §8/§12/§23/§24/§47、`verification/TASK-019/ac-status.md`、实现 diff 全文与测试实跑。
- 未审到的部分：`infrastructure/rendering/**` 与 `application/rendering/**` 的 TASK-014 冻结行为本体（本切片零改动，仅审计其被调用方式）；真实 OCR/翻译/修复质量（环境无 torch/端点，维持 BLOCKED/NOT_RUN，不在本 Review 可验证范围）。

**隔离要求声明**：本次 Review 由单线程子 agent 执行，无独立线程可并行两轴。按 09_COLLABORATION.md §6 第 6 条兜底，做了**两遍相互隔离**的检查（非一遍通读充当两轴）：第一遍仅以仓库记录标准对照 diff（不引用 AC 满足性）；第二遍仅以 AC/归属裁决/单一写者要求对照 diff 与测试（不复用第一遍结论）。Standards 与 Spec 分别成节、独立取材。

## Standards（executed）

硬性项（引用标准文件 + 条款）：

1. **允许路径**：diff 文件清单（10 文件）全部落在 `doc/tasks/TASK-033.md` 允许范围内——`src/infrastructure/providers/handlers.py`、`src/application/translation/knowledge/term_extraction.py`（新，Task 明示"若无术语抽取服务，则需在本 Task 内新建"）、`src/application/rendering/service.py`（小幅适配，Handoff 已按条款说明理由）、`src/bootstrap/app.py`、`tests/providers/**`（4 文件）、`verification/TASK-019/ac-status.md`（仅 AC ⑥ 口径）、`doc/tasks/TASK-033.md`（状态流转）。
2. **禁止项零触碰**：`git diff --name-only` 核实无 `src/infrastructure/pipeline/**`、`src/application/translation/pipeline/**`、`src/ports/rendering/**`、`src/infrastructure/rendering/**`、依赖清单、Schema/migration、路由判定算法、`AGENTS.md`、生产数据、其他 Task 文件。
3. **既有测试保护**：`test_handlers_pipeline.py` 的 diff 仅有 docstring/注释措辞更新（`grep "^-"` 过滤 assert/skip/xfail 零命中，断言 `assert "render" not in steps`、`assert decisions["render"].value == "blocked"` 逐字保留）；`providers_helpers.py` 仅新增 `page_png`；全 diff 无新增 `pytest.mark.skip`/`xfail`。唯一新增 `pytest.importorskip("PySide6")`（test_full_chain.py:26）与同目录 `test_bootstrap_providers.py:17` 等 10 处既有先例口径一致，且本环境实际执行（全仓 0 skipped）——判定为环境声明范式而非掩盖失败（见 R-002）。
4. **BLOCKED 口径**：`ac-status.md` 的 AC-RFULL-001 为 **BLOCKED（handler 与生产装配面已打通；真实 OCR/翻译/修复能力仍缺）**，未记 PASS；真实能力项与解锁条件逐项保留；第 49 行登记项仅由"handler 不在允许路径"更新为"已解除（TASK-033）"，未动任何模型质量项状态。
5. **fail-closed 与不伪造**（D08 AC-FALLBACK-001 / AC-OPTIONAL-002 口径）：三 handler 在服务未装配时抛 `ProviderNotConfigured`（→ `PROVIDER_NOT_CONFIGURED`）；render 缺 Clean 时透传 RenderService 的 `MISSING_REQUIRED_INPUT`；`needs_fallback` 是 color 的**输出**（provenance 记录），render 侧应用，无静默降级。grep 验证 `term_extraction.py` 零网络/零写入（`requests|urllib|socket|http|open(|.write|store.add|insert|commit|sqlite3|subprocess` 零命中）。
6. **分层与组装**：`term_extraction.py` 仅 import stdlib + `application.translation.knowledge.tm` + `domain.constraints.entities`；`content_decoder` 闭包 `_render_content_decoder` 定义于 bootstrap 组装层，application 层只持有 `Callable[[str, bytes], bytes]`——维持 AGENTS.md「QML 不直接访问数据库」同源的 S-1 分层约束（`tests/providers/test_ports_contract.py` + `tests/core -k contract` 实测 7 passed）。

Baseline smell 判断项（Fowler，仓库未记录处，均非硬性违规）：

- **possible Large Class**：`handlers.py` 增至 967 行 / 8 个 handler（hunk：`ProductionHandlers._handlers` 注册 8 键）。归属裁决第 1 条明确所有 pipeline handler 归该文件——仓库记录的标准优先于 baseline，不构成违规（R-003 备查）。
- **possible Duplicated Code**：`handle_color`/`handle_term_extract` 的 region 取用校验块（`get_region` + deleted 检查，handlers.py:566-570、619-623）与既有 `handle_translate`:287-291 同型。三处各 5 行、语义各自独立，暂不值得抽象。

本轴小结：0 个硬性违规；2 个 baseline 判断项（均被仓库标准豁免或可忽略）+ 1 个范式备查（R-002）。最严重项为 R-003（P3）。

## Spec（executed）

来源要求但缺失/部分完成的项：

1. **AC ③ 的跨 run 面（继承缺陷，登记而非缺失）**：AC ③ 原文"上游（Clean 等）齐备时规划不再 `BLOCKED(missing_clean_artifact)` 且步骤可执行"。实测：同 run 内 `inpaint RUN → render RUN` 满足（`test_full_translation_chain_runs_every_step` 与三次全链重跑）；但 **render-only 命令跨 run 仍规划 BLOCKED**——`src/application/tasks/service.py` 的 `_clean_available` 检查 `stages["clean"]`，而没有任何步骤写入名为 `clean` 的 stage（inpaint 写 `inpaint` stage），条件恒假。该 planner 文件不在本 Task 允许路径（修复需 Codex 释放范围），TASK-019 释放前核对表本就登记了此规划行为，作者未越权修改、在 Handoff 遗留 1 / Task 交付记录 / ac-status 继承登记三处如实登记并给出后续切片建议——处理方式符合协作规则与 AC ⑥ 精神，记 **R-001（P2，deferred）**，不阻断本切片。
2. **AC ⑧**：待本 Review 与集成——预期状态（frontmatter `status=delivered`/Task `status=in_progress`），非缺陷。

来源未要求的行为（scope creep）：未发现。`PageImageSource.page_png`（handlers.py Protocol + bootstrap 实现）与 `AppServices.render` 字段均为"新增 handler 与接线 / 装配渲染侧依赖"授权范围内的直接后果；`_render_content_decoder` 是 AC ④ 装配的必要组成，且有 Handoff 适配理由（NMFR 为 TASK-018 有意设计的非共享容器，渲染组合需要 PNG，桥接只能发生在装配面；`_read_managed` 的 `None` 分支保持历史行为，已存制品不动——与 Handoff「默认行为不变」声明一致，diff 核实）。

看似实现但实现有误的项：未发现。逐条核对结果——

- **AC ①（color）**：`handle_color` 产出 §8 全部判定字段（detected_source_font_size / confidence / text_color / stroke_hint_color / background_color / direction_hint）+ provenance（`ai_invoked=False`、box、`fallback_to_default`）；`revision_updates={}`（判定步骤无 Region 写，写作用域约束满足）；无外部调用（§8「若无法可靠估算字号→fallback」以 `needs_fallback` 输出呈现，非静默）。满足。
- **AC ②（term_extract）**：`TermExtractionService`（glossary 命中 → 只读 TM 扫描 → 重复 n-gram 启发式）+ handler 只产候选；零 glossary/TM 写、零网络（grep 验证）；未 import TM 写侧服务；fail-closed（`PROVIDER_NOT_CONFIGURED`）。满足。
- **AC ③（render + 单一写者 P0）**：单一写者决策在 Task 中明确记录（RenderService 为 `translated` 指针唯一写者，CAS）；实现侧 `handle_render` 返回 `revision_updates={}`（handlers.py:724，seam 只处理 revision_updates 列出的键，故不再翻转指针）；`RenderService._commit_translated` → `commit_revision(expected_current_revision_id=...)` 乐观 CAS；测试 `test_render_is_the_single_translated_pointer_writer`：三次 render 后 translated revisions 恰为 `[1,2,3]`、current 恒指最新、`handle_render` 直调断言 `revision_updates == {}`、`not executed.candidates`（candidates 是 seam 在 `input_revision_changed`/`lock_changed` 冲突时的候选记录，零命中即无指针冲突）。既有规划守卫未放宽（`test_translate_step_commits_but_render_remains_unwired` 断言原样通过）。缺上游 handler 层 fail-closed（`MISSING_REQUIRED_INPUT`，detail 含 "clean"，零写入断言）。满足（跨 run 继承缺陷见上第 1 条）。
- **AC ④（渲染侧生产装配）**：`assemble_services` 构造 `RenderService`（复用 QtFontCatalog/SqlitePageArtifactLocator/PixelSourceStyleAnalyzer/QtImageCompositor/QtTextLayoutEngine，**零新增/修改适配器实现**——diff 核实 `ports/rendering`、`infrastructure/rendering` 零改动）；`test_render_stack_and_full_chain_handlers_assemble` 在无重型依赖环境（本机无 torch/numpy，AC-OPTIONAL-001 同口径）构建成功并断言 `services.render` 类型 + 三 handler 键在 seam `executor._handlers`。满足。
- **AC ⑤（完整链用例）**：`test_full_translation_chain_runs_every_step`——真实 SQLite（open_database + MigrationRunner + default_migrations）+ 真实 seam（build_production_pipeline）+ 真实渲染装配（含真实 bootstrap `_render_content_decoder` 与真 QImage 页 PNG）+ 替身 provider（FakeOcr/FakeTranslation/SimpleFill/EdgeBleed，既有替身范式）。8 步逐 `COMPLETED` + run `COMPLETED`（save = 每步 commit，D06 §24「save 不重复提交已持久化的 Step」口径，docstring 明示 9-step contract = 8 step types + save）；目标 Region 写入（ocr_text/final_translation）；region-a 的 text/pointer/stage 全不变（AC-RFULL-002）；region-c 人工译文 `人工译文`、`manual_edited`/`final_source="edited"`/`translation_locked=1` 保留（AC-RFULL-005）；`for stage in FULL_CHAIN: _stage(...) is COMPLETED` 逐步 status。满足。
- **AC ⑥**：见 Standards 第 4 条。满足（未记 PASS）。
- **AC ⑦**：主套件 230/0、回归 114/0、全仓 ×5 日志（`verification/TASK-033/full-suite-runs.log` 含 5×`754 passed` + 5×`EXIT=0`）——本 Review 独立复跑全部一致（见验证表）。
- **归属裁决 4 条**：① 三 handler 归 `handlers.py` + `build_production_handlers` 构建 + bootstrap 注入，seam 本体零改动（diff 核实）；② color/term_extract 无外部调用、fail-closed（grep + 测试）；③ render 调应用层 `RenderService`（含 `rerender_region` 的 region_locked → `LOCK_CHANGED` 既有保护，D06 §47），生产装配复用既有适配器；④ `ports/rendering` 与 `infrastructure/rendering` 零改动、未改任何 TASK-014 签名/行为。全部满足。

本轴小结：0 个未满足 AC；1 项继承缺陷登记（R-001，deferred 交 Codex）；无 scope creep。最严重项为 R-001（P2）。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-001 | P2 | `src/application/tasks/service.py` `_clean_available`（**不在本切片允许路径，非 933819f 引入**） | render-only 命令（`RERENDER_ALL/SELECTED/SINGLE/REGION`）**跨 run** 规划恒 `BLOCKED(missing_clean_artifact)`：该查询检查 `stages["clean"]`，但没有任何步骤写入名为 `clean` 的 stage（inpaint 写 `inpaint` stage），条件恒假——Clean artifact 实际存在也被拦。影响 AC-RFULL-001 在 render-only 场景的可达性；同 run `inpaint RUN → render RUN` 分支正常（本 Task 完整链即此路径） | 作者已在 Handoff 遗留 1、Task 交付记录、`ac-status.md` AC-RFULL-001 行三处登记；`test_render_is_the_single_translated_pointer_writer` 注释同证 | 后续切片（需 Codex 释放 `src/application/tasks/service.py` 范围）：`_clean_available` 改为查询真实 Clean artifact 存在性，并补跨 run render-only 用例 | open（deferred，移交 Codex 裁决后续切片） |
| R-002 | P3 | `tests/providers/test_full_chain.py:26` | 新增 `pytest.importorskip("PySide6", reason="the render/color assembly is Qt-backed")`。与同目录 `test_bootstrap_providers.py:17` 等 10 处既有先例一致（Qt-backed 装配测试的模块级环境声明）；本环境实际执行（全仓 754 passed / 0 skipped，无 skip 发生），**不构成**"新增 skip 掩盖失败" | `grep -rn importorskip tests/`（11 处，本切片仅 +1）；全仓实跑 0 skipped | 无需处理；若未来 PySide6 缺席的环境跑 CI，该模块将 skip，届时须按分列口径报告 skip 数 | open（备查） |
| R-003 | P3 | `src/infrastructure/providers/handlers.py`（967 行 / `_handlers` 8 键） | possible Large Class（Fowler baseline 判断项）：handler 数量增长使该文件持续变大 | 归属裁决第 1 条明确所有 pipeline handler 归该文件（"该文件已是 pipeline seam 的 handler 归属地"）——仓库记录的标准优先于 baseline | 无需处理；若未来 handler 再增，可由 Codex 评估按 step 族分模块（属接口/协作规则变更，须先裁决） | open（备查） |

在记录范围内未发现 P0/P1 问题。

## 验证

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| 主套件 | `PYTHONDONTWRITEBYTECODE=1` + `G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe -m pytest tests/providers tests/pipeline -q -p no:cacheprovider -rs -rf` | worktree @ 933819f，Git Bash | **PASS**：230 passed / 0 skipped，exit 0 | 本 Review 实跑 |
| 回归 | `python -m pytest tests/core tests/storage tests/rendering -q -p no:cacheprovider -rs -rf` | 同上 | **PASS**：114 passed / 0 skipped，exit 0 | 本 Review 实跑 |
| 全仓 | `python -m pytest -q -p no:cacheprovider -rs -rf` | 同上 | **PASS**：754 passed / 0 skipped，exit 0（Git Bash 口径 openssl 可用、6 条 TLS 真实执行，与 PowerShell 748/6 差异仅此） | 本 Review 实跑 |
| 分层守卫 | `python -m pytest tests/providers/test_ports_contract.py tests/core -k contract -q ...` | 同上 | **PASS**：7 passed / 18 deselected，exit 0（application→infrastructure 反向依赖为 0） | 本 Review 实跑 |
| 作者全仓 ×5 | 核对 `verification/TASK-033/full-suite-runs.log` | 作者 @ 933819f | **PASS**：日志含 5×`754 passed` + 5×`EXIT=0`，与本 Review 独立复跑结果一致 | `verification/TASK-033/full-suite-runs.log` |
| 单一写者（P0） | `test_render_is_the_single_translated_pointer_writer`（含于主套件实跑）+ `handlers.py:724` `revision_updates={}` + `_commit_translated` CAS 实现审读 | 933819f | **PASS**：三次 render 指针 `[1,2,3]`、current 恒指最新、handler 无指针更新、run 无 candidate | 实跑 + `tests/providers/test_full_chain.py:468-532` |
| fail-closed | `test_color_and_term_extract_fail_closed_without_assembly`（三 handler → `PROVIDER_NOT_CONFIGURED`）、`test_render_fails_closed_when_clean_is_missing`（`MISSING_REQUIRED_INPUT` + 零写入） | 933819f | **PASS**（含于主套件实跑） | 本 Review 实跑 + 断言审读 |
| 红线核对 | `git diff --name-only`、`grep "^-"`（assert/skip/xfail）、`grep -i`（term_extraction 网络/写入关键字）、ac-status 口径 | a3c36b1..933819f | **PASS**：允许路径 10/10、禁止项 0、断言零删除、无新增 skip/xfail 标记、AC-RFULL-001 保持 BLOCKED | 本 Review 审读记录 |
| 真实模型/端到端 | 无 torch/diffusers/numpy/端点 | 环境 | **NOT_RUN**（维持既有 BLOCKED/NOT_RUN 口径，未以 mock 冒充） | `ac-status.md` 解锁条件 |

**四轴声明**：Standards = executed；Spec = executed；Architecture = executed；Verification = executed。

**Architecture 轴结论**（executed）：分层守卫实测 7 passed；`term_extraction.py` 依赖方向仅 stdlib + application + domain；`content_decoder` 闭包来自 bootstrap 组装层、application 层零 infrastructure import；三 handler 为组装面接线，seam 与 executor 未被修改以迁就实现。本轴 0 findings。

**Verification 轴结论**（executed）：作者证据（5×754 passed + 分套件分列）与本 Review 独立实跑完全一致；`test_full_chain.py` 4 个测试名与断言覆盖 AC ⑤ 全部断言点、单一写者 P0、两类 fail-closed。本轴 0 findings。

## 结论与复审

**Decision：`approved_subagent`。**

理由：AC ①–⑦ 全部满足且有可复跑证据（AC ⑧ 为预期待办）；P0「单一写者」约束以决策记录 + 实现（`revision_updates={}` + RenderService CAS）+ 测试（`[1,2,3]` 无抖动、无 candidate）三重证明；红线（允许/禁止路径、既有测试保护、BLOCKED 口径、无静默降级）零违规；分套件与全仓独立复跑全绿；三项 findings 均为 P2/P3 且无一项要求修改本 reviewed_head（R-001 为继承缺陷、已按规则登记移交，R-002/R-003 为备查判断项）。

固定 head 933819f **可交 Codex 进入集成**（含 e308f98 的文档提交；集成后的测试与验证由 Codex 另行记录）。

剩余风险：① R-001 继承 planner 缺陷使 render-only 命令在后续切片修复前不可跨 run 执行——AC-RFULL-001 口径已如实登记该限制；② 本 approved_subagent 为窗口内同体审查，**不等同**跨 Agent 独立批准，T1 后必须由 Codex + DeepSeek Harness 补外部 post-hoc 复审（可推翻本结论）；③ 替身 provider 只证明集成语义，模型质量证据仍 BLOCKED/NOT_RUN。

复审记录：首次 Review（reviewed_head=933819f，docs head=e308f98），无后续追加。
