---
task_id: TASK-009
reviewer: DeepSeek Harness
author: ZCode
base_commit: 3de750ab7558f4c90841b96005dbbe58b8064e71
reviewed_head: f1dd6022e756ab733c5b6eb20c625b6ce340ba27
decision: changes_requested
---

# Review：TASK-009 Provider 配置、网络策略与凭据边界（f1dd602）

Review worktree：`G:/CODEX/New Manga.worktrees/TASK-009-deepseek-review`，分支 `agent/deepseek/TASK-009-review`。分支从 release `57896ef` 创建并 fast-forward 到固定 `reviewed_head`；`3de750ab..57896ef` 仅含 Codex release 的文档差异，不纳入被审实现。未修改 ZCode worktree、被审 `src/` 或 `tests/`；本提交只新增本报告。

## 范围与依据

- 固定范围：`git diff 57896ef...f1dd602`，37 文件，`+4020/-2`；提交链为 `0a7bce7`、`de2b5f9`、`d96c920`、`09ce903`、`1fee38c`、`517ef12`、`f1dd602`。
- 需求：`doc/tasks/TASK-009.md`、D03 §25～28、D06 §49～57、D07 §63～72/87～89、D08 AC-PROVIDER/NET/SEC/SET/PRIVACY；复审重点取 [TASK-009 Handoff](../../handoffs/TASK-009-f1dd602.md) 的“复审建议”。
- 范围过滤：37 个变更路径全部在 TASK-009 白名单内；`src/domain`、SQLite、QML/UI、依赖、bootstrap 与其他 Task 未触碰。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-001 | P1 | `src/infrastructure/transport/stdlib.py:263-321` | HTTP 目标经 HTTP/HTTPS proxy 时走 absolute-form，但没有加入 `Proxy-Authorization`；代理返回 407 被当作普通 `TransportResponse`，不会抛 `ProxyAuthenticationError`，也不会触发显式 direct fallback。 | 本地带 Basic auth 的 forward proxy：结果 `response.status=407`、`target.requests=[]`、`proxy.auth_failures=1`，即使 vault 有正确密码也未发送。 | 给 absolute-form 请求补受保护凭据生成的代理认证，并把经代理收到的 407 映射为 `ProxyAuthenticationError`；补 HTTP forward-auth/fallback 测试。 | open |
| R-002 | P1 | `src/infrastructure/transport/stdlib.py:257-284,307-325,388-420` | `_send_once` 的 `finally` 只在 `_establish` 返回后执行；CONNECT 407/502 或 proxy/target TLS 握手失败会在此之前抛出，已打开 socket 未显式关闭。 | fake socket 收到 CONNECT 502：抛 `ProxyTunnelError`，但 `closed=False`。 | 让 establishment/tunnel/TLS 全程由同一 cleanup seam 承担，失败路径逐一关闭底层 socket。 | open |
| R-003 | P1 | `src/infrastructure/transport/stdlib.py:265-272` | 建连后 HTTP 写入、读取响应或读取 body 的 `TimeoutError`/`OSError` 未映射到 typed transport error；因此 proxy read timeout 不进入 `_PROXY_FAILURE_TYPES`，显式 fallback 不执行，诊断可能抛原始异常。 | silent HTTP proxy、timeout=0.1、HTTP 目标：`StdlibTransport.send` 抛原始 `TimeoutError('timed out')`，而非 `TransportTimeoutError`。 | 映射 connect/read/write 的超时与连接错误，区分 proxy hop/target，并让允许的 proxy failure 进入既有 fallback gate。 | open |
| R-004 | P1 | `src/infrastructure/network/diagnostics.py:171-194` | Provider-Auth 阶段的第二次 `send()` 得到的 `fallback_events` 被丢弃；报告只返回 HTTP 探测阶段的 events，无法审计 auth 请求发生的 direct fallback。 | 代码路径中 `auth_stage` 只读取 status；返回的 `outcome` 未保存或合并到 `ConnectionTestReport`。 | 累积所有阶段 outcome 的 fallback events，并保留发生阶段/原始错误信息。 | open |
| R-005 | P1 | `src/infrastructure/network/diagnostics.py:177-180`; `src/ports/network/transport.py:18-64` | 401/403 被包装成基类 `TransportError`，error code 为 `TRANSPORT_FAILED`；既不能按 ProviderAuthenticationError 区分，也与 D02 的 Provider 鉴权/代理鉴权分层不符。 | `ConnectionTester` 对 401 probe 返回 `ok=False, error_code=TRANSPORT_FAILED`。 | 增加并使用 Provider-Auth typed error/code；保持 proxy auth 与 provider auth 可区分。 | open |
| R-006 | P1 | `src/application/settings/snapshots.py:26-70` | `frozen=True` 只冻结 dataclass 属性，`settings`/`bindings`/profile mappings 仍可直接写入；`build_snapshot` 仅浅拷贝调用方 mapping。Run 内部若持有该对象，可静默替换 Provider 或设置。 | `snapshot=build_snapshot(...); snapshot.provider_profiles['injected']=...` 成功，结果为 `snapshot_mapping_mutable=True`。 | 用不可变 mapping/value seam，或禁止公开可写容器；增加直接修改 snapshot 的回归测试。 | open |
| R-007 | P1 | `src/application/settings/bindings.py:80-94,105-129` | Resolver 只检查 profile 存在/启用，不检查 `profile.capabilities` 包含请求 capability；task 选择与普通 binding 都可把 OCR 解析到 translation-only profile。 | translation-only profile 以 `task_profile_id` 解析 `ocr` 成功，返回 `frozenset({'translation'})`。 | 两条解析路径都校验 capability；不匹配时抛 `UnresolvedCapabilityError`，不得生成无效 snapshot。 | open |
| R-008 | P1 | `src/ports/network/profiles.py:40-45`; `src/infrastructure/transport/stdlib.py:90-99` | proxy URL 接受 `user:password@host`，且 `NetworkProfile` repr/持久化字段保留完整 URL；这绕过 `credential_ref` 并可把代理密码写入 SQLite/日志。 | 构造带 userinfo 的 proxy URL 后，脱敏前的密码仍出现在 `repr(profile)`。 | 在模型/URL解析信任边界拒绝 userinfo；代理密码只允许从 Credential Store 解析，日志/导出使用脱敏 URL。 | open |
| R-009 | P2 | `src/ports/providers/credentials.py:65-68` | `hmac.compare_digest` 直接比较 Unicode 字符串；非 ASCII SecretValue equality 抛 `TypeError`，与 SecretValue 的通用字符串契约不符。 | `SecretValue('密码') == SecretValue('密码')` 抛 `TypeError: comparing strings with non-ASCII characters is not supported`。 | 比较固定编码后的 bytes，并增加 Unicode secret 测试。 | open |
| R-010 | P2 | `tests/network/test_credential_stores.py:48-52` | Windows vault teardown 吞掉所有删除异常；测试即使留下 Credential Manager 条目也会通过，无法形成真实清理证据。 | 代码对 `base.delete_credential(ref)` 的任意异常直接 `pass`。 | 清理失败应使测试失败或至少留下明确失败记录；继续使用唯一、可识别的测试 ref。 | open |
| R-011 | P2 | `src/infrastructure/transport/stdlib.py:1-7,102-103` | D02 §2 将 HTTP 外呼技术选型记录为 `httpx`，实现却明确采用 stdlib `socket/http.client`；这是未在 Task 中批准的架构偏差。 | 文档与源码并列核对；Handoff 只记录了设计取舍，没有 Codex 偏差裁决。 | 记录批准的架构偏差，或改回已选客户端。 | open |
| R-012 | P2 | `doc/tasks/TASK-009.md:19,57` | 正文仍写 `ready`/“等待 ZCode”/测试目录尚未建立，但 frontmatter 已是 `in_progress` 且同一 diff 已新增测试目录。 | `git diff 57896ef f1dd602 -- doc/tasks/TASK-009.md`。 | 在新 Handoff/Task 元数据提交中修正状态与测试记录。 | open |

无 P0 finding；R-001～R-008 为未解决 P1，按协议不能批准集成。

## Standards

硬标准问题：

- R-005：D02 §6.2.5/§6.2.7 要求区分代理连接失败与目标 API 鉴权失败并使用分类错误；当前 Provider-Auth 使用 `TRANSPORT_FAILED`。
- R-006：冻结快照暴露可写 mapping，违反 `AGENTS.md` 的任务可恢复/当前 Provider 保护原则。
- R-011（P2，非阻塞）：D02 §2 的 HTTP 外呼技术选型记录为 `httpx`，而实现明确使用 stdlib `socket/http.client`；Handoff 记录了此设计取舍，但 Task 没有批准的架构偏差记录。请由 Codex 记录偏差，或改回已选客户端。
- R-012（P2，非阻塞）：`doc/tasks/TASK-009.md:19,57` 的正文仍写 `ready`/“等待 ZCode”/测试目录尚未建立，但同一 diff 已将 frontmatter 改为 `in_progress` 并新增测试目录；应在新 Handoff/Task 元数据提交中修正。

判断型 Fowler smell（不单独阻塞）：`NetworkProfileService` 同时承担 profile CRUD/gate 与 provider-network resolution（Divergent Change）；mode 分支在 profile 校验与 transport route resolution 重复（Repeated Switches）；测试 fake 与 Windows credential lookup 有重复形状（Duplicated Code）；mode/capability/scope/policy 用 raw string 表达（Primitive Obsession）。

## Spec

R-001～R-005 覆盖代理 forward/CONNECT/SOCKS5、typed handshake/auth、fallback 可追踪和 AC-NET-004；R-006～R-008 覆盖 D06 §49～50、D03 §25～27 与 AC-SEC-001/002。R-009～R-010 是凭据契约及证据质量问题。未发现超出 TASK-009 白名单的物料 scope creep；SQLite 持久化、UI、Provider adapter、bootstrap 保持在 Task 明确的后续范围外。

## 五项复审重点

| 重点 | 结论 |
|---|---|
| fallback 分支边界 | **部分通过**：`route.via_proxy and profile.allow_proxy_failure_direct_fallback` 正确阻止 direct 路径误 fallback，`MissingCredentialError` 也被排除；但 R-003 的 post-connect 原始 timeout 不会进入该分支，R-004 的 Provider-Auth fallback event 不会进入报告，R-001 的 HTTP forward 407 甚至不会形成 typed proxy-auth failure。 |
| 握手错误映射 | **部分通过**：CONNECT 407、非 200、SOCKS5 `0xff`/auth status/其他 reply code 的分类方向正确；SOCKS5 typed failure 会关闭 socket。R-001 覆盖 plain HTTP forward 的 407 缺口，R-002 覆盖 CONNECT/TLS 失败清理缺口。 |
| SecretValue 泄漏面 | `repr`/`str`/f-string/普通日志格式化均显示 `<redacted secret>`，Windows 与 in-memory vault round-trip 在当前 Windows 运行通过；但 R-008 允许 URL userinfo 明文，R-009 使 Unicode Secret equality 失败。Windows teardown 的真实清理证据另见 R-010。 |
| 快照冻结 | **不通过**：builder 对输入 mapping 做了浅拷贝，能隔离调用方后续替换；但返回对象自身的 dict 可写，R-006 允许运行时直接改 snapshot。`validate_snapshot` 只报告 missing/disabled，不返回替代 Provider，这一点通过。 |
| AC-SEC-005 门控边界 | **服务层通过**：`NetworkProfileService.create_profile/update_profile` 对新建或从 true→false 的变化要求 `confirm_disable_tls=True`，测试通过。模型直接构造 `verify_tls=False` 是当前明确的 value-object 边界；因此本轮不把它单列为 finding，但后续装配必须保证用户写路径不绕过 service。 |

## Verification

| 场景 | 命令或步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| fixed object / commit chain | `git rev-parse 3de750ab 57896ef f1dd602`；`git log 57896ef..f1dd602 --oneline` | Review worktree，固定 head | PASS | base/head 可解析；merge-base=`57896ef`；7 个实现/交付提交 |
| 白名单 | `git diff --name-only 57896ef f1dd602` + path filter | `f1dd602` | PASS | 37 changed，outside=0 |
| 任务命令 1 | `PYTHONPATH=src python -m pytest tests/network`（PowerShell 等价：`$env:PYTHONPATH='src'; python ...`） | Windows 10/11 环境，Python 3.14.6，`f1dd602` | PASS：75 passed，6 skipped | 1 个 ConnectionTester TLS + 5 个 transport TLS 用例因 openssl 不可用 skip；Windows vault 参数轮次已执行 |
| 任务命令 2 | `PYTHONPATH=src python -m pytest tests/storage tests/library tests/editing` | 同上 | PASS：81 passed，2 skipped | 无失败；既有 Qt 相关测试在无 PySide6 环境 skip |
| 任务命令 3 | `PYTHONPATH=src python -m pytest tests` | 同上 | **FAIL：160 passed，8 skipped，2 failed** | 两项均为既有 `tests/core/test_bootstrap.py`，运行解释器缺少 `PySide6`；非 TASK-009 路径，release 基线同样失败 |
| 必查 whitespace | `git diff --check 57896ef f1dd602 --` | 同上 | PASS，退出码 0 | 无输出 |
| 定点探针 | HTTP forward auth、CONNECT 502 fake socket、silent proxy read timeout、snapshot mapping、capability mismatch、URL userinfo、Unicode Secret equality、SecretValue formatting | `f1dd602` | 复现 R-001～R-009；formatting redaction 通过 | 探针输出记录于本报告 Findings/重点复审证据 |
| OpenSSL / PySide6 | `shutil.which('openssl')`；导入 PySide6 | 当前 Python 3.14.6 | NOT_AVAILABLE | TLS 6 skip；bootstrap 2 fail；Python 3.12.3 可用但未安装 pytest/PySide6，未虚构通过结果 |

## 三轴结论

- **Spec：changes_requested。** fallback/握手/快照/凭据边界仍有 R-001～R-009 的行为或证据缺口；其余 scope creep 检查通过。
- **Architecture：changes_requested。** 分层方向和白名单通过；R-005 的错误分类、R-006 的可变 snapshot、R-011 的 `httpx` 偏差记录未闭环。
- **Verification：changes_requested。** 网络与既有切片通过，whitespace 通过；TLS 与 Qt 关键路径受环境阻塞，且独立定点探针已复现多个 P1。

## 结论与下一步

**decision：changes_requested。** 固定 `reviewed_head=f1dd6022e756ab733c5b6eb20c625b6ce340ba27` 不可交 Codex 集成。P1 修复后必须提交新的 implementation head 与更新 Handoff，再建立新的固定 Review；本报告结论不延用于后续 head。

推荐下一步：ZCode 在 `G:/CODEX/New Manga.worktrees/TASK-009-zcode` 基于 `f1dd6022e756ab733c5b6eb20c625b6ce340ba27`，仅在现有 TASK-009 白名单内修复 R-001～R-008，更新 Task/Handoff，复跑 `python -m pytest tests/network` 与范围检查后交付新 head；Codex 再安排独立复审。
