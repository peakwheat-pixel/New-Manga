---
task_id: TASK-038
author: ZCode（窗口第二轮 W5 Owner，head 317f33e）
reviewer: ZCode 子 agent（窗口授权同体审查；结论仅 approved_subagent/changes_requested，§6 临时例外条款）
base_commit: 047164e（分支起点 202dd21 = round2 指令提交，与实现无代码差异）
reviewed_head: 317f33e（docs heads 69be768 + 9049c4b 仅 Handoff/证据，单列核对）
decision: approved_subagent
---

# Review：TASK-038（生产装配收口：reader/export ViewModel 注册 + tile_factory 注入 + 文档导入接线）

## 范围与依据

- 实现固定为 `git diff 047164e..317f33e`（实现提交单提交 6 文件：`src/bootstrap/app.py`、`src/ui/viewmodels/bookshelf/viewmodel.py`、`src/ui/viewmodels/reader/viewmodel.py`、`src/infrastructure/imaging/webtoon_tiles.py`、`tests/core/test_bootstrap.py`、`tests/reading_export/test_qml_contract.py`；+385/−18）。`047164e..317f33e` 区间中其余提交（`202dd21` 指令包、`066a4c7` 开工状态）均为纯文档，逐提交核实不属实现。
- 文档增量 `git diff 317f33e..9049c4b` 仅 `doc/handoffs/TASK-038-317f33e.md` + `verification/TASK-038/full-suite-runs.log`，与作者声明一致。
- 依据：`doc/tasks/TASK-038.md` AC ①–⑧ 与允许/禁止范围、`doc/handoffs/TASK-038-317f33e.md`、`AGENTS.md` 产品边界、`doc/09_COLLABORATION.md` §6（含窗口例外条款）、D06 §97、D07 §37、02 技术架构 §16。
- 未审到的部分：未做 UI 人工点击流（无 UI 自动化框架，作者已在 Handoff 风险节声明；装配链路 enterReader→openChapter→tilesActive 已由契约测试覆盖）。

## Standards（状态：executed）

**仓库记录在案的标准，逐条核对：**

1. **QML 不直接访问数据库/文件/模型（AGENTS.md 产品边界）——通过**。实现 diff 对 `src/ui/qml/**` 零改动（`git diff 047164e..317f33e -- src/ui/qml/` 为空）。逐一核对 5 个注册名与 QML 实际消费点：`navigationViewModel`（AppShell.qml:40-55、PrimaryNavigationRail.qml:20-37）、`bookshelfViewModel`（BookshelfView/BookDetailPanel/ChapterList/BookGrid/BookshelfToolbar）、`workbenchViewModel`（WorkbenchView.qml:20-21）、`readerViewModel`（ReaderView.qml:27）、`exportViewModel`（ExportWindow.qml:22，standalone）；reader 发起导出走 `rv.model.exportController`（ReaderView.qml:59，即 context property 的 QObject 属性）。`importDocumentsFromUrls(chapter_id, urls)` 中 QML 只传 URL 列表，`Path(...).read_bytes()` 全在 Python VM 侧（bookshelf/viewmodel.py:377-383），与既有 `importFilesFromUrls` 同构；`_open_reader_chapter` 为 Python 信号处理器，QML 不参与打开章节。tile 工厂纯 Python 注入，QML 零文件访问。
2. **允许/禁止范围（TASK-038.md）——通过**。实现提交触碰的 6 文件全部在白名单（`src/bootstrap/app.py`、`src/ui/viewmodels/{bookshelf,reader}/**`、`src/infrastructure/imaging/**`、`tests/**`）；Schema/migration、依赖清单、pipeline seam、路由判定、`AGENTS.md`、其他 Task、QML 均零触碰；`tests/` 下仅 `test_bootstrap.py`（纯新增 2 例）与 `test_qml_contract.py`（仅 `:224` 用例 + 新增 `_EmptyCatalog` double）有改动，既有其余测试零改动；diff 中无任何新增 `pytest.mark.skip`/`xfail`。
3. **D07 §37（源文件只读）——通过**。装配新增写面仅：tile 缓存（managed root `/cache/webtoon-tiles`，可重建）、`reading_progress.json`/`export_history.json`（数据根派生物）、`exports/` 输出目录；`_ManagedReaderCatalog` 只读 repository/locator/Managed Copy，无源文件写路径。
4. **D06 §97（不静默假装最新）——通过**。`_ManagedReaderCatalog` 取 locator 的 current TRANSLATED revision（无则 `translated_path=None`），回退与显式状态消息由既有 `ReadingService.resolve_page`（service.py:254-266："译图不是最新渲染结果，可重新渲染后阅读"）承担，非静默。
5. **02 技术架构 §16 / 分层方向——通过**。`_ManagedReaderCatalog` 位于 bootstrap（组合根），依赖 application.reading.ports、ports.repositories.artifacts 与 infrastructure 适配器，方向合法；dependency guard 实跑 14 passed（见验证表）。

**Fowler baseline 判断项（仓库未记录标准的方面，带 label 与 hunk）：**

- possible Inappropriate Intimacy（测试深入实现细节）：`tests/core/test_bootstrap.py` 断言 `standalone._chapter_id` 与 `services.document_importer._raster` 私有属性——公开行为断言（`contextProperty` 重发布、`isinstance` 契约）已存在，私有断言为补充；立 R-003（P3）。
- possible Speculative Generality 残留：`webtoon_tiles.py:255-260` `tile_file` 仍只 `put` 不 `get`（TASK-020 R-002 的装饰化本质未消除，本切片仅使构造显式化）——归入 Spec 轴 AC⑥ 处置核对，见 R-001。
- 无其他新命中：`_open_reader_chapter`/`_rebuild_export_viewmodel` 为装配粘合函数，单一职责清晰；`export_state` dict + updater 闭包避免了 engine 引用进入 `AppServices` 数据类，未发现 Feature Envy / Divergent Change 形态。

**本轴小结**：发现 2 项（R-001 P3 归本轴残留、R-003 P3），最严重为 R-001（P3，不阻断）。

## Spec（状态：executed）

**AC 逐条（来源：`doc/tasks/TASK-038.md` AC ①–⑧ 原文）：**

- **AC ①（reader 接入 + :224 等价更新）——满足**。`assemble_engine` 注册 `readerViewModel`（app.py:677 区段）；契约测试断言 `contextProperty("readerViewModel") is services.reader` 与 `enterReader → hasChapter=True`。`:224` 用例**未删除**，改名 `test_reader_loads_with_injected_viewmodel_shows_empty_state`，docstring 明确说明前提变化（"生产装配尚未注入"不再成立）；断言由原 2 条（空态可见 + toolbar 存在）增至 3 条（+`vm.hasChapter is False`），强度不降，构成"前提变化后的等价更新"而非放宽，符合 AC① 与禁止范围条款的界定。
- **AC ②（export 双路径）——满足**。Handoff 注册名对应关系与实现/QML/测试三方一致：路径 A = `readerViewModel.exportController`（ReaderView.qml:59 消费；`openExporter()` 以同一 `export_service` 惰性构建并 `parent=self`）；路径 B = context property `exportViewModel`（ExportWindow.qml:22 消费；`workbenchContextChanged → _rebuild_export_viewmodel → set_export_context_updater` 向 live engine 重发布，无章节上下文时为 `None`）。契约测试断言 `controller is not standalone`（VM 独立、共享 ExportService）、enterWorkbench 后重发布为绑定该章节的 `ExportViewModel`。AC② 明文要求"在 Handoff 列明实际注册名与两条路径的对应关系"——Handoff 已列。
- **AC ③（tile_factory 注入）——满足**。装配闭包 `TiledPageRasterizer(path, cache_dir=managed_root/cache/webtoon-tiles, tile_height=4000, overlap=64, prefetch=1, cache=TileCache(512MiB))`；契约测试经真实 `enterReader` 得 `tilesActive=True` 且 tile PNG 落 managed 根；TASK-020 的 typed fail-closed 路径未改动。
- **AC ④（文档导入接线）——满足**。`ImportDocumentsUseCase(PdfiumDocumentRaster(), copy_store, repository)` 与 `ImportImagesUseCase` 共用同一 `copy_store` 与 repository（同构）；MOBI 保持 typed `UNSUPPORTED_FORMAT`（documents/service.py:117-120，未伪造），契约测试锁定 `failed == 1`。
- **AC ⑤（装配契约测试）——满足**。新增 2 例；断言的 5 个 context property 名与 QML 实际消费逐一吻合（见 Standards 第 1 条），`exportViewModel` 初值 None 与装配语义一致。
- **AC ⑥（R-002/003/005/006 处置）——部分满足，1 项定性存疑（R-001，P3）**。R-003 `default_cache_root` 确已删除（含 uuid import 与 `__all__` 条目）；R-006 `wanted`/`wanted_keys` 死绑定确已删除且保留 `ensure_viewport` 副作用并注释；R-005 deferred（QML 触碰需按白名单申请，纯清理无行为价值）理由成立。**R-002 标记 "fixed（投产）"偏乐观**：原始 finding 给出的两个修复选项（`tile_file` 优先经 `cache.get` 定位，或 rasterizer 只保留磁盘+`_file_keys`）均未执行，`tile_file` 依然只 `put` 不 `get`、`_trim` 淘汰不删 PNG，生产行为相对 TASK-020 未变——变化仅在构造点显式化（装配注入 512MiB，替代默认 256MiB）。"Speculative 不成立"部分成立（不再是推测性默认组件），但"装饰性记账"的核心未消除，建议后续改记 accepted 或补 get 路径。AC⑥ 原文只要求"逐条处置（fixed/deferred 均须写明理由）"，形式上已满足，故不阻断。
- **AC ⑦（回归与分列）——满足**。mandated 六套件 378 passed/0 skipped、全仓 ×5 每次 772 passed/0 skipped exit 0（同一 PowerShell-inherited-PATH 口径 + 同一 venv，Handoff 已声明口径）；基线 770（`066a4c7`，实现前）→ 772 = +2 新测试，passed 不减少，收集总数自洽（047164e 纯 PowerShell 口径 764 passed + 6 network skip = 770 collected，与 772 − 2 一致）。Review 复跑见验证表。
- **AC ⑧（Handoff/取证/Review）**——Handoff 与证据已交付，本文件即窗口内 Review，集成后 done。

**来源未要求的行为（scope creep）**：未发现。`_ManagedReaderCatalog` 与 `_open_reader_chapter` 空页预检是 AC① 接线的必要组成（隐藏缺陷处置，Handoff 已列，均在白名单内）。

**看似实现但有误的项**：未发现阻断级错误；R-001 为 AC⑥ 处置定性问题（P3）。

**本轴小结**：发现 1 项（R-001 P3，归 AC⑥ 处置核对），最严重为 R-001（P3，不阻断）。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-001 | P3 | src/infrastructure/imaging/webtoon_tiles.py:255-260；doc/handoffs/TASK-038-317f33e.md（AC⑥ disposition 表） | R-002 标记 "fixed（投产）"但原 finding 的两个修复选项均未执行：`tile_file` 仍只 `cache.put` 不 `cache.get`，`_trim` 淘汰 entry 不删 PNG、不阻止磁盘命中，与 TASK-020 时行为一致；后续维护者可能据 "fixed" 误认为三重缓存装饰化已消除 | 读 webtoon_tiles.py `tile_file` 全文：`_file_keys`→磁盘→decode，无 `self._cache.get` 调用点；TASK-020 review R-002 行原文对照 | 将 R-002 处置改记 accepted（构造显式化 + 512MiB 预算为实际增量），或在后续 infrastructure 切片补 `cache.get` 优先定位；不改本 reviewed_head | open（移交 post-hoc 复审/Codex 裁决） |
| R-002 | P3 | verification/TASK-038/full-suite-runs.log:14-61 | 5 次全仓串跑仅第 1 次留 `RUN_EXIT=0`，第 2–5 次无逐次退出码行；证据纪律（TASK-038.md"每条命令记退出码"）未完全落实到证据文件。每次均有 pytest 收尾行 "772 passed"（正常结束才输出），实质风险极低 | `grep -c "772 passed" ...` = 5，`grep -c "RUN_EXIT=0" ...` = 1 | 后续验证日志对每条命令统一追记退出码行 | open（证据纪律备注） |
| R-003 | P3 | tests/core/test_bootstrap.py（`standalone._chapter_id`、`services.document_importer._raster` 断言） | possible Inappropriate Intimacy（baseline 判断项）：契约测试断言私有属性，重构 VM 内部字段名会破坏与生产行为无关的测试 | 读 test_bootstrap.py 新增用例 | 可改为经公共接口断言（如 ExportViewModel 的章节属性/QProperty），随下次触碰该测试文件时处理 | open（不阻断） |

## 验证

Reviewer 实跑环境声明：**shell = Git Bash（`/usr/bin/bash`，ZCode 子 agent 自己的 shell）；解释器 = `G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3）；worktree = `G:/CODEX/New Manga.worktrees/TASK-038-zcode`（common dir = `G:/CODEX/New Manga/.git`，分支 `agent/zcode/TASK-038-production-assembly` @ `9049c4b`，工作区干净）；全部命令 `PYTHONDONTWRITEBYTECODE=1`、`-p no:cacheprovider`、退出码均记录、passed/skipped 分列。口径说明：本 shell PATH 含 mingw64 OpenSSL 3.5.4，与作者的 PowerShell-inherited-PATH 同为"openssl 可用"口径，故 `tests/network` 6 条 TLS 测试真实执行、全仓 0 skipped——与作者口径可比。**

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| mandated 六套件 | `python -m pytest tests/core tests/providers tests/pipeline tests/reading_export tests/import_formats tests/library -q -p no:cacheprovider -rs -rf` | Git Bash + TASK-012-py312 / 9049c4b（含 317f33e） | **PASS：378 passed / 0 skipped，exit 0**（与作者一致） | 本次会话实跑输出 |
| 全仓 ×2（Reviewer 复跑） | `python -m pytest -q -p no:cacheprovider -rs -rf` | 同上 | **PASS×2：每次 772 passed / 0 skipped，exit 0**（两次稳定） | 本次会话实跑输出 |
| 依赖分层守卫 | `python -m pytest tests/providers/test_ports_contract.py tests/core/test_architecture.py -q -p no:cacheprovider` | 同上 | **PASS：14 passed / 0 skipped，exit 0** | 本次会话实跑输出 |
| 作者证据核对 | 读 full-suite-runs.log + Handoff 验证表 | 69be768/9049c4b | log 含 5 次 "772 passed"；基线 770/0（066a4c7）、mandated 378 与本 Review 实跑吻合；openssl 口径差异已被作者正确解释且收集总数自洽 | 本文件 Verification + Spec AC⑦ |
| QML 零改动红线 | `git diff 047164e..317f33e -- src/ui/qml/` | 317f33e | PASS：空输出；5 个注册名与消费点逐一核对（见 Standards） | 本文件 |
| skip/xfail 红线 | `git diff 047164e..317f33e -- tests/ \| grep skip/xfail` | 317f33e | PASS：无任何新增 skip/xfail 标记（命中的 "skip" 全为文档行文） | 本文件 |
| `:224` 用例等价更新 | 对照 base 版用例与 diff | 047164e→317f33e | PASS：未删除；断言 2→3 不降；docstring 说明前提变化；`_EmptyCatalog` 为唯一新增 double | 本文件 Spec AC① |

## 四轴结论

- **Standards：executed**——发现 2 项（R-001、R-003，均 P3）；最严重 R-001（P3）。无硬性违规。
- **Spec：executed**——发现 1 项（R-001，P3，AC⑥ 处置定性）；AC ①–⑧ 其余全部满足，无 scope creep。
- **Architecture：executed**——0 项发现。`_ManagedReaderCatalog` 组合根分层合法、依赖方向正确（QML/UI → Application → Domain/Ports → Infrastructure，无反向）；dependency guard 实跑 14 passed；`export_state` 闭包不泄漏 engine 引用；translated 取 current revision 语义与 ReaderPage 契约一致。
- **Verification：executed**——0 项阻断；R-002（P3）为证据留痕备注。关键路径（装配契约、tile 生产可达、MOBI typed fail、双导出路径隔离）全部由 Reviewer 独立复跑证实。

（隔离要求声明：本轮 Review 为单线程子 agent，无法并行两个独立 sub-agent 执行 Standards/Spec 两轴；按 `doc/09_COLLABORATION.md` §6 第 6 条兜底要求执行了**两遍相互隔离**的检查——Standards 轴仅依据仓库记录标准 + Fowler baseline 独立走查 diff/QML 消费点/红线，Spec 轴仅依据 TASK-038 AC 原文与引用条文独立逐条核对，两遍独立进行、未以一遍通读充当两轴，特此声明该偏差与替代做法。）

## 结论与复审

**decision = `approved_subagent`**（窗口条款 §6 例外；**不等同**跨 Agent 独立批准，T1 后须 Codex + DeepSeek Harness 外部 post-hoc 复审，可推翻）。

固定 head `317f33e`（docs 至 `9049c4b`）可交 Codex 集成：实现严格落在白名单内，QML/Schema/依赖/seam 零触碰，AC ①–⑧ 证据链完整，Reviewer 独立复跑全部通过（378/0、772/0 ×2、14/0，exit 0），无 P0/P1/P2。三项 P3 findings（R-001 AC⑥ 处置定性、R-002 证据留痕、R-003 测试私有断言）均不阻断，建议随 post-hoc 复审或后续切片处置。剩余风险：① OCR/Region 文本尚未进 `ReaderPage.text`，文本导出生产接线属后续装配切片（作者已登记）；② UI 人工点击流无自动化覆盖（既有边界）；③ R-002 的 TileCache 装饰化本质待后续裁决。

## 勘误（2026-09-19，ZCode post-hoc 复审之后）

- 本文结论中的“tile 生产可达”只证明 bootstrap/VM 装配与瓦片文件生产可达，**不证明 QML 渲染可见**。`ReaderView.qml` 的 Repeater `model.tiles` 遮蔽使 delegate 自 TASK-020 起从未创建，直到 TASK-045 `a165aa3` 修复；本 Review 的 `approved_subagent` 结论与既有测试证据不撤回，但“生产可达”的历史措辞应按下述事实收窄。
- ZCode 的 [qml_shadow_probe.py](../../verification/TASK-045/posthoc-zcode/qml_shadow_probe.py) 独立复现 legacy=0 / partial=2 / fixed=2；post-hoc 最终 decision=`uphold_with_findings`（3×P3，不要求返工），见 [TASK-045 post-hoc 报告](TASK-045-posthoc-zcode.md)。
