---
task_id: TASK-017
reviewer: DeepSeek Harness
author: Codex
base_commit: 754eb4f87b83816f7dd0e1bb2bcbc17ba18fc731
reviewed_head: 971efe689f9c8c000eac3a6d97fe1a3c9a0807bb
metadata_head: 387bb1e6a9bad2c881fb4992ee7b362b97070235
decision: approved
---

# Review：TASK-017 尾项修订切片（971efe6，独立 Review）

Reviewer 非本变更作者（作者为 Codex）。固定对象：`base_commit = 754eb4f`、`reviewed_head = 971efe6`、元数据 `387bb1e`。

**接收方式**：Reviewer 在**新建的独立 linked worktree** `G:/CODEX/New Manga.worktrees/TASK-017-deepseek-tail`（分支 `agent/deepseek/TASK-017-tail-review`，基于 `971efe6` 新建）中完成全部复核。**作者工作区 `TASK-017-zcode` 仅被只读检查**（`HEAD = 387bb1e`，`git status` 为空），Reviewer 未在其中写入任何文件。未修改生产代码；未 push、未合并。

## 范围与依据

**变更**：`git diff --name-only 754eb4f 387bb1e` = **7 个路径**：

| 类别 | 路径 |
|---|---|
| 实验实现 | `experiments/TASK-017/protocol.py`、`experiments/TASK-017/mock_provider.py` |
| 实验测试 | `experiments/TASK-017/test_protocol.py` |
| 文档/证据 | `doc/handoffs/TASK-017-971efe6.md`、`doc/research/TASK-017.md`、`doc/tasks/TASK-017.md`、`verification/TASK-017/revision-971efe6.md` |

**越界检查**：对禁止路径（`doc/STATUS.md`、`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/tasks/README.md`、`src/`、`tests/`、`AGENTS.md`）的命中数 = **0**。

**依据**：首轮 Review `doc/reviews/TASK-017-protocol.md` 登记的 **R-001 / R-002 / R-003**（当时为 open，"移交生产实现 Task"）；D06 §13（阅读顺序）、§56（错误分类，含 §56.1 命名 `ProviderRateLimitError`）。

## 复核建议的四项 —— **全部符合预期**

环境：Windows `10.0.26200`；Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312`）；`PYTHONDONTWRITEBYTECODE=1`；CWD = `experiments/TASK-017`。

| # | 命令 | 退出码 | passed | skipped | 与建议预期 |
|---|---|---:|---:|---:|---|
| 1 | `python -m pytest test_protocol.py -q` | **0** | **20** | **0** | ✅ 20 passed / 0 skipped |
| 2 | `python -m pytest test_run_experiment_cli.py -q` | **0** | **1** | **0** | ✅ 1 passed |
| 3 | **判别力复验**：把 `971efe6` 的 `test_protocol.py` 放到 `754eb4f` 的 `protocol.py`/`mock_provider.py` 上运行 | **1** | 15 | 0 | ✅ **`5 failed, 15 passed`** |
| 4 | `python run_experiment.py`（在 `%TEMP%` 副本内） | **0** | — | — | ✅ 退出码 0；`results.json` 与提交版本**仅 `latency_ms` 一处差异** |

**skip 原因**：命令 1、2 均 **`0 skipped`**。

**命令 3 的判别力细节**（失败的 5 例中可见 3 例）：`test_rate_limit_is_retryable_and_other_4xx_declared`、`test_context_keeps_the_nearest_contiguous_run`、`test_reading_order_beats_page_id_string_order`（后者以 `TypeError` 失败，正对应 R-001 的非字符串输入）。**新测试确实能判别旧实现**——这不是"改了实现再补一个必过的测试"。

**命令 4 的逐字段比对**：整份 JSON 递归比对得到 **`total diffs = 1`**，唯一差异为 `/scenarios/0/latency_ms`（`16.0 → 47.0`）。其余字段（含 `regions`、`reading_order`、`truncated_context_pages`、`request` 等）**逐值相同**。

> 说明：`latency_ms` 是运行期测量值，本质易变（本次为 47.0，提交版本为 16.0）。这不构成缺陷，但见下方观察 R-101。

## 五项重点确认

### ① R-001 非字符串输入不再崩溃、不再造出 `"None"` —— **成立**

- `protocol.py:114`：`, ".join(repr(value) if not isinstance(value, str) else value for value in values)` —— 非字符串值以 `repr()` 呈现，**不会被静默转成 `"None"`** 或字符串化的空值；
- `protocol.py:229`：`if not isinstance(rid, str):` —— 非字符串 `region_id` 被**显式处理**（不再是 `TypeError` 崩溃路径）；
- `protocol.py:234`：`if not isinstance(item["translated_text"], str):` —— 非字符串译文同样显式校验；
- `protocol.py:216/226`：`isinstance` 结构校验（`translations` 为 list、条目为 dict 且含必需键）。
- **判别力证据**：命令 3 中 `test_reading_order_beats_page_id_string_order` 在旧实现上以 `TypeError` 失败。

### ② R-002 429 可重试、其余 4xx 显式声明 —— **成立**

- `protocol.py:170`：`RETRYABLE = {` 集合中显式包含 `"http_429"`，行内注释写明 *"ProviderRateLimitError — D06 §56.1 names it retryable (R-002)"*；
- `protocol.py:12-15`（模块 docstring）：*"Rate limiting (HTTP 429) is retryable (§56.1 names `ProviderRateLimitError`); every other 4xx is declared request-side and non-retryable **instead of being left undeclared** (Review R-002)"* —— 即**其余 4xx 是被显式声明为非重试**，而非"未分类/落空"；
- `mock_provider.py:214-220`：新增 `http_429` 故障类型，并以 `elif error.code == 429: kind = "http_429"` 把 429 **单独归类**，注释说明"so the retryable set can name it; the remaining 4xx stay one kind that is declared non-retryable instead of falling through"；
- `mock_provider.py:94-95`：`if fault == "http_429": self._respond(429, ...)` —— 该分类**可在实验中端到端触发**，不是仅存在于文档。

### ③ R-003 排序真的按 `reading_order`、截断策略与 docstring 一致 —— **成立**

**排序**：
- `protocol.py:104`：`ordinal = page.reading_order if page.reading_order is not None else _page_ordinal(page.page_id)` —— **调用方给出 `reading_order` 时优先采用**，仅在缺失时退回由 `page_id` 推导；
- `protocol.py:100` 的 docstring 与 `:126` 的"Ordering (D06 §13)"说明与实现一致；
- 判别力：`test_reading_order_beats_page_id_string_order` 专门断言"阅读顺序胜过 page_id 的字符串序"。

**截断**（docstring 与实现逐条对齐）：

| docstring 声明（`protocol.py:130-135`） | 实现 |
|---|---|
| pages are considered **nearest-first** and kept while the request still fits the budget | `:148` `if token_budget is not None and size_of([*kept, page]) > token_budget:` → 按已排序（nearest-first）顺序逐个纳入，超预算即停 |
| the **farthest pages are dropped first** | `:149` `truncated.extend(candidate.page_id for candidate in ordered[index:])` → 自当前 index 起的**远端**页面全部计入截断 |
| `truncated_context_pages` is reported **nearest-first** | `:157` `truncated_context_pages=tuple(truncated)`，其构造顺序即 nearest-first |
| **Region texts and glossary are never truncated** | 截断只作用于 `ordered` 中的 context page；`:7-8` 的模块 docstring 亦声明 *"Region payloads are never truncated and every output must map back to its Page/Region id"*，实现中未见对 region 文本或 glossary 的裁剪 |

### ④ `results.json` 未被改写 —— **成立**

- `git diff --stat 754eb4f 971efe6 -- experiments/TASK-017/results.json` **无输出**（该文件在本切片**零改动**）；
- Reviewer 与作者两个 worktree 的 `git status --porcelain` **均为空**；
- 命令 4 的复跑在 `%TEMP%` 副本内进行，**未触碰仓库内 `results.json`**。

### ⑤ 未越界、未 push / 未合并 —— **成立**

- 变更路径 7 个，全部落在该 Task 白名单（`experiments/TASK-017/**`、`doc/research/TASK-017.md`、`doc/tasks/TASK-017.md`、`doc/handoffs/TASK-017-*.md`、`verification/TASK-017/**`）；
- 禁止路径命中 **0**；未触碰生产 `src/`、`tests/`、Schema、依赖清单、`AGENTS.md`；
- Reviewer 侧未见任何 push 或把分支合并回 master 的痕迹（本 Review 亦未执行）。

## Findings

| ID | 级别 | 文件/行 | 触发条件 | 影响 | 复现 | 建议 | disposition |
|---|---|---|---|---|---|---|---|
| R-101 | P3（观察，非阻塞） | `experiments/TASK-017/results.json`（`scenarios[0].latency_ms`） | 任何复跑 `run_experiment.py` 的场景 | `latency_ms` 是运行期测量值，**每次运行都会不同**（本次复跑 `47.0` vs 提交版本 `16.0`），使"与提交版本逐字段一致"的比对**必然出现 1 处差异**；若将来把该 JSON 当作可复现证据的指纹，会被这处噪声干扰 | 在 `%TEMP%` 副本内复跑并与提交版本递归比对，得 `total diffs = 1`，唯一差异即 `latency_ms` | 可选：在 `results.json` 或 `experiments/TASK-017/README.md` 中注明 `latency_ms` 为**易变字段**、比对时应排除；或在生成时把该字段标为 `measured_once`/单独存放 | 记录，不阻塞 |

**未发现 P0/P1/P2 问题。**

## 未验证项

| 项 | 状态 | 说明 |
|---|---|---|
| 真实 Provider 端到端（OpenAI-compatible / 本地 Sakura） | **NOT_RUN** | 需已配置端点或本地模型；本切片只改协议与 mock，未引入真实 Provider |
| 成本 / 时延实测 | **NOT_RUN** | 同上 |
| 生产集成验证 | **N/A** | 实验不代表产品集成；本 Task 不修改 `src/` |
| 首轮 Review 中其余 findings 的闭合 | **NOT_RUN（本次未逐条复算）** | 本次只对 R-001/R-002/R-003 与本切片变更范围负责 |

## 结论与复审

**`971efe6` 独立 Review 结论：approved（P0 = 0，P1 = 0，P2 = 0，P3 = 1 观察）。**

- 复核建议的四项**全部符合预期**：协议套件 **20 passed / 0 skipped**、CLI 套件 **1 passed**、**判别力复验 5 failed / 15 passed**（新测试确实能判旧实现）、复跑退出码 0 且 `results.json` **仅 `latency_ms` 一处差异**。
- 五项重点**全部成立**：R-001 非字符串输入被显式处理且不再造出 `"None"`；R-002 429 在 `RETRYABLE` 中被点名且其余 4xx 被**显式声明**为非重试（并在 mock 中可端到端触发）；R-003 排序确实以 `reading_order` 优先、截断策略（nearest-first 保留 / farthest 先丢 / region 与 glossary 永不截断）与 docstring 逐条一致；`results.json` 零改动；越界 0、未 push/合并。
- R-101 为 P3 观察（`latency_ms` 天然易变），**不影响正确性**，不阻塞。
- 本报告**不包含 `integration_commit`**：该值只能由 Codex 在集成动作完成后填写，不得由 Review 报告虚构。

**剩余风险**：真实 Provider 行为、成本与时延仍未验证；`latency_ms` 的易变性使"逐字段一致"需排除该字段；本次仅覆盖 `754eb4f..387bb1e` 的固定范围，分支后续变化不延用本结论。
