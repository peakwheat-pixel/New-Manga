---
task_id: TASK-053
reviewer: 独立子代理（ZCode 会话内新开子对话，与作者不同对话；窗口条款 §8.2 口径）
author: ZCode
base_commit: 8bf8da3f988cdeffce83ad251a15c22ce862dd1a
reviewed_head: 3b58736
decision: approved_subagent
started_at: 2026-09-19 02:59 +0800
superseded_by: 修订 16340b5（R-001 守卫）复审——见追加登记
---

# Review：TASK-053（TASK-044 遗留收口 R-03 / R-04）

独立子对话 Review（开始时间 2026-09-19 02:59，reviewer=独立子代理，与作者不同对话）

> 编者注（作者落盘）：以下为独立子代理报告原文（结构保留、表格逐字保留）；作者仅添加 frontmatter 与本注。R-001 守卫修订见 Handoff「追加登记」与 `16340b5`。

## 固定对象

| 项 | 值 | 核对结果 |
|---|---|---|
| worktree（只读） | `G:/CODEX/New Manga.worktrees/TASK-053-zcode` | 与 Task 文件一致 |
| 分支 | `agent/zcode/TASK-053-run-files-leak` | `git branch --show-current` 一致 |
| base_commit | `8bf8da3f988cdeffce83ad251a15c22ce862dd1a`（开工已 merge master `c9e0675`） | `git log` 证实 `c9e0675` 在实现提交 `f4aa66b` 之前 |
| reviewed_head | `3b58736`（= `f4aa66b` 实现 + `d20a30f` handoff + `3b58736` 文档） | `git rev-parse HEAD` 一致 |
| 复核时工作区 | `git status --short` 为空 | 全程只读，无残留（终检通过） |

## 范围与依据

- **依据**：`doc/09_COLLABORATION.md` §6（Architecture + Verification 两面必覆盖）、`doc/templates/REVIEW.md`、Task 文件 AC ①–⑥ 与允许/禁止范围、TASK-044 Review 的 R-03/R-04（P3 收口对象）。
- **视角声明**：Architecture `executed`；Verification `executed`；Spec 视角以 AC ①–⑥ 判定形式 `executed`；Standards 视角以白名单/禁止范围/断言纪律核对形式 `executed`（不单独成节）。
- **完整 diff**：`git diff c9e0675..3b58736` = 15 文件 `+785/−6`。
- **写集合逐文件核对**（全部在允许范围）：`src/application/maintenance/{ports,trash}.py`、`src/infrastructure/sqlite/pipeline.py`、`tests/storage/{conftest,test_run_files_leak}.py`、`doc/tasks/TASK-053.md`、`doc/handoffs/TASK-053-delivery.md`、`verification/TASK-053/**`（×5 全仓日志 + 前后探针日志 + 探针脚本）。
- **禁止范围核对（全部未触碰）**：无 Schema/migration 改动（diff 无 schema.py/migrator）；无 `requirements.txt`；无 `AGENTS.md`；无其他 Task；无 `src/ui/qml/**`；`−6` 删除行全部位于 `doc/tasks/TASK-053.md` 状态行（src/tests **零删除行** ⇒ 既有断言零改动属实）；diff 无新增 `skip`/`xfail`；代码中无 `PRAGMA foreign_keys = OFF`；无 push（仓库无 remote）。

## Architecture 面（executed）

**小结：分层与依赖方向正确；保护纪律未被绕过；0 项阻断缺陷，1 项 P2 设计守卫缺失（R-001）。**

- **端口方向**：`RunLedgerMaintenance`（Protocol）定义在 `src/application/maintenance/ports.py`，实现在 `src/infrastructure/sqlite/pipeline.py`（`SqliteRunLedgerMaintenance`）。实现与端口之间为 Python Protocol **结构化满足、无 import 依赖**。依赖方向 `QML/UI → Application → Domain/Ports → Infrastructure` 未反向。
- **purge_batch 顺序**：take_batch → 计算 targets → `_record_pending_purge`（原子写 manifest）→ `purge_pages`（单事务删行）→ `_drop_batch`（出账）→ `_remove_pending_targets`（逐文件删除，**全部成功才**清条目）。独立探针实证：即使 `purge_pages` 抛错，pending 条目已落盘——**「清单持久化先于一切破坏性步骤」成立**；该顺序的副作用见 R-001。
- **R-04 语义**：失败 → 条目含完整 targets + 批次已出账 + 孤儿在盘 → retry 删除并清条目 → 幂等（独立探针全数 PASS）。
- **R-03 语义**：修前 `purge_pages` 即**不删 `pipeline_runs`**，只按 page_id 删 `step_result_candidates/step_runs/pipeline_tasks/pipeline_run_targets` 等 12 条语句；`purge_targetless_runs` 的 SQL 为 `DELETE FROM pipeline_runs WHERE NOT EXISTS(pipeline_run_targets) AND status NOT IN ('running','paused')`，参数化、单事务。FK 全链核对 `schema.py`：`pipeline_run_targets/pipeline_tasks/step_runs/step_result_candidates` → `pipeline_runs` 均为 `ON DELETE CASCADE`，更深层亦 CASCADE。独立实证：回收后 tasks/steps/candidates 全为 0、无悬挂、第二次 purge 为 no-op。
- **TRIGGER 纪律（AC ④ 独立验证）**：在**真实迁移库**枚举 `sqlite_master WHERE type='trigger'` 全集 = `{trg_media_artifacts_current_not_clearable, trg_regions_current_not_clearable}`，逐条读 SQL 确认均为 `BEFORE UPDATE` 且仅拦 `current_revision_id → NULL`——与 run/manifest 清理面零交集；`DELETE FROM pipeline_runs` 实测成功且 `PRAGMA foreign_keys` 全程 = 1。无 `IntegrityError` 吞掉。
- **保护纪律**：pending targets 的删除全部经 `ManagedFileStorage.remove_managed`（resolve + relative_to(root) → 越界抛 `ImmutablePathViolation`；缺失文件静默跳过=幂等）。remover 的 managed-root 约束**未被绕过**；用户源文件在 managed root 之外，结构上不可达（测试亦断言 user_source 逐字节幸存）。
- **manifest 兼容性与 F-7 相互作用**：旧 manifest 无 `pending_purges` 段按 `get(..., [])` 兼容，无迁移；原子写保持。F-7 退化（manifest 损坏）确实**不覆盖 pending 段**——Handoff 风险段声明属实、诚实。
- **装配观察**：三个新入口当前无生产调用方（grep 全 src 仅定义与测试）；Handoff 已声明「生产接线属后续装配/UI 面」。符合 AC ③ 字面要求，记 R-003 提醒后续。

## Verification 面（executed）

环境：venv `G:\CODEX\New Manga.task-envs\TASK-012-py312\Scripts\python.exe`（Python 3.12.3）、`PYTHONDONTWRITEBYTECODE=1`、`-p no:cacheprovider`、**`QT_QPA_PLATFORM` 未设**。修前验证在 `git archive c9e0675` 的 TEMP 导出树。

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| V1 | 定向新套件 `pytest tests/storage/test_run_files_leak.py -v` | **PASS：7 passed, EXIT=0** | 会话输出 |
| V2 | 定向既有面 `pytest tests/storage tests/core -q` | **PASS：79 passed（72 既有 + 7 新），EXIT=0** | 会话输出 |
| V3 | 全仓复跑（head 树） | **PASS：880 passed / 0 skipped，1 warning（既有 mobi/imghdr DeprecationWarning），EXIT=0**；与作者 `full-suite-run{1..5}.log`（880/0 ×5）逐条一致 | 会话输出 |
| V4 | 基线算术：修前树（c9e0675 导出）全仓 | **PASS：873 passed，EXIT=0 ⇒ 873+7=880 成立** | TEMP 导出树 |
| V5 | 判别力：修前树运行新测试文件 + `git show c9e0675:` grep 新符号 | **PASS：collection 即 `ImportError: cannot import name 'SqliteRunLedgerMaintenance'`，EXIT=2（整个文件修前必失败）；c9e0675 的 trash.py/ports.py/pipeline.py 均无 `pending_purge*`/`RunLedgerMaintenance`/`count_targetless*`** | 判别力声明证实且更强；R-03 keep-by-policy 用例「钉住、不计判别力」的声明保守诚实 |
| V6 | 修前探针重放 | **PASS：REPRODUCED，EXIT=0**（manifest 仅 `batches` 段、孤儿在盘不可发现、run 残留 targets=0、无维护入口） | 输出与 `prefix-probe.log` 一致 |
| V7 | 独立语义探针（TEMP 一次性脚本，不进仓库） | **24/24 PASS，EXIT=0**（T1–T3 触发器/FK；R4a–R4k R-04 全语义与持久化顺序；R3a–R3h R-03 全语义） | TEMP（未留仓库痕迹） |
| V8 | 纪律终检 | **PASS：工作区干净，无缓存残留** | 会话输出 |

**AC 逐条判定**：AC ① **PASS**（修前 REPRODUCED 独立重放证实，修后 NOT REPRODUCED）；AC ② **PASS**（持久化 + retry 入口 + 判别用例 + 独立复现）；AC ③ **PASS**（明确保留策略、docstring 记录理由、清理入口、三个用例 + 独立复现级联/active 保留）；AC ④ **PASS**（独立枚举真实迁移库触发器全集并实测 DELETE 成功、FK ON）；AC ⑤ **PASS**（判别力证实、既有断言零改动、全仓 880 ≥ 873、×5 日志与复跑一致）；AC ⑥ **PASS（按流程进度）**——集成与 STATUS 台账行属集成后步骤。

## Findings

| ID | 级别 | 文件/行 | 触发条件与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-001 | P2 | `trash.py`（`purge_batch` 中 `_record_pending_purge` 先于 `purge_pages`）及 `retry_pending_purges` 无 ledger 守卫 | 若 `purge_pages` 本身失败或 record 之后进程崩溃：页行仍 live、batch 仍在 ledger，但 pending 条目已在。此时 `retry_pending_purges()` 会删除**仍存活页**的托管文件，页行指向已消失文件（用户源文件不受影响，可重导入）。属本切片引入的新状态空间。不构成 P0/P1：需两个非典型事件叠加、当前无生产调用方、数据为可重建的托管副本 | 独立探针：`purge_pages` 抛错后 `retry_pending_purges()` 返回 1、活页文件被删、`page_alive=True`、`batch_in_ledger=True` | `_remove_pending_targets`/retry 增加守卫：`batch_id` 仍在 ledger 的条目**跳过并保留**（行删除未发生，条目 premature；真正 purge 时 `_record_pending_purge` 会替换同 id 条目） | open（作者已随 `16340b5` 修复守卫，见复审） |
| R-002 | P3 | `verification/TASK-053/r03_r04_probe.py`（硬编码 "pre-fix" 文案） | 探针在修后树运行时输出仍带 "pre-fix" 字样，`postfix-probe.log` 文案易误读；`EXIT=1 = NOT REPRODUCED` 的退出码语义未在文档写明 | `postfix-probe.log` 全文 | 探针文案改为按树自适应或在 postfix-probe.log 头部补说明；数值本身正确，不影响证据有效性 | open |
| R-003 | P3 | `maintenance/ports.py`、`sqlite/pipeline.py` | 三个新入口（retry/count/purge）当前无生产接线；R-04 的「可重试性」在产品中要等装配才真正可达 | grep 全 src 无调用方 | Handoff 已如实声明；提醒集成方与后续装配 Task（接线时一并处理 R-001 守卫） | open（已声明，非缺陷） |

## 结论

**Architecture 面**：通过。**Verification 面**：所有关键声明均经独立复跑或独立探针证实——通过。0 项 P0/P1；R-001 为 P2 设计守卫缺失（非正常路径、无当前调用方、局部可修），R-002/R-003 为 P3 文档/接线提醒，均不阻塞。

`reviewed_head=3b58736` 可交 Codex 集成。剩余风险：R-001（建议在装配前补 ledger 守卫）、F-7 极端退化下 pending 段丢失（与既有 F-7 面一致，已声明）、清理入口未接线（R-003，属后续装配）。

**decision: `approved_subagent`**

---

## 追加登记（作者落盘时补记，2026-09-19）

- R-001（P2）守卫已随 `16340b5` 修复：`retry_pending_purges()` 跳过 batch 仍在 ledger 的 premature 条目（docstring 记录理由）；新用例 `test_retry_skips_premature_entry_while_rows_are_still_live` 钉住；idempotent 用例构造修正为真实流程（条目残留时 batch 必已出账）。修订后新套件 8/0、全仓 ×5（881/0，=873+8，EXIT=0 ×5）→ `verification/TASK-053/revision-full-suite-run{1..5}.log`。
- R-002/R-003 维持 open/已声明（探针文案、接线提醒），不阻塞。
- 修订后由新独立子对话对固定新 head 复审（见 `doc/reviews/TASK-053-16340b5.md`）。
