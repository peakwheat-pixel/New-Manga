---
task_id: TASK-064
reviewer: Avicenna、Ptolemy（独立非作者；DeepSeek Harness reviewer slot）
author: Codex
base_commit: 604c47a
reviewed_head: d154044
decision: approved
---

# Review：TASK-064

## 范围与依据

审查固定范围 `604c47a..d154044`，覆盖本 Task AC、真实 `assemble_services`/`assemble_engine` 调用链、`DiagnosticsService` provider/sink、`BoundedLogStore` data-root 边界、QML 禁止边界、测试与逐次 verification 日志。两位 Reviewer 均为变更作者之外的只读审查者，未修改工作区。

## Architecture

- `AppServices.diagnostics` 由真实 `assemble_services` 注入，不新增 QML context property。
- snapshot provider 只输出固定诊断字段；sink 复用 `BoundedLogStore`，写入 `data_root/diagnostics`，每次报告独立成文件并保留既有 5 文件/512000 字节边界。
- 真实装配导出保留 `PROVIDER_AUTH_FAILED` 错误码，同时对 Bearer/`sk-` credential-shaped 值与敏感 `generated_at` 做脱敏。
- 未发现 Schema、依赖、SQLite、QML、managed storage、cleanup、`tests/workbench` 或 TASK-055 历史变更；未发现 scope creep。

## Verification

- 定向：`pytest tests/diagnostics tests/core -q -p no:cacheprovider -rs`，`75 passed / 0 skipped / EXIT=0`。
- 全仓连续 5 次：每次 `951 collected / 945 passed / 6 skipped / EXIT=0`；6 项逐条均为既有本机 `openssl unavailable`，没有新增 skip/xfail。
- 判别性回归：[discrimination.log](../../verification/TASK-064/discrimination.log) 记录修复前 `2 failed / EXIT=1`（重复导出 `JSONDecodeError` 与缺少 `write`）及修复后 `2 passed / EXIT=0`。
- 每份日志均包含 shell、venv、命令、EXIT、passed/skipped 与逐条 skip 原因；QML stderr null context-property 仅为既有噪声，不改变 pytest 退出码。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-001 | P1 | `src/bootstrap/app.py` sink | 原实现重复导出会把多个 JSON 文档追加到同一文件，第二次不可解析 | 修复前选测 `JSONDecodeError: Extra data` | 每次 bundle 写入独立受界文件 | fixed `2024bfb` |
| R-002 | P2 | `src/bootstrap/app.py` sink | 原实现把外部 `generated_at` 用于文件名，可能把敏感值带入路径 | 修复前敏感时间戳文件名含 token 片段 | 文件名使用 sink 自己生成的 UTC 时间 | fixed `2024bfb` |
| R-003 | P2 | `src/bootstrap/app.py` diagnostics assembly | 当前只注入新的空 `BoundedErrorLog`，未扩大接入既有 pipeline/command 错误采集路径 | 代码审查；本 Task AC 只要求 service/provider/sink 生产装配 | 后续若要求生产错误聚合，另立 diagnostics error-source wiring Task | deferred（本 Task 范围外） |
| R-004 | P1 | `verification/TASK-064/**` | 首轮证据只有 head 通过摘要，缺少修复判别力 artefact | 首轮 Review finding | 记录修复前/修复后同组选测 | fixed `4a89031` |
| R-005 | P2 | Handoff | 首轮路径清单未列 docs/verification 交付文件 | 首轮 Review finding | 区分实现变更路径与交付记录路径 | fixed `2024bfb` |
| R-006 | P2 | `doc/tasks/TASK-064.md` | 首轮正文残留旧 delivery head | 复审指出旧 hash | 与 Handoff/verification 对齐 | fixed `d154044` |

## 结论与复审

`d154044` 满足实现范围、禁止范围、AC①～⑤ 与 Review 证据门槛；P0/P1 均已关闭，R-003 为明确的后续范围外事项。两位独立非作者 Reviewer 对 `d154044` 均返回 `approved`。可交 Codex 合并；合并后的全仓测试与 `integration_commit` 由 Codex 另行登记，随后才可置 `done`。
