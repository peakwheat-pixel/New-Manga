---
task_id: TASK-016
reviewer: Codex
author: DeepSeek Harness
base_commit: f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86
reviewed_head: 4834447992974d4a16bad35c0e8029bf34ecfed4
metadata_head: 000ed5bc2db133a3ab8ba0a7c3e05426c510d115
decision: changes_requested
---

# Review：TASK-016 OCR 与检测路线独立实验（修订轮）

本报告复审 `f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86...4834447992974d4a16bad35c0e8029bf34ecfed4`，并核对元数据提交 `000ed5bc2db133a3ab8ba0a7c3e05426c510d115`。首轮报告为 `de0f675`，其结论为 `changes_requested`；本轮不继承首轮批准状态。

## 范围与依据

- 工作区：`G:/CODEX/New Manga.worktrees/TASK-016-deepseek`
- 分支：`agent/deepseek/TASK-016-ocr-detection-experiment`
- Owner：DeepSeek Harness；Reviewer：Codex
- 必读：`doc/handoffs/TASK-016-4834447.md`、`doc/tasks/TASK-016.md`、`verification/TASK-016/experiment-log.md`、`verification/TASK-016/author-verification.md`、`doc/research/TASK-016.md`
- 依据：TASK-016 Acceptance Criteria、`doc/09_COLLABORATION.md` §4/§5/§6、用户指定的 Review 门槛。
- Standards 与 Spec 两轴均独立复核；未把作者 Handoff 自述当作结论。
- `git diff --name-status f9edd688...000ed5bc` 仅涉及 TASK-016 白名单；未修改生产 `src/`、`tests/`、Schema/migration、依赖清单、AGENTS 或其他 Task；`git diff --check` 通过。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-001 | P1 | `experiments/TASK-016/run_experiment.py`；`results/probe.json` | 检测器候选已进入 `CANDIDATES` 并在最新 probe 中出现，但 YOLO 仍是“仓库无实现来源、待范围裁决”；旧模型运行证据也未包含检测器，故本 AC 的完整证据链仍未闭合。 | 最新 probe 为 6 候选×5 样本；跟踪的模型运行文件仍为 3 候选×5 样本。 | 明确 YOLO 仅为文档候选或移除；同步所有固定结果后再关闭。 | partially fixed，open |
| R-002 | P2 | `experiments/TASK-016/protocol.py:55-94`；`run_experiment.py:357-359,416-418` | 辅助函数已要求调用方点名且路由在配置集内，但 Paddle/Vision 的推理异常路径仍直接写 `FAIL`，`fallback` 保持 `NOT_RUN`，没有把“无已配置 fallback”落成可审计结果。 | 审查 `_run_paddle`/`_run_openai_compatible` 的 `except` 分支；现有测试只覆盖协议辅助函数。 | 失败路径统一调用 fallback 门控；无配置时明确写 `BLOCKED`，有配置时验证点名路由。 | partially fixed，open |
| R-003 | P2 | `doc/research/TASK-016.md:7-13`；`verification/TASK-016/experiment-log.md:59-63` | 候选表和旧日志表仍直接显示具体版本/许可证，只有文末附录声明 unverified；脱离附录阅读会误认为已核实。 | 直接读取上述表头和值可见未加 `UNVERIFIED` 标识。 | 表头和值直接标为 `unverified candidate metadata`，并保留权重许可待核实声明。 | partially fixed，open |
| R-004 | P2 | `experiments/TASK-016/run_experiment.py:49,213-244` | `model_sha256=NOT_AVAILABLE` 已统一且没有冒用 sample hash；这是诚实的未加载权重哨兵，但仍需在 Review/证据模板中明确它不是质量/性能结果。 | 最新 probe 的 hash 集合为 `{NOT_AVAILABLE}`，质量与性能字段为 `NOT_RUN`/`null`。 | 保持哨兵语义，不把它当模型质量证据；本项代码层通过。 | fixed in code, accepted with explicit sentinel |
| R-005 | P1 | `experiments/TASK-016/run_experiment.py:261-284,317-335`；任务测试计划 | `--run-models` 仅检查 Python 包；`MangaOcr()`/`PaddleOCR()` 的初始化没有本地权重存在性或离线门槛，可能自行获取权重，违反“只使用已有依赖/权重、不下载”。当前环境因包缺失未触发该路径，不能据此证明安全。 | 代码审查；默认 probe 的 `450-462` 不调用模型，但显式模型路径可进入两个构造器。 | 增加本地权重存在与 Hash 门槛，缺失时在构造器前返回 `BLOCKED`；或强制明确的离线本地加载。 | open |
| R-006 | P1 | `experiments/TASK-016/results/run-models.json:2-10`；`results/model-run-blocked.json:2-10` | 两个已跟踪模型运行证据仍为 `task016-experiment-result-v1`、3 个候选、15 条记录，缺少检测器、`verification_matrix` 和 `UNVERIFIED` 元数据；与最新代码/probe/Handoff 的 v2、6 候选、30 条声明不一致。修订轮完整结果无法从仓库固定复核。 | 当前两个文件均 `schema=v1`、`providers=3`、`records=15`；最新临时复跑为 `BLOCKED=30`。 | 以最新代码重新生成两份固定结果，或明确旧文件为 superseded 并提供新的完整证据。 | open |
| R-007 | P2 | `verification/TASK-016/author-verification.md:56`；`verification/TASK-016/experiment-log.md:93`；`doc/research/TASK-016.md:93` | 记录的 `probe.json` SHA-256 为 `bd33d38...`，当前文件实际为 `47d496579a777fac48e48420f3b5d66013d84bd9a6e1c5b52a80b4b274b2816a`，证据指纹失效。 | Windows `Get-FileHash -Algorithm SHA256` 实测。 | 重新计算并同步所有证据 Hash；提交后固定新 head。 | open |
| R-008 | P2 | `doc/tasks/TASK-016.md:5,19,80-85,87` | 顶部 `status: in_review`，正文仍写“当前处于 `in_progress`”，并保留“Handoff：待创建”“Review：待...”；历史记录与当前状态混在一起，S-001 未真正关闭。 | 直接读取固定元数据 head。 | 把旧句改成有日期的历史记录，填入实际 Handoff/Review 链接，并只保留一个当前状态。 | open |
| R-009 | P2 | `experiments/TASK-016/run_experiment.py:432-433` | 显式 `--run-models` 的 detector 缺依赖分支先写 `vram_before_mb`；这使 `BLOCKED` 记录含有非 `null` 模型性能字段，违反“模型侧字段保持 `NOT_RUN`/`None`”。 | 在固定 worktree 运行显式 `--run-models`：30 条均 `BLOCKED`，其中 15 条 detector 记录写入 `vram_before_mb`（约 4004–4087 MB）。 | 在确认可真实运行前不要采集资源指标；把 detector 前置门槛放在资源采样之前，或将阻塞结果的指标清回 `null`。 | open |

## Findings disposition

- 首轮 R-001：已补 detector 候选与最新 probe 矩阵，但受 R-006 和 YOLO 范围裁决影响，不能完全关闭。
- 首轮 R-002：协议辅助函数与测试已修订；实际 Provider 失败路径仍见 R-002，不能完全关闭。
- 首轮 R-003：新代码/probe 已加 `metadata_status: UNVERIFIED`，但研究表、旧结果和日志的直接可见性仍见 R-003/R-006。
- 首轮 R-004：代码层通过；`NOT_AVAILABLE` 只作为模型 hash 不可得哨兵，未用 sample hash 冒充。
- 首轮 S-001：未关闭，见 R-008。

## 验证

环境：Windows `10.0.26200`、Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`）；`PYTHONDONTWRITEBYTECODE=1`；未设置 `QT_QPA_PLATFORM`，本实验不加载 Qt。

| 场景 | 命令或手工步骤 | 环境/commit | passed | skipped | 结果 | 证据 |
|---|---|---|---:|---:|---|---|
| 协议自检 | `$env:PYTHONPATH='experiments/TASK-016'; python -m unittest experiments/TASK-016/test_protocol.py -v` | Windows / `000ed5b` | 5 | 0 | PASS，退出码 0 | 本轮实际复跑；无 skip |
| 默认 probe | `python experiments/TASK-016/run_experiment.py --manifest experiments/TASK-016/samples/manifest.json --output <temp>.json` | Windows / `000ed5b` | 0 | 0 | `BLOCKED=30`，退出码 0；默认路径不联网、不下载 | 本轮实际复跑；6 候选×5 样本 |
| 显式模型探测 | 同上并追加 `--run-models`，输出到仓库外临时文件 | Windows / `000ed5b` | 0 | 0 | `BLOCKED=30`，退出码 0；但 15 条 detector 记录含 `vram_before_mb`，见 R-009 | 本轮实际复跑 |
| 模型字段核验 | 检查临时 probe 结果记录 | Windows / `000ed5b` | N/A | N/A | 默认 probe 的质量/性能字段均 `NOT_RUN`/`null`，hash 为 `NOT_AVAILABLE`；显式模型模式不满足该条件 | 本轮 PowerShell 汇总 |
| 代码/范围检查 | `git diff --name-status f9edd688...000ed5bc`、`git diff --check f9edd688...000ed5bc`、工作区状态 | Windows / `000ed5b` | N/A | N/A | PASS；白名单内、无生产路径越界、无未提交改动 | 本轮实际检查 |
| 固定结果一致性 | 对 `probe.json`、`run-models.json`、`model-run-blocked.json` 读取 schema、候选数、记录数并计算 SHA | Windows / `000ed5b` | N/A | N/A | FAIL，见 R-006/R-007 | 当前文件实测：probe SHA `47d496...`，旧 model-run 两文件 SHA `7a8b0a...` |

未执行/未完成项继续保持：真实 OCR/检测质量、模型坐标/顺序、耗时/RAM/VRAM 为 `BLOCKED` 或 `NOT_RUN`；真实漫画样本为 `NOT_RUN`；权威版本/许可确认与权重条款核验为 `NOT_RUN`；生产集成为 `N/A`。本轮未以 Mock 冒充模型质量，未 push、未合并、未释放 TASK-015 或其他冻结 Task。

## 结论与复审

本轮 decision=`changes_requested`。P0=0；未解决的 P1 为 R-005（显式模型模式的权重/下载门槛）与 R-006（固定模型运行证据过期），故不能批准交 Codex 集成；R-009 另阻断“模型侧字段全为 `NOT_RUN`/`None`”门槛。R-002、R-003、R-004 的修订方向已核实，但不能覆盖上述证据与安全缺口。

TASK-016 保持 `in_review/changes_requested`，不填写 `integration_commit`，不标记 `approved`/`done`。下一轮须在同一工作区仅修改 TASK-016 白名单，提交新的 delivery head 和 Handoff，修复 R-002/R-003/R-005～R-009，并重新提供可复现的协议、probe、显式模型阻塞结果及精确 SHA；Review 后才可进入 Codex 集成。TASK-015、TASK-017～TASK-027 及其他冻结 Task 不因本报告变化。
