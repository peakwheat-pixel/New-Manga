decision: approved_subagent

# Review — TASK-049 生产区域输入面（被审 head `a57617f`，分支 head `49256c9`）

- Reviewer：独立子对话（非实现作者），2026-09-19。
- 方法：`git diff 0f8ea3e..49256c9` 全量审读 + 三个 src 文件与关联调用面（`domain/tasks/models.py`、`application/editing/service.py`、`ports/detection/ports.py`、`ports/providers/errors.py`、`infrastructure/sqlite/pipeline.py`、`infrastructure/pipeline/assembly.py`）静态审读 + 实际复跑测试 + 证据文件逐份核对。
- worktree：`G:/CODEX/New Manga.worktrees/TASK-049-zcode`（head `49256c9`，树干净）。

## 1. 白名单核对 — 合规

`git diff --numstat 0f8ea3e..49256c9` 共 18 个文件，全部落在 Task「允许修改范围」：

| 文件 | 判定 |
|---|---|
| `src/infrastructure/providers/handlers.py`（+100/−1） | 允许（providers/**） |
| `src/application/tasks/service.py`（+56/−2） | 允许（application/tasks/**） |
| `src/bootstrap/app.py`（+36/−1） | 允许（装配） |
| `tests/providers/test_detect_handler.py`（+344，新增） | 允许（tests/providers/**） |
| `doc/tasks/TASK-049.md`、`doc/handoffs/TASK-049-a57617f.md`、`verification/TASK-049/**`（11 个） | 允许 |

禁止范围实测为零触碰：`git diff 0f8ea3e..49256c9 -- src/ui/qml src/application/editing src/ui/viewmodels requirements.txt` 输出为空；无 Schema/migration 文件；tests 目录 diff 仅新增 1 个文件（既有断言零改动）；新测试文件无任何 skip/xfail（grep 实证）。STATUS.md 未在本切片改动（Task 台账行属集成步，与 AC ⑥ 的时序声明一致）。

## 2. 代码审读

### 2.1 fail-closed 与错误码链路 — 成立

- `handlers.py:250-254`：`detector is None or region_creator is None` → `ProviderNotConfigured(stage="detect")`；`ports/providers/errors.py:59-62` 确认 `error_code="PROVIDER_NOT_CONFIGURED"`（typed，非静默成功）。
- `handlers.py:277-281`：零候选 → `ProviderInputError`（`INVALID_INPUT`）。
- 保码链：`_typed`（handlers.py:204-215）`ProviderError → StepExecutionError(error.error_code, detail)`；`StepExecutionError(code, detail)`（executor.py:16-20）；`execute_run` 捕获后 `_fail_step(..., error.code, ...)`（service.py:790-791）→ `step_run.error_code`。链路每一环均有代码实证，新用例 `test_detect_fails_typed_without_a_detector` / `test_detect_fails_typed_when_no_candidates` 直接断言 `step_run.error_code`。
- 未发明检测器：生产装配 `detector=None`（app.py:543-546），fail-closed 如实。

### 2.2 Region 写入唯一性 — 成立

- 写入物理路径唯一：`bootstrap/app.py:506-525` 闭包 `_create_detected_region` → `RegionEditingService.create_region(..., origin=RegionOrigin.MACHINE)`（editing/service.py:247-274，首 revision + pointer 单次原子提交）；handler 自身不写 Region。全仓 `create_region` 调用点核验无第二套写入。
- 几何换算仅取整/求 bbox 差，reading_order 透传候选值（0 时以 enumerate 序号兜底），revision 元数据直传 provenance。

### 2.3 回填逻辑 — 正确，边界已收敛

- `_backfill_detected_regions`（service.py:921-944）：仅绑 `region_id is None` 的单元，`region_id` 非 None 的单元不动；一 task 一 target 一页，`_plan_region` 每 task 只可能产生一个 detect 单元，无错绑多 detect 的路径。
- 索引式 while（service.py:760-763）每轮重读 `task.units[unit_index]`，`replace()` 重建 tuple（PlanUnit frozen dataclass，models.py:294-311）后下一单元可见新 region_id；旧 for 迭代确实看不到，改动必要且正确。
- 回填在 `commit_step_result` 内（service.py:650-655），手工驱动 attempt/commit 的契约层路径同样触发。
- 重入语义：detect 失败 → `_cancel_remaining` 取消后续（不产生 step_run）；INTERRUPTED→continue 走 `plan_run(mode="continue")` 全量重建 units（service.py:286-312），此时页已有 Region 则 catalog 展开为区域级 units（models.py:261-279 对 REGION target 兜底合成 snapshot，region 恒非 None）；detect 已 commit 时页级重 plan 命中 handler reuse 分支或 detect stage 已 COMPLETED → SKIP_VALID，不重复建 Region。中断后重复检测仅发生在 detect 未 commit 的中断（与修前 step 重跑语义一致，非本切片回归）。

### 2.4 detect 插入条件 — 对无关命令零影响

- 插入仅当 `region is None and "ocr" in steps`（service.py:346-358）。对照 `_COMMAND_STEPS`（service.py:98-117）：rerender/reinpaint 的 steps 无 `ocr` → 不插；区域级命令（OCR_REGION 等）经 `_validate_command_scope` 强制 REGION scope 且 `region_snapshots()` 对 REGION target 恒产出非 None region（models.py:261-279）→ 不插；页有 Region 时 `_plan_target` 走区域级分支 → 不插。handoff「设计取舍 3」的竞态窗口描述准确。

## 3. 测试复跑（Reviewer 独立实测，未设 QT_QPA_PLATFORM，venv=TASK-012-py312）

| 命令 | exit | 结果 | 证据 |
|---|---|---|---|
| `pytest tests/providers tests/editing tests/core tests/workbench tests/storage -q -rs -p no:cacheprovider` | 0 | **326 passed, 0 skipped** | `verification/TASK-049/review-rerun-targeted.log` |
| `pytest -q -rs -p no:cacheprovider`（全仓） | 0 | **859 passed, 0 skipped**（1 warning = imghdr DeprecationWarning，与切片无关） | `verification/TASK-049/review-rerun-full.log` |
| `pytest tests/providers/test_detect_handler.py -v` | 0 | 5 passed | 本次会话记录 |
| `pytest tests/providers -q -rs` | 0 | 174 passed | 本次会话记录 |

与声明的 326 / 859 完全一致。基线数学自洽：`git diff 560f30e..0f8ea3e` 仅任务文档 1 行，src/tests 与 master 560f30e 相同；既有测试文件零改动 ⇒ 修前基线 = 859 − 5 = 854，与 handoff 声明一致，全仓 passed 未减少（AC ⑤）。

## 4. 判别力复核 — 证据链自洽

1. **import 级**：`discriminating-new-tests-vs-prefix.log` 显示修前树（`TMP-049-prefix`）collection ERROR：`cannot import name 'STEP_DETECT'`。静态实证：`git show 0f8ea3e:src/infrastructure/providers/handlers.py` 中 `STEP_DETECT` 出现 0 次，而新测试第 29-33 行 import 它 ⇒ 修前必 collection error。日志内容与代码事实一致，无伪造嫌疑。
2. **行为级**：`region-{pre,post}-fix-run{1,2}.txt` 数字两两一致且与声明相符（修前无 detect / ocr=INVALID_INPUT / MISSING=1；修后 detect 单元 / PROVIDER_NOT_CONFIGURED / MISSING=0）；探针 `region_probe_pre.py` 无分支逻辑，双树差异只能来自代码。
3. 备注：`TMP-049-prefix` 树现已清理，行为级对照无法独立复跑；判别力结论由 import 级代码事实 + `test_page_level_plan_grows_a_leading_detect_unit`（本树复跑通过）+ 探针日志交叉支撑，判定成立。

## 5. Handoff 声明 vs 事实

逐条核对结果：AC ①-⑤ 的达成声明与代码/证据一致，无夸大；AC ⑥ 如实标注 Review/集成/台账待办。两处表述失准（见 P2-1、P3-3），另有 AC ④ 的说明位置偏差（P3-8）。

## 6. 问题清单

**P0（阻断）**：无。

**P1（需修但可现场修）**：无。

**P2（应改不阻断）**

1. Handoff 第 9 行声明「`git diff --stat 0f8ea3e..a57617f`：仅 3 个 src 文件 + 1 个新测试文件，536 insertions / 4 deletions」与实测不符：该命令实际输出 **8 files, 631/4**——实现提交还包含 4 个 verification 证据文件（probe 脚本 + 3 个对照/log 文件，+95 行）。536/4 仅为 src+tests 子集。数字子集真实、文件均在白名单内，但这是 Handoff 中可复现命令的错误陈述，集成前应修正该句（改为「8 files 631/4，其中 src+tests 536/4，其余为 verification 证据」）。

**P3（备忘）**

1. 测试证据日志只保留 pytest 输出尾部 1-3 行，无命令行、rootdir、时间戳、exit code（`full-suite-post-fix-run{1..5}.log`、`targeted-...log`）；`discriminating-...log` 亦未记 exit code。逐次记录的可复核性弱（本次 Review 已用同环境独立复跑补证，数字全部吻合，不削弱结论）。
2. `tests/providers/test_detect_handler.py:16` `import time` 未使用。
3. Handoff「设计取舍 2」称回填「不落库（只改内存 task）」不准确：`commit_step_result` 在 backfill 后 `_persist(run)`（service.py:657），`SqlitePipelineStore.put` 将含 `tasks.units` 的整个 run 序列化进 `run_json`（infrastructure/sqlite/pipeline.py:330-355, 393-397），回填结果随 run 持久化。语义无害（反而更稳），表述应修正。
4. `HandlerDependencies.detector` / `region_creator` 标注 `Any`（handlers.py:170-171）；`ports.detection.ports.DetectionProvider` Protocol 已存在且该文件已 import 其模块成员，本可做类型标注。风格问题。
5. 退化几何（候选共线或零宽高）会使 `BBox` 校验抛 `ValueError`（domain/regions/entities.py:68-70）→ 被 `execute_run` 兜底为 `STEP_FAILED` 而非 typed `INVALID_INPUT`（bootstrap/app.py:510-517 未预检）。真实检测器接入前的理论边缘，仍 fail-closed 不静默，留给真实检测器切片补 typed 预检。
6. `candidate.reading_order or index`（handlers.py:287）：显式 `reading_order=0` 会被 enumerate 序号覆盖。端口默认 0=未指定，当前语义合理；备忘未来 0-based 语义的 provider 会被静默改序。
7. `test_detect_fails_typed_without_a_detector` 的 `for step in executed.step_runs[1:]`（test_detect_handler.py:208-209）循环体执行 0 次（detect 失败后 `_cancel_remaining` 不产生后续 step_run）——该断言实际为空断言；主断言（`detect_step.error_code`）有效，且系统级对照由探针 MISSING=1→0 补强。
8. AC ④ 字面要求「并在 **Task 内**说明 QML 入口待 TASK-047 后另开切片」，该说明实际落在 Handoff AC ④ 节（Task 文件交付记录仅引用 Handoff）。实质满足，位置偏差备忘。

## 7. 结论

实现切片在白名单内完成且六个 AC 全部达成（AC ⑥ 的 Review/集成/台账为流程后置步，状态标注如实）；fail-closed typed、Region 写入唯一性、回填与重入语义、detect 插入条件收窄均经代码与测试双重验证；复跑数字与声明一致。唯一 P2 为 Handoff 一处 stat 陈述失准（措辞修正即可，不涉及代码）。**approved_subagent**，建议集成时顺手完成 P2 措辞修正。
