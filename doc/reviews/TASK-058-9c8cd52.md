---
task_id: TASK-058
reviewer: Qoder（非作者；外部后置复审，窗口内 Review 为 approved_subagent 同体口径）
author: ZCode（窗口 W11 自建切片）
base_commit: 726c1bb
reviewed_head: 9c8cd52
verified_at: master 3d79403（该切片已集成于 414a8e1，故本轮为可推翻的追认复审）
decision: approved
---

# Review：TASK-058 应用退出排空 workbench 运行线程（P-10）

**一句话**：切片方向正确、写集极小（`src` 只有 `bootstrap/app.py` +14/−2）、判别力真实（我在 base `726c1bb` 复算 3/3 失败，`ImportError`）；**它交付的那版有一个真 P2——`_shutdown_services` 丢弃 `shutdown()` 的排空结果并无条件 `conn.close()`**，该缺陷已由 TASK-060 的 `1195baf` 修掉，我用真装配探针实测确认现在超时路径**不关连接、worker 随后写入成功、进程不崩**。因此对 `9c8cd52` 判 `changes_requested` 已无意义，本轮判 **`approved`（追认）**，另登记 6 条 P3：AC2 的用例从未真正遇到活动 worker（我 5/5 测到 `alive=False`）、8 份证据日志无 `EXIT=` 与环境头却被台账写成"EXIT=0 ×6"、`TASK-058.md:22` 的错路径在我声称已更正后仍在、`AppServices.conn` 类型注解自 TASK-060 起失真、邻片一条恒真断言、未排空只写 stderr 不进诊断包。

## 范围与依据

- 来源顺序：[TASK-058.md](../tasks/TASK-058.md) AC1~AC5 + 允许/禁止范围 + 测试要求表 → [Handoff](../handoffs/TASK-058-5df4168.md)（含 `9c8cd52` 返修记录）→ 我本人提出的原始条目 [POSTHOC-WINDOW-2026-09-19-Qoder](POSTHOC-WINDOW-2026-09-19-Qoder.md) 的 **Q-003 / Q-009 / Q-011** → [09 协作协议](../09_COLLABORATION.md) §6 第 12 条证据纪律。
- 写集（`git diff --numstat 726c1bb..9c8cd52`）：`src/bootstrap/app.py` 14/2、`tests/core/test_shutdown_drain.py` 162/0（新）、Task/Handoff/`verification/TASK-058/**`。**`src/ui/viewmodels/workbench/**` 与 `src/ui/qml/**` 零改动** ⇒ 禁止范围遵守，AC5 成立。
- 受影响调用链：`main()` 两条退出路径（正常 exec 尾部 + bootstrap 失败的 except 分支）都改走 `_shutdown_services`；`WorkbenchViewModel.shutdown:847-858` → `RunController.shutdown` → `_reap_worker`。`_shutdown_services` 的**唯一**生产调用者就是 `main()`，其余调用者全在测试里。
- 后续修正在邻片：`_reap_worker` 与 `ThreadRoutedConnection.close` 的契约 docstring、以及 `drained = ...; if drained:` 分支都来自 `1195baf`（TASK-060），不是本切片。
- 未审/未复跑：真实 GUI 会话下用户点关闭的端到端（`main()` 需要 QML 事件循环，探针只驱动到 `_shutdown_services` 一层）；Windows 上 `temp_root.cleanup()` 与"worker 仍持文件锁"的组合（超时路径现在不关连接 ⇒ 该锁可能仍被持有，见 F-007）；POSIX 分支。

## 标准轴

**总结**：形状干净，是本窗口少数"改动量与风险相称"的切片；问题集中在**它把安全性质寄托在"排空总会成功"这个未证明的前提上**，以及证据口径。

- [认可] 层次正确：退出顺序属于装配层，`bootstrap` 知道 VM 与 conn 的关闭次序是它的职责；没有为此改 VM/Controller 语义，也没有新增存储实现。
- [认可] 复用既有语义：`RunController.shutdown` 的"bounded wait、不强杀"（R-001 注释）被原样使用；超时不 kill 线程是正确的保守选择。
- [认可] **返修（R-001）到位**：模块级 `sys.path` 注入（`test_shutdown_drain.py:30-32`）使文件可单跑——我实测单文件 `3 passed`、目录 `-k` 形态同样绿、且在 base 树单跑能正确失败（`3 failed / EXIT=1`），说明自包含不是靠"先收集别的模块"撑起来的。
- [认可] 第三条用例显式声明"pinning-only，不计判别"——这正是我希望作者主动写的东西。
- [严重] **丢弃返回值**（F-001，当时 P2）：`9c8cd52` 版函数体只有两条语句，`shutdown()` 的 bool 被丢；`_reap_worker` 明确能返回 `False`（预算耗尽、线程仍在写），此时 `close()` 会遍历 registry 关掉**包括 worker 线程在内**的所有真连接 ⇒ 跨线程关闭正在使用的句柄。现状已由 `1195baf` 修正并实测。
- [建议] **注释/注解与类型漂移**（F-005）：`AppServices.conn: sqlite3.Connection`（`app.py:483`）自 TASK-060 起实际是 `ThreadRoutedConnection`；这条失真注解正是"关一个连接能有什么事"这种误判的认知来源。
- [建议] 超时只 `print` 到 stderr（F-007），无重试、无 diagnostics 记录；`temp_root.cleanup()` 随后仍会执行。

## 规格轴（AC1~AC5 逐条）

**总结**：AC1/AC3/AC4(口径除外)/AC5 达成；**AC2 的实质由邻片与我的探针承担，本切片自己的用例从未真正遇到活动 worker**。

- **AC1 达成**：两条退出路径都改走统一函数，顺序固定；`main()` 的 except 分支同样收口（diff 可对应）。
- **AC2 名义达成、实测未覆盖**：AC2 写"真实装配下，**存在活动 run 时**调用 `_shutdown_services` 能让 worker QThread 在返回前排空"。我把这条用例的前提量出来：5 次采样，`_shutdown_services` 被调用那一刻 `vm._controller.is_running` **全是 False**、run 状态**全是 `completed_with_failures`**——mock 步骤太快，run 在断言循环里就跑完了。所以该用例排空的是一个**已经结束的** worker，`is_running is False` 与"连接随后关闭"都平凡成立。活动 worker 的真实排空我另外测了（S4：步骤阻塞 1.0 s ⇒ 调用时 `alive=True`、drain 0.70 s、写入先于 close、close 随后发生），**行为是对的，但仓库里没有这条用例**。⇒ F-002（P3，修法我已验证）。
- **AC3 达成**：base `726c1bb` 上 `3 failed / EXIT=1`，失败原因 `ImportError: cannot import name '_shutdown_services'`（我复跑与作者 artefact 同形）；第三条按声明不计判别。
- **AC4 部分达成**：`911 passed` 的**数字**在 6 份日志里都有；但 8 份日志（`run01..06`、`standalone-file`、`directory-k`）**没有一份带 `EXIT=` 或 shell/venv/env 头**，而 Task 测试要求表写"3 passed, **EXIT=0**""911 passed / 0 skipped, **EXIT=0 ×6**" ⇒ 退出码属"台账声称、artefact 无凭"，正是我自己在 Q-009 里对窗口提的那条，对 058 成立且未修。我补了两次带 EXIT 的合规全仓跑（`930 passed / 0 skipped / EXIT=0` ×2 @ master）。⇒ F-003（P3）。
- **AC5 达成**：`src` 写集只有 `app.py`，VM/Controller/QML 零改动；幂等性由第三条用例钉住。
- **范围纪律**：允许/禁止范围逐条对照无越界；`approval: zcode_window_self_approved` 属窗口临时授权（已在 T1 收口时移除），本切片是"自建 Task + 自批"，故本轮外部复审是它第一次拿到非作者判定。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 状态 |
|---|---|---|---|---|---|---|
| F-001 | **P2（切片当时）→ 已闭** | `9c8cd52:src/bootstrap/app.py` `_shutdown_services` 两条语句（现 `app.py:915-917` 已带分支） | 丢弃 `shutdown()` 的 bool 并无条件 `conn.close()`；`close()` 遍历 registry 关掉**含 worker 线程**的真连接 ⇒ 跨线程关闭正在使用的句柄，与 `_reap_worker` 现契约（"False 时调用方不得关闭共享连接"）相反。生产可达：真实 provider 单步 > 5 s 属常态 | [复现] 现状由我实测为**已修**：S1 步骤阻塞 7 s ⇒ `shutdown()` 返回 `False`、耗时 5.02 s、线程仍在跑、**连接仍开着**、worker 随后写入 `OK - the connection was still usable`、探针进程 `EXIT=0` 无崩溃 | 无需再动代码。要做的是**记账**：把我窗口复审的 Q-003 行标为 closed@`1195baf`，并在 TASK-058 记录里注明"超时不关连接由 TASK-060 补齐，非本切片交付物" | closed-elsewhere（待 Codex 记账） |
| F-002 | P3 | `tests/core/test_shutdown_drain.py:113-153`（AC2 用例） | 用例标题/文档写"with a run executing on the worker thread"，实测 5/5 采样在 `_shutdown_services` 调用点 worker 已不活动（`completed_with_failures`）⇒ AC2 的核心前提（活动 run 时排空）在仓库里**没有任何真装配用例**；`is_running is False` 与"连接随后关闭"都平凡通过 | [复现] `drain-probe-master.log` S2 五行 `worker alive = False`；[复现] 同探针 S4 给出可用形状：步骤 sleep 1.0 s ⇒ `alive=True`、drain 0.70 s、写入先于 close、close 发生 | 把 AC2 改成 S4 形状：给 executor 注入 ~1 s 阻塞（`DeterministicStepExecutor(on_execute=...)` 现成可用，见 `test_drain_shutdown.py:30-47`），断言调用点 `is_running is True` → 返回后 `False` 且连接已关。约 5 行，零产品改动 | open |
| F-003 | P3（证据口径） | `verification/TASK-058/run01..06.log`、`standalone-file.log`、`directory-k.log`；`doc/tasks/TASK-058.md:51-53` | 8 份日志均无 `EXIT=`、无 shell/venv/env 头（`run0*` 首行即 pytest 进度点），但测试要求表写"EXIT=0"与"×6" ⇒ §6 第 12 条要求的"退出码与口径可复核"未满足；这是我 Q-009 对窗口 7 个切片的指控之一，058 未随批次重采 | [记录] `grep -c EXIT=` 八份全 0；`head -6 run01.log` 直接是进度行 | 用带 header+EXIT 的 runner 重采一次全仓（我可提供 `verification/TASK-058/review-9c8cd52/run.sh`），或把表里的"EXIT=0"改成"artefact 仅记录 passed 行" | open |
| F-004 | P3（台账 vs 实际） | `doc/tasks/TASK-058.md:22` | 仍写 `src/workbench/run_controller.py:96`（真实路径 `src/ui/viewmodels/workbench/run_controller.py`）。我在窗口 docs 批次（Q-011）里**记为"已更正"**，实际未落 ⇒ 记账与文件现状不符 | [记录] `grep -n "src/workbench/run_controller.py" doc/tasks/TASK-058.md` 命中第 22 行 | 改路径并连带核对 Q-011 其余三项是否真落地（`TASK-054/055` 勾选、`WorkbenchView.qml` 硬编码色是否已进 TASK-059 输入清单） | open |
| F-005 | P3 | `src/bootstrap/app.py:483` | `AppServices.conn: sqlite3.Connection` 自 TASK-060 起失真（实为 `ThreadRoutedConnection`）。注解是读者判断"close() 影响谁"的第一依据，失真会直接复刻 F-001 的误判路径 | [读码] `assemble_services` 里 `conn=open_database(...)` 返回 facade | 改注解（或 `ThreadRoutedConnection` 加 Protocol 别名）；同批把 `:904-912` docstring 里"single-connection design"的历史措辞更新为 per-thread registry 模型 | open |
| F-006 | P3（恒真断言） | `tests/workbench/test_drain_shutdown.py:116` | `assert sys.stderr is not None` 零判别力（永远为真）。上一行 `assert "drain timed out" in err` 已承担该职责，这条只是噪音——与我在 Q-008 前置②/TASK-062 里反复处理的同一形态 | [读码] 该行无副作用、`sys.stderr` 在 pytest 下恒非 None；删除不影响其余断言（我的定向跑含该文件：`7 passed / EXIT=0`，未改代码） | 删掉，或换成有判别力的断言（例如 `capsys` 里不得出现 traceback） | open（邻片文件，随下次触碰该测试时清） |
| F-007 | P3 | `app.py:918-921`（stderr 分支） | 超时只打印一行 stderr：不进 diagnostics、不重试、不阻止随后的 `temp_root.cleanup()`；若 worker 仍持 `library.db` 句柄，Windows 上临时根清理可能失败或留下句柄直到进程退出（AC 的"最后写者持锁"前提在超时路径不成立） | [读码] `main()` 在 `_shutdown_services` 之后无条件 `temp_root.cleanup()`；我的探针未覆盖该组合（见"未审"） | 诊断包接线（TASK-055/062 线）时把 `drained=False` 记成一条 diagnostics 事实；`cleanup()` 前对超时情形做一次有界重试或明确放弃清理并留痕 | open |

## 结论与后续

**`approved`（后置追认）**。判据：P0/P1 无；F-001 那个真 P2 已由 `1195baf` 闭合并由我实测确认；AC1/AC3/AC5 达成，AC2 的行为正确但缺一条真前提用例（F-002），AC4 的数字有据、退出码无据（F-003）。**这不改变已集成事实，也不需要回滚**——`git revert` 会把 TASK-060 依赖的 `drained` 契约一起拆掉。

**必须被记账的三件事**（都属 Codex 侧）：①我窗口复审的 **Q-003 标 closed@`1195baf`**，别让台账挂着一条已修的 P2；②**Q-011 对 TASK-058 路径的"已更正"未落地**（F-004），要么补上要么撤回该记账句；③Q-009 点名的 058 日志重采仍未做（F-003）。

**给下一片的清单**（一条 docs+tests 小切片即可全吃）：F-002（AC2 用例改成活动 worker 形状，约 5 行）、F-005（`conn` 注解与 docstring 口径）、F-006（删恒真断言）、F-003（重采带 EXIT 的日志）。F-007 挂到诊断包接线的 AC 里。

## 证据位置与落库状态

本报告与我的全部证据在**我自己的 Review worktree**（`G:/CODEX/New Manga.worktrees/TASK-058-qoder-review`，分支 `agent/qoder/TASK-058-review`，checkout @ master `3d79403`；**已 commit 于该本地分支，未 push、未合入 master**）：

- `doc/reviews/TASK-058-9c8cd52.md`（本报告）
- `verification/TASK-058/review-9c8cd52/`：`t058_drain_timeout_probe.py`（真装配 S1~S4 探针）、`run.sh`（带 tree/HEAD/shell/venv/env 头 + `EXIT=` 的 runner）、`drain-probe-master.log`、`targeted-three-forms.log`（058 三条 + 060 四条 = 7 passed）、`discrimination-base-726c1bb.log`（3 failed / EXIT=1）、`full-suite-run{1,2}.log`（930 passed / 0 skipped / EXIT=0）。

**Reviewer 自我记账**：我第一版探针在 S3 把结论硬编码成"the bool is discarded"——那是我从旧记忆带进来的假设，被它自己打印出的源码直接推翻（函数确实分支）。该失败运行未入库为结论，只保留修正后的 `drain-probe-master.log`；教训与我对作者立过的同一条：**别把预期写进打印语句**。另外我最初准备按窗口报告 Q-003 判 `changes_requested`，是实测把结论翻成"已由邻片闭"——读码记忆不等于当前代码。

临时修前树 `C:/Users/49745/AppData/Local/Temp/t058-prefix`（@`726c1bb` + 本切片测试文件副本）不属仓库，报告提交后 `git worktree remove`。作者树 `TASK-058-zcode` 我未进入、未写一字节。

## 追记：报告落库时 master 已前进（锚点复验）

本报告采集于 master `3d79403`；提交时共享主树已被并发推进到 `9f6420b`（其间合入 **TASK-064 diagnostics 生产接线**，`ec42feb`，`src/bootstrap/app.py` +69 行）。我在新 master 上逐条复验本报告的行锚，三处全部仍成立：`drained = services.workbench.shutdown()` 仍在（F-001 的修复未被回退）、`doc/tasks/TASK-058.md` 仍含错路径 `src/workbench/run_controller.py`（F-004 未修）、`tests/workbench/test_drain_shutdown.py` 仍有 `assert sys.stderr is not None`（F-006 未修）。

**F-007 因此从"建议"升格为"有落点的待办"**：TASK-064 之后 bootstrap 里已经存在生产诊断包路径（`diagnostics.py:260` 调 `build_diagnostics_report`，配 `bounded_log_store`），退出排空超时（`drained is False`）现在可以、也应当作为一条事实进包，而不是只打一行 stderr——修法是一行级：把该 bool 连同 run_id 写入既有 bounded log / 报告字段。
