---
task_id: TASK-018
reviewer: Codex
author: DeepSeek Harness
base_commit: 8c63f9b19f6daf065847aefce01bda578332cd6b
reviewed_head: 50633158d98f4382fe17da070454423d992b7496
decision: approved
---

# Review：TASK-018 Mask / Inpainting 路线独立实验（首轮 + 修订切片）

本文件是 TASK-018 的独立 Review 记录，覆盖三轮：首轮交付 `6c33e7f`（`approved`）、修订切片 `d7c10d4`（`changes_requested`）、复审收口 `5063315`（`approved`）。Reviewer 为 Codex，作者为 DeepSeek Harness，二者不同体。

## 固定对象

| 轮次 | base | 被审 head | 元数据 head | decision |
|---|---|---|---|---|
| 首轮 | `dce95ac`（起点 `9ee17189`） | `6c33e7f` | `ef6d1c3` | `approved`（R-001～R-007） |
| 修订切片 | `8c63f9b` | `d7c10d4` | `3b38d39` | `changes_requested`（R-101～R-104；工程实现已复核 PASS） |
| 复审收口 | `3b38d39` | `5063315` | `b10f1ba` | `approved` |

已集成基线：`integration_commit=4d189ce`（merge，parents `461e639`+`ef6d1c3`，被审 head 为 `6c33e7f`），**不因修订切片失效**。

## 首轮 findings 与处置（head `6c33e7f`）

| ID | 级别 | 内容 | 处置 |
|---|---|---|---|
| R-001 | P2 | `viol` 被表述为“保护框内改动数”，实际是 final Mask 外改动数；`manifest.json` 的 `protected_boxes` 未被逐框断言 | 报告口径已于集成收口更正；逐框断言由 `d7c10d4` 实现 → **closed** |
| R-002 | P2 | 路线门控是静态常量，非环境探测；`runnable_here`/`blocked_reason` 不可作环境证据 | `d7c10d4` 改为可探测描述符 → **closed** |
| R-003 | P3 | `--output-dir` 越出实验根抛 `ValueError`、退出码 1 并留部分产物 | `d7c10d4` 回退绝对路径 → **closed** |
| R-004 | P3 | “nearest-neighbour”与实现（正交邻居均值）不符 | 报告口径已更正；数据 `route_label` 保留旧名并加注 → **closed** |
| R-005 | P2 | edge-bleed 覆盖不足未量化，`residual` 被归因为扩散 | 报告补 Mask 内改写覆盖复核数据并改写结论 → **closed** |
| R-006 | P2 | 学习型路线残留未实测体积标注（“数 GB / 10GB+”） | 报告与数据标签均已清理 → **closed** |
| R-007 | P2（潜在） | 非 `simple-fill` 路线回落到 `edge_bleed_fill`，一旦门控变真会产出伪造成果 | `d7c10d4` 引入显式 `FILLERS` + fail-closed 门控 → **closed** |

## 修订切片 findings 与处置（head `d7c10d4`）

| ID | 级别 | 内容 | 复现证据 | 处置 |
|---|---|---|---|---|
| R-101 | P2 | `test_route_gating.py` 自称“pure standard library / no PySide6”，但它 `import run_experiment`（该模块导入 PySide6 做图像 I/O）——声明与实际依赖链不符，且同一说法在 handoff、revision 取证 §3、实验日志 §7.1 重复出现 | 用 `find_spec` 级屏蔽 PySide6 后，`import run_experiment` 与 `import test_route_gating` 均 `ImportError` | **fixed**（`5063315` 改 docstring 为“测试代码本身只用标准库，但经 `run_experiment` 间接依赖 PySide6”；日志 §7.2 记录更正） |
| R-102 | P2 | `results/experiment.json` 已由 `d7c10d4` 重生成（v1→v2），但报告 §4 表与日志 §5 仍是首轮 `6c33e7f` 的耗时/RSS，且 §4 结论 4 的“~32–46 ms”上限已被新值 48.609 ms 越过 | 解析报告 §4 表逐行比对提交的 JSON | **fixed**（`5063315` 更新三列为当前值、加“数据归属”注、同步结论 4/5 与 §7 OOM 行） |
| R-103 | P3 | `doc/tasks/TASK-018.md` 同节出现两个“当前状态（唯一）”块，且正文称 `done` 而 frontmatter 为 `in_review` | 全文 `当前状态（唯一` 计数 = 2 | **fixed**（`5063315` 合并为单一状态块，去重标题，统一为 `in_review`） |
| R-104 | P3（可选） | 建议加启动断言：`ROUTES[*].implementation` 非 None ⇒ 该 route 必须在 `FILLERS` 注册，否则误配会在运行中途 `KeyError` | 反例 V2：把 `manga-lama.implementation` 改为已存在的 filler 名 → 中途 `KeyError`、无伪造成果 | **部分处置**（`5063315` 实施了等价的“导入来源”断言：若 `run_experiment` 不来自本目录即失败，防静默测错 harness）；原建议的 `implementation ⇒ FILLERS` 一致性断言未实施，登记为 deferred 观察项（误配时仍为响亮失败，不产生伪数据） |

### 复审收口验证（head `5063315`，本 Reviewer 实际执行）

| 场景 | 命令 | 结果 |
|---|---|---|
| 协议自检 | `python -m unittest experiments/TASK-018/test_mask_protocol.py` | **12 passed / 0 skipped**，退出码 0；该文件本轮**未被改动** |
| 门控与探测测试 | `python -m unittest experiments/TASK-018/test_route_gating.py` | **13 passed / 0 skipped**，退出码 0 |
| 变更范围 | `git diff --name-only 3b38d39 5063315` | 4 个文件；**不含** `test_mask_protocol.py`、`results/experiment.json`；`doc/STATUS.md`/`00_INDEX`/`12_ROADMAP`/`tasks/README`/`src`/`tests`/`AGENTS.md` 命中 0 |
| 数字可定位性 | 解析报告 §4 表 10 行 vs `results/experiment.json` | **10/10 逐值一致，0 处不符** |
| 资源区间 | `max latency_ms_max=48.609`、`max peak_rss_mb_after=57.55` vs 报告“≤ 48.7 ms / ≤ 57.6 MB” | 区间成立 |
| 空白/规范 | `git diff --check 3b38d39 5063315` | 退出码 0 |

## 集成前已复核的工程结论（不改动）

- `d7c10d4` 的 fail-closed、真实探测、越界输出、逐框保护证据、标签清理，以及"基线测量相对 `4d189ce` 无回归（`output_sha256`/`residual`/`mask_sha256` 0 差异）"均经本 Reviewer 独立复跑确认，`5063315` 未触碰任何测量数据与测试文件。
- 仍未解决项保持不变、不得视为通过：四条学习型路线的质量/耗时/内存/显存 **`BLOCKED`**；Mask 内部结构损伤量化、真实 OOM、真实漫画样例 **`NOT_RUN`**；模型 Hash `NOT_AVAILABLE`。生产 Router 属 **TASK-019**，本 Review 不释放任何冻结 Task。

## 结论

**`approved`**：固定 `reviewed_head=5063315` 可交 Codex 集成。修订切片的两条 P2（R-101、R-102）已按事实修正并经本 Reviewer 独立核对；R-103 已修；R-104 以等价防护部分落地并登记 deferred。无 P0/P1，无关键验证缺失。

剩余风险：R-104 的 `implementation ⇒ FILLERS` 一致性断言缺失（误配为响亮失败，不产生伪数据）；学习型路线与 Mask 内部结构损伤维度仍无证据。本结论仅对该固定 head 有效，head 变化不延用。
