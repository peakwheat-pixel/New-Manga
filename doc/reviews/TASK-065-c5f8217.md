---
task_id: TASK-065
reviewer: Codex independent review agent (non-author)
author: Codex implementation agent
base_commit: 772d63c2c921a08606d742438a146cb310159b02
reviewed_head: c5f8217929b0c5170b8bf5e54ec915f4ba37aa46
decision: changes_requested
---

# Review: TASK-065

## Scope and basis

Reviewed the fixed range `772d63c2c921a08606d742438a146cb310159b02...c5f8217929b0c5170b8bf5e54ec915f4ba37aa46`, including implementation `73a7557`, Handoff `c5f8217`, the T1.3.2 definition in `REBASELINE_PLAN.md`, TASK-065 AC1-AC5, TASK-058 Review F-002/F-003/F-005/F-006, the affected shutdown call chain, and all submitted verification artefacts. No implementation file was modified by this review.

## Standards (executed)

- **P1 hard violation:** `verification/TASK-065/{targeted,integration,full-suite}.log` all record `HEAD=772d63c...`, while the Handoff presents them for delivery `73a7557`. A pre-commit dirty worktree is the only recorded execution state. This violates `09_COLLABORATION.md` section 6 items 11-12: results apply only to their recorded commit, and claimed runs require committed, reproducible evidence.
- **P1 hard violation:** `doc/handoffs/TASK-065-73a7557.md` claims a discriminating RED result (`1 failed / EXIT=1`) without an artefact. Section 6 item 12 requires missing discrimination evidence to be recorded as `NOT_RUN`.
- **P2 hard violation:** `doc/tasks/TASK-065.md` still says `in_progress`, final verification `NOT_RUN`, Handoff absent, and actual tests absent, contradicting the fixed Handoff. This violates the timely Task-state/evidence update rules in sections 3-4.

No Fowler smell was found. The changed paths are all allowed; no product behavior, Schema, dependency, QML, storage, cleanup, or unrelated Task escaped scope.

## Spec (executed)

- **P1 / AC1 partial:** `tests/core/test_shutdown_drain.py` signals `worker_entered` and then sleeps for one second. `RunController.is_running` only tests whether `_active_run` has been cleared; its completion callback is queued to the GUI thread. The assertion can therefore remain true after the worker step or run has already finished, especially if the test thread is descheduled, so the test does not prove that `_shutdown_services` is invoked while work is still executing. Keep the step blocked, with a timeout, until shutdown sets `run.cancel_requested`.
- **P1 / AC2-AC5 evidence partial:** the three submitted logs are complete in shape (shell, interpreter, environment, commands, counts, EXIT, and six individual skip reasons), but bind the base rather than either delivery/review head. Re-run them on a committed delivery head.
- **P2 / ledger partial:** update TASK-065 to `in_review`, check completed implementation ACs, and link the Handoff and executed evidence. AC5 remains incomplete until this Review is approved.

AC3 is accurate: `AppServices.conn` is a `ThreadRoutedConnection`, and its `close()` closes every registered per-thread connection. AC4 removes only the tautology and unused import while retaining the discriminating stderr assertion. No skip/xfail was added, and collection remains 951 in the submitted full-suite log.

## Findings

| ID | Severity | File/line | Trigger and impact | Reproduction evidence | Recommendation | Status |
|---|---|---|---|---|---|---|
| R-001 | P1 | `tests/core/test_shutdown_drain.py:127-150`; `src/ui/viewmodels/workbench/run_controller.py:66-68,139-145` | A timed sleep ends independently of shutdown, while `is_running` can stay true until the GUI thread handles the queued completion signal. AC1 can pass without an active worker at `_shutdown_services`. | Static call-chain inspection; reviewed-head targeted tests pass but do not distinguish this stale-slot case. | In `slow_step`, set the entered event and bounded-wait for `run.cancel_requested`; shutdown is then the event that releases the executing step. | open |
| R-002 | P1 | `verification/TASK-065/targeted.log:4`, `integration.log:4`, `full-suite.log:4`; Handoff lines 34-36 | All PASS evidence records base `772d63c`, not delivery `73a7557` or reviewed head `c5f8217`; the delivery is therefore unverified under the repository's commit-binding rule. | Direct log header inspection. | After the AC1 fix is committed, rerun targeted, integration, collect-only, and full suite; commit logs that record the new delivery HEAD. | open |
| R-003 | P1 | `doc/handoffs/TASK-065-73a7557.md:20` | The claimed `1 failed / EXIT=1` discrimination run has no committed artefact, so it must be treated as `NOT_RUN`. | `git ls-tree -r c5f8217 verification/TASK-065` contains no RED/discrimination log. | Add the raw RED artefact with shell/interpreter/environment/command/EXIT, or remove the claim and mark it `NOT_RUN`. | open |
| R-004 | P2 | `doc/tasks/TASK-065.md:5,51-63` | The Task ledger contradicts its Handoff and submitted logs, obscuring the actual review state. | File inspection at `c5f8217`. | Move to `in_review`, link the Handoff/evidence, and record actual results; leave AC5 unchecked until approval. | open |

## Verification

| Scenario | Command/check | Environment/commit | Result | Evidence |
|---|---|---|---|---|
| Fixed-head shutdown tests | `python -m pytest tests/core/test_shutdown_drain.py tests/workbench/test_drain_shutdown.py -q -p no:cacheprovider -rs` | PowerShell; TASK-012-py312; `PYTHONPATH=src`; `PYTHONDONTWRITEBYTECODE=1`; `QT_QPA_PLATFORM` unset; `c5f8217` | PASS: 7 passed / 0 skipped / EXIT=0; supplemental reviewer run only | Review session output; no new verification artefact committed |
| Added skip/xfail scan | Diff scan for added pytest/unittest skip/xfail markers | `772d63c...c5f8217` | PASS: none added | `git diff ... -- tests` |
| Diff whitespace | `git diff --check 772d63c...c5f8217` | fixed range | PASS / EXIT=0 | Review session output |
| Author full regression | Submitted `verification/TASK-065/full-suite.log` | Header records `772d63c`, not delivery | FAIL as delivery evidence; content says 951 collected / 945 passed / 6 skipped / EXIT=0 | Submitted log |
| Discrimination RED | Claimed old-fixture run | No artefact | NOT_RUN under section 6 item 12 | Handoff prose only |

**Architecture:** PASS. The production edit is annotation/docstring-only, reflects the actual connection facade, and introduces no dependency or runtime change.

**Verification:** FAIL. The active-worker premise is not deterministically held through shutdown, and the committed logs do not bind the delivery head; key discrimination evidence is missing.

## Decision and re-review

**Changes Requested.** Do not integrate `c5f8217`. No production-code change is requested: fix the AC1 test synchronization, refresh commit-bound evidence, synchronize TASK-065/Handoff, and submit a new fixed head for independent re-review. The untracked `.superpowers/` directory was pre-existing and was not touched.
