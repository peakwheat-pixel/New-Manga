---
task_id: T1.3.1 / TASK-057
reviewer: Codex（独立非作者 Reviewer，Round 3）
author: ZCode（固定交付）/ Codex（Rebaseline Integrator）
base_commit: 676319bb1b9e9682b13402c322fded43bbaa90c8
reviewed_head: 85832bbaa656ebbfd53351edd65cbb35eee35ee9
decision: changes_requested
---

# Review：T1.3.1 / TASK-057 Rebaseline Integration — Round 3

## 范围与依据

固定全范围为 `676319bb1b9e9682b13402c322fded43bbaa90c8..85832bbaa656ebbfd53351edd65cbb35eee35ee9`；本轮返修范围为 Round 2 reviewed candidate `d7e9aa9f1e8981d6bd12bc7ca6eea8453c3d67ab..b2dd525ef503886df9814e8dc8367bfeaf5b8773`。候选 `85832bb` 的父提交是产品返修 `b2dd525`，其后只更新本 Task/Handoff 与加入 `*b2dd525*` 验证日志。

已读取 `AGENTS.md`、Rebaseline T1.3.1、`STATUS.md`、协作协议、TASK-057、integration Handoff、Round 2 Review、Review 模板、相关 D02/D03/D06/D07 条款及完整实现/测试/diff。工作树原有 `?? .superpowers/` 未触碰。

## Round 2 findings disposition

| ID | Round 3 disposition | 实证 |
|---|---|---|
| R-002 | **fixed** | read-back、Hash、sidecar 均针对临时 DB；canonical 发布顺序为 manifest → DB，ledger 最后写。DB publish 后注入 `SystemExit`，`finally` 清除 canonical DB/manifest/temp 且 ledger=0。 |
| R-010 | **fixed** | v0 restore 后复用正式 migrations 升到 latest，再执行 live integrity/schema/reference check；主 restore 在实际覆写后失败会用 pre-restore 恢复原 live；主/rollback 双失败返回 `RESTORE_ROLLBACK_FAILED` 并保留可验证的 pre-restore DB+manifest 凭据。 |

旧 R-001/R-003/R-004/R-005/R-006、R-008/R-009 的定向测试与调用顺序均未回归。R-007 继续适用 evidence-hygiene waiver：全范围 `diff --check` 的 228 行只来自固定交付随附的 **12 个**历史 raw log 文件；当前源码/测试、`d7e9aa9..b2dd525` 产品返修及 `b2dd525..85832bb` evidence 子范围均 clean。

## Standards（executed）

R-002/R-010 的实现符合 TASK-057 AC③、D07 §31/§49/§80；连接/Worker/latch 边界、允许路径和无新增 skip/xfail 均符合仓库标准，未见需单列的 Fowler smell。

仍有 1 项 hard finding：`restore_backup()` 的前置验证阶段不在 typed error 边界内。`src/infrastructure/sqlite/backup.py:199` 的 `sha256_file(target)` 遇到 Windows 文件访问 `OSError` 时原样逃逸，违反 TASK-057 AC④“失败返回 typed 原因”和 `src/application/maintenance/backup.py:13` 的 “every failure is typed”。

## Spec（executed）

R-002/R-010 的明确要求均已实现：无半成品发布、v0 升级、live 三类 post-check、主失败回滚、双失败 typed+凭据；未见 scope creep 或错误实现。AC⑥仍按正常生命周期等待本轮 Review、master integration 与 STATUS closure。

但 AC④ 的全称要求仍因下列 R-011 部分缺失，因此候选尚不能批准。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 状态 |
|---|---|---|---|---|---|---|
| R-011 | P1 | `src/infrastructure/sqlite/backup.py:193-200` | restore 在创建 pre-restore 之前计算目标 hash；该文件读取发生 `OSError` 时没有映射为 `BackupVerificationError`。live DB 虽尚未写入，但调用者拿到无 `code` 的裸异常，违反 AC④及应用层 typed 契约。 | Reviewer 在合法已登记 backup 上仅注入 `sha256_file -> OSError('forced read failure')`：`PREVALIDATION_TYPE=OSError CODE=None`；live 仍为 schema 3、revision count 1。 | 在 restore 前置读取/校验边界将 `OSError`（以及同阶段可能的 `sqlite3.Error`）映射为稳定 typed code；补一条前置 hash/read I/O failure 判别测试并断言 live/ledger 未写。 | open |

## 验证

统一环境：Windows PowerShell 7.6.6；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3）；`PYTHONPATH=src`（对抗 probe 另含 `tests/storage`）；`PYTHONDONTWRITEBYTECODE=1`；`QT_QPA_PLATFORM` 未设置；HEAD=`85832bbaa656ebbfd53351edd65cbb35eee35ee9`。Reviewer probes 仅使用系统临时目录。

| 场景 | 命令/步骤 | 结果 |
|---|---|---|
| targeted | `python -m pytest tests/storage/test_backup_restore.py tests/workbench/test_restore_gate.py -q -p no:cacheprovider -rs` | **PASS：29 passed / EXIT=0** |
| affected integration | targeted + connection ownership/eviction + run-thread e2e + schema migration | **PASS：47 passed / EXIT=0** |
| Round 3 三条新行为测试 | abrupt publish + v0 restore + restore failure rollback，逐项 `-vv` | **PASS：3 passed / EXIT=0** |
| chokepoint probe | `python verification/TASK-057/review-29546f7/t057_chokepoint_probe.py <worktree>` | **PASS / EXIT=0**：nested latch、状态半写、Worker birth 均安全 |
| full regression | `python -m pytest tests -q -p no:cacheprovider -rs` | **PASS：975 passed + 6 skipped / EXIT=0**，1 warning；6 条均为既有 `openssl unavailable`，无 xfail |
| 作者 evidence 审计 | `verification/TASK-057/rebaseline-integration/*b2dd525*` | **PASS**：targeted 29、integration 47、probe 0、full 5× 975+6；HEAD 均为 `b2dd525`、SOURCE_TREE_STATE 空、EXIT=0 |
| publish order | 包装 verify/hash/write_sidecar/replace 记录真实调用顺序 | **PASS**：temp verify → temp hash → temp sidecar → canonical manifest → canonical DB → ledger completed |
| DB publish 后中断 | 新行为测试在真实 DB replace 后抛 `SystemExit` | **PASS**：backup root 空、ledger=0 |
| v0 + live checks | 真实 v0 → live v3 → restore，配置 managed root | **PASS**：schema=3、integrity=ok、reference check 实际执行（`checked=0, skipped=''`） |
| 主 restore 写后失败 | 首次 `_restore_database` 真实覆写后再抛错，第二次真实 rollback | **PASS**：`RESTORE_FAILED`、2 calls、原 live 两份 revisions 恢复、含 pre_restore 凭据 |
| 主/rollback 双失败 | 主写后抛错，rollback 再抛错 | **PASS**：`RESTORE_ROLLBACK_FAILED`、2 calls；detail 含 pre_restore；对应 DB+manifest 存在且 schema=3 |
| restore 前置 hash I/O | 合法 backup 上注入 `sha256_file -> OSError` | **FAIL**：裸 `OSError`、code=None；R-011 |
| skip/xfail | base/head `git grep` 与 test diff | **PASS**：两端源码命中均为既有 5 处，无新增 |
| diff check | 产品返修、当前 evidence、全 `src tests`、全范围 | 前三项 **EXIT=0**；全范围 **EXIT=2**，仅历史 raw logs，按 R-007 waiver |

Architecture：**approved for R-002/R-010, changes_requested overall**。原子发布、恢复迁移与回滚结构成立；剩余 R-011 位于 restore service 自身错误契约边界。

Verification：**changes_requested**。29/47/975+6、5×作者证据和重点对抗路径均通过，但 AC④ 的前置 I/O typed failure 已被 Reviewer 独立击穿且无持久测试。

## 结论与复审

**Changes Requested。** 固定候选 `85832bbaa656ebbfd53351edd65cbb35eee35ee9` 暂不可合入 master。R-002、R-010 已关闭；新 R-011 是唯一未闭合 P1，其余旧 findings/gaps 已关闭或按既定 evidence waiver 处理。

推荐下一步：仍留在 T1.3.1/TASK-057，只修改 `src/infrastructure/sqlite/backup.py` 与 `tests/storage/test_backup_restore.py`，给 restore 前置 hash/read I/O 增加 typed mapping 和一条判别测试；重跑 targeted、affected integration、chokepoint、full suite 后固定新 head 复审。不得修改本 Review 的历史结论。
