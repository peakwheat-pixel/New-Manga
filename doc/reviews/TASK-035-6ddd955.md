---
task_id: TASK-035
reviewer: Codex（非作者独立 Review）
author: DeepSeek Harness
base_commit: 2e1bf2d3d9610c1c53537f45abc12e82e8e5ee4f
reviewed_head: 6ddd955
metadata_head: 092957e
decision: approved
---

# Review：TASK-035 修复渲染层 SFX Policy Gate 缺失 region_type 前置（F-1 渲染面）

Reviewer=Codex（**非作者**；作者=DeepSeek Harness）。固定 `base_commit=2e1bf2d`（分支起点 `e8e1750` = 释放纯文档提交）、被审 `reviewed_head=6ddd955`（元数据 `092957e`）。本文只对被审 head 有效；分支随后变化不延用本次批准。

**执行口径（强制，2026-09-17 用户批准）**：四轴 = **Standards** + **Spec**（`code-review` 技能两轴，分别报告、不合并、**不跨轴排名**）+ 本仓库既有的 **Architecture** + **Verification**。

**并行偏差声明（§6 第 6 条）**：本环境未能提供两条独立 sub-agent 线程（此前为 TASK-029 派发的双轴线程仍未取得执行槽，`list_threads` 无对应任务）。故按该条**兜底**执行：Standards 与 Spec 各做**一遍相互隔离**的检查——两轴分别独立取材、独立成文，未用一遍通读充当两轴——并在此显式声明该偏差。替代做法的证据来源：Standards 取材自 `AGENTS.md`／[09 协作协议](../09_COLLABORATION.md) §6／[02 技术架构](../02_TECHNICAL_ARCHITECTURE_.md)／本 Task 允许-禁止范围 + 全量 diff 与依赖扫描；Spec 取材自本 Task AC → D06 §85/§47 → D08 AC-SFX-001/002/003 → D03 §7 → STATUS 用户裁决，且以**独立重跑**（非引用作者日志）为准。

## 范围与依据

- 被审 diff：`git diff --name-only e8e1750 092957e` = **18 个路径**（非证据 6：代码/测试 4 + `doc/tasks/TASK-035.md` + `doc/handoffs/TASK-035-6ddd955.md`；证据 `verification/TASK-035/**` 12），越界 **0**；`git diff --name-status` 逐条比对允许范围（`src/application/rendering/**`、`src/domain/tasks/models.py`、`tests/rendering/**`、`tests/pipeline/**`、`doc/tasks/TASK-035.md`、`doc/handoffs/TASK-035-*.md`、`verification/TASK-035/**`）全部命中。
- 受影响调用链（已读完整代码，非只看摘要）：`RenderService.rerender_page`（D06 §41 批量）→ `_prepare_region(allow_sfx=False)`；`RenderService.rerender_region`（§47 单 Region）→ 自身门控 → `_prepare_region(allow_sfx=True)`；`_prepare_region` 仅这两处调用（`rg` 全仓 3 命中，含定义）。
- 共享契约：`application.translation.context.gate.decide_sfx_translation`（planner `application/tasks/service.py:178`、Translate Step `context/step.py:168`、渲染层 `rendering/service.py:537` 三处唯一实现）。
- **未审到**：真实模型/端点端到端图像质量（本环境无端点/权重，NOT_RUN）；`src/ui/**`（不在允许路径，本 Task 未触碰）。

## Standards

轴状态：**`executed`**。依据仓库记录在案的标准逐条核对：

- **AGENTS.md 范围与角色**：①实现仅落允许路径、越界 0；②Reviewer=Codex 与作者 DSH 分离，非作者；③未改 Schema/migration、依赖清单、pipeline seam 本体、`AGENTS.md`、`src/ui/**`、其他 Task；④交付 Handoff + Task + 证据齐备。**无硬性违反**。
- **[09 协作协议](../09_COLLABORATION.md) §6**：固定 base/reviewed_head 已在 Task 与 Handoff 登记；作者未自批、未自标 `done`（`status=in_review` 保持）。**合规**。
- **[02 技术架构](../02_TECHNICAL_ARCHITECTURE_.md) 依赖方向**：新增 `src/domain/tasks/models.py → domain.regions.entities`（domain→domain，同层）；新增 `src/application/rendering/service.py → application.translation.context.gate`（application→application，同层）。**无反向依赖**（`src/domain/regions/entities.py` 仅导入 stdlib，不导入 `domain.tasks`，无环；已实测 `import domain.tasks.models` 通过）。
- **工具已强制项跳过**：未重复报告 formatter/linter/类型检查。
- **Fowler smell baseline（判断项，非硬性违规）**：命中 1 项 —— "**possible Shotgun Surgery / 泄漏的策略词汇**"：`rendering/service.py:540` 的逃生口仍直接命名 `SfxPolicy(sfx_policy) is SfxPolicy.MANUAL`，即"`sfx`+`manual` 可由显式手动命令覆盖"这一知识同时存在于共享规则（→ `skipped`）与渲染层（→ 逃生口）。这是本 Task 刻意保留的 §47 逃生口而非第二份策略映射（非 SFX 分支在 `action.skipped is False` 时提前返回，永远走不到该行），但策略**取值**词汇仍出现在渲染层。详见 R-03。

**本轴小结**：发现 **0 项硬性违反记录在案标准**、**1 项 baseline 判断项**（R-03，P3，非阻塞）；本轴最严重项 = R-03。

## Spec

轴状态：**`executed`**。来源顺序：本 Task AC → D06 §85/§47 → D08 AC-SFX-001/002/003 → D03 §7。

**来源原文对照**：D06 §85 前置明写 `region_type = sfx`，`skip` → `Translation / Mask / Inpaint / Translated Render → SKIP_POLICY`、`manual` → 自动链 `SKIP_POLICY` 且"用户保存人工 final 并明确发起替换后才可进入图像处理"、`translate` → 进入正常 Translation；D06 §47 = 单 Region `Force Render`，允许在 Translation/Inpaint Lock 下执行；D08 AC-SFX-001 = `region_type = sfx` 且未覆盖 → `SKIP_POLICY` / `reason = sfx_skip`；D03 §7 = SFX 策略默认 `skip`。

① **缺失/部分完成**：**无**。AC ①～⑥ 逐条对应到 diff 与可执行证据：① 渲染层与 planner 同一判据（`_sfx_gate_allows` → `decide_sfx_translation`，非 SFX 任何取值不参与）→ `rendering/service.py:242-248`、`:355-357`；② 单一规则来源（渲染层两处 `SfxPolicy(...)` 判定删除）→ 全层 `rg` 仅余逃生口 1 行；③ 修正固化缺陷用例并新增真实默认回归 → `tests/rendering/test_rerender.py`（`make_region` 增 `region_type`/`sfx=None`，两例显式 `region_type="sfx"` 且断言逐字保留，新增 7 例）；④ `RegionSnapshot.sfx_policy` 默认对齐 → `models.py:186 = SfxPolicy.SKIP.value`；⑤ `tests/pipeline` 夹具默认对齐 + 三方一致守卫；⑥ 主套件与全仓分列结果（见 Verification）。
② **diff 中来源未要求的行为（scope creep）**：**无**。`RegionSnapshot` 默认改动（AC ④）、夹具默认改动（AC ⑤）、提示文案措辞属 AC 覆盖范围；未新增公开接口、未改 Schema/migration/依赖/seam。
③ **看似实现但实现有误**：**无**。逐条核对 D06 §85 三段语义：非 SFX 永不被该策略拦截（前置，实测 12 组合矩阵一致）；`sfx`+`skip` **任何情况下**拦截（含批量与单 Region，PASS）；`sfx`+`manual` **仅** `allow_manual_sfx=True` 放行，对应 §85"明确发起替换"/§47 显式单 Region 强制渲染（PASS）；`sfx`+`translate` 进入正常渲染（PASS）。`sfx` 取值域外由共享 gate `ValueError` fail-closed，与 TASK-032 规划层同口径；返回顺序未调整（N-3 已声明，见 Findings 接受项）。

**本轴小结**：发现 **0 项**（缺失 0 / scope creep 0 / 实现有误 0）；本轴最严重项 = 无（六个 AC 与 D06 §85 三段语义逐条吻合）。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-01 | P3 | `verification/TASK-035/changed-paths.txt`（`### whitespace` 段） | 该文件记录 `git diff --check 2e1bf2d..HEAD` **exit=0**，实测 **exit=2**（14 处尾随空白，全部位于 `verification/TASK-035/discriminative-prefix.log` 的 pytest `E ` 行与续行缩进内，即被捕获的原始 pytest 输出）。影响：边界证据的可复现性声明不准确；**不影响代码**（代码交付范围实测干净）。与 TASK-032 R-01 同类。 | `git diff --check 2e1bf2d..HEAD` → exit 2，仅 `verification/TASK-035/discriminative-prefix.log` 命中（14 处）；`git diff --check e8e1750..6ddd955` → exit 0 | 不篡改作者证据材料；由集成方在 `verification/TASK-035/integration-*.md` 以实测更正该声明口径（区分"代码交付范围干净"与"证据日志含原始 pytest 尾随空白"） | open → 已由集成记录更正（`verification/TASK-035/integration-bd0d030.md`） |
| R-02 | P3 | `tests/providers/**`（4 个用例的裸 `from conftest import …`） | 同一 pytest 调用中先收集到 `tests/editing/conftest.py` 时解析到错误 conftest → `ImportError: cannot import name 'FakeTransport' from 'conftest'`，4 个 collection errors。影响：测试收集顺序相关的既有基础设施缺陷，**与本 Task 无关**（`tests/providers`/`tests/editing` 均不在本次 diff 内）。 | 实测 HEAD：`pytest tests/providers tests/editing -q` → **4 errors**（exit 2）；`pytest tests/editing tests/providers -q` → **137 passed**（exit 0） | 转 [TASK-034](TASK-034.md)（测试与分层硬化）把共享替身移入唯一命名 helper 模块并改导入；本 Task 禁止范围明确不得处理 | deferred（登记转 TASK-034） |
| R-03 | P3 | `src/application/rendering/service.py:540` | 逃生口仍直接命名策略取值 `SfxPolicy(sfx_policy) is SfxPolicy.MANUAL`。影响：策略**取值词汇**在渲染层仍出现一次；语义正确（非 SFX 提前返回，不可能误放行），但日后若共享规则改写"manual 可被覆盖"的定义，渲染层需同步。架构判断项，非缺陷。 | 代码通读 `rendering/service.py:520-540`；`rg "SfxPolicy\." src/application/rendering/` 仅此 1 处策略取值引用 | 后续切片可让共享规则显式暴露"该 skip 是否可被人工显式覆盖"，渲染层只读该标志，不再自行命名策略值 | open（非阻塞） |

**作者声明的越界项裁处**：

- **N-2（非 SFX 的域外策略不再抛错）**：**接受**。这是 AC ① + D06 §85 前置的必然结果（门控只服务 `region_type=sfx`），且实体 setter（`Region.sfx_policy = SfxPolicy(value)`）与 Schema 已保证落盘值在域内，实际不可达；不构成 fail-open。
- **N-3（`rerender_region` 策略校验位于"缺 Clean / 无 final"之后）**：**接受为不变更**。返回顺序与修前一致，未引入回归；若产品希望策略优先，属用户裁决事项，本 Task 不擅自调整。

## 验证

环境：Windows `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；全部 `-p no:cacheprovider`。**下列数字均为 Reviewer 独立重跑所得**，非引用作者日志。

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| 主套件 | `python -m pytest tests/rendering tests/pipeline -q -p no:cacheprovider -rs` | `6ddd955` | **PASS** 124 passed / **0 skipped**，exit 0（与作者一致） | 本次重跑 |
| 强制回归 | `python -m pytest tests/core tests/storage tests/providers -q -p no:cacheprovider -rs` | `6ddd955` | **PASS** 159 passed / **0 skipped**，exit 0 | 本次重跑 |
| 全仓 | `python -m pytest -q -p no:cacheprovider -rs` | `6ddd955` | **PASS** 679 passed / 6 skipped，exit 0（**连续 2 次一致**）；6 条 skip 全为既有 `tests/network` 的 `openssl unavailable`（`test_connection_tester.py:106`、`test_transport_tls.py:39/47/62/69/83`） | 本次重跑 ×2 |
| 判别力（独立复验） | `git archive e8e1750` 导出树 → 覆盖本次 `tests/rendering/test_rerender.py` + `tests/pipeline/test_pipeline.py` → `pytest tests/rendering tests/pipeline -q` | 修前 `src/` + 新用例 | **PASS**（符合声称）**5 failed / 119 passed**，exit 1；失败 = 4 条"非 SFX 不得被拦" + 1 条三方默认一致守卫；"SFX 仍被拦""SFX+translate 正常"两例在修前也通过（证明同时固定缺陷与原意） | 本次重跑（临时导出树） |
| 门控语义 | 通读 `_sfx_gate_allows` + 共享 gate，核对 12 组合矩阵；核对 D06 §85/§47 原文 | `6ddd955` | **PASS** 矩阵与共享规则逐条一致：`speech`×{skip,manual,translate}×{allow_manual False,True} 全放行；`sfx`×skip 始终拦截；`sfx`×manual 仅显式放行；`sfx`×translate 放行 | 本次审查 + `defaults-and-gate-matrix.txt` |
| 单一规则来源 | `rg "SfxPolicy\|decide_sfx_translation\|sfx_policy" src/application/rendering/` | `6ddd955` | **PASS** 渲染层无第二份策略映射；`_prepare_region` 仅 2 个调用方（`rerender_page` `allow_sfx=False`、`rerender_region` `allow_sfx=True`），全部覆盖 | 本次审查 |
| 依赖方向/环 | 读 `src/domain/regions/entities.py` 导入表；`rg "domain\.tasks" src/domain/regions/`（0 命中）；实测 `import domain.tasks.models` | `6ddd955` | **PASS** domain→domain 同层、无环；application→application 同层 | 本次审查 |
| 范围与边界 | `git diff --name-status e8e1750 092957e` 逐条比对允许范围 | `092957e` | **PASS** 13 路径全在允许范围，越界 0；未触碰 Schema/migration/依赖清单/seam/`AGENTS.md`/`src/ui/**`/其他 Task | 本次审查 |
| 空白检查 | `git diff --check e8e1750..6ddd955`；`git diff --check 2e1bf2d..HEAD` | `6ddd955` / `092957e` | 代码交付范围 exit 0；全范围（含证据日志）exit 2 → **R-01** | 本次重跑 |
| N-1 越界复核 | `pytest tests/providers tests/editing -q`；`pytest tests/editing tests/providers -q` | `6ddd955` | **PASS（与作者描述一致）** 顺序 A → 4 collection errors（exit 2）；顺序 B → 137 passed（exit 0）；两目录均不在本次 diff → 既有问题 | 本次重跑 |
| 真实端点/模型端到端 | — | — | **NOT_RUN**（本环境无端点/权重/依赖；不因本 Task 而改变，不得视为通过） | — |

**四轴结论（逐轴声明，不跨轴排名）**

- **Standards：`executed`** —— 发现 0 项硬性违反记录在案标准；1 项 Fowler baseline 判断项（R-03）。本轴最严重项 = R-03（P3）。
- **Spec：`executed`** —— 发现 0 项（缺失 0 / scope creep 0 / 实现有误 0）；AC ①～⑥ 与 D06 §85 三段语义逐条吻合。本轴最严重项 = 无。
- **Architecture：`executed`** —— 发现 0 项；单一共享规则（三处唯一实现）成立、渲染层无第二份策略映射、依赖方向正确无环、逃生口边界清晰。本轴最严重项 = 无。
- **Verification：`executed`** —— 发现 1 项证据记录类（R-01，P3）+ 1 项既有基础设施（R-02，P3，转 TASK-034）；关键路径（主套件/回归/全仓/判别力/门控矩阵）全部独立复现且与作者声称一致。本轴最严重项 = R-01（P3）。

**P0/P1：0 项**。无未解决 P0/P1，关键验证无缺失（唯一 NOT_RUN 为真实端点端到端，属环境限制且与本 Task 无关）。

## 结论与复审

**decision = `approved`**。`reviewed_head=6ddd955` 可交 Codex 集成。

理由：①AC ①～⑥ 全部有可执行证据且经 Reviewer 独立复现（含判别力 5 failed / 119 passed 独立重跑成功）；②D06 §85 前置 `region_type = sfx` 与 §47 显式手动逃生口的语义在渲染层正确落地，且 `sfx`+`skip` 的拦截原意未放宽；③渲染层不再持有第二份策略映射，F-1 的根因（两份漂移副本）已在规划层（TASK-032）与渲染层（本 Task）双侧消除；④范围合规、无反向依赖、无 Schema/依赖/seam 改动。

**剩余风险**：R-01（证据记录口径，集成时更正）、R-02（既有 conftest 收集顺序问题，转 TASK-034）、R-03（逃生口词汇，非阻塞）。`tests/reading_export` 的 QML flaky 归作者已登记范畴，**不记为通过、不归因于本 Task**；Reviewer 本次连续 2 次全仓未复现该 flaky。

**集成后动作**：F-1 由"部分关闭（规划面 closed / 渲染面 open）"改为 **closed**（关闭条件 = 本 Task 完成并集成，见 [TASK-035](../tasks/TASK-035.md)）。

复审追加新 head 与对应 findings disposition，不抹掉本记录。
