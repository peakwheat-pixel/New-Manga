---
task_id: T1.3.1 / TASK-057
reviewer: Qoder（独立非作者 Reviewer，Round 4）
author: ZCode（固定交付）/ Codex（Rebaseline Integrator；R-011 返修 `0b957d1`）
base_commit: 676319bb1b9e9682b13402c322fded43bbaa90c8
reviewed_head: c79cd360ce29bb45fb319e347c5325c95aee5567
decision: approved
---

# Review：T1.3.1 / TASK-057 Rebaseline Integration — Round 4

## 范围与依据

- **固定全范围**：`676319bb1b9e9682b13402c322fded43bbaa90c8..c79cd360ce29bb45fb319e347c5325c95aee5567`（114 个文件）。
- **本轮返修固定范围**：`85832bb..0b957d1`，实际由两个提交构成 —— `5c6ce73`（R3 Review 文档入库，Reviewer 自有产物）与 `0b957d1`（R-011 产品返修）。`0b957d1` 单独看只动 **2 个文件**：`src/infrastructure/sqlite/backup.py`（+7/-1）与 `tests/storage/test_backup_restore.py`（+27/-0），与 R3 结论中「只修改这两个文件」的推荐范围逐字一致。
- **evidence 子范围**：`0b957d1..c79cd36` 的 `src`/`tests` diff 为空（逐字节相同），只新增 `*0b957d1*` 验证日志与 Handoff/Task 台账行。故本轮代码结论对 `0b957d1` 与 `c79cd36` 同时成立。
- 已读取：`AGENTS.md`、`doc/REBASELINE_PLAN.md`（T1.3.1 定义）、`doc/STATUS.md`、`doc/templates/REVIEW.md`、`doc/tasks/TASK-057.md`、integration Handoff、R1/R2/R3 三份 Review、`src/infrastructure/sqlite/backup.py` 全文、`src/application/maintenance/backup.py`、`tests/storage/test_backup_restore.py` 相关段、全范围 `connection.py`/`run_controller.py`/`viewmodel.py` diff，以及 `verification/TASK-057/rebaseline-integration/*0b957d1*` 全部 8 份作者日志。
- **独立性声明**：本会话（Qoder）未编写本范围内任何 `src`/`tests` 变更；仓库全部提交共用同一 git 身份 `peakwheat-pixel`，因此作者归属依据角色记录（Task/Handoff：Owner=ZCode，Integrator=Codex）而非 git author。需披露：`doc/reviews/TASK-057-b0e4cb1.md`（冻结交付的历史 Review）由 Qoder 窗口撰写，属**审查产物而非实现**，本轮未修改该文件（其范围内 2 次修改来自 `74b9b52`/`257ee5b`，均为原作者复审追加，`decision_history` 保留 round 1 结论）。
- 工作树原有 `?? .superpowers/` 未触碰；本轮 Reviewer 新增内容仅 `doc/reviews/T1.3.1-TASK-057-c79cd36.md` 与 `verification/TASK-057/review-c79cd36/**`（自有证据，属 TASK-057 允许路径）。

## Round 3 findings disposition

| ID | Round 4 disposition | 实证 |
|---|---|---|
| R-011 | **fixed** | `backup.py:199-204` 在 restore 前置 hash 读取处把 `OSError` 映射为 `BackupVerificationError("BACKUP_FILE_UNREADABLE", f"{target.name}: {error}")` 并 `from error` 保留因果链；detail 只用 `target.name`，与同族 `SIDECAR_UNREADABLE`/`BACKUP_FILE_MISSING` 命名与泄露面一致。新增持久判别测试 `tests/storage/test_backup_restore.py:483-508`，断言 `code == "BACKUP_FILE_UNREADABLE"` + live `artifact_revisions == 1` + `backup_records` 计数不变（即 pre_restore 行未写）。**判别力已双向实测**：把 `backup.py` 换回 `85832bb` 版本、测试保持 head 版本的沙箱上该测试 **FAIL（裸 `OSError: forced read failure`，EXIT=1）**；head 上 **PASS（EXIT=0）**。Reviewer 另以**真实 OS 失败**（非 Mock）独立复现：在 `verify_backup_file` 成功后、`sha256_file` 之前对备份文件下 Windows ACL read-deny，实测 `PermissionError/errno=13`；pre-fix 树 `PREVALIDATION_TYPE=PermissionError CODE=None`（probe EXIT=1），head 树 `CODE=BACKUP_FILE_UNREADABLE`（probe EXIT=0），且 live revisions=1、ledger 计数不变、backup root 无 pre_restore/tmp 半成品、ACL 复位后文件可再读。 |

旧 findings 无回归：R-001/R-003/R-004/R-005/R-006/R-008/R-009 的定向测试、R-002 的中断清理测试、R-010 的 v0 restore + 主/rollback 失败测试全部在 head 上通过（targeted 30/30；其中 abrupt publish、v0 restore+migration、restore failure rollback、R-011 四条逐项 `-v` 复跑 4 passed / EXIT=0）。R-007 evidence-hygiene waiver 继续适用且**范围未扩大**：全范围 `git diff --check` 的 216 行全部归属与 R3 完全相同的 **12 个**历史 raw log 文件；`src`/`tests`、`5c6ce73..0b957d1`、`85832bb..c79cd36`（含本轮新日志）三个子范围 `diff --check` 均 **EXIT=0**。

## Standards（executed）

R-011 返修符合仓库记录标准：改动落在 TASK-057 允许路径 `src/infrastructure/sqlite/**` + `tests/storage/**`；零 Schema/migration、零依赖、零 QML、零 `AGENTS.md` 改动（禁止路径 grep 命中 0）；`except OSError` 窄捕获 + typed code + 异常链，符合 `src/application/maintenance/backup.py:13` 的 "every failure is typed" 契约面；`src/infrastructure/sqlite/connection.py` 在全范围内的 48/8 行改动经核对**全部是 docstring 文本**（`_current()` 在 base `676319b` 已存在），无行为变更。无新增 skip/xfail：base 与 head 的 `git grep` 命中均为同样的既有 5 处，范围内 diff 无新增 `skip`/`xfail` 行。未见需单列的 Fowler smell（返修 hunk 5 行，无重复、无发散）。

## Spec（executed）

- **AC④（失败返回 typed 原因）**：R3 击穿的唯一缺口已闭，且由持久测试 + Reviewer 真实 OS 失败探针双向固定。就**所有可达的产品配置**而言 AC④ 满足。
- **AC①/②/③**：未受本轮改动影响；AC③「异常时不留半成品」在 R-011 失败路径上被 Reviewer 独立验证（backup root 文件集合前后一致、live 未写）。
- **AC⑤（不回归 + 判别力）**：head collected **982**（`--collect-only` 实测），相对 rebaseline 951 净增 31，与 Handoff 声明一致；passed 相对 base 只增不减。作者 5× 全量 + Reviewer 8× 全量（见下表）全部 EXIT=0，`QT_QPA_PLATFORM` 全程未设置。
- **AC⑥**：仍待本文档之后的 master integration、post-integration regression 与 STATUS/TASK-057 收口，由 Codex 执行；不属本轮返修缺口。
- 未见 scope creep：`0b957d1` 无返修范围外文件；`c79cd36` 仅台账 + 日志。
- **文档/事实不一致（交 Codex）**：`doc/REBASELINE_PLAN.md:86` 的 T1.3.1 Verification 指向 `tests/maintenance/test_sqlite_backup.py`，该文件在全仓不存在（`tests/maintenance/` 只有 `test_cleanup.py`）；实际套件是 `tests/storage/test_backup_restore.py`。见 R-013。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 状态 |
|---|---|---|---|---|---|---|
| R-012 | P2（advisory，**不阻断批准**） | `src/infrastructure/sqlite/backup.py:152-156` | R-011 建议中括号里的「同阶段可能的 `sqlite3.Error`」仍未映射：restore 前置读 ledger 行的 `self._conn.execute(...)` 在任何 typed 边界之外。当 live DB 没有 `backup_records` 表（v0 形态/未迁移的 live DB）时抛裸 `sqlite3.OperationalError`，调用者拿不到 `code`。**可达性极低**：`backup_records` 自 migration v1 即存在，任何已迁移 live DB 都有该表；backup service 至今未接线生产 bootstrap（Handoff「风险与遗留」已记）；且该失败发生在**任何写入之前**，AC③ 安全边界完好，影响仅限诊断面。 | Reviewer probe Scenario B（真实临时工作区，`DROP TABLE backup_records` 后调用 `restore_backup`）：`LEDGER_READ_TYPE=OperationalError CODE=None`；同场景断言 live `artifact_revisions == 1` **PASS**（未写）。pre-fix 与 head 两棵树表现相同，即本轮返修未引入也未修复该路径。 | 在接线 backup service 到 bootstrap 的那个切片里，把 restore 前置的 live 读（含 `backup_records` 缺失/`database is locked`/`disk I/O error`）一并纳入 typed 映射（如 `LEDGER_UNREADABLE`），并补一条判别测试。不需为本项单独返修 T1.3.1。 | deferred（记录以免静默丢失） |
| R-013 | P2（doc 精度，**不阻断批准**；owner=Codex） | `doc/REBASELINE_PLAN.md:86` | T1.3.1 的 Verification 字段引用不存在的 `tests/maintenance/test_sqlite_backup.py`；实际验证套件为 `tests/storage/test_backup_restore.py`。属计划文本与仓库事实不一致（`AGENTS.md` 第 4 条要求留证据交 Codex），非作者实现缺陷，也不影响本轮任何结论。 | `find tests -name 'test_sqlite_backup*'` → 0 命中；`ls tests/maintenance` → `test_cleanup.py`；`grep -n test_sqlite_backup doc/REBASELINE_PLAN.md` → 第 86 行。 | 收口 T1.3.1 时把该行更正为 `tests/storage/test_backup_restore.py`（+ `tests/workbench/test_restore_gate.py`）。 | open（交 Codex 收口时处理） |

R-011 已 **fixed**。除上列两项 P2 外，在记录范围内未发现 P0/P1 问题。

## 验证

统一环境：`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3）；`PYTHONPATH=src`；`PYTHONDONTWRITEBYTECODE=1`；`QT_QPA_PLATFORM` **未设置**；被审 HEAD=`c79cd360ce29bb45fb319e347c5325c95aee5567`。Reviewer probe 与沙箱只使用系统临时目录，未在交付树内产生任何被追踪文件的修改。

**口径差异（同一处说明）**：Reviewer 主口径为规定的 **PowerShell 7.6.6**，PATH 取自注册表 Machine+User（排除 Git-for-Windows 的 `mingw64/bin`），此时 `openssl` 不在 PATH → 与作者一致：**976 passed + 6 skipped = 982 collected**。Reviewer 另有 **Git Bash** 口径 5 次：`shutil.which("openssl")` 命中 `C:\Program Files\Git\mingw64\bin\openssl.EXE`，6 条既有 `openssl unavailable` skipif 因此**真实执行** → **982 passed + 0 skipped = 982 collected**。两口径 collected 总数相同（982），差值 6 全部来自 `tests/network/test_connection_tester.py` / `test_transport_tls.py` 的既有 skipif，Git Bash 口径为**严格更强覆盖**，非回归、非新增 skip。

| 场景 | 命令/步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| targeted | `python -m pytest tests/storage/test_backup_restore.py tests/workbench/test_restore_gate.py -q -p no:cacheprovider -rs` | PowerShell 7.6.6 / `c79cd36` | **PASS：30 passed / EXIT=0** | `review-c79cd36/targeted-c79cd36.log` |
| affected integration | targeted + `tests/core/test_connection_ownership.py` + `tests/workbench/test_connection_eviction.py` + `tests/workbench/test_run_thread_e2e.py` + `tests/storage/test_schema_migration.py` | PowerShell 7.6.6 / `c79cd36` | **PASS：48 passed / EXIT=0** | `review-c79cd36/integration-c79cd36.log` |
| full regression ×3（规定口径） | `python -m pytest tests -q -p no:cacheprovider -rs` | PowerShell 7.6.6 / `c79cd36` | **PASS：3× 976 passed + 6 skipped（982 collected）/ EXIT=0**，1 warning（既有 `imghdr` DeprecationWarning） | `review-c79cd36/full-suite-c79cd36-run{1,2,3}.log` |
| full regression ×5（Git Bash 口径） | 同上 | Git Bash / `c79cd36` | **PASS：5× 982 passed + 0 skipped / EXIT=0** | `review-c79cd36/full-suite-c79cd36-gitbash-run{1..5}.log` |
| collect 总数 | `python -m pytest tests --collect-only -q -p no:cacheprovider` | Git Bash / `c79cd36` | **PASS：982 collected**（951 baseline + 31，与 Handoff 一致） | `review-c79cd36/collect-count-c79cd36.log` |
| R-011 对抗 probe（真实 ACL 拒绝） | `python verification/TASK-057/review-c79cd36/t057_r011_acl_probe.py <tree>` | Git Bash / `c79cd36` | **PASS / EXIT=0**：`GENUINE_OS_ERROR=('PermissionError', 13)`、`CODE=BACKUP_FILE_UNREADABLE`、live=1、ledger 不变、backup root 无半成品 | `review-c79cd36/r011-acl-probe-c79cd36.log` |
| 同一 probe 跑 pre-fix 沙箱（双翻转） | 同上，tree=`/tmp/.../prefix`（`backup.py` 取自 `85832bb`） | Git Bash / PRE-FIX | **FAIL（预期）/ EXIT=1**：`PREVALIDATION_TYPE=PermissionError CODE=None`，2 条断言挂 | `review-c79cd36/r011-acl-probe-prefix-85832bb.log` |
| R-011 新测试判别力（pre-fix） | `pytest tests/storage/test_backup_restore.py::TestTypedFailures::test_hash_read_failure_is_typed_and_leaves_live_untouched -v` | Git Bash / PRE-FIX 沙箱 | **FAIL（预期）/ EXIT=1**：裸 `OSError: forced read failure` | `review-c79cd36/discrim-r011-test-prefix-85832bb.log` |
| R-011 新测试判别力（head） | 同上 | Git Bash / `c79cd36` | **PASS：1 passed / EXIT=0** | `review-c79cd36/discrim-r011-test-c79cd36.log` |
| chokepoint probe（旧 Reviewer 器械） | `python verification/TASK-057/review-29546f7/t057_chokepoint_probe.py <worktree>` | Git Bash / `c79cd36` | **PASS / EXIT=0**：nested latch、状态半写、Worker birth 三面安全 | `review-c79cd36/chokepoint-probe-c79cd36.log` |
| 前轮关键判别测试逐项复跑 | abrupt publish（R-002）+ v0 restore+migration（R-010）+ restore failure rollback（R-010）+ R-011 typed hash failure，`-v` 逐条 | Git Bash；日志头 `HEAD=561739a`（本 Review 的入库提交，其 `src`/`tests` 与被审 `c79cd36` 逐字节相同，`git diff --stat c79cd36..561739a -- src tests` 为空） | **PASS：4 passed / EXIT=0** | `review-c79cd36/prior-round-discrim-c79cd36.log`（该 4 条同时含于 targeted 30） |
| 作者 evidence 审计 | `verification/TASK-057/rebaseline-integration/*0b957d1*`（8 份） | 作者 PowerShell 7.6.6 / `0b957d1` | **PASS**：8/8 均 `HEAD=0b957d1`、`SOURCE_TREE_STATE` 空、`QT_QPA_PLATFORM=<unset>`、同一 venv、`EXIT=0`；targeted 30、integration 48、probe 0、full 5× 976+6，与 Handoff 表格逐项相符 | 同上 8 份日志 |
| skip/xfail 平价 | base/head `git grep -nE 'pytest\.mark\.(skip\|skipif\|xfail)\|pytest\.skip\(\|@unittest\.skip' -- src tests` + 范围 diff grep | `676319b` vs `c79cd36` | **PASS**：两端均 5 处且逐行相同；diff 中无新增 | `review-c79cd36/skip-xfail-c79cd36.log` |
| 允许/禁止路径 | `git diff --name-status 676319b..c79cd36`；禁止路径 grep（`schema.py`/`migrator.py`/`requirements*`/`AGENTS.md`/`src/ui/qml/`/`doc/STATUS.md`） | 全范围 | **PASS**：命中 0；改动仅 `src/application/maintenance`、`src/infrastructure/sqlite`、`src/ui/viewmodels/workbench`、`tests/{core,storage,workbench}`、`doc/{tasks/TASK-057.md,handoffs,reviews}`、`verification/TASK-057/**` | `review-c79cd36/scope-paths-c79cd36.log` |
| 历史 Review 结论未被改写 | 各 review doc 的 `--diff-filter=A` 与 `--diff-filter=M` | 全范围 | **PASS**：三份 T1.3.1 Review（`8318b64`/`d7e9aa9`/`85832bb`）`MOD_AFTER_ADD=0`；`TASK-057-b0e4cb1.md` 的 2 次修改来自其原作者复审提交（`74b9b52`/`257ee5b`，2026-09-20，`decision_history` 保留 round 1） | `review-c79cd36/scope-paths-c79cd36.log` |
| diff hygiene | `git diff --check`：`src tests` / `5c6ce73..0b957d1` / `85832bb..c79cd36` / 全范围 | 全范围 | 前三项 **EXIT=0**；全范围 **EXIT=2**，216 行全部归属既有 12 个历史 raw log（R-007 waiver，范围未扩大；12/12 均在 `review-29546f7`/`review-b0e4cb1` 下） | `review-c79cd36/diffcheck-c79cd36.log` |
| Reviewer 自有证据卫生 | 交付文件 `grep -nP '[ \t]+$'` + 暂存 Reviewer 产物后 `git diff --check --cached` | 提交前 | **PASS（含 1 项声明）**：`backup.py` / `test_backup_restore.py` 各 **0** 处行尾空白、0 处 CRLF；Reviewer 日志的唯一命中在 `discrim-r011-test-prefix-85832bb.log`，来自 pytest 对**故意失败**的 pre-fix 运行的原始渲染（空行缩进填充、`file:500: ` 定位头），按 R-007 同一 raw-capture 口径保留原文未编辑；全范围 raw dump 未重复入库，只留 EXIT + 归属表 + 可复现命令 | `review-c79cd36/diffcheck-c79cd36.log` |
| 断言未被放宽 | `0b957d1` numstat：`backup.py` +7/-1（-1 为被 try 包裹的原 `actual_hash = sha256_file(target)` 行）、测试 +27/-0 | 返修范围 | **PASS**：零删除断言、零弱化 | `review-c79cd36/scope-paths-c79cd36.log` |

**Architecture：approved。** typed 错误边界现在覆盖 restore 前置阶段的全部**文件 I/O** 失败（sidecar 读、DB 读、hash 读），映射点放在信任边界而非散在调用点，异常链与 detail 泄露面与既有同族 code 一致；唯一残留是 live ledger 读的 `sqlite3.Error`（R-012），位于当前不可达的产品配置上，且在任何写入之前，不动摇原子发布/回滚结构。

**Verification：approved。** 规定口径 30 / 48 / 3×(976+6) 与作者数字逐项相符，另有 Git Bash 口径 5×982 passed 的更强覆盖；R-011 的修复与判别力经**真实 OS 级失败**双向翻转验证（head EXIT=0 / pre-fix EXIT=1），非仅 Mock 断言。

## 结论与复审

**Approved。** 固定 head `c79cd360ce29bb45fb319e347c5325c95aee5567` 可交 Codex 合入 master。

理由：R3 唯一开放 P1 R-011 已按推荐范围闭合，并具备持久判别测试 + Reviewer 独立的真实 OS 失败复现；返修零范围蔓延、零禁止路径、零断言弱化、零新增 skip/xfail；全量回归在规定口径与更强口径下均绿且 collected 数与台账一致；历史 Review 结论未被改写。

剩余风险（均非阻断）：
1. **R-012（P2, deferred）**：restore 前置 live ledger 读的 `sqlite3.Error` 仍裸漏，只在「live DB 无 `backup_records`」这一当前不可达配置下触发；应在接线 backup service 到生产 bootstrap 的切片里一并 typed 化，否则该缺口会随着接线变成可达。
2. **R-013（P2, doc）**：`doc/REBASELINE_PLAN.md:86` 引用了不存在的测试路径，收口时更正。
3. 既有产品限制不变：生产 bootstrap 仍未接线 backup service（Handoff 已记），不是 T1.3.1 的集成验收缺口。
4. AC⑥ 尚未闭合：需 Codex 完成 master 合入 + post-integration regression + `doc/STATUS.md` 与 `doc/tasks/TASK-057.md`（`status`/`integration_commit`/`reviewer` 行）收口，方可置 done。回退方式沿用 Handoff：在 master 上 revert 最终 T1.3.1 merge commit，不得单独回退 restore gate 而保留其测试。

复审追加（不抹旧记录）：Round 1 `8318b64` = changes_requested；Round 2 `d7e9aa9` = changes_requested；Round 3 `85832bb` = changes_requested；**Round 4 `c79cd36` = approved**。R-001~R-011 全部 fixed/closed，R-007 按 evidence waiver 处理，R-012/R-013 为 P2 非阻断遗留。
