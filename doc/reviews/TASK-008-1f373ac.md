---
task_id: TASK-008
reviewer: DeepSeek Harness
author: ZCode
base_commit: f129ae96900fb567288f91a78d55c0ff4ecafe6d
reviewed_head: 1f373ac2240fb5fc6304e447b7d0fd873310aef1
decision: approved
---

# Review：TASK-008 Region 编辑、Revision 与人工保护（1f373ac）

Review worktree：`G:/CODEX/New Manga.worktrees/TASK-008-deepseek-review`（分支 `agent/deepseek/TASK-008-review` @ `f7becb4`，工作区干净；`1f373ac..f7becb4` 仅 doc 元数据，已核对 src/tests 与被审 head 完全一致）。
独立验证目录：`%LOCALAPPDATA%\Temp\task008-deepseek-review`（独立 venv 3.12.3 + PySide6/pytest；5 个独立探针脚本；被审代码经 `git archive 1f373ac -- src` 提取后运行，探针不依赖作者测试）。未触碰 ZCode worktree 与其他 Task 环境。
Reviewer 与作者（ZCode）不同角色；审查基于完整 diff、D03/D06/TASK-002/D08 原文对照与独立复现。

## 范围与依据

- 被审范围：`git diff f129ae9..1f373ac`，11 文件，+1317/−8；`git diff --check` 退出码 0；allowed_paths 过滤 0 命中（`src/domain/regions/**`、`src/application/editing/**`、`tests/editing/**`、`doc/tasks/TASK-008.md`）。
- 依据：TASK-008 四条 AC 与九个主责任 AC；[Handoff](../handoffs/TASK-008-1f373ac.md)、[author-verification](../../verification/TASK-008/author-verification.md)@head；TASK-002 §2；D03 §6~11/§15；D06 §87~90；D08 AC-REGION-001~004/AC-OCR-002/AC-TRANS-001~002/AC-REV-003~004。
- 未审到的部分：`f7becb4` 元数据提交内容（仅按元数据用途读取）；SQLite 持久化行为（本切片范围外，见 NOT_RUN）。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-201 | P2 | `service.py` `merge_regions` | 合并保留人工**锁**（最强保护）但不继承 `edited_confirmed`：两个已确认源 region 合并后需重新确认才恢复 edited final（final 回落 machine 拼接）。行为与 Handoff「最强保护继承」表述在"确认状态"维度略有出入 | 探针 ③：merged.text.edited_confirmed=False | 属产品语义空白（D03 未定义合并的确认态合并规则）；在 TASK-011/014 或产品裁决时明确即可，不要求修订本 head | noted |
| R-202 | P2 | `service.py` `split_region` | 拆分 parts 的 reading_order = 源值 + index，可能与页内既有 region 重叠；D03 未定义 reading_order 唯一性约束，当前由用户重排消解 | 代码阅读；`test_reading_order_manual_adjustment_and_context_order` 证明重排可用 | 后续编辑器切片可考虑拆分后归一化；不阻塞 | noted |

无 P0/P1 finding；除上述两条 noted 级观察外，在记录范围内未发现问题。

## 重点核验项结论（派单四项）

| 重点 | 结论 |
|---|---|
| ① `resolve_final` 与 D03 §8.3 等价性 | **等价**。实现：非空且 `edited_confirmed` 的 edited → final=edited；否则非空 machine → machine；否则空（`final_source` 元数据区分 none/machine/edited，值语义与 §8.3 一致且更精确）。未确认（needs_review）与空白 edited 均回落 machine（AC-TRANS-001 提示场景）；探针 ① 四象限独立验证通过。人工保存 → needs_review 不即 final，`confirm_final` 才确认——与 §8.3「人工确认」字面及 author-verification 声明一致 |
| ② §90 守卫比较对象与 Candidate 承接 | **正确**。比较对象为调用方进入 Step 时的 `current_revision_id`（expected）与写入时重读的 current，等价于 StepRunInputRef 记录的目标 revision；translation/region 锁在写入时重查（§89 不做一次性检查）；OCR 路径按 AC-OCR-002 豁免 translation lock 且变化时返回 `retranslate_hint`。Candidate 表缺位以 `GuardedWriteOutcome`（status/conflicts/payload 语义）返回并在 docstring、Task、Handoff 三处登记由 TASK-011 落库——承接清晰，无越权实现 |
| ③ merge/split 文本拼接与保护继承 | **行为与声明一致，裁决可接受**。探针 ③：merge = covering bbox + 多边形拼接 + OCR/machine 空格拼接 + 人工锁取最强（R-201 noted：确认态不继承）；split = 调用方几何、OCR 与保护标志继承、machine/edited 文本不复制（避免分块重复文本，编辑器负责分配——合理裁决）、源软删。两者均以 user origin 开启独立 revision 历史 |
| ④ restore 不改写历史 | **成立**。探针 ④ 以 JSON 序列化对比恢复前后 revision 1~3 快照逐字节一致，仅追加 revision 4（origin=restored、`restored_from_revision_id` 溯源），current 指向恢复副本——满足 AC-REV-004 全部三条且不触碰历史 |

## 验证

| 场景 | 命令或步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| diff --check（必查） | `git diff --check f129ae9 1f373ac --` | Review worktree | PASS，退出码 0 | 无输出 |
| diff --stat 与 allowed_paths | `git diff --stat`；白名单过滤 | @`1f373ac` | PASS（11 文件全在允许路径） | 本报告"范围与依据" |
| 任务命令 1 | `python -m pytest tests/editing` | **Review 独立 venv（3.12.3）**，checkout=`f7becb4`（tests 与 1f373ac 一致，已核对） | PASS，退出码 0，**17 passed** | Review 会话输出 |
| 全量回归 | `PYTHONPATH=src python -m pytest tests` | 同上 | PASS，退出码 0，**79 passed**（core 6 + storage 31 + library 25 + editing 17） | 同上 |
| 对照环境 | `python -m pytest tests/editing` | 项目任务环境 3.12.3 | PASS，退出码 0，17 passed | 同上 |
| 独立探针 ×5 | `review_probes.py`（对 `git archive 1f373ac` 提取的 src 运行） | Review venv | **PASS ×5**：① final 四象限 ② 守卫+锁+提示 ③ merge/split ④ restore 历史不可变 ⑤ 创建不变量+快照往返 | `%LOCALAPPDATA%\Temp\task008-deepseek-review\probes\` |
| 越界检查 | name-only 过滤 domain/ui/bootstrap/ports/infrastructure/core/storage/library | `f129ae9..1f373ac` | PASS（0 命中） | 本报告范围节 |
| WAL 并发 / SQLite RegionRepository / StepResultCandidate / 完整 TextStyle / UI autosave 计时器 / 性能 | 未执行 | — | NOT_RUN / N/A ×6（与作者声明一致；分属 TASK-011/014/021 与 infra 切片） | — |

### 三轴结论

- **Spec**：九个主责任 AC 在切片范围内全部由测试+探针对应成立（AC-LIB 无关项不涉）；AC1 的「按原图坐标保存并重启恢复」以快照往返+模拟重启验证；Dirty flush 语义（AC3）由 `EditingSession` 显式 flush/discard 覆盖。仓库契约消费侧定义与 TASK-007 先例一致，且 Task 文件已在接管时登记该边界与 SQLite 落地的协调需求。
- **Architecture**：依赖方向正确（editing→ports(domain 契约) 单向；domain/application 零 sqlite3/PySide6）；统一 Region 模型未产生第二套 UI 临时对象（AC-REGION-001）；复合职责（Lock/Review/Revision）均在单一模型上；无业务功能越界、无 QML、无新依赖。
- **Verification**：作者证据（17/79 passed、范围过滤）与独立复现完全一致；5 个独立探针在提取的被审源码上全部通过；两条 P2 观察均为语义空白登记而非缺陷。

## 结论与复审

**decision：approved。** `reviewed_head = 1f373ac` 可交 Codex 集成。

移交说明：
1. R-201/R-202 为 noted 级语义观察，随本报告入档；TASK-011（Candidate/守卫消费）与编辑器切片落地时应一并考虑合并确认态与拆分 reading_order 归一化。
2. SQLite Region/Revision 持久化与 schema 扩展仍未授权（TASK-006/007/008 三切片均以消费侧契约运行）——建议 Codex 尽快协调统一 infra 落地切片，避免契约漂移。
3. 本报告事实仅适用于 `1f373ac`；分支后续变化不延用本结论。
