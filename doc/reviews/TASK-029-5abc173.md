---
task_id: TASK-029
reviewer: DeepSeek Harness
author: ZCode
base_commit: 79529bc9805fda6b72b99860f649fca0922c6cf9
reviewed_head: 5abc173497e112db4da1c00beac3c19fbcccf455
decision: approved
---

# Review：TASK-029 复审（5abc173）

本报告是 [首轮 Review](TASK-029-a2348e9.md)（decision=`changes_requested`）的复审，只覆盖 `79529bc..5abc173`。`a2348e9` 已拒，其 Handoff 结论不再有效。未修改被审实现、测试或验证脚本，未触碰 ZCode worktree；本报告是 Reviewer 新增的唯一文件。

**环境**：worktree `TASK-029-deepseek-review`，分支 `agent/deepseek/TASK-029-review`（Reviewer 于首轮创建）。复审开始时分支位于首轮报告提交 `2652973`（基于 `a2348e9`）；`5abc173` 不包含该报告，故 `git merge 5abc173` 产生 merge commit（`74aca54`），工作区现包含被审 head 的全部内容。

## 范围与依据

**固定对象**：`base_commit = 79529bc9805fda6b72b99860f649fca0922c6cf9`、`reviewed_head = 5abc173497e112db4da1c00beac3c19fbcccf455`。提交链：`a2348e9` → `1b5ff57`（Handoff）→ **`5abc173`**（修订）→ `4fcb150`（修订 Handoff，不在被审范围）。

**修订增量** `a2348e9..5abc173` = 10 文件、`+171/−19`：源码 4 个（`editing/service.py`、`infrastructure/sqlite/{regions,library}.py`、`application/library/ports.py`）、测试 3 个（`tests/editing/test_sqlite_regions.py`、`tests/library/{test_import_images,test_page_order}.py`）、文档 3 个。**v1 migration SQL 与 v2 DDL 未被本轮改动**（`schema.py` 不在修订文件列表中）。

## F-01～F-05 逐条 disposition

| ID | 原级别 | disposition | Reviewer 独立核验 |
|---|---|---|---|
| F-01 | **P1** | **fixed** | `RegionEditingService.__init__` 改为：显式 `committer` 优先 → 否则 `hasattr(repository, "commit_region_revision")` 为真时**采用 repository 自身的原子 seam** → 仅对无该能力的内存 fake 回退 `InMemoryRegionCommitter`。**重跑首轮探针**（默认构造于 `SqliteRegionRepository`）：默认 committer = `SqliteRegionRepository`；`create_region` 后 DB `current_revision_id` **非空**；`save_geometry` 后 DB current 非空且 **`geometry_json` 更新为 `{"bbox": [5, 6, 70, 80]}`**、`region_revisions` 行数 = 2、DB current 与实体 current 一致。首轮"DB current 恒为 `None`、几何不更新"的缺陷场景已反转为通过。作者新增的正向测试 `test_default_construction_uses_repository_seam` 与该场景一一对应 |
| F-02 | P2 | **fixed** | ①异常捕获放宽为 `except Exception` → `rollback()` → 返回 `DB_FAILED`（`detail=repr(error)`）；`except Exception` 不吞 `BaseException`（`KeyboardInterrupt`/`SystemExit` 仍向上传播），范围恰当。②删除 `region.updated_at = region.updated_at` no-op，保留 APPLIED 时把新指针写回调用方对象。**独立探针（注入不可序列化 snapshot）**：status = `db_failed`、`detail = TypeError('Object of type object is not JSON serializable')`、**`conn.in_transaction == False`（事务已释放）**、current 保持旧值、`region_revisions` 行数未变、**释放后同一连接再次提交成功（`applied`）** |
| F-03 | P2 | **fixed** | `get_page` 增加 `deleted_at IS NULL`，与 `list_pages` 对齐；`PageRepository` 契约 docstring 明确"软删页不经本契约返回；回收站/恢复视图属后续切片"。**独立探针**：软删前 `get_page` 非空；软删后 `get_page is None` 且 `list_pages(chapter)` 不含该页——get/list 一致 |
| F-04 | P2 | **fixed（勘误）** | 修订轮分项为 core 6 + storage 33 + library 32 + editing **26** = **97**；Reviewer 在项目固定环境复跑全量为 **97 passed**，**与分项相加一致**。首轮所见 96 对应修订前的 editing 25，勘误成立 |
| F-05 | P2 | **fixed** | `tests/library/test_import_images.py` 与 `test_page_order.py` 增加模块级 `pytest.importorskip("PySide6", …)`。**独立核验**：系统 Python `3.14.6`（无 PySide6）下 `tests/library` 由首轮的 **9 failed / 23 passed** 变为 **22 passed, 2 skipped**（两个模块各记 1 个 module-level skip），退出码 0；领域/SQLite 用例完整运行 |

**五项全部 resolved，无 unresolved、无 regressed。**

## 本轮新 findings 与观察

| ID | 级别 | 文件/行 | 问题 | 影响 | 复现 | 建议 | disposition |
|---|---|---|---|---|---|---|---|
| F-06 | P2 | `tests/library/test_page_order.py:1-5` | `pytest.importorskip(...)` 被插在模块 docstring **之前**，原 `"""Page ordering: source_order frozen, sort_order user-owned (AC-PAGE-001)."""` 因此不再是模块 docstring，降级为无副作用的字符串表达式（模块 `__doc__` 变为 `None`） | 仅丢失该模块文档字符串（不影响测试行为与覆盖）；同类文件的说明性文字在 `--collect-only`、IDE 与文档工具中不再被识别 | 阅读该文件前 5 行；`python -c "import ...; print(__doc__)"` 层面的语义变化 | 把 `import pytest` / `importorskip` 移到 docstring **之后**（或把说明改写为注释） | open（非阻塞） |

**次要观察（不作为 finding）**：`existing_source_hashes()` 与 `max_source_order()` **未随 F-03 一同**加入 `deleted_at` 过滤（本轮只改了 `get_page`）。因此软删除页的 `source_hash` 仍参与同 Chapter 去重、其 `source_order` 仍计入最大序号。两种取值都有合理理由（避免重复导入已删文件 / 保持序号连续），但该语义未在任何契约中定义。建议在后续切片于 `ImportPageSink` docstring 中明确，避免读者把三处的软删处理理解为不一致。**本轮不据此判缺陷。**

## 验证

| 场景 | 命令或步骤 | 环境 / commit | 结果 | 证据 |
|---|---|---|---|---|
| 必需检查 1 | `git diff --check 79529bc 5abc173 --` | Git 2.52.0 | PASS，退出码 0 | 无输出 |
| 必需检查 2 | `git diff --stat a2348e9 5abc173` | 同上 | PASS | 10 files changed, 171 insertions(+), 19 deletions(−) |
| pytest 1 | `python -m pytest tests/storage` | Reviewer venv Python 3.12.3 | **PASS，退出码 0，33 passed** | 与作者一致 |
| pytest 2 | `python -m pytest tests/library` | 同上 | **PASS，退出码 0，32 passed** | 与作者一致 |
| pytest 3 | `python -m pytest tests/editing` | 同上 | **PASS，退出码 0，26 passed**（含 F-01 新测试） | 与作者一致 |
| pytest 4 | `PYTHONPATH=src python -m pytest tests` | 同上 | **PASS，退出码 0，97 passed** | 6+33+32+26 = 97，分项一致（F-04） |
| 对照（系统解释器） | `python -m pytest tests/library`；`python -m pytest tests` | 系统 Python 3.14.6 | library **22 passed, 2 skipped**（F-05 生效）；全量 **2 failed, 85 passed, 2 skipped** | 2 项失败为 `tests/core/test_bootstrap.py` 缺 PySide6（TASK-005 的被测对象本身是 Qt 应用，且 `tests/core` 不在本 Task 白名单），与 TASK-029 无关 |
| ① F-01 探针重跑 | 默认构造 `RegionEditingService(SqliteRegionRepository(conn))`，执行 `create_region` + `save_geometry` | Reviewer venv 3.12.3 | **PASS** | committer = `SqliteRegionRepository`；DB current 两次均非空；geometry 更新为 `[5,6,70,80]`；revision 行数 2；DB 与实体 current 一致 |
| ② F-02 探针 | 注入 `snapshot={"bad": object()}` 的 `RegionRevision` | 同上 | **PASS** | status `db_failed`；`detail = TypeError(…not JSON serializable)`；`in_transaction=False`；current 与 revision 行数不变；释放后再次提交 `applied` |
| ③ F-03 探针 | 软删前后分别 `get_page` / `list_pages` | 同上 | **PASS** | 软删前 get 非空；软删后 get = `None` 且 list 不含该页 |
| v1/v2 冻结未回归 | 修订文件列表与 `tests/storage` 的 `test_v1_schema_is_unchanged` | `5abc173` | PASS | `schema.py` 不在 `a2348e9..5abc173` 改动集；storage 33 passed |
| 范围 | `git diff --name-status a2348e9 5abc173` | 同上 | PASS | 仅白名单内 4 源码 + 3 测试 + 3 文档；`src/domain`、`src/bootstrap`、`src/ui`、依赖清单、其他 Task、TASK-028 设计零改动 |
| 多连接并发竞争 | 未执行 | — | **NOT_RUN** | 单连接语义已由测试与探针证明；真实并发属 TASK-011/集成验证 |
| 生产 bootstrap 装配 | 未执行 | — | **NOT_RUN（范围外）** | `src/bootstrap` 不在白名单 |
| 迁移损坏库端到端恢复 | 未执行 | — | **NOT_RUN** | 属 TASK-021 |
| `managed_original_ref` 收敛 / R-201 / R-202 | 未执行 | — | **N/A** | TASK-028 §3.1/§7 明确保留 |
| 性能 / 大数据量 | 未执行 | — | **N/A** | 无本 Task AC |

## 三轴结论

**Spec**：TASK-029 的 AC1～AC7 在修订后全部成立，且首轮五项重点结论未回退（v1 SQL 逐字符冻结、v2 与 §3 逐条相符、`commit_region_revision` 五步与 §4.2 对应、staged-copy 冲突语义、Page 占位拒绝、重复检查分工）。F-01 的修复让"在 editing seam 中实现 `commit_region_revision`"这一 AC5 要求在**默认构造路径**上也成立——这正是首轮判 P1 的依据，现已闭环。AC8 待 Codex 集成后勾选。

**Architecture**：F-01 的修法（repository 具备原子 seam 时自我承担 committer）比"强制显式注入"更贴合现有结构：SQLite adapter 本身就是该事务的唯一实现者，让 service 复用它消除了"两套提交语义并存"的分歧；内存 fake 无该能力而继续走 `InMemoryRegionCommitter`，两条路径的语义由 `hasattr` 显式区分。F-02 的异常边界放宽后，seam 在任何失败下都保证事务释放（探针已证"释放后可继续提交"），使"要么整体提交、要么整体回滚"成为可依赖的不变量。F-03 把软删语义对齐并写进 Port docstring，方向正确；`existing_source_hashes`/`max_source_order` 的软删取值仍待文档化（次要观察）。

**Verification**：四条命令在项目固定环境（Python 3.12.3）**全部通过**，全量 **97** 与分项相加一致（F-04 勘误成立）；三项复审重点均由 Reviewer **独立运行时探针**复现，未依赖作者自述——F-01 是首轮缺陷场景的反转验证，F-02 验证了事务释放与后续可用性，F-03 验证 get/list 对称。F-05 在无 Qt 解释器上的表现由 FAILED 转为 SKIP，方向正确。未运行项（并发、bootstrap 装配、损坏库恢复、性能）一律 **NOT_RUN/N/A**，未以推测替代。

## 结论与复审

**`5abc173` 可交 Codex 集成：decision = approved。**

- 首轮 F-01（P1）与 F-02～F-05（P2）**全部 fixed**，经独立探针与测试复核无回归。
- 本轮新增 F-06（P2，`test_page_order.py` 的 docstring 降级）与一条次要观察（去重/序号未随软删过滤），**均不阻塞集成**，可在集成前后的文本/小改提交中关闭，不需新的 reviewed head。
- 本批准覆盖 `79529bc..5abc173` 的全部实现与测试内容；`base_commit` 未变，v1 migration 与 v2 DDL 未被修订轮触碰。

**集成时须由 Codex 完成**：核对 `5abc173` 为当前 head、按协议 §6.6 串行集成、记录 `integration_commit`，把本轮 decision 与 F-06 disposition 回填 `doc/STATUS.md`、`doc/tasks/TASK-029.md` 与两份 Handoff，并勾选 AC1～AC8（AC8 需集成验证完成后）；集成后执行该切片的集成检查。

**剩余风险**：多连接并发（WAL/busy_timeout）与生产装配仍未验证，属 TASK-011 与入口切片；`managed_original_ref` 与 D03 `managed_original_artifact_id` 的差异、R-201/R-202 仍按 TASK-028 §3.1/§7 保留；`existing_source_hashes`/`max_source_order` 对软删页的取值语义待文档化。四个一级页面、Provider、Pipeline、打包与发布均未实现。本报告事实仅适用于 `5abc173`；分支后续变化（含 `4fcb150`）不延用本批准。
