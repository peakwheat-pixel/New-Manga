---
task_id: TASK-032
reviewer: Codex
author: DeepSeek Harness
base_commit: ca17d454edc03bc1b057e9e3fc463c81da652e53
reviewed_head: 771977c
metadata_head: ff887db
decision: approved
---

# Review：TASK-032 修复 SFX Policy Gate 缺失 region_type 前置（F-1）

Reviewer=Codex（**非作者**），作者=DeepSeek Harness。固定对象：`base_commit=ca17d45`、`reviewed_head=771977c`（切片交付）、元数据/分支 head `ff887db`。切片 diff 基准为其直接父提交 `c004d4e`（Codex 的释放/改派纯文档提交）。本 Review 在分支工作区只读执行（未修改任何文件、未 commit/push/merge）。

## 轴执行声明（协作协议 §6 第 6/7 条）

| 轴 | 状态 | 说明 |
|---|---|---|
| **Standards** | **executed** | 依据本仓库记录的标准（`AGENTS.md`、§6、D02 §架构方向/§16、D03 §7、D06 §85、D08 AC-SFX-001、本 Task 允许/禁止范围）+ Fowler smell baseline（判断项） |
| **Spec** | **executed** | 来源顺序：TASK-032 AC ①②③④⑤ → D06 §85 / D08 AC-SFX-001 / D03 §7 → 用户释放指令的验收要点 → `doc/STATUS.md` 的 F-1 登记 |
| **Architecture** | **executed** | 依赖方向、共享契约、白名单与 seam 边界 |
| **Verification** | **executed** | 逐条独立复跑作者声明 + 判别力复核 + 消费方排查 |

**并行偏差（如实声明）**：§6 第 6 条要求 Standards 与 Spec 优先由**独立执行者/线程并行**执行。本轮仍**未能并行**——已派发的两条 TASK-029 双轴线程至今仍排在执行槽之后（`doc/STATUS.md` 已登记该状态：无 worktree、无输出）。因此本 Review 采用该条允许的兜底：**两遍相互隔离的检查**（先只做 Standards 与 Architecture/Verification 的取证，再单独做 Spec 的来源逐条核对，两遍之间不引用对方结论），并在此显式声明该偏差。未使用一遍通读冒充两轴。

## 范围与依据

- 作者改动（`c004d4e..ff887db`）= **11 个路径，全部落在 TASK-032 允许范围内**：`src/application/tasks/service.py`、`src/infrastructure/sqlite/pipeline.py`、`tests/pipeline/test_pipeline.py`、`tests/pipeline/test_pipeline_production_seam.py`、`doc/tasks/TASK-032.md`、`doc/handoffs/TASK-032-771977c.md`、`verification/TASK-032/**`（7 个取证文件）。禁止路径命中 **0**（Schema/migration、依赖清单、seam 本体、`AGENTS.md`、其他 Task 均未触碰；`src/domain/**` 亦未触碰）。
- 修复要点（本 Review 逐行读过 `git diff c004d4e 771977c -- src/`）：`_decide_step` 的 SFX 判定改为 `region is not None and step_type in _SFX_GATED_STEPS and _sfx_gate_suppresses(region)`；`_sfx_gate_suppresses` **先判 `region_type == "sfx"`**，再把策略委托给 Translate Step 同一函数 `application.translation.context.gate.decide_sfx_translation`；`_effective_sfx_policy` 对缺失/空白回落 `SfxPolicy.SKIP`；`sqlite/pipeline.py::_region()` 的快照默认由 `"translate"` 改为 `SfxPolicy.SKIP.value`。

## Standards

**依据的 standards sources**：`AGENTS.md`（白名单/边界纪律）、`doc/09_COLLABORATION.md` §3/§4/§6、`doc/02_TECHNICAL_ARCHITECTURE_.md` §架构方向与 §16、`doc/03_DATA_MODEL.md` §7、`doc/06_TRANSLATION_PIPELINE.md` §85、`doc/08_ACCEPTANCE_CRITERIA.md` AC-SFX-001、`doc/tasks/TASK-032.md` 允许/禁止范围。仓库无 `CODING_STANDARDS.md`/`CONTRIBUTING.md`、无 formatter/linter，故 baseline smell 适用（判断项；仓库标准优先；工具已强制项跳过）。

**硬性项（记录在案标准）**：未发现违反。白名单合规、依赖方向未被破坏（修复把规则收敛到 application 层共享模块，planner 未新增任何 infrastructure 依赖）、`sfx_policy` 默认值语义与 Schema 均未改动（D03 §7 保持 `skip`）。

**Baseline smell（判断项）**：

| label | hunk / 位置 | 判断 |
|---|---|---|
| **possible Duplicated Code（消除，非新增）** | 旧 `_decide_step` 内联的 `{"skip","manual"}` 分支被删除，改为委托 `decide_sfx_translation` | **正向**：F-1 的根因正是"同一规则两份实现互相漂移"，本次把它收敛为单一来源，属结构性改善 |
| **possible Divergent Change / Speculative Generality** | `_effective_sfx_policy`、`_sfx_gate_suppresses`、`_SFX_GATED_STEPS`、`_SFX_REGION_TYPE` | 未命中：均为 AC ③ 与"类型前置"所必需，无投机抽象；`_sfx_gate_suppresses` 虽短但承担"前置 + 默认值 + 委托"三件事，命名诚实 |
| **possible Primitive Obsession**（既有） | `RegionSnapshot.region_type` / `sfx_policy` 为 `str`，planner 用 `.value` 常量比较，`decide_sfx_translation` 内部再 `RegionType(...)` 校验 | 既有设计（快照层用原语），本次未恶化；仅记录，不建议在本 Task 改 |
| **possible Fixture Drift（测试夹具）** | `tests/pipeline/test_pipeline.py:51` 的 `region()` 辅助默认仍是 `sfx_policy="translate"` | **判断项**：生产默认是 `skip`（实体/Schema/快照三者现已一致），夹具默认仍是旧值 —— 这正是 F-1 长期未被现有套件照到的原因。作者已用新增的"真实默认"用例（见 Spec ④）补偿，故不阻塞；建议后续把夹具默认对齐为真实默认，见 Findings R-02 |

**Standards 轴小结**：发现 2 条（P2×1、P3×1），本轴最严重项 = **R-02（测试夹具默认与生产默认漂移，P2）**。

## Spec

**来源与逐条核对**（每条引来源原文；新增用例名与断言经我实际复跑）：

| AC | 来源要求 | 我的核对 |
|---|---|---|
| ① 仅 `region_type == sfx` 才应用策略 | TASK-032 AC ①；D06 §85 前置 `region_type = sfx`；D08 AC-SFX-001 | **PASS**：`_sfx_gate_suppresses` 首行即类型前置；`decide_sfx_translation` 内部对非 SFX 直接返回 `translate`（**双重保护**，即使调用点将来漏判也不会误抑制） |
| ② 非 SFX 任何策略取值都不影响规划 | AC ②；用户指令验收要点② | **PASS**：`test_sfx_policy_never_gates_a_non_sfx_region` 参数化 5 类型 × 3 策略；我另核对**执行点唯一**——所有命令族（`translate_*`/`reocr_*`/`reinpaint_*`/`rerender_*`，page/chapter/region 三种 scope）都经同一 `_decide_step`，不存在按命令分叉的漏判路径（既有 `test_every_catalog_command_has_a_page_or_region_plan` 在 60 项里全绿佐证） |
| ③ 缺字段口径唯一 | AC ③；D03 §7 默认 `skip` | **PASS**：实体 `SfxPolicy.SKIP`、Schema `DEFAULT 'skip'`、快照 `_region()` 现为 `SfxPolicy.SKIP.value`（`verification/TASK-032/schema-defaults.txt` 三方实测与 Handoff 一致）；我另 grep 全部 `sfx_policy` 消费方（`domain/regions/entities.py`、`infrastructure/sqlite/{regions,pipeline}.py`、`application/editing/service.py`），**未发现任何依赖旧 `"translate"` 默认的消费方** |
| ④ 真实默认值回归用例 | AC ④ | **PASS**：`test_pipeline_production_seam.py` 用真实 SQLite 插入 region **不写** `sfx_policy`（取 Schema 默认）+ 快照反序列化默认用例；判别力复核证实其有效性（见 Verification） |
| ⑤ 测试与分列 | AC ⑤；用户指令验收要点⑤ | **PASS**：60 / 159 / 671+6 三档与作者记录一致，skip 全为既有 `openssl unavailable`；未新增 skip、未放宽断言 |

**来源未要求却做了的（scope creep）**：未发现。11 个路径全部服务于本 AC；`_effective_sfx_policy` 的"空白回落"是 AC ③ 的自然延伸（属于"缺字段口径唯一"）。

**看似实现但有误的**：**F-1 的用户可见症状未完全消除**——见 Findings **R-04**（渲染层同类缺陷，`src/application/rendering/service.py:219/238/347-351`）。本 Task 的 AC 只覆盖规划阶段，故不算 AC 偏差；但**F-1 因此不能被记为已关闭**。

**Spec 轴小结**：发现 2 条（P1×1 既有遗留、P2×1 越界登记），本轴最严重项 = **R-04（渲染层仍拦截普通 Region，P1，既有、非本变更集引入）**。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-01 | P3 | `doc/handoffs/TASK-032-771977c.md` 验证表第 6 行 vs `verification/TASK-032/discriminative-prefix.log` | Handoff 声称 `git diff --check ca17d45..HEAD` **退出码 0**，实测为 **2**：`discriminative-prefix.log` 是 pytest 原始输出捕获，含 28 处行尾空白（`E         ` 等断言 diff 行）。影响：仅取证记录准确性，无功能影响 | 我实跑 `git diff --check ca17d45 ff887db` → **exit 2**；`git diff --check c004d4e ff887db` 同样 exit 2 且全部命中该 log | 二选一：①在该验证行注明"日志为原始捕获、含行尾空白，`--check` 非 0 属预期"；②规范化该日志（去行尾空白不损失信息） | **fixed at integration（按①）**：更正 Handoff 验证表第 6 行的退出码声明，并在集成验证记录留痕；不改动作者的原始捕获日志 |
| R-02 | P2（判断项） | `tests/pipeline/test_pipeline.py:51` | 测试夹具 `region()` 的 `sfx_policy` 默认仍是 `"translate"`，与生产默认 `skip` 漂移——这正是 F-1 长期未被现有套件照到的机制性原因。作者已用新增"真实默认"用例补偿，故不阻塞 | 代码走读 + 我另在 `c004d4e` 导出树核对旧默认 | 后续把夹具默认对齐生产默认（`tests/pipeline/**` 在允许路径内），或至少在夹具处注明它与生产默认的差异；建议随 TASK-035 一并处理 | **deferred**（登记，非阻塞） |
| R-03 | P3 | `src/application/tasks/service.py`（`decide_sfx_translation` → `SfxPolicy(...)`） | N-3：取值域外（非 skip/manual/translate）的 SFX 策略现在于**规划阶段**抛 `ValueError`（旧行为是执行阶段失败）。Schema `CHECK (sfx_policy IN ('skip','translate','manual'))` 已限定取值域，故这是 fail-closed 的收紧 | `test_sfx_region_with_an_out_of_contract_policy_fails_loudly`；`gate.py:36` | **裁决：接受**——不做产品决策。fail-closed 且与 Translate Step 同源一致；若将来支持新取值，应同时改 Schema 与该枚举 | **accepted（无需动作）** |
| R-04 | **P1（既有，非本变更集）** | `src/application/rendering/service.py:219,238`、`:347-351` | **F-1 的同类缺陷在渲染层仍在**：`sfx = SfxPolicy(stored.sfx_policy)` 后直接按 `SKIP`/`MANUAL` 拦截，**无 `region_type` 前置**。普通（`speech`）Region 取真实默认 `skip` 时，`rerender`/render 仍返回 `SKIP_POLICY`/`skip_policy` → **规划已修，执行仍被拦**。该行为由既有测试以真实默认形态固定（`tests/rendering/test_rerender.py:246-265`：`make_region(..., sfx="skip")` 未设 `region_type`，断言 `status=="skipped"`、`skip_reason=="skip_policy"`） | 我实测：`tests/rendering` 在**修前树**（`c004d4e`）与**交付树**均为 **56 passed** → 该形为**先于本变更集存在**、且未被本 Task 改变；代码走读确认两处 gate 均无类型前置 | 裁决：**另立 Task（见下）**，不并入 TASK-033（TASK-033 的允许路径明确排除 `application/rendering/**`，强行并入会造成范围扩张）；修复时需同时修正那条把缺陷固化的渲染测试（把 fixture 的 region 建为 `region_type="sfx"`） | **登记为 TASK-035（proposed，未释放）** |
| R-05 | P2 | `src/domain/tasks/models.py:182` | N-2：`RegionSnapshot.sfx_policy` 字段默认仍为 `"translate"`（生产路径已通过显式传值/反序列化默认消除影响，但直接构造仍得旧值）。`src/domain/**` 不在本 Task 允许路径 | `models.py:182` 实读；grep 消费方未见依赖 | 与 R-04 同批收敛（同属"SFX 策略口径残留"） | **登记为 TASK-035（proposed，未释放）** |

## 验证

环境：Windows `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；`-p no:cacheprovider`。

| # | 场景 | 命令 / 步骤 | 结果 |
|---|---|---|---|
| V1 | 主套件 | `pytest tests/pipeline -q -rs`（分支工作区） | **60 passed / 0 skipped** ✓ 与作者一致（改动前 30 例） |
| V2 | 强制回归 | `pytest tests/core tests/storage tests/providers -q -rs` | **159 passed / 0 skipped** ✓ |
| V3 | 全仓 | `pytest -q -rs` | **671 passed / 6 skipped** ✓（6 项均为既有 `tests/network` 的 `openssl unavailable`） |
| V4 | **判别力（独立复现）** | `git archive c004d4e` 导出树 + 新测试文件 → `pytest tests/pipeline` | **17 failed / 43 passed** ✓ 与作者一致 → 新用例对修前代码有判别力，非空断言 |
| V5 | N-1 时序（独立） | `pytest tests/rendering` 在 `c004d4e` 导出树与交付树 | **56 passed / 56 passed** → 渲染层形为**先于本变更集存在**，未被本 Task 改变 |
| V6 | 缺字段口径三方 | 实体 / Schema（插入不写字段）/ 快照反序列化 | 三方均 `skip` ✓（作者 `schema-defaults.txt` 与我读代码一致） |
| V7 | 消费方排查（独立） | `rg -n "sfx_policy" src --glob '*.py'` + 逐个读 | 无消费方依赖旧 `"translate"` 默认 ✓ |
| V8 | 边界与空白 | 我实跑 `git diff --name-only c004d4e ff887db`（11 路径全在允许范围、禁止路径 0）与 `git diff --check`（**exit 2**，见 R-01） | 范围 ✓ / 空白 ✗（已登记 R-01） |

**Architecture**：**executed，未发现问题**。依赖方向保持 `QML/UI → Application → Domain/Ports → Infrastructure Adapters`：修复让 planner 复用 application 层共享 gate（`application/translation/context/gate.py`），未引入反向依赖，也未改 pipeline seam 本体；`sqlite/pipeline.py` 的默认值对齐属基础设施对 domain 枚举的正常引用。白名单边界未被扩张。

**Verification 轴小结**：V1–V7 全部独立复现且与作者记录一致（含判别力 17/43 与 N-1 的"先于本变更集"证据）；V8 暴露 1 条记录准确性问题（R-01）。

## 结论与复审

**decision = `approved`**：固定 `reviewed_head=771977c`（元数据 `ff887db`）可交 Codex 集成。

- **本变更集自身无 P0/P1**：修复精准（类型前置 + 共享委托 + 默认口径），判别力经独立复现，范围与依赖方向合规，未触碰 Schema/依赖/seam/`AGENTS.md`/其他 Task。
- **R-04（P1）是既有缺陷的遗留面，不阻塞本 Task 集成**，但**F-1 不得记为"已关闭"**：规划阶段已修（本 Task AC 达成），**渲染阶段仍会拦截普通 Region**。集成时我把 F-1 状态收口为"**部分关闭：规划面 closed；渲染面 open（R-04）**"，并登记 TASK-035（proposed，未释放）承接 R-04 + R-05 + R-02。
- 四轴分别报告、未跨轴排名；每轴小结见各节末尾。剩余风险：R-01（取证记录，集成时修正）、R-02（夹具漂移，deferred）、R-03（已接受）、R-04/R-05（登记为 TASK-035）。

本结论仅对该固定 head 有效；分支或 master 后续变化不延用本批准。
