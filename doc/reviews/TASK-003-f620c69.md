---
task_id: TASK-003
reviewer: Codex
author: DeepSeek Harness
base_commit: 9472df5c44a1bad24cda639201b0824a9ea5eecc
reviewed_head: f620c693b9b6d3df45c0b7c6965ab3db32943702
decision: changes_requested
---

# Review：TASK-003 复审（f620c69）

审查日期：2026-09-14（Asia/Shanghai）。固定范围为 `9472df5..f620c69`；后置 Handoff 提交 `d02db4d` 仅用于核对交付元数据。

## 范围与依据

- TASK-003 AC、D07、D08、D13、TASK-002 契约、协作协议。
- 前两份 Review 的 R-001～R-014及 `doc/handoffs/TASK-003-f620c69.md`。
- 固定差异 10 个路径、`+978/-33`，均在 TASK-003 `allowed_paths` 内。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-015 | P1 | `verification/TASK-003/verify.ps1:152-164`；`doc/fixtures/MANIFEST.md:31-42` | `available` 许可防线只拒绝包含“待定”的来源；未知或明确未授权来源仍会通过，且未严格限定 Manifest 声明的来源类型。无法满足“防止未授权素材入库”的核心要求。 | 合成 `acquired / 未授权` + 合法格式 Hash + `记录取得途径与版本`，当前逻辑返回接受；`pirated / unknown` 还会绕过 generated/acquired 的方式检查。 | 对来源/许可使用明确 allowlist：仅接受项目自制、CC0、已记录用户授权；其他值一律拒绝。generated 必须给出脚本路径和参数，acquired 必须给出取得途径、版本及授权依据。 | open |
| R-016 | P2 | `verification/TASK-003/verify.ps1:139-169`；`doc/fixtures/MANIFEST.md:11` | Manifest 规定 Fixture ID 唯一且不可重命名，脚本只检查行数，没有拒绝重复 ID。重复一个 ID 并删除另一个 ID仍可能满足 20 行。 | `$fxRows` 未提取并比较唯一 ID 集合。 | 验证 20 个固定 ID 完整且各出现一次。 | open |
| R-017 | P2 | `doc/tasks/TASK-003.md:5,19` | frontmatter 为 `in_review`，正文仍称“当前为 ready”，同一 Task 内状态矛盾。 | 固定 head 直接对照两行。 | 正文改为当前 `in_review`，避免复制过期流程描述。 | open |
| R-018 | P2 | `doc/fixtures/MANIFEST.md:99` | 声称素材由“后续已授权 Task”执行，但当前没有获准执行 Fixture 生成的后续 Task，属于无事实来源的授权断言。 | 对照 STATUS 与已释放 Task。 | 改为“后续获授权 Task”。 | open |

## 验证

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| 作者脚本 | `pwsh -NoProfile -File ./verification/TASK-003/verify.ps1` | Windows / `f620c69` detached | PASS，exit 0 | 12 条 PASS；仅代表作者护栏执行成功 |
| 空白检查 | `git diff --check 9472df5 f620c69 --` | 同上 | PASS，exit 0 | 无输出 |
| R-011 | 检查状态枚举与库存行 | 同上 | PASS | `planned` 已删除，20 项均为 `missing` |
| R-012 | 阅读 available 三条件并执行负向合成输入 | 同上 | FAIL | 未授权/未知来源仍被接受，见 R-015 |
| R-013 | 比较完整 24-ID 固定集合 | 同上 | PASS | 规范与 D13 集合相同且唯一 |
| R-014 | 对照 D08 §68/69/72/73 | 同上 | PASS | 派生文件不再复制条目数量 |
| 185 AC 与 F-08 | D08 diff；三份状态图 diff | 同上 | PASS | D08 不变；仅三条 `Blocked → Cancelled`，0 删除 |
| 产品、模型、性能、SQL、打包 | 未执行 | 同上 | N/A / NOT_RUN / BLOCKED | 仓库无产品实现；素材和阈值未就绪 |

### Standards

路径范围、权威回链、后置 Handoff 方式符合仓库规则。R-015/R-016 表明验证器没有完全执行 Manifest 自己声明的许可与唯一性规则；R-017/R-018 是事实状态不一致。

### Spec

R-011、R-013、R-014 已解决；R-012 仅部分解决并形成 R-015。未发现产品范围扩张，185 AC、F-08、性能协议及 24 个 ACG 的当前内容保持正确。

### Verification

固定 head 的作者脚本与 Git 检查通过，但未捕获 R-015～R-018。独立合成输入证明关键许可防线仍存在误报 PASS。

## 结论与复审

`decision = changes_requested`。P1 R-015 未解决，`f620c69` 不可集成。Owner 应只修复 R-015～R-018，生成新 reviewed head 与 Handoff；TASK-004 不受影响。
