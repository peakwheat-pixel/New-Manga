---
task_id: TASK-056
reviewer: 独立子代理（ZCode 会话内新开子对话，与作者不同对话；窗口条款 §8.2 口径）
author: ZCode
base_commit: 8bf8da3f988cdeffce83ad251a15c22ce862dd1a
reviewed_head: 15f977a
decision: approved_subagent
started_at: 2026-09-19 03:52 +0800
supersedes: doc/reviews/TASK-056-4c8b8a7.md（approved_subagent，R-001 守卫复审）
---

# 复审：TASK-056 修订复审（head `15f977a`）

独立子对话复审（开始时间 2026-09-19 03:52 +0800，reviewer=独立子代理，与作者不同对话；首轮 approved_subagent 的 R-001 守卫复审）

> 编者注（作者落盘）：以下为独立子代理复审报告原文（结构保留）；作者仅添加 frontmatter 与本注。R-005/R-006/R-007 随集成收口处置。

## 一、固定对象核对

- 作者 worktree（全程只读）：`G:/CODEX/New Manga.worktrees/TASK-056-zcode`，分支 `agent/zcode/TASK-056-cache-version-cleanup`，head `15f977a`；确认直接父提交为首轮被审 head `4c8b8a7`，无夹带提交。
- 修订写集合（`git diff --stat 4c8b8a7..15f977a`，+220/−0）：`cleanup.py` +10、`test_cleanup.py` +20、handoff +9、取证 6 份。全部在允许范围内；STATUS.md、Schema、依赖、`src/ui/**`、其他 Task 零触碰；`webtoon_tiles.py` 零改动。
- 首轮报告已读，R-001 场景与建议逐条对照修订。

## 二、R-001 复核（守卫正确性 + 判别力）

**守卫实现**（cleanup.py:184-195）：反斜杠归一化后先取 `PurePosixPath(normalized).parts` 做组件检查，含 `..`（或 `.`）一律 `False`，之后才判 `CACHE_SAFE_PREFIXES` 前缀——与首轮建议完全一致。关键技术点核实：`PurePosixPath` **不解析** `..` 组件（parts 保留原样），故检查有效；docstring 完整记录 naive startswith 的逃逸场景与 remover 第二层防护的分工。`preview()`、`run()`、`retry_pending_cleanups()` 函数体零改动，语义不变；preview 与 run 共用 `_is_safe`，同受守卫保护。

**合法路径不误拒（探针实测，head 版 `_is_safe` 直接调用，脚本在 TEMP）**：13 形态全 PASS——正斜杠 tile、raw 反斜杠 tile、hex digest tile 均放行；R-001 sneaky 正/反斜杠形态、短 sneaky、root 逃逸正/反斜杠、业务文件、空串均拒绝。首轮 8 用例在新守卫下全绿，交叉证实合法 tile 生产路径无回归。

**判别力验证（TEMP overlay 树实证，非仅代码审读）**：`git archive 15f977a` 导出整树至 TEMP，用 `git show 4c8b8a7:src/application/maintenance/cleanup.py` 覆盖（确认无 `PurePosixPath`），对 `tests/maintenance` 实跑：

- 单跑新用例 → **FAILED**，失败模式正是 R-001 危害本体：sneaky 路径 `status='ok'`（root 内业务文件被实际删除）而非 skipped；
- 全套件 → `1 failed, 8 passed`：恰好只钉住 R-001 缺口，不干扰首轮 8 用例。

## 三、flaky 事件复核

- `revision-full-suite-run2.log`（1 failed / 900 passed / EXIT=1）失败用例确认为 `test_worker_run_and_main_thread_access_coexist`——`tests/workbench/test_run_thread_e2e.py:134`，属 TASK-048 并发面，**非本切片变更面**。
- 作者归因声明核实：TASK-048 Review R-2（P3）确有该用例的时序性/并发强度浮动注记——归因**属实且诚实**。
- 处置复核：`flaky-diagnostic.log` 记录单跑 ×6 全 PASS（0.39–1.32s）+ 复跑全仓 901/0 + run1/3/4/5 均 901/0——与 5 份日志逐一吻合，处置诚实妥当。run2 的 `EXIT=1` 被如实保留未粉饰。

## 四、独立复跑

环境：venv `TASK-012-py312`，PYTHONDONTWRITEBYTECODE=1、`-p no:cacheprovider`、QT_QPA_PLATFORM 未设。

| # | 场景 | 结果 |
|---|---|---|
| 1 | 定向 `pytest tests/maintenance` | **9 passed / 0 skipped，EXIT=0**（= 首轮 8 + 新增 1） |
| 2 | 全仓 | **901 passed / 0 skipped / 1 warning（imghdr deprecation），41.96s，EXIT=0** |
| 3 | 与 revision 日志对照 | **一致**：run1/3/4/5 均 901/0，run2 1 failed/900；作者日志可信 |
| 4 | 架构守卫 `tests/core/test_architecture.py` | **7 passed** |
| 5 | 判别力 overlay 实验 | 旧守卫 + 新套件 = 1 failed / 8 passed |

## 五、AC ①–⑥ 维持判定

修订仅收紧 `_is_safe` + 新增用例 + 文档/取证，无语义放宽：AC ① 由修复**改善**；AC ②/③/④ 函数体与旧用例零触碰、全绿维持 PASS；AC ⑤ 全仓 901 = 892 基线 + 9，复跑 ≥5 次维持 PASS；AC ⑥ 交付链齐备维持 PASS。首轮 R-002/R-003/R-004 代码位置未动，**如实维持 open**。

## 六、新 findings（均 P3 非阻塞）

| ID | 级别 | 位置 | 说明 | 建议 |
|---|---|---|---|---|
| R-005 | P3 | `doc/handoffs/TASK-056-delivery.md` 追加登记 | 「全仓 ×5（881…）」数字无来源，实测为 901（或 run2 的 900/1 failed），疑笔误 | 集成时更正为 901 |
| R-006 | P3 | `doc/tasks/TASK-056.md` 交付与运行记录节 | 未随修订更新（仍写 8/0、900/0、「Review：尚无」），滞后 | 集成收口时随 status/integration_commit 一并更新 |
| R-007 | P3 | Handoff 追加登记措辞 | 「登记 STATUS flaky 跟踪」未写明由谁执行；STATUS.md 在修订中零改动，flaky 跟踪节现无该用例条目；作者不写 STATUS.md 属正确权限边界 | 集成者须执行：把该用例按口径登入 flaky 跟踪节 |

## 七、结论

R-001 守卫按首轮建议准确落地并经 TEMP overlay 判别力实证（旧守卫必失败、失败模式即业务文件被删）；合法路径零误拒；flaky 事件处置诚实（归因可核、单跑+复跑证据齐、日志未粉饰）；AC 全维持、无 P0/P1/P2 新问题。3 项 P3 均为文本/登记精确性，可在集成收口时处理。

固定 head `15f977a` 可交 Codex 按 §6 串行集成；集成时请处理 R-005/R-006 文本更正并执行 R-007 的 flaky 登记。

**decision: `approved_subagent`**
