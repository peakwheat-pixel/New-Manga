---
task_id: TASK-016
reviewer: Codex
author: DeepSeek Harness
base_commit: f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86
reviewed_head: 9ef06d9cdecdbe98d3894ce638294d9a14408574
metadata_head: af958b3d8d6dbc8c0af77343e66ae3b45e3790fb
decision: changes_requested
---

# Review：TASK-016 OCR 与检测路线独立实验（第二次复审）

本报告复审固定 delivery `9ef06d9cdecdbe98d3894ce638294d9a14408574`，并核对元数据 `af958b3d8d6dbc8c0af77343e66ae3b45e3790fb`。`4834447`/`000ed5b` 的首轮修订结论不再沿用。

## 范围与依据

- 工作区：`G:/CODEX/New Manga.worktrees/TASK-016-deepseek`
- 分支：`agent/deepseek/TASK-016-ocr-detection-experiment`
- Owner：DeepSeek Harness；Reviewer：Codex
- 必读：`doc/handoffs/TASK-016-9ef06d9.md`、`verification/TASK-016/experiment-log.md` 第三轮证据、`doc/research/TASK-016.md`、`doc/tasks/TASK-016.md`
- 依据：TASK-016 Acceptance Criteria、`doc/09_COLLABORATION.md` §4～§6、用户指定的第二次复审门槛。
- `git diff f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86...HEAD` 与提交链已固定；变更仅在 TASK-016 白名单，工作区干净，`git diff --check` 通过。
- 未修改生产代码、生产测试、Schema/migration、依赖清单、AGENTS 或其他 Task；未 push、未合并、未释放 TASK-015 或其他冻结 Task。

## Standards

范围、Handoff/证据结构、passed/skipped 分列和 `NOT_RUN`/`BLOCKED` 标记符合仓库协作协议。状态文档中主线 `STATUS.md` 继续将 TASK-015～TASK-027 标为冻结，与本次“不释放冻结 Task”的约束一致，不把该主线状态当作本次交付授权。

唯一未满足的硬性安全标准见 Spec R-005：代码注释声称“强制”离线，但实现使用 `setdefault`，不能覆盖调用环境中的 `0`。这是安全边界，不是 Fowler smell。

## Spec

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-005 | P1 | `experiments/TASK-016/run_experiment.py:202-206` | `enforce_offline()` 使用 `os.environ.setdefault(name, "1")`；当调用环境预先设置 `HF_HUB_OFFLINE=0`、`TRANSFORMERS_OFFLINE=0`、`HF_DATASETS_OFFLINE=0` 时，模型运行结果仍记录 `0,0,0`。若存在包和权重，后续 Provider 仍可能联网获取资源，违背“已有依赖、权重或授权 endpoint 才允许真实调用”及本轮“离线开关为 1”门槛。另 `local_weight_gate()` 只计算路径摘要，未将路径传给 `MangaOcr()`/`PaddleOCR()`；应确保构造器只能读取该本地资源。 | 本轮在固定 worktree 以三个环境变量均为 `0` 运行 `--run-models`：退出码 0、30 条 `BLOCKED`，但输出 `offline=0,0,0`。正常空环境运行虽为 `1,1,1`，不能证明覆盖反例。 | 用赋值强制设置为 `1`；并让 Provider 显式使用已校验的本地权重/缓存路径，或在无法保证时构造器前 `BLOCKED`。补一条预置 `0` 的回归测试。 | open |

### 已核实的修订项

- R-002：失败路径统一经过 `_fallback_gate()`；三份结果的 fallback 均为无配置时的 `BLOCKED` 判定。
- R-003：研究表前置声明和表头均直接标注 `UNVERIFIED`；版本/许可证仍不得作为合规事实。
- R-006/R-007：`probe.json`、`run-models.json`、`model-run-blocked.json` 均为 v2、6 候选、30 记录，SHA 与 Handoff 一致，`blocked_with_metrics=0`。
- R-009：`_blocked()` 清空所有 metrics，detector 前置检查在资源采样前执行。
- R-004：`model_sha256` 为 `NOT_AVAILABLE` 哨兵，未冒用样本 Hash；质量/性能字段保持 `NOT_RUN`/`null`。

## detector-yolo 范围裁决

**保留** `detector-yolo`，但仅保留为 `DOCUMENTATION_ONLY` 候选，不视为可运行 detector、质量证据、生产实现或依赖。理由是现有架构文档仍列出该候选，保留可追踪性；仓库没有实现来源，当前标记和专门 `BLOCKED` 理由能避免把文档名称误升级为产品能力。后续若要真实运行，必须另有明确范围和实现来源。

## 验证

环境：Windows `10.0.26200`、Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`）、`PYTHONDONTWRITEBYTECODE=1`；未设置 offscreen，实验本身不加载 Qt。

| 场景 | 命令/检查 | passed | skipped | 结果 | 证据 |
|---|---|---:|---:|---|---|
| 协议测试 | `PYTHONPATH=experiments/TASK-016 python -m unittest experiments/TASK-016/test_protocol.py -v` | **5** | **0** | PASS，退出码 0 | 本轮实际复跑；无 skip |
| 默认 probe | `run_experiment.py --manifest experiments/TASK-016/samples/manifest.json --output <仓库外临时文件>` | 0 | 0 | PASS，`BLOCKED=30`；默认路径不联网、不下载 | 本轮实际复跑；`blocked_with_metrics=0` |
| `--run-models` | 同上追加 `--run-models`，输出仓库外临时文件 | 0 | 0 | PASS，`BLOCKED=30`、`blocked_with_metrics=0`、离线值 `1,1,1` | 本轮实际复跑 |
| 离线反例 | 预置三个 `HF_*_OFFLINE=0` 后运行 `--run-models` | 0 | 0 | **FAIL 门槛**：`BLOCKED=30` 但离线值为 `0,0,0` | R-005 复现 |
| 固定结果 | 读取三份 tracked JSON 并计算 SHA/字段汇总 | N/A | N/A | PASS：v2、6 候选、30 条、均 `BLOCKED`、`blocked_with_metrics=0`；`probe`=`b3c30a8b...`，两份 model-run=`5986fc15...` | 第三轮证据与本轮实测 |
| 范围与空白 | `git diff --name-status f9edd688...HEAD`、`git diff --check f9edd688...HEAD`、`git status` | N/A | N/A | PASS；白名单越界 0、工作区干净 | 本轮实际检查 |

继续标记为 `BLOCKED`/`NOT_RUN`/`N/A` 的项目：真实 OCR/检测质量、模型坐标/顺序、耗时/RAM/VRAM、真实漫画样本、权威版本/许可及权重条款确认、生产集成验证。未以 Mock 冒充模型质量。

## 结论与复审

本轮 `decision=changes_requested`，P0=0、P1=1。正常环境的五项协议测试、两种 30 条阻塞探测、固定结果 SHA、`blocked_with_metrics=0` 和 YOLO 文档候选边界均已核实；但 R-005 的离线保护反例未通过，不能批准 `approved`，也不填写 `integration_commit` 或标记 `done`。

下一轮仅需在 TASK-016 白名单内修复 R-005、补反例测试并重新提交 delivery/Handoff；之后由 Codex再次复审。TASK-015 及其他冻结 Task 保持冻结。
