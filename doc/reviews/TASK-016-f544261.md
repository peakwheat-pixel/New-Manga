---
task_id: TASK-016
reviewer: Codex
author: DeepSeek Harness
base_commit: f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86
reviewed_head: f5442615911cf60125fe8bde604c5237f482610b
metadata_head: 81532749586f0a30eb02b780b11fa6eb691dd244
decision: approved
---

# Review：TASK-016 OCR 与检测路线独立实验（第三次复审）

本报告审查固定 delivery `f5442615911cf60125fe8bde604c5237f482610b`，并核对元数据提交 `81532749586f0a30eb02b780b11fa6eb691dd244`。`9ef06d9`/`af958b3` 的结论已被本轮复审取代。

## 范围与依据

- 工作区：`G:/CODEX/New Manga.worktrees/TASK-016-deepseek`
- 分支：`agent/deepseek/TASK-016-ocr-detection-experiment`
- Owner：DeepSeek Harness；Reviewer：Codex
- 必读：`doc/handoffs/TASK-016-f544261.md`、`verification/TASK-016/experiment-log.md` 第四轮证据、`doc/research/TASK-016.md`、`doc/tasks/TASK-016.md`
- 依据：TASK-016 Acceptance Criteria、`doc/09_COLLABORATION.md` §4～§7、用户指定的第三次复审门槛。
- 固定 diff：`git diff f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86...HEAD`；变更仅在 TASK-016 白名单，`git diff --check` 通过，worktree 干净。
- 未修改生产代码/测试、Schema/migration、依赖清单、AGENTS 或其他 Task；未 push、未合并、未释放 TASK-015 或其他冻结 Task。

## Standards

独立 Standards 复核未发现 documented standard 的硬性违反，也未发现会阻塞本实验交付的 Fowler smell。Handoff、证据、passed/skipped 分列、固定 base/delivery/metadata head 及白名单范围均可追溯。`STATUS.md` 的冻结记录保持不变，符合本次不得释放冻结 Task 的约束。

## Spec

独立 Spec 复核未发现 P0/P1 缺口。TASK-016 是实验任务，不把模型质量和性能的 `BLOCKED/NOT_RUN` 误报为通过；协议、候选矩阵、失败门控和离线边界均由固定证据支持。

## Findings

在固定 delivery 与元数据范围内，未发现新的 P0/P1/P2 finding。首轮 R-001～R-009 的修订状态如下：

| ID | 结论 | 证据 |
|---|---|---|
| R-001 | fixed；范围已裁决 | detector candidates 进入矩阵；`detector-yolo` 保留但标记为 `DOCUMENTATION_ONLY`，不作为可运行实现或生产依赖 |
| R-002 | fixed | 所有失败路径通过 `_fallback_gate()`；无点名/无配置时记录 `BLOCKED` |
| R-003 | fixed | research 表前置声明和表头直接标注 `UNVERIFIED`；权重许可仍需权威核验 |
| R-004 | fixed | `model_sha256=NOT_AVAILABLE` 仅为不可得哨兵，未使用 sample hash 冒充模型 hash |
| R-005 | fixed | 离线变量无条件覆盖为 `1`；本地权重 gate 在 Provider import/构造前执行 |
| R-006 | fixed | 三份结果均为 v2、6 候选、30 记录并按本轮固定 SHA 生成 |
| R-007 | fixed | 三份结果 SHA 与 Handoff/日志一致 |
| R-008 | fixed | Task 当前状态唯一，历史状态已明确区分，交付链已记录 |
| R-009 | fixed | 阻塞记录的 metrics 全为 `null`，detector 不在阻塞前采样 |

## detector-yolo 范围裁决

**保留** `detector-yolo`，限定为 `DOCUMENTATION_ONLY — no implementation source in this repository`。理由：现有架构文档仍列出该候选，保留可追踪性；仓库没有实现来源，因此不得把它视为可运行 detector、质量证据、生产实现或依赖。当前专门 `BLOCKED` 理由和范围字段足以防止文档候选升级为产品能力。若未来真实运行，必须另建明确范围并提供实现来源。

## 验证

环境：Windows `10.0.26200`、Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`）、`PYTHONDONTWRITEBYTECODE=1`；未设置 offscreen，实验不加载 Qt。

| 场景 | 命令/检查 | passed | skipped | 结果 | 证据 |
|---|---|---:|---:|---|---|
| 协议测试 | `PYTHONPATH=experiments/TASK-016 python -m unittest experiments/TASK-016/test_protocol.py -v` | **5** | **0** | PASS，退出码 0 | 本轮实际复跑；无 skip |
| 默认 probe | `run_experiment.py --manifest experiments/TASK-016/samples/manifest.json --output <仓库外临时文件>` | 0 | 0 | PASS，`BLOCKED=30`；`blocked_with_metrics=0` | 本轮实际复跑 |
| `--run-models` | 同上追加 `--run-models` | 0 | 0 | PASS，`BLOCKED=30`；`blocked_with_metrics=0`；离线开关 `1,1,1` | 本轮实际复跑 |
| 预置离线反例 | 先将 `HF_HUB_OFFLINE`、`TRANSFORMERS_OFFLINE`、`HF_DATASETS_OFFLINE` 均设为 `0`，再运行 `--run-models` | 0 | 0 | PASS，强制覆盖后仍为 `1,1,1`；`BLOCKED=30`；`blocked_with_metrics=0` | 本轮实际复跑 |
| 固定 JSON | 检查 `probe.json`、`run-models.json`、`model-run-blocked.json` | N/A | N/A | PASS：均 v2、6 候选、30 记录、全 `BLOCKED`、质量字段 `NOT_RUN/null`、fallback 为无配置 `BLOCKED` | SHA：probe `b3c30a8bca68d7d8466ffa7246e2b13fcd2a73173a397977e915a67ab2c91e2a`；两份 model-run `5986fc158edb154a37d38e4ef10390d05bf92476084aa309988eb6f4cf4421bd` |
| 范围/空白 | `git diff --name-status f9edd688...HEAD`、`git diff --check f9edd688...HEAD`、`git status` | N/A | N/A | PASS；白名单越界 0、worktree 干净 | 本轮实际检查 |

未执行项继续标记：真实 OCR/检测质量、真实漫画样本质量、模型坐标/顺序误差、耗时/RAM/VRAM、权威版本/许可及权重条款核验为 `BLOCKED`/`NOT_RUN`；生产集成验证为 `N/A`。未以 Mock 冒充模型质量。

## 结论与集成

本轮 `decision=approved`。`f544261` 的独立 Review 已通过，可交 Codex 按 §6.6 串行集成；本报告不填写 `integration_commit`，TASK-016 在集成验证完成前不标记 `done`。本轮用户明确要求只读且不合并，因此不执行集成、不释放任何冻结 Task。
