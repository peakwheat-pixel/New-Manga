---
task_id: TASK-037
reviewer: ZCode 子 agent（窗口授权同体审查：Reviewer 与 Owner 同属 ZCode，系 09 协作 §6 临时条款的例外授权，结论仅 `approved_subagent`/`changes_requested`，不得视为跨 Agent 独立批准；T1 后须 Codex + DeepSeek Harness 外部 post-hoc 复审）
author: ZCode（窗口内 Owner，实现 head `7b96e72`）
base_commit: c3dabc8（Task 元数据 base=e96b3eb 与之仅差两笔纯文档时间戳提交 `2483011`/`c3dabc8`，无代码差异，已核对）
reviewed_head: 7b96e72
decision: approved_subagent
---

# Review：TASK-037

Reviewer 不能是变更作者。本 Review 在 **ZCode 全权窗口（2026-09-17，T1=2026-09-18 08:50）** 内执行：Owner=ZCode、Reviewer=ZCode 子 agent，属窗口临时条款的同体审查例外；decision 只能登记 `approved_subagent` / `changes_requested`。窗口期硬红线（不得放宽/删除断言、不得新增 `skip`/`xfail`、不得把命中记为通过）已逐条核对。

**两轴隔离偏差声明（§6 第 6 条兜底）**：本 Reviewer 为单线程子 agent，环境无法并行派发两条独立子审查线程（与本仓库 TASK-034/036 集成记录中"双轴子代理通道不可用"同现象）。按 §6 第 6 条兜底做了**两遍相互隔离的检查**：第一遍仅以"仓库记录标准 + diff hunk"为取材（Standards 轴），第二遍仅以"Task AC 原文 + 交付"为取材（Spec 轴），两遍独立成节、不合并、不跨轴排名。此为本报告唯一的流程偏差。

## 范围与依据

- diff：`git diff c3dabc8..7b96e72`（代码与证据，11 files / +844/−26；其中 tests+verification 为 10 files / +842/−24，与 Handoff 口径一致）；`git diff 7b96e72..b0e243e`（Handoff/Task/STATUS 文档，3 files）。
- 逐文件清单：`tests/reading_export/reading_export_helpers.py`（+42）、`tests/reading_export/test_helpers.py`（新增 +71）、`tests/reading_export/test_qml_contract.py`（+65/−26 段内改动）、`verification/TASK-037/`（clamp-race-probe.md、pre/post-fix-runs.log、discriminability-pre-fix.log、probes/×3）、`doc/tasks/TASK-037.md`、`doc/handoffs/TASK-037-7b96e72.md`、`doc/STATUS.md`。
- 依据：[TASK-037](../tasks/TASK-037.md)（AC 与允许/禁止范围）、[Handoff](../handoffs/TASK-037-7b96e72.md)、[REVIEW 模板](../templates/REVIEW.md)、[09 协作](../09_COLLABORATION.md) §6 与窗口临时条款、[STATUS](../STATUS.md) flaky 节（作者更新后）、[clamp-race-probe.md](../../verification/TASK-037/clamp-race-probe.md)、`tests/reading_export/` 三文件现状与 `c3dabc8` 基线逐行对比。
- 未审到的部分：修前树 30 次单目录 + 2 次全仓对照为作者执行，Reviewer 未回到修前树复跑（仅核对日志内部一致性与时间戳序列）；Qt/PySide6 内部（`StopAtBounds` extent 回写批次的准确调度时机）以探针实测为据，未独立重做机制实验（探针 2/3 脚本已抽查与文档描述一致）。

## Standards

**状态：executed**（第一遍隔离检查，仅以仓库记录标准 + diff hunk 取材）。

硬性项（记录在案标准，逐条给结论）：

1. **允许修改范围**（TASK-037「允许修改范围」+ 窗口 Review 指令）：diff 11 个文件全部落在 `tests/reading_export/**`、`verification/TASK-037/**`、`doc/tasks/TASK-037.md`、`doc/handoffs/TASK-037-*.md`、`doc/STATUS.md` 内，越界 0。**符合**。
2. **禁止改生产 `src/**`**（TASK-037「禁止范围」）：`git diff c3dabc8..7b96e72 --stat` 无任何 `src/**` 条目；探针脚本仅 `sys.path` 指向 src 只读导入。**符合**。
3. **窗口硬红线：不得放宽/删除断言、不得新增 `skip`/`xfail`**（09 协作 §6 临时条款第 3 条）：对 webtoon 用例做基线 `c3dabc8` 与现状的函数级逐行 diff——差异仅 4 处：夹具换名（`reader_stack`→`reader_stack_webtoon`）、两处 `(safe_property(...) or 0) > 0` → `safe_number(...) > 0`（等价数值条件 + R-01 修复）、新增 `landed == 240.0` 落点断言（**加强**）。旧 `contentHeight > 0` 有界等待、2 s 保存断言（`scroll_offset_y == 240.0`）、恢复段 `contentY == 240.0` 断言、`5.0/5.0/2.0/5/5.0` 等待预算**逐字保留**；全 diff 无新增 `skip`/`xfail`（文件唯一 `importorskip` 为基线既有 `:23`，本切片未触碰）；`webtoon_save_diagnostics` 全部诊断保留。**符合**。
4. **不得删改 flaky 历史登记**（TASK-037「禁止范围」+ AC ⑤）：STATUS flaky 第 1 条 diff 为纯追加 + 状态列改写，TASK-017 登记、TASK-036 定性原文、频率小样本数据、"已推断合并入第 1 条"条目全部原样保留。**符合**。
5. **测试证据分列与入库**（09 协作 §4「passed 与 skipped 必须分列」、§5「证据提交到 verification/」）：pre/post-fix-runs.log 每次运行独立记录 summary + `EXIT=`；Handoff/STATUS 全部写明 `0 skipped`；证据均已入库并被 Handoff 引用。**符合**。
6. **STATUS 修改口径**：本切片对 STATUS 只改 flaky 第 1 条与同节"建议"段第一句。Task 字面允许范围为"仅 AC ⑤ 的 flaky 条目"，"建议"段同步严格说超出 Task 字面；但窗口 Review 指令明确授权"仅 flaky 条目及其同节'建议'段"，且改动内容是对第 1 条修复状态的描述性同步（无需求/契约变更），按窗口指令口径**接受并记录**（见 R-002）。
7. **依赖清单/Schema/migration**：diff 无 `requirements*`/`pyproject.toml`/migration 条目。**符合**。

baseline smell（Fowler 第 3 章，判断项而非违规；仓库未记录处）：

- **possible Duplicated Code**：`test_qml_contract.py` 新增 `reader_stack_webtoon`（:207-219）与既有 `reader_stack` 的装配体（`make_reading_service`/`make_service`/`ReaderViewModel(...)` 三行）重复。判断：3 行 fixture 装配、两者页面参数不同（`page_height=1000` vs 默认 60），提取参数化 fixture 的间接成本大于收益，P3 非阻塞（R-001）。
- `real_png_pages` 增加 `page_height` 参数、`safe_number`/`safe_property` 相邻放置于 helpers——无 smell 命中。docstring 有一处中英文括号混排空格微瑕，不值单独 finding。

**小结**：Standards 轴 0 个硬性违规；2 项 P3 观察（R-001 fixture 装配重复判断项、R-002 STATUS 建议段口径记录）。

## Spec

**状态：executed**（第二遍隔离检查，仅以 Task AC 原文 + 交付取材）。

- **AC ①（flaky 根因）**：AC 原文要求"把滚动准备改为**成立的前置条件**——**例如**先有界等待 `contentHeight` 足够容纳…，**或**设值后**断言 `contentY == 240`** 再等保存"。交付采用第二示例：夹具 `reader_stack_webtoon`（40x1000 高页）使 `contentHeight=1000` 真正容纳 240 + 新增 `landed == 240.0` 落点断言（`test_qml_contract.py:320-324`）。第一个示例（有界等待容纳）被探针 1 证伪——该场景 `contentHeight=60` 恒定、`contentHeight - height ≥ 240` 恒假（clamp-race-probe.md「事实 A」），且修复 v1 实测 100% 失败于该等待后**如实否决**（有实测、有留档，非口头排除）。「例如」措辞为示例性列举，核心要求"成立的前置条件"满足；"不得放宽/新增 skip/不得删除诊断"子句已核（见 Standards 第 3 条）。**满足**。
- **AC ②（R-01）**：`safe_number(obj, name, default=0.0)` 新增（helpers :135-152），两处数值等待条件（:307、:344）全部改用；`safe_property` 原样移入 helpers 并继续服务字符串/诊断场景（恢复断言 :353 仍用 `safe_property`）。「诊断不得掩蔽真实失败」性质保持：`safe_number` 在对象删除/非数值时回落 `default`，有界等待继续等待、最终由带诊断的断言报告失败；新 `test_helpers.py` 6 例无 Qt 锁定该契约，含 R-01 回归形状用例（`test_numeric_wait_condition_stays_numeric_with_deleted_object`）。判别力证据：修前树 ImportError → exit 2（discriminability-pre-fix.log，Reviewer 核对内容一致）。**满足**。
- **AC ③（对照证据）**：修前单目录 ×30（每次 66 passed、EXIT=0，0 命中）+ 全仓 ×2（1 次 743 passed、1 次 96% 处 EXIT=127 无 summary **如实登记为环境异常而非测试失败**）；修后单目录 ×30（72 passed）+ 全仓 ×6（749 passed）全部 EXIT=0、**0 命中**。每侧 ≥10 次满足；逐次记录 EXIT + passed/failed 满足（日志每次运行独立一行 EXIT）；0 命中无用例名可记、修前 0 命中已附频率解释（单目录口径低于历史全仓 ≈15% + 探针 2 的 100% 复现 + v2 中间态全仓 2/2 命中的机制级判别力），未记为通过。**满足**。
- **AC ④（回归与分列）**：`tests/reading_export` 72 passed 全绿（66+6）；mandated 三套件 Reviewer 实测 **208 passed / 0 skipped、EXIT=0**（与 Handoff 声明一致，通过数不减少）；全仓 ≥5 次（作者 ×6 + Reviewer 复跑 1 次，均 749 passed / 0 skipped、EXIT=0）。**满足**。
- **AC ⑤（STATUS 口径）**：第 1 条状态列 → `fixed（TASK-037，integration 待登记）`，证据列追加探针修正定性与对照数据，历史记录全保留，"建议"段同步（口径见 R-002）。**满足**。
- **AC ⑥**：Handoff 已交付；本 Review 即其"窗口内子 agent Review"环节；集成后才能 done——**尚未完成，属预期状态而非缺陷**。
- **来源未要求的行为（scope creep）**：修正 TASK-036 对该 flaky 的机制定性（STATUS 条目与 clamp-race-probe.md「对 TASK-036 定性的修正」）。这超出"修复"字面，但正是 AC ⑤"说明本次修复依据"的组成部分，且修正以三探针实测为据、对旧定性原文保留不改，**不构成越权 scope creep**。
- **看似实现但有误的项**：未发现。`default=0.0` 与 AC 示例 `default=0` 数值等价。

**小结**：Spec 轴 AC ①–⑤ 全部满足（含一处有探针证据的示例偏离），AC ⑥ 按流程待完成；0 项发现，最严重为上文已记录的 R-002 口径观察（记录在 Standards 轴，本轴无独立发现）。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-001 | P3 | `tests/reading_export/test_qml_contract.py:207-219` | `reader_stack_webtoon` 与 `reader_stack` 装配体 3 行重复（possible Duplicated Code，判断项）。无功能影响；后续若再出现第三种页面变体，重复开始累积间接成本 | 两个 fixture 的 diff 对照 | 出现第三种变体时再提取参数化 fixture | open（非阻塞，deferred 可） |
| R-002 | P3 | `doc/STATUS.md`（flaky 节"建议"段） | 同步修改超出 Task 字面允许范围（"仅 AC ⑤ 的 flaky 条目"）；内容为第 1 条修复状态的描述性同步，无需求/契约变更，且窗口 Review 指令明确授权"flaky 条目及其同节'建议'段" | `git diff 7b96e72..b0e243e -- doc/STATUS.md` | 无需动作；后续 Task 若授权"仅条目"而需同步同节文字，建议在 Task 内显式写明同节段落 | open（口径记录，按窗口指令接受） |

在记录范围内未发现 P0/P1/P2 问题。

## 验证

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| 单目录（Reviewer 实跑） | `PYTHONDONTWRITEBYTECODE=1 …TASK-012-py312/Scripts/python.exe -m pytest tests/reading_export -q -p no:cacheprovider -rf`（worktree 内） | `b0e243e`（测试码=`7b96e72`），Git Bash | **72 passed / 0 skipped、EXIT=0** | 本 Review 会话记录 |
| 全仓（Reviewer 实跑） | `python -m pytest -q -p no:cacheprovider -rs -rf` | 同上 | **749 passed / 0 skipped、EXIT=0**（Git Bash 口径 openssl 可用，6 条 TLS 真实执行，与作者口径一致） | 本 Review 会话记录 |
| mandated 三套件（Reviewer 实跑） | `python -m pytest tests/providers tests/core tests/editing -q -p no:cacheprovider -rs` | 同上 | **208 passed / 0 skipped、EXIT=0** | 本 Review 会话记录 |
| 红线 diff 核对 | `git diff c3dabc8..7b96e72 --stat` + 逐文件 hunk 核对 + webtoon 用例函数级基线 diff + skip/xfail 全文 grep | `c3dabc8` vs `7b96e72` | PASS（无放宽/删除断言、无新增 skip/xfail、无 `src/**`、无依赖/Schema 改动、flaky 历史保留） | 本报告 Standards 轴 |
| 证据链一致性 | 抽查 pre/post-fix-runs.log、discriminability-pre-fix.log、probes/×3 与 clamp-race-probe.md 交叉核对 | `b0e243e` 树 | PASS（66+6=72、743+6=749、737+6=743 两口径收集总数一致、10 files/+842/−24、EXIT 逐行、时间戳序列自洽） | 本报告 Spec/Verification 轴 |
| 修前对照复跑 | 作者执行 ×32，Reviewer 未回修前树复跑 | `c3dabc8`（作者） | PASS（作者日志）/ NOT_RUN（Reviewer 侧复跑，风险低：测试码与基线逐字节未变的口径已核对） | pre-fix-runs.log |

**四轴结论**：

- **Standards：executed** —— 0 硬性违规，2×P3（R-001/R-002）。
- **Spec：executed** —— AC ①–⑤ 满足、AC ⑥ 预期待办，本轴 0 发现。
- **Architecture：executed** —— 本切片仅测试侧改动：`src/**` 零改动（无生产影响、无层级/契约破坏）；helpers 为测试目录内纯工具、无新依赖、无反向依赖；探针放 `verification/` 且 `pytest.ini` `testpaths = tests` 确认不进收集面。本轴 0 发现。
- **Verification：executed** —— Reviewer 实跑三组命令全绿（见上表）；证据链数字自洽；修前 30 次对照未由 Reviewer 复跑（作者日志已核，测试码与基线无差异使复跑价值有限）。本轴 0 发现。

## 结论与复审

**decision：`approved_subagent`**（窗口内同体审查结论，不等同 §6 第 1 条跨 Agent 独立批准；T1 后须 Codex + DeepSeek Harness 强制外部 post-hoc 复审，可推翻本结论）。

固定 head `7b96e72` 可交 Codex（窗口内 ZCode）集成。理由：AC ①–⑤ 有实测证据且满足原文；窗口硬红线零违反（断言只增不减、预算原样、无 skip/xfail、生产零改动、历史保留）；Reviewer 独立复跑单目录/全仓/mandated 三组全部 0 skipped、EXIT=0；两处 P3 均非阻塞（fixture 重复为判断项、STATUS 建议段为口径记录）。

剩余风险（继承 Handoff「风险与遗留」，均不阻塞）：修前全仓 1 次 EXIT=127 未定性（修前树复跑 6 次未再现，若再现按 flaky 节口径登记）；修后 0 命中基于 30+6+1（含本 Review 1 次全仓）样本，历史全仓频率 ≈15%，后续全仓批次应继续观测该用例（若再现，`landed` 断言将给出 `contentY` 落点新签名）；`paintedWidth/Height` 读回 0 的探针侧未决项不影响本切片结论。

集成后：TASK-037 AC ⑥ 方可置 `done`，STATUS flaky 第 1 条状态列"integration 待登记"由集成提交回填；TASK-036 R-01 随集成正式关闭。
