---
task_id: TASK-042
reviewer: Codex（非作者外部 Review）
author: ZCode
base_commit: 904fca185c9900c0b2df297529a58ece831dc0a0
reviewed_head: fc6c649
metadata_head: 49b0608
branch_head: 98b38b7（= 合并 master `f1c6ea0` 后的分支 head）
decision: approved
---

# Review：TASK-042 超大 Webtoon 带状/流式解码

Reviewer=Codex（**非作者**；作者=ZCode）。固定 `base_commit=904fca1`、被审 `reviewed_head=fc6c649`（docs head `49b0608`；分支 head `98b38b7` = 合并 master `f1c6ea0`）。**本 Task 为常规协议非作者 Review**（窗口已失效，不再有 `approved_subagent` 例外）。

**执行口径**：四轴 = Standards / Spec / Architecture / Verification，逐轴声明、分别报告、**不跨轴排名**。双轴隔离按已固化环境结论走 §6 第 6 条**兜底**（本环境无并行子代理通道），特此声明偏差。

## 范围与依据

- **作者真实改动 = 4 文件 `+712/−81`**（与 Handoff 一致，`git show --stat fc6c649`）：
  `src/infrastructure/imaging/streaming_png.py`（新增 279 行）、`src/infrastructure/imaging/webtoon_tiles.py`（±142）、`tests/reading_export/test_streaming_png.py`（新增 296）、`tests/reading_export/test_webtoon_tiles.py`（±76）；另有 docs 提交 `49b0608`（Handoff + `verification/TASK-042/full-suite-runs.log`）。
- **一处必须先澄清的"疑似删除"**：`git diff f1c6ea0 fc6c649` 会列出 `D doc/tasks/TASK-043.md` 等若干条 —— 这是**基线产物，不是删除**：`fc6c649` 的父提交是 `ebe5755`（分支起点线），而 master 的 `f1c6ea0` 是经 `98b38b7` **合并**进来的。核实：`f1c6ea0` 是 HEAD 的祖先（`merge-base --is-ancestor` 退出 0），所列文件在 HEAD **全部存在**，且 `git diff --diff-filter=D --name-only 904fca1 HEAD` **为空**。**分支内无任何删除**。
- 未审到：真机 GUI 视觉确认（不适用本 Task——本切片无 QML 改动，`src/ui/**` 零改动）。

## Standards

轴状态：**`executed`**。

- **白名单**：最终树 `git diff --name-status 904fca1 HEAD` 的 `src/`+`tests/` 恰为上述 4 文件，全在 `src/infrastructure/imaging/**`、`tests/reading_export/**` 内；**越界 0**；未改 Schema/migration、依赖清单、seam 本体、`AGENTS.md`、其他 Task ✓。
- **Qt 依赖移除**：`rg 'PySide6|QImage|QRect' src/infrastructure/imaging/webtoon_tiles.py` 仅剩 docstring 里的一处文字提及 → 该模块**已无 Qt 导入**，栅格化半边可在无 PySide6 下单测 ✓（真实改进）。
- **依赖纪律**：`git diff f1c6ea0 fc6c649 -- requirements.txt` **为空** → AC ⑤ 的"未新增依赖（stdlib-only，pyvips 未启用）"**成立** ✓。
- **skip 纪律**：新增声明仅 1 处 —— `tests/reading_export/test_streaming_png.py:18` 的模块级 `pytest.importorskip("PySide6", reason="pixel-identity comparison uses QImage")`。与既有多处先例一致、在**声明 venv** 中不生效（PySide6 已装），且该文件的核心（过滤器/边界）不依赖 Qt。**P3，接受并登记**（与此前 P-01 同类；未来应优先带理由的显式 fixture/skipif）。全仓 skip 总数未变（6 条 `tests/network` `openssl unavailable`）✓。
- **断言未放宽**：见 Spec 轴 AC ②（钩子翻转**更强**）。未新增 `xfail` ✓。
- **口径诚实性**：作者声明其全仓数字为 Git-Bash 继承 PATH 口径（`798 passed / 0 skipped`）；我在**声明口径**（PowerShell + `TASK-012-py312`）下独立测得 **792 passed / 6 skipped** —— 差值恰为 6 条 network TLS skip↔pass，**总数一致**，不构成"skip 被消除"。作者已在 Handoff 明写口径 ✓。

**本轴小结**：0 项硬性违反；1×P3（`importorskip`，已披露、无实际影响）。本轴最严重项 = 该 P3。

## Spec

轴状态：**`executed`**。逐条 AC 核对，**其中 AC ①②③ 的三处关键主张由我独立复现/实测**：

| AC | 判定 | 我的独立验证 |
|---|---|---|
| ① 带状解码 + 内存实测 | **PASS** | **自建探针**（`800×20000`）经生产 `TiledPageRasterizer.ensure_viewport(0,4000)` 成功材料化；`tracemalloc` 峰值 **≈39.7 MB**，而整页 rgb32 = 64 MB、整页**压缩源**仅 113 KB → 峰值 ≪ 整图。**精确性修正见 R-02** |
| ② TASK-020 表征钩子按原设计翻转 | **PASS（更强，非放宽）** | 逐字核对：旧 `pytest.raises(OSError)` 已改为**成功断言**——`len(files)==2`、`QImage` 非空且尺寸 `(1600,4000)`、几何比率 `ratio < 0.05`、并写出 `oversize-record.json` 记录峰值；**TASK-020 的几何断言逐字未动**（`tile_count==50`、带宽和 ==200000、`to_page_coords`），**测试未删除**。旧"预期失败"→新"预期成功 + 尺寸 + 记录" = 严格更强 |
| ③ fail-closed 变体矩阵 | **PASS（枚举内）** | 我独立验证 `REWIND_REQUIRED` 为 typed；作者的 `test_variant_matrix_fails_typed` 覆盖 NOT_PNG/INTERLACED/16-bit/PALETTED/TRUNCATED。**枚举之外发现一处缺口 → R-01** |
| ④ 端到端 + 回退路径不变 | **PASS** | 超大页经 `ensure_viewport` 走 tile 路径；未注入 `tile_factory` 时整图路径由 TASK-020/038 既有测试保持（本次全仓 792/6 通过，含 `test_qml_contract` 的"未注入→空态"用例） |
| ⑤ 依赖纪律 | **PASS** | `requirements.txt` 零改动（已核）；pyvips 未启用，故无"打包影响"义务 |
| ⑥ 判别力 + 回归 | **PASS（判别力部分采信作者留证）** | 新模块在修前不存在 ⇒ 新测试必然收集失败（结构性事实）；钩子翻转的反事实由其会话留证（接入前 `DID NOT RAISE`）。**我未另行在修前树上重跑**（如实标注） |
| ⑦ Review | 本文件 | — |

**我独立复现的两项核心主张（对方未覆盖的交叉验证）**：

1. **过滤器正确性（焦点①）**：我**自己重写**了 PNG 五种行过滤器的**正向**编码（0/1/2/3/4 + 逐行混合 `i % 5`），对 4 组色型/bpp（1/2/3/4 通道）随机内容编码后交给 `StreamingPngReader`，在**每个采样带偏移**（含 `[0,h]`、`[0,1]`、`[h-1,*]`、跨中段）断言输出**逐字节等于原始行** → **全部一致**。
2. **与 Qt 逐像素一致（焦点①）**：用 Qt 生成**随机 RGBA** 的 PNG（`QImage.save`，真实 libpng 逐行自适应过滤），再对多个带偏移用读者解码，与 `QImage` 的 `Format_RGBA8888` 原始像素**逐字节比对** → **全部一致**。

**本轴小结**：AC ①～⑦ 全部达成（AC ⑥ 判别力部分采信作者留证）；**1 项**枚举外缺口（R-01）。本轴最严重项 = R-01（P3）。

## Architecture

轴状态：**`executed`**。

- **分层与归属**：读者落在 `infrastructure/imaging/**`（基础设施适配器），`application/**` 未新增对 Qt/DB 的依赖 ✓；`webtoon_tiles.py` 去掉 Qt 后，栅格化半边可在无 PySide6 环境单测 —— 符合本仓库"适配器可替换"的既有取向。
- **错误边界**：`_decode` 把 typed 的 `StreamingPngError` 转成 `OSError` 上抛，恰好落在 reader VM 的 `except (OSError, ValueError)`（`viewmodel.py:309/351`）→ 失败时优雅回退/清空瓦片 ✓ 设计正确。**但该桥只覆盖 typed 错误**（见 R-01）。
- **游标语义与成本（焦点③）**：我核实 `visible_tiles` 以 **升序** `range(lo, hi+1)` 返回 ⇒ **每次 `ensure_viewport` 调用最多触发一次 rewind**；重扫成本以**目标带末端**为界，而非"每 tile 一次整页重扫"。**实测**（`800×20000`）：前向扫到 20000 行 0.39 s；回跳至 row 12000（重扫 16000 行）0.156 s；回跳至 row 0（重扫 4000 行）0.219 s。⇒ 作者的"整段重扫、正确性优先、常态不受影响"**方向正确但未量化**，且**使其有界的那个性质（升序请求）在代码里是隐式的** → R-03。
- **写入侧**：`_encode_png` 为 stdlib filter-0 写入，CRC 计算正确、色型映射 `{1:0, 2:4, 3:2, 4:6}` 正确 ✓；产物可被 `QImage` 正常读入（其测试断言非空）。
- **未发现**：能力"已实现但生产不可达"（TASK-038 已注入 `tile_factory`，本切片是对该路径的实现替换）✓。

**本轴小结**：0 项缺陷；1 项**成本声明未量化/隐含性质未文档化**（R-03）。本轴最严重项 = R-03（P3）。

## Verification

轴状态：**`executed`**。

| 场景 | 方法 | 结果 |
|---|---|---|
| 独立全仓 | PowerShell + `TASK-012-py312`，`-q -p no:cacheprovider -rs -rf` | **PASS 792 passed / 6 skipped**（exit 0）；6 条仍为既有 `tests/network` `openssl unavailable` |
| `tests/reading_export` | 同上 | **PASS 93 passed**（82 + 11） |
| 过滤器正确性 | 我自写正向编码（5 过滤器 + 逐行混合）+ 4 组 bpp + 随机内容 + 多带偏移 | **PASS 逐字节一致** |
| 与 Qt 一致性 | Qt 编码随机 RGBA PNG → 读者带状解码 vs `QImage` 像素 | **PASS 逐像素一致**（多带偏移） |
| 游标语义 | 回退请求 | **PASS** typed `REWIND_REQUIRED`；`rewind()` 后重扫正确 |
| 成本量化 | 大页前向/回跳计时 | 见 Architecture（0.39 / 0.16 / 0.22 s） |
| 内存构成 | `tracemalloc` 峰值 vs 带宽 vs 整图 | 峰值 ≈39.7 MB（带宽 12.8 MB、整图 64 MB、压缩源 113 KB）→ 见 R-02 |
| 损坏载荷 | 翻转 IDAT 载荷字节 | **FAIL（新发现）** 未 typed 的 `zlib.error` 逃出 → R-01 |
| 边界/删除/依赖 | `--name-status`、`--diff-filter=D`、`requirements.txt` diff | **PASS** 4 文件、无删除、依赖零改动 |
| 未验证 | 修前树判别力重跑（采信作者留证）；其 5 次 full-suite 日志（抽样）；`oversize-record.json` 的实跑数值（我用自己的探针替代） | **未验证（如实标注）** |

**四轴结论（逐轴声明，不跨轴排名）**

- **Standards：`executed`** —— 0 硬性违反；1×P3（`importorskip`）。本轴最严重项 = 该 P3。
- **Spec：`executed`** —— AC ①～⑦ 达成；1 处枚举外缺口（R-01）。本轴最严重项 = R-01（P3）。
- **Architecture：`executed`** —— 分层正确、错误桥设计正确、升序请求使重扫有界；1 项成本声明未量化（R-03）。本轴最严重项 = R-03（P3）。
- **Verification：`executed`** —— 我独立完成了"过滤器逐字节"与"与 Qt 逐像素"两项核心交叉验证、成本与内存量化、边界审计；**4×P3 全部为新增发现**，无一项推翻本切片的实现正确性。本轴最严重项 = R-01（P3）。

**P0/P1：0 项。**

## Findings

| ID | 级别 | 位置 | 触发与影响 | 复现证据 | 建议 | 处置 |
|---|---|---|---|---|---|---|
| **R-01** | **P3** | `streaming_png.py::_pump` | **损坏但结构完整的载荷未被 typed 化**：`decompress()` 抛的 `zlib.error` 未被捕获。其 MRO 为 `(zlib.error, Exception)` —— **既非 `OSError` 也非 `ValueError`**，因此**逃出** reader VM 的 `except (OSError, ValueError)`，逐字节损坏的源文件不再走"优雅回退/清空瓦片"，而是抛到 QML Slot（PySide6 打印回溯后继续）。**失败仍是 fail-closed（不会产出错误像素：Adler-32 会在流末报错）**，故定为 P3 而非 P2 | 我的探针：翻转 IDAT 载荷 6 字节 → `Error -3 while decompressing data: invalid stored block lengths`（untyped） | 把 `decompress` 包进 `try/except zlib.error` → typed `StreamingPngError("INVALID_PNG"/"CORRUPT_DATA")`；补"容器合法但载荷损坏"用例（作者的变体矩阵未覆盖此格） | **open（P3）→ 归入 [TASK-045](../tasks/TASK-045.md)** |
| **R-02** | P3 | `streaming_png.py` / `webtoon_tiles.py` docstring | **内存声明不够精确**：模块称"never more than one scanline pair plus the requested band"、类称"Peak memory is one decode window (tile + upward overlap)…never the whole page"。实测构成 = **整份压缩源**（`__init__` 的 `read_bytes()` 常驻）+ **≈3–4× 带宽**（`read_band` 的 raw 累积 + `_encode_png` 的 `scanlines` 复制 + zlib 输出）。AC ①（"显著低于 1.28GB"+"带宽×常数界"）**仍成立**，但现状不是 1× 带 | 800×20000：峰值 39.7 MB、带宽 12.8 MB、压缩源 113 KB、整图 64 MB（比值见上） | 把声明写成"峰值 = 压缩源 + O(带宽)（实测 ≈3–4× 带）"，并说明"整份压缩源常驻"；将来若要 O(带) 总体需改成流式/分块读源 | **open（P3）→ TASK-045** |
| **R-03** | P3 | `webtoon_tiles.py::ensure_viewport` / `_decode` | **rewind 重扫成本未量化，且"使其有界"的性质是隐式的**：`visible_tiles` 升序 ⇒ 每调用**最多一次** rewind，最坏 = 一次以目标带末端为界的重扫；交替上下滚动（或缓存被清后回跳高行）会重复付该成本 | 我的实测：0.39 s（扫 20000 行）/ 0.156 s（回跳 row 12000）/ 0.219 s（回跳 row 0） | 在 docstring 写明"升序请求 ⇒ 每次调用最多一次 rewind"与该实测；若将来需要，再加"最近 N 带缓存" | **open（P3）→ TASK-045** |
| **R-04** | P3 | `tests/reading_export/test_webtoon_tiles.py` | **超大页夹具来源变更未被声明**：`write_streaming_png` 由测试自己的 stdlib 写入器（filter 0）生成 → 该用例**不再证明"真实编码器产物 + 超大"**能被带状读；小尺寸 Qt 编码的逐像素一致（`test_decodes_qt_encoded_png_pixel_identical`）与 5 过滤器单测**另**覆盖了编码器多样性 | 读测试实现；我自己的 Qt 编码一致性探针仅在小图（96×60） | 增设一个**中尺寸 Qt 编码**夹具（如 `1600×20000` ≈128 MB rgb32，**低于 Qt 的 ~300 MB 失效门**）走带状读 + 像素一致性，用低成本补上"真实编码器 × 超大"这一格 | **open（P3）→ TASK-045** |

## 结论

**decision = `approved`**（0 P0/P1；4×P3 均非阻塞，且不推翻本切片的实现正确性）。`reviewed_head=fc6c649` 可交集成。

理由：①AC ①～⑦ 逐条达成；②本切片最核心的两项主张（**五种过滤器正确性**、**与 Qt 逐像素一致**）由我**独立重写探针交叉验证通过**；③TASK-020 表征钩子的翻转经逐字核对确认为**更强而非放宽**，测试未删除、几何断言未动；④边界干净（4 文件、无删除、依赖零改动）、QC 层已被完整移除出栅格化模块；⑤作者的诚实性良好（口径声明、rewind 成本披露、混合 filter 性能预期、未新增依赖）；⑥R-01～R-04 全部为**新增**P3 发现，已归入 TASK-045 收口。

**集成后**：TASK-020 遗留②（超大 webtoon 像素解码 `BLOCKED`）**关闭**；TASK-042 置 `done`。R-01～R-04 随 [TASK-045](../tasks/TASK-045.md) 关闭（该 Task 白名单已覆盖 `src/infrastructure/imaging/**` 与 `tests/reading_export/**`）。

**剩余风险**：损坏载荷的失败类型（R-01，不产出错误像素）；内存声明精度（R-02）；回跳重扫成本（R-03）；"真实编码器 × 超大"覆盖（R-04）。**未验证项**（修前树判别力重跑、作者 5 次日志抽样）已如实标注。
