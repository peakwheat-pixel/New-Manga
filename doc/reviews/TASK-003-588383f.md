---
task_id: TASK-003
reviewer: Codex
author: DeepSeek Harness
base_commit: 9472df5c44a1bad24cda639201b0824a9ea5eecc
reviewed_head: 588383f62006a4ab89de1a42bbdabe0bf9d906ae
decision: changes_requested
---

# Review：TASK-003

审查日期：2026-09-14（Asia/Shanghai）。本报告只绑定 `9472df5..588383f`；作者后续的 Handoff/状态提交 `c55ec27` 已用于核对交付完整性，但不属于被审内容。未修改任何被审文件，未实现修复，未启动或审查其他 Task。

## 范围与依据

- 任务与规则：[TASK-003](../tasks/TASK-003.md)、[协作协议](../09_COLLABORATION.md)、[Source of Truth](../00_INDEX.md)。
- 产品与验收来源：D07 §97～101、D08 §3/4/43/44/51～76、D13、[TASK-002 最小契约](../contracts/TASK-002_MINIMUM_DATA_EXECUTION_CONTRACT.md)及其 F-08。
- 固定差异：`git diff 9472df5...588383f`，共 8 个路径、`+504/-16`。
- 三轴：Spec、Architecture/Standards、Verification。代码 smell 仅作为启发式；仓库规则优先。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 状态 |
|---|---|---|---|---|---|---|
| R-001 | P1 | `doc/verification-plan/TASK-003_ACCEPTANCE_AND_FIXTURE_SPEC.md` L13/L16/L117-L130/L210-L219；`doc/13_ACCEPTANCE_TRACEABILITY.md` L4 | 规范声明 D13 负责“当前结果”，却又在自身维护 `当前结果` 列和状态摘要，且 L16 将“产品语义、范围”笼统归给 D08，超出索引规定的 D01/D04/D05 产品职责。结果真值已出现 `NOT_RUN` 与 `BLOCKED` 两种口径，后续会继续漂移。 | 对照 `doc/00_INDEX.md` Source of Truth 表和规范自己的权威边界即可复现。 | 从方法规范删除可变结果列/状态摘要；当前结果只留 D13 或绑定 commit 的 Handoff/Review。将 L16 收窄为“验收目标与优先级以 D07/D08，产品范围按索引职责”。 | open |
| R-002 | P1 | 规范 L128-L130；D13 L214；D08 L2877-L2895 | `ACG-SYNC` 被写成非枚举复合值 `PASS（契约层）；实现层 NOT_RUN`，同时 D08 明确正式 Schema 满足契约并通过向量后才可通过该 Gate。当前无 Schema/实现，契约获批只能作为证据，不能使整项 PASS。 | D08 §67 与两处结果值直接对照。 | 将当前结果恢复为单一合法枚举 `NOT_RUN`（若按已知缺实现处理则 `BLOCKED`，需统一口径）；把 TASK-002 契约批准移到证据列。 | open |
| R-003 | P1 | Task L31/L59；规范 L40-L49/L113-L130/L202-L208；D13 L216/L218-L228；D08 §68～§76 | Task 要求所有未编号组和扩展要求有稳定标识/处理路径；交付只给 10 个“零编号主题”分配 `ACG-*`。D08 §68～§76 的自动化、UI、视觉、Benchmark、安全、Release/READY 等未编号规范，以及 D13 的 7 类扩展项仍只有章节或自然语言，没有稳定测试标识。 | `rg '^# (68|69|70|71|72|73|74|75|76)\.' doc/08_ACCEPTANCE_CRITERIA.md` 有 9 项；规范/D13 的 ACG 表只有 10 个 §51～§67 标识。 | 为所有适用未编号规范建立稳定 ID 和执行/路由；纯建议性章节可明确标注非验收项及理由。验证脚本检查完整预期集合，而非固定“恰好 10 个”。 | open |
| R-004 | P1 | 规范 L78-L94，尤其 L80/L84；Task L62；D08 AC-PERF-001 | 性能协议虽固定次数，却未定义计时起止边界、缓存/进程重置和 P95 算法。并把“冷启动”放入 3 次预热后的普通操作，可能实际测成暖启动；20 样本使用不同 percentile estimator 也会产生不同 Gate 结果。 | 比较规范 §6 与 D08 的冷启动 P95 Gate；规范中无 estimator、计时边界或冷启动重置规则。 | 固定单调时钟、每项起止事件、异常样本规则和 P95 estimator（例如 nearest-rank）；冷启动每个测量样本使用满足定义的冷进程/缓存状态，不能套用会改变被测状态的通用预热。 | open |
| R-005 | P1 | 规范 L120；D08 L2509-L2529 | `ACG-SMOKE` 方法只列“安装→导入→翻译→阅读→导出→退出”，遗漏默认书架、创建 Book/Chapter、保存、关闭后再启动及数据仍存在，不能覆盖 D08 的完整发布主流程。 | 逐步对照 D08 §53。 | 测试方法完整引用 D08 §53，或列出全部步骤而不缩写掉持久化检查。 | open |
| R-006 | P1 | 规范 L175；D08 AC-CAP-004 L2229-L2244；Manifest L56 | 容量主题只路由 Dataset B/C/E，遗漏 `AC-CAP-004` 所需 200k Webtoon Dataset D。虽然 Manifest 定义 DS-D，验收方法没有把它接到 AC-CAP。 | 规范 §9 的 AC-CAP 行与 D08 AC-CAP-001～004、Manifest DS-D 对照。 | AC-CAP 路由加入 Dataset D，并由脚本验证四个编号容量 AC 均有数据集路径。 | open |
| R-007 | P1 | Task L32；Manifest L13-L28/L61-L70/L77-L80；verify.ps1 L89-L108 | Task 明确“未取得素材标缺失”，但 0 个 available 的 19 项全部标为 `planned`、missing=0，掩盖当前素材阻塞。许可防线也只检查字段存在：脚本未拒绝 `available + 待定许可`、未要求 available 使用真实 SHA256；“所有素材必须由脚本生成”又与允许用户授权/CC0 取得途径冲突。 | Manifest 当前汇总与 Task AC2 对照；静态阅读验证脚本可见其只约束 planned/missing 的 Hash。 | 按 Task 将当前未取得项标为 missing，或经 Codex/用户修订 AC 后再保留 planned；区分 generated/acquired 规则，并让验证拒绝未确认许可或非 SHA256 的 available 项。 | open |
| R-008 | P2 | `verification/TASK-003/verify.ps1` L71-L85 | ACG 检查先 `Select-Object -Unique` 再计数，且只验证 D13 行唯一；规范表中重复行或散落 prose 的额外 ID 可能被去重后继续 PASS。 | 阅读 `$acgSpec/$acgTrace` 的构造与 `$rows` 仅针对 D13 的循环。 | 分别解析两张权威表的行 ID，验证每个预期 ID 各出现一次，并检查集合与完整未编号清单一致。 | open |
| R-009 | P2 | `verification/TASK-003/verify.ps1` L112-L129 | F-08 护栏只统计新增行，删除 D02/D04/D11 的既有内容仍可能通过“nothing else added”断言。实际 `588383f` 没有该问题，但脚本不能防止后续回归。 | 实际 diff 只有 3 条授权新增边；脚本 `$added` 未检查删除行。 | 同时断言三文件无删除内容行，或将每个文件与基线比较为“恰好新增指定一行”。 | open |
| R-010 | P2 | 规范 L122；D08 L2607-L2617 | `ACG-PRIVACY` 的执行方式增加“首次使用提示”，而 D08 只要求 Provider 设置页说明上传数据类型。这是未经批准的 UI 行为。 | 规范 §8 与 D08 §57 对照。 | 删除“首次使用提示”，只验证 D08 已定义的设置页披露；若要新增首次提示，先走产品范围决定。 | open |

统计：**P0=0，P1=7，P2=3**。P1 未解决，固定 head 不可集成。

## 三轴结论

### Spec

`changes_requested`。185 个编号 AC 的 ID、标题、优先级保持不变；模型阈值没有虚构，F-08 内容也符合限定范围。但 R-002～R-007 表明组级覆盖、Smoke、容量数据集、性能口径和 Fixture 状态尚未满足 Task AC。

### Architecture / Standards

`changes_requested`。8 个路径均位于 allowed_paths，未加入应用源码或素材二进制。R-001 违反单一事实来源边界；R-010 是小范围未授权产品语义扩张。未发现其他代码 smell 适用于本次纯文档切片。

### Verification

`changes_requested`。作者脚本在固定 head 上退出码 0，Git 检查也通过；但 R-008/R-009 是可产生误报 PASS 的护栏缺口，且当前脚本没有发现 R-002～R-007 的内容错误。

## 实际验证

| 场景 | 命令或手工步骤 | 环境 / commit | 结果 | 证据 |
|---|---|---|---|---|
| 固定对象与祖先关系 | `git rev-parse`；`git merge-base --is-ancestor 9472df5 588383f` | Git 2.52.0 / `588383f` detached | PASS | 两个 SHA 可解析，base 是祖先，diff 非空 |
| 作者验证脚本独立复跑 | `pwsh -NoProfile -File ./verification/TASK-003/verify.ps1` | Windows / `588383f` detached | PASS，exit 0 | 8 条 PASS；仅代表护栏执行成功 |
| 空白错误 | `git diff --check 9472df5 588383f --` | 同上 | PASS，exit 0 | 无输出 |
| 变更统计 | `git diff --stat 9472df5 588383f` | 同上 | PASS | 8 files，+504/-16 |
| allowed_paths | 独立读取 Task 白名单并检查 8 个变更路径 | 同上 | PASS | 全部在 TASK-003 范围 |
| 185 AC 保留 | 独立提取 base/head 的 `## AC-* [P*]` 并逐行比较 | 同上 | PASS | 185/185，完全相同 |
| ACG 与未编号要求覆盖 | 对照 ACG 两表、D08 §68～§76、D13 扩展表 | 同上 | FAIL | 10 个 ACG 集合一致，但未覆盖全部未编号规范；见 R-003 |
| Fixture/许可规则 | 对照 Task AC2、Manifest 生命周期及脚本断言 | 同上 | FAIL | 0 available、19 planned、0 missing；见 R-007 |
| 性能协议 | 对照 Task P95 要求与 D08 冷启动 Gate | 同上 | FAIL | 无 estimator/计时边界，冷启动被通用预热；见 R-004 |
| F-08 实际内容 | `git diff --unified=0 ... -- D02 D04 D11` | 同上 | PASS | 只新增三条 `Blocked → Cancelled`，无其他实际改动 |
| 模型阈值纪律 | 搜索数值阈值、Mock/PASS 规则 | 同上 | PASS | 使用 `UNAPPROVED_THRESHOLD`，未发现虚构模型质量数值 |
| Handoff 元数据 | 只读检查作者分支 `c55ec27` | 不在 reviewed head | PASS（交付元数据） | Handoff 绑定正确，Task frontmatter 为 `in_review`；不扩展本报告审查范围 |
| Mermaid 渲染/语法解析 | 未执行 | `588383f` | NOT_RUN | 仅检查围栏；无渲染器证据 |
| 产品、模型质量、性能、SQL、打包 | 本 Review 不执行 | `588383f` | N/A / NOT_RUN / BLOCKED | 仓库无产品实现；素材和阈值未就绪 |

## 结论与复审

`decision = changes_requested`。不得合并 `588383f`。作者应修复 R-001～R-010，生成新的内容 head 和 Handoff；复审必须重新绑定 `base_commit=9472df5` 与新 `reviewed_head`，不能沿用本次批准状态。F-08 的实际修订已经通过，复审只需确认其未回归。

