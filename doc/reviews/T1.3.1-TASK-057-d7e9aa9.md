---
task_id: T1.3.1 / TASK-057
reviewer: Codex（独立非作者 Reviewer，Round 2）
author: ZCode（固定交付）/ Codex（Rebaseline Integrator）
base_commit: 676319bb1b9e9682b13402c322fded43bbaa90c8
reviewed_head: d7e9aa9f1e8981d6bd12bc7ca6eea8453c3d67ab
decision: changes_requested
---

# Review：T1.3.1 / TASK-057 Rebaseline Integration — Round 2

## 范围与依据

固定审查范围为 `676319bb1b9e9682b13402c322fded43bbaa90c8..d7e9aa9f1e8981d6bd12bc7ca6eea8453c3d67ab`，返修 disposition 范围为首轮 reviewed head `8318b64b73f84898f1122b459ebb15506e2b67db..940d373537d610d22150c42b1983482179c3bc87`。候选 `d7e9aa9` 的父提交是产品返修 `940d373`，其后只修改本 Task/Handoff 并加入验证证据；固定交付 `29546f7` 与历史 review head `257ee5b` 均为候选祖先。

已完整读取 `AGENTS.md`、`doc/REBASELINE_PLAN.md` T1.3.1、`doc/STATUS.md`、`doc/09_COLLABORATION.md`、`doc/tasks/TASK-057.md`、`doc/handoffs/T1.3.1-TASK-057-integration.md`、首轮 Review、Review 模板、相关 D02/D03/D06/D07 条款、实现、调用链及测试。工作树原有 `?? .superpowers/` 未触碰。

## 首轮 findings 与集成 gaps disposition

| ID | Round 2 disposition | 实证 |
|---|---|---|
| R-001 | **fixed** | `create_backup()` 在 completed ledger 写入前由 service 调用 `verify_backup_file()`；注入 read-back 失败返回 `BACKUP_CREATE_FAILED`，无 ledger row。 |
| R-002 | **partial / open（P1）** | read-back、sidecar、ledger 的普通异常路径会 typed 并清理；但正式 DB 在校验/Hash/sidecar 之前发布，进程中断仍留下半成品，见本轮 R-002。 |
| R-003 | **fixed** | 仅接受 32 位小写 hex ID；相对 traversal 与绝对路径均 typed `BACKUP_UNKNOWN`；resolved target 受 backup root 约束。 |
| R-004 | **fixed（原 finding）** | DB/sidecar/ledger schema 交叉校验成立；mismatch=`SCHEMA_MISMATCH`、v999=`SCHEMA_TOO_NEW`，均在 pre-restore/live write 前拒绝。v0 的新问题另记 R-010。 |
| R-005 | **fixed** | 测试真实建立 original + translated artifacts，漂移两者 current/pinned；restore 后逐值回到 backup point，并核对两份 Managed Copy 字节。 |
| R-006 | **fixed** | latch 内 restart/retry 明确断言 control/retry/plan 零调用；`abandonRun` 在 latch 内仍转为 cancelled。 |
| R-007 | **waived（evidence hygiene）** | 全范围 `diff --check` 的 228 条输出仍只来自 13 份固定交付随附历史 raw logs；返修源码/测试、本轮 Task/Handoff 与 `*940d373*` evidence 子范围均 clean。按 Codex ruling 保留原字节，不掩盖本轮文件问题。 |
| R-008 | **fixed** | `beginRestore()` 拒绝重入；嵌套 `restoreGate()` 内层拒绝且不释放外层 latch。 |
| R-009 | **fixed** | continue/restart/retry 在 control/retry/plan 写入前拒绝；旧 reviewer chokepoint probe 无 Worker 出生，状态未半写。 |

## Standards（executed）

连接按线程路由、`RunController.start()` 唯一 Worker 出生点、restore latch、允许/禁止路径及无新增 skip/xfail 均符合仓库约束；未见需单列的 Fowler smell。

但 `src/infrastructure/sqlite/backup.py:71-82` 先 `os.replace(temporary, target)`，后 read-back、Hash 和 sidecar，违反 D07 §31 的“临时文件 → 验证 → Hash → 原子移动”顺序及 TASK-057 AC③“异常时不留半成品”。`src/infrastructure/sqlite/backup.py:204-224` 的 restore 执行与 post-check 也没有统一 typed/rollback 边界，违反应用契约 `src/application/maintenance/backup.py:13` 的 “every failure is typed”。

## Spec（executed）

R-001/R-003/R-005/R-006、R-008/R-009、schema mismatch/v999、真实 current/pinned/translated 回滚均忠实实现；未见 scope creep。

仍缺两项：AC③ 的原子发布在进程中断窗口不成立；AC④ 的 typed/retryable restore 对合法 v0 pre-migration backup 不成立。D07 §49 要求 Restore 后执行 Integrity/Schema/Managed File Reference Check，§80 要求旧数据库通过 Migration 升级；当前实现覆盖 live DB 后直接查询 v0 不存在的 `artifact_revisions`，既不升级/跳过，也不从 pre-restore 回滚。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 状态 |
|---|---|---|---|---|---|---|
| R-002 | P1 | `src/infrastructure/sqlite/backup.py:64-87` | 正式名 `.db` 在 read-back、Hash、sidecar、ledger 前暴露。进程在 `os.replace` 后中断时，留下可长期存在的 canonical DB 半成品，无 manifest/ledger；不满足 AC③ 与 D07 §31。 | Reviewer 将 `os.replace` 包装为“先真实 replace、随后 `SystemExit`”：`CRASH_WINDOW_FILES=['<id>.db']`、`LEDGER_ROWS=0`、`HALF_ARTIFACT=True`。 | 在 temporary DB 上完成 read-back/Hash，并使用临时 sidecar；按安全发布顺序发布 manifest/DB，再写 ledger。补能杀进程或等价模拟 abrupt interruption 的判别测试。 | open |
| R-010 | P1 | `src/infrastructure/sqlite/backup.py:200-224,227-242` | service 自己生成的合法 v0 pre-migration backup 可通过前置验证，却在覆盖 live DB 后因查询不存在的 `artifact_revisions` 抛裸 `sqlite3.OperationalError`；调用者收到非 typed failure，live 已从 v3 降为 v0，pre-restore 未自动回滚。另行注入 restore SQLite destination failure同样泄漏裸 `ProgrammingError`。违反 AC③/④、D07 §49/§80 及应用层 typed 契约。 | Reviewer probe：`V0_BACKUP_SCHEMA=0`、`LIVE_BEFORE=3`、`RESTORE_ERROR_TYPE=OperationalError`、`CODE=<none>`、`LIVE_AFTER=None`；destination failure probe：`ProgrammingError`、`CODE=<none>`。 | 冻结 v0 restore 语义：恢复后迁移，或在旧 schema 上安全跳过/延后 reference check；所有 post-write 失败须 typed，并用已创建的 pre-restore 自动回滚或明确保持可恢复状态。补 v0 restore 与 restore-write failure 判别测试。 | open |

## 验证

统一环境：Windows PowerShell 7.6.6；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3）；`PYTHONPATH=src`；`PYTHONDONTWRITEBYTECODE=1`；`QT_QPA_PLATFORM` 未设置；HEAD=`d7e9aa9`。Reviewer probes 只使用系统临时目录。

| 场景 | 命令/步骤 | 结果 |
|---|---|---|
| targeted | `python -m pytest tests/storage/test_backup_restore.py tests/workbench/test_restore_gate.py -q -p no:cacheprovider -rs` | **PASS：26 passed / EXIT=0** |
| affected integration | targeted + `test_connection_ownership.py` + `test_connection_eviction.py` + `test_run_thread_e2e.py` + `test_schema_migration.py` | **PASS：44 passed / EXIT=0** |
| chokepoint probe | `python verification/TASK-057/review-29546f7/t057_chokepoint_probe.py <worktree>` | **PASS / EXIT=0**：nested gate 拒绝、outer latch 保持、正式 gate 下 continue 无 control 半写/Worker 出生 |
| full regression | `python -m pytest tests -q -p no:cacheprovider -rs` | **PASS：972 passed + 6 skipped / EXIT=0**，1 warning；6 条均为既有 `openssl unavailable`，无 xfail |
| 作者 evidence 审计 | 检查 `verification/TASK-057/rebaseline-integration/*940d373*` | **PASS**：targeted 26、integration 44、probe 0、full suite 5× 972+6；每份 HEAD=`940d373`、SOURCE_TREE_STATE 空、EXIT=0 |
| schema mismatch / v999 | 定向测试及源码顺序检查 | **PASS**：typed 拒绝且 live schema/revision 未变；拒绝发生在 `create_backup('pre_restore', ...)` 前 |
| v0 pre-migration restore | Reviewer 临时真实 SQLite probe：创建 v0 backup → migrate v3 → restore v0 | **FAIL**：裸 `OperationalError`，live schema 已变 None；R-010 |
| restore SQLite failure | pre-restore 成功后注入 closed destination | **FAIL**：裸 `ProgrammingError`，无 typed code；R-010 |
| abrupt publish window | `os.replace` 完成后模拟 `SystemExit` | **FAIL**：正式 `.db` 半成品残留，无 sidecar/ledger；R-002 |
| skip/xfail | base/head `git grep` + test diff | **PASS**：两端 skip/xfail 源码命中均为既有 5 处；无新增 |
| diff check | full range；`src tests`；返修/evidence 子范围 | full **EXIT=2**（仅 13 份历史 raw logs，evidence waiver）；其余 **EXIT=0** |

Architecture：**changes_requested**。路径与 newer-schema 信任边界已关闭，但备份发布顺序仍有 crash 半成品窗口，v0 restore 的 post-write 失败没有回滚边界。

Verification：**changes_requested**。正常路径、相邻集成、全仓和 5× 作者证据均绿，但缺失的 v0 restore / abrupt interruption / restore-write failure 用例已被 Reviewer probes 实际击穿。

## 结论与复审

**Changes Requested。** 固定候选 `d7e9aa9f1e8981d6bd12bc7ca6eea8453c3d67ab` 暂不可合入 master。R-002 仍为 partial/open，R-010 为新发现 P1；其余旧 findings/gaps 已闭合或按既定 evidence-hygiene ruling 豁免。

推荐下一步：在同一 T1.3.1/TASK-057 范围内只修 `src/infrastructure/sqlite/backup.py` 与对应 storage 测试，关闭 R-002/R-010；随后重跑 targeted、affected integration、chokepoint、full suite，并由独立非作者 Reviewer 固定新 head 复审。不得修改本 Review 的历史结论。
