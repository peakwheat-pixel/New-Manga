---
task_id: TASK-056
reviewer: 独立子代理（ZCode 会话内新开子对话，与作者不同对话；窗口条款 §8.2 口径）
author: ZCode
base_commit: 8bf8da3f988cdeffce83ad251a15c22ce862dd1a
reviewed_head: 4c8b8a7
decision: approved_subagent
started_at: 2026-09-19 ~03:15 +0800（完成 03:44）
superseded_by: 修订 15f977a（R-001 守卫）复审——见追加登记
---

# Review：TASK-056 缓存·版本·模型清理（TASK-021 冻结子集②）

独立子对话 Review（开始时间 2026-09-19 ~03:15 Asia/Shanghai，完成 03:44，reviewer=独立子代理，与作者不同对话）

> 编者注（作者落盘）：以下为独立子代理报告原文（结构保留、内容逐字）；作者仅添加 frontmatter 与本注。R-001 守卫修订与复审见 [TASK-056-15f977a.md](TASK-056-15f977a.md)。

## 固定对象

- 作者 worktree（只读）：`G:/CODEX/New Manga.worktrees/TASK-056-zcode`，分支 `agent/zcode/TASK-056-cache-version-cleanup`
- base_commit：`8bf8da3f988cdeffce83ad251a15c22ce862dd1a`（作者开工已 merge master 至 `c083f60`）
- reviewed_head：`4c8b8a7`（实现 `cae3dd3` + 文档 `4c8b8a7`），head 树与记录一致，两仓库工作区均干净
- 依据：Task AC ①–⑥ 与允许/禁止范围、Handoff、`doc/09_COLLABORATION.md` §6（Architecture + Verification 两面）、`doc/templates/REVIEW.md`；参照 TASK-053 `trash.py`、TASK-045 R-005、TASK-046 `_TILE_CACHE_FORMAT`/`_cache_key`

## 范围与写集合核对

`git diff --stat c083f60..4c8b8a7`：10 文件 +585/−3。逐文件核对：

| 文件 | 允许范围判定 |
|---|---|
| `src/application/maintenance/cleanup.py`（新增 185 行） | 允许（src/application/**） |
| `src/infrastructure/imaging/tile_cache_sweep.py`（新增 39 行） | 允许（src/infrastructure/**） |
| `tests/maintenance/test_cleanup.py`（新增 202 行，目录为新建） | 允许（tests/**） |
| `doc/handoffs/TASK-056-delivery.md`、`doc/tasks/TASK-056.md` | 允许（Handoff/本 Task） |
| `verification/TASK-056/full-suite-run{1..5}.log` | 允许 |

禁止范围零触碰：无 Schema/migration、无 `requirements.txt`、无 `AGENTS.md`、无其他 Task、无 `src/ui/qml/**`；无放宽断言、无新增 skip/xfail；`git diff --check` exit 0；无 remote 未 push。`webtoon_tiles.py` 零改动（diff 0 行）。

## Architecture 面（executed）

- **依赖方向**：`CleanupService`（application）只 import `application.maintenance.ports`，零 infrastructure import——grep 证实 cleanup.py 无 sqlite/database/connect。`CacheInventorySource` Protocol 在 application（消费者侧）定义、实现在 `infrastructure/imaging`，依赖倒置正确。`tile_cache_sweep.py` import `application.maintenance.cleanup.CleanupTarget`（infrastructure→application）沿用仓库既有广泛模式（十余处先例），架构守卫 `tests/core/test_architecture.py` 7 passed（含 `test_application_layer_does_not_import_infrastructure`）。
- **maintenance 域内聚**：不重复实现删除/清单逻辑——remover 与 manifest store 均为注入复用，`pending_cleanups` 与 TASK-053 的 `batches`/`pending_purges` 段同文件异键共存（探针 B 实测互不干扰），原子写/全清摘段语义同源。`_is_safe` 反斜杠归一化 + remover 的 managed-root 硬边界构成双层防护。
- **生产布局匹配**：`bootstrap/app.py:663` 生产 `cache_dir=<managed_root>/cache/webtoon-tiles` 与 sweeper 布局一致；`_cache_key` 产物 `tile-{digest}-{index:05d}` 与 `tile-*.png` glob 匹配，glob 的宽松语义恰是跨代回收所需（v1 遗留均被枚举）。sweeper 只列举不删除，删除单点走 remover。
- **安全边界独立验证（探针 A，TEMP 脚本，真实组件装配）**：15/17 PASS——preview 数字正确、run 全删且结构化结果正确、再 run 幂等、非 `cache/` 前缀条目 skipped 带原因且文件逐字节幸存、`remove_managed("../...")` 抛 `ImmutablePathViolation` 且 root 外文件幸存、`_is_safe` 对正常反斜杠路径归一化、revisions 布局不在可清理前缀。**2 项 FAIL 是 reviewer 自设对抗探针（见 R-001）**：`cache/webtoon-tiles/../../books/.../original/p2.png` 与反斜杠形态通过 `_is_safe` 字符串前缀检查后，managed root 内的 current original 被实际删除；逃出 managed root 的形态仍被 remover 硬拒绝（fail-closed，文件幸存）——**用户源文件（root 外）永不触碰的核心不变量结构性成立**。

## Verification 面（executed）

环境：Git Bash + venv `TASK-012-py312`，`PYTHONDONTWRITEBYTECODE=1`、`-p no:cacheprovider`、`QT_QPA_PLATFORM` 未设。

| # | 场景 | 结果 |
|---|---|---|
| 1 | 定向维护套件 | **PASS**：8 passed / 0 skipped，EXIT=0 |
| 2 | 全仓复跑 | **PASS**：900 passed / 0 skipped / 1 warning，41.96s，EXIT=0 |
| 3 | 基线算术（`git archive c083f60` TEMP 导出树实跑） | **PASS**：892 passed / 0 skipped，EXIT=0 → 892+8=900 成立 |
| 4 | 作者 ×5 日志对照 | **一致**：均 900 passed / EXIT=0（41.77–44.34s） |
| 5 | 探针 A（安全边界） | 15/17 PASS；2 FAIL=R-001 对抗证据 |
| 6 | 探针 B（pending 语义，直读 manifest JSON） | **16/16 PASS**：失败不静默→`pending_cleanups` 持久化→retry 清除且段被摘除；与 TASK-053 段共存不受扰；重复失败去重合并；全成功 run 无残留 |
| 7 | 架构守卫 `tests/core/test_architecture.py` | **PASS**：7 passed |

探针脚本与导出树均在 TEMP，仓库零写入；结束时 `git status --short` 两仓均为空。

## AC 逐条判定

| AC | 判定 | 依据 |
|---|---|---|
| ① 范围与安全边界 | **PASS**（附 R-001 P2 open） | 两个清单明确；前缀外 skipped 且从不递交 remover；用户源文件结构性不可达（remover 硬边界实测）；current/pinned 不在 `cache/` 前缀。缺口：`..` 组件可绕过前缀白名单（R-001，生产当前不可达） |
| ② 预览与结果 | **PASS** | preview 实测正确；`CleanupOutcome` 实测。R-002（P3）：混合清单含越界条目时 run 抛异常而非逐项 failed |
| ③ 瓦片缓存跨代回收 | **PASS** | 两代均被枚举与回收；缺失目录=空；生产布局/命名匹配已核 |
| ④ 幂等与失败 | **PASS** | 重复 run 幂等实测；失败不静默（结构化 failed + pending 持久化 + retry）；与 TASK-053 模式对称一致 |
| ⑤ 不回归 + 判别力 | **PASS** | 基线 892→900 全过、0 新增 skip；判别力声明诚实 |
| ⑥ 交付链 | **PASS**（按交付边界） | Handoff ✓、verification ✓、本 Review ✓；集成与 STATUS 台账属集成阶段 |

## NOT_RUN 与判别力声明诚实性

- **生产接线 NOT_RUN：如实**（bootstrap 无 CleanupService 注入，Handoff 声明）。
- **模型目录未注册 NOT_RUN：如实**（真实 provider/模型路径属窗口排除项）。
- **判别力「新能力不适用」声明：诚实**（base 树 892 中无这 8 个用例；全部为安全不变量回归钉住）。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 建议 | 处理状态 |
|---|---|---|---|---|---|
| R-001 | P2 | `cleanup.py` `_is_safe`；交互点 `run()` | `_is_safe` 为纯字符串前缀匹配：相对路径含 `..` 组件（`cache/webtoon-tiles/../../books/.../original/p2.png`）归一化后仍以 `cache/` 开头即放行，remover 只强制 managed-root 边界，解析后仍在 root 内的 **current/pinned revision 被实际删除**。触发条件：inventory 返回带 `..` 的路径——生产唯一 inventory（`WebtoonTileCacheSweeper`）当前不可达；但模型目录「可后续注册」，新 inventory 实现有误即成洞。root 外路径仍被硬拒绝，用户源文件不变量成立 | `_is_safe` 改为拒绝含 `..`/`.` 组件（`PurePosixPath(normalized).parts` 检查）后判前缀；补一个 `..` 绕过被 skipped 的回归用例。可在后续切片收口，不阻塞本 head | open（作者已随 `15f977a` 修复，见复审） |
| R-002 | P3 | `cleanup.py` `run()` 仅捕获 `OSError` | inventory 含越界条目时 remover 抛 `ImmutablePathViolation`（RuntimeError）中止整个 run：已删文件不可逆、outcome 丢失。fail-closed 方向正确，但 AC ② 的逐项结果在此场景拿不到。对象为可再生缓存，重跑可恢复 | 可考虑一并捕获记为 failed（fail-closed 保持）或文档声明此边界 | open |
| R-003 | P3 | `cleanup.py` Protocol 内联 | `CacheInventorySource` 内联于 cleanup.py，而 TASK-053 模式为集中 `ports.py`；两种端口放置方式并存 | 后续统一时迁 ports.py | open |
| R-004 | P3 | `test_cleanup.py`（访问 `service._inventory`）；命名契约散布两文件 | 测试触碰私有成员；`tile-*.png` 命名契约无单一来源 | 测试改显式注入 inventory；命名契约加注释互链 | open |

## 结论

Architecture 与 Verification 两面均 executed 且通过：写集合 100% 在允许范围内、禁止范围零触碰；安全边界核心不变量经独立探针全部复现；基线算术（892+8=900）经导出树实跑证实；作者 ×5 日志逐项吻合，NOT_RUN 与判别力声明诚实。唯一实质缺口 R-001（P2）为防御纵深问题：生产当前唯一 inventory 输出受控、不可达，root 硬边界完整，不构成 P0/P1。无 P0/P1 未解决、无关键验证缺失。

固定 head `4c8b8a7` 可交 Codex 按 §6 串行集成；集成时建议把 R-001 登记入 STATUS 台账行或后续 Task。

**decision: `approved_subagent`**
