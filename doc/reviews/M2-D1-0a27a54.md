---
task_id: M2-D1
reviewer: Codex
author: DeepSeek Harness
base_commit: 307a940265fc325076fe77698750392531f8fce1
reviewed_head: 0a27a54c9048fa1d3149ef4641a5d67fcb20849a
handoff_tip: 6f2cbb3002b9ab6db048d6464fb83c696fe746ce
decision: approved
---

# Review：M2-D1 Headless & Qt Coupling Audit

## 范围与固定点

本报告是 Codex 对 DeepSeek Harness 交付的非作者独立 Review。审查固定代码 base `307a940`、研究 Delivery `0a27a54` 与仅追加 Handoff/Task/范围证据的 tip `6f2cbb3`；`0a27a54` 的 parent 是 Codex 登记起点 `bbeb489`，`6f2cbb3` 的 parent 是 `0a27a54`，base 是两者祖先。Reviewer 在独立 `codex/M2-D1-review-0a27a54` worktree 只读审查作者交付。

依据：[M2-D1 Task](../tasks/M2-D1.md)、[AD-001](../AD-001_TARGET_DESKTOP_ARCHITECTURE.md)、[09 协作协议](../09_COLLABORATION.md)、[研究报告](../research/M2-D1_HEADLESS_QT_COUPLING_AUDIT.md)、[Handoff](../handoffs/M2-D1-0a27a54.md)、`verification/M2-D1/**` 原始记录，以及固定 base 上的 `src/**`/相关测试夹具。Architecture 与 Verification 均已执行；Standards 与 Spec 两个辅助视角已执行。未运行产品测试，也未在装有 PySide6 的环境验证 Qt 平台行为。

## Architecture：26 项矩阵逐项核对

下表的行号指固定 base 的源码；P1/P4/P5/P6 指 `verification/M2-D1/05`、`07`、`08`、`10` 探针日志。`支持`表示该行所述耦合与分类方向有源码或运行证据，**不表示业务行为已测试通过**。

| ID | 分类 | 核对证据 | 结论 |
|---|---|---|---|
| 01 | KEEP | `src/domain/books/entities.py:3`；P1 13/13；必跑 `rg` 的 domain 命中只有注释 | 支持 Qt-free 导入面 |
| 02 | KEEP | `src/ports/rendering/ports.py:5`；P1 23/23；必跑 `rg` 的 ports 命中无 Qt 导入 | 支持 |
| 03 | KEEP | `src/application/export/pdf_qt.py:19-20` 以外的导入清单；P1 66/66；`tests/core/test_architecture.py:12-16` | 支持，运行行为未证 |
| 04 | DECOUPLE | `src/application/export/pdf_qt.py:18-20`；`src/application/export/ports.py:25`；`src/bootstrap/app.py:837` | 支持，调用 `compose()` 仍需 Qt |
| 05 | KEEP | `src/infrastructure/sqlite/connection.py:324-370`；P4；写者搜索 W2/W5 | 支持 Qt-free SQLite 适配器 |
| 06 | KEEP | `src/infrastructure/providers/step_writes.py:207,249,270,358,365,435,452,500`；W5/W6 | 支持，保留直接 SQL 例外 |
| 07 | KEEP | `src/infrastructure/rendering/locator.py:20-50`；P4 | 支持 |
| 08 | KEEP | `src/infrastructure/imaging/streaming_png.py:48-50`、`webtoon_tiles.py:21-23,317`；P4 | 支持 Qt-free 瓦片通路 |
| 09 | KEEP | `src/infrastructure/settings/json_profile_stores.py:35-38` 等包的 P4 导入结果；补充搜索 A1/A2 | 支持无 Qt 导入，不能推出所有业务行为无需调整 |
| 10 | DECOUPLE | `src/infrastructure/importing.py:46-47,180-181,354-355`；像素路径 `:211-222,364-377` | 支持 |
| 11 | DECOUPLE | `src/infrastructure/rendering/qt_compositor.py:17-26,40,53-59,80-104`；P4 | 支持 |
| 12 | DECOUPLE | `src/infrastructure/rendering/pixel_source_style.py:24,52-55,152`；P4 | 支持 |
| 13 | REPLACE 候选 | `src/infrastructure/rendering/font_catalog.py:11,26-34`；P4 | 支持候选，替代技术未决 |
| 14 | REPLACE 候选 | `src/infrastructure/rendering/qt_layout.py:15,43-51,88-149`；P4 | 支持候选，替代技术未决 |
| 15 | DECOUPLE | `src/bootstrap/app.py:36-42,596-605,675-687,1077,1108,1125`；P2/P3 | 支持 |
| 16 | LEGACY | `src/ui/viewmodels/workbench/run_controller.py:18,101-130`；其他 ViewModel 的 PySide6 导入；P5 | 分类支持；业务方法占比估算无可复算证据（R-004） |
| 17 | REPLACE 候选 | `src/ui/models/library/models.py:10`、`src/ui/models/tasks/page_list_model.py:12`；P5/P6 | 模型替换方向支持；`models/tasks/*` 范围过宽（R-002） |
| 18 | LEGACY | `src/ui/qml/Main.qml:32-34`；25 个 QML 文件清单；AD-001 parity 约束 | 支持 |
| 19 | DECOUPLE | `src/ui/viewmodels/export/viewmodel.py:23-24,57-60,410`、`reader/viewmodel.py:185,425` | 支持 |
| 20 | DECOUPLE | `src/ui/viewmodels/workbench/run_controller.py:101-130`、`workbench/viewmodel.py:140`、`reader/viewmodel.py:69` | 支持 |
| 21 | DECOUPLE | `src/bootstrap/app.py:1077,1182`；入口搜索 E4 零命中；P2/P3 | 支持当前无独立 headless 入口 |
| 22 | UNVERIFIED | `tests/rendering/conftest.py:3-6`、`tests/core/test_bootstrap.py:3-5`；P2/P3 因缺 PySide6 失败 | 正确保留 Qt 平台实验未验证 |
| 23 | DECOUPLE | `src/bootstrap/app.py:283-334`、`qt_compositor.py:40,53`、`reader/viewmodel.py:425` | 支持大二进制边界待设计 |
| 24 | KEEP | P4 原始结果：infrastructure 52/56 导入 OK，4 个 Qt 渲染模块失败 | 支持非 UI 基础设施 Qt-free 导入面 |
| 25 | DECOUPLE | `src/ui/models/tasks/projection.py:16-17`；P6 文件加载 OK、包名导入 FAIL | 支持 |
| 26 | DECOUPLE | `src/ui/viewmodels/workbench/region_canvas.py:3,38-56,91-95`；P6 同上 | 支持 |

Architecture 结论：六棵目录及 Qt imaging/rendering/font、Webtoon、importing、bootstrap、SQLite、headless 入口均有分类和可追溯证据。26 项的主要分类方向可用；第 17 项的通配路径必须按 R-002 缩窄后引用。报告未选择 Qt 替代技术，也未释放实现。

**写者权威的精确口径**：W1/W5/W7 与源码只证明所查静态 SQL 写语句位于 Python `src/infrastructure/**`（包括 `providers/step_writes.py`），且 `src/ui/**` 零直接 SQL；它们不证明只有单线程写入、所有持久数据都在 SQLite、或当前 Qt ViewModel 从不发起持久化操作。`src/ui/viewmodels/settings/viewmodel.py:255-257,272-273,543-551` 会调用 JSON profile/凭据存储；`src/infrastructure/settings/json_profile_stores.py:35-38` 和 `credentials/windows.py:110-119` 是另两种持久化实现。报告 §2.5/§5 已记录例外及待决边界，所以将其“基本成立”限定为 **SQL 写入位置**后不构成阻断；AD-001 的未来 Python Core 持久化权威仍是目标约束，不能据本审计宣布已经完整实现。

## Verification

| 项 | 实际核对 | 结果 |
|---|---|---|
| 固定点 | `git merge-base --is-ancestor 307a940 0a27a54`、两提交 parent、Owner branch/tip/worktree | PASS；base 为祖先，tip=`6f2cbb3` |
| 改动范围 | `git diff --name-status 307a940..6f2cbb3`：22 个路径，含 Codex 先前的 2 个 Task 登记提交；`bbeb489..0a27a54` 是 Owner 的 16 路径；`0a27a54..6f2cbb3` 仅 Handoff、Task、范围日志 | PASS |
| 禁止路径 | 独立按 `src/`、`tests/`、`packaging/`、`requirements*`、T3.2.1、历史 Review/Handoff/Verification 过滤：0；`git diff --check 307a940..6f2cbb3` exit 0 | PASS |
| 范围日志 | `verification/M2-D1/11-delivery-scope-and-diff-check.log` 的首次 `rg` exit 2 和首次 PowerShell `checked_paths=0` **不是 PASS**；同日志更正后 `checked_paths=16, violations=0`，且独立 Git diff 一致 | PASS（按更正结果） |
| 必跑检索 | 复核 `rg -n 'PySide6\|QObject\|QImage\|QPdfWriter\|sqlite3\|connect'` 六目录：286 命中；`02-required-rg.log` 有 pwsh/HEAD/原始输出/exit 0 | PASS |
| 探针 | P1 102/102 导入 OK；P4 53 OK/5 FAIL（其中 infrastructure 52/4、bootstrap 1/1）；P5 5 OK/16 FAIL；P6 两文件按路径加载 OK；P2/P3 因缺 PySide6 exit 1 | PASS（只限各探针实际测试面） |
| PySide6 环境 | 复算 `py -3.10/-3.11/-3.12/-3.14 -B -c '...find_spec("PySide6")'`：四个解释器均 `None`、exit 0；交付日志只直接证明默认 3.14 | PASS（事实）；交付证据缺口见 R-005 |
| 产品测试 | 未运行；代码未修改，环境无 PySide6 | NOT_RUN，未冒充 PASS |
| Qt 平台行为 / 无 GUI 生产装配 | 无可用 PySide6；P2/P3 的失败只证明当前环境入口无法导入 | UNVERIFIED，未冒充 PASS |
| React/Tauri/IPC | Delivery tree 无 `frontend/`、`src-tauri/`、`package.json`、`Cargo.toml` 或迁移实现；§6 明言候选需新 Task | PASS（无提前授权） |

固定 base..tip 没有修改 T3.2.1 Gate、AC3、REPAIR-13、M2 实现授权；集成时仍需另行核对 `STATUS`/Plan 的最终状态。没有运行产品测试属于本研究 Task 允许的 `NOT_RUN`，不构成产品或 Headless 运行 PASS。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 建议与处理 |
|---|---|---|---|---|
| R-001 | P2 | `doc/research/M2-D1_HEADLESS_QT_COUPLING_AUDIT.md:36` | “Core 三层在导入期与运行期都不依赖 Qt”超出 P1 导入探针；`src/application/export/pdf_qt.py:19-20` 的 `compose()` 调用仍需 Qt。报告 §4/§7 已正确将运行行为列未验证，故不阻断整体结论。 | 后续文档修订收窄为“导入期可无 Qt”；`open`，本次不改作者 head。 |
| R-002 | P2 | 同报告 `:145`（M2D1-17） | `src/ui/models/tasks/*` 包含 `projection.py`，但该文件在 M2D1-25 `:143` 被正确列为 `DECOUPLE`，不能整包归 `REPLACE`。 | 后续仅列 `page_list_model.py` 与 `library/models.py`；`open`。 |
| R-003 | P2 | `verification/M2-D1/09-evidence-manifest-and-coverage.md:40` | infrastructure 误写 `51 OK / 5 FAIL`，把 bootstrap 的失败计入该树；P4 原始日志与报告 §2.1 正确值为 52/4，bootstrap 1/1，总数 53/5。 | 后续修正覆盖表；本 Review 使用原始日志，`open`。 |
| R-004 | P2 | 同报告 `:142`（M2D1-16） | ViewModel 业务方法体 `≈55%~60%` 与分 VM 百分比没有列出计算口径或逐方法证据，不能作量化事实。 | 后续给计数法或只保留定性观察；`open`。 |
| R-005 | NOTE | 同报告 `:27` 与 `verification/M2-D1/09-evidence-manifest-and-coverage.md:54` | “四个解释器均无 PySide6”在交付日志只列解释器和默认 3.14 的 `find_spec`；Reviewer 逐个复算为真。 | 后续补全证据口径；`open`。 |
| R-006 | NOTE | 同报告 `:38,177,205` | “写者权威基本成立”仅适用于静态 SQL 写入位置；JSON profile 与 Windows 凭据也是持久状态，Settings ViewModel 会发起写入。报告已列例外，读者仍需区分现状与 AD-001 目标。 | 后续单独审计持久化权威；`open`，本 Review 已限定结论。 |

无 P0/P1 finding。以上 P2/NOTE 不改变六目录和指定边界的事实分类，也不要求在已审 head 后修改作者交付；可在后续独立文档治理或审计补充中处理。作者 Handoff 对证据文件数量少计两份保留的失败尝试日志，是非阻断导航误差；本 Review 核对了实际文件清单，未把失败尝试当作成功探针。

## 结论与集成边界

**Architecture：APPROVED。Verification：APPROVED。整体 `decision: approved`，仅绑定 `reviewed_head=0a27a54`、Handoff tip=`6f2cbb3`。** M2-D1 可由 Codex 串行集成；本批准只接受研究证据及其明确的 `NOT_RUN`/`UNVERIFIED` 边界，不批准产品、Headless 运行、迁移实现或未来 Tauri packaging。

集成后应在 Task 回填本 Review commit 与 integration commit、勾选 AC5，收敛 `STATUS`/`00_INDEX` 的导航与状态。T3.2.1 Full Gate 仍须 `OPEN`、AC3 `BLOCKED`、REPAIR-13 `ready`；M2 实现仍为 `PLANNED / NOT_RELEASED`。任何后续报告内容修订需要按新增切片重新审查，不能沿用此批准。
