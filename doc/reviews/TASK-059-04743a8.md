---
task_id: TASK-059
reviewer: Codex
author: ZCode
base_commit: 07397ac74b3e2682ea90d06248b183240ded1b1d
reviewed_head: 04743a85d75daf4bbf47097459f490952d6bcbfb
decision: changes_requested
---

# Re-review：TASK-059（R4/F @ `04743a8`）

Reviewer=`Codex`（非作者）。固定对照旧审 `07397ac`，复审 R4 组合候选 F。作者工作区只读；本报告落在 Codex Review 分支。

## 范围与依据

- R4 自身 diff：`714585a..04743a8`。`07397ac..04743a8` 含 master merge 带入的 TASK-061 变更，不归 R4 切片。
- 规范来源：用户 ND-1 裁决、TASK-059、契约 §6C/§15、R3-001。
- 证据：`tokens-cand-f.json`、F HTML 块、60 组合审计、D1/D2/D3 判别、49 张截图。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R4-001 | P1 | `doc/design/ui-reference.html:438,442`；`doc/contracts/UI_UX_GUI_DESIGN.md:152-153` | 用户 ND-1 要求“比例大小按 B”，但 F 的书架卡片宽仍取 A：B=`--card-w:158px`，F=`--card-w:132px`。契约把 `--card-w` 从“几何令牌相等”断言中排除，因此 `F.geometry == B.geometry` 只在自定义的 20 项子集上成立，属于事后缩小验收面。卡片宽直接影响网格尺寸与封面高度，不是纯粹 IA 字段。 | 独立读取 HTML：B 158px、F 132px；JSON geometry 无 `card-w`。 | 若继续执行用户原裁决，F 改为 B 的 158px，并把 `card-w` 纳入几何相等断言和审计；若确实想保留 A 的 132px，必须作为新的用户例外另行裁决，不能由设计稿自行豁免。 | open |
| R4-002 | P2 | `verification/TASK-059/screenshots/` | 复审要求目检 F 的“书架+工作台两主题”，但入库的 F 截图只有 `f-dark-bookshelf.png`、`f-dark-workbench.png`、`f-light-workbench.png`，缺 `f-light-bookshelf.png`。我没有在 `04743a8` 的实现上复现出视觉崩溃，但提交物无法独立复核 F 亮色书架，也无法观察 R4-001 的卡片宽差异。 | `Get-ChildItem f-*.png` 仅 8 张；亮色只有 workbench。 | 补 `f-light-bookshelf.png`（若 R4-001 修正为 158px，需用新 head 重拍）；如保留代表图策略，必须清楚说明并取得用户同意。 | open |
| R4-003 | P3 | `verification/TASK-059/export-tokens.py:4`；`verification/TASK-059/discriminate-r001-r004.sh:6,8` | 辅助脚本说明仍有 R3 残留：导出脚本 docstring 仍称生成 `{a,b,c,d,e}`，判别脚本仍写“两个检测/判定通过 = 两个检测”，但现为 D1/D2/D3 三项。 | 文件行与 D3 实际输出不一致；不影响产物正确性。 | 更新为 a–f 与三项判别说明；顺带删除未使用的 `geom_sel` 死变量。 | open |

## 验证

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| 固定点 | `git rev-parse` | `04743a8` | PASS | 分支 HEAD 与被审 head 一致 |
| R4 范围 | `git diff --name-status 714585a..04743a8` | Git | PASS | 仅 `doc/**` 与 `verification/TASK-059/**` |
| 空白检查 | `git diff --check 714585a..04743a8` | Git | PASS | 退出码 0 |
| HTML 语法 | Node 提取 `<script>` | Node v24 | PASS | `syntax OK` |
| F 色彩 | `JSON.stringify(F.modes)==JSON.stringify(A.modes)` | `04743a8` | PASS | true，含 glass material |
| F 几何 | `JSON.stringify(F.geometry)==JSON.stringify(B.geometry)` | `04743a8` | PASS（但只对 20 项子集） | true；`card-w` 未在集合内，见 R4-001 |
| R3-001 对 | 独立复算 F dark `st-skip/panel`、`ink/selected-row` | `04743a8` | PASS | 7.22:1、约 11.34–11.36:1；差值来自 8-bit 合成/取整，两者均 PASS |
| D1/D2/D3 | 运行 `discriminate-r001-r004.sh` | Git Bash + Chrome + venv | PASS | D1 4.22 FAIL、D2 overlap=1、D3 3.01 FAIL、`DISCRIM_EXIT=0` |
| 60 组合 | 读取 `audit-result.txt` | `04743a8` | PASS | 60 条组合结果，`AUDIT_EXIT=0` |
| 截图数量 | 文件计数 | `04743a8` | PASS | 49 张 PNG；F 缺亮色书架见 R4-002 |
| F 视觉 | F dark/light bookshelf/workbench 目检 | `04743a8` + 临时补齐亮色书架 | PASS（视觉） | 暗色书架/工作台、亮色工作台已入库；亮色书架临时补拍正常 |

## Architecture / Verification

**Architecture**：`executed`。R4 自身不触碰 `src/**`、`tests/**`、Schema、依赖或启动装配；F 的 IA/objectName 继承 A，不引入 C/D/E 范式。

**Verification**：R3-001 的 7.22/11.34 与 D3 3.01 口径成立；60 组合和判别脚本均 Pass。但 R4-001 是用户裁决的规格偏差，R4-002 使该偏差缺少完整视觉证据，故当前 head 不能进入集成。

## 结论

固定 reviewed_head=`04743a8` 判定 **`changes_requested`**。

返修最小路径：明确 `--card-w` 是否属于用户“比例大小按 B”的范围。若无新用户例外，F 改为 158px、把 `card-w` 纳入 `tokens-cand-f.json` 几何相等断言，并补 F 亮色书架截图后重跑审计。ND-4/5/6/7 仍待用户裁决，ND-6 继续作为 F 实现前待决项。
