---
task_id: TASK-002
reviewer: DeepSeek Harness
author: Codex
base_commit: b1b3f5d65e7b07b6f7edab89af4b1cb769ec6bf4
reviewed_head: c335315511918a4d9e7bc09d216c141a7af0dde6
decision: changes_requested
---

# Review：TASK-002 最小数据与执行契约冻结（c335315）

本报告只覆盖 `b1b3f5d..c335315`。Review 期间未修改任何被审文件；本报告是 Reviewer 新增的唯一文件。

## 范围与依据

**提交链**：`b1b3f5d`（master，TASK-001 集成完成）→ `c6391e7`（授权契约冻结）→ **`c335315`（本次 reviewed_head）** → `e9cf6a3`（交接元数据，`doc/STATUS.md`、`doc/tasks/TASK-002.md`、`doc/handoffs/TASK-002-c335315.md` 三个文件，不含被审契约）。

- `c335315`：`docs(TASK-002): freeze minimum data execution contract`，15 路径，+652/−89，其中新增 `doc/contracts/TASK-002_MINIMUM_DATA_EXECUTION_CONTRACT.md`（343 行）与 `verification/TASK-002/verify.ps1`（136 行）。
- 环境：Review worktree `G:/CODEX/New Manga.worktrees/TASK-002-deepseek-review`，分支 `agent/deepseek/TASK-002-review`，Git common directory `G:/CODEX/New Manga/.git`。为在固定 head 上执行验证，另建一次性 detached 检出 `%TEMP%/nm-review2-c335315`（核验后移除），未触碰作者工作区 `G:/CODEX/New Manga`。

**依据**：TASK-002 的 AC 与 allowed_paths；`doc/00_INDEX.md` 的 Source of Truth 表；`doc/09_COLLABORATION.md`；契约全文；D03/D04/D05/D06/D08/D10/D11 的相关章节；TASK-001 Review（`doc/reviews/TASK-001-615a073.md`、`TASK-001-cdc736c.md`）中的 G05/G10/G11/G13/G07 与 R-011。

**作者护栏声明**：`verification/TASK-002/verify.ps1` 由被审作者编写，是回归护栏，**不等于独立批准**；其 PASS 只证明已编码的模式条件成立。本报告的结论来自独立阅读与独立复跑，不以该脚本代替判断。

**范围外（未审到）**：应用源代码、SQL DDL/迁移、Python/QML 类型、Repository 与事务实现、Provider 行为、性能与打包（仓库内均不存在）；Mermaid 渲染仅做结构检查；契约语义的**可实现性**只能在 TASK-005/006 落地后验证。

## 审查重点逐项结论

| 重点 | 结论 | 依据 |
|---|---|---|
| 单一权威契约与现有文档回链 | **部分不成立**：D03/D04/D05/D06/D08/D11 均已回链且关键枚举一致，但 **D02 §5.3 未同步**（F-01） | 契约 §1 开头、各文档头部回链；D02 全文 |
| Revision/current、人工修改、Pin、TM disabled 保护 | **成立** | 契约 §2.1～§2.3；D03 §6.6/§14/§15.1 新增 current_revision_id、is_pinned、status；V01/V16/V17/V18 |
| RunTarget、快照、多目标映射、原子 compare-and-write | **成立** | 契约 §3.1/§3.2/§4/§8.1/§8.2；D03 §21 target_type 收敛为 `page\|region` + requested_targets_json；V03/V04/V11/V13/V15 |
| Run/Task/Step/Stage/Decision 分层 | **成立** | 契约 §1/§5.1；独立逐值核对：PipelineRunStatus 9 值、PipelineTaskStatus 8 值、StepRunStatus 6 值在契约、D03 §22.1/§23/§24、D06 §73 完全一致 |
| Pause/Stop/Continue/Restart/Abandon、竞态与恢复 | **成立** | 契约 §6.1/§6.2；D06 §64 三动作语义；D04 §30.1 图；V06/V07/V08/V09/V10；TASK-001 R-011 已由契约 §6.2 关闭 |
| SFX、失效矩阵、Candidate、局部合成、孤儿清理 | **成立** | 契约 §7.1/§7.2/§8.1/§8.2；D06 §29 拆分内容几何与排版几何、§85 四步 skip；V12/V13/V14/V17 |
| 六个 Port、错误码、V01～V19 是否足够 | **基本足够，有两处动作缺口**（F-02）；错误码 13 项覆盖主要失败/拒绝路径；V01～V19 + 覆盖矩阵与 §5～§8 逐块对应 | 契约 §9/§10/§11 |
| 无来源功能、提前实现细节、越范围 | **未引入无来源功能**（blocked、StepRunInputRef/OutputRef、TM.status、SFX 图像策略、Port、interruption_disposition 均可回溯 G06～G13 与 R-011）；15 路径全部在 allowed_paths；实现细节仅 §8.1 的事务语句名（F-06） | 契约 §12；TASK-002 AC1～AC4；`git diff --name-only` |

## Findings

| ID | 级别 | 文件/行 | 问题与触发条件 | 影响 | 证据 | 建议 | disposition |
|---|---|---|---|---|---|---|---|
| F-01 | **P1** | `doc/02_TECHNICAL_ARCHITECTURE_.md` L353-369（§5.3） | Run 状态机视图与控制语义未随 TASK-002 契约同步：图中缺 `blocked`、`Paused --> Cancelled`、`Interrupted --> Cancelled`（Abandon）；L369 仍写"Restart / Abandon 的完整落库语义，由 TASK-002 冻结"，而 TASK-002 已完成冻结。触发条件：读者或实现者以 D02 §5.3 作为 Run 生命周期依据 | D02 是技术架构的权威视图之一，其余五处（契约 §5.1/§5.2/§6、D03 §22.1、D06 §73、D04 §30.1、D11 §10）都已含 blocked 与 Abandon，只有 D02 缺；其文本把已冻结语义描述为待冻结，属事实错误陈述，会误导后续实现与验收 | `git diff --stat b1b3f5d c335315` 不含 D02；`git show c335315:doc/02_TECHNICAL_ARCHITECTURE_.md`（§5.3）；对照契约 §5.1/§6.1/§6.2 与 D04 §30.1（`Paused --> Cancelled: 停止`、`Interrupted --> Cancelled: 放弃`） | D02 **不在** TASK-002 allowed_paths，属范围缺口：由 Codex 决定扩展本 Task 范围，或用一个元数据提交同步 D02 §5.3 的图与 L369 措辞（不涉及产品范围） | open |
| F-02 | P2 | 契约 §9（L270-281）对照 §8.1（L261）与 §5.3（L171） | 六个 Application Port 未覆盖两处已定义动作：① 接受/拒绝 `StepResultCandidate`（§8.1 要求"重新执行同一 compare-and-write"，但 `CommitStepResult` 的载荷只有 step_run_id/expected refs/output mapping/temp artifact metadata，**没有 candidate_id**）；② blocked Run 的"重新规划"（§5.3 明确"解除后原 Run 可重新规划"，但 `ControlPipelineRun` 的 action 只有 `pause\|continue\|stop\|restart\|abandon`）。孤儿/历史清理动作同样未落到任何 Port。触发条件：TASK-005/006 实现 Candidate 处置与 blocked 恢复时各自发明接口 | AC4 要求"记录最小 DTO/Port"；契约 §12 声明端口载荷已冻结。缺口会使冲突结果的人工处置与 blocked 解除在实现层无统一入口，容易出现与契约不一致的私有接口 | 契约 §8.1/§5.3/§9 原文；V16 要求"六个 Port 的输入输出均可追溯"，但未覆盖 Candidate 处置路径 | 在 `CommitStepResult` 增加可选 `candidate_id`，或新增 `ResolveStepCandidate` 与 `ReplanPipelineRun`（或把重规划定义为 blocked→pending 的显式动作），并在 §11 补一条向量 | open |
| F-03 | P2 | `doc/08_ACCEPTANCE_CRITERIA.md` L1632-1650（AC-PROGRESS-002，P0） | 验收清单"运行时至少显示"只列完成/失败/跳过/等待页数，未含 `blocked` 计数与原因；而契约 §5.1 新增 Run/Task 的 `blocked` 状态、§5.3 规定 blocked 不计入 terminal 且可解除，D03 §22.1 增加 `blocked_page_count`，D06 §74 同步，D05 L1167 要求"单独显示 blocked 数量和原因"。触发条件：发布验收按 AC-PROGRESS-002 检查 | blocked 是本次新冻结的**用户可见**状态，验收标准未列出它，可能导致实现漏做或发布时漏验；措辞"至少显示"不构成冲突，但构成覆盖缺口。TASK-002 AC4 要求"更新对应 AC" | `git grep -i 'blocked' c335315 -- doc/08_ACCEPTANCE_CRITERIA.md` 仅命中验收状态枚举与 AC-RENDER-002 的 PlanDecision，无进度面板条目 | 在 TASK-003（补齐验收规格）或本 Task 中扩展 AC-PROGRESS-002 或新增一条 blocked 显示/解除 AC；若坚持由 TASK-003 承接，需在 §67 或 13 追踪表中显式登记 | open |
| F-04 | P2 | `doc/05_UI_MAPPING.md` L1115（§31.2）、L1123-1167（§32） | 三处 UI 映射未随新状态收口：① L1115 展开布局的统计行仍是"完成 27 │ 失败 2 │ 跳过 4 │ 等待 7"，无 blocked；② §32 只定义 Running/Paused/Completed(WithFailures)/Interrupted 的按钮状态，未定义 `Blocked`、`Cancelled`、`Failed` 下暂停/停止/继续的 enable 规则（blocked 反而需要"重试/重规划"类入口）；③ L1167 的 blocked 说明句被放在 `## Interrupted` 小节内 | QML 实现者按 D05 制作进度面板时得不到 blocked 的按钮契约，与控制语义（契约 §5.3"解除后可在原 Run 重新规划"）脱节 | `git show c335315:doc/05_UI_MAPPING.md`（§31.2/§32）；对照 D05 L1167 与契约 §5.1/§5.3 | 在 §31.2 统计行加入 blocked，给 §32 补 `## Blocked`（含解除入口）与 Cancelled/Failed 的按钮规则，并把 L1167 移到合适小节 | open |
| F-05 | P2 | `doc/11_ARCHITECTURE_MAPS.md` L284 | 派生状态机用自环表达 Restart：`interrupted --> interrupted: Restart创建新Run并标记restarted`。触发条件：读者把自环理解为一次状态转换 | Restart 不改变原 Run 状态（契约 §6.2：原 Run 保持 `interrupted`，仅写 `interruption_disposition=restarted`，新 Run 独立）。自环在状态机语义上表示"发生转换但仍为 interrupted"，与契约的"状态不变、另建 Run"表述不同，属派生视图精度问题 | `git grep -n 'interrupted --> interrupted' c335315 -- doc/11_ARCHITECTURE_MAPS.md`；对照契约 §6.2 与 D04 §30.1 | 改为在图上以注记/注释表达 disposition（例如把该行改为注释文字，或标注"原 Run 状态不变；新 Run 另行创建"） | open |
| F-06 | P2 | 契约 §8.1 L236（`→ BEGIN IMMEDIATE`、L240 `→ COMMIT`） | 提交协议伪代码使用 SQLite 具体事务语句名，而 §12（L343）声明"实际 SQL DDL/迁移、Repository 与事务实现"由后续 Task 完成。触发条件：非 SQLite 或不同事务策略的实现者按字面理解 | 措辞层张力，不是语义冲突（立即写事务确实是 compare-and-write 的必要条件）。风险低，但会让"实现中立"的边界声明显得不彻底 | 契约 §8.1 与 §12 原文 | 改为实现中立表述（例如"同一立即写事务（SQLite 下为 BEGIN IMMEDIATE）"），或在 §12 明确该语句是语义示例而非实现冻结 | open |
| F-07 | P2 | `doc/STATUS.md` L26 | "DeepSeek Review 工作区"仍指向 TASK-001 的 worktree（`TASK-001-deepseek-review`）与分支（`agent/deepseek/TASK-001-review`），报告路径也仍是 `doc/reviews/TASK-001-*.md`；而 TASK-002 的 Review 在 `TASK-002-deepseek-review` / `agent/deepseek/TASK-002-review` 进行，报告为 `doc/reviews/TASK-002-*.md`。触发条件：后续 Agent 按 STATUS 寻找 Review 工作区 | 运维元数据与事实不符；STATUS 在 allowed_paths 内，本可在本 Task 修正 | `git show c335315:doc/STATUS.md`（L26） | 改为按 Task 描述工作区与报告模式（如 `doc/reviews/TASK-XXX-*.md`），避免每个新 Task 都要改一次 | open |

P0 = 0，P1 = 1（F-01），P2 = 6（F-02～F-07）。

## 验证

| 场景 | 命令或手工步骤 | 环境 / commit | 结果 | 证据 |
|---|---|---|---|---|
| 固定对象与祖先关系 | `git rev-parse b1b3f5d c335315`、`git merge-base --is-ancestor b1b3f5d c335315` | 本地仓库 | PASS | 退出码 0；`b1b3f5d65e7b…`、`c33531551191…` |
| 必需检查 1 | `git diff --check b1b3f5d c335315 --` | `b1b3f5d..c335315` | PASS | 退出码 0，无输出 |
| 必需检查 2 | `git diff --stat b1b3f5d c335315` | 同上 | PASS | 15 files changed, 652 insertions(+), 89 deletions(−) |
| 必需检查 3 | `git diff b1b3f5d c335315 --`（逐文件阅读：D03/D04/D05/D06/D08/D10/D11/00_INDEX/12/13/tasks/README/STATUS/契约） | 同上 | PASS | 全部变更已逐行阅读；结论见 Findings 与上表 |
| 变更路径范围 | `git diff --name-status b1b3f5d c335315` 对照 TASK-002 allowed_paths | 同上 | PASS | 15 路径全部授权（`doc/contracts/**`、`verification/TASK-002/**` 等） |
| 作者护栏复跑 | `pwsh -NoProfile -File ./verification/TASK-002/verify.ps1` | 一次性 detached 检出 `c335315`；PowerShell 7.6.6 / Windows 10.0.26200 | PASS（不等于独立批准） | 退出码 0；6 条输出与 Handoff 声称一致：changed paths authorized + TASK-001 unchanged + 25 tasks frozen；489 local links；12 sections + V01-V19；D03/D04/D05/D06/D08/D11 无已知旧冲突；185 AC headers + G06-G13；git diff --check |
| 检出洁净度 | `git status --short --branch` | `c335315` detached | PASS | `## HEAD (no branch)`，无未提交修改 |
| 状态枚举逐值核对 | 从契约 §5.1 与 D03 §22.1/§23/§24、D06 §73 分别提取枚举块比对 | `c335315` | PASS | PipelineRunStatus 9 值、PipelineTaskStatus 8 值、StepRunStatus 6 值三处完全一致；D03 §23 已移除 `paused`，D03 §24 注明 `blocked/skipped` 不属 StepRun |
| 旧字段残留 | `git grep -n 'input_region_revision_id' c335315` | `c335315` | PASS | 仅命中 Gap Register 原文、契约 §4 的说明句与 TASK-001 历史报告；D03/D06 正文已改用 `StepRunInputRef/StepRunOutputRef` |
| PlanDecision 一致性 | 对照契约 §5.1 与 D06 §26 | `c335315` | PASS | 两处均为 `RUN / SKIP_VALID / SKIP_LOCK / SKIP_POLICY / BLOCKED`；Lock 不再归入 BLOCKED |
| D08 中 `BLOCKED` 用法 | `git grep -n -i 'blocked' c335315 -- doc/08_ACCEPTANCE_CRITERIA.md` 并阅读上下文 | `c335315` | PASS | 三处分别为验收状态枚举、AC-RENDER-002 的 PlanDecision、`LockBlocked` 错误名，与契约语义一致，无冲突 |
| AC 覆盖检查（blocked） | 同上 + `AC-PROGRESS-002` 全文 | `c335315` | FAIL（覆盖缺口） | 进度面板 AC 未含 blocked；见 F-03 |
| D02 回链检查 | `git diff --stat`（D02 未变）+ 阅读 D02 §5.3 | `c335315` | FAIL | 见 F-01 |
| 185 个 AC 标题 | 作者脚本内与基线比对（本报告独立复跑该脚本） | `b1b3f5d..c335315` | PASS | 185 条编号/优先级/标题未变 |
| Mermaid 渲染 | 仅围栏结构检查（脚本内） | `c335315` | NOT_RUN | 未执行渲染器或语法解析 |
| 契约语义可实现性、SQL、类型、Repository、性能、打包 | — | 无实现 | N/A | TASK-002 明确禁止生成这些实现；契约 §12 已声明边界 |

## 三轴结论

**Spec 合规**：AC1～AC3 在文档层成立——契约确实冻结了 Revision/current 与人工保护、多目标映射与事务内 compare-and-write、分层状态与恢复语义、失效矩阵、SFX 图像安全、每 Step 提交与局部合成、六个 Port、错误码与 V01～V19；未改变 185 个 AC 的编号/优先级/标题，未扩大产品范围，未生成 SQL 或业务代码。**AC4 部分成立**：DTO/Port/错误码/快照时点与测试向量均已成文，但"更新对应 AC"只落到 §67 Gate 与 13 追踪表，进度面板的 blocked 显示没有进入验收清单（F-03）。

**Architecture / 文档一致性**：契约作为单一权威的定位清楚，并在六份目标文档中建立了回链；关键枚举与动作语义逐值一致，旧字段（`input_region_revision_id`）已从正文清除，Lock 与 BLOCKED 的分类冲突（G06）已修正，几何失效矩阵的两种几何已分开（G10），SFX 不再自动擦除无替代译文的文字（G11），每 Step 提交与孤儿清理已定义（G13），R-011 的 Restart 语义已冻结。**唯一结构性缺口是 D02 §5.3 未回链**（F-01），另有 UI 映射与派生视图的三处精度问题（F-04/F-05）与 STATUS 元数据滞后（F-07）。

**Verification**：三项必需 git 检查与作者护栏脚本均在固定 head 上实际执行并通过；枚举一致性、旧字段残留、PlanDecision 一致性、D08 的 BLOCKED 用法均为本人独立核验，未依赖作者摘要。契约的**可实现性**与 V01～V19 的**可执行性**无法在无实现仓库中验证，已标 N/A；Mermaid 渲染标 NOT_RUN。

## 结论与复审

**`c335315` 暂不可交 Codex 集成：decision = changes_requested。**

阻塞项为 F-01（P1）：D02 §5.3 的 Run 状态机缺 `blocked` 与 Abandon 路径，且仍把已冻结的 Restart/Abandon 语义写成"由 TASK-002 冻结"；其余五处 Run 状态视图均已同步，只有 D02 不一致，属"单一权威契约与现有文档回链不一致"的实例。D02 不在 TASK-002 allowed_paths，需 Codex 决定扩展范围或以元数据提交修复；两种方式都只改文本，不涉及产品范围与测试重跑。

F-02～F-07 为 P2，可与 F-01 同批处理，或明确 de-scope 并登记：建议 F-02、F-03 在 TASK-003/005 之前关闭（它们直接影响后续实现的接口与验收面），F-04、F-05 随后续 UI/派生图任务处理，F-06、F-07 属措辞与元数据。

**复审要求**：修订后提供新的交付 commit，重新绑定 `base_commit = b1b3f5d`、`reviewed_head = <新 head>`，逐条给出 F-01～F-07 的 disposition（fixed / deferred + 理由）。复审将复跑 `verification/TASK-002/verify.ps1`、`git diff --check`、变更范围检查，并针对修订点做定向核验。

**剩余风险**：契约冻结的是语义而非实现，实际 DDL 的 FK/唯一性/NULL、事务原子性与 Provider 输出映射都可能在 TASK-005/006 落地时暴露契约未覆盖的边界（F-02 已提示接口缺口）；产品能力、模型质量、性能与打包仍 NOT READY。本报告事实仅适用于 `c335315`，分支后续变化（含 `e9cf6a3`）不延用本结论。
