---
task_id: TASK-005
reviewer: DeepSeek Harness
author: Codex
base_commit: d65901b953e6fb26043344ed3d668520847eb295
reviewed_head: f3430080d88f07d789c21e76d5b5b4310fbf3458
decision: approved
---

# Review：TASK-005 复审（f343008）

本报告是 [首次 Review](TASK-005-e914f39.md)（decision=`changes_requested`）的复审，只覆盖 `d65901b..f343008`。未修改首次报告、被审源码、测试、依赖或验证脚本；本报告是 Reviewer 新增的唯一文件。

## 范围与依据

**固定对象**：`base_commit = d65901b953e6fb26043344ed3d668520847eb295`、`reviewed_head = f3430080d88f07d789c21e76d5b5b4310fbf3458`。

**提交链**：`e914f39`（首次交付）→ `4c86c9e`（首次 Handoff）→ `480f432`（首次 Review 报告）→ `8c8482f`（记录 changes_requested）→ **`f343008`（本次 reviewed_head）** → `dcca601`（复审 Handoff，`doc/handoffs/TASK-005-f343008.md`，**不在被审范围**）。

**变更**：`git diff --stat d65901b f343008` = 16 路径、`+948/−13`（含首次交付、Review 报告与本次修订）；修订增量 `e914f39..f343008` 触及 `tests/core/test_architecture.py`、`doc/tasks/TASK-005.md`、`verification/TASK-005/{author-verification,implementation-plan}.md`。

**Reviewer 环境（独立，沿用首次）**：Windows `10.0.26200`；venv `C:\Users\49745\AppData\Local\Temp\task005-review-venv`（Python `3.12.3`）；一次性 detached 检出 `%TEMP%/nm-task005-f343008`（审查后已移除）。

## R-001～R-004 逐项状态

| ID | 原级别 | 状态 | 核验依据 |
|---|---|---|---|
| R-001 | P1 | **resolved** | `test_architecture.py` 的 `ImportFrom` 分支改为 `prefix = "." * node.level`，`node.module is None` 时扫描 `node.names` 别名，匹配前 `lstrip(".")` 取根模块，诊断字符串保留相对层级。Reviewer 独立探针（直接调用被审 `find_forbidden_imports`）：`from . import infrastructure` → `DETECTED .infrastructure`；`from .. import infrastructure` → `DETECTED ..infrastructure`；`from ..infrastructure import x` → `DETECTED ..infrastructure`；诊断含文件与行号。**无误报**：`from . import json`、`from .foo import bar`、`import json` 均 clean。新增测试 `test_relative_from_import_reports_forbidden_alias` 断言两种相对形式与行号 |
| R-002 | P2 | **resolved** | `author-verification.md` 表格改为三行明确区分：「首次被测生产/测试提交 = `b318bdd`」「首次 `reviewed_head` = `e914f39`（复跑见 Handoff 与独立 Review）」，正文改写为「不把较早的 `b318bdd` PASS 误引为固定 head 证据；Review 修订后的新固定 head 由对应新 Handoff 记录」。三个 commit 的角色不再依赖正文说明维系 |
| R-003 | P2 | **resolved** | `implementation-plan.md` 第 4 行新增说明：「上述名称只记录 Codex Desktop 本地执行流程，不是本项目 Source of Truth、依赖或其他 Agent 的必需工具；项目协作规则只以 `AGENTS.md` 与 `doc/09_COLLABORATION.md` 为准」。外部 sub-skill 引用不再可能被读作项目依赖 |
| R-004 | P2 | **deferred（理由成立，Reviewer 接受）** | 独立核验：`git grep -n 'main(' f343008 -- src tests verification` 仅命中定义（`app.py:16`）与 `raise SystemExit(main())`（`app.py:34`），**全仓无第二个调用者**，与「唯一调用为 CLI」一致。`argv` 是 argparse 的应用参数，传入 Qt 会把 `--smoke-test` 泄漏给 Qt 参数解析；在没有程序化调用者或 Qt 自有参数需求前不增加接口分流，符合最小实现原则。理由成立，接受 deferred；若未来出现程序化调用或 Qt 参数需求，应由对应获授权 Task 补测试后调整 |

首次 Review 的 P1 已实质关闭，其余三项按 fixed/deferred 合理处置。

## 本轮新 findings

| ID | 级别 | 文件/行 | 问题与触发条件 | 影响 | 复现证据 | 建议 | 状态 |
|---|---|---|---|---|---|---|---|
| F-01 | P2 | `doc/handoffs/TASK-005-f343008.md:25`（对照 `doc/tasks/TASK-005.md` §架构守卫 L49-54） | 复审 Handoff 声明「Reviewer 提到的动态导入限制**已写入 Task**」，但 `doc/tasks/TASK-005.md` 中没有任何关于动态导入、安全边界或护栏定位的表述。触发条件：后续 Task 或读者按 Handoff 认为守卫定位已在 Task 落档 | 守卫的能力边界（只覆盖静态 import，不构成安全边界、不覆盖 `importlib.import_module` / `__import__`）没有落到权威文档，Handoff 的「已写入」属事实不符；可能导致后续 Task 误以为架构守卫提供完备保障 | `git grep -n '动态' f343008 -- doc/tasks/TASK-005.md` → 无命中（exit 1）；`git grep -n -E '安全边界\|护栏\|沙箱\|静态' f343008 -- doc/tasks/TASK-005.md` 仅命中包边界与 `ast` 描述；全仓仅首次 Review 报告提及动态导入 | 在 `doc/tasks/TASK-005.md` 的「架构守卫」小节补一句定位说明（例如「守卫只覆盖静态 import 的常见违规，不构成安全边界，不覆盖动态导入」），或改正 Handoff 中该句声明 | open（非阻塞） |
| F-02 | P2 | `doc/tasks/TASK-005.md:87` | 「测试要求」仍写「完整 core 测试 **5 passed**」，但本次修订新增 `test_relative_from_import_reports_forbidden_alias` 后，固定 head 上实际为 **6 passed**。触发条件：读者按 Task 正文核对测试数量 | 派生摘要与固定 head 的实测数字不一致（属同类「复制会漂移的数字」问题），削弱证据可核对性 | 本次在 `f343008` 上运行 `verify.ps1`：`collected 6 items` / `6 passed`；`doc/tasks/TASK-005.md:87` 原文为 5 passed；`author-verification.md` 已更新为 6 passed（两处不一致） | 把 L87 改为 6 passed，或改为「core 测试全部通过」不写数量（与 D08 派生摘要的既有处理方式一致） | open（非阻塞） |

本轮 **P0 = 0，P1 = 0，P2 = 2**（F-01、F-02），均不阻塞集成。

## 验证

| 场景 | 命令或手工步骤 | 环境 / commit | 结果 | 证据 |
|---|---|---|---|---|
| 必需检查 1 | `git diff --check d65901b f343008 --` | Git 2.52.0 | PASS，退出码 0 | 无输出 |
| 必需检查 2 | `git diff --stat d65901b f343008` | 同上 | PASS | 16 files changed, 948 insertions(+), 13 deletions(−) |
| 必需检查 3（作者验证脚本） | `pwsh -NoProfile -File ./verification/TASK-005/verify.ps1 -BaseCommit d65901b… -ReviewedHead f343008… -PythonExe '<Reviewer venv>'` | 一次性 detached 检出 `f343008` | **PASS，退出码 0** | 16 allowed paths、精确依赖、Python 3.12.3 无重型依赖、**6 passed**、真实 QML smoke、12 links/fences、`git diff --check` |
| R-001 独立探针 | 调用被审 `find_forbidden_imports` 对 11 种 import 形式 | Review venv | **PASS** | 8 种违规形式全部 DETECTED 并保留相对层级（`.infrastructure` / `..infrastructure`）；3 种干净形式无误报 |
| R-004 调用点核验 | `git grep -n 'main(' f343008 -- src tests verification` | `f343008` | PASS | 仅定义 + `__main__` 调用；无程序化调用者，deferred 理由成立 |
| F-01 核验 | `git grep -n '动态' f343008 -- doc/tasks/TASK-005.md` 与 Handoff 声明对照 | `f343008` / `dcca601` | **FAIL** | Task 无该说明；见 F-01 |
| F-02 核验 | 对照 `verify.ps1` 输出的 `6 passed` 与 `doc/tasks/TASK-005.md:87` | `f343008` | **FAIL** | 数字不一致；见 F-02 |
| 冻结范围 | `git diff --name-only d65901b f343008` 过滤 `doc/tasks/TASK-006..027.md` | 同上 | PASS | 0 个其他 Task 变更；16 路径全部在 TASK-005 白名单 |
| 无业务越界 | `git ls-tree -r --name-only f343008` 查看 `src`/`tests` | 同上 | PASS | `src` 仅 `bootstrap/`、`domain/__init__.py`、`ui/__init__.py`、`ui/qml/Main.qml`；`tests` 仅两个 core 测试文件；无数据库/Provider/四页导航/打包/CI |
| 首轮结论复核 | 重读首次 Review 的 R-001 复现证据并与本次探针对照 | 同上 | PASS | 首次 MISSED 的两种形式现已 DETECTED |
| 正常交互式窗口人工操作 | 未执行 | — | NOT_RUN | 本 Task 仅要求自动 smoke |
| PyInstaller / 干净 Windows VM | 未执行 | — | NOT_RUN | TASK-005 禁止打包配置；TASK-004 证据不替代本 head |
| SQL / Provider / 模型质量 / 性能 | 未执行 | — | N/A / NOT_RUN | 本 Task 无这些实现或获批阈值 |

作者验证脚本在 Reviewer venv 上的实际输出（节选）：

~~~text
PASS: base/head ancestry and 16 allowed paths
PASS: exact Core/dev dependency lock with no heavy AI or PySide6 Addons
No broken requirements found.
PASS: Python 3.12.3 environment is consistent and heavy AI dependencies are absent
collected 6 items
tests\core\test_architecture.py ....                                     [ 66%]
tests\core\test_bootstrap.py ..                                          [100%]
============================== 6 passed in 0.35s ==============================
PASS: core pytest and real QML smoke startup
PASS: 12 local Markdown links and balanced code fences
PASS: git diff --check d65901b953e6fb26043344ed3d668520847eb295 f3430080d88f07d789c21e76d5b5b4310fbf3458
~~~

## 三轴结论

**Spec**：TASK-005 四条 AC 中，AC1（依赖与入口）、AC2（Core 无重型依赖仍可启动，首次已由 Core-only 环境独立强化验证）、AC3（架构守卫，经 R-001 修复后覆盖绝对与相对导入形式）均在文档与实测层成立；AC4（交接与独立审查）由本次复审关闭。方案 A 的边界未被扩张：未创建 `pyproject.toml`/`uv.lock`/Poetry，未引入 import-linter。遗留 F-01/F-02 是 Task/Handoff 的表述精度问题，不改变 AC 结论。

**Architecture / Standards**：最小包边界仍符合 D02 §16「目标边界示意」的定位；守卫的匹配逻辑现在按「去掉相对层级前缀后的根模块」判断，语义正确且诊断信息更精确（保留 `..` 层级）。16 个变更路径全部在白名单内，TASK-006～TASK-027 零变化，未触碰 AGENTS/STATUS/D08。R-004 的 deferred 是合理的最小实现取舍，不构成架构缺陷。

**Verification**：三项必需检查与作者 `verify.ps1` 在 **Reviewer 自己的独立 venv** 上实际执行并通过（`6 passed`、真实 QML offscreen 加载）；R-001 的两个原始 MISSED 形式由独立探针确认已修复且无新增误报；R-004 的「唯一 CLI 调用」经全仓检索确认。未执行项（PyInstaller、干净 Windows、人工窗口、性能、模型质量）保持 NOT_RUN/N/A，未以推测或他人 head 的证据代替本 head。

## 结论与复审

**`f343008` 可交 Codex 集成：decision = approved。**

- 首次 Review 的 R-001（P1）**resolved**；R-002、R-003 **resolved**；R-004 **deferred 且理由成立**（Reviewer 接受）。
- 本轮新增 F-01、F-02 均为 P2，不构成阻塞：F-01 建议在 Task 的架构守卫小节补一句能力边界（或改正 Handoff 声明），F-02 建议把 `doc/tasks/TASK-005.md:87` 的「5 passed」更新为 6 passed 或改为不写数量。
- 无 P0/P1 未解决，关键验证实际执行并通过。

**集成时须由 Codex 完成**：核对 `f343008` 为当前 head、按协议 §6.6 串行集成、记录 `integration_commit`、把本次 decision 与 F-01/F-02 disposition 回填 `doc/STATUS.md`、`doc/tasks/TASK-005.md` 与两份 Handoff，并勾选 AC3/AC4（AC4 需集成验证完成后）；集成后执行该切片的集成检查。

**建议的下一步处置（非阻塞）**：F-01、F-02 可在集成前后的任一文本提交中关闭，不需新的 reviewed head；若在集成后关闭，应在 Task 中记录该 disposition。

**剩余风险**：静态 AST 守卫不覆盖动态导入（`importlib.import_module`、`__import__`）与字符串拼接 import，应始终理解为「防止常见静态违规的护栏」而非安全边界——F-01 正是要求把这一句写进权威文档。四个一级页面、业务流程、数据库、Provider、打包与发布均未实现，也未获本 Task 授权。本报告事实仅适用于 `f343008`；分支后续变化（含 `dcca601`）不延用本批准。
