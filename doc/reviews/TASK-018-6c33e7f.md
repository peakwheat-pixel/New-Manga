---
task_id: TASK-018
reviewer: Codex
author: DeepSeek Harness
base_commit: dce95acbb57a3494cb0f9d8d2d42e27d164176bb
reviewed_head: 6c33e7f237f63fd9b777335e72b84fe317cf866b
decision: approved
---

# Review：TASK-018 Mask / Inpainting 路线独立实验

本 Review 由 Codex 独立执行；被审交付作者为 DeepSeek Harness，二者不同体。固定对象：`base_commit=dce95ac`、`start_head=9ee17189`、`delivery_head=6c33e7f`、元数据 head `ef6d1c3`；工作区 `G:/CODEX/New Manga.worktrees/TASK-018-deepseek`，分支 `agent/deepseek/TASK-018-mask-inpainting-experiment`。本 Review 全程只读：未修改被审 worktree 或任何仓库文件，未创建提交，未 push；所有复跑都在 `%TEMP%` 副本或临时输出目录中进行。

## 范围与依据

- 已读：`doc/handoffs/TASK-018-6c33e7f.md`、`doc/research/TASK-018.md`、`verification/TASK-018/experiment-log.md`、`experiments/TASK-018/mask_protocol.py`、`generate_samples.py`、`run_experiment.py`、`test_mask_protocol.py`、`samples/manifest.json`、`results/experiment.json`、`doc/tasks/TASK-018.md`；AC 来源为 D01 §5、D02 §6.1、D06 §19～22/53/69、D07 §107。
- 白名单核对（相对 `start_head=9ee17189`）：交付 24 文件全部落在 `experiments/TASK-018/**`、`doc/research/TASK-018.md`、`doc/tasks/TASK-018.md`、`verification/TASK-018/**`；元数据提交 `ef6d1c3` 仅动 `doc/handoffs/TASK-018-*.md` 与 `doc/tasks/TASK-018.md`；越界 0。`git diff --check dce95acb 6c33e7f` 退出码 0。相对 `base_commit` 还会看到 `doc/STATUS.md`、`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/contracts/extensions.md`、`doc/tasks/README.md`、`doc/tasks/TASK-019.md`，这些来自授权提交 `9ee1718`（Codex 释放记录），不是本次交付的越界。
- 未覆盖：Mask 内部结构损伤的量化指标、真实 OOM、真实（非合成）漫画样例——作者已登记 `NOT_RUN`，本 Review 未补测；四条学习型路线的质量与性能为 `BLOCKED`，本环境无依赖与权重，无法独立复现。

## 四个重点确认

| 确认项 | 结论 | 证据 |
|---|---|---|
| 四条学习型路线一律 `BLOCKED`、无伪造产物 | **确认** | `results/experiment.json`：30 条记录中 20 条 `BLOCKED`，其 `output_image`/`latency_ms` 为 `null`、`residual_text_pixels_after`/`protected_violations` 为 `"NOT_RUN"`，且不含 `output_sha256`/`mask_sha256` 键；`results/` 下仅 10 张基线 PNG |
| `edge-bleed` 被正确标为非模型基线 | **确认** | 全部 30 条记录 `learned_model=false`、`route_kind="baseline"`、`size_class="none"`；报告与日志同样标注“非模型基线”，并声明不得当作模型结果（算法命名问题见 R-004） |
| `default_eligible` 仅两个基线为 true | **确认** | `simple-fill`、`edge-bleed` 为 `true`；`manga-lama`、`aot`、`brushnet-powerpaint`、`flux` 全部 `false`；代码常量与落盘数据一致 |
| 研究报告中未对学习型路线写入资源数字 | **部分确认** | 学习型路线无任何实测资源数字（无 ms/MB/显存值，均 `BLOCKED`）；但报告与数据留有未实测、无出处的体积数字，见 R-006 |

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-001 | P2 | `doc/research/TASK-018.md:52`；`experiments/TASK-018/mask_protocol.py:143-151`；`experiments/TASK-018/run_experiment.py:207`；`samples/manifest.json` | 报告称 `viol` 为“**保护框**内被改动的像素数”，实际度量是 `protected_pixels()`＝**final Mask 外全部像素变更数**；`manifest.json` 的 `protected_boxes` 从未被 harness 读取（只在 `generate_samples.py:149` 写出）。判据严格更强，故无可靠性损失，但指标陈述不实、声明保护框是死数据 | 全仓 `rg -n protected_boxes experiments/TASK-018` 仅命中 manifest 与 generator；`run()` 只读 `sample["target_box"]`；`protected_pixels` 以 `mask[y][x]` 为跳过条件 | 指标改名为“final Mask 外像素变更数”，并注明其覆盖范围严格包含声明的保护框；或在 harness 中显式断言 `protected_boxes` | **fixed**（集成收口提交修正报告与日志口径；保护框显式断言留待 R-002 同一解锁条件） |
| R-002 | P2 | `experiments/TASK-018/run_experiment.py:68,77,86,95`（`requirements` 全为字面 `False`）、`:171-175`（`_route_status` 仅读常量） | 路线门控是**静态声明**而非环境探测：全仓无 `find_spec`/import 探测；因此 20 条 `BLOCKED` 属构造性结论，换到已装 torch/权重的环境重跑仍会报 `BLOCKED`，`routes[*].runnable_here`/`blocked_reason` 不能作为环境探测证据被 TASK-019 引用 | `rg -n "find_spec|importlib" experiments/TASK-018` 无命中；`_route_status` 返回值只依赖 `ROUTES` 常量 | 增加实验内 `importlib.util.find_spec` + 权重路径探测；若不改，必须在报告与日志显式标注“静态声明、非探测” | **deferred**（事实本身经独立复核为真，限制已在报告与日志登记；解锁条件见文末） |
| R-003 | P3 | `experiments/TASK-018/run_experiment.py:266` | `--output-dir` 指向实验根之外即崩溃：`ValueError: ... is not in the subpath of 'G:/.../experiments/TASK-018'`，退出码 1，且崩前已写出部分 PNG，失败无清理 | 本 Review 执行 `run_experiment.py --repeat 3 --output-dir %TEMP%\task018-review-*` 复现（栈顶为 `pathlib.relative_to`）；随后改用整目录副本复跑才成功 | `relative_to` 失败时回退绝对路径，或在写盘前 fail fast | **deferred**（已在日志登记为 `output_image` 相对路径限制） |
| R-004 | P3 | `experiments/TASK-018/run_experiment.py:10,56`；`experiments/TASK-018/mask_protocol.py:226`；`doc/research/TASK-018.md:26` | “nearest-neighbour / 最近邻扩散”与实现不符：`edge_bleed_fill` 取正交邻居的**均值**（`mask_protocol.py:254`），属扩散/平滑 | 读 `edge_bleed_fill` 实现 | 文档改为“正交邻居均值扩散”，并注明落盘数据中的 `route_label` 为历史命名、含义以报告为准 | **fixed**（报告与日志口径修正；落盘数据 label 保持不变以免重生成 JSON） |
| R-005 | P2 | `doc/research/TASK-018.md:71`（另见 :54 表） | `edge_bleed_iterations=8` 对 52～56 px 高的 Mask 只覆盖边界环，基线对照不对等；报告把 residual（line-art 1932、structure-crossing 2031）解释为“把邻域深色像素扩散进 Mask”，而这两个数是**剩余暗像素数**，不是扩散量 | Reviewer 复算（同一交付样例与输出 PNG 逐像素比对）：Mask 内实际改写像素 s1 0/6864（产物与样例逐像素相同）、s2 1100/4176、s3 528/6864、s4 1994/6864、s5 1504/8400；line-art 实际去掉的 ink 像素为 2010−1932=78 | 报告补“Mask 内实际改写像素/占比”与结论改写为“覆盖不足”，避免 TASK-019 继承“edge-bleed 修复更差”的误读 | **fixed**（报告补复核数据并改写结论；方法已写进报告，可重算） |
| R-006 | P2 | `doc/research/TASK-018.md:29,30`；`experiments/TASK-018/results/experiment.json`（`routes[*].requirements`/`note`） | 学习型路线残留未实测、无出处的体积数字（“数 GB 权重”“10GB+ 权重”），与报告 :103 自身“不得为它们写入任何资源数字”及本轮 Review 判据冲突；读者/TASK-019 可能把 `10GB+` 当作有证据的资源要求 | 报告 :29/:30 与数据 `flux weights (HF, 10GB+)` 等键名 | 删除或标注“未核实、非本环境实测” | **fixed**（报告口径修正）；`experiment.json` 的 requirements 标签为 **deferred**，随 R-002 同一条件清理 |
| R-007 | P2（潜在、当前不可达） | `experiments/TASK-018/run_experiment.py:189-192` | `_evaluate_baseline` 对任何非 `simple-fill` 路线回落到 `edge_bleed_fill`：一旦某学习型路线被判定为 runnable（例如按 R-002 补上真实探测），harness 会把 edge-bleed 的结果当成该模型路线结果并写成 `MEASURED`＋`output_sha256`，即**伪造模型结果**。当前仅因门控为静态 `False` 而不可达，属被静态常量掩盖的隐患 | 读 `:189-192` 分支；结合 R-002 的门控分析 | 引入“已实现/未实现”门控，未实现路线在依赖满足时也必须落 `BLOCKED`（fail-closed），而不是回落到基线 | **deferred**（当前交付无伪造数据；作为解锁前置条件登记，见文末） |

## 验证

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| 协议自检 | `PYTHONPATH=experiments/TASK-018 python -m unittest experiments/TASK-018/test_mask_protocol.py -v` | worktree `6c33e7f`（只读），Python 3.12.3 | **PASS**：12 passed / 0 skipped，退出码 0 | 本 Review 实际执行 |
| 实验复跑 | `run_experiment.py --repeat 3`（在 `%TEMP%` 整目录副本中执行，避免改写被审 worktree） | 同上 | **PASS**：`{"MEASURED": 10, "BLOCKED": 20}`，退出码 0 | 本 Review 实际执行 |
| 非目标像素保护 | 逐记录核对 `protected_violations` | 交付 `experiment.json` | **PASS**：10/10 为 `[]`（违规 0） | 本 Review 实际执行 |
| 可复现性（确定性字段） | 与交付 `experiment.json` 逐字段比对 | 复跑输出 vs 交付 | **PASS**：`routes` 块、`manifest_sha256`、`mask_sha256`、`output_sha256`、`sample_sha256`、`parameters`、`mask` 记录完全一致；latency 复现差 ≤1 ms；仅 `peak_rss_mb` 有 ≤1 MB 正常波动 | 本 Review 实际执行 |
| 产物 Hash | 重算交付 PNG 与样例 SHA-256 | 交付文件 | **PASS**：10/10 `output_sha256` 与 `samples/manifest.json`、`manifest_sha256` 全部吻合 | 本 Review 实际执行 |
| 环境声明 | 探测 `torch`/`diffusers`/`onnxruntime`/`numpy`/`PIL`/`cv2`/`PySide6`；`nvidia-smi` | 固定 Python 3.12 环境 | **PASS**：前六者全部缺失（与报告 §1 一致），`PySide6 6.11.2` 在场，GPU `RTX 5070 Ti, 16303 MiB` | 本 Review 实际执行 |
| 保护框专项 | `rg -n protected_boxes experiments/TASK-018` | worktree | **FAIL（见 R-001）**：声明保护框未被 harness 消费 | 本 Review 实际执行 |
| 越界输出目录 | `--output-dir %TEMP%\...` | worktree | **FAIL（见 R-003）**：`ValueError` 退出码 1，残留部分 PNG | 本 Review 实际执行 |
| Mask 内部结构损伤 | — | — | **NOT_RUN** | 作者登记；本 Review 未补测 |
| 真实 OOM | — | — | **NOT_RUN** | 基线为 CPU 路径、峰值 ≤58 MB，无法触发 |
| 学习型路线质量/性能 | — | — | **BLOCKED** | 环境缺依赖与权重；本 Review 未独立复现，也未替代 |

### 三轴结论

- **Spec**：AC-1（路线可用范围比较）PASS，未能测试项如实保留未知；AC-2（固定五类样例 + 残字/损伤/耗时/峰值资源）**部分 PASS**——样例、残字代理、耗时、峰值 RSS、Mask 外保护已记录，Mask 内部结构损伤为 `NOT_RUN` 且理由充分；AC-3（Router/fallback/资源建议、保留参数与原始/最终 Mask、大型模型不设未验证默认）PASS；AC-4 落到本 Review 与后续集成，本文件即其证据。
- **Architecture**：本 Task 为 experiment，未写生产 `src/`、`tests/`、Schema、依赖清单、`AGENTS.md` 或其他 Task；实验协议纯标准库，图像 I/O 仅依赖已在场的 PySide6；未扩展到 TASK-019。边界干净。
- **Verification**：核心路径可独立复跑且确定性字段逐一吻合；两项 FAIL 均已在 Findings 登记且不影响已落盘数据的真实性。无 P0/P1，无关键验证缺失。

## 结论与复审

**`approved`**：固定 `reviewed_head=6c33e7f` 可交 Codex 集成。理由：交付在白名单内、无越界；最关键的诚实性要求成立——学习型路线 20 条 `BLOCKED` 且无任何伪造图、数字或 Hash；`edge-bleed` 全程标为非模型基线；`default_eligible` 仅两个基线为 true；实测确定性字段可精确复现。P2/P3 findings 属口径与健壮性，未推翻任何已落盘测量。

findings_disposition：R-001/R-004/R-005/R-006（报告部分）在集成收口提交内以文档口径修正收口，并由本 Reviewer 在同 head 复核；R-002/R-003/R-006（数据标签）/R-007 为 **deferred**，限制逐条登记，不随本 Task 一并关闭。

剩余风险与解锁条件：

- TASK-019 不得引用 `routes[*].runnable_here`/`blocked_reason` 作为环境探测证据（R-002）；需要环境事实时必须在具备依赖的机器上重跑并重新取证。
- **在依赖或权重可用的环境重跑本实验前必须先修 R-007**（未实现路线 fail-closed），否则补上真实探测会立刻把基线结果当成模型结果写盘——这正是 AC-1/AC-2 禁止的伪造证据。这是本 Task 后续任何重跑的**前置条件**。
- 学习型路线的质量/性能与“哪条路线更好”仍无证据；两条基线只是保底能力，不代表质量达标。
- AC-2 的“背景/边框损伤”目前只覆盖 Mask 外；Mask 内结构损伤仍 `NOT_RUN`。

本 Review 结论仅对该固定 head 有效；分支或 master 若再变化，不延用本批准。
