---
task_id: TASK-036
reviewer: Codex（非作者独立 Review）
author: DeepSeek Harness
base_commit: edfdcf2fe923c8c6e97d19377c61c4b8bba25abc
reviewed_head: c931db0
metadata_head: 5606221
decision: approved
---

# Review：TASK-036 设置输入校验与导出发布顺序硬化

Reviewer=Codex（**非作者**；作者=DeepSeek Harness）。固定 `base_commit=edfdcf2`（分支起点 `81ffd83` = 释放纯文档提交）、被审 `reviewed_head=c931db0`（元数据 `5606221`）。

**执行口径（`code-review` 技能 + 协作协议 §6）**：四轴 = **Standards** + **Spec**（技能两轴，分别报告、不合并、**不跨轴排名**）+ **Architecture** + **Verification**。

**并行偏差声明（§6 第 6 条）**：按已固化的环境结论（[STATUS](../STATUS.md) 2026-09-17「并行双轴子代理能力：本环境确认不可用」，累计三次派发均未取得执行槽），本次**不再重复派发**，径由 **两遍相互隔离**的检查（Standards / Spec 各自独立取材、独立成文）执行，并在此显式声明该偏差。**本报告的 Standards/Spec 两轴不应被解读为"已由独立 context 执行"。**

## 范围与依据

- 被审 diff：`git diff --name-status edfdcf2...5606221` = **22 路径**（`src/` **2**：`application/translation/inpaint/router.py`、`ui/viewmodels/export/viewmodel.py`；`tests/` 4；`doc/` 4；`verification/TASK-036/**` 12）。其中 `doc/STATUS.md`、`doc/tasks/README.md` 来自**释放提交 `81ffd83`（Codex 本人）**，非 Owner 改动。
- 需求来源（Spec）：[doc/tasks/TASK-036.md](../tasks/TASK-036.md) AC ①～⑦（承接 TASK-034 的 **R-02 / R-05 / R-07** 与 TASK-034 R-2 已确立的 fail-closed 原则）。
- **未审到**：真实模型/端点端到端（与本 Task 无关，仍 `NOT_RUN`）；`src/ui/**` 中除 `viewmodels/export/viewmodel.py` 之外的部分（未触碰）。

## Standards

轴状态：**`executed`**（隔离 pass 1：仓库记录标准 + Fowler baseline）。

- **`AGENTS.md` 范围与角色**：`src/` 改动恰为允许清单的 2 个文件；未改 Schema/migration、依赖清单、seam 本体、`AGENTS.md`、其他 Task；Reviewer 与作者分离。**无硬性违反**。
- **[09 协作协议](../09_COLLABORATION.md) §6**：作者未自批、未自标 `done`；**主动把三处需要裁决的口径（⑥ 两处扩大、④ 二次 `changed`、② 非序列拒绝）在 Handoff/Task 中列出并请裁**，未自行以"符合要求"收尾。**合规**。
- **[02 技术架构](../02_TECHNICAL_ARCHITECTURE_.md) 依赖方向**：`router.py` 仅新增 `collections.abc.Sequence` 导入（stdlib）；`viewmodel.py` 未新增导入。**无新增依赖、无方向倒置**。
- **工具已强制项**：跳过。**未新增 `skip`/`xfail`**（diff 正则核对 0 命中）。
- **Fowler smell baseline（判断项）**：
  - "**possible Duplicated Code**" — 校验逻辑被抽成两个具名辅助（`_route_sequence`、`_requirement_flags`）并各自带 docstring 说明解除的是哪条冻结，**不重复**；错误消息均由辅助函数统一构造。
  - "**possible Mysterious Name**" — 未命中；`_MISSING` 哨兵名字直白，且注释说明"absent ≠ null"的区别。
  - "**possible Primitive Obsession（残留，已知）**" — `RoutePolicy.allowed_routes` 仍是 `tuple[str, ...]`、`requirements` 仍是 `dict[str, bool]`，未引入值对象。属既有设计，本 Task 不做（引入类型会扩大范围）。

**本轴小结**：**0 项硬性违反记录在案标准**；0 项新增 baseline smell（2 项判断项均为已存在的设计取舍）。本轴最严重项 = 无。

## Spec

轴状态：**`executed`**（隔离 pass 2：AC 原文 → TASK-034 的 R-02/R-05/R-07 与 R-2 原则）。

- **① 缺失/部分完成：无。** AC ①～⑦ 逐条达成，均有可执行证据且经我独立复核（见 Verification）：
  - AC ①（R-02）：`_MISSING` 哨兵把"**键缺失**（返回**同一** `default` 对象）"与"**键存在但类型错误**（抛 typed error）"正确分开；两级消息可区分（`inpaint must be a mapping (got str)` vs `inpaint.route_policy must be a mapping (got NoneType)`）。装配与运行**各一条回归**（`test_runtime_assembly_reports_a_malformed_section_instead_of_ignoring_it`、`test_run_settings_non_mapping_inpaint_section_fails_the_step_loudly`）。
  - AC ②（R-07a）：`str`/`bytes`/`bytearray` 与非 `Sequence`（含 `set`）一律拒绝；非 `str` 元素拒绝；消息含字段名与实测类型。
  - AC ③（R-07b）：`requirements` 值必须真 `bool`；`"false"`/`0` 一律拒绝；非 mapping 由裸 `ValueError` 改为 typed error。
  - AC ④（R-05）：终态发布顺序改为 `_status → changed → 终态信号 → _running=False → changed`；导出用例等待**收回** `pump_until(not vm.running)`，**预算与断言未放宽**。
  - AC ⑤：**冻结解除已声明**，三处锁定旧行为的用例被**更新/改名而非静默删除**（详见下方核算）。
  - AC ⑥：12 类输入前后对照矩阵，**我用 base `edfdcf2` 导出树与 HEAD 各自独立重跑探针，逐条复现**。
  - AC ⑦：mandated 四套件 **274 passed / 0 skipped**、全仓 **737 passed / 6 skipped**（我独立复核，见 Verification）。
- **② scope creep：无。** 所有改动可归到 AC ①～⑤；未顺手处理 R-03/R-06。唯一超出 AC 字面的是两处**主动扩大的**行为变化（`route_policy: null`、`requirements` 非 mapping），作者已显式登记请裁 → 见 R-03（**accepted**）。
- **③ 看似实现但实现有误：无（1 处诊断路径缺陷，见 R-01）。** 合法语义（TASK-034 裁决 R-1～R-6）逐条保持：R-1 身份返回、R-3 `default.allowed_routes` 回落、R-4 `()`、R-5 缺省 `{}`、R-6 Option B（`color_route` 缺省/空白 → `None`，显式指定仍并入 allowed）。**路由判定算法零改动**（`decide_route`/`acceptable_routes`/`preferred_route_order`/`route_gate`/`RouterFeatures` 未出现在 diff 中）。

**本轴小结**：**AC ①～⑦ 全部达成**，0 缺失 / 0 scope creep / 0 实现有误；两处主动扩大行为已裁决 accepted（R-03）。本轴最严重项 = 无。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-01 | P3 | `tests/reading_export/test_qml_contract.py`（`safe_property` + 两处等待条件） | `safe_property` 在 `RuntimeError` 时返回**字符串**占位（`"<unavailable: …>"`），但它同时被用在**数值比较**里：`lambda: (safe_property(scroll, "contentHeight") or 0) > 0`。占位串为**非空**（真值）→ `str > int` 抛 **TypeError**，即把"RuntimeError 掩蔽真实 AssertionError"换成了"TypeError 掩蔽真实 AssertionError"，**只是换了一种掩蔽**。诊断辅助函数（`object_names`/`webtoon_save_diagnostics`）的用法**正确**，问题只在数值用法 | 我独立复现：`('<unavailable: Internal C++ object already deleted>' or 0) > 0` → `TypeError: '>' not supported between instances of 'str' and 'int'`。仅在等待期间对象被销毁时可达（不会造成假通过） | 数值场景改用返回数值兜底的辅助（如 `safe_number(obj, name, default=0)`）或比较前强制转换；**不影响本次批准** | **open（P3，非阻塞）** |
| R-02 | P3 | `verification/TASK-036/changed-paths.txt` | 该文件是 `1b0c89e` 时刻的**快照**，未含其后 `dcba248` 新增改动的 `tests/reading_export/test_qml_contract.py`；而 `git diff --name-status edfdcf2...5606221` 显示该文件确在 Owner 改动内（仍在 `tests/**` 允许范围，**无越界**）。影响：边界证据与最终 diff 不一致（同 TASK-032 R-01 / TASK-019 T-3 类） | 对比 `changed-paths.txt` 与 `git diff --name-status edfdcf2...5606221`；我逐条核对 Owner 路径全部落在允许范围 | 不篡改作者材料；由集成方在 `verification/TASK-036/integration-*.md` 以实测更正口径 | **fixed（集成记录更正）** |
| R-03 | P3 | `router.py::from_settings` | **作者主动扩大的两处行为变化，请裁**：① `route_policy: null` 由"静默回落默认"改为 typed error；② `requirements` 非 mapping 由裸 `ValueError` 改为 typed error | 我在 base `edfdcf2` 导出树实测：`route_policy=None` → `same_default=True`（静默）、`requirements='x'` → 裸 `ValueError`；HEAD 上两者均为 `ProviderInputError` | **两处均 accepted**：① TASK-034 裁决 R-2 的原文是"present but **non-mapping**"，`null` 属"存在且类型错误"，而 R-1 的身份返回只适用于**键缺失**——旧实现把"缺失"与"显式 null"混为一谈，正是 fail-closed 的漏洞；② 该输入此前**已经失败**（只是非类型化），改为 typed error 属改进而非新增拒绝 | **accepted** |
| R-04 | P3 | `viewmodel.py::_finish/_fail` | 新顺序下，**DirectConnection** 的终态信号处理函数在 emit 内运行，因此**读到 `running == True`**（属新契约的一部分，测试已断言 `in_call == [True]`）。影响：任何在终态信号处理函数内直接读 `running` 的消费者会看到旧值 | `export-publish-order.txt` 逐槽观测 + 我阅读 `changed = Signal()` 与 `running = Property(bool, _running, notify=changed)` | **accepted**：该顺序正是"`running == False` ⇒ 终态已发布"的必要代价；QML 走 auto/queued 投递，处理函数运行时标志已清。**记录为消费者须知**（后续若有直连消费者需注意） | **accepted（已记录）** |
| R-05 | P3 | `doc/handoffs/TASK-036-c931db0.md` | 内部数字不一致：Handoff 一处写"3 次失败且都在同一已登记用例"，另一处写"**两次**失败均未记为通过"（`registered-flaky-signature.md` 记为 3 次） | 对比 Handoff 两段与 `registered-flaky-signature.md` 的频率表 | 后续修订时统一为"3 次"；**纯文档精度，非阻塞** | **open（P3，非阻塞）** |

**裁决：已登记 flaky（R-06，非本 Task 引入）—— 本轮首次被"定性"**

- **本 Task 的附带价值**：该 flaky 此前在 [STATUS](../STATUS.md) 中以"**用例名未捕获**"登记（TASK-034 集成时 1/14）。本轮两次复现**都落在同一已登记用例** `test_reader_webtoon_swaps_in_vertical_viewer`，并给出**可读签名**：`pump(2s): iterations=98 elapsed_ms=2000 ok=False scroll_contentY=-0.0 saved_scroll_offset_y=0.0` —— `setProperty("contentY", 240.0)` 被 Flickable **夹回 0**（`StopAtBounds`），前一处断言只保证 `contentHeight > 0`、未保证内容可容纳 240，故**无值变化 → 无 `onContentYChanged` → 节流保存不启动**。这比此前"事件循环饥饿"的候选假设更贴近证据。
- **我的独立复核与诚实边界**：本轮我在 head 上跑 mandated 四套件**第 1 次即命中**（`1 failed, 273 passed`，失败点 `test_qml_contract.py:312`，与登记签名一致）；随后 head 全仓 **5/5** 绿（737/6）、mandated 1 次绿（274/0）。在**纯净 base `edfdcf2`** 上我跑 mandated 四套件 **8 次 0 命中**——**未能复现基线**，故我不能凭自己的小样本独立证成频率；**归属结论依据结构性证据**：(a) 该用例**在 TASK-036 之前**即由 TASK-017 R-007 / TASK-019 T-1 登记为 flaky；(b) 本 Task 对该测试文件的改动**仅为诊断稳健化**（无断言／预算／时序语义改动，diff 逐行核对）；(c) 本 Task 的产品码改动（inpaint 设置解析、导出 ViewModel）不在读者 QML 执行图上；(d) 作者的对照组在纯净 `edfdcf2` 上复现同一用例（1/6）。→ **不归因本 Task**，且**未记为通过**。
- **R-06 状态**：**仍 open**，但由"未定性"升级为"**已定性（含可读签名与最小修复候选）**"。最小候选：等 `contentHeight >= 240` 后再设 `contentY`，或设值后断言 `contentY == 240` 再等保存 —— 属后续切片，本 Task 明确不修。

## 验证

环境：Windows `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；全部 `-p no:cacheprovider`。**下列均为我独立重跑**。

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| mandated 四套件 | `pytest tests/providers tests/core tests/reading_export tests/editing -q -rs` | `c931db0` | **PASS（1 次命中 flaky、1 次全绿）**：绿时 **274 passed / 0 skipped**，exit 0；命中时 `1 failed, 273 passed`（= 同一已登记用例） | 本次重跑 ×2 |
| 全仓 | `pytest -q -rs -rf` | `c931db0` | **PASS** 5/5 次 `737 passed, 6 skipped`，exit 0；6 条 skip 全为既有 `tests/network` `openssl unavailable` | 本次重跑 ×5 |
| **输入矩阵（独立复现）** | 同一探针分别对 **base `edfdcf2` 导出树** 与 **HEAD** 运行 | 两棵树 | **PASS**：absent/inpaint 空/`route_policy={}`/`allowed=[edge-bleed]`/`allowed=[]`/`color_route=flux-fill`/`color_route=''`/`requirements={x:True}` 等合法输入 **before == after**（含 `same_default` 判定）；非法输入 before 静默或裸错 → after 全部 typed `ProviderInputError` 且消息含字段名 | 本次重跑（非引用作者日志） |
| **哨兵语义（提问①）** | 读 `_MISSING` 用法 + 探针断言身份 | `c931db0` | **PASS**：键缺失 → `same_default=True`（同一对象）；段/键类型错误 → typed error 且两级消息可区分 | 本次审查 |
| **发布顺序（提问④）** | 读 `_finish`/`_fail` + `changed`/`running` 属性接线 + `export-publish-order.txt` 逐槽观测 | `c931db0` | **PASS**：after = `changed(True) → changed(True) → 终态信号(True) → changed(False)`；`running = Property(bool, _running, notify=changed)` ⇒ 清标志后**必须**再发 `changed`，二次 emit **必需而非冗余** | 本次审查 |
| **冻结解除清单（提问⑤）** | diff 逐条核对改名/更新/删除 | `c931db0` | **PASS**：3 处旧行为用例**改名或更新**（非静默删除）——非 mapping 段→专用失败用例；裸字符串→两字段参数化拒绝用例；`bool("false")`→真 bool 拒绝用例。另新增合法输入保持用例 + 装配/运行两条 fail-loud 回归 | 本次审查 |
| 断言/跳过完整性 | diff 正则：`-.*assert` 逐条有对应替代；`+.*skip\|xfail` | `c931db0` | **PASS** 无断言丢失、**未新增 skip** | 本次审查 |
| **`safe_property` 数值用法（R-01）** | 直接构造 `RuntimeError` 后走 `(v or 0) > 0` | — | **FAIL（我新发现）** → `TypeError`（换一种掩蔽） | 本次重跑 |
| 边界 | `git diff --name-status edfdcf2...5606221` + 禁止路径正则 | `5606221` | **PASS** Owner 改动全在允许范围（2 个 `src/` + `tests/**` + `verification/TASK-036/**`）；`src/ui/**` 仅 `viewmodels/export/viewmodel.py`；越界 0 | 本次审查 |
| 真实端点/模型 | — | — | **NOT_RUN**（与本 Task 无关） | — |

**四轴结论（逐轴声明，不跨轴排名）**

- **Standards：`executed`** —— 0 硬性违反；0 新增 baseline smell（1 项既有设计取舍）。本轴最严重项 = 无。
- **Spec：`executed`** —— AC ①～⑦ 全部达成，0 缺失 / 0 scope creep / 0 实现有误；两处主动扩大行为 accepted。本轴最严重项 = 无。
- **Architecture：`executed`** —— 校验逻辑以具名辅助收敛在唯一入口内；`_MISSING` 正确表达"缺失 ≠ 显式 null"；发布顺序的选择（终态先发布、标志最后清且带信号）与 Qt 属性 notify 机制一致；路由判定算法与合法语义零改动；未触碰 Schema/依赖/seam。本轴最严重项 = R-04（P3，已记录的消费者须知）。
- **Verification：`executed`** —— 作者的输入矩阵、发布顺序、冻结清单、回归数字我**全部独立复核**（含独立重跑 base 与 HEAD 两侧探针）；另**新发现 1 处诊断路径缺陷（R-01）**与 1 处证据快照不一致（R-02）；已登记 flaky 首次**定性**并经结构性证据归属为非本 Task 引入。本轴最严重项 = R-01（P3）。

**P0/P1：0 项。** 无未解决 P0/P1，关键验证无缺失（唯一 `NOT_RUN` 与本 Task 无关）。

## 结论与复审

**decision = `approved`**。`reviewed_head=c931db0` 可交 Codex 集成。

理由：①AC ①～⑦ 逐条达成且有可执行证据，`route_policy` 设置解析现在"合法输入一字未变、非法输入一律 typed fail-loud"，与 TASK-034 R-2 已确立的原则一致；②R-05 的发布顺序缺陷被真正修好（终态先发布、标志最后清且带 notify），并且 TASK-034 的判别测试被**更新为锁定新不变量**（含检测器非空性检查），导出用例等待**收回自然等待**且未放宽任何断言；③`_MISSING` 哨兵把"缺失"与"显式 null"正确分开；④冻结解除完整透明、无断言丢失、未新增 skip；⑤范围合规；⑥附带把仓库级 flaky 从"用例名未捕获"升级为"已定性 + 最小修复候选"，且未记为通过、未归因本 Task。

**剩余风险**：R-01（诊断数值用法，P3，仅在等待期间对象销毁时可达，不会造成假通过）、R-05（Handoff 数字不一致，纯文档）、R-02（证据快照，集成时更正）；已登记 flaky 仍 open（已定性，待后续切片）。

复审追加新 head 与对应 findings disposition，不抹掉本记录。
