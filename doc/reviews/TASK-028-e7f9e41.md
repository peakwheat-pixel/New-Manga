---
task_id: TASK-028
reviewer: DeepSeek Harness
author: Codex
base_commit: 4a1df8fb5bc90e1542113c5ae3b15cb837146f79
reviewed_head: e7f9e410d6a5ebc06a0d0bc0494ee798d8286251
decision: approved
---

# Review：TASK-028 复审（e7f9e41）

本报告是 [首轮 Review](TASK-028-f9f2811.md)（decision=`changes_requested`）的复审，只覆盖 `4a1df8f..e7f9e41`。未修改 `src/`、`tests/`、UI、迁移、依赖或现有设计语义；本报告是 Reviewer 新增的唯一文件。

**环境**：worktree `G:/CODEX/New Manga.worktrees/TASK-028-deepseek-review`，分支 `agent/deepseek/TASK-028-review`。复审开始时该分支位于首轮报告提交 `52754ca`，因 `e7f9e41` 已包含该报告，`git merge` 为 **fast-forward**，工作区现直接反映被审 head。

## 范围与依据

**固定对象**：`base_commit = 4a1df8fb5bc90e1542113c5ae3b15cb837146f79`、`reviewed_head = e7f9e410d6a5ebc06a0d0bc0494ee798d8286251`。

**变更**：`4a1df8f..e7f9e41` = 7 路径、`+374/−12`，全部为文档（新增设计契约 178 行、TASK-028 85 行、首轮 Review 报告 94 行，修改 `00_INDEX`/`12_ROADMAP`/`STATUS`/`tasks/README`）。**修订增量 `f9f2811..e7f9e41`** = 4 文件、`+115/−11`：设计契约 +9/−4、`TASK-028.md` +15/−4、`STATUS.md` +8/−3，以及纳入首轮 Review 报告。**未改动 `src/`、`tests/`、UI、迁移或依赖**。

**依据**：首轮 Review 的 F-01～F-03；[TASK-002 最小契约](../contracts/TASK-002_MINIMUM_DATA_EXECUTION_CONTRACT.md) §2/§8/§10；D03 §10.2/§5.2；`doc/09_COLLABORATION.md` §4 状态定义。

## F-01～F-03 逐条 disposition

| ID | 原级别 | disposition | Reviewer 独立核验 |
|---|---|---|---|
| F-01 | **P1** | **fixed** | §3.2 现**只有一行** `tags(...)` 定义，且**不含** `UNIQUE(name)`：`tags(tag_id TEXT PRIMARY KEY, name TEXT NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL)`，后接"本设计不新增 `UNIQUE(name)` 数据库约束…`LibraryService` 已通过 `DuplicateTagName` 负责…重复检查"。独立检索确认：`git grep -n 'tags(tag_id' e7f9e41` 仅命中该定义行与紧随的 `book_tags(...)`（后者为关联表，非重复定义）；`UNIQUE(name)` 字样只出现在那句**解释**中，不再是表定义。首轮所指的第 72 行重复定义已删除，矛盾消除 |
| F-02 | P2 | **fixed** | **(a)** §3.1 新增 Page 状态值域："`review_state` 的非 NULL 值限定为 `unreviewed / needs_review / confirmed`，`overall_status` 的非 NULL 值限定为 `not_started / pending / running / completed / stale / failed / skipped / interrupted / cancelled`"。Reviewer 逐值对照 D03 §10.2 的 StageState 代码块（9 个值）**完全一致**，`review_state` 三值与契约 §2.2 的 ReviewState 一致。**(b)** §3.4 两处复合外键均补齐延迟属性：current 外键"与 TASK-006 的 current 外键同型，使用 `DEFERRABLE INITIALLY DEFERRED`，以支持首次 Revision 插入与 current pointer 更新在同一事务内完成"；`restored_from_revision_id` 外键"使用同型的 `DEFERRABLE INITIALLY DEFERRED` 约束"。与 `src/infrastructure/sqlite/schema.py` 中 `media_artifacts` 的既有写法一致，先例引用准确 |
| F-03 | P2 | **fixed** | §5 新增独立段落明确承接门槛："本设计不预留实现 Task 编号，也不直接释放实现。后续实现必须由 Codex 新建一个独立的 implementation Task；在该 Task 进入 `ready` 前，必须回填本 Task 与 `doc/tasks/README.md`、`doc/STATUS.md` 的承接编号，并明确新的 owner、reviewer、当前集成 master `base_commit`、branch/worktree、收紧后的 `allowed_paths`、禁止范围和逐项验收证据。未完成这项登记前，不得据本设计启动 ZCode 或修改 SQLite 源码。"该条把"由谁实现、何时登记、登记什么"全部写明，可执行性不再悬空 |

**三项全部 resolved，无 unresolved、无 regressed。**

## 本轮新 findings

| ID | 级别 | 文件/行 | 问题 | 影响 | 复现 | 建议 | disposition |
|---|---|---|---|---|---|---|---|
| F-04 | P2 | `doc/tasks/TASK-028.md:5`（`status: changes_requested`） | 修订提交 `e7f9e41` 已按 F-01～F-03 完成设计修订并"等待新 head 复审"，但 frontmatter 仍为 `changes_requested`。`doc/09_COLLABORATION.md` §4 明确："`changes_requested`：Reviewer 要求修订，**修订后回到 `in_review`**"；`STATUS.md` 同步写作"正在文档修订复审"，措辞同样滞后于"修订已完成" | 状态登记与协议及本 Task 自身"最近状态"叙述（"Codex 已按 F-01～F-03 修订设计，当前等待新 head 复审"）不一致，后续 Agent 依 frontmatter 判断阶段时会误读为"仍在等待作者修订" | `git show e7f9e41:doc/tasks/TASK-028.md` 的 frontmatter；对照 `09_COLLABORATION.md` §4 与 TASK-028.md 的"最近状态"段 | 将 `status` 改为 `in_review`（复审通过后再转 `approved`），并把 `STATUS.md` 的阶段/任务分派行同步为"设计复审中"；属元数据登记修正，不改任何设计语义 | open（非阻塞） |

P0 = 0，P1 = 0，P2 = 1（F-04），不阻塞设计批准。

## 本轮观察（不作为 finding）

- **契约 §5 的排版**：新增的承接门槛段落被插在引导句"后续实现 Task 的最小允许范围拟为："与随后的 `- src/infrastructure/sqlite/**` 列表之间，使引导句与列表被一段正文隔开。语义清晰、不产生歧义，仅建议后续把该段移到列表之后或改为引用块。
- **`TASK-028.md` 新增 `reviewed_head` 字段**：该字段不在项目 Task 模板的既有字段集中，且与正文"Review"段记录的 `reviewed_head=f9f2811` 重复。作为信息性记录无害，但若后续 Task 普遍采用会扩大模板口径，建议由 Codex 决定是否纳入模板。

## 验证

| 场景 | 命令或步骤 | 环境 / commit | 结果 | 证据 |
|---|---|---|---|---|
| 必需检查 | `git diff --check 4a1df8f e7f9e41` | Git 2.52.0 | **PASS，退出码 0** | 无输出 |
| 变更统计 | `git diff --stat 4a1df8f e7f9e41` | 同上 | PASS | 7 files, 374 insertions(+), 12 deletions(−) |
| 修订增量 | `git diff --stat f9f2811 e7f9e41` | 同上 | PASS | 4 files, 115 insertions(+), 11 deletions(−) |
| 范围与冻结 | `git diff --name-status 4a1df8f e7f9e41` | 同上 | PASS | 仅 `doc/`：设计契约、TASK-028、首轮报告、`00_INDEX`、`12_ROADMAP`、`STATUS`、`tasks/README`；`src/`、`tests/`、UI、迁移、依赖零改动 |
| F-01 复核 | `git grep -n 'tags(tag_id' e7f9e41`；检索 `UNIQUE(name)` | 同上 | **PASS** | 表定义唯一且无 `UNIQUE(name)`；该字样仅在解释句中 |
| F-02a 复核 | 阅读 §3.1 的值域句；对照 D03 §10.2 代码块与契约 §2.2 | 同上 | **PASS** | 9 个 `overall_status` 值与 D03 §10.2 逐值一致；3 个 `review_state` 值与契约一致 |
| F-02b 复核 | `git grep -n 'DEFERRABLE' e7f9e41 -- <契约>`；对照 `schema.py` 的 `media_artifacts` 外键 | 同上 | **PASS** | 两处（current、restored_from）均标注 `DEFERRABLE INITIALLY DEFERRED`，与 TASK-006 先例同型 |
| F-03 复核 | 阅读 §5 承接段落 | 同上 | **PASS** | 登记门槛、登记对象与"未登记不得启动实现"约束齐备 |
| 首轮结论未回退 | 对照 `TASK-028.md` 的 AC 勾选与"首轮 Review findings disposition"段 | 同上 | **PASS** | AC1~AC7 保持 `[x]`，AC8 保持 `[ ]`；F-01～F-03 的 disposition 段已登记且注明"待复审" |
| **SQLite 行为（v2 迁移、round-trip、重启、回滚、Region 原子提交、Artifact 回归）** | 未执行 | — | **NOT_RUN** | 本设计 Task 不实现；见下节 |
| **产品 AC 结果** | 未执行 | — | **NOT_RUN** | `doc/13_ACCEPTANCE_TRACEABILITY.md` 未被本轮改动，AC 结果不得据本设计更新 |

## 三轴结论

**Spec**：首轮 F-01 的内容矛盾已彻底消除，§3.2 只剩一份无 `UNIQUE(name)` 的定义，与 `LibraryService.DuplicateTagName` 的应用层检查及 TASK-007 已集成契约一致；F-02 补全的两类约束（Page 状态值域、Region 复合外键延迟性）经对照 D03 §10.2 与 TASK-006 先例确认准确；F-03 把实现承接登记写成可执行门槛。TASK-028 的 AC1~AC7 在文档层成立，AC8 正确保持未勾选。首轮已核实的现状证据（v1 单迁移、消费侧契约位置、`RegionEditingService` 两调用与同 ID upsert、`reading_order` 属 revision-owned state）未被本轮改动影响，仍然成立。

**Architecture**：本轮修订只收紧约束文字与登记要求，未改变任何冻结语义——共享连接 + 按消费契约拆分 Adapter、v1 不可编辑、复用现有 `open_database()`/`MigrationRunner`/Artifact commit 语义、拒绝独立库与巨型 Repository 的方向均保持原样。Region 复合外键与 TASK-006 同型后，两处 current 指针的 SQL 层实现方式一致，架构一致性反而增强。

**Verification**：`git diff --check 4a1df8f e7f9e41` 由 Reviewer 复跑通过（退出码 0）。三项修复均由 Reviewer 独立检索/对照确认，未依赖作者的 disposition 自述。

**重要边界**：本轮批准的对象是**设计文档**，不是任何 SQLite 实现。以下行为在本轮（及之前）**均未执行，一律 NOT_RUN**：v2 migration 的生成与应用、v1→v2 升级与迁移前备份/checksum/失败回滚、新 Schema 的 `TOO_NEW`/`query_only` 门控、Library/Page 的 SQLite round-trip 与重启、Region/RegionRevision 的 SQLite adapter 与 `commit_region_revision` 原子 seam、FK/软删除/排序与 `source_order`/`sort_order` 分离在 v2 上的行为、现有 Artifact safe commit 在 v2 数据库上的回归、以及「无 StepResultCandidate/Constraint/TM/Secret/二进制误入 SQLite」边界检查。**Reviewer 未批准任何实现语义**，也不把 TASK-006/007/008 的内存 fake 测试视为 SQLite 证据；`doc/13_ACCEPTANCE_TRACEABILITY.md` 的产品 AC 结果不因本设计更新。

## 结论与复审

**`e7f9e41` 的设计内容可通过，decision = approved。**

- 首轮 F-01（P1）与 F-02、F-03（P2）**全部 fixed**，经独立核验无回退、无新增问题。
- 本轮新增 F-04（P2）为元数据登记问题（`TASK-028.md` 的 `status` 应回到 `in_review`），**不阻塞设计批准**，可在 Codex 集成前的任一元数据提交中关闭。
- 本批准只针对设计文档的冻结内容；TASK-028 的 AC8（独立 Review 与 Codex 集成记录）需在 Codex 完成集成记录后才可勾选，Task 在此之前不得置 `done`，也不得释放 SQLite 实现 Task。

**集成时须由 Codex 完成**：按 §5 的新门槛新建独立 implementation Task，并在其进入 `ready` 前回填承接编号、owner、reviewer、当前 master `base_commit`、branch/worktree、收紧后的 `allowed_paths`、禁止范围与逐项验收证据；同时把 F-04 的 `status` 置为 `in_review`（集成后转 `approved`），回填 `STATUS.md` 与本 Task 的 Review 记录；`integration_commit` 仍为空。

**剩余风险**：设计冻结不等同于实现可用——v2 迁移、Region 原子提交、重启与回滚等全部 `NOT_RUN`，必须由后续实现 Task 以绑定 commit 的真实测试证据补齐；`managed_original_ref` 与 D03 `managed_original_artifact_id` 的命名差异仍待收敛（设计已要求不得静默合并）；R-201（`edited_confirmed` 继承）与 R-202（`reading_order` 归一化）仍为 TASK-008 遗留的产品语义问题，本设计未裁决。本报告事实仅适用于 `e7f9e41`。
