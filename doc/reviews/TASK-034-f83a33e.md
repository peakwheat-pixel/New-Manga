---
task_id: TASK-034
reviewer: Codex（非作者独立 Review）
author: DeepSeek Harness
base_commit: b34b27e2bc7c1dbd4c3b15a91f08e158d966f365
reviewed_head: f83a33e
metadata_head: 8ca99d4
decision: approved
---

# Review：TASK-034 测试与分层硬化（AC ②③④⑤ 交付部分）

Reviewer=Codex（**非作者**；作者=DeepSeek Harness）。固定 `base_commit=b34b27e`（分支起点 `c94c183` = 释放纯文档提交）、被审 `reviewed_head=f83a33e`（元数据 `8ca99d4`）。本 Review 只对被审 head 有效。

**审查范围声明**：本次只审 **AC ②③④⑤**。**AC ① 不在此列**——它的第 1 步（裁决请求）是交付物但第 2 步按 Task 要求冻结，故 AC ① 保持 **`BLOCKED`**，不得记为达成。AC ① 的裁决另见 [TASK-034-ac1-route-policy-ruling.md](TASK-034-ac1-route-policy-ruling.md)。

**并行偏差声明（§6 第 6 条）**：本环境仍未提供两条独立 sub-agent 线程（为 TASK-029 派发的双轴线程至今未取得执行槽）。按该条**兜底**执行：Standards 与 Spec 各做**一遍相互隔离**的检查（独立取材、独立成文，未用一遍通读充当两轴），并在此显式声明偏差。Standards 取材自 `AGENTS.md`／[09 协作协议](../09_COLLABORATION.md) §6／[02 技术架构](../02_TECHNICAL_ARCHITECTURE_.md)／Task 允许-禁止范围 + 全量 diff 与路径扫描；Spec 取材自 Task AC 原文 → TASK-019 Review T-2/T-4 → STATUS 用户裁决，且以**独立重跑**（非引用作者日志）为准。

## 范围与依据

- 被审 diff：`git diff --name-only c94c183 8ca99d4` = **22 路径**（`tests/core` 1、`tests/providers` 7、`tests/reading_export` 2、`doc/tasks/TASK-034.md`、`doc/handoffs/TASK-034-f83a33e.md`、`verification/TASK-034/**` 10），越界 **0**；`git diff --name-only b34b27e 8ca99d4 -- src/` **为空**（`src/` 零改动，符合裁决前冻结）。
- 受影响范围：`tests/core/test_architecture.py` 的 AST 守卫（含新增动态导入检测）、`tests/providers/**` 的共享替身迁移、`tests/reading_export/**` 两个已登记 flaky 用例的有界诊断。
- **未审到**：AC ① 第 2 步（未开始）；`src/**`（本次零改动）；真实端点/模型端到端（与本 Task 无关，仍 `NOT_RUN`）。

## Standards

轴状态：**`executed`**。

- **`AGENTS.md` 范围与角色**：改动全部落在允许路径（`tests/**`、`doc/tasks/TASK-034.md`、`doc/handoffs/TASK-034-*.md`、`verification/TASK-034/**`），越界 0；未改 Schema/migration、依赖清单、pipeline seam 本体、`AGENTS.md`、`src/**`、其他 Task；Reviewer 与作者分离（非作者）。作者在 `changed-paths.txt` 主动标注"`doc/STATUS.md`/`doc/tasks/README.md` 的 forbidden hits 来自 Codex 的释放提交 `c94c183`，不属于本人"——**我核对属实**（`git diff --name-only c94c183 8ca99d4` 不含这两个文件），这是诚实取证而非掩盖。**无硬性违反**。
- **[09 协作协议](../09_COLLABORATION.md) §6**：作者未自批、未自标 `done`（`status=in_review`）；AC ① 未被标成通过（作者在 Handoff 明写"**不可**把本次交付记为 AC ① 达成"）。**合规**。
- **[02 技术架构](../02_TECHNICAL_ARCHITECTURE_.md) 依赖方向**：未新增任何生产依赖；守卫从 `tests/providers` 迁入 `tests/core`（更贴近它保护的层级约定），且**只保留一份实现**——旧行前缀守卫被删除并留下指向新位置的 NOTE。**合规**。
- **工具已强制项**：跳过（无 formatter/lint/type-check 变化）；未新增 `skip`/`xfail`（我以 diff 正则核对：`+.*skip|xfail` 0 命中）。
- **Fowler smell baseline（判断项）**：命中 1 项 —— "**possible Duplicated Code（同类基础设施脆弱性在兄弟目录残留）**"：`tests/reading_export/test_qml_contract.py:12`、`test_viewmodels.py:13` 仍以裸 `conftest` 名导入 `requires_pyside6`，与 AC ④ 修掉的 `tests/providers` 是**同一根因、同一失效形态**。详见 R-01（我独立复现）。

**本轴小结**：**0 项硬性违反记录在案标准**；1 项 baseline 判断项（R-01，P2，**deferred 且已绑定强制后续切片**）。本轴最严重项 = R-01。

## Spec

轴状态：**`executed`**。来源顺序：本 Task AC 原文 → 其引用的 TASK-019 Review T-2/T-4 与 STATUS → 用户裁决。

- **① 来源要求但缺失/部分完成的项**：**无（AC ②③④⑤ 范围内）**。逐条：
  - **AC ②（T-4）**：守卫迁入 `tests/core/test_architecture.py`、复用并扩展 AST 版 `find_forbidden_imports` 覆盖字面量 `importlib.import_module`/`__import__`；旧守卫删除；判别力证据齐备（现树 0、历史树 `726baf5` 2、合成动态导入 新 1 / 旧 0）。**我独立复现**：对合成文件调用新守卫得 2 条命中、对现树 `src/application` 得 0 条 —— 与作者 `guard-discriminative.txt` 一致。既有 4 条单测**未被修改**（diff 仅新增），`tests/core` 15→18 全绿。
  - **AC ③（flaky）**：两个已登记用例均加有界诊断，无断言删除、无新增 skip、无预算放宽（webtoon 首处的等待预算仍是 5.0 s，与同文件既有的 `pump(window, 5.0, …)` 一致）。全仓 12 次串跑 0 失败，作者如实登记"**未复现**"而非宣称"已修复"。判据见 R-03/R-04。
  - **AC ④（TASK-035 R-02）**：共享替身移入唯一命名 `tests/providers/providers_helpers.py`，四处裸 `from conftest import …` 改为显式导入；`pytest tests/providers tests/editing` 与反向顺序**均 136 passed / 0 collection errors**（基线顺序 A 为 4 errors）。**我独立复现两种顺序均 136 passed**；`tests/providers` 由 111 降至 110 的原因是**守卫迁出**（`tests/core` +3），非测试丢失——我已逐条对账（见 R-02）。
  - **AC ⑤（回归与分列）**：`tests/providers tests/core tests/reading_export` **193 passed / 0 skipped**、`tests/core tests/storage tests/providers` **161 passed**、全仓 **682 passed / 6 skipped**（6 条 skip 全为既有 `tests/network` 的 `openssl unavailable`）。**全部由我独立重跑确认**，与作者一致。
- **② diff 中来源未要求的行为（scope creep）**：**无**。所有改动可归到 AC ②③④（测试文件与测试辅助），外加 AC ① 第 1 步的只读取证；未新增公开接口、未改生产行为。
- **③ 看似实现但实现有误的项**：**无**。AC ③ 的导出用例改动方向经我核对**是收紧而非放宽**：`src/ui/viewmodels/export/viewmodel.py` 的引文（`:380/389`、`:392/398`）经我逐行核对**完全准确**——`_finish`/`_fail` 确实先清 `running` 再发布状态与信号，故原来"等 `not vm.running`"会在半发布窗口提前返回；新等待以"断言所关心的终态信号"为条件，并配一条判别测试证明旧谓词在该状态即满足而新等待不满足。**AC ① 未被误记**：`src/` 零改动、AC ① 仍 `BLOCKED`。
- **唯一偏离字面文本之处**：AC ⑤ 字面写"passed 不减少"，而 `tests/providers` 该目录 passed 由 111 降为 110。这是 AC ② 迁移的**必然结果**，总量 +3，且无断言语义丢失 → 见 R-02（**accepted**）。

**本轴小结**：AC ②③④⑤ **全部达成**，0 缺失 / 0 scope creep / 0 实现有误；唯一字面偏离（AC ⑤ 的逐目录计数）已裁决为 accepted（R-02）。本轴最严重项 = 无（R-02 为 P3 并已 accepted）。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-01 | **P2** | `tests/reading_export/test_qml_contract.py:12`、`tests/reading_export/test_viewmodels.py:13`（`from conftest import requires_pyside6`；定义在 `tests/reading_export/conftest.py:28`） | **与 AC ④ 同根因、同失效形态的残留实例**（裸 `conftest` 模块名按 `sys.path` 顺序解析到别的目录的 conftest）。影响：只要把 `tests/reading_export` 排在**任何**自带 `conftest.py` 的目录**之前**，该调用即 2 个 collection errors、exit 2——例如 `pytest tests/reading_export tests/editing`。即 TASK-034 自身的 mandated 命令 `… tests/providers tests/core tests/reading_export` **仅因参数顺序恰好可用**；换序即红。属**既有**缺陷（非本切片引入）。 | 我独立复现：`tests/reading_export` + `editing`/`workbench`/`rendering`/`providers`/`knowledge`/`library` 六种伙伴目录**全部 exit 2 / 2 errors**，报错 `ImportError: cannot import name 'requires_pyside6' from 'conftest' (…\tests\editing\conftest.py)`；反向顺序 `pytest tests/editing tests/reading_export` = 91 passed。**基线对照**：`b34b27e` 纯净导出树上 `pytest tests/reading_export tests/editing` **同样 exit 2** → 既有问题 | 与 AC ④ 同法处理：把 `requires_pyside6` 移入/改从**唯一命名**模块（该目录已有 `reading_export_helpers.py`）并改两处显式导入。**不**要求在本切片返工 | **deferred，且绑定为强制项**：AC ① 第 2 步切片**必须**一并消除，并按本行证据给出**两种参数顺序 + 一个伙伴目录顺序**的对照（已写入 [AC ① 裁决](TASK-034-ac1-route-policy-ruling.md)「本裁决附带的强制项」第 2 条） |
| R-02 | P3 | `tests/providers`（111→110）+ 验收口径 AC ⑤ | AC ⑤ 字面要求"passed 不减少"，而该目录 passed 减 1。影响：字面口径与事实不符；实质上**无覆盖损失**（迁出守卫在 `tests/core` 以**更强**的 AST 形式重建，并 +2 条判别用例），总量 679→682 | `test-counts.txt` + 我独立对账：`tests/providers` −1（`test_application_layer_never_imports_infrastructure` 移至 `tests/core`）、`tests/core` +3、`tests/reading_export` +1，净 +3；`tests/core tests/storage tests/providers` 由 159（b34b27e）升为 161 | 接受现状；**不要**在 `tests/providers` 留一个转发薄用例——那与 AC ②"不得留下两套"直接冲突 | **accepted**（口径按"总量不减少 + 无断言语义丢失"解释，并在集成记录中写明） |
| R-03 | P3 | `tests/reading_export/test_qml_contract.py`（webtoon 用例首处等待） | 该处由"一次 `processEvents()` 后断言对象存在"改为"5 s 内有界等待后断言"。影响：**这是观察窗口的改变**，严格讲不是"纯诊断"；但断言语义（换页后出现纵向 Flickable）未变，且预算与该文件既有 `pump(window, 5.0, …)` 一致 | diff 逐行；作者在 Handoff 明写"首处对象等待改为同断言的有界等待"（**已主动披露**，未冒充无变化） | 接受为"观察窗口归一化到该文件既有的有界等待约定"；**不得**把它记为"完全无变化"——集成记录与 Handoff 须保留该披露 | **accepted**（口径见左；导出用例方向的改动经核对为**收紧**，不属本条） |
| R-04 | P3 | `tests/reading_export/test_viewmodels.py`（`test_export_outcome_wait_does_not_accept_the_half_published_state`） | 判别测试用**合成的 `HalfPublished` 假对象**证明"旧谓词在半发布状态即满足、新等待不满足"，并非让**真实** VM 处于该交错点执行。影响：对真实 VM 的"半发布窗口"结论仍依赖对源码的**行号引用**而非运行时证明 | 行号引用经我逐行核对**准确**（`viewmodel.py:380/389`、`:392/398`） | 可接受为诚实的能力边界；若日后要彻底证明，可在导出切片中注入可控的发布时序 | **open（非阻塞，如实登记）** |
| R-05 | P3 | `src/ui/viewmodels/export/viewmodel.py`（作者 N-1） | 生产侧"先清 `running`、后发布状态/信号"的发布顺序是导出类用例的机制根因；本 Task 只在测试侧规避 | 作者 Handoff N-1；行号经我核对准确 | **不在本 Task 修**（`src/ui/**` 在禁止范围）。登记为后续切片候选（如与 TASK-033 方向合并裁决） | **deferred**（登记） |
| R-06 | P3 | 两个已登记 flaky 的根因（作者 N-2） | webtoon 保存 flaky 在 12 次串跑中**未复现**，真实根因未定 | `flaky-repro-before.log`（681×6）、`flaky-repro-after.log`（682×6），全部 exit 0；我独立跑全仓亦 682/6 | 保持登记；诊断已就位（失败即输出轨迹），下次复现可直接定位 | **deferred**（登记，等待复现） |

**作者请求我裁决的四个问题（明确答复）**：

1. **AC ② 是否未改变既有 4 条单测语义、旧守卫确无第二份** → **是**。既有 4 条单测未改动（diff 仅新增），`tests/core` 全绿；旧行前缀守卫已删除，全仓 `rg` 无第二份实现。
2. **AC ③"等待代理量 → 等待终态信号"是否可接受为不放宽断言** → **可接受**，但须分两种情形记账（见 R-03）：导出用例方向是**收紧**（等待目标从"标志位"换成"断言所关心的终态信号"，并有判别测试）；webtoon 首处是**观察窗口归一化**（已披露）。两者都未删改断言语义。
3. **AC ④ helper 命名是否推广** → **采纳该范式**（唯一命名 helper + 显式导入，不再依赖裸 `conftest`），但**不**在本 Task 回溯重命名其它套件；唯一的残留实例就是 **R-01**，已绑定为强制后续项。
4. **AC ① R-6 选项** → **采纳 Option B**，并加重对齐要求（`DEFAULT_ROUTE_POLICY` 与 `RoutePolicy` 默认及 TASK-018 `default_eligible` 三者一致）。详见 [AC ① 裁决](TASK-034-ac1-route-policy-ruling.md)。

## 验证

环境：Windows `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；全部 `-p no:cacheprovider`。**下列数字均为我独立重跑所得**。

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| mandated 三套件 | `pytest tests/providers tests/core tests/reading_export -q -rs` | `8ca99d4` | **PASS** 193 passed / 0 skipped，exit 0 | 本次重跑 |
| AC ④ 顺序 A | `pytest tests/providers tests/editing -q` | `8ca99d4` | **PASS** 136 passed，exit 0 | 本次重跑 |
| AC ④ 顺序 B | `pytest tests/editing tests/providers -q` | `8ca99d4` | **PASS** 136 passed，exit 0 | 本次重跑 |
| 全仓 | `pytest -q -rs` | `8ca99d4` | **PASS** 682 passed / 6 skipped，exit 0；6 条 skip 全为既有 `tests/network` `openssl unavailable` | 本次重跑 |
| AC ② 判别力（独立复现） | 直接调用 `tests/core` 的 `find_forbidden_imports`：合成含 `importlib.import_module("infrastructure…")`/`__import__("infrastructure…")` 的文件 vs 现树 `src/application` | `8ca99d4` | **PASS** 合成 2 命中、现树 0 命中 | 本次重跑 |
| AC ③ 代码判据 | 逐行核对 `src/ui/viewmodels/export/viewmodel.py` 的发布顺序与作者引文 | `8ca99d4` | **PASS** 引文 `:380/389`、`:392/398` 准确 | 本次审查 |
| 断言/跳过完整性 | diff 正则：删除的 `assert` 逐条有对应替代；`+.*skip\|xfail` 0 命中；`+/-def test_` 对账 | `8ca99d4` | **PASS** 无断言丢失、无新增 skip；+3 测试净增 | 本次审查 |
| 边界与空白 | `git diff --name-only c94c183 8ca99d4`；`git diff --name-only b34b27e 8ca99d4 -- src/`；`git diff --check b34b27e..8ca99d4` | `8ca99d4` | **PASS** 22 路径全在允许范围、`src/` 空、`--check` exit 0 | 本次审查 |
| **R-01（我新发现）** | `pytest tests/reading_export <partner> -q` ×6 伙伴目录 + 反向顺序 + 基线纯净树对照 | `8ca99d4` / `b34b27e` 导出树 | **FAIL（既有缺陷）** 六种伙伴全部 exit 2 / 2 collection errors；反向顺序 91 passed；基线同样 exit 2 | 见 R-01 |
| 真实端点/模型端到端 | — | — | **NOT_RUN**（与本 Task 无关，不因本 Review 改变） | — |

**四轴结论（逐轴声明，不跨轴排名）**

- **Standards：`executed`** —— 0 项硬性违反记录在案标准；1 项 Fowler baseline 判断项（R-01）。本轴最严重项 = R-01（P2）。
- **Spec：`executed`** —— AC ②③④⑤ 逐条达成，0 缺失 / 0 scope creep / 0 实现有误；唯一字面偏离（AC ⑤ 逐目录计数）已 accepted。本轴最严重项 = 无。
- **Architecture：`executed`** —— 守卫**合并为唯一 AST 实现**并落在与其保护对象匹配的位置；测试辅助模块采用唯一命名范式；未新增依赖、未引入反向依赖、未触碰生产代码。本轴最严重项 = 无。
- **Verification：`executed`** —— 作者的三项核心数字（193/0、136/136、682/6）我全部独立复现；另独立复现判别力探针、核对行号引文与路径边界；**并发现 1 项作者未覆盖的同根因残留（R-01）**。本轴最严重项 = R-01（P2）。

**P0/P1：0 项**。唯一 P2（R-01）为**既有**缺陷、不阻塞本次批准，但已**绑定为强制后续项**（不可选、须在 AC ① 第 2 步切片内关闭）。

## 结论与复审

**decision = `approved`**（就 AC ②③④⑤ 的交付部分）。`reviewed_head=f83a33e` 可交 Codex 集成。

理由：①AC ②③④⑤ 均有可执行证据且经我独立复现；②AC ② 把"两套守卫"收敛为唯一 AST 实现，判别力在修前/修后双向成立；③AC ③ 未放宽任何断言语义（导出方向实为收紧、webtoon 方向为已披露的窗口归一化）；④AC ④ 按其字面范围完全达成且顺序无关性已证明；⑤范围合规、`src/` 零改动、AC ① 未被误记为通过。

**剩余风险**：R-01（同根因残留，已绑定强制后续项）、R-03/R-04（口径与证明强度的诚实边界）、R-05/R-06（生产侧发布顺序与未复现 flaky，均已登记）。

**本次批准不包含 AC ①**：AC ① 仍为 `BLOCKED`（第 2 步未开始）。裁决已出（[TASK-034-ac1-route-policy-ruling.md](TASK-034-ac1-route-policy-ruling.md)），第 2 步切片完成后须再次走非作者 Review 与集成；**在 AC ① 关闭前 TASK-034 不得标记 `done`**。

复审追加新 head 与对应 findings disposition，不抹掉本记录。
