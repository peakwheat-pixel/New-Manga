---
task_id: T1.3.1 / TASK-057
reviewer: Codex（独立非作者 Reviewer）
author: ZCode（固定交付）/ Codex（Rebaseline Integrator）
base_commit: 676319bb1b9e9682b13402c322fded43bbaa90c8
reviewed_head: 8318b64b73f84898f1122b459ebb15506e2b67db
decision: changes_requested
---

# Review：T1.3.1 / TASK-057 Rebaseline Integration

## 范围与依据

本轮固定审查 `676319bb1b9e9682b13402c322fded43bbaa90c8..8318b64b73f84898f1122b459ebb15506e2b67db`。已读取 `AGENTS.md`、`doc/REBASELINE_PLAN.md` T1.3.1、`doc/STATUS.md`、`doc/tasks/TASK-057.md`、历史 Review `doc/reviews/TASK-057-b0e4cb1.md`、集成 Handoff `doc/handoffs/T1.3.1-TASK-057-integration.md`、相关架构/非功能条款及完整 diff；检查了 backup/restore、restore admission、Worker 生命周期、ThreadRoutedConnection 以及全部新增测试和证据。

固定交付 `29546f7` 与历史 review head `257ee5b` 都是 reviewed head 的祖先；merge `3087975` 的父提交为 activation base + fixed delivery，merge `1433e02` 纳入 review 分支。`f92530e..8318b64` 仅增加 Task/Handoff 与验证证据，未在证据采集后改动产品代码。工作树原有 `?? .superpowers/` 未触碰。

## Standards（executed）

restore admission 的结构符合既定三件式约束：`RunController.start()` 是唯一 Worker 出生点，controller latch 是最终防线；`beginRestore()` 的重入拒绝关闭 R-008；`continueRun`、restart、retry 的当前实现均在 control/plan 写之前返回，`abandonRun` 未被该条件误挡。连接所有权、退出排空与私有 `_current()` 过渡适配和固定交付一致；未发现新增 skip/xfail、Schema migration、依赖、QML 或 AGENTS 改动。

但 `AGENTS.md` 的源文件/恢复安全边界、TASK-057 AC①③④及 D07 §49/§80 没有在 backup service 的信任边界成立，详见 R-001～R-004。

## Spec（executed）

R-008/R-009 的实现顺序成立，18 项 gate/backup 用例、28 项相邻集成及对抗 probe 均通过；全仓回归也通过。阻断点在固定 delivery 本体：备份完成前未执行 read-back、失败会留下半成品且不是 typed failure、restore ID 可逃逸受控根目录、较新 schema 可覆盖当前 live DB。另有 AC② 明写的 current/pinned/translated 指针一致性断言并未实际存在，测试注释与 SQL fixture 不一致。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态/修订 commit |
|---|---|---|---|---|---|---|
| R-001 | P1 | `src/infrastructure/sqlite/backup.py:43-99`；`tests/storage/test_backup_restore.py:95-110` | `create_backup()` 写 DB、sidecar 和 `completed` 记录后直接返回，从未调用 `verify_backup_file()`；测试是在 service 返回后自行调用 verifier，无法证明 AC①“完成后校验可读”。任何 backup API/文件系统产生的不可读但未立即抛错的产物都可被登记完成。 | Reviewer 将模块级 `verify_backup_file` 替换为必抛函数后调用 `create_backup()`：`CREATE_READBACK_CALLS=0 STATUS=completed RESULT=ACCEPTED_WITHOUT_VERIFY`。 | 在写 `completed` 记录/返回 ID 前由 service 自己执行 read-back；补一条 verifier 失败时不得成功/不得登记 completed 的判别测试。 | open |
| R-002 | P1 | `src/infrastructure/sqlite/backup.py:52-99,211-278` | 备份直接写最终 `.db`；sidecar 或 ledger 失败时没有清理/原子发布，且文件系统错误以原始异常外泄。违反 AC③“异常时不留半成品（原子/可回滚）”和 AC④ typed failure。 | 注入 `write_sidecar -> OSError`：`ERROR_TYPE=OSError DB_FILES_LEFT=1 RECORD_ROWS=0 RESULT=HALF_ARTIFACT`。 | 在同一受控根内写临时 DB/manifest、校验后原子发布；所有失败清理临时/最终半成品并映射为稳定 typed code。补 sidecar/ledger/backup 失败测试。 | open |
| R-003 | P1 | `src/infrastructure/sqlite/backup.py:125-156` | 未约束 `backup_id` 形状，也未在解析后验证 `target` 仍位于 `_backup_root`；record 缺失时 sidecar fallback 接受 `../`。调用 `restore_backup('../escaped')` 可从 backup root 外选择 DB 并覆写 live 库，违反 AC③“路径必须落在受控范围内”。 | Reviewer 把一份合法备份移到 backup root 的父目录后调用上述 ID：`PATH_ESCAPE_RESTORED_FROM='../escaped' OUTSIDE_ROOT=True RESULT=ACCEPTED`。 | backup ID 只接受生成器同形的 canonical token（例如严格 hex UUID），并对 resolved target 做 safe-root containment；非法 ID 返回 typed `BACKUP_UNKNOWN`/专用 code。补 traversal 与绝对路径用例。 | open |
| R-004 | P1 | `src/infrastructure/sqlite/backup.py:144-190`；D07 §49、§80 | restore 只读取目标库的 schema version，覆盖后又把 live version 与同一目标 version 放进 report；没有与当前应用支持的最高版本比较。完整、哈希正确的较新库会把当前 writable facade 覆盖为不受支持 schema，绕过 `open_database()` 对 newer schema 的拒绝/只读门。 | Reviewer 构造 hash/sidecar/record 都一致的 schema v999 备份；当前 `latest_known=3`，结果 `RESTORED_SCHEMA=999 REPORT=999 RESULT=ACCEPTED`。 | 在任何 live write 前把 backup schema 与当前支持版本比较；较新版本 typed 拒绝且 live DB 保持不变。sidecar/record/DB schema 三者也应交叉校验。 | open |
| R-005 | P1 | `tests/storage/test_backup_restore.py:55-81,133-167`；`src/infrastructure/sqlite/backup.py:192-207` | AC②要求恢复后对 current/pinned/translated 指针与 Managed Copy 一致性有断言；fixture 只创建 `original` artifact，`media_artifacts.current_revision_id` 保持 NULL，仅插入一条 `is_pinned=1` revision。所谓“pointer moves”实际只是新增第二个独立 page/revision，测试只断言 revision 数量与文件存在；service report 也只检查所有 revision 的 `managed_path`。因此明确 AC 没有持久、可判别证据。 | 阅读 fixture INSERT 与断言：没有 current pointer 更新、没有 translated artifact/revision、没有恢复前后 pointer 值比较。 | 构造 original + translated revisions，设置并漂移 current/pinned 指针；restore 后逐值断言回到 backup point，且各被引用 Managed Copy 文件存在。 | open |
| R-006 | P2 | `tests/workbench/test_restore_gate.py:163-199` | R-009 当前实现顺序正确，但 restart/retry 用例把 `control_run` / `retry_failed_targets` 替换为无副作用 lambda，且没有调用计数。若未来 gate 退到这些调用之后、赋值/plan 之前，两例仍会通过，无法持续证明“任何 control/plan 写之前拒绝”。continue 用例走真实状态变更，判别力较强。 | 静态对照 stub 与断言；两条 stub 被调用也不会改原 run/failed target，测试未断言 `call_count == 0`。 | 用 spy/mock 明确断言 latched 时 `control_run`、`retry_failed_targets`、`plan_run` 均零调用；保留当前状态断言。另补一个持久测试固定 `abandonRun` 在 latch 下仍可执行。 | open（非当前行为缺陷） |
| R-007 | P2 | 固定 diff 中 13 个 `verification/TASK-057/review-*` 历史日志 | 用户指定的全范围 `git diff --check base head` 返回 2；228 条输出来自原样合入的历史 raw log（CRLF/尾随空格）。产品源码/测试子范围与本轮 integration artifact 子范围均干净，因此不影响执行语义，但固定范围不能声明 diff-check PASS。 | `FULL_DIFF_CHECK_EXIT=2`；`git diff --check base head -- src tests` 为 0；`git diff --check 3fa7958 head -- doc/tasks/TASK-057.md doc/handoffs/T1.3.1-TASK-057-integration.md verification/TASK-057/rebaseline-integration` 为 0。 | Codex 决定 raw evidence 是否按证据豁免；不要无说明地改写历史日志。若项目要求全范围 hygiene gate，另做保留原始字节证据的迁移/归档方案。 | open（证据卫生，非产品阻断） |

## 验证

统一环境：Windows PowerShell、`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3）、`PYTHONPATH=src`（probe 按需含 `tests`）、`PYTHONDONTWRITEBYTECODE=1`、未设置 `QT_QPA_PLATFORM`，检出 `8318b64b73f84898f1122b459ebb15506e2b67db`。Reviewer probe 仅写仓库外临时目录。

| 场景 | 命令或手工步骤 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| targeted 18 | `python -m pytest tests/storage/test_backup_restore.py tests/workbench/test_restore_gate.py -q -p no:cacheprovider -rs` | 上述环境 / `8318b64` | PASS：18 passed / exit 0 | 本轮独立运行，2.28s |
| R-008/R-009 chokepoint | `python verification/TASK-057/review-29546f7/t057_chokepoint_probe.py <worktree>` | 同上 | PASS / exit 0：nested gate 被拒、outer latch 保持；paused run 未半写入、无 Worker 出生 | 本轮独立运行输出 H1/H2/H3 |
| abandon 不被误挡 | Reviewer 临时 probe：latch 内对真实 INTERRUPTED run 调 `abandonRun()` | 同上 | PASS / exit 0：run 变为 `cancelled`，latch 仍 closed，error 为空 | `ABANDON_STATUS=cancelled LATCH_STILL_CLOSED=True ERROR=''` |
| 相关集成 28 | targeted 18 + connection ownership/eviction + run-thread e2e | 同上 | PASS：28 passed / exit 0 | 本轮独立运行，1.72s |
| 全仓回归 | `python -m pytest tests -q -p no:cacheprovider -rs` | 同上 | PASS：964 passed + 6 skipped / exit 0；6 条均为既有 `openssl unavailable`，1 个第三方 deprecation warning | 本轮独立运行，52.08s |
| 作者证据审计 | 检查 `verification/TASK-057/rebaseline-integration/*f92530e*` | 已提交 `f92530e` | PASS：targeted 18、integration 28、probe exit 0、5× full suite 均为 970 collected = 964 passed + 6 skipped；每份 HEAD=`f92530e` 且 SOURCE_TREE_STATE 为空 | 已入库日志；`8318b64` 只在其后增加证据/文档 |
| create read-back 对抗 probe | monkeypatch verifier 为必抛后调用 `create_backup` | `8318b64`，仓外 temp | FAIL：verifier 0 次调用，record=`completed` | R-001 输出 |
| 半成品/typed failure probe | monkeypatch sidecar 写入抛 `OSError` | 同上 | FAIL：残留 1 个 `.db`、0 ledger row、原始 `OSError` | R-002 输出 |
| 路径边界 probe | 合法 DB/sidecar 移到父目录后 `restore_backup('../escaped')` | 同上 | FAIL：越界 DB 被接受并恢复 | R-003 输出 |
| newer schema probe | 构造 hash/sidecar/record 一致的 schema v999 备份并 restore | 同上；latest known=3 | FAIL：live schema 被覆盖为 999 | R-004 输出 |
| skip/xfail 与 task escape | 审查测试 diff、允许/禁止路径、merge parents、call sites | `676319b..8318b64` | PASS：无新增 skip/xfail；未改 Schema migration/依赖/QML/AGENTS/STATUS/其他 Task；历史 connection/workbench 改动均在当前 Task 白名单及已记录的 Codex 追认范围 | `git diff --name-status`、`git log`、`rg` |
| diff check | `git diff --check 676319b 8318b64` | 固定范围 | FAIL / exit 2：仅历史 raw logs；源码/测试及本轮 integration artifact 子范围各自 PASS | R-007 |

Architecture：**changes_requested**。R-008、R-009、Worker drain、连接所有权与 admission choke point 本身成立；生产 restore wiring 仍是 Handoff 已声明的 NOT_RUN/后续装配边界。但 R-003/R-004 位于 backup service 自身的信任边界，允许越界选择备份和不受支持 schema 覆盖 live DB。

Verification：**changes_requested**。正常路径回归全部绿，不能覆盖 R-001～R-005 的对抗路径与缺失 AC；Reviewer probes 已实际复现四项失败。全范围 diff-check 另有非产品 P2 边界。

## 结论与复审

**Changes Requested。** 固定 reviewed head `8318b64b73f84898f1122b459ebb15506e2b67db` 不应合入 master。R-001～R-005 是未闭合 P1：AC① read-back、AC③ 原子/受控路径、AC④ typed failure、D07 newer-schema 安全门及 AC② 指针证据均未满足。R-008/R-009 修复本身通过，不需要回退；`abandonRun` 已实测保持可用。

推荐下一步：Codex 在同一 T1.3.1/TASK-057 授权范围内安排返修，只修改 backup service、对应 storage/gate 测试与本 Task 的验证/Handoff；先为 R-001～R-006 写能在 `8318b64` 上按预期失败的判别用例，再修复并提交新 delivery head。返修后至少重跑 targeted、路径/schema/失败注入 probes、connection/Worker 集成、全仓回归，并由非作者对新固定 head 复审；不要修改本 Review 的历史结论。
