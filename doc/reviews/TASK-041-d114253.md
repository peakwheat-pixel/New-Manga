---
task_id: TASK-041
reviewer: Codex（非作者外部 Review）
author: ZCode
base_commit: 6ea3dd39d1bb2b61c49af2ead070f40b83a9b9b4
reviewed_head: d11425376fe482f782c4efc0ae08ae42a930b6f3
implementation_head: 3230f45e8d7b4a01a892aea112514cf3c1e46557
decision: changes_requested
---

# Review：TASK-041 picture-MOBI 导入（TASK-023 遗留①）

Reviewer=Codex（**非作者**；作者=ZCode）。固定 `base_commit=6ea3dd3`、被审 `reviewed_head=d114253`（实现 `3230f45`；分支 head `d114253`）。

**decision = `changes_requested`** —— **R-001（P1）**：适配器的页面收集/排序对真实容器不成立（重复或乱序页），且该路径**经 UI 可达**。按 §6.8「P0/P1 未解决不可批准」，**本 head 不集成**，退回作者修订后复审。

**执行口径（用户 `[$code-review]` 调用）**：四轴 = Standards / Spec / Architecture / Verification，逐轴声明、分别报告、**不跨轴排名**。技能第 4 步要求 Standards/Spec 各由**并行 sub-agent** 运行；**本会话无 sub-agent 通道**（与 2026-09-18 用户取消双轴强制的原因一致），故两轴由同一 Reviewer 单遍执行，**偏差在此声明**（技能要求如实声明，不生造"两遍隔离"）。技能第 1 步（issue tracker）不适用：本仓库无 tracker、`docs/agents/issue-tracker.md` 不存在，未运行其 `/setup-matt-pocock-skills`；按协议 §6.2 顺序改取 Task 文件 AC → D01～D08/`doc/contracts/**` → STATUS 用户裁决。

## 范围与依据

- **被审 diff = `6ea3dd3..d114253`**（24 文件 `+1132/−19`）。生产面 4 文件：`requirements.txt`（+1 行）、`src/application/importing/documents/service.py`（±38）、`src/infrastructure/importing.py`（+149）、`src/bootstrap/app.py`（±14）；测试 `tests/import_formats/test_mobi_import.py`（+343，9 例）；证据 `verification/TASK-041/**`（16 文件）；文档 `doc/tasks/TASK-041.md`、`doc/handoffs/TASK-041-3230f45.md`、`doc/STATUS.md`（+1 台账行）。
- 受影响调用链（实际读过）：`bookshelf/viewmodel.py:370-392`（`@Slot(str, list) importDocumentsFromUrls`）→ `ImportDocumentsUseCase.import_documents`（`service.py:115-275`）→ `MobiDocumentRaster.open` → `mobi.extract`（第三方内嵌 KindleUnpack）→ `_MobiDocumentHandle.render_page` → `ManagedCopyStoreAdapter.store_original` + `ImportPageSink.add_page`。
- **未审到**：真实商业 MOBI/AZW3 容器、真实 DRM 容器（无获取渠道；作者已如实登记为未验证项）；真机 GUI 点击面（本切片无 QML 改动）。
- 依赖绑定源码（只读核对）：`site-packages/mobi/{extract.py,kindleunpack.py,unpack_structure.py,mobi_cover.py}`。

## Standards（executed）

轴状态 **`executed`**。

- **白名单**：`src/`+`tests/` 改动恰为上述 4+1 文件，全在 Task 允许路径内（`requirements.txt` 亦为授权项）；`doc/STATUS.md` 仅 +1 台账行；**越界 0**；未碰 Schema/migration、pipeline seam 本体、路由判定/SFX 语义、`AGENTS.md`、其他 Task、他人 worktree ✓
- **分层（[02 技术架构](../02_TECHNICAL_ARCHITECTURE_.md) §架构方向/§16、AGENTS.md）**：`rg "import mobi|from mobi" src/` 仅 2 处、均在 `src/infrastructure/importing.py`（适配器方法内 lazy import）；`rg "from infrastructure|import infrastructure" src/application/` **0 命中** ⇒ 端口/适配器纪律成立 ✓（AC ⑤）
- **依赖纪律**：`requirements.txt` **净 +1 行 `mobi==0.4.1`**，无顺带升级 ✓；3 个传递依赖（loguru/standard-imghdr/win32_setctime，纯 Python）在 Handoff 如实声明、未写入 requirements ✓
- **fail-closed 纪律**：`_is_mobi` 为固定偏移字节判定（不解析、不猜）；DRM 判定在**任何解析之前**；临时 `.mobi` 有 `finally` unlink、提取树 owner = handle 且 `service.py:259-260` `finally: handle.close()` ✓；无 `except Exception: pass` 式吞错（仅既有先例的 close best-effort）✓
- **smell baseline（判断项，均不阻塞）**：① possible Duplicated Code —— PDB 魔数/偏移在 `service.py:68-75` 与 `importing.py:352-364` 各写一份（测试构造第三份 `_pack_pdb`）；② possible Mysterious Name —— `cover`/`HDimage` 的页面语义未在收集处显式表达（并入 R-001）。
- **口径诚实性**：Handoff 称证据口径为「PowerShell + `TASK-012-py312`」，但其 5 份全仓日志为 `828 passed / 0 skipped`；我在**同一 venv 的 PowerShell** 下测得 `822 passed / 6 skipped`（6 条=既有 `tests/network` `openssl unavailable`）——**总数一致（822+6=828=819+9）**、无屏蔽、无新增 skip，但「同口径」表述不成立 → **R-006**。

**本轴小结**：0 项硬性违反；2 项 baseline 判断项 + 1 项口径表述问题（R-006，P3）。本轴最严重项 = R-006。

## Spec（executed）

轴状态 **`executed`**。来源=`doc/tasks/TASK-041.md` AC ①～⑦（原始文本）+ 其引用的既有用例路径与 STATUS 用户裁决（MOBI 依赖已由用户批准）。

| AC | 判定 | 依据 / 我的独立验证 |
|---|---|---|
| ① 依赖与留证 | **PASS** | diff 仅 +1 行；装前/装后各 `819/0` exit 0（作者，`6ea3dd3` 同 venv 同代码状态）；**我独立静态核对** `mobi` 包 `socket/urllib/http/urlopen/requests` **0 命中**；venv 实测 `mobi==0.4.1` + 3 传递依赖在装 |
| ② 解析→带图页面 | **FAIL（集合/排序口径）** | 合成 KF7 容器端到端 9 例通过，但**页面清单 = 提取树全量图片文件名按字典序** ⇒ 含 cover/HD/KF8 树的真实容器会**重复或乱序**。**探针复现**：注入含 `cover00001`/`HDimage00004` 的树 → `page_count=4`（逻辑页仅 2），顺序非 record 序（R-001）。Task 文件、STATUS 行、Handoff 三处写的「文件名=容器 record 编号 ⇒ 文件名序即容器页序」**对真实容器不成立** |
| ③ fail-closed 矩阵 | **PARTIAL** | DRM/截断/text-only/坏图页/未注入/非 MOBI 六行**成立**，且我另证**受控副本零残留**（见 Verification）；但「依赖缺失（readiness）」只由 double 覆盖、适配器自身分支零覆盖（R-003）；「坏图页→后续页 pending、已成功页保留」只覆盖单页文档（R-004） |
| ④ 不回归 PDF | **PASS** | 本切片未触碰 `test_document_import.py`（diff 可证）；定向 `tests/import_formats tests/library` **65 passed / 0 skipped** exit 0（我复跑）；全仓 `822/6`（我复跑；基线 `813/6` ⇒ +9 恰为新例） |
| ⑤ 可替换性 | **PASS（声明面）/ 见 R-002** | 端口之后、方法内 lazy import、替换路径与「上游停更」对策写入 Handoff；但 `_is_mobi` 与类 docstring 声称 **AZW3**，实测仅 KF7 picture 容器覆盖，而 KF8 树正是 R-001 的触发面 |
| ⑥ 判别力 + 回归 | **PASS** | **我独立复现判别力**（自建临时树：`git archive 6ea3dd3 src` + 本切片测试）→ `ImportError: cannot import name 'MobiDocumentRaster'`，**exit 2**；全仓串跑作者 5 次 + 我 2 次逐次一致 |
| ⑦ Handoff / 取证 / 非作者 Review | **PASS（本文件）** | Handoff 完整、证据入库、未验证项如实登记；AC 勾选见下 |

**AC 勾选争议（Spec 轴）**：作者把 AC ②③ 标 `[x]`。按本 Review：**AC ② 应回到未完成**（集合/排序对真实容器不成立），**AC ③ 应记 PARTIAL**（两行未触达真实分支）。

**scope creep**：无。`mobi_raster` 为**可选注入**，`None` 时与 TASK-023 行为逐字节一致（`test_without_injected_mobi_raster_mobi_stays_unsupported` 锁定）。

**本轴小结**：1 项 FAIL（AC ②，并入 R-001）、1 项 PARTIAL（AC ③ → R-003/R-004）、1 项声明越界（R-002）。本轴最严重项 = AC ②。

## Architecture（executed）

轴状态 **`executed`**。

- **依赖方向/端口面**：复用 `DocumentRaster/DocumentHandle/RenderedDocumentPage`，未另造一套；`mobi_raster=None` 保持既有语义（含原文 detail）✓
- **资源生命周期**：`mkstemp` + `finally` unlink；提取树 owner = handle，`service.py:259` 的 `finally: handle.close()` 覆盖成功/页失败/取消/复制失败路径 ✓。**观察项（不阻塞）**：`handle.page_count` 在 `try` 之外读取（`service.py:180`），若将来实现可能抛错则存在一次清理缺口。
- **不可信输入边界**：`_pdb_crypto_type` 的越界读取有 `len(data) < …` 守卫与 `record_count == 0` 早退；DRM 判定先于一切解析 ✓；用户字节只落 OS 临时文件、交给第三方解析器 ✓
- **边界缺陷（= R-001）**：把第三方提取器**整棵临时树**的文件名集合当作页面清单，等于把「页面语义」交给上游命名约定；`cover%05d`（`kindleunpack.py:418-419`，`CREATE_COVER_PAGE=True` @:153）、`HDimage%05d`（:331）、以及 KF8 容器 `mobi8/OEBPS/Images/<同名>`（`unpack_structure.py:105-119`）三种命名都会进入清单，且排序**无显式键**。
- **可达性（决定严重度）**：`bookshelf/viewmodel.py:370` 的 QML `@Slot` **无类型过滤**即转交 `import_documents`，`bootstrap/app.py:464` 已注入 `MobiDocumentRaster()` ⇒ **用户可从书架 UI 直接导入 .mobi/.azw3**，无需等未来接线。

## Verification（executed）

轴状态 **`executed`**。口径：pwsh + `G:/CODEX/New Manga.task-envs/TASK-012-py312`、`PYTHONDONTWRITEBYTECODE=1`、`-p no:cacheprovider`；被审树 = 作者 worktree `G:\CODEX\New Manga.worktrees\TASK-041-zcode`（只读）。

| 场景 | 命令 / 步骤 | commit | 结果 | 证据 |
|---|---|---|---|---|
| 定向套件 | `pytest tests/import_formats tests/library -q -rs` | `d114253` | **PASS** 65 passed / 0 skipped，exit 0 | 本报告 |
| 新用例单跑 | `pytest tests/import_formats/test_mobi_import.py -q` | `d114253` | **PASS** 9 passed | 本报告 |
| 全仓 ×2 | `pytest -q -p no:cacheprovider -rs -rf` | `d114253` | **PASS** 822 passed / 6 skipped，exit 0 | [reviewer-full-suite-822-6.txt](../../verification/TASK-041/review-d114253/reviewer-full-suite-822-6.txt) |
| 判别力（修前） | 临时树 `git archive 6ea3dd3 src` + 本切片测试 | `6ea3dd3` src | **PASS（判别力成立）** ImportError，exit 2 | [discriminating-base-6ea3dd3.txt](../../verification/TASK-041/review-d114253/discriminating-base-6ea3dd3.txt) |
| 静态：依赖/泄漏/分层/无网络 | `rg` + `importlib.metadata` | `d114253` | **PASS** | [static-checks.txt](../../verification/TASK-041/review-d114253/static-checks.txt) |
| 绑定输出树（信息性） | 作者合成容器过真 `mobi.extract` | `d114253` src | **PASS**：仅 `mobi7/Images/image0000{2,3}.jpeg`；`HDImages` 空；无 `mobi8` | [probe-extract-tree.txt](../../verification/TASK-041/review-d114253/probe-extract-tree.txt) |
| 适配器收集语义 | 注入含 `cover`/`HD` 名的提取树 | `d114253` src | **FAIL（R-001 复现）** `page_count=4`（逻辑页 2），HD 居首 | [probe-collect-output.txt](../../verification/TASK-041/review-d114253/probe-collect-output.txt) |
| fail-closed 无脏数据 | 探针三例（DRM/截断/text-only） | `d114253` src | **PASS** 三例均 `managed_root_exists=False`、`files=[]` | 同上一行文件 |
| 真实 MOBI/AZW3/DRM 端到端 | — | — | **NOT_RUN**（无样本） | — |

**独立复跑 vs 复用作者证据（分列）**

- **我独立复跑**：定向 65/0、新用例 9 passed、全仓 ×2（822/6）、判别力 exit 2、静态四项（依赖/泄漏/分层/无网络面）、绑定输出树、适配器收集语义探针、fail-closed 零残留探针。
- **我复用作者留证（已在报告中标注，未另行复现）**：装前/装后对照 `819/0`（作者在临时 detached worktree `6ea3dd3` 上的两次全仓）、作者全仓 ×5（我核对其 5 份日志的汇总行一致为 `828 passed`，但**未重跑第 3~5 次**）、`pip-install-mobi.log` 的安装记录、`mobi_builder_probe.py` 的容器构造过程。
- **两者冲突处**：全仓 skip 列（作者 `0 skipped` vs 我 `6 skipped`，总数一致）→ 已登记 R-006。

## Findings

| ID | 级别 | 文件/行 | 触发与影响 | 复现证据 | 建议 | 处理状态 |
|---|---|---|---|---|---|---|
| R-001 | **P1** | `src/infrastructure/importing.py:288-293`（`sorted(root.rglob("*"))` + `_IMAGE_SUFFIXES` 过滤） | 页面清单=提取树**全量**图片文件按**文件名字典序**。上游会写出 `mobi7/Images/cover%05d`（EXTH `CoverOffset` 存在时；`kindleunpack.py:418-419`，`CREATE_COVER_PAGE=True` @:153）、`HDImages/HDimage%05d`（:331）、KF8 容器的 `mobi8/OEBPS/Images/<同名>`（`unpack_structure.py:105-119`）。后果：**KF8/双格式容器每页落两遍**、含 CoverOffset 者**封面被提到第 1 页**、含 HD 资源者**多出额外页**；全程静默「导入成功」。与类 docstring / Handoff / STATUS 的「文件名序即容器页序」矛盾，并违反本 Task 的「不伪造页面、不静默降级」 | [probe-collect-output.txt](../../verification/TASK-041/review-d114253/probe-collect-output.txt)（注入树→4 页/2 逻辑页、HD 居首）；命名与目录来自绑定源码，见 [static-checks.txt](../../verification/TASK-041/review-d114253/static-checks.txt) | 收敛收集面与排序键：仅取 KF7 页图目录 + `^image\d{5}\.` 模式，按名字中的 record 号排序（并显式声明 cover/HD 策略）；对 KF8 树**去重或 typed fail-closed**；补回归用例（注入树或真实容器） | **open** |
| R-002 | P2 | `src/infrastructure/importing.py:222-241`（类 docstring）、`service.py:68-75`（`_is_mobi` docstring） | 代码/注释声称支持 **AZW3**，实测仅 KF7 合成容器；AZW3（KF8）正是 R-001 的触发面 ⇒ 声明面超出验证面 | 同 R-001；作者 Handoff「未验证项」自述 AZW3 未留证 | 二选一：①把 AZW3 纳入验证并随 R-001 一并修；②声明收窄为「KF7 picture MOBI」，AZW3 **typed fail-closed** 并登记 `BLOCKED` + 解锁条件 | open |
| R-003 | P3 | `importing.py:249-256`；`tests/import_formats/test_mobi_import.py:314` | AC ③ 的「依赖缺失（readiness）」只由 double 覆盖；适配器自身 `except (ImportError, OSError) → MISSING_DEPENDENCY` **零覆盖** | 读测试路径：double 在 `open` 即抛，未进适配器 | 用 `sys.modules["mobi"] = None` 或 monkeypatch import 触达真实分支并断言 `MISSING_DEPENDENCY` | open |
| R-004 | P3 | `tests/import_formats/test_mobi_import.py:280-294` | 声明矩阵行「坏图页→后续页 pending、已成功页保留」只有**单页**用例，缺「中间页坏」 | 读测试；PDF 同类先例 `tests/import_formats/test_document_import.py:363` | 加多页中段坏图用例（断言 `pending_after_cancel` 与已导入页） | open |
| R-005 | P3 | Handoff「fail-closed 矩阵」表 vs `test_mobi_import.py:248/263/277` | 表称断言「managed 目录无新增文件」，用例实际只断言 `sink.pages == []`。**性质本身成立**（我的探针证明受控根未创建），但**证-据映射不准** | [probe-collect-output.txt](../../verification/TASK-041/review-d114253/probe-collect-output.txt) | 强化断言（断言受控根不存在/为空）或改写表内措辞 | open |
| R-006 | P3 | Handoff「同口径」表述、`verification/TASK-041/full-suite-run{1..5}.log` | 日志 `828 passed / 0 skipped` 与所声明的 PowerShell 口径不符（同口径实测 `822/6`）。**总数一致、无屏蔽、无新增 skip** | [static-checks.txt](../../verification/TASK-041/review-d114253/static-checks.txt) | Handoff 补口径勘误（哪一 shell、为何 0 skipped），维持「同一 shell + 同一 venv」纪律 | open |
| R-007 | P3 | `src/application/importing/documents/service.py:160-167` | 未注入绑定时的诊断仍写「MOBI rasterisation is BLOCKED, TASK-023」；MOBI 已获批准，真实原因是不存在绑定 | 读代码（保留 TASK-023 原文是刻意行为） | 改为「no MOBI binding was injected」；若坚持保留旧文案，在 Task 记明例外 | open |

## 结论与复审

- **不可集成**：`decision = changes_requested`。R-001（**P1**）为用户可达路径上的静默重复/乱序页；R-002 为 P2；另有 5 项 P3。按 §6.8 与协议 §6，`d114253` **退回作者**。
- **其余面质量良好（无需返工）**：分层/端口/依赖纪律、临时文件与提取树生命周期、fail-closed 语义（我另证受控副本零残留）、判别力、PDF 零回归、证据入库与未验证项披露，均通过。**预期修订面很小**：R-001 的收集面+排序键收敛与对应用例、R-002 的声明或失败语义；P3 可一并处理。
- **状态影响**：**TASK-041 保持 `in_review`**（作者文档提交里把它标为 `in_review`，与之一致）；**TASK-023 遗留① 不关闭**（解锁条件已满足，但交付未通过）；`integration_commit` 与 STATUS 收口待复审通过后由 Codex 执行。本次 Review **未修改**作者分支、生产代码、Schema、依赖清单或其他 Task；复审证据落在 `verification/TASK-041/review-d114253/`。
- **复审要求**：新 head + 逐条 disposition；R-001 须给出**可复现证据**（重复页/顺序反例消失；含 cover/HD/KF8 三类树形覆盖，或对未覆盖形态 typed fail-closed）。
