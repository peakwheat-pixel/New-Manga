---
task_id: TASK-012
author: ZCode
recipient: DeepSeek Harness（独立 Review）→ Codex（集成）
base_commit: 2cceb1e734c5079870662b7a28da316e46444810
delivery_head: ca5848b746210564a2503e8c5f59e0a2118e56a1
status: delivered
---

# Handoff：TASK-012（四页导航与书架 UI）

分支 `agent/zcode/TASK-012-navigation-library-ui`，worktree
`G:/CODEX/New Manga.worktrees/TASK-012-zcode`。
`delivery_head` = `ca5848b`（本分支被审内容固定于此；后续若有仅状态的文档提交，Review 以本 hash 为准）。

## 交付结果

白名单内全部完成；`Main.qml` / `bootstrap/app.py` 的装配改动按协议**未实施**，已登记范围变更请求（见"风险与遗留"）。

提交列表（base `2cceb1e` → delivery `ca5848b`）：

| Commit | 内容 |
|---|---|
| `01f5bdb` | 认领，`in_progress` |
| `c9b3fac` | 实施计划 + Main.qml scope-change request 登记 |
| `cf0df63` | `application/navigation` + `NavigationViewModel`（19 tests） |
| `17a4a48` | `BookListModel`/`ChapterListModel` + `BookshelfViewModel`（33 tests） |
| `967dbc0` | QML 批次：shell/bookshelf/workbench/reader/settings/common 共 12 个 QML + `test_qml_shell.py`（11 tests）；viewmodel 增加 ISO 日期适配 |
| `ca5848b` | `doc/ui-baseline.md` + verification 证据 |

变更路径（均属 Task 白名单）：`src/application/navigation/`、`src/ui/viewmodels/{navigation,bookshelf}/`、
`src/ui/models/library/`、`src/ui/qml/{shell,bookshelf,common,workbench,reader,settings}/`、
`tests/ui_shell/`、`doc/ui-baseline.md`、`doc/tasks/TASK-012.md`、`verification/TASK-012/`。

### AC 对照

- **AC-NAV-001**（默认书架）✅：服务层默认 + QML rail checked 绑定，python/QML 双层测试。
- **AC-NAV-002**（只有四个一级入口）✅：`NavigationService.navigate` 拒绝第五 Route；AppShell 恰好四页四钮。
- **AC-NAV-003**（切换保持上下文）✅：四页常驻仅切 `visible`，模型行数在 settings↔bookshelf 往返后不变。
- **AC-LIB-001 / AC-CH-001..004** ✅：CRUD 全部经 `LibraryService`（TASK-007 用例），QML Dialog/输入行端到端。
- **AC-LIB-002/003（tag）** N/A：本切片 UI 无标签编辑入口（D05 §8 属更多菜单，TASK-022）。
- 导入入口 ✅：`importPages`（python seam）/`importFilesFromUrls`（QML QVariantList→ImportSource，源文件只读）。

### 视觉基线

`doc/ui-baseline.md`：新设计首次落地基线（布局/色板/字体/空状态/禁用态），非既有截图。

## 验证证据

环境：Windows 11（10.0.26200 x64）、Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312`）、PySide6 6.11.2、pytest 9.1.1、Qt Windows 平台（offscreen 无字体库不可用于 QML）。

| AC/场景 | 实际命令（worktree 根） | 被测 commit | 结果 | 证据 |
|---|---|---|---|---|
| 全仓回归（含架构守卫） | `…-py312/python.exe -m pytest tests -q` | `ca5848b` | **PASS**（296 passed，exit 0） | `verification/TASK-012/pytest-full-20260915.txt` |
| 导航 + 书架 VM + QML 装载 | `…-py312/python.exe -m pytest tests/ui_shell -v` | `ca5848b` | **PASS**（46 passed，exit 0） | `verification/TASK-012/pytest-ui_shell-verbose-20260915.txt` |
| 100/150/200% DPI 截图初验 | — | — | **NOT_RUN/BLOCKED**：应用装配（Main.qml/bootstrap）待 Codex 裁决范围变更，无法从入口启动完整应用 | Task 文件"测试要求" |
| 键盘焦点全量矩阵 | — | — | **NOT_RUN**（Rail focus 已设；矩阵归 TASK-022） | — |
| 导入 FileDialog 原生交互 | — | — | **NOT_RUN**（原生对话框不可无头驱动；调用契约 python 侧已覆盖） | — |

不得由 Mock 结论外推的部分：无（本切片不含模型/视觉算法；"导入摘要"文本为 UI 拼装，非质量断言）。

## 接收方式

Review 检查建议（DSH）：

1. `git diff 2cceb1e..ca5848b -- src/application/navigation src/ui` 对照 D05 §2/§3.1/§7/§8/§9/§43.1/§60/§62。
2. 运行验证（隔离 venv 路径见上表；QML 测试必须用 Windows 平台，勿加 `-p no:cacheprovider` 以外平台参数）。
3. 重点复核：QML 是否残留业务逻辑（应只有绑定与转发）；`BookshelfViewModel` 对源文件只读（`data_provider` 延迟读）；空状态诚实性（"—" 占位无虚构进度）。

复现启动（Review 用，非产品装配）：`pytest tests/ui_shell/test_qml_shell.py -v` 即以真实 ViewModel 装载 AppShell。

## 风险与遗留

1. **范围变更请求（阻塞产品可见性，待 Codex）**：`src/ui/qml/Main.qml` 改为加载 `shell/AppShell.qml`（约 3 行）+ `src/bootstrap/app.py` 构造服务/ViewModel 并 `setContextProperty`。不裁决则新 Shell 只能经测试装载，用户不可见。裁决后由 Codex 或下一切片实施。
2. **阅读进度字段不存在**（TASK-007 未交付）：UI 显示"—"。TASK-007 后续交付字段时需同步卡片/详情绑定。
3. **PySide6 6.11 平台事实**（已写入 test_qml_shell docstring 与 QML 注释，Review 不视为缺陷）：`QQmlComponent.status` 是方法；Repeater delegate item 不在 QObject 树、python 侧 `itemAt()` 返回 None；Loader item 为 JS ownership（BookGrid 因此弃用 Loader，双 view 常驻）；QML 不能调用非 slot 的 python 模型方法（ChapterList 行选中走 delegate 角色）。
4. **编辑/页面管理/翻译设置/更多菜单/标签管理** 为 disabled 占位（D05 §8/§9 映射的悬浮窗属 TASK-022）；工作台/阅读器真实内容属 TASK-013/015。
5. 回退：分支整体 revert 至 `2cceb1e` 即可，无数据迁移、无用户文件接触。
