---
task_id: TASK-018
author: DeepSeek Harness
recipient: Codex（复审）
base_commit: 3b38d39de719c41267c7170808928040af6287f9
delivery_head: 50633158d98f4382fe17da070454423d992b7496
status: in_review
review_context: 复审结论 changes_requested（工程实现已复核 PASS，代码无需改动）
---

# Handoff：TASK-018 复审收口（R-101 / R-102 / R-103 + 可选 R-104）

## 交付结果

收口提交 **`5063315`**（4 文件、`+63/−23`），固定 base `3b38d39`（复审前分支 head）。

Reviewer 的复审结论为 `changes_requested`，**工程实现已复核 PASS、代码无需改动**；本切片**只做措辞与文档口径收口**，并**未重跑实验**：

| 未改动（用户明确禁止/要求） | 状态 |
|---|---|
| `experiments/TASK-018/test_mask_protocol.py` | **未改动**（仍为原 12 例） |
| `experiments/TASK-018/results/experiment.json` | **未改动**（未重跑覆盖） |
| `doc/STATUS.md`、`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/tasks/README.md` | **未改动**（集成元数据归 Codex） |
| 生产 `src/`、`tests/`、Schema、依赖清单、`AGENTS.md` | **未改动** |

变更路径（4 个，全部在允许范围内）：

| 文件 | 内容 |
|---|---|
| `experiments/TASK-018/test_route_gating.py` | R-101 docstring 措辞 + R-104 导入断言（**代码逻辑未改**） |
| `doc/research/TASK-018.md` | R-102 §4 表与结论区间更新 + 数据归属注；同步 §4 引言中已过时的"未逐框断言"表述 |
| `doc/tasks/TASK-018.md` | R-103 合并重复状态块与过时占位 |
| `verification/TASK-018/experiment-log.md` | 新增 §7.2 复审收口记录 |

## 逐项处置

### R-101（docstring 措辞）—— **fixed（方案 A）**

`test_route_gating.py` 的 docstring 原先声称 *"Standard library only — no PySide6, no third-party package, no model"*。但该模块 `import run_experiment as rx`，而 `run_experiment` 会导入 PySide6 做图像 I/O —— **原表述与实际依赖链不符**。

**采用方案 A（仅措辞，代码逻辑不动）**：docstring 现如实写明"本测试代码只用标准库，但通过 `run_experiment` **间接依赖 PySide6**，不是端到端无依赖；需要一个能导入 `run_experiment` 的解释器"。未采用方案 B（下沉 `mask_protocol.py`），因为 Reviewer 已判定代码无需改动。

### R-102（耗时与 RSS 的 artifact 归属）—— **fixed（更新为新值 + 标注归属）**

**问题确认**：`doc/research/TASK-018.md` §4 表的 `min ms`/`max ms`/`peak RSS MB` 三列原取自**首轮** `6c33e7f` 的 `experiment.json`；而仓库内当前 `experiment.json` 已由修订切片 `d7c10d4` **重生成**，两者数值不同。经逐列比对：**`ink`/`residual`/`viol` 三列逐值相同**（确定性量），**只有耗时与 RSS 两列变了**（易变量）。

**处置**：
- §4 表的 `min ms`/`max ms`/`peak RSS MB` 三列**更新为当前 `experiment.json` 的值**；
- 表下新增**"数据归属"**注，明确：这三列所属 artifact 是 **`d7c10d4` 的 `results/experiment.json`**，且首轮 `6c33e7f` 的对应数值不同（耗时/RSS 是易变量）；
- §4 结论 4 区间同步为 **2.20–3.05 ms（simple-fill）/ 32.28–48.61 ms（edge-bleed）/ 峰值 45.32–57.55 MB**；
- 结论 5 与 §7 的 OOM 行同步为 **≤ 57.6 MB / ≤ 48.7 ms**（原为"≤ 58 MB / ≤ 46 ms"）；
- §4 引言中"harness 目前未按保护框逐个断言"已**过时**（`d7c10d4` 已实现），改为指向 `protected_box_violations`（20/20 框全 0，见 §9.1）。

**未重跑**：全部数字均**只读**自仓库内现有 `experiment.json`。

### R-103（合并任务文件重复状态块）—— **fixed**

原状：`doc/tasks/TASK-018.md` 同时存在 `## 交付与运行记录`（内容全为"尚无"）、`## 交付与运行记录（补充）`、`## 当前状态（唯一，集成后）` 以及**第二个** `- **当前状态（唯一）**` 块，且正文称 `status` 为 `done` 而 frontmatter 为 `in_review`。

**处置**：
- 空的"尚无"三项（Handoff / Review / 实际执行）替换为**实际链接**（两份 Handoff、Review 报告、研究/实验日志/修订取证/集成验证）；
- `## 交付与运行记录（补充，2026-09-17）` 降为 `### 首轮交付与实测（2026-09-17）`（消除同级重复标题）；
- `## 当前状态（唯一，集成后）` 改为 `## 集成收口与来源（2026-09-17）`（不再自称"唯一"），**全文只保留一个 `当前状态（唯一）`**；
- 正文状态表述与 frontmatter 统一为 `in_review`；`尚无` 残留计数 = **0**。

### R-104（可选，导入断言）—— **已实施**

`test_route_gating.py` 在导入期新增断言：若被导入的 `run_experiment.__file__` **不来自本目录**（例如被其他 Task 的 `experiments` 目录遮蔽），立即 `AssertionError` 并提示检查 `PYTHONPATH` 顺序。理由：该套件全部断言都作用于 `rx`，导入错误的 harness 会让结果**静默失去意义**。

## 验证证据

环境：Windows `10.0.26200`；Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312`）；PySide6 6.11.2；`PYTHONPATH=experiments/TASK-018`；`PYTHONDONTWRITEBYTECODE=1`。

| # | 命令 | 退出码 | passed | skipped | 结果 |
|---|---|---:|---:|---:|---|
| 1 | `python -m unittest experiments/TASK-018/test_mask_protocol.py` | **0** | **12** | **0** | `OK`（**该文件本轮未被改动**） |
| 2 | `python -m unittest experiments/TASK-018/test_route_gating.py` | **0** | **13** | **0** | `OK`（R-101/R-104 改动后仍全绿） |
| 3 | `git diff --check 3b38d39 5063315` | **0** | — | — | 无输出 |
| 4 | 允许/禁止范围检查 | — | — | — | touched = 4，**禁止路径命中 0** |

**skip 原因**：命令 1、2 均 **`0 skipped`**。

**数字可定位性核对**（要求"doc 中每个数字都能在 `results/experiment.json` 中定位"）：

| 抽样（doc §4 表） | `experiment.json` 实测 | 匹配 |
|---|---|---|
| white-background / simple-fill：2.203 / 3.054 / 46.13 | 2.203 / 3.054 / 46.13 | ✅ |
| structure-crossing / edge-bleed：44.075 / 48.609 / 57.55 | 44.075 / 48.609 / 57.55 | ✅ |
| line-art / edge-bleed：32.282 / 38.306 / 56.11 | 32.282 / 38.306 / 56.11 | ✅ |

**未改动确认（脚本判定）**：`test_mask_protocol.py changed = False`、`results/experiment.json changed = False`。

## 未完成项（未因本切片变化）

四条学习型路线（Manga LaMa / AOT / BrushNet-PowerPaint / FLUX）的**质量、耗时、内存与显存**仍为 **BLOCKED**；**Mask 内部结构损伤量化**、**真实 OOM**、**真实漫画样例**仍为 **NOT_RUN**。本切片**未新增任何模型数字**，也未重跑覆盖既有 measured 数据。

## 边界与合规

- 未改 `doc/STATUS.md`、`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/tasks/README.md`；未改生产 `src/`、`tests/`、Schema、依赖清单、`AGENTS.md`。
- **未重跑覆盖 `results/experiment.json`**；**未改动 `test_mask_protocol.py`**。
- 未释放 TASK-019 或其他冻结 Task；未 push、未合并 master。

## 状态

`doc/tasks/TASK-018.md` 为 **`in_review`**，交 Codex 复审。本 Task 不自行标记 `approved`／`done`。
