---
task_id: TASK-065
author: Codex
recipient: Independent non-author reviewer
base_commit: 772d63c2c921a08606d742438a146cb310159b02
delivery_head: 73a7557
status: ready_for_review
---

# Handoff：TASK-065

## 交付结果

T1.3.2 的实现与证据位于分支 `agent/codex/TASK-065-task058-followups`，worktree
`G:/CODEX/New Manga.worktrees/TASK-065-codex`，固定 review range 为
`772d63c2c921a08606d742438a146cb310159b02..73a7557`。

- AC1：真实装配用例注入现成 `DeterministicStepExecutor` 的 1 秒步骤；先证明 worker 已进入步骤且
  `is_running is True`，再调用 `_shutdown_services`，保留返回后线程已排空、run 已终态、SQLite
  facade 已关闭的既有强断言。仅增加活动断言时旧夹具得到预期 RED：1 failed / EXIT=1。
- AC2：新增可复跑的 PowerShell runner 与 targeted / integration / full-suite 三份原始日志；均记录
  shell、venv、命令、collection、环境和 EXIT，全量日志逐条列出 6 个 skip 原因。
- AC3：`AppServices.conn` 改注解为实际的 `ThreadRoutedConnection`，相邻 docstring 改为
  per-thread connection 口径；运行逻辑不变。
- AC4：删除恒真的 `assert sys.stderr is not None` 和失去用途的 import，保留 stderr 文本断言。
- AC5：实现、验证与本 Handoff 已完成；独立 Review 待执行。

未修改 Schema、Provider、QML、依赖、产品行为、其他 Task 或主工作区既有改动。

## 验证证据

| AC/场景 | 实际命令/步骤 | 环境与被测提交 | 结果 | 日志/产物 |
|---|---|---|---|---|
| AC1/AC4 targeted | `pytest <2 exact node ids> -q -p no:cacheprovider -rs` | PowerShell 7.6.6；TASK-012-py312；提交前工作树（内容即 `73a7557`） | PASS：2 collected / 2 passed / EXIT=0 | [targeted.log](../../verification/TASK-065/targeted.log) |
| 相关 integration | `pytest tests/core/test_shutdown_drain.py tests/workbench/test_drain_shutdown.py -q -p no:cacheprovider -rs` | 同上 | PASS：7 collected / 7 passed / EXIT=0 | [integration.log](../../verification/TASK-065/integration.log) |
| AC2/AC5 full suite | `pytest tests -q -p no:cacheprovider -rs` | 同上 | PASS：951 collected / 945 passed / 6 skipped / EXIT=0；54.56s | [full-suite.log](../../verification/TASK-065/full-suite.log) |

6 个 skip 与 rebaseline 基线完全相同，均为 `openssl unavailable`；无 xfail/xpass、无新增 skip。
`git diff --check` 为 EXIT=0。

## 接收方式

Reviewer 请固定审查 `772d63c2c921a08606d742438a146cb310159b02..73a7557`，重点核对：

1. 调用 `_shutdown_services` 前的 worker 活跃前提是否真实且非恒真；
2. 返回后的排空、终态和连接关闭断言是否仍具判别力；
3. 类型/docstring 是否只反映 `ThreadRoutedConnection` 事实而未改变运行行为；
4. 是否存在断言放宽、Task Escape 或越界文件。

## 风险与遗留

- 测试通过 1 秒阻塞建立确定的活动窗口；生产路径与等待预算没有改变。
- TASK-058 F-004 与 F-007 不属于 TASK-065 正式 AC，本切片未处理，也未创建新 Task。
- 评审通过后仍需由 Codex 合并、复跑合并后全量、更新 `TASK-065` / `STATUS` 并登记集成证据。
