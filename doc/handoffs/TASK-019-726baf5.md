---
task_id: TASK-019
author: DeepSeek Harness
recipient: Codex（独立 Review；集成由 Codex 执行）
base_commit: 36242fb00f9f432ec66cc3c33afc167578d0f341
delivery_head: 726baf5
status: in_review
---

# Handoff：TASK-019 集成已验证的检测/OCR/翻译/修复 Provider

## 交付结果

**范围**：按 TASK-019 允许路径交付 Provider 集成层——4 个能力端口、统一错误/就绪分类、设备与模型生命周期、OpenAI-compatible 客户端与本地/远程适配器、修复路由与 Simple Fill/Edge Bleed 基线、Sakura 健康/就绪探测、Inpaint Step、seam 兼容的 prepare-only 写回、5 个生产 handler，并在 `bootstrap.app` 注入。

**提交**（分支 `agent/deepseek/TASK-019-provider-integration`，工作区 `G:/CODEX/New Manga.worktrees/TASK-019-deepseek`）：

| commit | 内容 |
|---|---|
| `07d78d3` | 开工：`doc/tasks/TASK-019.md` 置 `in_progress` |
| `8effbeb` | 实现主体（45 文件、+9477/−5）：端口、基础设施适配器、Inpaint Step、handler、bootstrap 注入、`tests/providers` |
| `5fdd80a` | 写入侧乐观守卫（`expected_current_revision_id`）、翻译锁双读、Sakura 就绪口径一致、+5 例测试 |
| `3176520` | 取证与交付文档：`verification/TASK-019/**`、Handoff、Task 文件 `in_review` |
| `726baf5` | 页面级修复累积：inpaint 以当前 Clean 为上游、非 Region 目标显式失败、页面级 `REINPAINT_ALL` 端到端用例 |

`delivery_head = 726baf5`（代码）；`3176520` 及之后的文档提交记录取证。固定代码基线 `36242fb`。**未 push、未合并 master**。

**AC 逐条对照**：见 [`verification/TASK-019/ac-status.md`](../../verification/TASK-019/ac-status.md)（PASS 15 条 + BLOCKED 7 条 + NOT_RUN 2 条，逐条给出证据与解锁条件）。摘要：

- **PASS**：AC-OCR-001（写作用域集成语义）、AC-OCR-004（显式 fallback 部分）、AC-INPAINT-001/002/003、AC-INPAINT-004（选择与追踪）、AC-RFULL-002/003/004/005、AC-FALLBACK-001/002、AC-GPU-001/002/003、AC-OPTIONAL-001/002、AC-MODEL-001（生命周期）/002、AC-EXT-SAKURA-001（探测实现）。
- **BLOCKED**：AC-OCR-001 真实识别质量、AC-OCR-004（PaddleOCR 运行时）、AC-INPAINT-004 真实彩色修复、**AC-RFULL-001 完整链**、AC-MODEL-001 真实权重下载。
- **NOT_RUN**：AC-EXT-SAKURA-001 真实服务验证、真实端点端到端/成本/时延。

## 验证证据

环境：Windows 11 `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`。原始日志与报告见 [`verification/TASK-019/`](../../verification/TASK-019/author-verification.md)。

| # | AC/场景 | 实际命令/步骤 | 环境与被测 commit | 结果 | 日志/产物 |
|---|---|---|---|---|---|
| 0 | 改动前基线 | `python -m pytest -q -p no:cacheprovider` | `c9eb65a`（释放提交） | **530 passed, 6 skipped**（基线事实） | — |
| 1 | 新增套件 | `python -m pytest tests/providers -q -p no:cacheprovider -rs` | `726baf5` | **PASS**：110 passed, 0 skipped（退出码 0） | `pytest-providers.log` |
| 2 | 强制回归 | `python -m pytest tests/pipeline tests/core tests/storage -q -p no:cacheprovider -rs` | `726baf5` | **PASS**：78 passed, 0 skipped（退出码 0） | `pytest-regression.log` |
| 3 | 全仓 | `python -m pytest -q -p no:cacheprovider -rs` | `726baf5` | **PASS**：640 passed, 6 skipped（退出码 0；+110 全部为本 Task 新增） | `pytest-full.log` |
| 4 | skip 明细 | 同上 `-rs` | — | 6 条全部位于既有 `tests/network`：`openssl unavailable`（`test_connection_tester.py:106`、`test_transport_tls.py:39/47/62/69/83`），与本 Task 无关 | `pytest-full.log` |
| 5 | AC-OCR-001/002、RFULL-002/003/005 | `pytest tests/providers/test_handlers_pipeline.py` | `726baf5` | **PASS**：真实 SQLite + 真实 seam；目标 Region 写入、A/C 未变、人工译文与锁保留、陈旧 revision 与锁被拒 | 同 1 |
| 6 | AC-INPAINT-001/002/003/004 | `pytest tests/providers/test_inpaint.py tests/providers/test_handlers_pipeline.py` | `726baf5` | **PASS**：Mask 原始+精修两 revision 与文件、逐位回读、Clean revision 递增且旧版本保留、Router provenance、彩色路线选择与追踪；页面级 `REINPAINT_ALL` 展开为 3 个 Region 单元、Clean 累积（3 revision 串联） | 同 1 |
| 7 | AC-FALLBACK-001/002、D06 §56/§57 | `pytest tests/providers/test_retry_fallback.py` | `726baf5` | **PASS**：无显式 fallback 只试 primary；显式 fallback 记录两次尝试；重试不改变 payload；认证类错误不重试 | 同 1 |
| 8 | AC-GPU-001/002/003 | `pytest tests/providers/test_devices.py` | `726baf5` | **PASS**：OOM → `OUT_OF_MEMORY` 且不逃逸；双线程 `max_observed=1`；CPU fallback 仅声明支持时发生并记录 provenance | 同 1 |
| 9 | AC-OPTIONAL-001/002 | `pytest tests/providers/test_bootstrap_providers.py tests/providers/test_registry_readiness.py` | `726baf5` | **PASS**：本机无 torch/numpy/OCR 运行时仍完成真实装配；六态就绪报告 | `readiness-report.json` |
| 10 | AC-MODEL-001/002 | `pytest tests/providers/test_model_lifecycle.py` | `726baf5` | **PASS**（生命周期）：进度/取消/重试/hash 门控/懒加载/OOM 卸载 | 同 1 |
| 11 | AC-EXT-SAKURA-001 探测 | `pytest tests/providers/test_sakura_probe.py` + 真实 `StdlibTransport` 探测 `127.0.0.1:8080` | `726baf5` | 探测**PASS**；真实服务**NOT_RUN**：`reason_code=unreachable`、`connection refused`、仅 1 个请求 | `sakura-probe-attempt.json` |
| 12 | 真实端点 / 模型质量 / 显存性能 | 无（未配置端点、无依赖、无权重） | `726baf5` | **BLOCKED / NOT_RUN**（未产出任何质量、成本、时延、显存数字） | `ac-status.md` §2 |
| 13 | 允许路径与空白 | `git diff --name-only c9eb65a..HEAD`、`git diff --check 36242fb..726baf5` | `726baf5` | 56/56 在允许范围内、禁止路径 0；`--check` 退出码 0 | `changed-paths.txt` |

**明确未运行**：真实 OCR/翻译/修复模型质量与性能、真实 OpenAI-compatible 端到端、成本/时延、真实 Sakura 服务验证、真实权重下载、真实 GPU OOM 复现。**未将任何替身结果表述为模型质量或真实端点证据**。

## 接收方式

1. **分支/工作区**：`agent/deepseek/TASK-019-provider-integration` @ `726baf5`；`G:/CODEX/New Manga.worktrees/TASK-019-deepseek`。固定 base `36242fb`（代码基线，与 release 提交仅差纯文档提交）。
2. **复现命令**（PowerShell，仓库根）：
   ```powershell
   $env:PYTHONDONTWRITEBYTECODE='1'
   $py = "G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe"
   & $py -m pytest tests/providers -q -p no:cacheprovider -rs
   & $py -m pytest tests/pipeline tests/core tests/storage -q -p no:cacheprovider -rs
   & $py -m pytest -q -p no:cacheprovider -rs
   ```
3. **不需要**前置数据、端点、权重或额外依赖；`tests/providers` 无网络、无 Qt 依赖（`test_bootstrap_providers.py` 走真实 `assemble_services`，仅需已安装的 PySide6）。
4. **Reviewer 重点检查建议**：
   - `src/infrastructure/providers/step_writes.py`：prepare-only 语义是否满足 seam 契约（pointer 归 seam）；region 目标 artifact 的 CAS 收编是否可接受（§2.1 与风险 R-1）；
   - `src/infrastructure/providers/inpaint_router.py`：彩色/复杂场景拒绝单色基线（R-3）是否为期望产品行为；
   - `src/infrastructure/providers/handlers.py`：错误码穿越 seam 的方式（`StepExecutionError(error_code, …)`）；
   - `src/ports/providers/errors.py`：作为 `src/ports/providers/**` 内**新增模块**是否越界（R-6）；
   - 允许路径与依赖清单/Schema/seam 未改动（§6）。
5. **集成前需 Codex 裁决**：AC-RFULL-001 的 `color`/`term_extract`/`render` handler 归属与允许路径（R-2）。

## 风险与遗留

| ID | 风险 | 处置建议 |
|---|---|---|
| **R-1** | prepare-only 写入与 seam 收编之间存在极小并发窗口（窗口内并发写会让 region 文本已提交而指针未收编，步骤记 candidate） | 已用写前期望 revision 校验最小化窗口；彻底消除需 seam 提供「内容+指针同事务」入口（属 `src/infrastructure/pipeline/**`，超出允许路径） |
| **R-2** | AC-RFULL-001 完整链缺 `color`/`term_extract`/`render` handler；实测 `render` 在规划阶段即 `BLOCKED(missing_clean_artifact)` | 请 Codex 明确这三步的归属 Task 与允许路径；补齐前 AC-RFULL-001 保持 BLOCKED |
| **R-3** | 「彩色/高复杂度不接受单色基线」为本 Task fail-closed 裁决（D06 §21 仅给策略示例） | 若产品要允许降级，需用户裁决并在设置中显式允许；当前不会静默降级 |
| **R-4** | TASK-017 §4.1 口径：除 401/403/429 外的 4xx 显式归为不可重试 | 沿用并单测锁定；如需 408 等细分应在网络栈单独裁决 |
| **R-5** | **疑似跨 Task 缺陷（未修改）**：普通新建 Region 默认 `sfx_policy='skip'`，planner 对所有 region type 的 `translate/segment/mask_refine/inpaint/render` 一律 `SKIP_POLICY`；`src/ui/**` 无任何写入路径 | 证据：`schema.py:229`、`application/editing/service.py:254`、`domain/regions/entities.py:230`、`application/tasks/service.py:353-361`、`sqlite/pipeline.py:709` vs `:103`（快照默认 `"translate"`，口径不一致）、`tests/pipeline/test_pipeline.py:47`（辅助默认 `translate`）。请 Codex 确认意图与归属；影响面为 P0 级 |
| **R-6** | `src/ports/providers/errors.py` 为新增模块（任务文本允许该 glob 但限定「扩展 capability/可选字段」） | 若判定越界，请退回并指定共享错误/就绪分类的落点；`profiles.py` 仅新增 `CAPABILITY_DETECTION` 常量 |
| **R-7** | 完整链与真实模型路线未收口，AC-RFULL-001 与全部质量/性能类 AC 仍 BLOCKED | 解锁条件：依赖获批 + 权重路径与 SHA-256；端点类需用户提供端点或明确取消该路线 |
| **R-8** | 页级 Mask/Clean 与 Region 单元是「一页一 artifact 行」的既有数据模型：同页多 Region 各生成 mask revision，但当前 Mask 指针最终只指向最后一个 Region 的精修 mask（历史 revision 均保留；Clean 侧因累积上游不受影响） | 若产品需要按 Region 查看 mask，需要 mask artifact 归属（Schema/D03 §16）裁决，超出本 Task 允许路径。取证：`test_page_scope_reinpaint_expands_to_regions_and_accumulates_clean`（6 个 mask revision） |

## 回退方法

本 Task 只新增文件（除 `src/bootstrap/app.py` 与 `src/ports/providers/profiles.py` 的小改动）。回退 = 丢弃本分支，或 `git revert 726baf5 3176520 5fdd80a 8effbeb`（按此顺序）：`build_production_pipeline(conn)` 恢复为无 handler 形态，既有 `PROVIDER_UNAVAILABLE` 行为与全仓 530 passed 基线即恢复；无 Schema、无依赖清单、无数据迁移，无破坏性影响。
