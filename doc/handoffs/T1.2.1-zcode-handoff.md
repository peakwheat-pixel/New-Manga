# T1.2.1 Handoff — ZCode → Codex（非作者 Review 请求）

- **Task**：T1.2.1 Settings UI & ViewModel（Roadmap Order 3）
- **作者**：ZCode
- **Base**：`d11d927a46c9d2350b0e8e8161abb8ff21acba79`（master，T1.1.1 集成门关闭点）
- **Branch / Worktree**：`agent/zcode/T1.2.1-settings-ui-viewmodel` /
  `G:\CODEX\New Manga.worktrees\T1.2.1-settings-ui-viewmodel`
- **Delivery HEAD**：见 §2 提交表（R1 见 §2；R3 修订见 §8）
- **状态**：R3 blocker 修复完成、自验通过，**等待 Codex R3 非作者 Review；
  未合并 master；未启动 T2.1.1**

> **R3 说明**：Codex 于 `verification/T1.2.1/review-c54f360.md` 记录三个
> BLOCKING（B-001/002/003）与一处证据计数更正（I-001）。本文件 §1~§7 保留
> R1 原文作为证据；R3 修复、新增验证与范围扩展依据见 **§8**（优先阅读）。

## 1. 交付内容与固定 Scope

**Allowed paths（实际改动，`git diff d11d927..HEAD --stat` 可核）**：

| 文件 | 内容 |
|---|---|
| `src/infrastructure/settings/json_profile_stores.py`（新） | JSON 文件版 `ProviderProfileStore` / `NetworkProfileStore`（实现 D03 §25/§27 既有 Protocol；原子写 tmp+`os.replace`；仅 `credential_ref` 入盘） |
| `src/infrastructure/settings/__init__.py`（新） | 包声明 |
| `src/ui/viewmodels/settings/viewmodel.py`（新） | `SettingsViewModel`：provider/network profile CRUD、per-capability 绑定、默认网络配置、危险设置确认信号 |
| `src/ui/viewmodels/settings/__init__.py`（新） | 包声明 |
| `src/ui/qml/settings/SettingsView.qml`（重写） | Provider 与 网络/代理 两类别真实表单；其余类别保持占位（D05 §43.1 分类结构不变） |
| `src/bootstrap/app.py`（最小接线） | ① `AppServices` 末尾新增带默认值字段 `settings_vm=None`（既有构造零破坏）；② `assemble_services` 在 export_service 后新增 settings 装配段（JSON stores + `WindowsCredentialStore` best-effort + `SettingsViewModel`）；③ `assemble_engine` 注入 `settingsViewModel` context property |
| `tests/ui_shell/test_settings_viewmodel.py`（新，15 项） | VM 流转 + 脱敏 + 确认回路 |
| `tests/network/test_settings_profile_persistence.py`（新，8 项） | 重启存活 + 凭据文件形态 + 损坏文件 typed error |

**Forbidden paths（未触碰）**：SQLite Schema/migrations（无新表，`pipeline_defaults`
行结构不变）、`src/ports/**`（Protocol 零改动，stores 只是其新实现）、QML shell
（`Main.qml`/`AppShell.qml` 未动）、用户源文件、其他 Agent dirty 文件、
STATUS/Roadmap/共享契约文档。

## 2. 提交

| commit | 内容 |
|---|---|
| `c54f360` | **R1 delivery HEAD**：§1 全部代码+测试 |
| （文档提交） | 本 Handoff |

## 3. 四项核心说明（Review 重点）

### 3.1 持久化（survive restart）

provider/network profiles 存 `data_root/settings/` 下两份 JSON
（`provider_profiles.json`、`network_profiles.json`），与
`reading_progress.json` / `export_history.json` 同属 **JSON-backed 中间态**
（store Protocol 明文把 SQLite adapter 留给后续协调切片，T3.1.1 统一存储时
一并迁移）。文件含 `version` 域；损坏文件抛 typed
`ProfileFileFormatError`（fail-closed，不静默清空）；写入原子。两个 store
的行为语义（add 覆盖 / update 缺失抛 KeyError / delete 缺失 no-op）与
`tests/network/net_helpers.py` 的 in-memory 参照实现一致。

### 3.2 重启恢复

测试以"实例 A 写入 → 全新实例 B 读取"建模进程重启（`test_*_survives_restart`）。
凭据侧：API key / 代理密码写入 Windows Credential Manager（OS 托管，天然跨
重启）；profile 仅持 `credential_ref`，重启后经 `has_credential` 恢复
`credential_set` 状态显示。

### 3.3 pipeline 接入（feed the pipeline）

复用 TASK-050 既有通道，**零新增管线代码**：
`SettingsViewModel.saveBinding / saveSettingsSectionJson` →
`PipelineDefaultsService`（application 层唯一写面，含 known-step 校验）→
`pipeline_defaults` 行 → bootstrap 既有 `_load_pipeline_settings` /
`build_provider_runtime(settings=…)` 读取 → run 启动时冻结快照（D06 §49；
run 中修改不静默切换，AC-PROVIDER-003 语义不变）。能力默认绑定
（AC-PROVIDER-002）由 QML 每能力一行直接写入该行。

### 3.4 credential 脱敏（never enter logs/diagnostics）

- API key / 代理密码在 VM 内即刻包成 `SecretValue` 送 vault
  （`WindowsCredentialStore`）；profile/JSON/日志只持 `credential_ref`
  （AC-SEC-001/003）；
- VM 对 QML 只暴露 `credential_set` 布尔；QML 密码框 `echoMode: Password`、
  无回显、留空=不修改；
- `failed` / `confirmationRequired` 消息仅由 id 与静态文案拼成，测试
  断言 payload 中的 secret 值不出现在任何错误消息（AC-SEC-002）；
- TLS 默认开启（AC-SEC-004）；关闭必须走 `confirmationRequired` →
  QML 确认 Dialog → 带 `confirm_disable_tls=true` 重提交（AC-SEC-005），
  测试验证了未确认时不落盘。

## 4. 验证记录（详细命令与口径随代码提交入 author 留档）

| 项 | 结果 | exit |
|---|---|---|
| 新增测试（15 VM + 8 持久化） | 23 passed | 0 |
| focused（tests/ui_shell + tests/network 全目录） | **R1 口径更正（I-001）：160 passed, 6 skipped**（6 skip 为既有 OpenSSL 不可用 TLS 项），R1 记录 "166 passed" 有误 | 0 |
| full suite（impl venv，`pytest tests`） | 见 §4.1 | — |
| `compileall -q src tests` | 通过 | 0 |
| bootstrap smoke | exit 0；SQLite + managed 目录创建 | 0 |
| smoke QML 噪音（见 §5） | 修复后 **6×0**（SettingsView 相关 TypeError 全消） | — |
| `git diff --check` | 干净 | 0 |
| worktree | clean（提交后） | — |

### 4.1 full suite

`1 failed, 1099 passed, 1 warning`（86.46 s，exit 1，impl venv）。唯一失败：
`tests/providers/test_registry_readiness.py::
test_no_model_runtime_is_installed_in_this_environment` —— 该测试断言当前
环境无 torch，impl venv 装有 torch 故必然失败（预期环境差异，非回归；
TASK-019 BLOCKED 分类的环境证据断言在基线环境仍成立）。
1100 项 = base `d11d927` 的 1077 项 + 本任务新增 23 项，项数吻合。
T1.1.1 R2 记录的既有 export 信号竞态本次未触发（随机）；该项与
`d11d927` 上的 STATUS 记录一致，归属与修复窗口由 Codex 裁决。

## 5. 重要发现：QML context property 初始化竞态（既有，非本任务引入）

实现中观察到：QML 绑定对 context property 的**首次求值**可能早于
wrapper 可解析，随机（≈75%）报 `Cannot read property … of null`；
固定 `PYTHONHASHSEED`、禁用 QML 磁盘缓存均不能消除，属引擎初始化层
（Qt/PySide6 6.11）时序问题。**既有页面同受影响**——master 的
smoke/测试噪音里长期存在 `AppShell.qml / PrimaryNavigationRail.qml …
of null`（T1.1.1 测试输出可见）；SettingsView 因绑定的是数据 model
（失败后无 notify 不重估、页面会空），首次做了系统性防御：

- 所有读取 null-guard；
- `Component.onCompleted`（此时 context 可靠可解析）内以 JS
  `signal.connect()` 命令式接线（绕过 Connections 名称匹配），并调用
  `settingsViewModel.refresh()`（`@Slot`）以 change-signal 驱动全部
  guard 绑定重估。

修复后 smoke 六连跑 SettingsView 相关错误 0（其他页面的既有噪音不在
本任务范围）。**建议 Codex 评估**：既有四页是否在后续 slice 统一采用
同一防御模式。

## 6. 未尽事项（非阻塞）

- 连接测试按钮（AC-NET-004 分阶段测试）不在 Rebaseline Plan 的
  T1.2.1 writes 范围，未实现；
- 其余设置类别（OCR/翻译/图片修复/排版/模型 GPU/任务并发/缓存/回收站/
  备份/Plugin）保持占位，属后续 slice；
- JSON store → SQLite 迁移随 T3.1.1。

## 7. 推荐下一步

Codex 非作者 Review（对照 §1 Scope 核 diff、§3 四项说明核实现、§4 复跑
验证命令），随后按 Rebaseline Plan 走集成与最终验证；集成窗口内由 Codex
决定 bootstrap 装配段的序列化处理。Review/集成完成前不合并 master、
不启动 T2.1.1。

## 8. R3 Blocker 修复（当前 delivery，优先阅读）

依据用户 R3 指令（"修复 B-001 provider profile 到 production registry 的
解析闭环；修复 B-002 network/proxy profile 到实际 transport 的注入；保留
并完善 B-003 的 pipeline settings 接线。先补 focused failing tests，再做
最小实现"）。TDD 顺序执行：**22 项红测试先行**（`git stash` 前基线：
22 failed / 16 passed），实现后全部转绿。

### 8.1 R3 改动面（`git diff ace633a..HEAD --stat` 可核）

| 文件 | Blocker | 内容 |
|---|---|---|
| `src/infrastructure/providers/runtime.py` | B-001+B-002 | **生产缝（核心）**：`_apply_profile_bindings()` 装配期投影——把绑定命中的用户 profile 数据复制到该 step 对应的固定 registry 槽（`translate` 按 `provider_type` 分叉 `openai-compatible-translation`/`sakura-local`，`ocr`/`detect` → vision provider；其余 step 无远程语义不投影），返回 `profile_id → registry_id` alias；禁用/未知 profile 不投影（resolve 端保持 fail-closed）；输入映射不可变、固定 id 绑定直通（既有形状回归保护）。`_network_profile_for()`/`_network_profile_from_settings()`：从镜像 `providers.networks` 段重建 `NetworkProfile` 填入 `OpenAiCompatibleConfig.network_profile`；id 无匹配 → None（client 落 direct 默认，语义与现状一致）。`build_provider_registry`/`build_provider_runtime` 新增 `provider_bindings` 参数（投影幂等） |
| `src/infrastructure/providers/registry.py` | B-001 | `ProviderRegistry` 新增 `binding_aliases` 字段；`resolve()` 入口翻译 profile id → registry id（readiness 与实例缓存仍按 registry id 键控；`resolve_binding` 的字符串/mapping 形状均经此路径覆盖） |
| `src/ui/viewmodels/settings/viewmodel.py` | B-003+ | 在 Codex 候选 `0b4e434` 形状上完善：mirror dict 增加 `provider_type`/`network_profile_id` 字段；新增 network profile mirror（`providers.networks` 段）及删除同步；`saveBinding` 增加 capability→step 名翻译（QML 传 capability 名 `translation`，生产行存 handler step 名 `translate`）与绑定目标存在性检查（装配前拒绝，不在 run 时才发现）；`bindings` Property 反向翻译回 capability 名供 QML 显示 |
| `src/bootstrap/app.py` | 接线 | `build_provider_runtime` 增加 `provider_bindings=_load_pipeline_defaults(conn)["provider_bindings"]`（一行；每次装配重新投影，profile 编辑后重装配自然生效） |
| `tests/providers/test_provider_binding_projection.py`（新，16 项） | B-001/B-002 | 字符串/mapping binding 解析、sakura/vision 槽分叉、fail-closed（未知/禁用 profile）、固定 id 直通、投影不新增 registry 条目、network profile 进 config、**transport 收到的即绑定的 socks5 profile**、缺省回落 direct、镜像不可变性 |
| `tests/ui_shell/test_settings_viewmodel.py`（改，8 项新增） | B-003 | provider/network mirror 形状与删除同步、mirror 永不携带 secret 值、capability→step 名持久化、绑定缺失 profile 拒绝、**VM mirror → 生产 `build_provider_runtime` 全链路联合测试**；`_STEPS` fixture 更正为生产 step 名（R1 用 capability 名掩盖了命名空间脱节） |

QML 无改动（`saveBinding` 对外签名与调用点不变）。

### 8.2 B-003 与 Codex 候选的关系

Codex 候选 `0b4e434`（`agent/codex/T1.2.1-integration`，未合并）的 mirror
思路被**保留**：VM 写入 profile 后同步投影到 `pipeline_defaults` 行的
`settings.providers.profiles` 段，删除时同步清理。R3 在其上加：
`provider_type`/`network_profile_id` 字段（B-001 槽位判定与 B-002 注入的
数据前提）、network profile mirror、capability/step 名翻译、绑定存在性
检查、删除同步（provider 与 network 两侧）。候选中的 red/green 测试
（`test_provider_profile_write_feeds_pipeline_runtime_settings`）由
`TestPipelineMirror::test_provider_save_mirrors_runtime_shape` 覆盖
（形状含新增字段）。

### 8.3 R3 验证记录

| 项 | 结果 | exit |
|---|---|---|
| 红基线（新测试先于实现） | 22 failed / 16 passed（failures 全部落在 B-001/B-002/B-003 断言） | 1 |
| focused（tests/ui_shell + tests/network + tests/providers） | **409 passed, 1 failed**（唯一失败=已知 torch 环境断言，见 §4.1） | 1 |
| full suite（impl venv） | **1123 passed, 1 failed**, 1 warning（66.9 s；唯一失败=同一 torch 环境断言；1124 = 基线 1100 + 新增 24，项数吻合；0 skipped——TLS 项在本 venv 全执行） | 1 |
| `compileall -q src tests` | 通过 | 0 |
| bootstrap smoke ×6 | **6/6 clean** | 0 |
| `git diff --check` | 干净 | 0 |

### 8.4 生产路径 probe（B-001/B-002/B-003 运行时证据）

临时脚本（已删，可按本节复现）：生产 DDL 建 `pipeline_defaults` 表并插入
VM mirror 形状的行 → bootstrap 生产装载函数 `_load_pipeline_settings` +
`_load_pipeline_defaults` → 生产 `build_provider_runtime(settings=…,
provider_bindings=…, transport=RecordingTransport())` →

```text
loaded settings sections: ['providers']
loaded bindings: {'translate': 'my-openai-profile'}
resolved provider config: openai-compatible-translation
    https://api.example.com/v1 gpt-test          ← B-001：profile id binding 命中固定槽
transport received network profile: corp-proxy socks5
    socks5://gateway.internal:1080 verify_tls=False
    bypass=['.internal', '127.0.0.1', 'localhost'] ← B-002：transport 收到的即绑定的网络 profile
PROBE PASS                                       ← B-003：全程仅由 SQLite 行装载（重启恢复）；镜像行与 alias 不含明文 secret
```

### 8.5 设计要点（Review 关注项）

- **投影在装配端，不在 UI 写死**：`pipeline_defaults` 行保持用户语义
  （step ← profile id 字符串），每次装配重新投影，无 stale 槽位；
  runtime.py 的模块定位（"one place decides which providers exist"）不破。
- **fail-closed 不变**：未知/禁用 profile 不投影 → resolve 报
  `ProviderUnavailable`（与 R1 探针一致）；固定 id 直通路径零改动。
- **脱敏不变**：镜像段与投影路径只携带 `credential_ref`；测试断言
  `_SECRET` 不在任何 mirror/错误消息/JSON dump 中。
- **范围依据**：`runtime.py`/`registry.py` 不在 R1 allowed paths 内，本次
  改动依据 review B-001 的裁决原文（"the provider profile-to-adapter/
  registry seam must be explicitly resolved in the approved integration
  scope"）与用户 R3 指令授权。`src/ports/**`、SQLite schema、QML 仍零改动。

### 8.6 R3 推荐下一步

Codex R3 非作者 Review：① 复跑 §8.3 验证表；② 复现 §8.4 probe；③ 对照
§8.1 核 diff 范围；④ 裁决 I-001 更正后的 focused 口径（160+6 skipped；
本 worktree venv 下 TLS 项实际执行故为 0 skipped——两种环境的 skip 差异
请知悉）。通过后走集成与最终验证；作者侧继续不合并 master、不启动
T2.1.1。
