---
task_id: TASK-006
author: ZCode
recipient: Codex（转 DeepSeek Harness 独立 Review）
base_commit: fb29dfedc496cde61cea9ea2558ef274e94eb49e
delivery_head: ba1e7695a1ad806a2640fe077811ade95d3b90d4
status: in_review
---

# Handoff：TASK-006（持久化与 Artifact 安全提交基础）

实现 head：`ba1e769`。提交列表（自 base `fb29dfe`）：

| commit | 说明 |
|---|---|
| `2727d3c` | docs(TASK-006): 按派单接管，Task 文件补记 in_progress（派单时文件仍为 proposed 的差异已记录） |
| `ba1e769` | feat(TASK-006): ports + sqlite/filesystem 基础设施 + tests/storage（reviewed_head） |

变更路径（全部在允许白名单内）：`src/ports/repositories/**`（3 模块 + __init__）、`src/infrastructure/sqlite/**`（connection/schema/migrator/backup/artifacts）、`src/infrastructure/filesystem/**`（managed_storage/integrity）、`tests/storage/**`（conftest + 4 测试文件，29 项）。`src/domain`、`src/ui`、`src/bootstrap`、`tests/core`、依赖清单、AGENTS、其他 Task：零触碰。

## 交付内容与 AC 对照

- **AC1 Schema/外键/索引/短事务/不可变路径 + current 引用**：**满足**。v1 迁移建最小表集（books/chapters/pages 最小 FK 链 + media_artifacts/artifact_revisions + schema_migrations/application_metadata/backup_records，D03 §16~18/§34）；`media_artifacts(current_revision_id, artifact_id) → artifact_revisions(artifact_revision_id, artifact_id)` 复合 DEFERRABLE 外键把 TASK-002 §2.1「current 必须指向同一 artifact 的 revision」编码进 SQL 层；`UNIQUE(artifact_id, revision_no)` + 复合 `UNIQUE(artifact_revision_id, artifact_id)`；索引 `idx_chapters_book_id`/`idx_pages_chapter_sort`/`idx_media_artifacts_page`/`idx_artifact_revisions_artifact`；提交走 `BEGIN IMMEDIATE` 短事务。
- **AC2 任一失败保留旧 current、DB 不指向不存在文件**：**满足**。`SqliteArtifactRepository.commit_revision` 严格按 TASK-002 §8.1：temp 写入 → sha256/size 验证（含调用方声明 hash 对比）→ 不可变路径原子发布（存在即拒，`ImmutablePathViolation`）→ `BEGIN IMMEDIATE` 内重读 current 做 compare-and-write；不匹配回 `INPUT_REVISION_CHANGED` 冲突、DB 异常回 `COMMIT_CONFLICT`，两者均保留旧 current 并把已发布文件作为 orphan 路径返回（无 DB 引用）；写失败 `ARTIFACT_WRITE_FAILED`、hash 不符 `ARTIFACT_HASH_MISMATCH` 均零 DB 痕迹。测试：失败矩阵 11 项全过（含注入 publish 后 DB 前崩溃，AC-ART-002）。
- **AC3 migration 版本/checksum/备份入口、新 Schema 旧 App 拒写**：**满足**。`MigrationRunner`：每迁移独立事务、checksum（sha256 of DDL）写入并在重放时校验篡改、pending 应用前调用备份钩子（`SqliteBackupService`，SQLite backup API + BackupRecord，D03 §34.3、D07 §48）；`open_database` 对存储版本 > 已知最新版本的库以 `PRAGMA query_only` 只读打开并报 `TOO_NEW`，写入即失败（AC-DB-005）；`schema_migrations` + `application_metadata.schema_version` 双记录（D03 §34.1/34.2）。
- **AC4 Handoff/Review/集成**：本 Handoff 交付；状态置 `in_review`，done 待 DeepSeek 非作者 Review + Codex 集成。

## 验证证据

完整记录见 [verification/TASK-006/author-verification.md](../../verification/TASK-006/author-verification.md)。摘要：

| AC/场景 | 实际命令 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| 存储套件（任务计划命令） | `python -m pytest tests/storage -v` | py3.12.3 任务环境；head `ba1e769` | PASS，退出码 0，29 passed | author-verification.md #1 |
| 全量回归（含 TASK-005 架构守卫） | `PYTHONPATH=src python -m pytest tests` | 同上 | PASS，退出码 0，35 passed | 同上 #2 |
| whitespace 与范围 | `git diff --check fb29dfe ba1e769 --`；`git status --short` | 本 worktree | PASS（0 / 仅允许路径） | 同上 #3~4 |
| 架构边界 | grep ports 层禁用 import；守卫测试 | 同上 | PASS（ports 无 sqlite3/PySide6；core 守卫 6 passed） | 同上 |
| WAL/foreign_keys PRAGMA、非法 FK 拒绝、跨 artifact current 拒绝 | `tests/storage/test_db_integrity.py` | 同上 | PASS（5 tests；AC-DB-001/002） | pytest 输出 |
| Unicode 路径、源文件 Hash 不变、不可变发布 | `tests/storage/test_managed_storage.py` | 同上 | PASS（6 tests；D07 §35、D03 §43） | pytest 输出 |

NOT_RUN / N/A（详见 author-verification）：WAL 多连接并发竞争（TASK-011/集成验证）；v1→v2 升级链与迁移失败恢复端到端（TASK-021）；Restore/清理对 current/pinned 的保护执行（TASK-021，本切片提供 pinned/完整性字段与备份入口）；性能（无 AC）；Region/Constraint Revision 与 StepResultCandidate 持久化（范围决策：本切片无使用方，Region 属 TASK-008、Pipeline 属 TASK-011）。

## 接收方式与复审建议

- 分支 / worktree：`agent/zcode/TASK-006-persistence-artifact` / `G:/CODEX/New Manga.worktrees/TASK-006-zcode`；reviewed_head 固定 `ba1e769`。
- 复现：`python -m pytest tests/storage -v`（conftest 自注入 `src`，无需环境变量）；全量加 `PYTHONPATH=src`。
- Reviewer 建议重点：① 对照 TASK-002 §2.1/§8.1/§10 核对 `src/infrastructure/sqlite/artifacts.py` 的提交流程与错误码；② 复合 deferred FK 是否完整覆盖「current 同属」不变量（含首版 NULL 语义）；③ 失败注入测试是否真实模拟契约失败点；④ orphan 语义（返回路径、不清理）是否与 §8.1「进入 orphan 清理」一致且不越权实现清理；⑤ `SchemaTooNew` 只读门是否符合 AC-DB-005「拒绝写入或只读」；⑥ pre-migration 备份在 v0 时无 BackupRecord 行的取舍（D03 §34.3）。

## 风险与遗留

1. 首版只有 v1 迁移；「新 Schema 被旧 App 打开」以人为构造 version=2 验证，真实升级链待后续版本出现（TASK-021 端到端）。
2. pre-migration 备份的 v0 场景无 BackupRecord 行（表尚不存在）——已由测试固化该语义；若 Codex 要求改挂 ApplicationMetadata 或文件侧清单，需范围变更。
3. `publish` 使用 `os.replace`（同卷原子）；未做目录 fsync（Windows 限制），崩溃窗口由 orphan 检测覆盖，诊断清理属 TASK-021。
4. `busy_timeout=5000` 与 WAL 已配置但并发行为未测（见 NOT_RUN）。
5. 本切片不释放 TASK-007~027；测试环境沿用 `G:/CODEX/New Manga.task-envs/TASK-005-py312`（未新建、未共享给他人写入）。
