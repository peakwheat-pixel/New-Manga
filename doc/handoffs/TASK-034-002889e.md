---
task_id: TASK-034
author: DeepSeek Harness
recipient: Codex（独立 Review；集成由 Codex 执行）
base_commit: b34b27e2bc7c1dbd4c3b15a91f08e158d966f365
delivery_head: 002889e
status: in_review
scope: 尾项切片（AC ① 第 2 步 route_policy 收敛 + 强制项 R-01）
---

# Handoff：TASK-034 尾项切片（AC ① 第 2 步 + R-01）

## 交付结果

**范围**：按 [AC ① 裁决](../../doc/reviews/TASK-034-ac1-route-policy-ruling.md)（R-1～R-7、`src/` 3 文件范围批准）落地唯一入口，并关闭强制项 **R-01**。

**提交**（分支 `agent/deepseek/TASK-034-test-layer-hardening`，工作区 `G:/CODEX/New Manga.worktrees/TASK-034-deepseek`；固定 base `b34b27e`；先把 master 快进合入到 `34823b9`）：

| commit | 内容 |
|---|---|
| `34823b9` | `git merge master` 快进（master 现含已集成的 AC ②③④⑤：集成 `b6051e5`、元数据 `34823b9`） |
| `002889e` | 尾项切片（13 文件、`+494/−75`）：3 个 `src/` 文件 + `tests/providers/test_route_policy_settings.py`（新增）+ `tests/providers/test_handlers_pipeline.py` + `tests/reading_export/{conftest,reading_export_helpers,test_qml_contract,test_viewmodels}.py` + `verification/TASK-034/**` |

`delivery_head = 002889e`。**未 push、未合并 master（只把 master 合入本分支）。**

**逐项结果**：

| 项 | 状态 | 摘要 |
|---|---|---|
| **AC ① 第 2 步** | **完成** | `RoutePolicy.from_settings(settings, *, default)` 成为 `inpaint.route_policy` 的**唯一**解释；两处私有 `_route_policy` 删除；`build_production_handlers` 的第三份字面量改为复用 `DEFAULT_ROUTE_POLICY`；`DEFAULT_ROUTE_POLICY` 对齐为 `allowed=("simple-fill","edge-bleed")`、`fallback=()`、`color_route=None`、`requirements={}` |
| R-1 显式默认 | 完成 | 段/键缺失 → 返回调用方传入的 `default`（装配 `DEFAULT_ROUTE_POLICY`、运行 `deps.route_policy`），身份等价（`is`）已验证 |
| R-2 非 mapping | 完成 | 共享解析器抛 `ProviderInputError("inpaint.route_policy must be a mapping", stage="inpaint")`；装配路径与运行路径一致（各有回归） |
| R-3 / R-4 / R-5 | 完成 | `allowed_routes` 回落 `default.allowed_routes`；`fallback_routes=()`；`requirements={}` 且保留 `bool(value)`（R-7 输入收紧保持冻结） |
| R-6 Option B | 完成 | `color_route` 缺失/空白 → `None`；显式指定仍生效并并入 allowed（AC-INPAINT-004 正面要求有测试） |
| **R-01（强制项）** | **完成** | `requires_pyside6` 移入唯一命名 `reading_export_helpers` 并显式导入；全仓已无裸 `conftest` 导入；**十种收集顺序全部 exit 0**（修复前六种伙伴目录顺序为 exit 2 / 2 errors） |
| 路由判定算法 | **未改** | `decide_route`/`acceptable_routes`/`preferred_route_order`/`route_gate`/`RouterFeatures` 零改动（裁决禁止项） |

## 必须记录的裁决后可观测变化

1. **`runtime.status()["route_policy"]`**：裁决前 `allowed=[brushnet, edge-bleed, simple-fill]`、`color_route="brushnet"`、`requirements={"brushnet": False, "flux-fill": False}`（历史值见 TASK-019 Review V11）→ 裁决后 `allowed=('simple-fill','edge-bleed')`、`color_route=None`、`requirements={}`。属**记录刷新**（Option B 的预期结果），已由测试锁定。
2. **彩色/复杂场景候选理由**：`brushnet` 由 `not implemented: …` 变为 `not in the configured route policy`；其余候选理由不变。
3. **R-2 是唯一可能让应用启动失败的语义变化**：装配路径由"静默回落"改为"启动期即报配置错误"（裁决第 R-2 行要求显式登记）。用户需要知道：写错的 `inpaint.route_policy` 不再被无声忽略。

**四种特征组合的决策不变性（实测）**：彩色 webtoon、高复杂度 → **BLOCKED ×2（前后一致）**；线稿、普通 → **RUN `edge-bleed`（前后一致）**。四组的 `decision` 与 `route` 逐项相同，**只有理由与 allowed 集合变化**。

**⚠ 与裁决措辞的一处差异（如实登记，请 Reviewer 裁定）**：裁决第 62 行写"四种特征组合…裁决前后**均 BLOCKED**"，但实测**线稿**与**普通**两组在两侧都选择依赖无关基线 `edge-bleed`（RUN，非 BLOCKED）。实质要求（"选中哪条路线"未变）对四组全部成立。若裁决方认为这两组也必须 BLOCKED，那属于**判定算法**变更（本切片明确禁止），需另立裁决；本切片未自行接受或调整。

## 验证证据

环境：Windows 11 `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；全部 `-p no:cacheprovider`。完整记录：[`verification/TASK-034/ac1-step2-author-verification.md`](../../verification/TASK-034/ac1-step2-author-verification.md)。

| # | 场景 | 命令 / 步骤 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | mandated 三套件 | `pytest tests/providers tests/core tests/reading_export -q -rs` | **217 passed / 0 skipped**（退出码 0） | 取证 §5 |
| 2 | AC ① 矩阵 | `pytest tests/providers/test_route_policy_settings.py -q` | **22 passed**（12 类输入 + 2 条调用点回归 + 默认/配置锁定） | 取证 §2 |
| 3 | 运行调用点回归 | `pytest tests/providers/test_handlers_pipeline.py -q` | **13 passed**（含 Run 级策略采用 + 非 mapping 报错） | 取证 §2 行 11/12 |
| 4 | R-01 顺序对照 | 十种顺序（两文件参数顺序 + 六伙伴目录 + 单独） | 修复前 6 种 exit 2 / 2 errors；修复后**全部 exit 0** | `r01-collection-orders-{before,after}.log` |
| 5 | 全仓 | `pytest -q -p no:cacheprovider -rs` | **706 passed / 6 skipped ×4 次**（退出码 0；skip 全为既有 `tests/network` 的 `openssl unavailable`） | `ac1-step2-full-suite.log` |
| 6 | 决策不变性 + 可观测变化 | 只读探针（旧默认按 `b34b27e` 字面量重建） | 四组决策逐项未变；`brushnet` 理由变化；`status()` 前后值 | `ac1-decision-invariance.txt` |
| 7 | 逐目录计数 | `34823b9` 导出树 vs HEAD | providers 110→134、core 18→18、reading_export 65→65、全仓 682→706 | `ac1-step2-counts.txt` |
| 8 | 边界 | `git diff --name-only 34823b9..HEAD`；`git diff --check` | 3 个 `src/` 文件（恰为批准清单）+ `tests/**` + `verification/TASK-034/**`；越界 0；`--check` 退出码 0 | 取证 §6 |

## 接收方式

1. **分支/工作区**：`agent/deepseek/TASK-034-test-layer-hardening` @ `002889e`；`G:/CODEX/New Manga.worktrees/TASK-034-deepseek`。固定 base `b34b27e`。
2. **复现命令**：
   ```powershell
   $env:PYTHONDONTWRITEBYTECODE='1'
   $py = "G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe"
   & $py -m pytest tests/providers tests/core tests/reading_export -q -p no:cacheprovider -rs   # 217 passed / 0 skipped
   & $py -m pytest tests/reading_export tests/editing -q -p no:cacheprovider                    # exit 0（R-01）
   & $py -m pytest -q -p no:cacheprovider -rs                                                   # 706 passed / 6 skipped
   ```
3. **不需要**额外数据、依赖、端点或权重。
4. **Reviewer 重点检查**：
   - `from_settings` 是否真的成为唯一入口（两处私有实现已删、第三份字面量已消）；
   - R-2 在装配路径"由静默变报错"的登记是否充分（用户在启动期会看到配置错误）；
   - Option B 的可观测变化（`status()`、候选理由）与"决策未变"的证据强度；
   - 上节 ⚠ 与裁决措辞的差异（线稿/普通为 RUN）如何裁定；
   - R-01 的十种顺序对照是否满足"两种参数顺序 + 一个伙伴目录顺序"的要求。

## 风险与遗留

- **⚠ 裁决措辞差异**（见上）：需要 Reviewer/裁决方明确是"以实测为准（决策不变即满足）"还是"必须四组全 BLOCKED（需另立判定算法裁决）"。
- **R-2 的启动期影响**：配置写错的应用现在启动即失败；若产品希望"启动降级 + 明确告警"，需另立裁决（本切片按裁决实现 fail-loud）。
- R-07（输入校验：字符串 `allowed_routes` 逐字符展开、非 bool `requirements` 强制）保持**冻结登记**，未在本切片扩张。
- R-05（导出 viewmodel 发布顺序）、R-06（webtoon flaky 未复现）状态不变，仍按上一轮取证登记。
- 回退：`git revert 002889e`（3 个 `src/` 文件 + 测试/文档）；无 Schema/依赖/数据影响。
