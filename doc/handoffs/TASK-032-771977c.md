---
task_id: TASK-032
author: DeepSeek Harness
recipient: Codex（独立 Review；集成由 Codex 执行）
base_commit: ca17d454edc03bc1b057e9e3fc463c81da652e53
delivery_head: 771977c
status: in_review
---

# Handoff：TASK-032 修复 SFX Policy Gate 缺失 region_type 前置（F-1）

## 交付结果

**范围**：规划阶段的 SFX Policy Gate 补上 `region_type` 前置；统一缺字段口径；补"真实默认值"回归用例。

**提交**（分支 `agent/deepseek/TASK-032-sfx-policy-gate-region-type`，工作区 `G:/CODEX/New Manga.worktrees/TASK-032-deepseek`；固定 base `ca17d45`，分支起点 `c004d4e` = Codex 的释放/改派纯文档提交）：

| commit | 内容 |
|---|---|
| `a5823d1` | 开工：`doc/tasks/TASK-032.md` 置 `in_progress` |
| `771977c` | 修复 + 回归用例（4 文件、`+341/−10`）：`src/application/tasks/service.py`、`src/infrastructure/sqlite/pipeline.py`、`tests/pipeline/test_pipeline.py`、`tests/pipeline/test_pipeline_production_seam.py` |

`delivery_head = 771977c`。**未 push、未合并 master**。

**改动**：

- `_decide_step` 的 SFX 判定改为 `region is not None and step_type in _SFX_GATED_STEPS and _sfx_gate_suppresses(region)`；`_sfx_gate_suppresses` **只在 `region_type == "sfx"` 时**才把策略映射为决定，并**委托**给 Translate Step 早已使用的 `application.translation.context.gate.decide_sfx_translation`（F-1 根因是同一规则两份实现、彼此漂移）。
- `_effective_sfx_policy`：SFX 的 `sfx_policy` 缺失/空白 → 回落 `skip`（D03 §7）。
- `src/infrastructure/sqlite/pipeline.py::_region()` 的 `sfx_policy` 缺省 `"translate"` → `SfxPolicy.SKIP.value`（与实体/Schema 一致）。

**语义边界（请 Reviewer 重点确认）**：

1. 只有 `region_type == "sfx"` 参与该 gate；非 SFX 类型**任何**策略取值都不进入判定。
2. SFX 缺字段/空白 → `skip`；非 SFX 不参与。
3. 取值域外（非 `skip`/`manual`/`translate`）的 SFX 策略由共享 gate 在**规划阶段**拒绝（`ValueError`）——与 Translate Step 既有校验一致（旧代码会在步骤执行时失败）。已由测试固定并在此显式声明。
4. `sfx_policy` 的默认值语义（`skip`）、Schema、依赖清单、pipeline seam 本体**均未改动**。

**AC 对照**（完整证据见 [`verification/TASK-032/author-verification.md`](../../verification/TASK-032/author-verification.md)）：

- ① 仅 SFX 应用策略（`skip`/`manual` → `SKIP_POLICY("sfx_skip")`；`translate` → 正常链路）：**PASS**（含真实 SQLite 用例）。
- ② 非 SFX 不受影响：**PASS**（5 类型 × 3 策略 = 15 例全 `RUN`，其中 `speech × skip` 即 F-1 的真实默认组合）。
- ③ 缺字段口径唯一：**PASS**（实体/Schema/快照反序列化三方实测均为 `skip`；SFX 空值回落 `skip` 已测）。**残留见 N-2**。
- ④ 真实默认值回归用例 + 既有 `sfx` 用例不变：**PASS**（真实 SQLite 用例插入 region 时不写 `sfx_policy`，取 Schema 默认）。
- ⑤ 测试与分列：**PASS**（见下）。

## 验证证据

环境：Windows `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；全部 `-p no:cacheprovider`。

| # | 场景 | 命令 / 步骤 | 结果 | 证据 |
|---|---|---|---|---|
| 0 | 基线 | `c004d4e` 导出树全仓 `python -m pytest -q -p no:cacheprovider` | **641 passed, 6 skipped**（退出码 0） | — |
| 1 | 主套件 | `python -m pytest tests/pipeline -q -p no:cacheprovider -rs` | **60 passed / 0 skipped**（退出码 0；改动前 30 例） | `pytest-pipeline.log` |
| 2 | 强制回归 | `python -m pytest tests/core tests/storage tests/providers -q -p no:cacheprovider -rs` | **159 passed / 0 skipped**（退出码 0） | `pytest-regression.log` |
| 3 | 全仓 | `python -m pytest -q -p no:cacheprovider -rs`（2 次） | **671 passed / 6 skipped** ×2（退出码 0；6 条 skip 全为既有 `tests/network` 的 `openssl unavailable`） | `pytest-full.log` |
| 4 | 判别力 | 修前 `src/`（`c004d4e` 导出树）+ 新测试 → `tests/pipeline` | **17 failed / 43 passed**（退出码 1）→ 新用例对旧代码有判别力 | `discriminative-prefix.log` |
| 5 | 默认值三方实测 | 实体 / Schema（插入时不写字段）/ 快照反序列化 | `skip` / `skip` / `skip`（修前反序列化为 `translate`） | `schema-defaults.txt` |
| 6 | 边界与空白 | `git diff --name-only c004d4e..HEAD` + 允许范围正则；`git diff --check ca17d45..HEAD` | 5 个路径全在允许范围、越界 **0**；全范围禁止路径命中 **0**；**`--check` 退出码 2（Codex 集成时更正，Review R-01）**：命中项全部来自 `verification/TASK-032/discriminative-prefix.log` —— 该文件是 pytest 原始输出捕获、含断言 diff 的行尾填充空格；源码/测试/文档本身**无空白问题** | `changed-paths.txt` |

**明确未运行/未处理**：渲染层同类缺陷（N-1，越界）、`RegionSnapshot` 字段级默认（N-2，越界）、真实模型/端点端到端（环境限制，与本 Task 无关）；已登记的 `tests/reading_export` 低频 flaky **未改动、未新增 skip**，本 Task 不把任何 flaky 记为通过。

## 越界发现（只登记，未修改）

| ID | 级别 | 位置 | 内容与影响 | 建议 |
|---|---|---|---|---|
| **N-1** | P1（建议） | `src/application/rendering/service.py:219,238`、`:347-351` | **同类缺陷仍在渲染层**：只按 `SfxPolicy(stored.sfx_policy)` 判定、无 `region_type` 前置；既有测试以**真实默认形态**（`Region` 默认 `region_type='speech'` + 策略 `skip`/`manual`）固定了该行为（`tests/rendering/test_rerender.py:180-199`、`:246-264`、`:458-473`）。⇒ 规划已修好，但普通 speech Region 的**渲染执行**仍被拦截。允许路径不含 `src/application/rendering/**` | 请 Codex 决定并入 TASK-033（完整链收口）或另立 Task；证据可直接复跑 `tests/rendering/test_rerender.py` |
| **N-2** | P2 | `src/domain/tasks/models.py:182` | `RegionSnapshot.sfx_policy` 字段默认仍为 `"translate"`（实测），生产路径已通过显式传值/反序列化默认消除影响，但直接构造仍会得到 `translate`；`src/domain/**` 不在允许路径 | 后续切片统一为 `SfxPolicy.SKIP.value`，或由 Codex 明确接受 |
| **N-3** | — | `src/application/tasks/service.py` | 已声明的行为变化：取值域外策略在规划阶段被拒绝（`ValueError`），与执行层一致；Schema 只允许三个取值 | 无需动作，已在取证 §1/§4 声明并由测试固定 |

## 接收方式

1. **分支/工作区**：`agent/deepseek/TASK-032-sfx-policy-gate-region-type` @ `771977c`；`G:/CODEX/New Manga.worktrees/TASK-032-deepseek`。固定 base `ca17d45`。
2. **复现命令**（PowerShell，仓库根）：
   ```powershell
   $env:PYTHONDONTWRITEBYTECODE='1'
   $py = "G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe"
   & $py -m pytest tests/pipeline -q -p no:cacheprovider -rs
   & $py -m pytest tests/core tests/storage tests/providers -q -p no:cacheprovider -rs
   & $py -m pytest -q -p no:cacheprovider -rs
   ```
3. **不需要**额外数据、依赖、端点或权重；`tests/pipeline` 无外网、无 UI 交互。
4. **Reviewer 重点检查**：
   - `_sfx_gate_suppresses` 的委托是否覆盖了 Planner 与 Translate Step 的全部口径（含 N-3 的取值域外行为）；
   - "非 SFX 不参与判定"是否在**所有**命令路径上成立（`translate_*`/`reocr_*`/`reinpaint_*`/`rerender_*` 及 region/page 两种 scope）；
   - 快照默认改动（`skip`）是否影响任何既有消费方（本 Task 已核对：`_region_dict`/`_region` 往返与既有用例均未受影响）；
   - N-1/N-2 是否接受为后续切片范围。

## 风险与遗留

- **N-1 是本次修复的可见影响边界**：本 Task 的 AC 只要求规划阶段（③/④），因此交付按 AC 完成；但 F-1 的**用户可见症状**在渲染层仍可能存在，需上述裁决。
- 未知取值在规划阶段抛 `ValueError`（N-3）：若产品希望"未知取值静默按默认 `skip`"，需要用户裁决后由后续切片调整（本 Task 保持与执行层一致的严格校验）。
- 本 Task 未触碰 Schema/migration/依赖清单/seam/`AGENTS.md`/其他 Task；无数据迁移，回退 = 丢弃分支或 `git revert 771977c a5823d1`，行为回到 `641 passed` 基线。
- 已登记的 `tests/reading_export` 两个 flaky 仍在（属于测试基础设施，任务明确禁止顺手修复）。
