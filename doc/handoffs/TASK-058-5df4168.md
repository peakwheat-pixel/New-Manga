# TASK-058 Handoff：应用退出时排空 workbench 运行线程（P-10）

- 作者：ZCode（全权窗口 W11 自建切片，授权 97a69a9）
- 实现 commit：`5df4168`（worktree `G:/CODEX/New Manga.worktrees/TASK-058-zcode`，分支 `agent/zcode/TASK-058-graceful-shutdown-drain`，base `726c1bb`）
- Task 文件：`doc/tasks/TASK-058.md`

## 返修记录

- R-001（独立 Review）：测试文件不自包含，单文件跑缺 sys.path 注入 → `ebfc87a` 模块级 SRC_ROOT 注入（同 test_bootstrap.py 同款）；复验三形态（单文件/目录-k/全仓 911 passed）全绿，`verification/TASK-058/{standalone-file,directory-k,run06}.log`。实现 commit 最终以 **ebfc87a** 为准。

## 变更面（白名单内）

| 文件 | 变更 |
|---|---|
| `src/bootstrap/app.py` | 新增 `_shutdown_services(services)`：先 `services.workbench.shutdown()` 再 `services.conn.close()`；`main()` 两处收口（正常退出尾部 + bootstrap 失败 except 分支）改经该函数 |
| `tests/core/test_shutdown_drain.py` | 新增 3 用例（见下） |
| `doc/tasks/TASK-058.md` | 自建 Task（proposed→in_progress→done 走查） |

未动：`src/ui/viewmodels/workbench/**`、`src/ui/qml/**`、Schema、requirements。

## 设计要点

- **复用既有语义**：`WorkbenchViewModel.shutdown`（timer.stop + RunController.shutdown）在 `src/` 内原本零调用者（P-10 的缺口是"没接线"，不是语义缺失）；`RunController.shutdown(wait_ms=5000)` 已含 cancel_requested + bounded wait + 不强杀（R-001 注释）。
- **顺序约束**：TASK-048 起 worker 与主线程共享同一 SQLite 连接，排空必须先于 `conn.close()`。
- **两条退出路径全覆盖**：正常 exec 尾部与 bootstrap 失败 except 分支（后者 workbench VM 已在 `assemble_services` 内构造，shutdown 为安全 no-op）。

## 测试证据（同一 shell + TASK-012-py312 venv，PowerShell、PYTHONDONTWRITEBYTECODE=1、-p no:cacheprovider）

| 项 | 结果 |
|---|---|
| 定向 `pytest tests/core -k shutdown_drain` | 3 passed, EXIT=0 |
| 全仓 ×5（`pytest tests -q`） | 每次 `911 passed`（基线 908 + 新 3），EXIT=0 ×5，`verification/TASK-058/run01..05.log` |
| 修前判别力（detached 树 @726c1bb + 修后测试文件） | 3 failed（`_shutdown_services` 不存在，import 级），EXIT=1，`verification/TASK-058/prefix-discriminator.log` |
| skip/xfail | 新增 0；无既有断言放宽 |

## 判别力声明

- **计判别**：用例 1（顺序契约）与全部用例修前 import 失败/失败（3/3 red）。
- **不计判别**：`test_workbench_vm_shutdown_is_idempotent` 钉既有 VM 幂等语义（已在 docstring 声明 pinning-only）；run 的 cancel_requested 循环语义为既有覆盖（`tests/workbench/test_workbench_viewmodel.py`），本切片未重复钉。
- AC2 用例等待 run 离开 PENDING 再排空（规避 worker 启动竞态）；无 provider 的生产 executor fail fast，run 可能已 terminal 后才 drain——组合路径（真实装配 + 真实 VM run + 真实 drain + close）仍被端到端钉住。

## 已知限制（非阻塞）

- R-001（shutdown wait 超时不强杀）语义沿用：排空上限 5s，超时后连接仍会关闭（与修前行为一致，不引入新的挂起风险）；安全边界协议留 TASK-047 后续。
