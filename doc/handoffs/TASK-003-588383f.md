---
task_id: TASK-003
author: DeepSeek Harness
recipient: Codex
base_commit: 9472df5c44a1bad24cda639201b0824a9ea5eecc
delivery_head: 588383f62006a4ab89de1a42bbdabe0bf9d906ae
status: awaiting_review
---

# Handoff：TASK-003 验收规格与测试素材规范

交付日期：2026-09-14（Asia/Shanghai）。本轮只交付验收方法/环境/证据规范、Fixture 素材清单与 F-08 状态边；没有创建应用源码、SQL、迁移或功能实现，TASK-004～TASK-027 均未启动。Owner 为 DeepSeek Harness，Reviewer 为 Codex——**本 Handoff 不是自我批准**。

## 固定审查对象

- `base_commit = 9472df5c44a1bad24cda639201b0824a9ea5eecc`
- `reviewed_head = 588383f62006a4ab89de1a42bbdabe0bf9d906ae`
- 作者分支：`agent/deepseek/TASK-003-verification-spec`；工作区 `G:/CODEX/New Manga.worktrees/TASK-003-deepseek`
- Review 报告：`doc/reviews/TASK-003-<short-head>.md`；分支继续推进不能替代固定 head 复核。

## 交付结果

提交列表：

~~~text
9472df5 docs: authorize TASK-003 verification design      （基线）
fe87c53 docs: bind TASK-003 execution baseline             （执行基线绑定）
588383f docs(TASK-003): add acceptance and fixture spec, close F-08   （内容交付 = reviewed_head）
~~~

变更路径（8 个，全部落在 TASK-003 allowed_paths 内，`git diff --stat 9472df5 588383f`：+504/−16）：

| 路径 | 变更 |
|---|---|
| `doc/verification-plan/TASK-003_ACCEPTANCE_AND_FIXTURE_SPEC.md` | 新增 226 行：验收方法、权威边界、结果枚举、标识体系、环境档位、证据口径、性能协议、模型阈值状态、`ACG-*` 执行方式、主题级执行方式（61 主题覆盖 185 条 AC） |
| `doc/fixtures/MANIFEST.md` | 新增 82 行：必需字段定义、状态枚举、19 条素材/数据集条目（14 功能 + 5 数据集）、生成与校验规则、许可合规 |
| `verification/TASK-003/verify.ps1` | 新增 175 行：9 组只读检查 |
| `doc/13_ACCEPTANCE_TRACEABILITY.md` | 组级表改用 `ACG-*` 稳定标识；新增指向验收规范与 Fixture Manifest 的回链；不复制验收方法 |
| `doc/02_TECHNICAL_ARCHITECTURE_.md` | F-08：`Blocked --> Cancelled: Stop`（+1 行） |
| `doc/04_USER_FLOW.md` | F-08：`Blocked --> Cancelled: 停止`（+1 行） |
| `doc/11_ARCHITECTURE_MAPS.md` | F-08：`blocked --> cancelled: stop`（+1 行） |
| `doc/tasks/TASK-003.md` | `status: ready → in_progress`（本次元数据提交再改为 `in_review`） |

主要结果：

1. **单一权威规范**：`doc/verification-plan/...` 定义方法/环境/证据/阈值状态；D08 保持产品验收目标与优先级的唯一权威；D13 只保留追踪回链（两者都没有被复制第二套产品定义）。
2. **稳定组标识**：D08 中 10 个无编号子项的主题获得 `ACG-*` 标识（`ACG-DPI/SMOKE/OFFLINE/PRIVACY/SET/PROV/ERROR/ERRUI/AUTO/SYNC`），与 D13 一致且唯一。
3. **结果枚举固定**为 `PASS / FAIL / BLOCKED / NOT_RUN / N/A`，并写明"未执行不得写 PASS""文档证据不能证明产品行为"。
4. **性能协议固定**：普通操作 3 次预热后测量 20 次；AI 步骤 1 次预热后测量 5 次；均报 P95，环境档位 ENV-B/ENV-D，禁止合并 AI 步骤平均值。
5. **模型阈值状态**：当前无来源支持的模型质量数值全部标记 `UNAPPROVED_THRESHOLD`，未虚构数值；Mock 输出不得作为质量或性能证据。
6. **Fixture 清单**：19 条素材/数据集均为 `planned`、Hash 为 `NOT_AVAILABLE`，未提交任何图片；许可待定的素材不得用于 PASS。
7. **F-08 关闭**：D02 §5.3、D04 §30.1、D11 §10 各增加一条与冻结契约 §5.2/§6.1 一致的 `Blocked → Cancelled` 边，未引入其他状态或转换（脚本以新增行数 = 3 且全部匹配该边进行校验）。

## Acceptance Criteria 对照

| AC | 作者结论 | 证据 |
|---|---|---|
| 1：审阅 185 条与组级要求，补齐稳定标识、执行方式、环境与证据口径；不降低 P0/P1 | DONE | 规范 §3/§4/§5/§8/§9；185 条编号与优先级经脚本与基线逐字比对未变；10 个 `ACG-*` |
| 2：为日漫/韩漫/Webtoon/损坏图/透明 PNG/Unicode/重复图/大 metadata 提供清单、许可与生成方案；未取得标缺失 | DONE | `doc/fixtures/MANIFEST.md` 19 条（含上述全部场景与 D07 §99 数据集 A～E）；状态 `planned`，Hash `NOT_AVAILABLE` |
| 3：模型质量评估方法与待批准阈值；性能固定次数/环境/P95；未测量不填结果 | DONE | 规范 §6（3/20、1/5、P95、ENV-B/D）与 §7（四类指标 + `UNAPPROVED_THRESHOLD`） |
| 4：关闭 F-08，仅在 D02 §5.3、D04 §30.1、D11 §10 补齐 `Blocked → Cancelled` | DONE | 三处各 +1 行；`git diff` 新增行校验 |
| 5：交付 Handoff、测试/审阅记录与未完成项，经非作者独立 Review 与集成验证后才能 done | PARTIAL（正确） | 本 Handoff 与验证记录已交付；独立 Review 属 Codex，`integration_commit` 仍为空，Task 不得标 done |

## 验证证据

环境：Windows `10.0.26200`；PowerShell `7.6.6`；Git `2.52.0.windows.1`；CPU AMD Ryzen 9 7950X3D；RAM 31.6 GB。未安装项目依赖，未运行模型或 GPU 任务。

| 检查 | 命令 | 环境 / commit | 结果 |
|---|---|---|---|
| 交付验证脚本 | `pwsh -NoProfile -File ./verification/TASK-003/verify.ps1` | `588383f`（干净工作区） | **PASS，退出码 0**（8 条输出见下） |
| 空白错误 | `git diff --check 9472df5 HEAD --` | 同上 | PASS，退出码 0，无输出 |
| 工作区洁净 | `git status --short --branch` | 同上 | PASS，仅分支行 |
| 变更范围 | `git diff --stat 9472df5 588383f` | 同上 | 8 路径，全部在 allowed_paths |

脚本实际输出：

~~~text
PASS: changed paths authorized; TASK-001/002 unchanged; other 24 tasks frozen
PASS: all 185 AC IDs, priorities and titles unchanged and unique
PASS: 10 unique ACG identifiers, consistent between spec and D13 (ACG-AUTO, ACG-DPI, ACG-ERROR, ACG-ERRUI, ACG-OFFLINE, ACG-PRIVACY, ACG-PROV, ACG-SET, ACG-SMOKE, ACG-SYNC)
PASS: fixture manifest fields, states and 19 inventory rows present; no fabricated hashes
PASS: F-08 closed with exactly one Blocked-to-Cancelled edge in each of D02/D04/D11
PASS: 520 local links and all code fences valid
PASS: result enum, performance protocol and UNAPPROVED_THRESHOLD discipline present
PASS: git diff --check; owner-authored guards only, independent Review still required
~~~

**该脚本由 Owner 编写，是回归护栏，不构成独立批准。**

## 未执行项

| 项 | 状态 | 原因 |
|---|---|---|
| 产品 / 应用端到端测试 | `N/A` | 仓库无应用源码与可执行产物（D08/D13 一致） |
| 性能与容量实测 | `NOT_RUN` | 无实现、无 ENV-B/ENV-D 基准数据；本 Task 不执行模型实验 |
| 模型质量评估 | `BLOCKED` | 素材未取得（全部 `planned`）、阈值 `UNAPPROVED_THRESHOLD` 未获批 |
| 素材生成与 Hash 登记 | `NOT_RUN` | 属后续已授权 Task；本轮只交付方案 |
| Mermaid 渲染校验 | `NOT_RUN` | 仅做围栏结构检查，未执行渲染器或语法解析 |
| SQL / 迁移 / 依赖 | `N/A` | TASK-003 明确禁止生成 |

## 风险与遗留

- Fixture 素材全部未取得；在素材与许可确认前，任何依赖真实素材的质量结论只能记 `BLOCKED`/`NOT_RUN`。
- 模型质量阈值仍为 `UNAPPROVED_THRESHOLD`，需要用户批准并同步 D08 后才能判 PASS。
- F-08 的状态边属派生视图同步，需 Codex 在集成后确认三份文档与契约 §5.2/§6.1 仍一致。
- 规范 §9 的主题级执行方式是方法层映射，不改变 D08 的任何产品要求；若 Codex 认为某主题需要更细的逐条用例，应在 TASK-003 复审中以 finding 提出。
- TASK-004～TASK-027 保持未启动；本 Task 不释放任何后续 Task。

## 接收与复现

唯一 Git common directory：`G:/CODEX/New Manga/.git`；Owner worktree `G:/CODEX/New Manga.worktrees/TASK-003-deepseek`，分支 `agent/deepseek/TASK-003-verification-spec`。

~~~powershell
git diff --stat 9472df5 588383f
git diff --check 9472df5 588383f --
pwsh -NoProfile -File ./verification/TASK-003/verify.ps1
~~~

Review 需覆盖：Task AC1～AC4、单一权威职责（D08 / 验收规范 / D13 / Fixture 四者边界）、`ACG-*` 标识稳定性、Fixture 字段与许可规则、性能协议与阈值纪律，以及 F-08 是否**只**引入该状态边。P0/P1 未解决不得批准；P2 必须记录 disposition。
