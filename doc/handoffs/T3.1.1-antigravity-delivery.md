---
task_id: T3.1.1
author: Antigravity
recipient: Codex
base_commit: e771179beaa92b7c592a1986a32ffbf1b6302566
delivery_head: 899bd3c99752ce435da25e41166bafa1c2963abc
status: delivered
---

# Handoff：T3.1.1 阅读进度与导出历史统一进入 SQLite

## 交付结果

已在独立分支 `agent/antigravity/T3.1.1-storage-sqlite`（基于 `e771179`）中完成 T3.1.1 的全部要求，并生成固定 Delivery Head：`899bd3c99752ce435da25e41166bafa1c2963abc`。

### 变更文件清单

- **`src/infrastructure/sqlite/schema.py`**
  - 定义 `SCHEMA_VERSION_V4 = 4` 与 `MIGRATION_V4_NAME = "v4__reading_export_storage"`。
  - 新增 `MIGRATION_V4_SQL`：定义 `reading_progress` 与 `export_history` 数据表及专用索引（`idx_reading_progress_book`、`idx_export_history_created_at`、`idx_export_history_book`）。
  - `default_migrations()` 注册 v4 migration。历史 v1/v2/v3 migration SQL 字符与 SHA-256 校验和完全保持不变。
- **`src/infrastructure/sqlite/reading_export.py`**（新建）
  - 实现 `SqliteReadingProgressStore`：满足 `ProgressDocumentStore` 协议，提供针对 `(book_id, chapter_id, mode)` 的 upsert 与查询支持，支持 Webtoon 滚动偏移量与累计阅读时长恢复。
  - 实现 `SqliteExportHistoryStore`：满足 `HistoryDocumentStore` 协议，支持 terminal outcome 记录与按时间倒序查询，支持 `status` 过滤。
  - 提供兼容别名 `SqliteProgressDocumentStore` 与 `SqliteHistoryDocumentStore`。
  - 实现 `import_legacy_reading_progress` 与 `import_legacy_export_history`：仅在首次启动时安全导入；导入成功后在 `application_metadata` 中记录持久化 marker；缺失文件记录 marker 并安全跳过；损坏/非法结构抛出诊断异常；原始 JSON 文件严格保持只读，不修改、不删除、不重命名。
  - 定义明确的异常层次：`LegacyImportError`、`LegacyCorruptJsonError`、`LegacyInvalidStructureError`、`LegacyInvalidRecordError`。
- **`src/infrastructure/sqlite/__init__.py`**
  - 导出上述 Adapter 类、导入函数和异常类型。
- **`src/bootstrap/app.py`**
  - 在 `assemble_services` 中注入 `SqliteReadingProgressStore` 与 `SqliteExportHistoryStore`，移除生产运行对 JSON 存储的写入依赖。
  - 启动阶段自动调用一次性安全导入。
- **`tests/core/test_bootstrap.py`**
  - 对齐 diagnostics 中的 `schema_version` 断言（3 -> 4）。
- **`tests/storage/test_backup_restore.py`**
  - 对齐活动数据库与 pre-restore 检查中的 `schema_version` 断言（3 -> 4）。
- **`tests/storage/test_schema_migration.py`**
  - 对齐 `LATEST_KNOWN == 4`、应用迁移版本序列 `[1, 2, 3, 4]`、新版本拒绝测试（针对 v5）及 pre-migration 备份记录（3 个已记录 + 1 个 v0->v1 未记录守卫）。
- **`tests/reading_export/test_sqlite_reading_export.py`**（新建）
  - 包含 20 项自动化测试，完整覆盖 AC1 至 AC6 各项需求。
- **`verification/T3.1.1/`**
  - `run_probes.py`：4 个综合黑盒场景探测脚本。
  - `probes.log`：探测脚本执行输出（4/4 全部 PASS）。
  - `focused-reading-storage.log`：针对性测试日志（220 passed in 26.09s）。
  - `core.log`：核心引导套件测试日志（49 passed in 6.56s）。
  - `compileall.log`：字节码编译日志（ExitCode: 0）。
  - `smoke-test.log`：生产启动冒烟日志（ExitCode: 0）。
  - `full-suite.log`：全量回归测试日志（1229 passed, 6 skipped, 1 known failure, ExitCode: 1）。
  - `delivery-report.md`：详细交付验证报告与 AC 对照表。

---

## 验证证据

| AC/场景 | 实际命令/步骤 | 环境与被测 commit | 结果 | 日志/产物 |
|---|---|---|---|---|
| **AC1: SQLite Schema & Migration** | `pytest tests/storage -q` | `agent/antigravity/T3.1.1-storage-sqlite` (`899bd3c`) / Python 3.12.3 | **PASS** (80/80 passed) | `verification/T3.1.1/focused-reading-storage.log` |
| **AC2: 读写完整性与并发安全** | `pytest tests/reading_export/test_sqlite_reading_export.py -k "read_write or multi_mode or persistence"` | 同上 | **PASS** | `verification/T3.1.1/focused-reading-storage.log` |
| **AC3: 遗留 JSON 安全导入与幂等性** | `python verification/T3.1.1/run_probes.py` (Probe 1) | 同上 | **PASS** | `verification/T3.1.1/probes.log` |
| **AC4: 损坏/非法输入诊断与数据保护** | `python verification/T3.1.1/run_probes.py` (Probe 2) | 同上 | **PASS** | `verification/T3.1.1/probes.log` |
| **AC5: 生产装配与零 JSON 写入** | `python verification/T3.1.1/run_probes.py` (Probe 3) | 同上 | **PASS** | `verification/T3.1.1/probes.log` |
| **AC6: 故障与回滚保护** | `python verification/T3.1.1/run_probes.py` (Probe 4) | 同上 | **PASS** | `verification/T3.1.1/probes.log` |
| **整体阅读与存储套件** | `pytest tests/reading_export tests/storage -q` | 同上 | **PASS** (220 passed in 26.09s) | `verification/T3.1.1/focused-reading-storage.log` |
| **Core 模块套件** | `pytest tests/core -q` | 同上 | **PASS** (49 passed in 6.56s) | `verification/T3.1.1/core.log` |
| **字节码编译** | `python -m compileall -q src tests` | 同上 | **PASS** (ExitCode: 0) | `verification/T3.1.1/compileall.log` |
| **Bootstrap 冒烟** | `python -m bootstrap.app --smoke-test --data-root <temp>` | 同上 | **PASS** (ExitCode: 0) | `verification/T3.1.1/smoke-test.log` |
| **全量回归测试** | `pytest tests -q -p no:cacheprovider -rs` | 同上 | **1229 passed, 6 skipped, 1 known failure** | `verification/T3.1.1/full-suite.log` |

> **未运行/已知限制说明**：
> 全量回归套件中唯一的 1 项失败为 `tests/providers/test_registry_readiness.py:193`（PyTorch 未安装），与基线 commit `e771179` 时的表现完全一致（见 `doc/STATUS.md`），为既有环境已知限制；6 项 skip 为 OpenSSL 缺失导致的 TLS 离线测试跳过。没有任何由 T3.1.1 引入的失败或回归。

---

## 接收方式

- **仓库位置**：`G:/CODEX/New Manga`
- **实现分支**：`agent/antigravity/T3.1.1-storage-sqlite`
- **Worktree 路径**：`G:/CODEX/New Manga.worktrees/T3.1.1-antigravity-storage-sqlite`
- **固定 Base Commit**：`e771179beaa92b7c592a1986a32ffbf1b6302566`
- **固定 Delivery Head**：`899bd3c99752ce435da25e41166bafa1c2963abc`
- **复现与验证命令**：
  ```powershell
  # 激活虚拟环境
  $env:PYTHONDONTWRITEBYTECODE = "1"
  $env:PYTHONPATH = "src;."
  # 1. 运行 Probe 验证
  & "G:\CODEX\New Manga.task-envs\T1.1.1-impl-py312\Scripts\python.exe" verification/T3.1.1/run_probes.py
  # 2. 运行针对性测试
  & "G:\CODEX\New Manga.task-envs\T1.1.1-impl-py312\Scripts\pytest.exe" tests/reading_export tests/storage -q
  # 3. 运行冒烟与编译
  & "G:\CODEX\New Manga.task-envs\T1.1.1-impl-py312\Scripts\python.exe" -m compileall -q src tests
  & "G:\CODEX\New Manga.task-envs\T1.1.1-impl-py312\Scripts\python.exe" -m bootstrap.app --smoke-test --data-root "$env:TEMP\t311_smoke"
  ```

---

## 下一任务建议（必填）

- **Task ID / 标题**：T3.1.1-REVIEW / 独立非作者审查与集成准备
- **推荐 Agent / Reviewer**：**DeepSeek Harness**（独立非作者 Reviewer）与 **Codex**（Lead / Integrator）
- **推荐原因与前置依赖**：Antigravity 作为实现 Agent 严格禁止自审；按照协作协议，必须由非作者独立 Reviewer（DeepSeek Harness）在隔离上下文中对照 Base `e771179` 与 Delivery Head `899bd3c` 进行独立审核。
- **Recovery point**：
  - 仓库：`G:/CODEX/New Manga`
  - Base Commit：`e771179`
  - Delivery Head：`899bd3c`
  - Worktree：`G:/CODEX/New Manga.worktrees/T3.1.1-antigravity-storage-sqlite`
- **允许修改路径**：
  - DeepSeek Harness：只读审查，输出审查产物至 `verification/T3.1.1/review-899bd3c.md`。
  - Codex：文档状态更新（`doc/STATUS.md`、`doc/tasks/T3.1.1.md` 等）及集成合并。
- **禁止范围与新增授权条件**：
  - **在 DeepSeek Harness 独立审核通过并出具明确 PASS 结论前，Codex 严禁集成到 `master`。**
  - Antigravity 严禁合并 `master`，严禁修改主库既有的未跟踪/脏污文件（如 `experiments/TASK-017/README.md` 等）。
- **交付物**：DeepSeek Harness 独立 Review 报告（`verification/T3.1.1/review-899bd3c.md`）。
- **验证命令、环境与完成 Gate**：
  - Gate 1：DeepSeek Harness 独立审查无阻断项（BLOCKING Finding = 0）。
  - Gate 2：Codex 验证 Delivery Head `899bd3c` 的针对性测试、编译与冒烟全绿，且无未授权接口修改。
- **可直接转发给 Codex 的指令**：
  ```markdown
  请 Codex 调度 DeepSeek Harness 对 Antigravity 交付的 T3.1.1 进行独立非作者 Review：
  - Base Commit: e771179beaa92b7c592a1986a32ffbf1b6302566
  - Delivery Head: 899bd3c99752ce435da25e41166bafa1c2963abc
  - 分支: agent/antigravity/T3.1.1-storage-sqlite
  - 证据目录: verification/T3.1.1/
  - 交付报告: verification/T3.1.1/delivery-report.md 与 doc/handoffs/T3.1.1-antigravity-delivery.md
  重要约束：
  1. DeepSeek Harness 审核通过前，Codex 不得将本提交集成至 master。
  2. 审核应重点核验 AC1~AC6，尤其是 v1~v3 migration SQL hash 守卫、遗留 JSON 文件只读不被修改/删除、以及异常诊断保护。
  ```

---

## 风险与遗留

1. **环境已知限制**：当前虚拟环境中未安装 PyTorch，导致全量测试套件中 `tests/providers/test_registry_readiness.py:193` 报已知的环境断言失败，该现象在基线中已记录，未引入任何新增失败。
2. **遗留数据文件保护**：生产启动时若存在遗留 JSON 文件，导入完成并记录 marker 后，原始文件仍然保留在原位以防数据意外丢失；后续清理策略可由用户或后续维护任务决策。
3. **回滚方法**：若需要回滚，因 v4 migration 遵循向前追加原则，且生产代码完全解耦于 Adapter 协议，可直接检出至 `e771179` 恢复历史状态。
