---
task_id: TASK-023
author: ZCode（窗口插队项 Owner；Reviewer=ZCode 子 agent）
recipient: Codex（T1 后 post-hoc 复审）
base_commit: cca8b09（Task 元数据 base=e96b3eb 之上为窗口授权与 W1-W3 集成提交，写集合与本切片不相交）
delivery_head: 7fa9118
status: delivered（待窗口内子 agent Review；结论仅 approved_subagent/changes_requested）
---

# Handoff：TASK-023（PDF/MOBI 导入路线）

## 交付结果

**实现 head `7fa9118`**（分支 `agent/zcode/TASK-023-pdf-mobi-import`，worktree `G:/CODEX/New Manga.worktrees/TASK-023-zcode`；开工提交 `d117467`，分支快进至 master `cca8b09`）。7 文件，+603：

| 路径 | 内容 | 白名单 |
|---|---|---|
| `requirements.txt` | `pypdfium2==5.13.0`（**唯一**新增，用户 2026-09-17 批准；无其他升级/新增） | 明示允许 |
| `src/application/importing/documents/`（新建子包） | `ports.py`：`DocumentRaster`/`DocumentHandle`/`RenderedDocumentPage` + `DocumentDecodeError`（`INVALID_DOCUMENT`/`ENCRYPTED`）；`service.py`：`ImportDocumentsUseCase` | `src/application/importing/**`（"文档导入用例可在此新增子包"） |
| `src/infrastructure/importing.py` | `PdfiumDocumentRaster`（PDF 专用，U-2 范围；render scale=2.0 为**实现默认非契约**；PNG 编码复用同栈 Qt；pdfium 错误映射 `ENCRYPTED`(含 password)/`INVALID_DOCUMENT`） | `src/infrastructure/importing.py` |
| `tests/import_formats/`（新建） | 6 例（手造多页 PDF，无许可顾虑） | 明示允许新建 |

**设计（复用而非第二管线）**：文档文件 → 逐页光栅化 PNG → 每页走与图片导入完全相同的纪律——页级 source_hash 去重（`DuplicatePolicy.SKIP`/`IMPORT_AS_NEW`）、`source_order`/`sort_order` 顺延、Managed Copy 严格先于 Page 提交、取消保留已提交页并报告剩余（AC 2）。一文档的所有页均已存在 → 文件计入 `skipped_duplicates`。

**MOBI 边界（如实登记）**：pypdfium2 仅覆盖 PDF（U-2 批准范围）；MOBI 无批准解析依赖 → 非 PDF 载荷一律 typed `UNSUPPORTED_FORMAT`（可诊断，AC 2），**不自行发明解析器**；MOBI 光栅化 **BLOCKED**，解锁条件=用户批准 MOBI 解析依赖。

**生产装配点（白名单外，移交 Codex）**：`ImportDocumentsUseCase` 需在 `bootstrap/app.py` 与入口 UI 接线（本切片白名单不含 bootstrap）；当前生产书架导入走图片路径，行为不变。

## 依赖变更留证（Task 注意事项 ②③）

| 步骤 | 命令 | 结果 |
|---|---|---|
| 安装前全仓 | `pytest -q -p no:cacheprovider -rs -rf`（worktree `d117467`） | **764 passed / 0 skipped**、exit 0 |
| 安装 | `pip install pypdfium2` → **5.13.0**（仅此一项） | — |
| 安装后全仓 | 同上 | **764 passed / 0 skipped**、exit 0（零扰动） |
| ③ readiness 断言 | `grep -rn "pypdfium2\|pdfium" src/ tests/`（安装前） | **零命中**——无既有断言声明其缺失，无需更新 |

## 验证证据

| 场景 | 命令（worktree 内，`PYTHONDONTWRITEBYTECODE=1`） | commit | 结果 |
|---|---|---|---|
| 本切片 | `pytest tests/import_formats -q -p no:cacheprovider -rf` | `7fa9118` | **6 passed / 0 skipped**、exit 0 |
| library 回归 | `pytest tests/library tests/import_formats -q -p no:cacheprovider -rs` | `7fa9118` | **46 passed / 0 skipped**、exit 0 |
| 全仓 ×5 | `pytest -q -p no:cacheprovider -rs -rf` | `7fa9118` | 见 `verification/TASK-023/full-suite-runs.log` |

## 测试素材与许可

全部测试 PDF 由仓库内生成器**手工构造**（`tests/import_formats/test_document_import.py::minimal_pdf`），无第三方素材、无许可顾虑。

## 风险与遗留

1. **MOBI 光栅化 BLOCKED**（无批准依赖）——解锁=用户批准 MOBI 解析依赖；当前 .mobi 导入得到可诊断失败，不静默、不伪造。
2. 生产装配点在白名单外（同 TASK-020 的 `tile_factory` 情形）。
3. render scale=2.0 为实现默认；D07 若有 DPI 契约应在后续切片显式绑定。
4. 未 push；未改 Schema/其他依赖/其他 Task/`AGENTS.md`；共享 venv 变更仅 pypdfium2 一项且前后对照留证。
