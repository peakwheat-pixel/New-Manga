---
task_id: TASK-036
author: DeepSeek Harness
recipient: Codex（独立 Review；集成由 Codex 执行）
base_commit: edfdcf2fe923c8c6e97d19377c61c4b8bba25abc
delivery_head: c931db0
status: in_review
---

# Handoff：TASK-036 设置输入校验与导出发布顺序硬化

## 交付结果

**范围**：承接 TASK-034 的 R-02 / R-05 / R-07，把"用户写错的配置不得被无声忽略或无声改写"（fail-closed）落到 `inpaint.route_policy` 的设置解析与导出终态发布顺序上；**不改变任何路由判定算法，也不改变合法输入的既有语义**。

**提交**（分支 `agent/deepseek/TASK-036-settings-validation-and-export-order`，工作区 `G:/CODEX/New Manga.worktrees/TASK-036-deepseek`；固定 base `edfdcf2`，分支起点 `81ffd83`）：

| commit | 内容 |
|---|---|
| `dd608f4` | 开工：`doc/tasks/TASK-036.md` 置 `in_progress` |
| `c931db0` | 实现 + 测试 + 取证（11 文件、`+568/−92`）：`src/application/translation/inpaint/router.py`、`src/ui/viewmodels/export/viewmodel.py`、`tests/providers/{test_route_policy_settings,test_handlers_pipeline}.py`、`tests/reading_export/test_viewmodels.py`、`verification/TASK-036/**` |

`delivery_head = c931db0`。**未 push、未合并 master、未释放冻结 Task。**

**逐 AC**：

| AC | 状态 | 摘要 |
|---|---|---|
| ① R-02 段类型 fail-loud | **完成** | 用哨兵区分"键缺失"与"键存在但类型错误"：`inpaint` 非 mapping → `INVALID_INPUT: inpaint must be a mapping (got X)`；与 `route_policy` 的错误消息分级、互不混淆。**附带关闭** TASK-034 实现的 `route_policy: null` 静默漏洞（原为 `raw is None` → 回落默认）。装配与运行**各一条回归** |
| ② R-07a 序列校验 | **完成** | `allowed_routes`/`fallback_routes` 必须是真序列且元素全 `str`；裸字符串（原逐字符展开）、非序列、非 `str` 元素一律 `ProviderInputError`，消息含字段名与期望类型 |
| ③ R-07b `requirements` 值域 | **完成** | 值必须真 `bool`；`"false"`/`"true"`/`0`/`1`/`None` 一律拒绝（不再 `bool()` 强转）；非 mapping 由裸 `ValueError` 改为 typed error |
| ④ R-05 发布顺序 | **完成** | `_finish`/`_fail`：终态 `_status` → `changed` → 终态信号 → `_running = False` → `changed`（`running` 的 notify）；导出用例等待收回 `pump_until(not vm.running)`（预算 5 s、断言、诊断消息均未变） |
| ⑤ 冻结解除声明 | **完成** | **本轮解除 TASK-034 R-07 冻结**；更新三处锁定旧行为的用例（见下），未放宽/删除其它断言、未新增 skip |
| ⑥ 语义不变性 | **完成** | 12 类输入前后对照矩阵：[`verification/TASK-036/input-matrix-before-after.txt`](../../verification/TASK-036/input-matrix-before-after.txt)（8 类合法输入逐字相同；非法输入改为报错） |
| ⑦ 回归与分列 | **完成** | mandated 四套件 **274 passed / 0 skipped**（基线 243）；全仓 **737 passed / 6 skipped ×7 次**（基线 706/6）；skip 全为既有 `openssl unavailable` |
| ⑧ Review/集成 | **待办** | 本 Handoff + [取证](../../verification/TASK-036/author-verification.md)；Review 由 Codex 按四轴执行 |

## 合法输入不变 / 非法输入改为报错（AC ⑥）

| 类别 | 输入 | 前（`edfdcf2`） | 后 |
|---|---|---|---|
| 合法 | 无 `inpaint` / `inpaint={}` / `route_policy={}` / `allowed_routes=["edge-bleed"]` / `allowed_routes=[]`（R-3 回落）/ `color_route="flux-fill"` / `color_route=""`（Option B）/ `requirements={x: True}` | 见矩阵 | **逐字相同** |
| 非法 | `inpaint="oops"` | 静默回落默认 | `inpaint must be a mapping (got str)` |
| 非法 | `route_policy=None` | 静默回落默认 | `inpaint.route_policy must be a mapping (got NoneType)` |
| 非法 | `allowed_routes="simple-fill"` / `fallback_routes="edge-bleed"` | 逐字符展开 | `… must be a sequence of strings (got str)` |
| 非法 | `allowed_routes=[1]` | `str(1)` → `('1',)` | `… entries must be strings (got int)` |
| 非法 | `requirements={"brushnet": "false"}` / `{…: 0}` | `bool()` → `True`/`False`（反向/改写） | `… requirements['brushnet'] must be a bool (got str/int)` |
| 非法 | `requirements=["brushnet"]` | 裸 `ValueError` | `… requirements must be a mapping (got list)` |

## 冻结解除（AC ⑤，必须声明）

**TASK-034 的 R-07 冻结已由本 Task 解除**。被更新（非删除）的旧用例：

1. `test_missing_section_or_key_returns_the_explicit_default` 中"`{"inpaint": "not-a-mapping"}` → 返回默认"的断言 → 改为 `test_present_non_mapping_section_fails_loud_with_its_own_message`（抛错且消息分级；R-1 的"键缺失 → 默认"仍为同一对象）。
2. `test_allowed_routes_as_string_keeps_the_frozen_char_expansion` → `test_route_lists_reject_a_bare_string[allowed_routes/fallback_routes]`。
3. `test_fallback_routes_and_requirements_defaults_are_frozen` 中 `bool("false") → True` 断言 → `test_requirements_values_must_be_real_bools[...]`；合法默认（`()`/`{}`）保留在 `test_fallback_routes_and_requirements_defaults`。

## AC ④ 发布顺序证据

- **定向探针**（[`export-publish-order.txt`](../../verification/TASK-036/export-publish-order.txt)）：同步调用 `_fail`/`_finish`（`running` 置为进行中），逐事件记录槽内观测值 ——
  - 修复前：`changed(False)` → `changed(False)` → `exportFailed(False)`（终态尚未发布时标志已清）；
  - 修复后：`changed(True)` → `changed(True)` → `exportFailed(True)` → `changed(False)`（终态先发布，最后才清标志）。
- **测试**：`test_export_outcome_wait_does_not_accept_the_half_published_state` **按新顺序更新**（现锁定"任何 `running=False` 快照必伴随终态消息"，并以 `Qt.DirectConnection` 断言终态信号在清标志之前触发；保留"检测器非空转"断言），另加成功路径同型用例；三个导出用例的等待**收回**自然终态等待。
- **判别力**：上述两个新不变量用例放到 base `edfdcf2` 的 viewmodel 上 → **2 failed**（[`discriminative-prefix.log`](../../verification/TASK-036/discriminative-prefix.log)）。
- **AC ①②③ 判别力**：新测试放到 base `edfdcf2` 的 `src` 上 → **28 failed / 37 passed**（拒绝类用例失败、合法输入用例仍通过）。

## 验证证据

环境：Windows 11 `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；全部 `-p no:cacheprovider`。

| # | 场景 | 命令 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | mandated 四套件 | `pytest tests/providers tests/core tests/reading_export tests/editing -q -rs` | **274 passed / 0 skipped**（基线 243） | `test-counts.txt` |
| 2 | 全仓 ≥5 次 | `pytest -q -rs -rf` ×7 | **737 passed / 6 skipped**，逐次 exit 0 | `full-suite-runs.log` |
| 3 | 输入矩阵 | 探针跑 base `edfdcf2` 与本 head | 8 类合法逐字相同；非法改报错 | `input-matrix-before-after.txt` |
| 4 | 发布顺序 | 定向探针（前后对照） | 见上 | `export-publish-order.txt` |
| 5 | 判别力 | 新测试 × base src | providers 28 failed / 37 passed；reading_export 2 failed | `discriminative-prefix.log` |
| 6 | 边界 | `git diff --name-only edfdcf2..HEAD`（Owner 段） | 2 个 `src/` 文件（恰为允许清单）+ `tests/**` + `verification/TASK-036/**`；越界 0；`git diff --check` 退出码 0 | 本 Handoff §边界 |

**skip 明细**：6 条全为既有 `tests/network` 的 `openssl unavailable`（`test_connection_tester.py:106`、`test_transport_tls.py:39/47/62/69/83`）。

## 接收方式

1. **分支/工作区**：`agent/deepseek/TASK-036-settings-validation-and-export-order` @ `c931db0`；`G:/CODEX/New Manga.worktrees/TASK-036-deepseek`。固定 base `edfdcf2`。
2. **复现命令**：
   ```powershell
   $env:PYTHONDONTWRITEBYTECODE='1'
   $py = "G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe"
   & $py -m pytest tests/providers tests/core tests/reading_export tests/editing -q -p no:cacheprovider -rs   # 274 passed / 0 skipped
   & $py -m pytest -q -p no:cacheprovider -rs -rf                                                              # 737 passed / 6 skipped
   ```
3. **不需要**额外数据、依赖、端点或权重。
4. **Reviewer 重点检查**：
   - ① 的哨兵实现是否确实把"键缺失"（R-1，返回同一 `default` 对象）与"段类型错误"分开，且两级错误消息可区分；
   - ② / ③ 的拒绝范围是否与我的矩阵一致（尤其 `None`/字符串/非 str 元素/non-bool 的处置），以及"合法输入一字未变"的证据是否成立；
   - ⑥ 矩阵中我**主动扩大**的两处行为变化（`route_policy: null`、`requirements` 非 mapping 的 typed error）是否被接受——前者按 R-2 原文属"present but non-mapping"，后者原为裸 `ValueError`；
   - ④ 的发布顺序与"收回自然终态等待"是否符合要求，`_running` 的 notify 二次 `changed` 是否可接受；
   - ⑤ 的冻结解除声明与用例更新清单是否完整。

## 复现与登记：已登记 flaky（附带发现，本 Task 不修）

本轮全仓串跑 **14 次**：12 次 `737 passed / 6 skipped`（exit 0），**2 次失败且都在同一已登记用例** `test_reader_webtoon_swaps_in_vertical_viewer`：

1. **第 8 次**：`test_qml_contract.py:153: RuntimeError` —— 该行在**诊断辅助函数**内，说明断言**已失败**（flaky 触发），但诊断访问了**已销毁的 QML C++ 对象**，`RuntimeError` **掩蔽**了真实 `AssertionError`。
2. **诊断稳健化（本切片的小改动，请 Reviewer 裁定接受或回退）**：`tests/reading_export/test_qml_contract.py` 新增 `safe_property()`（`RuntimeError` → `<unavailable: …>`），并让 `object_names()`、webtoon 用例的两处等待条件与 `webtoon_save_diagnostics()` 走它。**不改任何断言、等待预算或测试语义**；**不解决、不重分类 R-06（仍 open/未处理）**。
3. **第 14 次（稳健化后）**：拿到**真实签名**——`pump(2s): iterations=98 elapsed_ms=2000 ok=False scroll_contentY=-0.0 saved_scroll_offset_y=0.0`。关键点：`setProperty("contentY", 240.0)` **被 Flickable 夹回 0**（`StopAtBounds`），前一断言只保证 `contentHeight > 0`、未保证内容可容纳 240；无值变化 → `onContentYChanged` 不触发 → 节流保存不会发生。比"事件循环饥饿"更贴近证据。

完整记录：[`registered-flaky-signature.md`](../../verification/TASK-036/registered-flaky-signature.md)、[`intermittent-failure-repro.log`](../../verification/TASK-036/intermittent-failure-repro.log)。**两次失败均未记为通过、未归因本 Task**（该用例在读者 QML 路径，TASK-036 改动为 `inpaint` 路由设置解析与**导出** ViewModel，不在该执行图上）。STATUS 的 flaky 条目此前记为"用例名未捕获"，**建议 Codex 决定是否更新**（本 Task 不自行改 STATUS）。若裁决方要根除，最小候选改动是"等 `contentHeight` 足够后再设 `contentY`，或设值后断言 `contentY == 240` 再等保存"，属后续切片。

## 风险与遗留

- **启动期行为变化（有意）**：`inpaint` 段类型错误现在会让装配失败（启动期报配置错误），与 TASK-034 R-2 的既有选择一致；若产品希望"启动降级 + 告警"，需另立裁决。
- **合法输入的边界情形**：`allowed_routes`/`fallback_routes` 若传给集合（`set`）或生成器，此前可被静默迭代，现按"序列"要求拒绝；如需支持其它可迭代类型，请裁决。
- 未处理（按 Task 禁止范围）：R-03（`DEFAULT_ROUTE_POLICY` 跨层归属）、R-06（webtoon 保存 flaky 未复现）。
- 回退：`git revert c931db0`（2 个 `src/` 文件 + 测试/文档）；无 Schema/依赖/数据影响。
