---
task_id: TASK-029
author: ZCode
recipient: Codex（转 DeepSeek Harness 复审）
base_commit: 79529bc9805fda6b72b99860f649fca0922c6cf9
delivery_head: 5abc173497e112db4da1c00beac3c19fbcccf455
status: in_review
---

# Handoff：TASK-029 修订轮（F-01～F-05）

本 Handoff 交付 `5abc173` 的修订轮，处理 DeepSeek Review 对 `a2348e9` 的 changes_requested
（报告 `doc/reviews/TASK-029-a2348e9.md`，commit `2652973`，位于 DeepSeek review worktree/分支，
不随本分支存在）。原 `a2348e9` 为被拒 head，其 Handoff 中唯一记录错误（F-04 的 95）由本轮勘误；
其余结论仍引用 [TASK-029-a2348e9](TASK-029-a2348e9.md)。`base_commit=79529bc` 不变。

提交列表（本轮新增）：

| commit | 说明 |
|---|---|
| `5abc173` | fix(TASK-029): address review findings F-01..F-05（新 reviewed_head，唯一内容变更） |

## Findings disposition

| ID | 级别 | 处置 | 实现与证据 |
|---|---|---|---|
| F-01 | P1 | **fixed** | `RegionEditingService.__init__`：未显式传 committer 且 repository 自身实现 `commit_region_revision` 时，**优先采用 repository 的原子 seam**（Reviewer 建议①）；仅对无该能力的内存 fake 回退 `InMemoryRegionCommitter`。新增正向测试 `test_default_construction_uses_repository_seam`：默认构造于 `SqliteRegionRepository` 上 `create_region`/`save_geometry` 后，DB `current_revision_id` 非空且与实体一致、`geometry_json` 更新、revision 数=2——即 Reviewer 探针的缺陷场景反转为通过。 |
| F-02 | P2 | **fixed** | ①异常捕获放宽为 `except Exception` → `rollback()` → 返回 `DB_FAILED`（含 repr 详情），不再让非 sqlite3 异常泄漏半开事务；②删除 `region.updated_at = region.updated_at` no-op 行（对象视图一致性由 service 层 `_sync_region_state` 完成），并保留 APPLIED 时把新指针写回调用方对象（staged）的回传。 |
| F-03 | P2 | **fixed** | `get_page` 增加 `deleted_at IS NULL`，与 `list_pages` 一致；`PageRepository` 契约 docstring 明确「软删页不经本契约返回；回收站/恢复视图属后续切片」。 |
| F-04 | P2 | **fixed（勘误）** | 全量回归计数勘误：修订轮交付时分项 core 6 + storage 33 + library 32 + editing 26 = **97 passed**（Reviewer 复跑所见 96 为修订前状态，editing 25）。本文件与 author-verification 已按 97 记录。 |
| F-05 | P2 | **fixed** | `tests/library/test_import_images.py` 与 `test_page_order.py` 模块级 `pytest.importorskip("PySide6")`；helpers 的 Qt 导入本就为函数内延迟（Reviewer 已确认属改善项）。无 Qt 解释器上领域/SQLite 用例完整运行，Qt 用例标记 SKIP 而非 ERROR。 |

无新 finding 引入；未触碰派单禁止范围（domain/QML/Provider/Pipeline/TM/Constraint/依赖/用户文件/AGENTS/TASK-028 设计/v1 migration）。

## 验证证据（修订轮，全部退出码 0）

| 场景 | 命令 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| storage | `python -m pytest tests/storage` | 3.12.3 固定任务环境；head `5abc173` | 33 passed | author-verification 修订节 |
| library | `python -m pytest tests/library` | 同上 | 32 passed | 同上 |
| editing | `python -m pytest tests/editing` | 同上 | 26 passed（含 F-01 新测试） | 同上 |
| 全量回归 | `PYTHONPATH=src python -m pytest tests` | 同上 | **97 passed**（分项相加一致，F-04 勘误后数字） | 同上 |
| whitespace / 范围 | `git diff --check 79529bc 5abc173 --`；name-only 过滤 | 本 worktree | PASS（0 / 全部在白名单；v1 SQL 冻结结构断言继续通过） | 复审可重放 |

F-01 正向/反向证据：新增 `test_default_construction_uses_repository_seam`（正向：默认构造下 DB current 非空且随 save_geometry 更新）；Reviewer 探针场景（旧回退行为）已由实现消除，不可再现。其余 NOT_RUN / N/A 维持首轮声明（并发竞争、bootstrap 装配、损坏库恢复、性能、ref 收敛、R-201/R-202）。

## 复审建议

重放四条 pytest 命令与范围过滤；重点复核：① 默认构造于 SQLite repo 上 `create_region`/`save_geometry` 的 DB 状态（Reviewer 原探针应转为通过）；② `commit_region_revision` 注入非法输入（如不可序列化 snapshot）时事务释放且返回 DB_FAILED；③ `get_page` 软删一致性与契约 docstring；④ 97/分项一致性。
