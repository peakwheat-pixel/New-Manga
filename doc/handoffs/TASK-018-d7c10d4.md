---
task_id: TASK-018
author: DeepSeek Harness
recipient: Codex（独立 Review）
base_commit: 8c63f9b19f6daf065847aefce01bda578332cd6b
delivery_head: d7c10d4dfc64dced97a23a7b4db6b7f169a8567b
status: in_review
supersedes_scope: 后续修订切片（R-001 剩余项 / R-002 / R-003 / R-006 / R-007）
integrated_baseline: 4d189ce（不因本切片失效）
---

# Handoff：TASK-018 后续修订切片（R-007 + R-002 + R-003 + R-006 + R-001 剩余项）

> **更正（2026-09-17，集成收口，Review R-101）**：本文件变更表与"skip 原因"中的"纯标准库""无第三方依赖"表述**不准确**——`experiments/TASK-018/test_route_gating.py` 通过 `import run_experiment` 间接依赖 PySide6，只是测试代码本身只用标准库。权威说明见 [实验日志](../../verification/TASK-018/experiment-log.md) §7.2 与 [`doc/reviews/TASK-018-5063315.md`](../reviews/TASK-018-5063315.md)。其余结论不受影响。

## 交付结果

修订提交 **`d7c10d4`**（4 文件、`+1967/−245`），固定 base `8c63f9b`（修订前分支 head，已与 master 同步，**未再次 merge**）。

**已集成基线 `4d189ce`（被审 head `6c33e7f`）不因本切片失效**：本切片是在其之上的增量修复，**未改写任何已集成提交**；`test_mask_protocol.py` 的原有 12 例一字未动。

变更路径（全部落在允许范围内，禁止路径命中 0）：

| 文件 | 内容 |
|---|---|
| `experiments/TASK-018/run_experiment.py` | 显式 `FILLERS` 映射 + fail-closed 门控（R-007）；可探测 requirements 描述符与 `blocked_stage`（R-002）；`_display_path` 越界回退绝对路径（R-003）；去体积标注与 `(unmeasured tier)`（R-006）；`protected_box_violations` 逐框断言（R-001） |
| `experiments/TASK-018/test_route_gating.py` | **新增**（161 行、纯标准库、13 例）：fail-closed、探测、标签、越界四组 |
| `experiments/TASK-018/results/experiment.json` | 按新代码**重新生成**（`schema = task018-experiment-v2`；字段变化属预期） |
| `doc/tasks/TASK-018.md` | 状态与运行记录 |

## 逐项处置

| ID | 处置 | 关键证据 |
|---|---|---|
| **R-007**（fail-closed） | **fixed** — `FILLERS` 显式映射；未登记路线 `raise KeyError`；`route_gate()` **先判可实现性再判依赖**，`implementation is None` → `not_implemented` | 反例：依赖全部强制 satisfied 后，`manga-lama`/`flux` 仍 **10/10 BLOCKED / `not_implemented`**，`png_written=[]`，`any_measured=False` |
| **R-002**（真实探测） | **fixed** — `module` → `importlib.util.find_spec`；`weight` → 环境变量指向的本地文件存在性；记录 `requirement_probes` 与 `blocked_stage` | 本环境 20 条 BLOCKED 全为 `not_implemented`；`ProbingTests` 4 例覆盖存在/缺失与 `dependency` 分支 |
| **R-003**（越界输出目录） | **fixed** — `_display_path()` 回退绝对路径，报告记 `output_dir_in_experiment_root`；每条 MEASURED 记 `output_image_in_experiment_root` | `--output-dir %TEMP%/task018-outofroot` → **退出码 0**、产物正常、路径记为绝对、`in_root=False` |
| **R-006**（体积标注） | **fixed** — 描述符键名去掉 `10GB+`/`数 GB`；`size_class` 一律标 `(unmeasured tier)`；`LabelTests` 2 例断言 | `experiment.json` 已重生成 |
| **R-001 剩余项**（保护框） | **fixed** — `protected_box_violations()` 逐框统计；每条 MEASURED 记录携带 | **20 框（10 MEASURED × 2）全部 `changed_pixels = 0`** |

## 验证证据

环境：Windows `10.0.26200`；Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312`）；PySide6 6.11.2；`PYTHONPATH=experiments/TASK-018`；`PYTHONDONTWRITEBYTECODE=1`；未设置 `QT_QPA_PLATFORM`。

| # | 命令 | 退出码 | passed | skipped | 结果 |
|---|---|---:|---:|---:|---|
| 1 | `python -m unittest experiments/TASK-018/test_mask_protocol.py` | **0** | **12** | **0** | `OK`（**原有 12 例未改动/未减少**） |
| 2 | `python -m unittest experiments/TASK-018/test_route_gating.py` | **0** | **13** | **0** | `OK`（新增，独立计数） |
| 3 | `python experiments/TASK-018/run_experiment.py --repeat 3` | **0** | — | — | `{"MEASURED": 10, "BLOCKED": 20}` |
| 4 | `run_experiment.py --output-dir %TEMP%/task018-outofroot --repeat 1` | **0** | — | — | 越界不崩溃、产物正常、绝对路径记录 |
| 5 | `git diff --check 8c63f9b d7c10d4` | **0** | — | — | 无输出 |
| 6 | 允许范围检查 | — | — | — | 4/4 在内；禁止路径命中 **0** |

**skip 原因**：命令 1、2 均 **`0 skipped`**（纯标准库，无模型/网络/第三方依赖）；命令 3、4 为进程执行。

**确定性（连续两次 `--repeat 3`）**：`manifest_sha256`、`routes`、记录键集合、`mask_sha256`、`output_sha256`、`sample_sha256`、`parameters`、`mask`、`protected_box_violations` **全部完全一致**。

**保护证据**：`protected_violations` 合计 **0**；`protected_box_violations` **20 框全 0**；`blocked_stage = {"not_implemented": 20}`。

完整记录：[verification/TASK-018/revision-d7c10d4.md](../../verification/TASK-018/revision-d7c10d4.md)。

## 未完成项（未因本切片变化）

| 项 | 状态 | 原因 |
|---|---|---|
| 四条学习型路线的质量/残字/背景损伤 | **BLOCKED** | 实现与依赖均不存在；本切片**未新增**任何数字 |
| 上述路线的耗时/内存/显存 | **BLOCKED** | 同上；**未用基线数字代替** |
| Mask 内部结构损伤量化 | **NOT_RUN** | 需结构连续性指标或人工目视 |
| 真实 OOM / 真实漫画样例 | **NOT_RUN** | 无法触发 / 仅自制样例 |

## 边界与合规

- 仅修改允许路径；**未修改** `doc/STATUS.md`、`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/tasks/README.md`（集成元数据归 Codex）、生产 `src/`、`tests/`、Schema、依赖清单、`AGENTS.md`、其他 Task。
- **未释放 TASK-019 或其他冻结 Task**；**未 push、未合并 master**。
- 未以 Mock 冒充真实模型、视觉或性能结果。

## 状态

`doc/tasks/TASK-018.md` 置 **`in_review`**，交 Codex 独立 Review。本 Task 不自行标记 `approved`／`done`。
