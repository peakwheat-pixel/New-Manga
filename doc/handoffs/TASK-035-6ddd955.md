---
task_id: TASK-035
author: DeepSeek Harness
recipient: Codex（独立 Review；集成由 Codex 执行）
base_commit: 2e1bf2d3d9610c1c53537f45abc12e82e8e5ee4f
delivery_head: 6ddd955
status: in_review
---

# Handoff：TASK-035 修复渲染层 SFX Policy Gate 缺失 region_type 前置（F-1 渲染面）

## 交付结果

**范围**：渲染层 SFX Policy Gate 补 `region_type` 前置并改为**单一共享规则**；`RegionSnapshot` 字段默认与实体/Schema 对齐（R-05）；`tests/pipeline` 夹具默认对齐生产默认（R-02）；修正把缺陷固化的渲染用例并补"真实默认值"回归。

**提交**（分支 `agent/deepseek/TASK-035-render-sfx-gate-region-type`，工作区 `G:/CODEX/New Manga.worktrees/TASK-035-deepseek`；固定 base `2e1bf2d`，分支起点 `e8e1750` = 释放纯文档提交）：

| commit | 内容 |
|---|---|
| `16cd171` | 开工：`doc/tasks/TASK-035.md` 置 `in_progress` |
| `6ddd955` | 修复 + 用例（4 文件、`+199/−16`）：`src/application/rendering/service.py`、`src/domain/tasks/models.py`、`tests/rendering/test_rerender.py`、`tests/pipeline/test_pipeline.py` |

`delivery_head = 6ddd955`。**未 push、未合并 master**。

**改动**：

- `RenderService.rerender_region` 与 `RenderService._prepare_region` 的 SFX 判定改为模块级 `_sfx_gate_allows(region_type, sfx_policy, *, allow_manual)`，内部调用 **planner 与 Translate Step 同一实现** `application.translation.context.gate.decide_sfx_translation`（该函数自带 `region_type == sfx` 前置）。渲染层不再有任何策略映射的第二份实现；`allow_manual_sfx`/`allow_sfx` 仅作为 D06 §47 的**显式手动逃生口**叠加在其上。
- `src/domain/tasks/models.py`：`RegionSnapshot.sfx_policy` 默认 `"translate"` → `SfxPolicy.SKIP.value`（R-05）。
- `tests/rendering/test_rerender.py`：`make_region` 新增 `region_type`（默认 `speech`）；`sfx=None` 表示"保留实体默认"（真实默认 `skip`）；两个固化缺陷的用例显式 `region_type="sfx"`（断言逐字保留）；新增 7 例。
- `tests/pipeline/test_pipeline.py`：`region()` 夹具默认 `"translate"` → `SfxPolicy.SKIP.value`（R-02），新增三门默认一致守卫 1 例。

**语义边界（请 Reviewer 重点确认）**：

1. 只有 `region_type == "sfx"` 参与该策略；非 SFX 任何取值都不影响批量渲染与单 Region 渲染。
2. `sfx` + `manual` 仍只在 `allow_manual_sfx=True` 时通过；`sfx` + `skip` 任何情况下都拦截（原意未放宽）。
3. `sfx` + 取值域外策略由共享 gate 拒绝（`ValueError`），与 TASK-032 规划层一致；非 SFX 不受该策略影响（旧代码对任意类型都解析策略值，因此非 SFX 的未知取值此前会误报错）。
4. `rerender_region` 的校验**返回顺序未调整**（缺 Clean / 无 final 仍在策略判定之前），以免改动其它用例期望。
5. 未改变 `sfx_policy` 默认值语义（仍 `skip`）、Schema、migration、依赖清单、seam 本体、`src/ui/**`。

**AC 对照**（完整证据见 [`verification/TASK-035/author-verification.md`](../../verification/TASK-035/author-verification.md)）：

| AC | 判定 | 关键证据 |
|---|---|---|
| ① 渲染层与 planner 同一判据 | **PASS** | 门控矩阵 12 组合（[`defaults-and-gate-matrix.txt`](../../verification/TASK-035/defaults-and-gate-matrix.txt)）：`speech` × {skip,manual,translate} × {allow_manual False,True} 全部放行；`sfx` × skip 始终拦住、sfx × manual 仅显式时放行、sfx × translate 放行 |
| ② 单一规则来源 | **PASS** | 两处判定改走 `decide_sfx_translation`；渲染层无第二份策略映射 |
| ③ 修正固化用例 + 真实默认回归 + 保留 SFX 拦截 | **PASS** | 两个 SFX 用例显式 `region_type="sfx"`（断言未改）；新增 7 例（真实默认 `speech`+`skip` 页面级/单 Region、非 SFX × 3 策略、SFX 真实默认仍被拦、SFX+translate 正常渲染） |
| ④ `RegionSnapshot.sfx_policy` 对齐 `SfxPolicy.SKIP.value` | **PASS** | 代码改动 + 四方默认实测均 `skip`（实体/Snapshot/夹具/Schema） |
| ⑤ `tests/pipeline` 夹具默认对齐（R-02） | **PASS** | 夹具默认改为生产默认并加守卫用例 `test_region_defaults_agree_across_entity_snapshot_and_fixture` |
| ⑥ 双侧全绿 + 全仓分列 | **PASS** | 主套件 124 passed / 0 skipped；回归 159 + 26 passed；全仓 679 passed / 6 skipped（基线 671/6；+8 为本 Task 新增） |

## 验证证据

环境：Windows `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；全部 `-p no:cacheprovider`。

| # | 场景 | 命令 / 步骤 | 结果 | 证据 |
|---|---|---|---|---|
| 0 | 基线 | `e8e1750` 导出树全仓 `python -m pytest -q -p no:cacheprovider` | **671 passed, 6 skipped**（多次一致） | — |
| 1 | 主套件 | `python -m pytest tests/rendering tests/pipeline -q -p no:cacheprovider -rs` | **124 passed / 0 skipped**（退出码 0；rendering 56→63、pipeline 60→61） | `pytest-main.log` |
| 2 | 强制回归 | `python -m pytest tests/core tests/storage tests/providers -q -p no:cacheprovider -rs` | **159 passed / 0 skipped**（退出码 0） | `pytest-regression.log` |
| 3 | 域模型消费方 | `python -m pytest tests/editing -q -p no:cacheprovider` | **26 passed**（退出码 0） | — |
| 4 | 全仓 | `python -m pytest -q -p no:cacheprovider -rs`（多次） | **679 passed / 6 skipped** ×3 与 **1 failed / 678 passed / 6 skipped** ×2；失败均为**已登记 flaky** `tests/reading_export/test_qml_contract.py::test_reader_webtoon_swaps_in_vertical_viewer`（见下） | `pytest-full.log`、`pytest-full-clean.log` |
| 5 | 判别力 | 修前 `src/`（`e8e1750` 导出树）+ 本次测试 → `tests/rendering tests/pipeline` | **5 failed / 119 passed**（4 条"非 SFX 不得被拦" + 1 条三方默认一致） | `discriminative-prefix.log` |
| 6 | 门控矩阵与默认值 | 自写脚本：实体/Snapshot/夹具/Schema 默认 + `_sfx_gate_allows` 矩阵 | 四方默认均 `skip`；12 组合与共享规则一致 | `defaults-and-gate-matrix.txt` |
| 7 | 边界与空白 | `git diff --name-only e8e1750..HEAD` + 允许范围正则；`git diff --check 2e1bf2d..HEAD` | 5 个路径全在允许范围、越界 **0**；全范围禁止路径命中 **0**；`--check` 退出码 0 | `changed-paths.txt` |

**skip 原因**：命令 1/2 `0 skipped`；命令 4 的 6 项全为既有 `tests/network` 的 `openssl unavailable`。

**已登记 flaky（如实登记，不掩盖、不归因，未修复）**：`tests/reading_export/test_qml_contract.py::test_reader_webtoon_swaps_in_vertical_viewer` 在**全仓串跑**时低频失败（TASK-017 `doc/reviews/TASK-017-protocol.md` R-007；TASK-019 `doc/reviews/TASK-019-ab26601.md` T-1 已把同族用例登记）。任务禁止顺手修复，本 Task 未改动 `tests/reading_export/**` 与被其覆盖的代码路径。为排除"本 Task 新增用例提高了该 flaky 频率"的可能，我做了对照实验（`tests/pipeline tests/reading_export` 组合，两棵树各 6 次）：结果见 `flaky-ab.log`；结论与残余不确定性一并写入取证 §3.1（**Reviewer 复跑请按同一口径登记**）。

## 越界发现（只登记，未修改）

| ID | 级别 | 位置 | 内容与影响 | 建议 |
|---|---|---|---|---|
| **N-1** | P2（测试基础设施） | `tests/providers/**` 4 个用例的 `from conftest import …` | 裸 `conftest` 模块名在同一 pytest 调用中与 `tests/editing/conftest.py` 冲突：`pytest tests/editing tests/providers` → 137 passed；`pytest tests/providers tests/editing` → **4 collection errors**（`ImportError: cannot import name 'FakeTransport' from 'conftest'`）。**修前树 `e8e1750` 上同样复现**，与本 Task 无关；`tests/providers/**` 不在本 Task 允许路径 | 由后续切片（如 TASK-034 测试硬化）把共享替身移入唯一命名的 helper（如 `providers_helpers.py`）并改导入 |
| **N-2** | P3（已声明） | `src/application/rendering/service.py` | 非 SFX 的取值域外策略不再报错（旧代码对任意类型都 `SfxPolicy(...)`）；这是 AC ① 的必然结果，已在取证 §1/§4 声明 | 无需动作 |
| **N-3** | P3（已声明） | `src/application/rendering/service.py::rerender_region` | 策略校验位置保持"缺 Clean / 无 final"之后（返回顺序未变），因此"SFX + skip 且缺 Clean"仍先返回 `MISSING_REQUIRED_INPUT` | 无需动作；若产品希望策略优先，需用户裁决 |

## 接收方式

1. **分支/工作区**：`agent/deepseek/TASK-035-render-sfx-gate-region-type` @ `6ddd955`；`G:/CODEX/New Manga.worktrees/TASK-035-deepseek`。固定 base `2e1bf2d`。
2. **复现命令**（PowerShell，仓库根）：
   ```powershell
   $env:PYTHONDONTWRITEBYTECODE='1'
   $py = "G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe"
   & $py -m pytest tests/rendering tests/pipeline -q -p no:cacheprovider -rs
   & $py -m pytest tests/core tests/storage tests/providers -q -p no:cacheprovider -rs
   & $py -m pytest -q -p no:cacheprovider -rs
   ```
3. **不需要**额外数据、依赖、端点或权重；渲染套件按产品边界使用默认 Windows 平台（需字体库），不设 `QT_QPA_PLATFORM`。
4. **Reviewer 重点检查**：
   - `_sfx_gate_allows` 的逃生口语义（`sfx`+`manual` 仅显式放行、`sfx`+`skip` 始终拦截）是否与 D06 §47/§85 一致；
   - 渲染层是否真的不再有第二份策略映射（含 `_prepare_region` 的调用方）；
   - `RegionSnapshot` 默认改动（`skip`）是否影响任何消费方（全仓 679/6 与本 Task 的 core/storage/providers/editing 回归均通过）；
   - N-1/N-2/N-3 的处置意见。

## 风险与遗留

- **flaky 归属**：本 Task 观测到的全仓失败全部落在 `tests/reading_export`（未触碰路径），且该用例早已登记；对照实验见 `flaky-ab.log`。若 Reviewer 认为需要更彻底排除，可扩大 A/B 样本量后按同一口径登记（不得顺手修复）。
- `sfx` 取值域外策略在渲染层现在由共享 gate 拒绝（`ValueError`）：与 TASK-032 规划层一致；若产品希望静默按默认处理，需用户裁决后由后续切片调整。
- 未触碰 Schema/migration/依赖清单/seam/`AGENTS.md`/其他 Task；无数据迁移。回退 = 丢弃分支或 `git revert 6ddd955 16cd171`，行为回到 `671 passed` 基线。
