---
task_id: TASK-021
author: ZCode（窗口第二轮 W7 Owner；Reviewer=ZCode 子 agent）
recipient: Codex（T1 后 post-hoc 复审）
base_commit: 047164e（分支起点 202dd21；开工提交 beb50b3 快进至 W5+W6 收口后 HEAD `22eafa8`）
delivery_head: 887e0d6
status: delivered（待窗口内子 agent Review；结论仅 approved_subagent/changes_requested）
---

# Handoff：TASK-021（自洽子集：Page 级软删除 + 同 batch 恢复 + 永久删除只作用受控数据）

## 子集声明（round2 指令要求）

按 round2 指令「**宁可只交付一个完整子集，也不要留下多个半成品**」，本切片只交付 **AC 第 1 条的 Page 级部分**（AC-TRASH-001~004 的用例层与落盘层）。**未开始的子集**（SQLite 一致性备份/恢复、缓存/版本/模型清理分离、日志轮转与诊断包）按原样留在 Task 中，窗口报告将如实登记 `frozen`。

**Schema 预检（无需回抛）**：books/chapters/pages 三表均已含 `deleted_at` 列（`schema.py:36/46/56`）、`SqliteLibraryRepository.soft_delete_page` 已存在（`library.py:371`，单页版）——本子集**零 Schema/migration 变更**。

## 交付内容

**实现 head `887e0d6`**。8 文件，+535：

| 路径 | 内容 | 白名单 |
|---|---|---|
| `src/infrastructure/sqlite/library.py` | `get_pages_by_ids`（**含**软删页——trash 视图必须看见被隐藏行）、`soft_delete_pages`（同 batch 共享同一时间戳；只影响未删行；返回实际数量）、`restore_pages`（清 `deleted_at`）、`purge_pages`（FK 顺序：region_revisions → regions → pages） | `src/infrastructure/sqlite/**` |
| `src/infrastructure/filesystem/managed_storage.py` | `remove_managed(relative_path)`：删受控文件；**拒绝逃逸 managed root 的路径**（`ImmutablePathViolation`） | `src/infrastructure/filesystem/**` |
| `src/application/maintenance/`（新建子包） | `ports.py`（`PageTrashStore`/`ControlledFileRemover`/`TrashManifestStore`/`TrashBatch`——消费端协议，避免应用层依赖具体 storage 类）；`trash.py`（`TrashService`：`soft_delete_pages`→batch、`list_batches`、`restore_batch`（同 batch 恢复）、`purge_batch`（先删受控文件→再删行→清 batch 记录）；manifest=JSON 存 managed 根（可重建记账，零 Schema 变更）） | `src/application/maintenance/**`（白名单明示新建） |
| `src/bootstrap/app.py` | `TrashService` 装配（同数据根 manifest），暴露 `AppServices.trash` | `src/bootstrap/app.py`（白名单明示装配点） |
| `tests/storage/test_trash.py`（新建 5 例）+ `tests/core/test_bootstrap.py`（装配契约 1 例） | 见下表 | `tests/storage/**`、`tests/core/**` |

**AC 保证的落地方式**：

- **软删除不破坏**：`soft_delete_pages` 只翻 `deleted_at`（同 batch 同时间戳），reader/imports 经既有过滤器天然隐藏；恢复只清 `deleted_at`——内容/顺序/managed 引用零损失（测试断言逐字段）。
- **同 batch 恢复**：batch=一次 `soft_delete_pages` 调用（manifest 记录 page_ids）；`restore_batch` 一次恢复整批；多 batch 相互独立（测试断言）。
- **永久删除只作用受控数据**：`purge_batch` ①删 managed 页文件（经 `remove_managed`，防逃逸）→ ②删行（含依赖 regions/revisions）→ ③清 batch 记录。**用户源文件在 managed root 之外，结构性不可达**——测试用 managed 根外的模拟源文件验证逐字节幸存。
- **可诊断**：未知 batch → `KeyError`（typed，非静默）；逃逸路径 → `ImmutablePathViolation`。

## 验证证据（同一 shell + 同一 venv）

**口径**：与 W5/W6 完全相同——`powershell.exe -NoProfile -Command`（Git Bash 会话启动，PATH 继承含 openssl）+ `TASK-012-py312`；全仓 **N passed / 0 skipped** 口径。

| 场景 | 命令 | commit | 结果 |
|---|---|---|---|
| 本切片 | `pytest tests/storage/test_trash.py -q -p no:cacheprovider -rf` | `887e0d6` | **5 passed / 0 skipped**、exit 0 |
| 装配契约 | `pytest tests/core/test_bootstrap.py -q ...` | `887e0d6` | 14 passed（含 trash 装配新例）、exit 0 |
| 全仓 ×5 | `-q -rs -rf` 逐次 | `887e0d6` | 见 `verification/TASK-021/full-suite-runs.log` |

## 未完成子集（窗口报告将登记 frozen）

- SQLite 一致性备份/迁移前备份/恢复（AC-DB-003/004、AC-BACKUP-*）；
- 缓存/版本/模型清理分离（AC-CACHE-*、AC-CLEAN-*、AC-DISK-001）；
- 日志轮转与诊断包（AC-LOG-*）。

## 修订记录（R-001 收口，Review R-005 补记）

- 修订 head=`0fe634f`（在 `887e0d6` 之上）：R-001 fixed——`TrashService.soft_delete_pages` 回查实际软删 id（`deleted_at` 等于本 batch 时间戳者）入 batch，跨 batch 重叠 id 不再串扰；回归 `test_overlapping_soft_delete_does_not_cross_batches`；连带加固 deleted_at 微秒精度＋防撞（同毫秒双 batch 合并缺陷已封，复审复现验证回归钩子有效）。
- 复审：`79ec0d2` = **approved_subagent**（对 `0fe634f`；R-001 fixed 复现+终态 7/7 PASS；R-002~R-004 P3 open 不阻断；R-005 本补记即响应）。
- 修订集成：`b940497`（merge，parents `5bc17f8`+`79ec0d2`），集成后复验全仓 **787 passed / 0 skipped**、exit 0。

## 风险与遗留

1. batch ledger 为 JSON manifest（受控存储内），崩溃窗口理论上可能留下"已软删但 manifest 未记录"的页——此类页仍可被恢复（deleted_at 可查），不是数据丢失；ledger 与 DB 的强一致属后续子集。
2. Chapter/Book 级回收站未含（子集边界）；region 级软删除已有独立实现（TASK-008）。
3. 未 push；未改 Schema/migration、依赖清单、AGENTS、其他 Task；无新增 skip/xfail；既有断言零改动（test_bootstrap 仅追加新用例）。
