---
task_id: TASK-018
author: DeepSeek Harness
recipient: Codex（尾项 Review）
base_commit: 6f130ade18c6c83a42a40a16e7620af518f55e33
delivery_head: 965bcd2308c367d3428f564d68edd68c73ac8545
status: in_review
scope: R-104 尾项（唯一未关闭项）
---

# Handoff：TASK-018 尾项切片（R-104 关闭）

## 交付结果

提交 **`965bcd2`**（5 文件、`+132/−1`）。

**起点**：按指示先把 master 合并到分支——`git merge master --no-edit` 为 **fast-forward**，分支 HEAD 由 `b10f1ba` 前进到 **`6f130ad`**（含 Codex 对 `5063315` 的 `approved` Review `doc/reviews/TASK-018-5063315.md`、集成 `14b92e4` 与集成验证 `integration-14b92e4.md`）。本切片在此之上进行，**未把分支合并回 master**。

**R-104 是本 Task 此前唯一未关闭项**（§9.2/§7.3 记为"部分处置 / deferred"：只实施了"导入来源"断言，缺少 Reviewer 原建议的启动期一致性断言）。本切片补齐。

变更路径（全部在允许范围内；`git diff --check 6f130ad 965bcd2` 退出码 **0**）：

| 文件 | 内容 |
|---|---|
| `experiments/TASK-018/run_experiment.py` | 新增 `assert_implementations_registered()` 并在**模块导入期**调用 |
| `experiments/TASK-018/test_route_gating.py` | 新增 `StartupAssertionTests`（3 例），13 → **16 passed**（**未减少**） |
| `doc/research/TASK-018.md` | 新增 **§9.3**：R-104 → **fixed**（取代 §9.2 的 deferred） |
| `verification/TASK-018/experiment-log.md` | 新增 **§7.4**：R-104 关闭记录与反例取证 |
| `doc/tasks/TASK-018.md` | 状态与运行记录 |

**未改动**（用户明确禁止 / 要求）：`experiments/TASK-018/results/experiment.json`（**未重跑覆盖**）、`experiments/TASK-018/test_mask_protocol.py`、`doc/STATUS.md`、`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/tasks/README.md`、生产 `src/`、`tests/`、Schema、依赖清单、`AGENTS.md`。

## R-104 处置 —— **fixed**

**要求**（Codex 原建议）：`ROUTES[*].implementation` 非 `None` ⇒ 该 route 必须在 `FILLERS` 注册；否则误配应在**启动**即失败。

**实施**：`run_experiment.py` 新增

```python
def assert_implementations_registered(routes=None, fillers=None) -> None:
    ...  # 收集 implementation 非 None 但不在 FILLERS 的 route，抛出 RuntimeError

assert_implementations_registered()   # 模块导入期即执行
```

- 误配在**导入期**（即"启动"）以 `RuntimeError` 失败，消息以 `route/FILLERS misconfiguration:` 开头、以 `refusing to start` 结尾；
- 不再等到运行到该 route 时才 `KeyError`——那个时点**可能已经写出部分输出图**；
- 函数带可选参数，便于测试用**故意破坏的表**演练守卫，而无需修改 harness 文件。

## 反例取证（独立脚本，不在仓库内）

在临时目录复制 `run_experiment.py` + `mask_protocol.py`，**移除 `FILLERS` 中的 `edge-bleed` 条目**（而该 route 仍声明 `implementation = "edge_bleed_fill"`），再以**全新解释器**执行 `import run_experiment`：

| 观测 | 结果 |
|---|---|
| `returncode` | **1**（导入即失败，未进入任何运行路径） |
| 消息 | `RuntimeError: route/FILLERS misconfiguration: edge-bleed declare an implementation but have no registered filler; refusing to start` |
| 写入的 PNG | **`[]`**（未写任何图像） |

> 反例脚本写在 `%TEMP%`，**不在仓库内**；仓库中的 `results/experiment.json` 与 `test_mask_protocol.py` 未被触碰。

## 验证证据

环境：Windows `10.0.26200`；Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312`）；PySide6 6.11.2；`PYTHONPATH=experiments/TASK-018`；`PYTHONDONTWRITEBYTECODE=1`。

| # | 命令 | 退出码 | passed | skipped | 结果 |
|---|---|---:|---:|---:|---|
| 1 | `python -m unittest experiments/TASK-018/test_mask_protocol.py` | **0** | **12** | **0** | `OK`（该文件**未被改动**） |
| 2 | `python -m unittest experiments/TASK-018/test_route_gating.py` | **0** | **16** | **0** | `OK`（13 → 16，新增 3 例，**通过数未减少**） |
| 3 | 反例脚本（临时副本 + 移除 FILLERS 条目 + 新解释器导入） | **0**（脚本）/ **1**（被测进程） | — | — | `png_written = []` |
| 4 | 确定性比对：临时目录 `--repeat 3` 输出 vs **仓库已提交** `results/experiment.json` | — | — | — | `manifest_sha256`、`routes`、`mask_sha256`、`output_sha256`、`sample_sha256`、`parameters`、`mask`、`protected_box_violations` **全部一致（0 差异）** |
| 5 | `git diff --check 6f130ad 965bcd2` | **0** | — | — | 无输出 |
| 6 | 允许/禁止范围检查 | — | — | — | touched = 5，**禁止路径命中 0** |

**skip 原因**：命令 1、2 均 **`0 skipped`**。

**未覆盖确认（脚本判定）**：`results/experiment.json changed = False`；`test_mask_protocol.py changed = False`。

## 未完成项

四条学习型路线（Manga LaMa / AOT / BrushNet-PowerPaint / FLUX）的**质量、耗时、内存与显存**仍为 **BLOCKED**；**Mask 内部结构损伤量化**、**真实 OOM**、**真实漫画样例**仍为 **NOT_RUN**。本切片**未新增任何模型数字**，也未重跑覆盖既有 measured 数据。

## 边界与合规

- 未改 `doc/STATUS.md`、`doc/00_INDEX.md`、`doc/12_ROADMAP.md`、`doc/tasks/README.md`；未改生产 `src/`、`tests/`、Schema、依赖清单、`AGENTS.md`。
- **未重跑覆盖 `results/experiment.json`**；**未改动 `test_mask_protocol.py`**。
- 未释放 TASK-019 或其他冻结 Task；**未 push、未把分支合并回 master**（仅 `merge master` 进分支，为 fast-forward）。

## 状态

`doc/tasks/TASK-018.md` 为 **`in_review`**，交 Codex 尾项 Review。本 Task 不自行标记 `approved`／`done`。
