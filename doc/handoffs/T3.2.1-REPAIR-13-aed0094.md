---
task_id: T3.2.1-REPAIR-13
author: ZCode
recipient: Codex / DeepSeek Harness（独立非作者 Review）
base_commit: 8738c41d7215920935e6fa067d3740e69280cdc6
delivery_head: aed009405fd523c52fa45e604afe40f01cef3d45
status: in_review
---

# Handoff：T3.2.1-REPAIR-13（最终 Delivery aed0094）

本 Handoff 取代 [`T3.2.1-REPAIR-13-c0aba79.md`](T3.2.1-REPAIR-13-c0aba79.md) 的
接收口径（该文件保留为上一段 delivery 的历史记录，其初版授权与追加授权描述仍然有效）。

本段授权（用户 2026-09-25）：按上段 Handoff 的 F-13-2 裁决实施一行修复
（`open()`→`show()`），添加限定范围内的回归守卫，重建固定包并完成本机 GUI
两页导出验证；新 delivery 固定后由 DeepSeek Harness 对 **base `8738c41` →
最终 delivery** 做独立 Review；批准前不合并。

## 提交链（分支 `agent/zcode/T3.2.1-repair-13`）

```text
8738c41  base（REPAIR-12 handoff）
2f18e78  fix(ui): 初版 delivery（导入接线 + 四页面按钮可读性）
df95ad1  docs(handoff): 第一段 Handoff
33f2ff7  docs(review): reviewer_unavailable（DSH 401，第一次）
c0aba79  fix(ui): ExportWindow/ChapterPicker 文字对比 + 补验证
efe9c85  docs(handoff): 第二段 Handoff（本机验证替代 Sandbox）
c47e6da  docs(review): DSH 401 第二次记录
9d6e3b8  fix(ui): F-13-2 —— ReaderView.qml:62 show() the export window
aed0094  fix(ui): F-13-4 —— ExportWindow 输出路径 TextField 补 id
（本 Handoff 提交为 evidence commit，跟在 aed0094 之后）
```

## 本段变更路径（c0aba79 → aed0094）

- `src/ui/qml/reader/ReaderView.qml` —— F-13-2：`exportWindowComponent` 内
  `Component.onCompleted: open()` → `show()`（ExportWindow 根是 Window，无
  `open()`；每次点击"导出…"抛 `ReferenceError`，窗口创建但从不显示）。
- `src/ui/qml/windows/ExportWindow.qml` —— F-13-4：输出路径 TextField 补
  `id: exportOutputPath`（`exportRunButton.onClicked` 读取 `exportOutputPath.text`，
  此前该名字只有 objectName 没有 id，handler 首行抛
  `ReferenceError: exportOutputPath is not defined`，`startExport()` 永不执行）。
- `tests/ui_shell/test_repair13_export_window_open.py`（新增 2 例，均判别性：
  修复前 RED 日志 `f13-2-pre-repair-red.log` / `f13-4-pre-repair-red.log` 入库）。
- `verification/T3.2.1/repair-13/**`（build7/build8 证据、RED 日志、GUI 日志、
  ZIP hash、截图、[`local-verification.md`](../../verification/T3.2.1/repair-13/local-verification.md) 增量章节）。
- 未改动：Python 业务逻辑、export service、`packaging/**`、依赖、Schema、
  `src/bootstrap/**`、theme tokens。

> 范围声明：F-13-4 在用户授权的"完成本机 GUI 两页导出验证"目标的关键路径上，
> 与 F-13-2 同性质（先存 QML 一行缺陷、阻断同一真实 UI 导出链路、同文件），
> 按达成既定目标所需的最小修复实施，在此如实登记交 Review 裁认。若 Review
> 不认，revert `aed0094` 即回到"F-13-2 已修、导出按钮仍被 F-13-4 阻断"的状态。

## 判别性验证（§6.12 口径）

- 守卫测试加载**真实** ReaderView.qml（stub readerViewModel 经 context property
  注入，与 bootstrap 同契约），驱动真实窗口按钮：
  - `test_export_button_opens_visible_window_without_referenceerror`：断言无
    QML ReferenceError 且 `exportWindow` 实例 **visible**（修复前：实例存在但
    visible=false + ReferenceError——正是 build6 本机 GUI 的失败形态）。
  - `test_export_run_button_passes_path_and_starts`：在真实 ExportWindow 内设
    输出路径后点击 `exportRunButton`，断言无 ReferenceError、`setOutputPath`
    收到键入路径、`startExport()` 被调用（修复前：`ExportWindow.qml:205`
    ReferenceError、start_calls=0——与 build8 前夜的打包 GUI stderr 一字不差）。
- 套件：`pytest tests/ui_shell tests/reading_export/test_qml_contract.py`
  → **162 passed，exit 0**（venv `TASK-012-py312`）。

## 固定包与本机 GUI 验证（用户裁决：本机验证代替 Sandbox）

| 构建 | Delivery | EXE SHA-256 | 说明 |
|---|---|---|---|
| build7 | `9d6e3b8…` | `11545a0d…` | 中间包：验证 F-13-2（窗口出现），同轮发现 F-13-4 |
| **build8** | **`aed0094…`** | `8b2343ddb295e5b69699851bc10701311ed285ce3b52b911593538e87a7bc6e1` | 最终包，manifest 5437 条 |

build8 本机 GUI 全流程（真实鼠标/键盘、`--data-root` 隔离、stderr 全程采集）：
建书→建章→选章→原生对话框双 fixture 导入（DB 2 页、hash 与源一致）→
**"导出成果"窗口真实打开**（F-13-2 修复生效；build6 同操作静默无窗口）→
切 Original + "继续导出现有版本" + 输出路径 → 点"导出"→ 状态"**导出完成**"→
ZIP 两页且**字节级与 fixture 一致**（`0001_fixture-page-a.png`/`0002_fixture-page-b.png`，
`97fbf856…`/`3294fd29…`）→ 导出窗口与主窗口正常关闭、进程无残留、
stderr 无 ReferenceError。明细：[`local-verification.md` 增量章节](../../verification/T3.2.1/repair-13/local-verification.md)。

## AC 对照（最终口径，取代上段）

- **R13-AC1** PASS（上段 delivery + 本段 build8 复证：提示随选择状态切换、导入按钮启停）。
- **R13-AC2** PASS（探针 + build8 本机包：原生对话框真实打开、双 fixture 导入、
  Managed Copy hash 一致、DB 2 页）。
- **R13-AC3** PASS（四页面 + 两对话框按钮可辨；build8 导出窗口按钮截图入库
  `screenshots/10-export-window-export-complete.jpg`；F-1 更正：该截图为 JPEG
  字节，扩展名已由 .png 更正为 .jpg）。
- **R13-AC4** **PASS（本段解除 BLOCKED）**：真实 UI 全路径（窗口打开 → Original +
  继续导出 → 输出路径 → 导出完成）生成两页 ZIP，字节级与源一致；Workbench 两页
  显示与选择在上段探针已证。
- **R13-AC5** 本机验证替代完成（用户 2026-09-25 裁决；开发机与干净 Windows 的
  覆盖差异在 local-verification.md 声明）；build7/build8 构建日志、manifest、
  EXE SHA-256 齐备。
- **R13-AC6** 变更路径清单如上；最终判定待独立 Review（base `8738c41` → `aed0094`）。

## 遗留（交 Codex，未在本切片触碰）

- **F-13-3（P2）**：重启后书架列表为空（DB 有书）；本段两次 GUI 会话均为全新
  data-root 一次通过，未受影响，问题维持原状待排查。
- **信息**：章节行页数文本导入后仍显示"0 页"（DB 已 2 页，model 未刷新页数列）。
- 上段 Handoff 中"F-13-2 微切片——交 Codex 裁决"一项已由用户裁决并在本段完成；
  同步清单其余项以本文件为准。

## Codex 同步清单

1. `doc/tasks/T3.2.1-REPAIR-13.md` / `doc/STATUS.md`：登记本段授权（F-13-2 微切片
   + 完成两页导出验证）、delivery head 更新为 `aed0094`、AC4 由 BLOCKED 改 PASS
   （以独立 Review 通过为准）。
2. DSH 凭证修复后重派 Review：**base `8738c41` → 最终 delivery `aed0094`**
   （完整 diff），可复用派发 prompt 见
   [`review-dispatch-record.md`](../../verification/T3.2.1/repair-13/review-dispatch-record.md)。
3. F-13-3 与页数列刷新排期（新 Task 或并入父 Gate 收尾）。

## 风险与回退

- 两个修复均为 QML 单行、互不依赖、有判别性守卫；revert 单个 commit 即回退对应
  修复，无 Schema/依赖/数据迁移影响。
- F-13-4 的范围裁认权在 Review/用户：若不认，revert `aed0094` 不影响 F-13-2。
- 批准前禁止合并 `master`；父 Gate（T3.2.1 Release Gate）不因本段改判，AC4 的
  PASS 供 Codex 在 Gate 收尾时采信。
