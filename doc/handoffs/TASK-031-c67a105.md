---
task_id: TASK-031
author: Codex
recipient: DeepSeek Harness / Codex Integrator
base_commit: 29b146f76b46682c6e9de3c40631fd7a5debed16
delivery_head: c67a105
reviewed_head: c67a105c4907be07166fe319c77f56ac4cc5f2f0
implementation_merge: 0b44b83bec8df8efad52d69f72c7aad2237e530b
integration_commit: e9d5185259ffa175b64d67d5ac00befadc6db643
review_report_commit: 0509003e4df0ca84428d61faf32dd16a70168d30
status: integrated
---

# Handoff：TASK-031

## 交付结果

实现提交：`c67a105`（`feat(TASK-031): add production image import adapters`）；R-001～R-003 收口提交：`d4fe993`。

- `G:/CODEX/New Manga/src/infrastructure/importing.py`
  - `QtImageDecoder` 使用 PySide6 `QImageReader` 解码 bytes，返回尺寸/MIME，解码失败抛 `ImageDecodeError`。
  - `ManagedCopyStoreAdapter` 接收 `book_id_for_chapter` resolver，复用 `ManagedFileStorage` 完成 temp 写入、SHA-256 校验和 D03 `original/` 不可覆盖发布；发布失败清理 temp。
- `G:/CODEX/New Manga/tests/library/test_production_adapters.py`
  - 覆盖有效/损坏 Qt 图片、D03 路径、逐字节 Managed Copy、use case 真实适配器链和失败清理/路径安全。

未修改 `src/application`、`src/domain`、`src/ports`、Schema、依赖清单、`src/bootstrap`、`src/ui` 或冻结 Task。

## Acceptance Criteria 对照

- [x] Qt 解码、尺寸/MIME 和损坏输入拒绝：专测通过。
- [x] Managed Copy 走 D03 路径、hash 校验、不可覆盖 publish、失败清理：专测通过。
- [x] 生产两个适配器驱动现有 `ImportImagesUseCase`，copy 成功后才创建 Page：专测通过。
- [ ] 独立 Review、主线 integration_commit、TASK-030 真实入口装配：不属于本交付 head，保持未完成。

## 验证证据

详见 [author-verification-c67a105.md](../../verification/TASK-031/author-verification-c67a105.md) 与 [integration-e9d5185.md](../../verification/TASK-031/integration-e9d5185.md)：

- 专测：`8 passed, 0 skipped`。
- library/import 回归：`40 passed, 0 skipped`。
- Windows 默认 Qt `tests/ui_shell -v`：`46 passed, 0 skipped`。
- 全量：`376 passed, 6 skipped`；6 项均因 `openssl unavailable`。
- 范围与 whitespace 检查均 PASS。

## 集成与 Review finding 收口

- `implementation_merge=0b44b83`：Codex 在 `master` 上以 `--no-ff` 合并作者分支。
- `integration_commit=e9d5185`：Codex 随后以 `--no-ff` 合并 approved Review 报告分支 `0509003`；未合并其他分支，未 push。
- **R-001 closed**：真实生产 `ManagedCopyStoreAdapter` 经过 `ImportImagesUseCase` 的 copy-failure E2E 断言 `COPY_FAILED`、零 Page 与 temp 清理。
- **R-002 closed**：storage 注解改为 `ManagedFileStoragePort`。
- **R-003 closed**：未知/空格式 MIME fallback 改为 `application/octet-stream`，并有回归测试。

## 接收方式与下一步

Reviewer 固定审查 `base_commit=29b146f76b46682c6e9de3c40631fd7a5debed16` 至 `delivery_head=c67a105` 的两文件 diff。后续入口装配只能通过 `book_id_for_chapter` 绑定真实 Chapter→Book 查询，不得硬编码 book id；TASK-030 在该切片独立 Review/集成前仍保持 BLOCKED。

## 风险与遗留

- Review `0509003` 已 approved；其 P2 finding R-001～R-003 已由 `d4fe993` 收口。
- 已合入 master，`integration_commit=e9d5185`；任务已完成集成，但不释放任何后续冻结 Task。
- Qt MIME 映射覆盖当前常见 image plugin 格式；未来新增 Qt 可解码格式时需补显式 MIME 映射与测试，不改变应用 Protocol。
- `TASK-030` 的 bootstrap/Main.qml 装配、真实入口 smoke、DPI 截图和真实 SQLite 导入探针均保持 `BLOCKED`/`NOT_RUN`，不得由本 Task 的局部测试替代。
