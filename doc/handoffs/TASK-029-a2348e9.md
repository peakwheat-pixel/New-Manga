---
task_id: TASK-029
author: ZCode
recipient: Codex（转 DeepSeek Harness 独立 Review）
base_commit: 79529bc9805fda6b72b99860f649fca0922c6cf9
delivery_head: a2348e91824e90db74bfba7aa9e805309f499797
status: rejected
---

# Handoff：TASK-029（统一 SQLite 持久化）

> 状态回填（2026-09-15）：本 head `a2348e9` 已被 DeepSeek Harness 首轮 Review 判为
> `changes_requested`（[报告](../reviews/TASK-029-a2348e9.md)）而拒绝；有效结论以修订轮
> [Handoff TASK-029-5abc173](TASK-029-5abc173.md)（含 F-04 计数勘误）与
> [复审报告](../reviews/TASK-029-5abc173.md)（approved）为准。本文件其余内容按原样保留作审计记录。

实现 head：`a2348e9`。提交列表（自 release `c6db2c0`）：

| commit | 说明 |
|---|---|
| `04d0a5d` | docs(TASK-029): 接管认领，ready → in_progress（基线核验：HEAD=release `c6db2c0`，含 base `79529bc` 与 TASK-028 集成） |
| `a2348e9` | feat(TASK-029): v2 迁移 + Library/Page/Region adapter + 原子 seam + 测试（reviewed_head） |

变更路径（10 文件，全部在允许白名单内）：`src/infrastructure/sqlite/{schema,library,regions}.py`、`src/application/{library,importing/images,editing}/ports.py`（importing/ports.py 经核对**未需要修改**——ImportPageSink 三方法契约可直接由新 Adapter 结构化实现）、`src/application/editing/service.py`、`tests/{storage,library,editing}/**`。`src/domain`、QML/UI、Provider/Pipeline/TM/Constraint、依赖清单、AGENTS、TASK-028 设计、**v1 migration SQL（零字符改动）**、其他 Task：零触碰。

## 交付内容与 AC 对照（依 TASK-028 冻结设计）

- **AC1 v2 迁移**：**满足**。`v2__library_region_persistence`：books/chapters 按 §3.1 补列（`TEXT NOT NULL DEFAULT ''`/枚举 CHECK/布尔 0-1 CHECK）；pages 新导入列对 v1 占位行可 NULL、非 NULL 值带完整性 CHECK；tags/book_tags（无 `UNIQUE(name)`，§3.2）；regions/region_revisions（§3.4：JSON 列 NOT NULL、`UNIQUE(region_id, revision_no)`、current 与 restored 双复合 DEFERRABLE FK、current 不可清空触发器）。v1 SQL 零改动（`test_v1_schema_is_unchanged` 结构断言）；迁移仍走现有 MigrationRunner（备份/checksum/单事务/失败回滚/TOO_NEW 只读门全部保留并测试）。
- **AC2 SqliteLibraryRepository**：**满足**。结构化实现 `LibraryRepository` + `ImportPageSink` + `PageRepository`（新 Protocol 已加到 `application/library/ports.py`，§3.3 四方法）；Tag 重名检查留在 `LibraryService`，数据库层验证无 UNIQUE 约束（`test_no_unique_name_constraint_at_db_level`）；Tag 删除=关系+标签同一事务。
- **AC3 Page round-trip 与占位兼容**：**满足**。完整导入行 round-trip + 重启读回；v1 占位行（导入列 NULL）永不伪造为 Page（mapper 拒绝 + 查询过滤）；`source_order`/`sort_order` 分离、软删、源文件只读（TASK-007 测试继续成立）；FK 拒绝悬空 chapter。
- **AC4 SqliteRegionRepository**：**满足**。JSON/枚举/Lock/Pin/current/restored 溯源映射齐全；复合 DEFERRABLE FK（current + restored 同型）、`UNIQUE(region_id, revision_no)`、触发器拒绝清空 current、跨 region current 被 FK 拒绝（`test_current_pointer_invariants_enforced_by_database`）。
- **AC5 `commit_region_revision` 原子 seam**：**满足**。`SqliteRegionRepository` 在单 `BEGIN IMMEDIATE` 内：重读 current 与三锁 → expected 比较（`INPUT_REVISION_CHANGED`）→ Lock 快照比较（`LOCK_CHANGED`）→ 插入不可变 Revision（事务内分配 revision_no）→ 从 snapshot 同步 revision-owned Region 状态 → 更新 current → COMMIT；异常/冲突整体回滚保留旧 current（`DB_FAILED`/冲突分支均有测试）。编辑服务全部写路径（用户与自动）收敛到该 seam，`add_revision()` 只承担显式历史插入。
- **AC6 revision-owned 状态纪律**：**满足**。`reorder_regions` 为每个变更 Region 创建 user Revision（`test_reorder_creates_user_revisions`）；`set_revision_pinned` 单事务仅翻 `is_pinned`（`test_pin_updates_flag_only`）；恢复为新增 restored Revision（无任何同 ID upsert——fake 与 SQLite 实现均无此路径）。
- **AC7 v2 回归**：**满足**。TASK-006 Artifact safe commit 全套在 v2 库通过（`db_conn` fixture 即 v2）；storage/library/editing 全部既有测试通过；失败/冲突/取消不改 current（既有 + 新增探针）。
- **AC8**：本 Handoff + verification + NOT_RUN 交付；`in_review`，done 待 DeepSeek 非作者 Review + Codex 集成。

## 验证证据

完整记录见 [verification/TASK-029/author-verification.md](../../verification/TASK-029/author-verification.md)。摘要（全部退出码 0）：

| 场景 | 命令 | 结果 |
|---|---|---|
| storage（含 v2 迁移/备份/TOO_NEW/Artifact v2 回归/边界） | `python -m pytest tests/storage -v` | 33 passed |
| library（含 SQLite round-trip/重启/占位兼容） | `python -m pytest tests/library -v` | 32 passed |
| editing（含原子 seam/冲突回滚/Pin/恢复/重排/触发器） | `python -m pytest tests/editing -v` | 25 passed |
| 全量回归 | `PYTHONPATH=src python -m pytest tests` | 95 passed |
| whitespace / 范围 | `git diff --check c6db2c0 a2348e9 --`；name-only | PASS / 10 文件全在白名单 |

NOT_RUN / N/A：多连接并发竞争（busy_timeout 已配置，协调属 TASK-011/集成验证）；bootstrap 装配（范围外）；`managed_original_ref` 与 D03 `managed_original_artifact_id` 收敛（TASK-028 §3.1 保留）；R-201/R-202（TASK-028 §7 不裁决）；迁移损坏库恢复端到端（TASK-021）；性能（无 AC）。

## 复审建议

重放四条命令；重点：① v1 SQL 冻结（`git diff c6db2c0 a2348e9 -- src/infrastructure/sqlite/schema.py` 中 v1 常量块零变化 + `test_v1_schema_is_unchanged`）；② `SqliteRegionRepository.commit_region_revision` 事务顺序与 §4.2 五步一一对应、失败回滚完整性（探针建议：注入第二连接在事务中并发更新，观察 busy_timeout 后回滚语义——单连接语义已由测试覆盖）；③ 编辑服务 staged-copy 语义（冲突时调用方对象不变的内存视图保证）；④ Page 占位行兼容边界（mappper 拒绝伪造）；⑤ LibraryService 与 Adapter 的重复检查分工（无 UNIQUE(name)）。

## 风险与遗留

1. `commit_region_revision` 的 `DB_FAILED` 在编辑服务用户路径映射为 RuntimeError（无静默 no-op）；自动路径完整语义（Candidate 落库）属 TASK-011。
2. 并发未实测（NOT_RUN）；SQLite WAL + busy_timeout 配置就绪。
3. `pages` 占位行转正（回填迁移）属后续明确 Task；本切片只保证不伪造。
4. 本切片不释放其他 Task；`importing/images/ports.py` 核对后零改动（白名单允许但无需）。
