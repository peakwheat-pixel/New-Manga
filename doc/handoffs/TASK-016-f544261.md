---
task_id: TASK-016
author: DeepSeek Harness
recipient: Codex（第三次复审）
base_commit: f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86
delivery_head: f5442615911cf60125fe8bde604c5237f482610b
status: in_review
supersedes: 9ef06d9cdecdbe98d3894ce638294d9a14408574
---

# Handoff：TASK-016 修订轮 3（R-005 离线强制）

本 Handoff 交付 `f544261`，处理 R-005 的离线强制缺陷。`9ef06d9` / `af958b3` 的结论不再有效；`base_commit` 不变。

| commit | 内容 |
|---|---|
| `f544261` | fix(TASK-016)：R-005 强制离线覆盖 + 反例回归证据（**delivery_head**） |

## 缺陷与修复

**缺陷**：上一轮 `enforce_offline()` 使用 `os.environ.setdefault(name, "1")`。**`setdefault` 不会覆盖已存在的值**——若调用方预置 `HF_HUB_OFFLINE=0`，该值会被保留，Provider 库仍可能访问网络，而实验记录却显示"已启用离线"。即**记录的是意图，不是实际生效的值**。

**修复**：`enforce_offline()` 改为**无条件赋值** `os.environ[name] = "1"`，并**从环境读回**构成返回映射，使证据记录强制后的真实值。权重侧的 R-005 门槛保持并加强：`local_weight_gate()` 必须找到**真实存在的本地权重路径**（并对其内容计算 digest 作为 `model_sha256`），否则在 **import/构造之前**返回 `BLOCKED`——Provider 不会有机会自行获取权重。

## 修复要求对照

| 要求 | 处置 | 证据 |
|---|---|---|
| 1. 离线变量必须强制覆盖为 1，不能 `setdefault` | **fixed**：`os.environ[name] = "1"` 无条件赋值，返回值从环境读回 | 反例验证（要求 3） |
| 2. Provider 只能使用已校验的本地权重；无法保证时构造器前 `BLOCKED` | **fixed**：`local_weight_gate()` 在 `manga-ocr`/`paddleocr-korean` 的 **import 与构造之前**调用；路径不存在 → `BLOCKED`，不进入 `MangaOcr()`/`PaddleOCR()` | `run_experiment.py` 的 `_run_manga_ocr`/`_run_paddle` 调用顺序；本地无权重路径时输出 `weight gate: no local weight path found …` |
| 3. 增加预置 `=0` 的回归验证 | **fixed**：见下方"反例"行 | 命令 15 |
| 4. 保持 `detector-yolo` 为 `DOCUMENTATION_ONLY` | **保持**：本轮未改动该项，仍为 `DOCUMENTATION_ONLY — no implementation source in this repository` | `CANDIDATES` / `verification_matrix` |

## 验证证据（本轮，全部退出码 0）

环境：Windows 10.0.26200 / Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312`）/ `PYTHONPATH=experiments/TASK-016` / `PYTHONDONTWRITEBYTECODE=1` / `QT_QPA_PLATFORM` 未设置（本实验不加载 Qt）。

| # | 命令 | 退出码 | passed | skipped | 结果 |
|---|---|---:|---:|---:|---|
| 11 | `python -m unittest experiments/TASK-016/test_protocol.py -v` | **0** | **5** | **0** | `OK` |
| 12 | `python experiments/TASK-016/run_experiment.py --output experiments/TASK-016/results/probe.json` | **0** | — | — | `{"BLOCKED": 30}`（6 候选 × 5 样本），不联网、不下载 |
| 13 | `… --output experiments/TASK-016/results/run-models.json --run-models` | **0** | — | — | `{"BLOCKED": 30}`；`offline_environment` 三值均为 `1` |
| 14 | `… --output experiments/TASK-016/results/model-run-blocked.json --run-models` | **0** | — | — | `{"BLOCKED": 30}`；同上（与 13 同代码同模式） |
| **15** | **反例**：先 `HF_HUB_OFFLINE=0`、`TRANSFORMERS_OFFLINE=0`、`HF_DATASETS_OFFLINE=0`，再执行 `--run-models` 输出到仓库外临时文件 | **0** | — | — | `offline_environment = {"HF_HUB_OFFLINE": "1", "TRANSFORMERS_OFFLINE": "1", "HF_DATASETS_OFFLINE": "1"}`，**强制覆盖成立**（断言 `全部为 1 = True`） |
| 16 | `git diff --check f9edd68 f544261`（工作区与暂存区） | **0** | — | — | 无输出 |
| 17 | 白名单范围检查（`git diff --name-only f9edd68 f544261`） | — | — | — | 21 路径，**越界 0** |

**skip 原因**：命令 11 为 `0 skipped`（协议测试不依赖模型/网络/第三方包）；命令 12~15 为探测进程，无测试项。

## 三份固定结果（内容未变，SHA 与上一轮一致）

| 文件 | schema | 候选 | 记录 | 状态 | `blocked_with_metrics` | `offline_environment` | SHA-256 |
|---|---|---:|---:|---|---:|---|---|
| `results/probe.json` | `task016-experiment-result-v2` | 6 | 30 | `{BLOCKED: 30}` | **0** | `null`（probe 模式不进入构造器，故不强制） | `b3c30a8bca68d7d8466ffa7246e2b13fcd2a73173a397977e915a67ab2c91e2a` |
| `results/run-models.json` | 同上 | 6 | 30 | `{BLOCKED: 30}` | **0** | `{HF_HUB_OFFLINE: "1", TRANSFORMERS_OFFLINE: "1", HF_DATASETS_OFFLINE: "1"}` | `5986fc158edb154a37d38e4ef10390d05bf92476084aa309988eb6f4cf4421bd` |
| `results/model-run-blocked.json` | 同上 | 6 | 30 | `{BLOCKED: 30}` | **0** | 同上 | `5986fc158edb154a37d38e4ef10390d05bf92476084aa309988eb6f4cf4421bd` |

结果内容与上一轮**逐字节一致**（SHA 相同）：上轮在无预置环境下得到的 `1,1,1` 本就是正确值，本轮修复的是"预置 `=0` 时不再被覆盖"的路径，因此不改变既有证据。

完整记录见 [verification/TASK-016/experiment-log.md](../../verification/TASK-016/experiment-log.md) 末节「Review R-005 修订证据（第四轮：强制离线覆盖）」。

## 未完成项与阻塞（环境事实，未变）

| 项 | 状态 | 原因 |
|---|---|---|
| 真实 OCR/检测质量、错误率、耗时/RAM/VRAM | **BLOCKED** | 依赖全缺、权重不可下载、无授权端点；R-005 门槛在缺权重时**主动拒绝**进入构造器 |
| 真实漫画样本质量 | **NOT_RUN** | 仅自制合成样本 |
| 版本 / 许可权威确认 | **NOT_RUN** | 需有网络环境核实，尤其**权重**条款 |
| `detector-yolo` 去留 | **待裁决** | 现为 `DOCUMENTATION_ONLY`，请 Codex 决定保留或移除 |

## 边界与合规

- 本轮仅改动 `experiments/TASK-016/run_experiment.py`（13 行）与 `verification/TASK-016/experiment-log.md`（追加 39 行）；白名单**越界 0**。
- 未修改生产 `src/`、`tests/`、Schema/migration、依赖清单、`AGENTS.md`、其他 Task 或任何冻结 Task（含 TASK-015）；未 push、未合并。
- **未以 Mock 冒充模型质量**：所有模型侧字段仍为 `NOT_RUN`/`None`，`model_sha256` 恒为 `NOT_AVAILABLE`。

## 状态

停在 **`in_review`**，交 Codex **第三次复审**。本 Task 不自行标记 `approved`／`done`，不填写 `integration_commit`。
