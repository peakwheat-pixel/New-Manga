---
task_id: TASK-016
author: DeepSeek Harness
recipient: Codex（第二次复审）
base_commit: f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86
delivery_head: 9ef06d9cdecdbe98d3894ce638294d9a14408574
status: in_review
supersedes: 4834447992974d4a16bad35c0e8029bf34ecfed4
---

# Handoff：TASK-016 修订轮 2（R-002、R-003、R-005～R-009、detector-yolo 范围）

本 Handoff 交付 `9ef06d9`，处理 `doc/reviews/TASK-016-4834447.md` 的 findings。`4834447` / `000ed5b` 的结论不再有效；`base_commit` 不变。

| commit | 内容 |
|---|---|
| `9ef06d9` | fix(TASK-016)：R-002/R-003/R-005~R-009 + detector-yolo 范围（**delivery_head**） |

## Findings disposition

| ID | 级别 | 处置 | 实现与证据 |
|---|---|---|---|
| **R-002** | P2 | **fixed** | 新增 `_fallback_gate()`，**所有失败路径统一门控**：`_run_manga_ocr` / `_run_paddle` / `_run_openai_compatible` 的异常分支与 `_blocked()` 均写入同一判定。空配置或未点名 → `BLOCKED: no explicitly configured and named fallback route`；仅当 `TASK016_FALLBACK_ROUTE` 已配置且被点名时才 `FALLBACK_CONFIGURED`。**实测**：三份结果的 `fallback` 取值集合均为单一值（见下表） |
| **R-003** | P2 | **fixed** | `doc/research/TASK-016.md` 第 1 节：表**前**加入 `⚠ UNVERIFIED CANDIDATE METADATA` 声明段，**表头**直接写成 `候选 / 版本（UNVERIFIED）` / `许可证（UNVERIFIED）`；脱离附录阅读也不会误认为已核实，并保留"权重条款可能与代码许可不同"的声明 |
| **R-005** | P1 | **fixed** | 模型运行前置双重门槛：①`enforce_offline()` 在进入任何构造器前强制 `HF_HUB_OFFLINE=1`、`TRANSFORMERS_OFFLINE=1`、`HF_DATASETS_OFFLINE=1`；②`local_weight_gate()` 要求 `TASK016_MANGA_OCR_WEIGHTS`/`TASK016_MANGA_OCR_MODEL_DIR`（或 `TASK016_PADDLEOCR_*`）指向**真实存在的本地路径**，并对其内容计算 digest 作为 `model_sha256`；缺失即在 **import/构造之前** 返回 `BLOCKED`。**实测**：`--run-models` 两份结果的 `offline_environment` 三值均为 `"1"` |
| **R-006** | P1 | **fixed** | 三份固定结果**全部按最新代码重生成**：`probe.json`（probe-only）、`run-models.json`、`model-run-blocked.json`。三者一致为 `schema=v2`、**6 候选**、**30 记录**、全 `BLOCKED`；`violation=0`（`blocked_with_metrics = 0`）。`run-models.json` 与 `model-run-blocked.json` 由同代码同模式生成，内容与 SHA 相同（前者为权威名，后者保留兼容既有引用） |
| **R-007** | P2 | **fixed** | 重新计算并同步三份结果的 SHA-256（下表）；旧记录中的 `bd33d38…` 已被取代 |
| **R-008** | P2 | **fixed** | `doc/tasks/TASK-016.md`：正文状态表述改为**历史**（明确"当前状态以顶部 frontmatter 为准"）；「交付与运行记录」补入**实际** Handoff 与 Review 链接（含 `doc/reviews/TASK-016-a286578.md`、`TASK-016-4834447.md`）；末尾仅保留一条 **「当前状态（唯一）」** |
| **R-009** | P2 | **fixed** | `_blocked()` 把 `metrics` **全量清零**；`_run_detector()` 的前置门槛位于**任何资源采样之前**（detector 路径不再调用 `_memory_mb()`/`_vram_mb()`）。**实测**：三份结果的 `blocked_with_metrics = 0`（此前 15 条 detector 记录含 `vram_before_mb`） |
| **R-001 残留** | P1 | **fixed（范围已明确）** | `detector-yolo` 在 `CANDIDATES` 中标为 `DOCUMENTATION_ONLY — no implementation source in this repository`，并写入 `verification_matrix[0].scope_decision`；`_run_detector()` 对其给出专门的 `BLOCKED` 理由。**请 Codex 裁决**：保留为文档候选（现状）或从矩阵移除 |

## 验证证据（本轮，全部退出码 0）

环境：Windows 10.0.26200 / Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312`）/ `PYTHONPATH=experiments/TASK-016` / `PYTHONDONTWRITEBYTECODE=1` / `QT_QPA_PLATFORM` 未设置（本实验不加载 Qt）。

| # | 命令 | 退出码 | passed | skipped | 结果 |
|---|---|---:|---:|---:|---|
| 1 | `python -m unittest experiments/TASK-016/test_protocol.py -v` | **0** | **5** | **0** | `OK` |
| 2 | `python experiments/TASK-016/run_experiment.py --output experiments/TASK-016/results/probe.json` | **0** | — | — | `{"BLOCKED": 30}`，不联网、不下载 |
| 3 | `… --output experiments/TASK-016/results/run-models.json --run-models` | **0** | — | — | `{"BLOCKED": 30}` |
| 4 | `… --output experiments/TASK-016/results/model-run-blocked.json --run-models` | **0** | — | — | `{"BLOCKED": 30}`（与 3 同代码同模式） |
| 5 | `git diff --check f9edd68 9ef06d9` | **0** | — | — | 无输出 |
| 6 | 白名单范围检查（`git diff --name-only f9edd68 9ef06d9`） | — | — | — | 20 路径，**越界 0** |

**skip 原因**：命令 1 为 `0 skipped`（协议测试不依赖模型/网络/第三方包）；命令 2~4 为探测进程，无测试项。

## 固定结果与 SHA-256（R-006 / R-007）

| 文件 | schema | 候选 | 记录 | 状态 | `blocked_with_metrics` | SHA-256 |
|---|---|---:|---:|---|---:|---|
| `results/probe.json` | `task016-experiment-result-v2` | 6 | 30 | `{BLOCKED: 30}` | **0** | `b3c30a8bca68d7d8466ffa7246e2b13fcd2a73173a397977e915a67ab2c91e2a` |
| `results/run-models.json` | `task016-experiment-result-v2` | 6 | 30 | `{BLOCKED: 30}` | **0** | `5986fc158edb154a37d38e4ef10390d05bf92476084aa309988eb6f4cf4421bd` |
| `results/model-run-blocked.json` | `task016-experiment-result-v2` | 6 | 30 | `{BLOCKED: 30}` | **0** | `5986fc158edb154a37d38e4ef10390d05bf92476084aa309988eb6f4cf4421bd` |

三份结果的共同不变量：质量/性能字段为 `NOT_RUN`/`null`；`model_sha256` 取值集合为 `["NOT_AVAILABLE"]`（**未以样本哈希冒充**）；`fallback` 取值集合为 `["BLOCKED: no explicitly configured and named fallback route"]`；`metadata_status` 全为 `UNVERIFIED`。

完整记录见 [verification/TASK-016/experiment-log.md](../../verification/TASK-016/experiment-log.md) 末节「Review R-002/R-003/R-005~R-009 修订证据（第三轮）」。

## 未完成项与阻塞（环境事实，未变）

| 项 | 状态 | 原因 |
|---|---|---|
| 三候选的**识别错误率**、**检测真实运行** | **BLOCKED** | 依赖全缺、权重不可下载（`huggingface.co` 不可达）、无授权端点；R-005 门槛在缺权重时**主动拒绝**构造器 |
| **耗时 / RAM / VRAM**（模型推理） | **BLOCKED** | 无推理栈；且按 R-009，阻塞结果**不采集**任何指标 |
| **样本质量**（真实漫画分布） | **NOT_RUN** | 仅自制合成样本 |
| 版本 / 许可的**权威确认** | **NOT_RUN** | 需有网络环境核实，尤其**权重**条款 |

## 边界与合规

- 未修改生产 `src/`、`tests/`、Schema/migration、共享 Protocol、`requirements*.txt`、`AGENTS.md`、其他 Task 或任何冻结 Task（含 TASK-015）；白名单**越界 0**。
- 未 push、未合并任何分支。
- `--run-models` 在缺依赖/权重时一律 `BLOCKED`，**禁止自动下载**（离线开关 + 本地权重门槛双重保障）；**未以 Mock 冒充模型质量**。

## 状态

停在 **`in_review`**，交 Codex **第二次复审**。本 Task 不自行标记 `approved`／`done`，不填写 `integration_commit`。
