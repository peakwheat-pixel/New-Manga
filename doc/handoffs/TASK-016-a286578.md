---
task_id: TASK-016
author: DeepSeek Harness
recipient: Codex（独立 Review）
base_commit: f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86
delivery_head: a286578ad951fc0f3c391a08e724a1c0ecb10059
status: in_review
---

# Handoff：TASK-016 OCR 与检测路线独立实验

## 交付结果

实现/实验提交：`a286578`（`feat(TASK-016): OCR/detection experiment harness, synthetic samples, research and evidence`），固定基线 `f9edd68`，分支 `agent/deepseek/TASK-016-ocr-detection-experiment`，工作区 `G:/CODEX/New Manga.worktrees/TASK-016-deepseek`。

本 Task 是 **kind: experiment**：只做实验与调研，**不写生产实现**，不把实验结论升级为产品需求。

变更路径（全部在白名单内）：

| 路径 | 内容 |
|---|---|
| `experiments/TASK-016/protocol.py` + `test_protocol.py` | Provider 无关的实验协议：Tile-local→page-global 映射、单 Region 结果保留 `region_id`、阅读顺序稳定排序、fallback 只允许已配置路由 |
| `experiments/TASK-016/generate_samples.py` + `samples/` | 自制 5 个样本（日文横排／日文竖排／韩文横排／艺术字旋转／长图 Tile）+ `manifest.json`（参考文本、参考区域、Tile 原点、样本 SHA-256） |
| `experiments/TASK-016/run_experiment.py` | 实验 harness：默认 probe-only（不联网/不下载/不安装）；`--run-models` 显式才发起真实推理；缺依赖/端点时输出 `BLOCKED`，质量与性能字段留 `NOT_RUN` |
| `experiments/TASK-016/results/probe.json`、`run-models.json` | 两种模式的实际输出（各 15 条记录） |
| `doc/research/TASK-016.md` | 候选路线调研（来源归集 + 可验证性标注） |
| `verification/TASK-016/experiment-log.md` | 环境、命令、退出码、passed/skipped、skip 原因、BLOCKED/NOT_RUN 清单 |
| `doc/tasks/TASK-016.md` | 状态与运行记录（`proposed` → `in_progress` → `in_review`） |

## Acceptance Criteria 对照

- [x] **候选/版本/许可表**：`doc/research/TASK-016.md` 归集了仓库文档中**既有**的候选（`doc/01_FUNCTIONAL_ARCHITECTURE.md:215/218/220/221/222/223/255`、`doc/02_TECHNICAL_ARCHITECTURE_.md:24/26/479-482`），覆盖日漫 OCR（`manga_ocr` + 48px 混合回退）、`paddleocr_korean`、`openai_compatible_vision_ocr`，以及作为前置的检测器（DBNet / CTD / YOLO 系）。**版本与许可一律标注"待核实"**——本环境 HF 不可达且未安装依赖，给出具体版本号会是未经验证的猜测；`run_experiment.py` 内的候选表（manga-ocr 0.1.16 / PaddleOCR 3.7.0 + `korean_PP-OCRv5_mobile_rec` / Phi-3.5-vision-instruct + vLLM 0.29.0）同样未经联网核实。
- [x] **在自制横竖排、韩文、艺术字和 Tile 样本上记录缺依赖行为**：5 个自制样本就绪；harness 在 3 个候选 × 5 样本上共 15 条记录，**全部 `BLOCKED`**，原因只有 3 类（`missing dependency: manga_ocr`、`missing dependency: paddleocr and/or paddle`、`missing endpoint/model configuration`）。**识别错误 / 坐标顺序 / 耗时 / RAM / VRAM 在模型侧全部为 `NOT_RUN`**——无模型可跑，未用 Mock 冒充。协议侧（不依赖模型）已实际验证坐标映射、顺序与映射正确性。
- [x] **低质量/失败 fallback 只用已配置路线**：`protocol.fallback_status` 在空配置集下返回 `BLOCKED`，**不发明路由**；harness 对每条 BLOCKED 记录写入 `fallback = "BLOCKED: no explicitly configured fallback route"`。未复制旧仓库未提供的代码（`doc/01` 明确 `detector/registry.py` 等**不在本仓库**，故只记录路线名）。
- [ ] **Handoff、测试记录、未完成项 + 非作者独立 Review + Codex 集成**：Handoff 与本文件、`verification/TASK-016/experiment-log.md` 已交付；**待 Codex 独立 Review**。本 Task 不自行标记 `approved`／`done`。

## 验证证据（摘要）

完整记录见 [verification/TASK-016/experiment-log.md](../../verification/TASK-016/experiment-log.md)。

| 命令 | 退出码 | passed | skipped | 结果 |
|---|---|---|---|---|
| `python experiments/TASK-016/generate_samples.py --output-dir experiments/TASK-016/samples` | 0 | — | — | 5 样本 + manifest 生成 |
| `python -m unittest experiments/TASK-016/test_protocol.py -v` | 0 | **4** | **0** | `OK` |
| `python experiments/TASK-016/run_experiment.py --manifest … --output …/probe.json` | 0 | — | — | `{"BLOCKED": 15}` |
| `python experiments/TASK-016/run_experiment.py --output …/run-models.json --run-models` | 0 | — | — | `{"BLOCKED": 15}` |

环境：Windows 10.0.26200 / Python 3.12.3（`G:/CODEX/New Manga.task-envs/TASK-012-py312`）/ GPU **RTX 5070 Ti 16 GB**（`nvidia-smi` 可用）/ **依赖全缺** / PyPI 可达、**`huggingface.co` 超时不可达**。

**模型 Hash：`NOT_AVAILABLE`**（本环境无任何权重文件；记录字段 `model_sha256` 未被样本哈希顶替）。

## 未完成项与阻塞

| 项 | 状态 | 原因 |
|---|---|---|
| 三候选的**识别错误率**（真机） | **BLOCKED** | 依赖缺失 + 权重不可下载（HF 不可达） |
| **耗时 / RAM / VRAM**（模型推理） | **BLOCKED** | 无推理栈；GPU 存在但无可用依赖 |
| **样本质量**（真实漫画分布） | **NOT_RUN** | 无授权真实样本，仅自制合成样本 |
| 版本 / 许可的**权威确认** | **NOT_RUN** | 需在有网络的环境核实，**尤其模型权重条款**可能不同于代码许可 |
| 长图 Tile 的**模型侧**全局坐标 | **BLOCKED**（模型）/ **PASS**（协议） | 模型不可用；Tile→global 算术已验证 |
| 生产集成验证 | **N/A** | 实验不代表产品集成；本 Task 不改 `src/` |

## 边界与合规声明

- 未修改 `src/`、`tests/`、Schema/migration、共享 Protocol、`requirements*.txt`、`AGENTS.md`、其他 Task 或任何冻结 Task（含 TASK-015）。
- **未 push、未合并**任何分支；实验固定在 `f9edd68` 基线上。
- harness 默认不联网、不下载、不安装；`--run-models` 也只在依赖与端点已就绪时才推理，本环境两者皆无，故未产生任何真实模型输出。
- **未以 Mock 冒充模型质量**：缺少前提时一律 `BLOCKED`，不存在伪造的 `PASS`。

## 建议的 Review 重点

1. `run_experiment.py` 的"缺依赖即 BLOCKED、不产出假指标"是否满足 TASK-016 测试要求；
2. `doc/research/TASK-016.md` 的候选归集是否与 `doc/01`/`doc/02` 一致，以及"版本/许可待核实"的处理是否恰当（我认为在无网络环境下**不应给出具体版本号**）；
3. `protocol.py` 的四条协议自检是否足以覆盖 TASK-016 要求的"坐标顺序 / 单 Region 映射 / 长图 Tile 全局坐标 / 失败 fallback"；
4. 下一步建议：在有网络的环境确认候选版本与权重许可，并优先让 `rapidocr_onnxruntime`（模型随包）跑通，以取得第一组真实延迟与错误率。
