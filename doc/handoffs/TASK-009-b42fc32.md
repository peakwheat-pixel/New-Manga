---
task_id: TASK-009
author: ZCode
recipient: Codex（转 DeepSeek Harness 复审）
base_commit: 3de750ab7558f4c90841b96005dbbe58b8064e71
delivery_head: b42fc321b37562e9596ca3bb678be1f5cda22828
reviewed_head: b42fc321b37562e9596ca3bb678be1f5cda22828
implementation_merge: dea1dee73d839f5c7fe8a0f31a054c837644ea11
integration_commit: 6c732be8fda712737197343728058194070c8488
review_report_commit: d2fe13c12a13e340291032f1e18adf976c2b58fa
decision: approved
status: integrated
---

# Handoff：TASK-009 修订轮（已集成）

本 Handoff 交付 `b42fc32` 的修订轮，处理 DeepSeek Review 对 `f1dd602` 的 changes_requested
（报告 `doc/reviews/TASK-009-f1dd602.md`，commit `2da1a39`，位于 DeepSeek review 分支）。
原 `f1dd602` 为被拒 head；其 [Handoff](TASK-009-f1dd602.md) 的设计取舍与复审建议仍然有效，
被拒状态已在原文件 frontmatter 标注。`base_commit=3de750a` 不变。

提交列表（本轮新增，自 `150c789`）：

| commit | 说明 |
|---|---|
| `b42fc32` | fix(TASK-009): address review findings R-001..R-010（**delivery_head**，唯一内容变更：8 源文件 + 3 测试文件，+16 回归测试） |

修订后另有元数据提交（本 Handoff、Task 状态与 author-verification 修订节，无源码变化），
不属被审内容。

## Findings disposition

| ID | 级别 | 处置 | 实现与证据（tests/network/test_review_revision.py 逐项对应） |
|---|---|---|---|
| R-001 | P1 | **fixed** | absolute-form 经代理携带 `_proxy_auth_token`（凭据仅从 vault 解析）生成的 `Proxy-Authorization`；经代理收到的 407 映射为 `ProxyAuthenticationError`（凭据错误时不触碰目标），显式开关下进入 fallback 并在 event.detail 带 407。测试 `test_r001_*` 3 项。 |
| R-002 | P1 | **fixed** | `_establish` 全程 try/except 关闭已打开 socket（CONNECT 拒绝/TLS 失败均覆盖），`_send_once` finally 覆盖 exchange 阶段；fake-socket 测试钉住两条 seam（`test_r002_*` 3 项）。 |
| R-003 | P1 | **fixed** | post-connect 读/写超时与断连映射为 `TransportTimeoutError`/`TcpConnectionError`（detail 注明 proxy/target hop），代理路径失败按既有 gate 进入显式 fallback（`test_r003_*` 2 项）。 |
| R-004 | P1 | **fixed** | `ConnectionTester` 用 `send_with_trace` 累积各阶段 fallback：`ConnectionTestReport.fallback_trace: tuple[StageFallback,...]`（保留发生阶段）；`FallbackEvent.detail` 携带原始代理错误。测试证明 http 与 provider-auth 两阶段的 event 都进入 trace（`test_r004_*`）。 |
| R-005 | P1 | **fixed** | 新增 typed `ProviderAuthenticationError`（`PROVIDER_AUTH_FAILED`），auth 阶段对 401/403 抛出；与 `PROXY_AUTH_FAILED` 可区分，并列入不可重试分类（`test_r005_*`）。 |
| R-006 | P1 | **fixed** | `SettingsSnapshot.__post_init__` 把四个 mapping 包为 `MappingProxyType`；注入/pop 引发 TypeError，只读访问不受影响（`test_r006_*`）。 |
| R-007 | P1 | **fixed** | binding 与 task 选择两条解析路径都校验 `capability in profile.capabilities`，不匹配抛 `UnresolvedCapabilityError`（`test_r007_*` 2 项）。 |
| R-008 | P1 | **fixed** | `NetworkProfile.__post_init__` 与 `_parse_proxy_url`/`_socks_endpoint` 双层拒绝 `user:password@host`；代理密码只能经 `credential_ref` 从 vault 解析（`test_r008_*` 2 项）。 |
| R-009 | P2 | **fixed（顺手）** | `SecretValue.__eq__` 比较 UTF-8 bytes；非 ASCII 相等/不等测试（`test_r009_*`）。 |
| R-010 | P2 | **fixed（顺手）** | Windows vault teardown 收集清理失败并 `pytest.fail` 报清单；`CredentialNotFoundError`（测试已自清）不算失败。 |
| R-011 | P2 | **fixed（Codex D02 §2 偏差登记）** | D02 §2 已登记并接受 TASK-009 采用 `socket` / `http.client` / `ssl` 的局部实现偏差：满足本切片所需网络语义、零新增依赖，改回 `httpx` 将重写既有 transport seam 并增加回归风险。 |
| R-012 | P2 | **fixed（本元数据提交）** | Task 文件正文状态与测试记录已随本提交更新（正文不再残留 ready/「等待接管」表述）。 |
| F-01 | P2 | **fixed（集成勘误）** | 修正通过数口径：`tests/network` 为 **91 passed, 6 skipped**，TASK-009 固定 head 全量为 **188 passed, 6 skipped**；6 项均因 `openssl unavailable`，TLS 保持 `NOT_RUN`，未计入通过。 |

实现 Review 未引入新的 P0/P1；F-01 为证据口径勘误。未触碰 `src/domain`、SQLite、QML/UI、依赖清单、AGENTS、其他 Task、
被拒 head 之外的既集成内容。

## 验证证据（修订轮，固定 Python 3.12.3，全部退出码 0）

| 场景 | 命令 | 结果 |
|---|---|---|
| 本 Task 测试 | `PYTHONPATH=src python -m pytest tests/network -q` | **91 passed, 6 skipped**（6 项均为 `openssl unavailable`；TLS `NOT_RUN`） |
| 既有切片回归 | `PYTHONPATH=src python -m pytest tests/storage tests/library tests/editing -q` | 91 passed |
| 全量回归（固定 TASK-009 head） | `PYTHONPATH=src python -m pytest tests -q` | **188 passed, 6 skipped**（6 项均为 `openssl unavailable`；TLS `NOT_RUN`） |
| whitespace / 范围 | `git diff --check 57896ef b42fc32 --`；`git diff --name-only 150c789 b42fc32` | PASS（0；修订 11 文件全在白名单） |

证据文件：[author-verification](../../verification/TASK-009/author-verification.md)（含修订节）。

## 集成复核

复审已按以下重点完成，Codex 以 [集成验证](../../verification/TASK-009/integration-6c732be.md) 记录主线结果：
① absolute-form 认证头来源只有 vault（无明文路径）且 407→typed→fallback 链路与 CONNECT 路径一致；
② `_establish`/`_send_once` 的 cleanup seam 无泄漏路径（可重放 fake-socket 参数化）；
③ `fallback_trace` 阶段归属与 `FallbackEvent.detail` 原始错误保真；
④ snapshot mapping 只读的绕过可能（如 `MappingProxyType(dict(...))` 的浅拷贝边界——value 均为 frozen dataclass）；
⑤ capability 校验在两条路径的一致性；⑥ R-008 拒绝 userinfo 后合法代理端点的回归（应不受影响）。

## 风险与遗留

1. R-011（httpx 偏差）已由 Codex 在 D02 §2 登记并接受 stdlib 实现。
2. Profile/Binding/NetworkProfile 的 SQLite 持久化、真实远程 Provider 端到端、UI、bootstrap 装配、并发 Run 共享 transport 维持首轮 NOT_RUN/N/A 声明，未被改写。
3. absolute-form 下代理返回 502/504 现按 ProxyTunnelError 参与显式 fallback（代理故障语义）；若 Review 认为应保留原响应语义，可小改回退。
