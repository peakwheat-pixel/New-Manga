---
task_id: TASK-037
author: ZCode（窗口内 Owner，Reviewer=ZCode 子 agent）
recipient: Codex（T1 后 post-hoc 复审）
base_commit: c3dabc8（Task 元数据 base=e96b3eb 之上按用户指令 merge master；差异仅两笔纯文档时间戳修正，无代码差异）
delivery_head: 7b96e72
status: delivered（待窗口内子 agent Review；结论仅 approved_subagent/changes_requested）
---

# Handoff：TASK-037（修复已定性 flaky 与 TASK-036 R-01）

## 交付结果

**实现 head `7b96e72`**（分支 `agent/zcode/TASK-037-flaky-diagnostic-fix`，worktree `G:/CODEX/New Manga.worktrees/TASK-037-zcode`）。提交列表：

- `e1ab37b` 开工元数据（status→`in_progress`；分支 fast-forward 至 master `c3dabc8`）。
- `7b96e72` 实现与证据：`tests/reading_export/**` 3 文件 + `verification/TASK-037/**` 8 文件（10 files，+842/−24）。**生产 `src/**` 零改动**。

变更内容（对照 [TASK-037](../tasks/TASK-037.md) AC）：

| AC | 落地 | 判别力/证据 |
|---|---|---|
| ① flaky 根因 | webtoon 用例改用 `reader_stack_webtoon` 夹具（40x1000 高页，`contentHeight=1000` 真正容纳 240）；新增 `landed == 240` 落点断言；原 `contentHeight > 0` 等待、2 s 保存断言与全部预算**原文保留** | 三份探针定性机制并修正 TASK-036 定性（[clamp-race-probe.md](../../verification/TASK-037/clamp-race-probe.md)）；"等容纳 240"候选被探针 1 证伪（该场景恒假） |
| ② R-01 | `reading_export_helpers.safe_number()` 新增；两处 `(safe_property(...) or 0) > 0` 全部改用；`safe_property` 移入 helpers（契约不变） | 新 `test_helpers.py` 6 例；**修前树 ImportError → exit 2**（[discriminability-pre-fix.log](../../verification/TASK-037/discriminability-pre-fix.log)） |
| ③ 对照 | 修前 vs 修后各 ≥10 次，逐次记录 | [pre-fix-runs.log](../../verification/TASK-037/pre-fix-runs.log) / [post-fix-runs.log](../../verification/TASK-037/post-fix-runs.log)，见下表 |
| ④ 回归 | mandated 三套件 + 全仓 ≥5 次 | mandated `tests/providers tests/core tests/editing` **208 passed/0 skipped** exit 0（与修前持平）；全仓 ×6 **749 passed/0 skipped** exit 0 |
| ⑤ STATUS | flaky 第 1 条 → `fixed（TASK-037）`，历史记录全保留；同节"建议"段同步 | [STATUS](../STATUS.md) flaky 节 |
| ⑥ Review | 待窗口内子 agent Review | 本文件 + Review 报告（另提交） |

**AC ③ 对照表**（准确命令：修前/修后均为 `python -m pytest tests/reading_export -q -p no:cacheprovider -rf`；全仓为 `python -m pytest -q -p no:cacheprovider -rs -rf`）：

| 树 | 范围 | 次数 | 结果 | 命中 |
|---|---|---:|---|---|
| 修前（`c3dabc8`+开工提交，测试码未变） | 单目录 | 30 | 每次 `66 passed`、exit 0 | 0 |
| 修前 | 全仓 | 2 | 1 次 `743 passed` exit 0；**1 次 96% 处进程异常终止（无 pytest summary、EXIT=127，非测试失败）** | 0（另有 1 次环境异常，如实登记） |
| 修后（`7b96e72`） | 单目录 | 30 | 每次 `72 passed`、exit 0 | **0** |
| 修后 | 全仓 | 6 | 每次 `749 passed`、exit 0 | **0** |

修前 0 命中的补充说明：单目录频率低于全仓语境（TASK-036 会话数据：全仓 3/20 ≈15%、mandated 1/7）；机制级判别力由探针 2（100% 复现历史签名）与 v2 中间态（全仓 2/2 命中后否决）补足，全部如实记录在 clamp-race-probe.md。

**修复过程中的关键转折（供 Review 关注）**：中间态 v2（"写入后校验落点+单次重写"）单目录 30/30 通过但**全仓 2/2 命中同一失败**——证明 240 写入不可滚动内容（`contentHeight=60`）本就是不稳定状态，重写是补丁且改变时序反而放大命中。v3 改为夹具提供真正可滚动内容后全绿。该否决过程完整留档。

**口径说明**：本切片全部命令在 Git Bash 下执行（`shutil.which("openssl")` 可用），6 条 `tests/network` TLS 测试真实执行，故全仓口径为 **749 passed / 0 skipped**；Codex 基线 737 passed / 6 skipped 为 PowerShell 口径（openssl 不在 PATH）。两口径收集总数一致，差异仅这 6 条 skip↔pass，**不是环境或依赖变化**。

## 验证证据

| AC/场景 | 实际命令/步骤 | 环境与被测 commit | 结果 | 日志/产物 |
|---|---|---|---|---|
| 机制定性 | 探针 1/2/3（`probes/*.py`，临时目录页面） | PySide6 venv，分支树 | PASS（100% 复现历史签名 / 高页稳定） | [clamp-race-probe.md](../../verification/TASK-037/clamp-race-probe.md) |
| 修前对照 | `pytest tests/reading_export -q -p no:cacheprovider -rf` ×30 + 全仓 ×2 | `c3dabc8`（+开工 doc 提交） | 66 passed×30；743 passed×1 + EXIT=127×1 | [pre-fix-runs.log](../../verification/TASK-037/pre-fix-runs.log) |
| 判别力 | `pytest tests/reading_export/test_helpers.py -q -p no:cacheprovider -rf`（stash 修复后重跑） | 修前树 | **exit 2 / ImportError**（预期失败） | [discriminability-pre-fix.log](../../verification/TASK-037/discriminability-pre-fix.log) |
| 修后对照 | 单目录 ×30 + 全仓 ×6 | `7b96e72` | 72 passed×30、749 passed×6，全部 exit 0、0 命中 | [post-fix-runs.log](../../verification/TASK-037/post-fix-runs.log) |
| ④ mandated | `pytest tests/providers tests/core tests/editing -q -p no:cacheprovider -rs` | `7b96e72` | **208 passed / 0 skipped**、exit 0 | 会话记录（数字见 STATUS 收口） |
| ⑤ STATUS/Task | 文档更新 | `7b96e72`+文档提交 | flaky 第 1 条 → fixed；AC ①–⑤ 勾选 | STATUS flaky 节、Task 文件 |

**未运行项**：无新增 `BLOCKED`/`NOT_RUN`；本切片不触碰真实端点/模型层。

## 接收方式

- 复现：`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe -m pytest tests/reading_export -q -p no:cacheprovider -rf`（worktree 内执行）。
- Review 固定范围建议：`base=c3dabc8`（Task 元数据 `e96b3eb` 与之仅差两笔纯文档提交）、`reviewed_head=7b96e72`。
- 无依赖变更、无 Schema/migration 变更、无 push。

## 风险与遗留

1. **修前那次全仓 EXIT=127（96% 处、无 summary）未定性**：非本切片改动引入（修前树），复跑 6 次全仓未再现；若再现建议按 flaky 节同一口径登记。
2. `paintedHeight/paintedWidth` 经 `safe_number` 读回 0（探针侧疑似读到同名残留对象或 PySide6 转换限制）——已在 clamp-race-probe.md「未决」登记，不影响结论，但后续若做渲染层几何断言可留意。
3. 修后 0 命中的对照基于 30+6 次运行；历史全仓口径频率 ≈15%，如需更强证据可在后续任意全仓批次继续观测（用例名 `test_reader_webtoon_swaps_in_vertical_viewer`，失败应不再出现；若再现，landed 断言会给出 `contentY` 落点值的新签名）。
4. TASK-036 R-01 随本切片关闭；STATUS flaky 节第 2 条（export 终态发布顺序）维持原登记不变。
