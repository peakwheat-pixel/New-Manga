---
task_id: TASK-064
author: Codex
recipient: Codex（integration）
base_commit: 604c47a
delivery_head: e39cc27
status: in_review
---

# Handoff：TASK-064

## 交付结果

已将现有 `DiagnosticsService` 接入真实 `assemble_services` / `AppServices`：生产 snapshot provider 读取既有 data-root、managed-root、diagnostics-root 与 schema/configured 摘要；bundle sink 复用既有 `BoundedLogStore`。`assemble_engine` 不新增 QML context property。

实现提交：`e39cc27 feat(TASK-064): wire diagnostics into bootstrap`。

实现变更路径仅为：

- `src/bootstrap/app.py`
- `tests/core/test_bootstrap.py`

交付记录还包括本 Task 白名单内的 `doc/tasks/TASK-064.md`、`doc/tasks/README.md`、`doc/STATUS.md`、本 Handoff 与 `verification/TASK-064/**`。

新增 contract coverage 验证真实装配、一次脱敏导出、错误/字段报告保留、diagnostics data-root 有界存储，以及 QML 边界不暴露。未修改 TASK-055、Schema、依赖、SQLite、QML、managed storage、cleanup 或 tests/workbench。

## 验证证据

| AC/场景 | 实际命令/步骤 | 环境与被测 commit | 结果 | 日志/产物 |
|---|---|---|---|---|
| AC ①～④ 定向 | `pytest tests/diagnostics tests/core -q -p no:cacheprovider -rs` | PowerShell 7；`TASK-012-py312`；`e39cc27` | PASS：74 passed / 0 skipped；EXIT=0 | [diagnostics-bootstrap-suite.log](../../verification/TASK-064/diagnostics-bootstrap-suite.log) |
| AC ⑤ 全仓第 1 次 | `pytest tests -q -p no:cacheprovider -rs` | 同上；`e39cc27` | PASS：950 collected；944 passed / 6 skipped；EXIT=0 | [full-suite-1.log](../../verification/TASK-064/full-suite-1.log) |
| AC ⑤ 全仓第 2 次 | 同上 | 同上；`e39cc27` | PASS：950 collected；944 passed / 6 skipped；EXIT=0 | [full-suite-2.log](../../verification/TASK-064/full-suite-2.log) |
| AC ⑤ 全仓第 3 次 | 同上 | 同上；`e39cc27` | PASS：950 collected；944 passed / 6 skipped；EXIT=0 | [full-suite-3.log](../../verification/TASK-064/full-suite-3.log) |
| AC ⑤ 全仓第 4 次 | 同上 | 同上；`e39cc27` | PASS：950 collected；944 passed / 6 skipped；EXIT=0 | [full-suite-4.log](../../verification/TASK-064/full-suite-4.log) |
| AC ⑤ 全仓第 5 次 | 同上 | 同上；`e39cc27` | PASS：950 collected；944 passed / 6 skipped；EXIT=0 | [full-suite-5.log](../../verification/TASK-064/full-suite-5.log) |

每次的 6 个 skip 都是同一组本机 TLS 前置缺失：`openssl unavailable`（逐条原因已写入日志）。新增测试没有 skip/xfail；pytest 仅报告既有 `mobi`/`imghdr` 弃用 warning。

## 接收方式

在 `G:/CODEX/New Manga` 检出 `agent/codex/TASK-064-diagnostics-production-wiring`，固定 base 为 `604c47a`。Review 需检查真实 bootstrap 注入、导出报告脱敏字段/错误码/字段清单、有界 sink 与不暴露 QML 的边界；Review approved 后由 Codex 合并并在合并后的全仓验证中登记 `integration_commit`。

## 风险与遗留

- 本机没有 OpenSSL，因此 TLS 相关 6 项按既有口径 skip；该环境事实不由本 Task 放宽。
- QML 测试仍可能输出既有 null context-property stderr 噪声；本 Task 未改 QML，也未将 diagnostics 暴露给 QML。
- `integration_commit` 尚待独立非作者 Review 与 Codex 合并后填写。
