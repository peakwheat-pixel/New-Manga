---
task_id: T3.2.1-REPAIR-13
author: ZCode
recipient: Codex / DeepSeek Harness（独立非作者 Review）
base_commit: 8738c41d7215920935e6fa067d3740e69280cdc6
delivery_head: c0aba799168d10ab20c63efa7d8c4ff0a44ed6cd
status: in_review
---

# Handoff：T3.2.1-REPAIR-13（最终 Delivery c0aba79）

本 Handoff 取代 [`T3.2.1-REPAIR-13-2f18e78.md`](T3.2.1-REPAIR-13-2f18e78.md) 的
接收口径（该文件保留为第一段 delivery 的历史记录）。两段用户授权：

1. 初版授权（STATUS 原 Task 范围）：Bookshelf 导入接线 + 四页面按钮可读性。
2. 追加授权（用户 2026-09-25）：`ExportWindow.qml`、`ChapterPicker.qml` 的按钮文字
   对比修复（原 R-13-1 请求）；同日裁决 **取消 Sandbox 验证、以本机验证代替**。

## 提交链（分支 `agent/zcode/T3.2.1-repair-13`）

```text
8738c41  base（REPAIR-12 handoff）
2f18e78  fix(ui): 初版 delivery（4 个 QML + 回归测试 + 探针/截图/判别证据）
df95ad1  docs(handoff): 第一段 Handoff 与证据
33f2ff7  docs(review): reviewer_unavailable 记录（DSH 401）
c0aba79  fix(ui): 最终 delivery——ExportWindow/ChapterPicker 文字对比 + FileDialog
         实际打开验证 + Workbench 两页验证
（本 Handoff 提交为 evidence commit，跟在 c0aba79 之后）
```

## 变更路径（base → 最终 delivery，`git diff --name-only`）

- `src/ui/qml/Main.qml`、`src/ui/qml/bookshelf/{BookshelfView,BookshelfToolbar,BookDetailPanel}.qml`
  ——初版授权范围。
- `src/ui/qml/windows/ExportWindow.qml`、`src/ui/qml/common/ChapterPicker.qml`
  ——追加授权（buttonText/text 四处 → `Tokens.inkInv`）。
- `tests/ui_shell/test_repair13_import_entry.py`（新增 5 例）。
- `verification/T3.2.1/repair-13/**`（证据）。
- 未改动：Python 业务逻辑、export service、`packaging/**`、依赖、Schema、`src/bootstrap/**`。

> 范围声明（按用户要求）：`git diff --check` 仅 whitespace 检查（exit 0）；它**不是**
> 完整审查结论。逐文件范围核对以上述清单为准，最终以独立 Review 为准。

## 修复摘要

- **AC1**：`BookDetailPanel` 以 readonly alias 暴露 `currentChapterId`；导入回调不再
  跨组件访问内部 id（Build 3 stderr 31 次 TypeError 的根因）；未选章节时导入禁用 +
  区分文案提示（"先新建作品…" / "已选作品还未选择章节…"）。
- **AC3**：原生 Windows Quick Controls 样式忽略 `palette.button`/自定义背景但遵守
  `palette.buttonText`（ComboBox 显示文字跟随 `palette.text`）——六处文字角色绑定
  `inkInv`（浅色系统表面上的 F 反色文字 token），零 token 改值。包内实测：主界面按钮
  14–17:1、ChapterPicker ComboBox 6.35:1、ExportWindow 17.7–18.7:1。
- **AC2 补验证**：探针实证点击导入**真实打开原生文件选择器**（`importDialog.visible`）
  后再驱动 viewmodel；本机包验证进一步用真实文件对话框双路径多选导入两张 fixture
  （见下）。

## 验证证据（全部在 `verification/T3.2.1/repair-13/`）

| 项 | 命令/方式 | 结果 | 证据 |
|---|---|---|---|
| 回归守卫 | `pytest tests/ui_shell/test_repair13_import_entry.py` | 5 passed | 修前 4 failed/修后 5 passed 判别：`discriminating-pre-repair.log` |
| 全套回归 | `pytest tests/ui_shell tests/reading_export/test_qml_contract.py -q -p no:cacheprovider -rs` | **160 passed, EXIT=0** | `pytest-ui-shell-qml-contract.log` |
| 源码探针 | `python verification/T3.2.1/repair-13/probe_ui_states.py` | 全 PASS, EXIT=0（含 FileDialog 打开、Workbench 两页可选 viewerPageId 匹配、真实 ZIP 2 页） | `probe-ui-states.stdout/stderr.log`、`screenshots/01–13` |
| 固定包 | `packaging/build.ps1`，head=c0aba79 | EXIT=0；EXE SHA-256 `18eac441…`；manifest 5437 | `build6-final-delivery.raw.log`、`build6-final-delivery-exe.sha256`、`build6-final-delivery-artifact-manifest.sha256` |
| 包冒烟 | `NewManga.exe --smoke-test` | EXIT=0 | `build6` 同期会话记录 |
| **本机验证** | 真实 GUI 驱动固定包（--data-root 隔离）：建书→建章→选章→原生文件对话框双 fixture 导入→Managed Copy 逐字节一致→DB 2 页→Workbench 两页显示+选择→Reader 阅读→退出无残留 | **PASS（AC4 导出窗口除外，见 F-13-2）** | [`local-verification.md`](../../verification/T3.2.1/repair-13/local-verification.md)、`G:\CODEX\repair13-local-verify\` |
| Sandbox | 三次启动尝试失败；**用户裁决取消 Sandbox 验证改本机验证** | 取代（历史记录保留） | [`sandbox-attempts.md`](../../verification/T3.2.1/repair-13/sandbox-attempts.md) |

## AC 对照（最终口径）

- **R13-AC1** PASS（源码探针 + 包内真实 GUI）。
- **R13-AC2** PASS（探针 + 本机包：原生对话框真实打开、双 fixture 导入、Managed Copy
  hash 一致、DB 2 页、Workbench 两页显示并可选）。
- **R13-AC3** PASS（源码 + 包内；含追加授权的两个对话框文件；色值/截图入库）。
- **R13-AC4** **BLOCKED（F-13-2）**：Reader 导出按钮启用、阅读正常，但点击"导出…"后
  ExportWindow 因先存缺陷 `Component.onCompleted: open()`（Window 无 `open()`，
  `ReaderView.qml:62 ReferenceError`）静默不显示。最小修复（`open()`→`show()`，一行，
  在已授权文件内）超出用户本轮"只可修改按钮文字对比问题"的限定，**未改**，交 Codex
  裁决。源码探针中导出文件本身生成正常（controller 通道 2 页 ZIP）。
- **R13-AC5** 本机验证替代完成（用户裁决；干净 Windows 覆盖差异已在
  local-verification.md 声明）；固定包构建/冒烟/manifest/EXE SHA 齐备。
- **R13-AC6** 变更路径清单如上；`git diff --check` exit 0（whitespace 检查，不作为
  完整检查结论）；最终判定待独立 Review。

## 新发现（交 Codex 裁决/排查）

- **F-13-2（P1）**：如上，阻断 AC4 真实 UI 路径，先于本切片存在。
- **F-13-3（P2）**：重启后书架列表为空（DB 有书、清空持久化搜索后仍空），
  `bookListModel` 启动恢复问题；本切片未触碰该逻辑。
- **信息**：导入后章节行页数文本仍"0 页"（DB 已 2 页，model 未刷新页数列）。

## Codex 同步清单（用户裁决落地）

1. `doc/tasks/T3.2.1-REPAIR-13.md` / `doc/STATUS.md`：R13-AC5 由"Sandbox 真实 GUI"
   修订为"本机验证（用户 2026-09-25 裁决）"；登记 Owner 改派（Qoder→ZCode）与
   范围追加授权（两对话框文件）。
2. F-13-2 的新范围裁决（一行 QML 修复建议在 local-verification.md）。
3. DSH 凭证修复后重派 Review：**base `8738c41` → 最终 delivery `c0aba79`**（完整 diff），
   可复用派发 prompt 见 [`review-dispatch-record.md`](../../verification/T3.2.1/repair-13/review-dispatch-record.md)。

## 风险与回退

- 本切片未修 F-13-2，父 Gate AC3/AC4 相关项维持 OPEN；批准前禁止合并 `master`。
- 回退：revert `c0aba79`（保留 2f18e78）或一并 revert 两者；无 Schema/依赖/数据迁移影响。

## 下一任务建议

- **Task**：F-13-2 微切片——`ExportWindow.qml:62` `open()`→`show()`（或等价 visible
  绑定）+ 包内导出窗口真实打开复测；随后按 Codex 排期做 base `8738c41`→最终 head 的
  DSH 独立 Review。
- **Owner / Reviewer**：ZCode 或 Qoder / DeepSeek Harness；Codex 冻结 base 与范围。
- **Recovery point**：`G:/CODEX/New Manga.worktrees/T3.2.1-zcode-repair-13`，
  branch `agent/zcode/T3.2.1-repair-13` @ `c0aba79`+。
