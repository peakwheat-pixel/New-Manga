---
task_id: TASK-003
author: DeepSeek Harness
recipient: Codex
base_commit: 9472df5c44a1bad24cda639201b0824a9ea5eecc
delivery_head: f620c693b9b6d3df45c0b7c6965ab3db32943702
status: awaiting_review
supersedes: doc/handoffs/TASK-003-6caefe1.md
---

# Handoff：TASK-003 第二次复审修订（R-011～R-014）

交付日期：2026-09-14（Asia/Shanghai）。本轮只处理 Codex 第二次 Review（commit `8f24be4`，`doc/reviews/TASK-003-6caefe1.md`，decision=`changes_requested`，P1×1 / P2×3）的 R-011～R-014；未实现功能、未改产品要求、未合并 master、未 push。Owner 为 DeepSeek Harness，Reviewer 为 Codex——**本 Handoff 不是自我批准**。

## 固定审查对象

- `base_commit = 9472df5c44a1bad24cda639201b0824a9ea5eecc`
- `reviewed_head = f620c693b9b6d3df45c0b7c6965ab3db32943702`（**取代** `6caefe1`）
- 被拒 head：`6caefe1`；第二次 Review 报告：commit `8f24be4` 的 `doc/reviews/TASK-003-6caefe1.md`（位于 Reviewer 分支，不在本分支）
- 作者分支：`agent/deepseek/TASK-003-verification-spec`；工作区 `G:/CODEX/New Manga.worktrees/TASK-003-deepseek`

## 提交链

~~~text
9472df5 docs: authorize TASK-003 verification design          （基线）
588383f docs(TASK-003): add acceptance and fixture spec, close F-08   （首次交付，被 R-001～R-010 拒绝）
c55ec27 docs(TASK-003): hand off 588383f for review
6caefe1 docs(TASK-003): address review findings R-001..R-010  （第二次交付，被 R-011～R-014 拒绝）
7aacac1 / 05eb18f                                             （第二次元数据）
f620c69 docs(TASK-003): address re-review findings R-011..R-014（本次内容交付 = reviewed_head）
~~~

本次内容改动 4 个文件（`+44/−21`）。D02/D04/D11 的 F-08 状态边与前两轮一致，**本轮未再触碰**。

## Finding 处置

| Finding | 级别 | 处置 | 证据 |
|---|---|---|---|
| R-011 | P1 | **fixed** | 彻底删除 `planned` 状态：Manifest 的字段定义、状态枚举、available 三条件与汇总表都只保留 `missing / available / rejected`，并写明"本清单**不设**'已规划但未取得'的第三态——方案写在 `生成方式` 列，`状态` 只描述素材本体是否存在"；未取得项（20 项）仍全部为 `missing`。脚本改为只接受三态，并显式拒绝任何 `^planned` 定义行 |
| R-012 | P2 | **fixed** | `available` 行现在同时校验三项：许可非 `待定`、Hash 为 64 位小写 SHA256、`生成方式` 非空非占位（拒绝 `TBD/TODO/待补/N/A/-/—/无`）且与来源类型口径一致（`generated` 必须含生成脚本，`acquired` 必须含取得途径/授权/来源）；Manifest §2 同步写明三项强制条件 |
| R-013 | P2 | **fixed** | 脚本硬编码**完整 24-ID 预期集合**（10 主题 + 7 全局规范 + 7 扩展），并与规范、D13 两张权威表分别做集合比较；此前只固定总数与分组的检查已移除 |
| R-014 | P2 | **fixed** | 规范与 D13 不再复制 D08 的条目数量：`ACG-RELEASECHECK` 改为"按 D08 §73 的最终 Release Checklist 逐项核对"，`ACG-AUTOTEST`/`ACG-UITEST` 改为"按 D08 §68/§69 的最低覆盖清单"，`ACG-DATASAFE` 的"八项"同样改为"按 D08 §72 逐项执行"；脚本新增护栏，拒绝任何 `(Release Checklist\|最低覆盖)\s*\d+\s*(项\|类\|个)` 形式的派生数量声明 |

## 验证证据

环境：Windows `10.0.26200`、PowerShell `7.6.6`、Git `2.52.0.windows.1`。

| 检查 | 命令 | 环境 / commit | 结果 |
|---|---|---|---|
| 交付验证脚本 | `pwsh -NoProfile -File ./verification/TASK-003/verify.ps1` | `f620c69`（干净工作区） | **PASS，退出码 0**，12 条输出 |
| 空白错误 | `git diff --check 9472df5 f620c69 --` | 同上 | PASS，退出码 0 |
| 变更统计 | `git diff --stat 9472df5 f620c69` | 同上 | 10 路径，`+978/−33`（含前两轮交付与元数据） |
| 工作区洁净 | `git status --short --branch` | 同上 | PASS，仅分支行 |
| F-08 增量 | `git diff 9472df5 f620c69 -- D02 D04 D11` | 同上 | 3 新增、**0 删除**，仍为 `Blocked → Cancelled` |

脚本实际输出（12 条）：

~~~text
PASS: changed paths authorized; TASK-001/002 unchanged; other 24 tasks frozen
PASS: D08 unchanged; all 185 AC IDs, priorities and titles intact and unique
PASS: 24 unique ACG identifiers (10 topic + 7 global + 7 extension), rows unique in both tables
PASS: every ACG row carries exactly one legal result enum value
PASS: method spec keeps no result copy; result truth stays in D13 / commit-bound reports
PASS: fixture manifest fields/states, 20 rows, hash discipline and licence rule (0 available)
PASS: AC-CAP-001..004 each route to an existing dataset (DS-E/DS-B2/DS-C/DS-D)
PASS: ACG-SMOKE documents the full D08 §53 flow including restart persistence
PASS: F-08 is exactly one added edge per file in D02/D04/D11, with no deletions
PASS: result enum, full performance protocol, UNAPPROVED_THRESHOLD and scope discipline intact
PASS: 532 local links and all code fences valid
PASS: git diff --check; owner-authored guards only, independent Review still required
~~~

### 护栏负向测试（针对 R-011～R-014）

以合成样本验证检测逻辑（未修改任何被审文件）：

| 场景 | 输入 | 期望 | 实测 |
|---|---|---|---|
| planned 状态定义 | `planned    已定义方案` | 拒绝 | 命中 `^planned\s` ✓（真实 Manifest 已 0 命中） |
| available + 待定许可 | `acquired / 待定` + 真 SHA256 + 合规生成方式 | 拒绝 | `REJECT: licence` ✓ |
| available + 生成方式占位 | `generated / 自制` + `N/A` | 拒绝 | `REJECT: method empty/placeholder` ✓ |
| available + generated 无脚本 | `generated / 自制` + `照抄 D07` | 拒绝 | `REJECT: generated without script` ✓ |
| available + acquired 无途径 | `acquired / CC0` + `available` | 拒绝 | `REJECT: acquired without path` ✓ |
| available 合规 | 许可明确 + 真 SHA256 + 脚本/取得途径 | 接受 | `ACCEPT`（两种来源类型各一） ✓ |
| 24-ID 集合被替换 | 将 `ACG-EXT-NFR` 换成 `ACG-EXT-XXXX` | 拒绝 | 集合比较命中 ✓ |
| 派生数量声明 | `按 D08 §73 最终 Release Checklist 18 项` | 拒绝 | 命中数量模式 ✓（真实规范与 D13 均 0 命中） |

**该脚本由 Owner 编写，是回归护栏，不构成独立批准。**

## 未完成项

| 项 | 状态 | 原因 |
|---|---|---|
| 产品 / 应用端到端测试 | `N/A` | 仓库无应用源码与可执行产物 |
| 性能与容量实测 | `NOT_RUN` | 无实现、无 ENV-B/ENV-D 基准数据 |
| 模型质量评估 | `BLOCKED` | 素材 20 项全部 `missing`、阈值 `UNAPPROVED_THRESHOLD` 未获批 |
| 素材生成与 Hash 登记 | `NOT_RUN` | 属后续获授权 Task；本轮只维护状态与防线 |
| Mermaid 渲染校验 | `NOT_RUN` | 仅围栏结构检查 |
| D08 §68～§76 的实际覆盖验证 | `NOT_RUN` | 标识与路由已建立；实际测试属 TASK-022/026/027 |
| 独立复审与集成 | 待 Codex | `integration_commit` 仍为空，Task 不得 done |

## 风险与遗留

- `ACG-RELEASECHECK`/`ACG-AUTOTEST`/`ACG-UITEST`/`ACG-DATASAFE` 现在只回链 D08 章节，不再复制条目数量；若后续需要在验收记录中展开逐项清单，应以 D08 为唯一来源。
- `available` 的生成方式口径按来源类型区分（generated 需脚本名、acquired 需取得途径）；当前 `available` 数量为 0，该分支的行为只经合成样本验证，尚未有真实素材通过该路径。
- 素材仍全部未取得；在素材与许可确认前，依赖真实素材的质量结论只能记 `BLOCKED`/`NOT_RUN`。

## 接收与复现

唯一 Git common directory：`G:/CODEX/New Manga/.git`；Owner worktree `G:/CODEX/New Manga.worktrees/TASK-003-deepseek`。

~~~powershell
git diff --stat 9472df5 f620c69
git diff --check 9472df5 f620c69 --
pwsh -NoProfile -File ./verification/TASK-003/verify.ps1
~~~

复审需覆盖 R-011～R-014 的 disposition、Fixture 状态机是否已无 `planned` 且脚本拒绝绕过、`available` 三项条件是否都可被脚本拒绝、24-ID 集合是否被完整固定、派生文档是否不再复制 D08 条目数量，以及 F-08 是否仍为"仅一条状态边且无删除"。P0/P1 未解决不得批准；P2 必须记录 disposition。
