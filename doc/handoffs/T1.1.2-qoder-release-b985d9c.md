---
task_id: T1.1.2
author: Codex
recipient: Qoder
base_commit: b985d9cf92a894f035550fe409179b3cb5adcbd8
delivery_head: null
status: released
---

# Handoff：T1.1.2 Qoder Release Packet

## 任务身份

- Current Roadmap Execution Unit：`T1.1.2 — Region Canvas & Creator`
- Historical mapping：`TASK-013 = VERIFIED_COMPLETE`；本任务是 follow-up slice，不得改写 TASK-013 原始状态。
- Coordinator / Integrator：Codex
- Implementation Owner：Qoder
- Non-author Reviewer：待 Codex 指定
- Branch：`task/t1.1.2-region-canvas-qoder`
- Worktree：`G:\CODEX\New Manga.worktrees\T1.1.2-qoder`
- Fixed base：`b985d9cf92a894f035550fe409179b3cb5adcbd8`
- Delivery head：尚未产生；Qoder 完成后必须填写实际 commit hash。

旧 `G:\CODEX\New Manga.worktrees\TASK-013-qoder` 已检查为 clean，但基于旧的 Stage A 前历史并已有 `52de319` 及相关实现文件；它被保留为既有工作树，不作为本次 release base，也不得 reset、删除或覆盖。

## 交付范围

在现有 Workbench 中增加人工 Region 创建：

- Rectangle：pointer down → drag → live preview → release → validate → create。
- Polygon：begin → append points → live preview → confirm → validate → create。
- Rectangle/polygon 均支持 cancel。
- 非法几何不得写入数据库。
- 新 Region 创建后立即进入现有 selection/Inspector 流程。
- 复用现有 `RegionEditingService.create_region`、Workbench ViewModel seam、region model、revision persistence 和坐标转换架构。
- 创建后必须生成正确的 `regions` 与 `region_revisions` 记录。
- 不得写入用户原始漫画源文件。

至少覆盖：zero-area、过小矩形、polygon 点数不足、重复点、越界点、degenerate geometry、page extent 缺失、cancel 与 reload。

坐标必须明确经过：

```text
pointer coordinates
→ QML/canvas transform
→ displayed image transform
→ zoom/pan correction
→ canonical page coordinates
→ RegionEditingService
→ persistence
```

验证至少覆盖 100% zoom、50% zoom、>100% zoom、fit-to-window、pan、不同页面尺寸和 reload。

## 允许修改

- `src/ui/qml/workbench/**`
- `src/ui/viewmodels/workbench/**`
- 必要的 `src/ui/models/tasks/**`
- `tests/workbench/**`
- `verification/TASK-013/**`
- `doc/tasks/TASK-013.md`（仅补充 T1.1.2 follow-up 证据，不改历史状态）
- `doc/handoffs/TASK-013-*`（仅在必要时补历史交接链接）
- `doc/handoffs/T1.1.2-qoder-release-b985d9c.md`（仅回填本次交付 head、证据和遗留）

## 禁止修改

- `src/bootstrap/app.py`；最终 `region_creator` wiring 由 Codex 串行完成。
- SQLite schema 或 migrations。
- `DetectionProvider`、OCR、Translation、Inpainting。
- Settings、Credentials、Proxy、Theme、Graphite Atelier F。
- top-level shell、Reader redesign、Windows packaging、release pipeline。
- shared contract、dependency 清单、Roadmap、`REBASELINE_PLAN.md`、全局 STATUS 规划字段。
- 用户源文件、其他 Agent dirty files、其他 Worker worktree。
- 新增一级 Task、Promote backlog、启动后续 Roadmap unit。

发现检测、设置、主题、打包、SQLite migration、Reader redesign 等内容时，只记录为 Deferred Finding；不得扩大范围。

## TDD 与验收

按 focused failing test → 最小实现 → focused green 的顺序推进。至少增加或更新：

- rectangle begin/preview/commit/cancel/invalid；
- polygon begin/add/preview/commit/cancel/invalid；
- coordinate transform、zoom/pan、page size、reload；
- persistence、revision、Inspector synchronization；
- source-file protection。

Qoder 交付前必须提供：

- actual commit list 与 final commit hash；
- changed files；
- implemented behavior；
- tests added；
- focused/full test results；
- known limitations；
- Deferred Findings / Candidate Backlog findings；
- 本 handoff 的更新版本。

Qoder 不得 merge master、修改 bootstrap、启动下一任务或宣布 `T1.1.2 VERIFIED_COMPLETE`。

## 验证与接收

Qoder 完成后先停止，交给非作者 Reviewer 检查：

- rectangle/polygon interaction；
- coordinate transforms、zoom/pan；
- persistence 与 revision；
- cancel 与 invalid geometry；
- source protection；
- Inspector sync；
- architecture reuse；
- regression 与 scope compliance。

Codex review 通过后负责：

1. 检查 commits、scope 和 worktree cleanliness；
2. 串行完成 `src/bootstrap/app.py` 的 `region_creator` wiring；
3. 运行 focused tests、full pytest、compileall、bootstrap smoke、`git diff --check`；
4. 验证 source-file protection；
5. 更新 STATUS、verification 和 handoff；
6. 集成到 `master`。

只有 Codex 完成 fresh verification 并集成后，才可把 T1.1.2 标记为 `VERIFIED_COMPLETE`。

## 恢复点

```text
Repository: G:\CODEX\New Manga
Release branch: master
Release HEAD: b985d9cf92a894f035550fe409179b3cb5adcbd8
Worker branch: task/t1.1.2-region-canvas-qoder
Worker worktree: G:\CODEX\New Manga.worktrees\T1.1.2-qoder
Roadmap ID: T1.1.2
Owner: Qoder
Next action: read STATUS/REBASELINE_PLAN and implement only this packet
```
