---
task_id: T1.3.1 / TASK-057
author: Codex（Integrator）
recipient: 独立非作者 Reviewer；Codex master integration
base_commit: 676319bb1b9e9682b13402c322fded43bbaa90c8
delivery_head: 0b957d17bca2f3d8786680ce14c5bc9b699c13fe
reviewed_head: c79cd360ce29bb45fb319e347c5325c95aee5567
integration_commit: 38d6eaaefe9d344d8534996f469f2ba868922845
status: done
---

# Handoff：T1.3.1 / TASK-057 Rebaseline Integration

## 交付结果

- 固定交付 `29546f7` 与 Qoder review 分支 `257ee5b` 已分别通过 merge `3087975`、`1433e02` 纳入隔离分支；相对 activation base 无重叠路径、无冲突，未重做 rebaseline。
- Codex 追认 `src/ui/viewmodels/workbench/**` 与 `tests/workbench/**` 白名单，仅用于 restore admission / Worker 生命周期门；Schema、Provider、QML、依赖均未改。
- R-008：公共入口 `beginRestore()` 拒绝重入；嵌套 `restoreGate()` 不再由内层退出提前打开外层门。
- R-009：`continueRun`、`restartRun`、`retryFailedPages` 在 control/plan 写入之前拒绝；测试断言原 run/status/失败 target 未被改写。`abandonRun` 不是 start path，保持原行为。
- 实现提交 `3fa7958`；固定验证 HEAD `f92530e`。用户源文件保护、typed backup failures、连接所有权和 Worker 排空语义均由既有测试与本轮相关集成套件覆盖。

## 验证证据

| AC/场景 | 实际命令/步骤 | 环境与被测 commit | 结果 | 日志/产物 |
|---|---|---|---|---|
| backup + restore gate | `pytest tests/storage/test_backup_restore.py tests/workbench/test_restore_gate.py -q -p no:cacheprovider -rs` | PowerShell；TASK-012-py312；`PYTHONPATH=src`；`PYTHONDONTWRITEBYTECODE=1`；`QT_QPA_PLATFORM` unset；`f92530e` | **18 passed / EXIT=0** | `verification/TASK-057/rebaseline-integration/targeted-f92530e.log` |
| affected integration | 上述两文件 + connection ownership/eviction + run-thread e2e | 同上 | **28 passed / EXIT=0** | `verification/TASK-057/rebaseline-integration/integration-f92530e.log` |
| reviewer adversarial probe | `t057_chokepoint_probe.py <worktree>` | 同上 | **EXIT=0**；H2 nested gate refused，outer latch remained closed；H3 paused run remained paused | `verification/TASK-057/rebaseline-integration/chokepoint-probe-f92530e.log` |
| full regression ×5 | `pytest tests -q -p no:cacheprovider -rs` | 同一 HEAD / shell / venv / env | 每次 **970 collected = 964 passed + 6 skipped / EXIT=0**；6 条均为既有 `openssl unavailable`；无 xfail | `verification/TASK-057/rebaseline-integration/full-suite-f92530e-run{1..5}.log` |

相对 rebaseline 951 collected，净增 19 个有效测试：固定 TASK-057 交付带入 18 个，本轮 R-008 增加 1 个；未删除必要测试、未放宽断言、未新增 skip/xfail。

## Changes Requested 返修（Review head `8318b64` → delivery `940d373`）

首轮独立 Review：[T1.3.1-TASK-057-8318b64.md](../reviews/T1.3.1-TASK-057-8318b64.md)，结论 `changes_requested`。本轮未拆 Task，逐条处置如下：

- R-001：`create_backup()` 在写 `completed` 前由 service 自己对最终 DB 执行 integrity/schema read-back。
- R-002：SQLite backup 先落同目录临时 DB，再发布最终文件；read-back、sidecar、ledger 任一步失败均清理 DB/manifest，并返回 `BACKUP_CREATE_FAILED`。判别用例覆盖 read-back、sidecar、ledger 三类失败。
- R-003：restore 入口仅接受生成器同形的 32 位小写 hex ID，并验证 resolved target 位于 backup root；相对 traversal 与绝对路径都返回既有 typed `BACKUP_UNKNOWN`。
- R-004：restore 在任何 live write 前交叉校验 DB/sidecar/ledger schema，并拒绝高于应用 `default_migrations()` 上限的备份（`SCHEMA_TOO_NEW`）。合法无 schema 表的 v0 pre-migration snapshot 仍通过 integrity read-back并记为 schema 0。
- R-005：storage 行为测试现在真实建立 original + translated artifacts，分别漂移 current 与 pinned revision；restore 后逐值断言回到 backup point，并核对两份 Managed Copy 字节。
- R-006：restart/retry 在 latch 内明确断言 control/retry/plan 零调用；新增行为测试固定 `abandonRun` 仍可执行。
- R-007：不改写历史 raw evidence。当前产品/测试返修与本轮新 evidence 均 `diff --check` 干净；全范围历史日志尾随空格作为原始证据保留并显式豁免。

| 返修场景 | 实际命令/步骤 | 固定 HEAD | 结果 | 日志/产物 |
|---|---|---|---|---|
| backup + restore gate | `pytest tests/storage/test_backup_restore.py tests/workbench/test_restore_gate.py -q -p no:cacheprovider -rs` | `940d373` | **26 passed / EXIT=0** | `verification/TASK-057/rebaseline-integration/targeted-940d373.log` |
| affected integration | targeted + connection ownership/eviction + run-thread e2e + schema migration | `940d373` | **44 passed / EXIT=0** | `verification/TASK-057/rebaseline-integration/integration-940d373.log` |
| reviewer adversarial probe | `t057_chokepoint_probe.py <worktree>` | `940d373` | **EXIT=0**；nested/half-write/Worker birth 均安全 | `verification/TASK-057/rebaseline-integration/chokepoint-probe-940d373.log` |
| full regression ×5 | `pytest tests -q -p no:cacheprovider -rs` | 同一 HEAD / shell / venv / env | 每次 **978 collected = 972 passed + 6 skipped / EXIT=0**；6 条均为既有 `openssl unavailable`；无 xfail | `verification/TASK-057/rebaseline-integration/full-suite-940d373-run{1..5}.log` |

相对 rebaseline 951 collected，净增 27 个有效测试：固定 TASK-057 交付与首轮 R-008 共 +19，本轮安全/AC 证据 +8；未删测试或弱化断言。

## Round 2 Changes Requested 返修（Review head `d7e9aa9` → delivery `b2dd525`）

Round 2 Review：[T1.3.1-TASK-057-d7e9aa9.md](../reviews/T1.3.1-TASK-057-d7e9aa9.md)，结论 `changes_requested`。其余旧 finding 均已关闭或豁免；本轮只处理两个 P1：

- R-002：DB read-back/hash/sidecar 全部在临时路径完成；发布顺序为 manifest 后 DB，正式 DB 只暴露已验证内容。统一 `finally` 清理未完成路径，`SystemExit` 等价中断测试证明不遗留 canonical DB/manifest/ledger 半成品。
- R-010：恢复旧 schema 后复用 `MigrationRunner(default_migrations())` 升级到当前版本，再执行 live integrity/schema/Managed Copy 检查。主恢复或 post-check 失败时，用已验证的 pre-restore DB 走同一 `_restore_database()` 路径自动回滚；主恢复与回滚均失败时返回带 `pre_restore` 凭据的 `RESTORE_ROLLBACK_FAILED`，不裸漏 SQLite 异常。

| Round 3 候选场景 | 实际命令/步骤 | 固定 HEAD | 结果 | 日志/产物 |
|---|---|---|---|---|
| backup + restore gate | `pytest tests/storage/test_backup_restore.py tests/workbench/test_restore_gate.py -q -p no:cacheprovider -rs` | `b2dd525` | **29 passed / EXIT=0** | `verification/TASK-057/rebaseline-integration/targeted-b2dd525.log` |
| affected integration | targeted + connection ownership/eviction + run-thread e2e + schema migration | `b2dd525` | **47 passed / EXIT=0** | `verification/TASK-057/rebaseline-integration/integration-b2dd525.log` |
| reviewer adversarial probe | `t057_chokepoint_probe.py <worktree>` | `b2dd525` | **EXIT=0** | `verification/TASK-057/rebaseline-integration/chokepoint-probe-b2dd525.log` |
| full regression ×5 | `pytest tests -q -p no:cacheprovider -rs` | 同一 HEAD / shell / venv / env | 每次 **981 collected = 975 passed + 6 skipped / EXIT=0**；6 条均为既有 `openssl unavailable`；无 xfail | `verification/TASK-057/rebaseline-integration/full-suite-b2dd525-run{1..5}.log` |

相对 rebaseline 951 collected，净增 30 个有效测试；Round 3 比 Round 2 新增 3 个行为测试（中断清理、v0 restore+migration、restore failure rollback）。

## Round 3 Changes Requested 返修（Review head `85832bb` → delivery `0b957d1`）

Round 3 Review：[T1.3.1-TASK-057-85832bb.md](../reviews/T1.3.1-TASK-057-85832bb.md)，结论 `changes_requested`；R-002/R-010 已关闭，唯一 P1 R-011 为 restore 前置 hash 读取 `OSError` 裸漏。delivery `0b957d1` 只在该信任边界把异常映射为 `BACKUP_FILE_UNREADABLE`，并新增行为测试证明 live revision 与 backup ledger 在失败时均未变化。

| Round 4 候选场景 | 实际命令/步骤 | 固定 HEAD | 结果 | 日志/产物 |
|---|---|---|---|---|
| backup + restore gate | `pytest tests/storage/test_backup_restore.py tests/workbench/test_restore_gate.py -q -p no:cacheprovider -rs` | `0b957d1` | **30 passed / EXIT=0** | `verification/TASK-057/rebaseline-integration/targeted-0b957d1.log` |
| affected integration | targeted + connection ownership/eviction + run-thread e2e + schema migration | `0b957d1` | **48 passed / EXIT=0** | `verification/TASK-057/rebaseline-integration/integration-0b957d1.log` |
| reviewer adversarial probe | `t057_chokepoint_probe.py <worktree>` | `0b957d1` | **EXIT=0** | `verification/TASK-057/rebaseline-integration/chokepoint-probe-0b957d1.log` |
| full regression ×5 | `pytest tests -q -p no:cacheprovider -rs` | 同一 HEAD / shell / venv / env | 每次 **982 collected = 976 passed + 6 skipped / EXIT=0**；6 条均为既有 `openssl unavailable`；无 xfail | `verification/TASK-057/rebaseline-integration/full-suite-0b957d1-run{1..5}.log` |

相对 rebaseline 951 collected，净增 31 个有效测试；Round 4 仅新增 1 个 R-011 判别测试。

## Round 4 独立 Review 批准与 master 集成（reviewed head `c79cd36` → merge `38d6eaa`）

Round 4 Review：[T1.3.1-TASK-057-c79cd36.md](../reviews/T1.3.1-TASK-057-c79cd36.md)，Reviewer=Qoder（独立非作者），review commit `ad17dbf`，结论 **`approved`**；Architecture 与 Verification 两面均 approved，Standards/Spec 视角 `executed`。R-001~R-011 全部 fixed/closed，R-007 按 evidence waiver 处理且范围未扩大。

R-011 的判别力经**双向**实测固定：交付测试在 pre-fix 源上 FAIL（裸 `OSError`，EXIT=1）、在 head 上 PASS；Reviewer 另以**真实 OS 失败**（Windows ACL read-deny，非 Mock）复现 `PermissionError/errno=13`，pre-fix 树 `CODE=None` / probe EXIT=1，head 树 `CODE=BACKUP_FILE_UNREADABLE` / probe EXIT=0，同时证明 live 未写、ledger 计数不变、backup root 无半成品。Reviewer 自有证据 21 件在 `verification/TASK-057/review-c79cd36/**`。

集成由 Qoder 作为用户本轮**指定的集成代行者**执行（`AGENTS.md` 默认仍为 Codex 唯一 master integrator）：

| 集成场景 | 实际命令/步骤 | 环境与 commit | 结果 | 日志/产物 |
|---|---|---|---|---|
| 合入内容 == 被审 head | `git merge --no-ff agent/codex/T1.3.1-backup-integration`；随后 `git diff --stat c79cd36 HEAD -- src tests` 与 `git diff --stat ad17dbf HEAD -- src tests` | master `38d6eaa`（parents `676319b` + `ad17dbf`） | **PASS**：两条 diff 均为空，即 `src`/`tests` 与被审 head 逐字节相同 | merge commit `38d6eaa`；同一断言写入下表日志头 `MERGED_CONTENT_EQUALS_REVIEWED_HEAD` |
| collect-only 计数 | `pytest tests --collect-only -q -p no:cacheprovider` | PowerShell（PATH 取自注册表 Machine+User，openssl 不可见）；TASK-012-py312；`PYTHONPATH=src`；`PYTHONDONTWRITEBYTECODE=1`；`QT_QPA_PLATFORM` unset；master `38d6eaa`，`src`/`tests` 树 clean | **982 collected / EXIT=0** | [integration-master-38d6eaa.log](../../verification/TASK-057/integration-master-38d6eaa.log) §1 |
| post-integration full regression | `pytest tests -q -p no:cacheprovider -rs` | 同上 | **982 collected = 976 passed + 6 skipped / EXIT=0**；51.08s；6 条均为既有 `openssl unavailable`；无 xfail；连跑两次结果一致 | 同上 §2 |

既有非 rebaseline 改动（`experiments/TASK-017/README.md` modified、`.qoder-credits/` 19 个未跟踪文件）在合并前后原样保留，未被触碰。**未 push。**

## 接收方式

Round 4 独立复审已批准 reviewed head `c79cd36`，T1.3.1 已以 `38d6eaa` 集成到 master 并通过合并后全量回归；TASK-057 `status: done`、AC①~⑥ 全部勾选、`integration_commit` 已填。复现命令与环境均在日志头。本 Handoff 已随 T1.3.1 / TASK-057 Closure Gate 收口；未自动启动下一项 Task。

被审分支与 worktree（保留作追溯，未删除、未清理）：`agent/codex/T1.3.1-backup-integration` @ `C:/Users/49745/.codex/worktrees/t1-3-1-backup-integration/New Manga`，tip `ad17dbf`（= merge 第二父）。

## 风险与遗留

- **R-012（P2, deferred）**：`src/infrastructure/sqlite/backup.py:152-156` restore 前置读 ledger 行的 `self._conn.execute(...)` 在任何 typed 边界之外；live DB 缺 `backup_records`（v0 形态/未迁移）时抛裸 `sqlite3.OperationalError`，调用者拿不到 `code`。当前不可达（该表自 migration v1 即存在，且 backup service 未接线生产 bootstrap），失败发生在任何写入之前故 AC③ 完好，影响仅限诊断面。**必须在把 backup service 接线到 bootstrap 的那个切片里一并 typed 化**（如 `LEDGER_UNREADABLE` + 判别测试），否则该缺口会随接线变成可达。
- **R-013（P2, fixed at closure）**：`doc/REBASELINE_PLAN.md` 的 T1.3.1 Verification 曾引用不存在的 `tests/maintenance/test_sqlite_backup.py`，收口时已更正为 `tests/storage/test_backup_restore.py` + `tests/workbench/test_restore_gate.py`。
- 既有产品限制不变：生产 bootstrap 仍未接线 backup service；这不是 T1.3.1 的集成验收缺口，未在本轮扩展。
- 回退：在 master 上 revert 最终 T1.3.1 merge commit（`git revert -m 1 38d6eaa`，故本次采用 `--no-ff` 保留单一回退点）；不得单独回退 restore gate 而保留其测试。
- `doc/REBASELINE_PLAN.md` 的 Task tree 状态表仍把 T1.3.2 记为 `ACTIVE — STOP before implementation`、T1.3.1 记为 `PLANNED`（上一轮收口也未同步该表）。当前运行状态以 `doc/STATUS.md` 为准；是否回填计划表由 Codex 裁定，本轮未越权修改。
