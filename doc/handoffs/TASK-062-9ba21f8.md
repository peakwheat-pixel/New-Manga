---
task_id: TASK-062
author: Codex
recipient: Qoder
base_commit: 41d7aee
delivery_head: 9ba21f8
status: done
integration_commit: ae50c8e
---

# Handoff：TASK-062（后置复审残留收口：Q-006 + TASK-046 R-001 + R-009）

> Reviewer=**Qoder（非作者）**；Owner=Codex。base=`41d7aee`，branch
> `agent/codex/TASK-062-posthoc-residuals`，worktree `G:/CODEX/New Manga.worktrees/TASK-062-codex`。
> 证据索引：[verification/TASK-062/README.md](../../verification/TASK-062/README.md)。

## 交付结果

| commit | 内容 |
|---|---|
| `9ba21f8` | **delivery head**：`diagnostics.py` 不变式成真 + 值形状判据扩到嵌入式凭据 + Qt 编码页补证（R-001）+ `verification/TASK-062/**` |
| 本 Handoff 提交 | Task 记录、Handoff、`doc/STATUS.md` 台账行（docs-only） |

变更面（3 个受测文件 + 证据）：`src/application/maintenance/diagnostics.py`、
`tests/diagnostics/test_report.py`（+6 例）、`tests/reading_export/test_webtoon_tiles.py`（+1 例）。
**未触碰** Schema/migration、`requirements.txt`、`src/ui/qml/**`、`AGENTS.md`；**未触碰 TASK-061 的在飞写集合**
（`src/infrastructure/sqlite/**`、`managed_storage.py`、`cleanup.py`、`tests/workbench/**`）；未 push。

### AC 逐项对照

- **AC ①（Q-006：不变式成真）——完成。** `build_diagnostics_report`
  （`src/application/maintenance/diagnostics.py:123` 起）现在对**每一段**的**每个值**过
  `redact_value`：`application`（`app_version`/`platform_python`）、`database`
  （`schema_version`/`database_path`）、`settings_summary`、`environment_paths`、`recent_errors`
  （值**与**由 `f"{occurred_at} {source}"` 合成的**键**）。值形状判据由
  `^Bearer\s+` / `^sk-` 的**前缀 match** 改为带左守卫的 **search**：
  `(?<![A-Za-z0-9])Bearer\s+[A-Za-z0-9._~+/=\-]{6,}`、`(?<![A-Za-z0-9])sk-[A-Za-z0-9_\-]{8,}`
  ⇒ 嵌入式凭据被遮，`task-live-run/`、`sketch.png` 一类的普通路径段**不**被误伤。
  边界用例两侧都钉住（`D:/workspace/sk-tools-market` 被遮 = 安全方向；
  `password=hunter2` 不遮 = 明确不属于本屏职责）。
- **AC ②（判别用例）——完成。** 新增 `TestRedactionReachesEverySection`（6 例）：
  ①`platform_python` / `database_path` 带凭据 ⇒ `"[redacted]"`；②`recent_errors` 的**值**与**键**
  含 `sk-…` / `Bearer …` ⇒ 序列化结果中令牌**不残留**，且键亦为 `[redacted]`；
  ③良性值（`0.1.0`、`CPython 3.12.3`、`…/library.db`、`D:/MangaData`、`a.png`）**全段幸存**；
  ④嵌入式形状被抓；⑤普通路径/版本/单词不被误伤；⑥文档化边界两侧。
  **既有 `tests/diagnostics/**` 断言一条未改**（该文件只有追加）。
- **AC ③（docstring 诚实）——完成。** `redact_value` 写明"键名 + 两个窄值形状、**不**扫任意自由文本"
  并给出 `password=hunter2` 反例；`DiagnosticsReport` 与 `build_diagnostics_report` 的
  "Every value passes the redaction screen" 改为 "**every value and every key**"，
  并保留"防御纵深，收集方仍应传摘要"的口径。原过强承诺已消除。
- **AC ④（TASK-046 R-001 补证）——完成。** `tests/reading_export/test_webtoon_tiles.py` 追加
  `write_qt_encoded_png()` / `png_row_filter_types()` 与
  `test_overlap_choice_on_a_qt_encoded_page`：**先断言夹具不是全 filter-0**（否则测试自动失效返回时
  会静默重开缺口），再断言 overlap `0` 与 `64` 的瓦片文件**逐字节相同**、瓦片行拼接**== 整页**。
  独立探针实测该夹具行过滤器集合 = **[0, 1, 4]**（None/Sub/Paeth）。
  **如实标注**：`Up`/`Average` 未被 libpng 在本图样上选中，本补证钉的是"非退化自适应过滤器"这一性质，
  **不等于**每种过滤器各一条夹具（见"风险与遗留"）。既有断言一条未改，只追加。
- **AC ⑤（不回归）——完成。** delivery head 全仓 **930 collected = 924 passed / 6 skipped / EXIT=0**（×4 次）；合并后 master **937 collected = 931 passed / 6 skipped / EXIT=0**，openssl 可用口径 **937 passed / 0 skipped**。
  基线 `41d7aee` 为 923 collected（917/6）⇒ **+7 例，未跌破**。`tests/diagnostics` 19→**25 passed**、
  `tests/reading_export` 112→**113 passed**，两套件通过数**均未减少**。**无新增 skip/xfail、
  未放宽任何既有断言**。
- **AC ⑥（证据口径）——完成，并按事实分列。** 4 次全仓 + 3 次定向 + 1 份探针日志，**每份带 EXIT 与
  shell/venv 头**，逐次入库到 `verification/TASK-062/`。判别力是 **artefact**
  （`discriminating-prefix-tree.txt`）：新用例跑在 `git archive 41d7aee` 的修前树上 ——
  **diagnostics 组 4 failed / 14 passed / EXIT=1（有判别力，4/6 例）**；
  **webtoon 新例 1 passed / EXIT=0（无判别力）** —— 因为本切片的 `src` 改动只有 `diagnostics.py`
  一个文件（`git diff --name-only 41d7aee -- src` 实证），该例钉的是**原本未被覆盖的性质**。
  此处**明确声明"未作修前判别"**，不写成"已判别"。另两例良性值用例在修前树亦通过，
  同属防假阳性钉子而非判别用例，一并标注。
- **AC ⑦（Handoff + 证据 + 非作者 Review + 集成 + STATUS 台账）——完成。** Qoder 非作者 Review
  `approved`（`0fe774d`），Codex 已以 merge `ae50c8e` 集成实现；评审证据以 merge `5d65d3b` 落库，
  Task 已置 `done`，STATUS 台账已刷新。

## 验证证据

| AC/场景 | 实际命令（原样） | 环境与被测 commit | 结果 | 日志/产物 |
|---|---|---|---|---|
| AC ⑤ 全仓 ×3 | `pytest tests -q -rs -p no:cacheprovider` | pwsh + `TASK-012-py312`、`PYTHONDONTWRITEBYTECODE=1`、不设 `QT_QPA_PLATFORM`；`@4c81dca` 工作树 | **PASS** 924 passed / 6 skipped / EXIT=0（×3） | [full-suite-run1.log](../../verification/TASK-062/full-suite-run1.log)、`…run2.log`、`…run3.log` |
| AC ⑤ 全仓（提交后） | 同上 | 同上；**`@9ba21f8` delivery head** | **PASS** 924 passed / 6 skipped / EXIT=0 | [full-suite-run4-post-commit.log](../../verification/TASK-062/full-suite-run4-post-commit.log) |
| AC ⑤ 全仓（合并后 master） | `pytest tests -q -p no:cacheprovider -rs` | 同上；merge `ae50c8e`，review evidence merge `5d65d3b` | **PASS** 931 passed / 6 skipped / EXIT=0（937 collected；openssl 可用口径 937/0） | [integration-master-2026-09-20.md](../../verification/TASK-062/integration-master-2026-09-20.md) |
| AC ①② | `pytest tests/diagnostics -q -rs -p no:cacheprovider` | 同上 | **PASS** **25 passed / 0 skipped** / EXIT=0 | [diagnostics-suite.log](../../verification/TASK-062/diagnostics-suite.log) |
| AC ④ | `pytest tests/reading_export -q -rs -p no:cacheprovider` | 同上 | **PASS** **113 passed / 0 skipped** / EXIT=0 | [reading-export-suite.log](../../verification/TASK-062/reading-export-suite.log) |
| AC ④ 新例定向 | `pytest tests/reading_export/test_webtoon_tiles.py -q -rs -k qt_encoded` | 同上 | **PASS** 1 passed / 22 deselected / EXIT=0 | [webtoon-tiles-targeted.log](../../verification/TASK-062/webtoon-tiles-targeted.log) |
| AC ④ 探针 | `python verification/TASK-062/qt-encoded-filter-probe.py` | 同上 | **PASS** 过滤器 `[0,1,4]`、3/3 瓦片字节相同、拼接==整页 / EXIT=0 | [qt-encoded-filter-probe.txt](../../verification/TASK-062/qt-encoded-filter-probe.txt) |
| AC ⑥ 判别力 | 新用例置于 `git archive 41d7aee` 修前树运行 | 同上；修前树 = base_commit | **有判别（diagnostics 4 failed/14 passed/EXIT=1）；无判别（webtoon 1 passed/EXIT=0，如实声明）** | [discriminating-prefix-tree.txt](../../verification/TASK-062/discriminating-prefix-tree.txt) |
| R-009 核对 | `git diff --name-only 9522f2d a2b23ad -- src tests`（空）；`Get-ChildItem -Recurse -Filter test_shutdown_drain.py` | `@9ba21f8` 工作树 | **PASS（勘误已核实并记录）** | [r009-erratum.md](../../verification/TASK-062/r009-erratum.md) |

**6 条 skip 逐条原因**（全部为**既有**环境缺失，非本切片引入）：
`tests/network/test_connection_tester.py:106`、`test_transport_tls.py:39`、`:47`、`:62`、`:69`、`:83`
—— 均为 `openssl unavailable`。

**口径换算**：delivery 的本机 PowerShell 口径为 924 passed / 6 skipped（共 930）；合并后 master 为
931 passed / 6 skipped（共 937）。这 6 条在 openssl 可用的机器上转为 pass ⇒ **openssl 可用口径 =
937 passed / 0 skipped**，总数一致。

**NOT_RUN / N/A 明示**：无 GUI 手工步骤；本切片不涉及真实 Provider、模型权重或网络端点，
故无"模型质量"类证据需求。

## 接收方式

分支 `agent/codex/TASK-062-posthoc-residuals`（worktree `G:/CODEX/New Manga.worktrees/TASK-062-codex`），
delivery head `9ba21f8`。Reviewer 复现：

```powershell
$env:PYTHONDONTWRITEBYTECODE = "1"
Remove-Item Env:QT_QPA_PLATFORM -ErrorAction SilentlyContinue
$py = "G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe"
cd "G:\CODEX\New Manga.worktrees\TASK-062-codex"
& $py -m pytest tests/diagnostics -q -rs -p no:cacheprovider     # 25 passed / EXIT 0
& $py -m pytest tests/reading_export -q -rs -p no:cacheprovider  # 113 passed / EXIT 0
& $py -m pytest tests -q -rs -p no:cacheprovider                 # 924 passed / 6 skipped / EXIT 0
& $py verification/TASK-062/qt-encoded-filter-probe.py
```

**请 Reviewer 重点判**：①`search` 化后**是否仍有误伤面**（左守卫只挡字母数字；`/`、`_`、`-` 起始的
`sk-…` 仍会被遮，是否接受这一"安全方向"）；②`recent_errors` **整值替换**为 `[redacted]`
（而非原地改写）是否牺牲了过多可诊断性；③`test_overlap_choice_on_a_qt_encoded_page` 的
"先断言夹具非全 filter-0"是否足以让该用例在 libpng 改变策略时**报警而非静默通过**；
④R-009 以"记录 + 待集成方 docs-only 更正"处置是否可接受（允许范围内不含 TASK-060 的两份文件）。

## 风险与遗留

1. **R-009 状态 = `closed`。** 三处 docs-only 更正已在合并后落地：
   `tests/core/test_shutdown_drain.py` 路径、TASK-060 `base_commit 9522f2d` 与判别树等价注记、
   TASK-060 READY 行说明（对应 [r009-erratum.md](../../verification/TASK-062/r009-erratum.md) §2）。
2. **过滤器覆盖面未穷尽。** Qt/libpng 在本夹具上只选用 `None`/`Sub`/`Paeth`；
   R-001 列举的 `Up`/`Average` **仍未有夹具**。不建议把测试改成"猜 libpng 的启发式"；
   若要收全，应另开小切片构造能稳定诱发 `Up`（行间高相关）/`Average`（中等梯度）的图样。
3. **脱敏屏的固有边界（有意为之）。** 键名 + 两个窄值形状；**不**扫任意自由文本。
   若未来的诊断字段要外发用户可控自由文本，应在**收集方**做摘要/裁剪，而不是继续加宽本正则
   （加宽会开始吞正常路径）。该边界已在 docstring 与用例两侧钉住。
4. **无破坏性操作**：不改 Schema/migration、无迁移、无数据写入语义变化；
   回退方法 = revert `9ba21f8`（纯代码+测试，无状态残留）。
5. **未做**：`doc/STATUS.md` 台账行以外的主线文档（`00_INDEX`/`12_ROADMAP`/`tasks/README`）改动——
   按 Task 边界由集成方在集成时处理。
6. **新登记（P3，docs 卫生，非本切片返工）**：TASK-047 已被用户否决为 `rejected`、设计门改挂 TASK-059，
   但 `src/` 中仍有 **6 处**把 "TASK-047 design gate" 当作在役前置的注释（
   `src/bootstrap/app.py:148`、`src/ui/viewmodels/workbench/viewmodel.py:96,437,825`、
   `src/application/maintenance/diagnostics.py:12`、`src/ui/qml/workbench/WorkbenchView.qml:82`）。
   其中 `diagnostics.py:12` 在本切片允许范围内，但**有意不改**：①单点改会造成"同一概念一半指向
   TASK-047、一半指向 TASK-059"的更差状态；②它属文档卫生类，宜一次扫齐 6 处。
   建议另开 docs-only 小切片（或并入 TASK-059 的令牌映射切片）统一改为 TASK-059 并记录失效理由。
