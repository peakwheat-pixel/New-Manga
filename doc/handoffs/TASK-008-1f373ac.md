---
task_id: TASK-008
author: ZCode
recipient: Codex（转 DeepSeek Harness 独立 Review）
base_commit: f129ae96900fb567288f91a78d55c0ff4ecafe6d
delivery_head: 1f373ac2240fb5fc6304e447b7d0fd873310aef1
status: in_review
---

# Handoff：TASK-008（Region 编辑、Revision 与人工保护）

实现 head：`1f373ac`。提交列表（自 base `f129ae9`）：

| commit | 说明 |
|---|---|
| `da7bf79` | docs(TASK-008): 按派单接管，Task 文件补记 in_progress（派单时仍为 proposed 的差异与切片边界声明已记录） |
| `1f373ac` | feat(TASK-008): region 领域 + 编辑服务 + tests/editing（reviewed_head） |

变更路径（11 文件，全部在允许白名单内）：`doc/tasks/TASK-008.md`、`src/domain/regions/**`、`src/application/editing/**`、`tests/editing/**`、`verification/TASK-008/**`、本 Handoff。`tests/library`、`src/ports`、`src/infrastructure`、`src/bootstrap`、`src/ui`、`tests/core`、`tests/storage`、依赖清单、AGENTS、其他 Task：零触碰（越界过滤复核 0 命中；开发中一次 tests/library 同名模块改名已当场回退，见 author-verification §3）。

## 交付内容与 AC 对照

- **AC-REGION-001 统一模型**：**满足**。`Region` 单模型承载几何（BBox+Polygon）、四级文本、TextStyle、SFX 策略、三种 Region 级 Lock、review 状态与 Revision 历史（`snapshot_state` 覆盖 D03 §15.1 全部保存范围）。
- **AC-REGION-002 几何编辑**：**满足**。新建/删除/移动/缩放/Polygon 编辑经 `save_geometry`（user revision）；合并（覆盖 BBox + 多边形拼接 + 文本拼接 + 最强保护继承 + 源软删）与拆分（调用方几何、保护继承、源软删）；模拟重启后几何与文本仍在（`test_geometry_and_regions_survive_simulated_restart`）。
- **AC-REGION-003 Reading Order**：**满足**。`reorder_regions` 人工调整；`reading_order_for_context` 返回人工最终顺序供翻译上下文（D03 §6.5）。
- **AC-REGION-004 Region Type**：**满足**。六类型枚举（speech/narration/sfx/title/note/other）+ SFX 策略三值。
- **AC-OCR-002 Re-OCR 不覆盖人工译文**：**满足**。`apply_ocr_result` 只更新 ocr_text；人工 edited/final 保留；源文本变化且有人工译文时返回 `retranslate_hint`（"原文已变化"提示）。
- **AC-TRANS-001 四级文本**：**满足**。ocr/machine/edited/final 四级区分；final 解析（D03 §8.3）：非空且**已确认**的 edited → edited；否则 machine；否则空（`test_final_resolution_rules` 覆盖空字符串/未确认/已确认/全空四象限）。
- **AC-TRANS-002 人工编辑自动保护**：**满足**。`save_manual_translation` 自动置 `manual_edited=true` + `translation_locked=true`（needs_review）；`confirm_final` 显式确认；仅用户显式解锁。
- **AC-REV-003 Pin**：**满足**。`set_revision_pinned` 标志（清理豁免的执行属 TASK-021）。
- **AC-REV-004 恢复 Revision**：**满足**。恢复 = 以旧快照创建**新 Revision**（origin=restored、restored_from_revision_id 溯源），current 指向恢复副本，历史不改写（`test_restore_revision_creates_new_revision_with_provenance`）。
- **AC4**：本 Handoff 交付；`in_review`，done 待 DeepSeek 非作者 Review + Codex 集成。

## 冻结契约的实现要点（TASK-002 §2、D06 §87~90）

- **§2.1**：`create_region` 在创建步骤即提交首个 Revision，current 立即非空；任何路径都不会把已建立的 current 清空。
- **§87**：OCR 保存 / 机器结果接受 / 人工保存 / final 确认 / Geometry 保存 / Lock 变化均产生 Revision（每事件一个 user/machine/confirmed revision）。
- **§89/§90 Optimistic Write Guard**：`apply_ocr_result`/`apply_machine_translation` 在写入前重读 current 与锁：expected current 不匹配 → `INPUT_REVISION_CHANGED`（current 不动，payload 交管道层作为 Candidate，落库属 TASK-011）；translation/region 锁定 → `LOCK_CHANGED`（同样不覆盖）；两者通过才提交 machine/needs_review revision。
- **§15.2**：不按输入逐字建版；仅上述事件建版。

## 验证证据

完整记录见 [verification/TASK-008/author-verification.md](../../verification/TASK-008/author-verification.md)。摘要：

| 场景 | 命令 | 环境/commit | 结果 | 证据 |
|---|---|---|---|---|
| 编辑套件（任务计划命令） | `python -m pytest tests/editing` | py3.12.3 任务环境；head `1f373ac` | PASS，退出码 0，17 passed | author-verification #1 |
| 全量回归 | `PYTHONPATH=src python -m pytest tests` | 同上 | PASS，退出码 0，79 passed | 同上 #2 |
| whitespace / 范围 / 越界 | `git diff --check f129ae9 1f373ac --`；name-only 过滤 | 本 worktree | PASS（0 / 11 文件全在允许路径 / 越界 0 命中） | 同上 #3~4 |
| 架构边界 | grep domain/application 禁用 import | 同上 | PASS | 同上 #5 |

NOT_RUN / N/A：SQLite RegionRepository（范围外，同 TASK-007 边界声明）；StepResultCandidate 落库（TASK-011）；Page Lock 消费（TASK-011）；TextStyle 完整字段（TASK-014）；UI/autosave 计时器（禁 QML）；性能（无 AC）。

## 复审建议

重放两条 pytest 命令与范围过滤；重点：① `resolve_final` 的确认语义是否等价 D03 §8.3（未确认→machine 的取舍）；② §90 守卫的比较对象（current revision id）与 Candidate 缺位的承接声明；③ merge/split 的文本/保护继承策略（产品细节我方裁决：OCR/machine 以空格拼接、人工保护取最强——如与预期不符请列为 finding）；④ restore 不改写历史的实现。

## 风险与遗留

1. 持久化未接 SQLite（同 TASK-007 边界）；`RegionRevision.snapshot` 为 JSONable dict，SQLite 落地时以 json 列存储即可，无结构风险。
2. merge 的文本拼接分隔符（空格）为最小实现，产品若要求逐段保留需新授权。
3. `reading_order` 调整未建 Revision（D06 §87 触发列表未含 reading_order 保存；D03 §15.1 要求其入快照——快照已含，独立建版留待 Codex 裁决）。
4. 本切片不释放 TASK-009~027。
