---
task_id: TASK-038
author: ZCode（窗口第二轮 W5 Owner；Reviewer=ZCode 子 agent）
recipient: Codex（T1 后 post-hoc 复审）
base_commit: 047164e（分支起点 202dd21 = round2 指令提交，已含该基线；开工提交 066a4c7）
delivery_head: 317f33e
status: delivered（待窗口内子 agent Review；结论仅 approved_subagent/changes_requested）
---

# Handoff：TASK-038（生产装配收口：reader/export ViewModel 注册 + tile_factory 注入 + 文档导入接线）

## 交付结果

**实现 head `317f33e`**（分支 `agent/zcode/TASK-038-production-assembly`；开工提交 `066a4c7`）。6 文件，+385/−18：

| 路径 | 内容 |
|---|---|
| `src/bootstrap/app.py` | 装配核心（下详） |
| `src/ui/viewmodels/bookshelf/viewmodel.py` | `document_importer` 可选注入 + `importDocumentsFromUrls` 槽（与 `importFilesFromUrls` 同构的 URL→bytes 纪律） |
| `src/ui/viewmodels/reader/viewmodel.py` | **R-006 fixed**：删除 `requestTiles` 死变量 `wanted` 绑定（保留 `ensure_viewport` 材料化副作用并注释） |
| `src/infrastructure/imaging/webtoon_tiles.py` | **R-003 fixed**：删除 `default_cache_root`（uuid 孤儿目录来源；生产由装配注入 `cache_dir`） |
| `tests/core/test_bootstrap.py` | **AC ⑤ 契约测试** ×2 |
| `tests/reading_export/test_qml_contract.py` | **AC ①**：`:224` 用例前提变化的**等价更新**（未删除、断言未放宽，docstring 说明前提变化） |

**装配内容**（`assemble_services`/`assemble_engine`）：

1. **AC ① reader 接入**：`ReaderViewModel(reading, reader_catalog, export_service, tile_factory)` 注册为 `readerViewModel`；`reading` 服务与 progress/export 历史落在与 library.db **同一数据根**；`readerContextChanged` → `_open_reader_chapter()`（导航 `enterReader` 链路自动打开章节；空章节防御性保持空态）；新增装配侧 `_ManagedReaderCatalog`（domain `Page` → reader `ReaderPage`：original=Managed Copy 绝对路径、translated=当前 TRANSLATED artifact（无则空 → reader 显式回退 D06 §97）；OCR 文本在 Region 体系非页面字段，`text` 留空——已登记遗留）。
2. **AC ② export 双路径（注册名与对应关系）**：
   - **路径 A（reader 发起）**：属性 **`readerViewModel.exportController`**——`ReaderViewModel.openExporter()` 惰性构建、绑定 reader 当前章节；`ReaderView.qml:59` 的导出窗口消费它。注册名即 `readerViewModel` 本身（controller 为其 QObject 属性）。
   - **路径 B（独立导出窗口）**：context property **`exportViewModel`**——装配持有，随 workbench 章节上下文重建（`navigation.enterWorkbench` → `workbenchContextChanged` → `_rebuild_export_viewmodel` → 经 `set_export_context_updater` 回调向 live engine 重发布）；无章节上下文时为 `None`。
   - 两路径共享同一 `ExportService`，VM 实例独立——契约测试断言 `controller is not standalone`。
3. **AC ③ tile_factory 注入**：`TiledPageRasterizer(cache_dir=managed_root/cache/webtoon-tiles, tile_height=4000, overlap=64, prefetch=1, cache=TileCache(512MiB))`——纯 Python 注入（QML 零文件/模型访问）；生产可达性由契约测试证明（`navigation.enterReader` → `tilesActive=True` → `requestTiles` 在 managed 根下材料化 tile PNG）；源不可解码仍 fail-closed（TASK-020 typed error 不变）。
4. **AC ④ 文档导入接线**：`ImportDocumentsUseCase(PdfiumDocumentRaster(), copy_store, repository)`——与 `ImportImagesUseCase` 同构（同一 Managed Copy store 与页 sink）；**MOBI 仍 BLOCKED**（非 PDF 载荷 typed `UNSUPPORTED_FORMAT`，契约测试锁定 failed=1）。
5. **隐藏缺陷暴露与处置（Task 风险预告应验，均白名单内）**：①`_ManagedPageCatalog` 返回 domain `Page` 与 reader `ReaderPage` 契约不匹配 → 新增 `_ManagedReaderCatalog` 适配器；②导航至无页章节时 `reading.open` 抛异常 → 装配预检空页保持空态。`src/application/reading/**` 与 QML **零改动**。

## AC 对照

| AC | 结果 | 证据 |
|---|---|---|
| ① reader 接入 + :224 更新 | ✅ | 契约测试（`contextProperty("readerViewModel") is services.reader`；`enterReader` 链 `hasChapter=True`）；`:224` 用例等价更新 |
| ② export 双路径 | ✅ | 本表对应关系 + 契约测试（path B 随上下文重建重发布；path A 独立实例） |
| ③ tile_factory | ✅ | 契约测试（tilesActive=True、tile PNG 落 managed 根） |
| ④ 文档导入 | ✅ | 契约测试（document_importer/Pdfium raster/importDocumentsFromUrls；MOBI→failed=1） |
| ⑤ 装配契约测试 | ✅ | `test_assemble_engine_registers_reader_export_and_tiled_reader` + `test_reader_service_and_tile_cache_live_on_the_data_root` |
| ⑥ R-002/003/005/006 | ✅ | disposition 表（fixed×3 / deferred×1 带理由） |
| ⑦ 回归 | ✅ | 见验证证据表 |
| ⑧ Review | 待子 agent | 本文件 |

## AC ⑥ disposition（TASK-020 遗留 R-002/003/005/006）

| ID | 处置 | 理由 |
|---|---|---|
| R-002（TileCache 装饰化） | **fixed → 复审建议改记 accepted（采纳）** | 生产装配实际构造 `TileCache(512MiB)` 注入（真实使用点成立）；Review R-001 指出 `tile_file` 只 put 不 get、装饰化本质未变——**最终口径=accepted**：保留现形态作为内存账本，get 路径随真实性能需求切片再评估 |
| R-003（`default_cache_root` uuid 孤儿目录） | **fixed（删除）** | 生产由装配注入确定 `cache_dir`，无参路径不复存在 |
| R-005（QML Repeater 冗余 `visible` / 无消费者属性） | **deferred** | QML 触碰需按白名单申请；纯清理无行为价值，随下次 QML 切片 |
| R-006（`wanted_keys` 死变量） | **fixed（删除）** | R-001 修订遗留；连带删除失效的 `wanted` 返回值绑定 |

## 验证证据（同一 shell + 同一 venv）

**口径声明**：本切片全部数字来自**同一调用方式**——`powershell.exe -NoProfile -Command`（由 ZCode 的 Git Bash 会话启动，PATH 继承含 openssl）+ `G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`。该口径下 `tests/network` 的 6 条 TLS 测试真实执行 → 全仓 **N passed / 0 skipped**；本窗口内所有数字严格同口径可比。指令给的基线 764/6 为纯系统 PATH PowerShell 口径（无 openssl），两者收集总数一致（770/772 对应 764/762+新增测试数）。

| 场景 | 命令 | commit | 结果（exit + passed/skipped） |
|---|---|---|---|
| 基线（开工后、实现前） | 全仓 `-q -rs -rf` | `066a4c7` | exit 0，**770 passed / 0 skipped** |
| mandated 六套件 | `pytest tests/core tests/providers tests/pipeline tests/reading_export tests/import_formats tests/library -q -rs -rf` | `317f33e` | exit 0，**378 passed / 0 skipped** |
| reading_export | 同目录 | `317f33e` | exit 0，**82 passed / 0 skipped**（含 AC ① 更新用例 + TASK-037 契约原样） |
| 全仓 ×5 | `-q -rs -rf` 逐次 | `317f33e` | **每次 772 passed / 0 skipped、exit 0**（[full-suite-runs.log](../../verification/TASK-038/full-suite-runs.log)） |

## Review 与集成记录

- Review：[doc/reviews/TASK-038-317f33e.md](../../doc/reviews/TASK-038-317f33e.md)（`fcf791f`）= **approved_subagent**；3×P3：R-001（R-002 disposition 口径——已上表改记 accepted）、R-002（full-suite log 第 2-5 次缺逐次退出码行——证据留痕不全，integrated 后复验单列补记）、R-003（契约测试断言私有属性，possible Inappropriate Intimacy）。
- 集成：`f835ac9`（merge，parents `047164e`+`fcf791f`）；集成后复验全仓 **772 passed / 0 skipped**、exit 0（同口径 PowerShell）。

## 风险与遗留

1. translated 阅读模式消费当前 TRANSLATED artifact（`_ManagedReaderCatalog`）；OCR 文本（Region 体系）不进 `ReaderPage.text`——文本导出的生产接线属后续装配切片。
2. UI 端到端人工点击流（书架→阅读器）无 UI 自动化框架覆盖，属既有边界；装配链路（enterReader → openChapter → tilesActive）已由契约测试自动化。
3. 未 push；未改 Schema/依赖清单/pipeline seam/AGENTS/其他 Task/QML；无新增 skip/xfail。
