---
task_id: TASK-010
author: ZCode
recipient: Codex（转 DeepSeek Harness 独立 Review）
base_commit: 2bdfd6f82b67a550c0550ee930d49bdb12656322
delivery_head: cd76d30fdc561eb8f22a849eeb5989473957dc5b
reviewed_head: cd76d30fdc561eb8f22a849eeb5989473957dc5b
implementation_merge: 1ea9c80fe29d33da33d533e22278cd2f474cb31a
integration_commit: a225790d5eaf90227249039255bb554307f5b2cc9
review_report_commit: 008b1013a9863f4f506764d17d01245cb2403db1
decision: approved
status: integrated
---

# Handoff：TASK-010 实现翻译约束、TM 与 Context（已集成）

交付 `cd76d30`（分支 `agent/zcode/TASK-010-translation-context`，worktree `G:/CODEX/New Manga.worktrees/TASK-010-zcode`），base=`2bdfd6f`（基线 release=`2cceb1e`）。依据：D03 §12~14、D06 §9~18/§85、D08 AC-SFX/CONSTRAINT/TM/TRANS、TASK-002 契约 §2/§3/§4/§7.2。主责 13 个 AC 的实现与测试证据见 [verification/TASK-010/author-verification.md](../../verification/TASK-010/author-verification.md)。

Review 报告 `008b101`（固定范围 `2cceb1e..cd76d30`）decision=`approved`，无 P0/P1；实现与 Review 已按协议串行合并，`integration_commit=a225790`。

## Review decision 与 findings disposition

- **F-01（已关闭）**：全量口径更正为 `322 passed, 6 skipped`；6 项均因 `openssl unavailable`，不是通过项。
- **F-02（已关闭）**：D03 §12.5/§14.4 已登记 `AUTO_ACTIVE_CONFIDENCE=0.90` 与 `DEFAULT_FUZZY_THRESHOLD=0.80`，均为可调实现参数而非冻结产品契约。
- **tests/rendering 顺序依赖（已复现）**：ZCode 以 `05effee` 完成最小复现并定位根因；Codex 随后在合并主线复跑为 `2 failed, 1 passed`。原因是无参数 pytest 先收集 `experiments/TASK-004/test_smoke.py`，其 offscreen `QGuiApplication` 进程级单例污染后续 rendering。详见 [rendering-order-repro.md](../../verification/TASK-010/rendering-order-repro.md)。

## 后续取证结果（2026-09-16）

ZCode 在 `G:/CODEX/New Manga.worktrees/TASK-010-zcode`、分支 `agent/zcode/TASK-010-translation-context` 提交 `05effee`，证明无参数 pytest 的命令形态稳定触发该问题，并定位为 `experiments/TASK-004/test_smoke.py` 的 offscreen `QGuiApplication` 进程级单例污染。Codex 以 merge commit `014231f` 纳入主线并独立复跑确认。

本次取证只改验证文档，未修改 `src/**`、`tests/**`、`experiments/**` 或 pytest 配置；不据此创建新 Task。后续若修复根因，需另行获得超出 TASK-010 白名单的范围授权。

## 提交列表（自 `2cceb1e`）

| commit | 说明 |
|---|---|
| `cda5ab8` | docs(TASK-010): claim task, ready -> in_progress（流程性认领，仅任务文件） |
| `0df119d` | feat: constraint entities, merge order and candidate rules（**批次 A**） |
| `26ec405` | feat: translation memory write/query service（**批次 B**） |
| `1998e96` | feat: context builder, constraint freeze, SFX gate and translate step（**批次 C**） |
| `cd76d30` | fix: normalize TM entry scope/status to frozen enums（**delivery_head**） |

元数据提交（本 Handoff、Task 状态与 author-verification，无源码变化）在 delivery_head 之后，不属被审内容。

## 实现结构

| 模块 | 内容 | 依据 |
|---|---|---|
| `src/domain/constraints/entities.py` | TranslationConstraint/ConstraintRevision、四态候选、normalized_key、每次状态变更记 Revision、创建事务须 attach（current 非空） | D03 §12~14、TASK-002 §2.1 |
| `src/domain/constraints/merge.py` | Chapter>Book>Global；同层 locked>manual>auto；pending/rejected/disabled 不作正式约束；确定性 tie-break | D03 §12.6、D06 §11 |
| `src/domain/constraints/candidates.py` | 高置信→active/低置信→pending；locked 不覆盖；manual 让位；rejected/disabled 键不再创建 | D03 §12.5、D06 §9、AC-CONSTRAINT-002/003/004 |
| `src/application/translation/knowledge/tm.py` | TM 记录模型（D03 §14.5 全字段）、consumer-side Store 协议、写入门禁（仅人工确认/已校对）、禁用保留历史、查询顺序 book Exact→book Fuzzy→全局 | D03 §14、D06 §12、TASK-002 §2.3 |
| `src/application/translation/context/snapshot.py` | effective constraint 快照冻结（MappingProxyType 只读）；Run 中改术语不影响冻结值 | D06 §10、TASK-002 §3.2 |
| `src/application/translation/context/builder.py` | 三种上下文模式（单页窗口/尽可能多页/Webtoon Region 连续窗口分 chunk）、Page sort_order+Region reading_order 排序、Context Scope ≠ Write Scope、provenance | D06 §13~17 |
| `src/application/translation/context/gate.py` | SFX Policy Gate：skip/manual→skip_policy(reason=sfx_skip)、translate 正常 | D06 §85、TASK-002 §7.2 |
| `src/application/translation/context/step.py` | Translate Step 编排（D06 §18 输入输出）、Provider Request 组装（UI 不拼 Prompt）、输出映射守卫（越界/缺失拒绝，TASK-002 §4 语义） | D06 §13/§18、AC-TRANS-003 |

## 验证（详见 author-verification）

- `PYTHONPATH=src python -m pytest tests/knowledge -q` → **78 passed**（含优先级冲突、重复 Rejected、未确认 TM 不写、越界输出拒绝、Run 中改术语不影响冻结值、Webtoon Region 窗口非 Tile）
- 全量 `pytest tests -q` → **322 passed, 6 skipped**（×3 稳定；6 项均因 `openssl unavailable`）；架构守卫 4 passed；`git diff --check 2cceb1e..cd76d30` 无输出；范围核对全部在白名单
- 退出码全 0；环境 Windows 10.0.26200 / Python 3.12.3 / pytest 9.1.1

## 设计取舍（请 Review 与 Codex 重点确认）

1. **Fuzzy 阈值**：获批规格无数值，默认 `0.80`（可配置），`difflib.SequenceMatcher`（标准库，未引入 Embedding/RAG）。
2. **自动候选高置信阈值**：D03 §12.5 无数值，默认 `AUTO_ACTIVE_CONFIDENCE = 0.90`（可配置）。
3. **TM 模型位置**：白名单无 domain/tm 路径，TM 记录与 Store 协议置于 application 层（consumer-side port，沿用 TASK-008 惯例）；SQLite 适配器留待后续持久化 Task。
4. **Token 预算**：1 token ≈ 1 字符（确定性、零依赖），estimator 可注入；真实 tokenizer 属 Provider 适配层。
5. **Webtoon chunk 重叠**：D06 §17 为"允许"非必须；本版为连续窗口+预算切块，chunk 间文本重叠留待 Provider 适配层。
6. **TranslateStep 边界**：不含 Planner 的 Lock/skip_lock 决策与真实 Provider 调用（TASK-011+）；`provider_call` 注入，映射守卫强制执行。

## 移交项与未完成项

- **tests/rendering 全量顺序依赖缺陷（已复现，根因已定位）**：无参数 pytest 先收集 `experiments/TASK-004/test_smoke.py`，offscreen `QGuiApplication` 单例导致后续 rendering 字体回退；ZCode 报告 `05effee`、Codex 合并 `014231f`，未修改测试套件。
- TM/约束 SQLite 持久化适配器：不在白名单，属后续 Task。
- 真实 Provider 端到端翻译与 chunk 重叠策略：依赖后续 Provider 适配 Task。

## Review 建议

被审范围 = `2cceb1e..cd76d30`（源码 14 文件 + tests/knowledge 10 文件）。建议 Reviewer 从 `tests/knowledge/` 的 78 项开始对照 AC，再核 design decisions 1~6 是否与获批规格一致。Review 完成后本 Task 状态推进与集成由 Codex 执行。
