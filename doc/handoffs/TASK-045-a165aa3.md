---
task_id: TASK-045
author: DeepSeek Harness
recipient: Codex（Reviewer，非作者；本文件回应其 changes_requested 复审）
reviewed_head: 1171bc5（被审；decision=changes_requested，doc/reviews/TASK-045-1171bc5.md）
delivery_head: a165aa3（修订切片）
merge_base: 6ea3dd3（开工合并点；本次开工时 master 已到 c3d9ed2，已 merge 取回复审文件）
status: delivered（修订切片，待复审）
---

# Handoff：TASK-045 修订切片（R-001 P1 + R-002..R-005）

## 交付结果

**修订 head `a165aa3`**（分支 `agent/deepseek/TASK-045-webtoon-display-fixes`，worktree `G:/CODEX/New Manga.worktrees/TASK-045-deepseek`）。18 文件 +943/−116，生产面仅 3 文件（`+72/−15`）：`src/ui/viewmodels/reader/viewmodel.py`、`src/ui/qml/reader/ReaderView.qml`、`src/infrastructure/imaging/webtoon_tiles.py`；测试 2 文件；证据 13 份。**0 删除、越界 0**（未碰 Schema/依赖/seam/`AGENTS.md`/其他 Task；`src/ui/qml/` 仅 `reader/ReaderView.qml`）。

## 逐条 disposition

| ID | 级别 | 处置 | 实现与证据 |
|---|---|---|---|
| **R-001** | **P1** | **FIXED** | **触发面改在 ViewModel（复审给的两个选项中的第二个）**：`_rebuild_tiles()` 在重建行之后**自行物化**「记住的页像素视口」——`requestTiles` 现在把换算后的 `(top, bottom)` 记入 `_viewport_page`，`_rebuild_tiles` 末尾调用 `_serve_viewport(top, bottom, notify=False)`，随后一次性 `tilesChanged`；无记忆时以 `(0, 0)` 作**页首 bootstrap**（`visible_tiles(0,0)` = tile 0 + prefetch）。`_viewport_for_new_page()` 把记忆窗口**按新页高度钳制**（`top ≤ page_height - span`），因此翻到更短的页也不会得到空窗口。`openChapter` 清空记忆（新章节由 QML 的 offset 恢复去请求真实窗口）。**未加 QML 触发**：瓦片生命周期归 VM，QML 保持"只渲染"；两个触发源会重复物化（见「风险与遗留 ①」） |
| R-001 断言 | — | **FIXED** | 新增 2 个 **QML 级**用例（**不调用 `requestTiles`**）：`test_tiled_webtoon_serves_the_first_page_without_an_explicit_request`、`test_tiled_webtoon_serves_the_new_page_after_a_turn_without_an_explicit_request`；新增 2 个 VM 级用例钉住 bootstrap 与钳制行为。**判别力**：在复审 head `1171bc5`（两棵树）上 **8 failed / 125 passed**，逐条原因见 [revision-pre-fix-failure-summary.txt](../../verification/TASK-045/revision-pre-fix-failure-summary.txt)。**复审自己的探针** `reviewer_probe.py` 复跑（[revision-reviewer-probe.txt](../../verification/TASK-045/revision-reviewer-probe.txt)）：**B1 首个打开 → 2 个 page-1 瓦片文件**（原 `sources=[]`）、**B3 `nextPage()` 后 → 2 个 page-2 瓦片文件**（原 `sources=[]`）、B2/B4 滚动后一致、section A（F-4）仍 `holds: True` |
| R-002 | P2 | **FIXED** | `TiledPageRasterizer` 类 docstring 改为实测口径："升序只保证**不向后跳**；`overlap>0` 时每个非首块的窗口起点落在游标之前 ⇒ 各自 rewind 并从 row 0 重扫 ⇒ 多块视口为 **O(块数²)**，而非一次顺序扫描"（与方法 docstring、测试 docstring、Handoff 关键技术点 5 一致）。`comment_drift_check.py` 增加**守卫**：`src/infrastructure/imaging/**` 内禁止出现 `at most one rewind`，且类 docstring 必须同时提到 rewind 与 overlap（复跑 [comment-drift-check.txt](../../verification/TASK-045/comment-drift-check.txt)，exit 0） |
| R-003 | P3 | **FIXED** | Repeater 绑定改为 `model: tilesHost.visible && rv.model ? rv.model.tiles : []`，并在注释里写明两个未限定名各自的遮蔽后果（`model` → Repeater 自身 model；`visible` → Repeater 自身 visible） |
| R-004 | P3 | **FIXED** | 改为**显式相等校验**：`band_height - offset != tile.content_height` 即抛 `OSError`（可诊断、且 VM 会捕获），不再用 `min(...)` 静默截短——静默截短正是 F-4 要消灭的"图短一截"同症状 |
| R-005 | P3 | **FIXED（文档化）** | `_TILE_CACHE_FORMAT` 注释写明：升版会**遗留上一代瓦片文件**（可重建像素、非业务数据），并由 `TiledPageRasterizer.clear()`（glob `tile-*.png`，跨代一并清除）或人工清目录回收 |

## 断言更新说明（诚实清单，均非放宽）

复审 head **未集成**（`changes_requested`），以下更新都在 R-001 同一条轴上是**加强**：

| 用例 | 旧断言 | 新断言 | 理由 |
|---|---|---|---|
| `test_tiled_reader_serves_viewport_tiles` | 打开后 `url` 全空 | 打开后 **0/1 已服务**（`[row["index"] for row in rows if row["url"]] == [0, 1]`） | 旧断言断言的正是 R-001 认定的缺陷行为 |
| `test_next_page_rebuilds_the_tiles_for_the_new_page` | 换页后 `url` 全空 | 换页后**新页文件已服务**且与第 1 页文件**不相交** | 同上 |
| `test_jump_to_page_rebuilds_the_tiles_too` | 换页后 `url` 全空 | 跳页后**已服务**且与旧页不相交 | 同上 |
| `test_request_tiles_converts_display_pixels_to_page_pixels` | 期望视口序列 3 项 | 序列**增加首项 `(0, 0)`**（打开时 bootstrap） | 记录新的打开期物化 |
| `test_tiled_webtoon_page_turn_drops_the_previous_pages_tiles` | 手动 `requestTiles` 后断言"清空再填" | **替换**为上述两个"不手动请求"用例 | 复审明确要求改成不手动请求的形态 |

## 证据（同一 shell + 同一 venv）

| 场景 | 命令 / 步骤 | 被测 | 结果 |
|---|---|---|---|
| 复审探针复跑 | `TASK045_WORKTREE=<wt> python verification/TASK-045/review-1171bc5/reviewer_probe.py` | `a165aa3` | **B1/B3 已有瓦片**、A 段 F-4 仍 True（[revision-reviewer-probe.txt](../../verification/TASK-045/revision-reviewer-probe.txt)） |
| R-001 判别力（**两棵树**） | `git archive 1171bc5 src pytest.ini` + 本切片 `tests/`，`pytest tests/reading_export tests/core -q -rf` | `1171bc5` src/QML | **8 failed / 125 passed**（[revision-pre-fix-tests.txt](../../verification/TASK-045/revision-pre-fix-tests.txt)、[summary](../../verification/TASK-045/revision-pre-fix-failure-summary.txt)） |
| 同上全仓 | `pytest -q -rs -rf` | 同上 | **8 failed / 822 passed / 6 skipped**，exit 1，**无崩溃**（[revbase-full-rerun-3s.txt](../../verification/TASK-045/revbase-full-rerun-3s.txt)） |
| 定向（交付树） | `pytest tests/reading_export tests/core -q -rf` | `a165aa3` | **133 passed / 0 skipped**，exit 0（[post-fix-reading-core.txt](../../verification/TASK-045/post-fix-reading-core.txt)） |
| 全仓 ×5（+1） | `pytest -q -rs -rf`（run 6 用 `-rs`） | `a165aa3` | **每次 830 passed / 6 skipped，exit 0**（[full-suite-runs.log](../../verification/TASK-045/full-suite-runs.log)）；6 条 skip 全为既有 `tests/network` `openssl unavailable`（`test_connection_tester.py:106`、`test_transport_tls.py:39/47/62/69/83`） |
| 逐目录 | 四套件 | `a165aa3` | reading_export **109**、core **24**、ui_shell **46**、workbench **51**；AC ⑥ 聚合 **230**（[per-directory-counts.txt](../../verification/TASK-045/per-directory-counts.txt)） |
| 基线 | 主仓库 master `c3d9ed2` 全仓 | `c3d9ed2` | **825 passed / 6 skipped**（[baseline-master-c3d9ed2.txt](../../verification/TASK-045/baseline-master-c3d9ed2.txt)） |
| 被审 head 基线（纯树） | `git archive 1171bc5 src tests pytest.ini` 全仓 | `1171bc5` | **827 passed / 6 skipped**，exit 0（[control-pure-1171bc5-full.txt](../../verification/TASK-045/control-pure-1171bc5-full.txt)；与复审自身两次一致） |
| 注释口径 | `python verification/TASK-045/comment_drift_check.py` | `a165aa3` | exit 0（含 R-002 新守卫）（[comment-drift-check.txt](../../verification/TASK-045/comment-drift-check.txt)） |

**计数一致性**：交付 830 = 被审 head 827 + 3（新增 4 例 − 替换 1 例）；基树（1171bc5 + 本切片测试）822 + 8 failed = 830 ✓。

## 附带发现（本轮实测到的环境级不稳定，已加固）

把本切片测试放到**修前**树上时，全仓运行出现两次 **Windows access violation（0xC0000005）**，崩溃点均为 `tests/workbench/test_ui_responsiveness.py::test_gui_thread_stays_alive_during_run`（工作线程 lambda 内），与 TASK-045 面无直接关系。定位过程：

1. 纯 `1171bc5` 树（旧测试）全仓 **827/6 exit 0**；`tests/workbench` 单独在基树上 **51 passed**；"QML 契约 + ui_responsiveness" 最小组合在基树上 **2 failed / 12 passed、无崩溃** ⇒ 需要整套顺序才能复现；
2. 相关性指向**修前树上新用例的失败路径**：4 个用例各自在 QML 窗口存活期间 `pump_traced(5.0)` 空转（基树没有自动服务，必然超时）；
3. **加固**：新用例的失败路径等待预算由 5s 收紧到 **3s**（正常路径是 `openChapter` 内同步完成、<100ms，仍有 ~30× 余量）→ 基树全仓复跑 **8 failed / 822 passed / 6 skipped、无崩溃**（`revbase-full-rerun-3s.txt`），交付树 6 次全仓全绿。

**交付树从未受影响**：加固前后各 6 次全仓运行均 exit 0。

## 风险与遗留

1. **未加 QML 侧触发**（复审给的选项一）：VM 侧物化后，QML 只需重算 `model.tiles` 绑定即可刷新 delegate，再加 `tilesChanged` 连接会造成同一视口二次物化；如复审要求"QML 也必须能自驱"，可在 `tilesHost.onVisibleChanged` 加一次 `requestTiles`，代价是重复调用（幂等、有文件缓存，但会产生额外 rewind）。
2. **`clearTileCache()` 后不自动补服务**（其语义是"丢弃可重建像素"，且当前**无 QML 入口**）；若将来接线为按钮，建议同时调用一次当前视口物化，避免同样的空白带。
3. **AC ⑩ 的解码面建议**（Codex 裁定 ① 已接受"文档化 + 实测"并称将由其在集成时更正 TASK-042 措辞；其附带的 `overlap=0` 小切片仍待其开立）：本切片**未**修改解码策略与生产 `overlap`，只把注释改到与实测一致（R-002）。
4. **仍未做真机截图**（沿用上一轮声明）：F-4 的像素真值断言在瓦片文件上；QML 级断言覆盖 delegate `source` 生命周期（本轮新增"打开即服务/换页自动服务"），未断言屏幕最终栅格。
5. **F-8 的 QML 入口**按裁定 ③ 仍不在本 Task 范围。
6. 未 push；未改 `doc/STATUS.md`（关闭登记待复审通过后由 Codex 执行）。

## 接收方式

- 分支 `agent/deepseek/TASK-045-webtoon-display-fixes`；**未 push**；开工时已 merge master（`c3d9ed2`），因此本次合并点包含复审文件。
- 复现：`$env:PYTHONDONTWRITEBYTECODE='1'`；`$py = "G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe"`；全部 `-p no:cacheprovider`。
  - 复审探针：`$env:TASK045_WORKTREE="G:\CODEX\New Manga.worktrees\TASK-045-deepseek"; & $py verification/TASK-045/review-1171bc5/reviewer_probe.py`（期望 B1/B3 有 sources）
  - 定向：`& $py -m pytest tests/reading_export tests/core -q -rs -rf`（期望 133 passed / 0 skipped）
  - 全仓：`& $py -m pytest -q -rs`（期望 830 passed / 6 skipped）
  - 两棵树判别力：`git archive 1171bc5 src pytest.ini | tar -x -C <tmp>` + 复制 `tests/` + `pytest tests/reading_export tests/core -q -rf`（期望 8 failed / 125 passed）
- Reviewer 建议优先核对：①**用你自己的 section B 探针**确认 B1/B3（我复跑的原文已入库）；②两个新 QML 用例是否确实**不**调用 `requestTiles`；③`_viewport_for_new_page` 的钳制是否覆盖"新页更短"这一情形（VM 用例 + QML 用例各有一处）；④是否接受"触发面放在 VM"这一选项（而非 QML）。
