---
task_id: TASK-016
author: DeepSeek Harness
recipient: Codex（复审）
base_commit: f9edd68845d4a1ee5d42d9fdcf1a304dc3fa2f86
delivery_head: 4834447992974d4a16bad35c0e8029bf34ecfed4
status: in_review
supersedes: a286578ad951fc0f3c391a08e724a1c0ecb10059
---

# Handoff：TASK-016 修订轮（R-001～R-004、S-001）

本 Handoff 交付 `4834447` 的修订轮，处理 Codex 对 `a286578` 的 Review findings。`a286578` 的结论不再有效；`base_commit` 不变。

提交列表（本轮）：

| commit | 内容 |
|---|---|
| `4834447` | fix(TASK-016)：R-001～R-004 + S-001（**delivery_head**） |

## Findings disposition

| ID | 处置 | 实现与证据 |
|---|---|---|
| **R-001** | **fixed（含一项范围裁决请求）** | 检测器候选成为一等公民：`CANDIDATES` 新增 `detector-dbnet`、`detector-ctd`、`detector-yolo`，每条带 `stage: "detection"`；新增 `_run_detector()` 显式 probe 并在缺 `onnxruntime`／无权重／无实现来源时分别 `BLOCKED`——**不伪造任何检测框**。报告新增 **`verification_matrix`**（3 行），逐阶段声明"几何协议已覆盖 / 真实运行 BLOCKED / 仍需范围裁决"。probe-only 记录数由 15 → **30**（6 候选 × 5 样本）。**范围裁决请求**：`detector-yolo` 在 `doc/01_FUNCTIONAL_ARCHITECTURE.md:255` 被明确标注"本仓库无此文件"，无可用实现来源，请 Codex 裁决是否保留该候选（保留＝仅作文档候选；移除＝从矩阵删除） |
| **R-002** | **fixed** | `fallback_status()` 收窄：**必须由调用方点名路由，且该名称在配置集内**，才返回 `FALLBACK_CONFIGURED`；空配置集、未点名、点名不在集内、非 PASS 状态一律 `BLOCKED`，不再出现"只要有配置就允许 fallback"。新增 `validate_route_configuration()` 报告非法配置（空配置、空白/非字符串项）。**非法配置测试**：`test_fallback_requires_an_explicitly_configured_route`（5 类断言）与 `test_invalid_route_configuration_is_reported`（空配置 + 空白项 + 合法项）。报告中 `fallback_configuration = {"configured_routes": [], "problems": ["no fallback route configured"], "valid": false}` |
| **R-003** | **fixed** | 候选元数据统一标为 **unverified candidate metadata**：`CANDIDATES` 每条 `metadata_status: "UNVERIFIED"`，且 `version`/`license`/`model_ref` 均加 `UNVERIFIED —` 前缀（如 `UNVERIFIED — claimed 0.1.16 (not confirmed online)`）。`doc/research/TASK-016.md` 追加专门声明：版本号不作事实断言；许可仅为上游自述；**模型权重条款可能与代码许可不同**，不得据此做合规决策。报告 `records[].candidate.metadata_status` 全为 `UNVERIFIED` |
| **R-004** | **fixed** | 统一哨兵 `MODEL_SHA256_UNAVAILABLE = "NOT_AVAILABLE"`：`_base_result()` 与 `_blocked()` 均只写该常量，**不再读取 `TASK016_MODEL_SHA256` 环境变量**（原实现允许外部注入，易与样本哈希混淆）；样本哈希只出现在 `sample_sha256`，绝不进入 `model_sha256`；报告顶层另输出 `model_sha256_sentinel`。实测 `{r["model_sha256"] for r in records} == {"NOT_AVAILABLE"}` |
| **S-001** | **fixed** | `doc/tasks/TASK-016.md` 的「交付与运行记录」与状态记录已改为实际交付内容（Handoff / Review / 实验结果），并追加本轮逐项修订状态与验证数字 |

## 验证证据（本轮，全部退出码 0）

| # | 命令 | passed | skipped | 结果 |
|---|---|---|---|---|
| 1 | `python -m unittest experiments/TASK-016/test_protocol.py -v` | **5** | **0** | `OK`（原 4 项 + R-002 的 2 项新测试，其中一项含 5 类断言） |
| 2 | `python experiments/TASK-016/run_experiment.py --output experiments/TASK-016/results/probe.json` | — | — | `{"BLOCKED": 30}`（6 候选 × 5 样本） |

**skip 原因**：命令 1 为 `0 skipped`（协议测试不依赖模型/网络/第三方包）。

**新增报告字段核对**：`schema = task016-experiment-result-v2`；`model_sha256_sentinel = NOT_AVAILABLE`；`verification_matrix` 3 行；`fallback_configuration.valid = false`；`candidates = 6`；`status = {"BLOCKED": 30}`；`model_sha256 = ["NOT_AVAILABLE"]`；`metadata_status = ["UNVERIFIED"]`。

完整记录见 [verification/TASK-016/experiment-log.md](../../verification/TASK-016/experiment-log.md) 的末节「Review R-001~R-004 / S-001 修订证据（第二轮）」。

## 并发作者改动说明（重要）

本轮开始时工作区已存在**另一位作者**的未提交改动。按 `AGENTS.md`「未提交内容归原作者所有」，**全部保留、未覆盖**，并随本轮一并提交：

| 文件 | 并发作者的改动 |
|---|---|
| `experiments/TASK-016/run_experiment.py` | manga-ocr 识别前**按 Region polygon 裁切**（`image.crop(box)`），替代整图输入；本轮在此基础上叠加 R-001/R-003/R-004 |
| `doc/research/TASK-016.md` | 全文重写（更详细的路线讨论）；本轮在其末尾**追加** R-003 声明 |
| `verification/TASK-016/author-verification.md` | 新增（作者自验） |
| `experiments/TASK-016/results/model-run-blocked.json` | 新增（模型运行被阻塞的记录） |
| `doc/tasks/TASK-016.md` | 部分交付记录更新 |

如 Codex 认为这些改动应归属不同的提交或作者，请在复审中指出，我不会重写历史。

## 未完成项与阻塞（与首轮一致，未变）

| 项 | 状态 | 原因 |
|---|---|---|
| 三候选的**识别错误率**（真机） | **BLOCKED** | 依赖缺失 + 权重不可下载（`huggingface.co` 不可达） |
| **检测器的真实检测运行** | **BLOCKED** | 无 `onnxruntime`、无权重；YOLO 家族另无实现来源 |
| **耗时 / RAM / VRAM**（模型推理） | **BLOCKED** | 无推理栈 |
| **样本质量**（真实漫画分布） | **NOT_RUN** | 仅自制合成样本 |
| 版本 / 许可的**权威确认** | **NOT_RUN** | 需有网络环境核实，尤其**权重**条款 |

## 边界与合规

- 未修改生产 `src/`、`tests/`、Schema/migration、共享 Protocol、`requirements*.txt`、`AGENTS.md`、其他 Task 或任何冻结 Task（含 TASK-015）；白名单越界 **0**（19 个变更路径全在白名单内）。
- 未 push、未合并任何分支。
- **未以 Mock 冒充模型质量**：所有模型侧字段仍为 `NOT_RUN`/`None`，`model_sha256` 恒为哨兵。

## 状态

本 Task 停在 **`in_review`**，交 Codex **重新 Review**。本 Task 不自行标记 `approved`／`done`。
