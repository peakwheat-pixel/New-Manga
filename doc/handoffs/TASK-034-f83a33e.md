---
task_id: TASK-034
author: DeepSeek Harness
recipient: Codex（裁决 AC ① + 独立 Review；集成由 Codex 执行）
base_commit: b34b27e2bc7c1dbd4c3b15a91f08e158d966f365
delivery_head: f83a33e
status: in_review
---

# Handoff：TASK-034 测试与分层硬化（T-2 / T-4 / flaky 诊断 / TASK-035 R-02）

## 交付结果

**范围**：AC ②③④⑤ 完成；AC ① 只交付**第 1 步（裁决请求）**，第 2 步按 Task 要求**冻结**（`src/` 零改动）。

**提交**（分支 `agent/deepseek/TASK-034-test-layer-hardening`，工作区 `G:/CODEX/New Manga.worktrees/TASK-034-deepseek`；固定 base `b34b27e`，分支起点 `c94c183` = 释放纯文档提交）：

| commit | 内容 |
|---|---|
| `cff86e4` | 开工：`doc/tasks/TASK-034.md` 置 `in_progress` |
| `f83a33e` | AC ②③④ + AC ① 第 1 步取证（17 文件、`+704/−193`）：`tests/core/test_architecture.py`、`tests/providers/**`、`tests/reading_export/**`、`verification/TASK-034/**` |

`delivery_head = f83a33e`。**未 push、未合并 master**。

**逐 AC 结果**：

| AC | 状态 | 摘要 |
|---|---|---|
| ① T-2 `route_policy` 语义唯一 | **第 1 步交付 / 第 2 步 BLOCKED（待裁决）** | 裁决请求 [`verification/TASK-034/route-policy-decision-request.md`](../../verification/TASK-034/route-policy-decision-request.md)：列出两处解析的**全部 12 类可观测分歧**（非 mapping、键缺失、`color_route` 缺省/空白、`allowed_routes` 缺省/为空/为字符串、`requirements` 缺省/非 bool、`fallback_routes`、以及"常量 vs 注入默认"），提出唯一规范解释（R-1～R-6，其中 R-6 给出 A/B 两案并推荐 **Option B：默认不指定彩色路线**）与迁移影响 + `src/` 三文件范围变更申请。**裁决前 `src/` 未改，两处调用点行为未变**（只读探针证据 [`route-policy-divergences.txt`](../../verification/TASK-034/route-policy-divergences.txt)） |
| ② T-4 守卫加固 | **完成** | 迁入 `tests/core/test_architecture.py`，复用并扩展 AST 版 `find_forbidden_imports`（新增字面量 `importlib.import_module`/`__import__` 检测）；`tests/providers` 旧行前缀守卫**删除**（不留两套）。判别力：现树 0；历史树 `726baf5` **2**（`step.py:23,29`）；合成动态导入 **新 1 / 旧 0** |
| ③ flaky 有界诊断 | **完成（未复现，机制已锁定）** | 两个已登记用例均加有界诊断，**断言与等待预算未放宽、未新增 skip**：webtoon 用例引入 `pump_traced` + `webtoon_save_diagnostics`（迭代/耗时/`contentY`/服务端 offset/`Image.status`/对象名），首处对象等待改为同断言的有界等待；导出用例把"等 `not vm.running`"改为**等终态信号**并附状态轨迹，另加确定性"半发布窗口"判别测试。全仓 **12 次串跑（诊断前 6 + 后 6）0 失败** → 如实登记"未复现"，并给出机制根因（N-1） |
| ④ TASK-035 R-02 | **完成** | 共享替身移入唯一命名 `tests/providers/providers_helpers.py`，显式导入；`pytest tests/providers tests/editing` 与反向顺序**均 0 collection errors、各 136 passed**（基线顺序 A 为 **4 collection errors**） |
| ⑤ 回归与分列 | **完成** | mandated 三套件 **193 passed / 0 skipped**；全仓 **682 passed / 6 skipped ×6 次**（逐次退出码 0）；`tests/providers` −1（守卫迁出）、`tests/core` +3、`tests/reading_export` +1，净 +3 |
| ⑥ 独立 Review 与集成 | **待办** | 本 Handoff + 取证；Review 由 Codex 按四轴执行 |

## 验证证据

环境：Windows 11 `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2 / pytest 9.1.1）；`PYTHONDONTWRITEBYTECODE=1`；全部 `-p no:cacheprovider`。完整记录：[`verification/TASK-034/author-verification.md`](../../verification/TASK-034/author-verification.md)。

| # | 场景 | 命令 / 步骤 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | mandated 三套件 | `pytest tests/providers tests/core tests/reading_export -q -rs` | **193 passed / 0 skipped**（退出码 0） | `collection-orders.txt` |
| 2 | AC ④ 两种顺序 | `pytest tests/providers tests/editing` / 反向 | **136 / 136 passed**，0 collection errors（基线顺序 A = 4 errors） | `collection-orders.txt`、`collection-orders-before.txt` |
| 3 | 回归 | `pytest tests/core tests/storage tests/providers -q` | **161 passed** | `collection-orders.txt` |
| 4 | AC ② 判别力 | 自写只读探针（新 AST vs 旧行前缀，三个候选根） | 现树 0/0；`726baf5` 2/2；合成动态导入 **1/0** | `guard-discriminative.txt` |
| 5 | AC ③ 前后对照 | 全仓 ×6（诊断前）/ ×6（诊断后） | **681×6 / 682×6，全部 exit=0，6 skipped 均为既有 `openssl unavailable`** | `flaky-repro-before.log`、`flaky-repro-after.log` |
| 6 | 逐目录计数 | `--collect-only` + 实跑，基线 `b34b27e` 导出树 vs HEAD | providers 111→110、core 15→18、reading_export 64→65、editing/storage 不变；全仓 679→682 | `test-counts.txt` |
| 7 | AC ① 分歧 | 只读探针调用两处现有 `_route_policy`（12 例 + 注入默认例） | 6 例 SAME / 6 例 DIVERGES，逐例给出两侧结果 | `route-policy-divergences.txt` |
| 8 | 边界 | `git diff --name-only c94c183..HEAD` + 允许范围正则；`git diff --check b34b27e..HEAD` | Owner 改动全在允许范围、越界 **0**；**`src/` 零改动**；`--check` 退出码 0 | `changed-paths.txt` |

**skip 原因**：全部 6 条均为既有 `tests/network` 的 `openssl unavailable`（`test_connection_tester.py:106`、`test_transport_tls.py:39/47/62/69/83`）。**未新增 skip**。

## 需要 Codex 裁决 / 决定的事项

1. **AC ① 第 2 步（阻塞项）**：按 [`route-policy-decision-request.md`](../../verification/TASK-034/route-policy-decision-request.md) §7 回填 R-1～R-7 与 `src/` 范围变更批准。收到裁决前 AC ① 保持 BLOCKED，`src/` 保持冻结。
2. **N-1（生产行为）**：`src/ui/viewmodels/export/viewmodel.py:380/389`、`:392/398` 的"先清 `running`、后发布状态/信号"顺序是导出用例 flaky 的机制根因；本 Task 只在测试侧改为等终态信号。是否另立切片修正生产侧顺序，请 Codex 决定。
3. **N-2（flaky 未复现）**：webtoon 保存 flaky 12 次串跑未触发，真实原因未定（三条候选见取证 §2.1/§5 N-2）；已交付轨迹，等待下次复现。
4. **`tests/providers` −1 的口径确认**：AC ⑤ 要求"passed 不减少"，而 `tests/providers` 由 111 降至 110——原因是 AC ② 把守卫**迁出**到 `tests/core`（该目录 +3，含旧守卫的等价断言与 2 条新增动态覆盖）。**未删除任何断言语义、总 passed +3**；如需保持该目录计数不减，请指示（可保留一个转发到共享实现的薄用例，但会与"不得留下两套"的取向有张力）。

## 接收方式

1. **分支/工作区**：`agent/deepseek/TASK-034-test-layer-hardening` @ `f83a33e`；`G:/CODEX/New Manga.worktrees/TASK-034-deepseek`。固定 base `b34b27e`。
2. **复现命令**：见取证 §6（PowerShell 片段，含 AC ④ 两种顺序与全仓）。
3. **不需要**额外数据、依赖、端点或权重；渲染/阅读套件按产品边界使用默认 Windows 平台（需字体库）。
4. **Reviewer 重点检查**：
   - AC ② 的 AST 扩展是否**未改变**既有 4 条单测的断言语义（本地已全绿）、旧守卫是否确无第二份；
   - AC ③ 的"等待代理量 → 等待断言所关心的终态信号"是否可被接受为**不放宽断言**（本 Handoff 明确列出该口径，便于 Review 挑战）；
   - AC ④ 的 helper 命名与导入方式是否可推广到其它套件（`tests/workbench`、`tests/editing` 亦有同名 helper 风险）；
   - AC ① 裁决请求中的 R-6 选项与本 Task 的"不改行为"边界。

## 风险与遗留

- **AC ① 未完成**（第 2 步）：本 Task 的交付在 AC ① 上只到"裁决请求"，集成时请把该 AC 保持 open 或按裁决结果另立实施切片；**不可**把本次交付记为 AC ① 达成。
- **flaky 未根除**：12 次未复现不等于已修复；诊断已就位（失败即输出轨迹），但若根因在生产侧（N-1）或 QML 侧（N-2），需另立含 `src/` 范围的切片。
- 回退：本 Task 只改 `tests/**` 与文档，`git revert f83a33e cff86e4`（或丢弃分支）即回到 `679 passed` 基线；无 Schema/依赖/数据影响。
