---
task_id: TASK-005
author: Codex
recipient: DeepSeek Harness
base_commit: d65901b953e6fb26043344ed3d668520847eb295
delivery_head: e914f39a0c721f717c45a01229cd6d0b82f0045e
status: ready_for_review
---

# Handoff：TASK-005

## 交付结果

本轮仅建立最小工程入口与架构守卫，没有实现业务功能：

- 精确锁定 Core/开发依赖；Core 只含 PySide6 Essentials，不含 Addons 或重型 AI Runtime；
- 新增 `python -m bootstrap.app` 与 `--smoke-test`，真实加载 `src/ui/qml/Main.qml`；
- QML 缺失时返回 1，并向 stderr 输出计算出的绝对路径；
- 新增 stdlib AST 守卫：Domain 禁止 Qt/SQLite/ML import，UI 禁止直接 import `infrastructure`；
- 新增 5 个 core 测试、统一 PowerShell 验证入口和作者证据。

固定提交列表：

```text
fb7f1a3d717cb5a72bdebd455e1132b234618673 docs(TASK-005): start implementation plan
728c0f957b39551316aadb4b2ae61a8456794cd5 build(TASK-005): lock minimal core dependencies
a4cdce4b9e5e5f4cf79ee7a404baa8d16c6e06a5 test(TASK-005): enforce minimal architecture boundaries
b318bdd5782681df6ff5398b80f8c597366c5b0b feat(TASK-005): add minimal QML bootstrap
e914f39a0c721f717c45a01229cd6d0b82f0045e test(TASK-005): add reproducible bootstrap verification
```

`d65901b..e914f39` 共 14 个变更路径，全部位于 TASK-005 白名单；TASK-006～TASK-027 零变化。

Acceptance Criteria 对照：

1. **依赖与入口：PASS**。`requirements.txt`/`requirements-dev.txt` 是唯一精确锁定方式，Python 3.12.3 安装与 `pip check` 成功。
2. **Core 可选依赖隔离：PASS**。独立 venv 未安装 torch/transformers/onnxruntime，真实 QML smoke 退出 0；没有批量空目录。
3. **架构守卫：PASS**。真实源码扫描无违规，Domain/UI 合成违规均报告文件、行号与模块。
4. **交接与独立审查：进行中**。Handoff 与作者证据已交付；DeepSeek Review、Codex 集成验证尚未发生，因此 Task 未标 done。

## 验证证据

| AC/场景 | 实际命令/步骤 | 环境与被测 commit | 结果 | 日志/产物 |
|---|---|---|---|---|
| 固定 head 全量守卫 | `pwsh -NoProfile -File ./verification/TASK-005/verify.ps1 -BaseCommit d65901b953e6fb26043344ed3d668520847eb295 -ReviewedHead e914f39a0c721f717c45a01229cd6d0b82f0045e -PythonExe 'G:/CODEX/New Manga.task-envs/TASK-005-py312/Scripts/python.exe'` | Windows 10.0.26200.0；Python 3.12.3；Qt/PySide6 6.11.2；`e914f39` | PASS，退出码 0 | [作者验证](../../verification/TASK-005/author-verification.md) |
| Core tests | 上述脚本内 `python -m pytest tests/core` | 同上 | PASS，5 passed | `tests/core/` |
| QML smoke | 上述脚本内 `python -m bootstrap.app --smoke-test`，offscreen | 同上 | PASS，退出码 0 | `src/ui/qml/Main.qml` |
| 缺失 QML | pytest 复制 bootstrap 到无 QML 的临时 `src` 后启动 | 同上 | PASS，非零且 stderr 含绝对路径 | `tests/core/test_bootstrap.py` |
| 架构违规 | pytest 合成 Domain/UI 违规 import | 同上 | PASS，两个违规均被捕获 | `tests/core/test_architecture.py` |
| PyInstaller / 干净 Windows | 未执行 | 本 Task 禁止打包配置且无干净 VM | NOT_RUN | TASK-004 证据不替代本 head |
| SQL / Provider / 模型质量 | 未执行 | 本 Task 无这些实现 | N/A | — |
| 性能 / 内存 / 容量 | 未执行 | 无获批阈值或业务实现 | NOT_RUN | — |

TDD 的两轮 RED 与 GREEN 逐项记录在 [作者验证](../../verification/TASK-005/author-verification.md)。固定 reviewed head 的最终脚本输出为：14 个 allowed paths、精确依赖 PASS、重型包缺失 PASS、5 tests PASS、真实 QML smoke PASS、8 个链接/围栏 PASS、`git diff --check` PASS。

## 接收方式

- Owner 分支：`agent/codex/TASK-005-minimal-bootstrap`
- Owner worktree：`G:/CODEX/New Manga.worktrees/TASK-005-codex`
- 固定 Review 对象：`d65901b953e6fb26043344ed3d668520847eb295..e914f39a0c721f717c45a01229cd6d0b82f0045e`
- Reviewer 只写 `doc/reviews/TASK-005-e914f39.md`，不得修改上述被审文件或实现修复。
- Reviewer 应使用自己的 Python 3.12 venv安装 `requirements-dev.txt`，并以自己的绝对解释器路径复跑同一 `verify.ps1`。

本地 smoke：

```powershell
$env:PYTHONPATH = (Resolve-Path 'src').Path
$env:QT_QPA_PLATFORM = 'offscreen'
& 'G:/CODEX/New Manga.task-envs/TASK-005-py312/Scripts/python.exe' -m bootstrap.app --smoke-test
```

## 风险与遗留

- 干净 Windows VM、PyInstaller、正常交互式人工窗口与性能均未在本 Task 验证；不影响最小入口 Review，但仍是后续发布 Gate。
- 当前 QML 只有标题窗口；四个一级页面、ViewModel、Repository、Provider、数据库和业务流程均未实现，也未获本 Task 授权。
- 架构守卫按当前 `src` 下的顶层包布局检查根 import；若后续获批引入统一外层包名，应在对应 Task 同步守卫与测试。
- 回退本交付只需不集成该分支；没有迁移、生产数据或用户文件写入。
