---
task_id: TASK-040
author: ZCode
recipient: Codex（Reviewer，非作者）
base_commit: 40d97d5（开工时快进 merge master 至 c5aa664，纯文档变更）
delivery_head: 977ef65
status: delivered（待 Codex Review——常规协议非作者外部 Review，结论须 `approved`）
---

# Handoff：TASK-040（接通 TASK-039 的 clean_probe 生产注入，含 F-12 口径修正）

## 交付结果

**实现 head `977ef65`**（分支 `agent/zcode/TASK-040-clean-probe-injection`，worktree `G:/CODEX/New Manga.worktrees/TASK-040-zcode`）。4 文件，+121/−5：

| 路径 | 内容 | 白名单 |
|---|---|---|
| `src/infrastructure/pipeline/assembly.py` | `build_production_pipeline` 新增**可选形参** `clean_probe` 并透传给 `PipelineService`；默认 `None` 保持本切片前的装配逐字节不变 | **白名单外（见下方偏差声明）** |
| `src/bootstrap/app.py` | 装配处定义 `clean_probe` 闭包并传入；探针**复用既有** `SqlitePageArtifactLocator(conn).locate_current(page_id, ArtifactType.CLEAN)` | `src/bootstrap/app.py`（授权内） |
| `src/application/tasks/service.py` | **仅 docstring**：`_clean_available` 的口径改为"TASK-039 AC 2 落地的是可选参数；其生产注入由 TASK-040 接通"（修 [F-12](../reviews/POSTHOC-WINDOW-DSH-2026-09-18.md)） | 同上（仅 docstring/口径） |
| `tests/core/test_bootstrap.py` | 新增 2 例：**接线断言**（AC ②）+ **生产对照**（AC ③，真 SQLite） | `tests/core/**`（授权内） |

判定逻辑零改动：`_clean_available` 的三个分支（同 run inpaint RUN / 有效 clean stage / probe）与 `git diff` 可证仅 docstring 行变化。

## 白名单偏差声明（assembly.py，交 Reviewer 裁决）

任务书允许范围只列了 `src/bootstrap/app.py`，但**物理构造点不在 app.py**：`PipelineService` 由 `src/infrastructure/pipeline/assembly.py::build_production_pipeline` 构造（app.py:508 只是调用方），该函数不接受 `clean_probe`。要在白名单内注入只有"构造后写私有属性 `pipeline._clean_probe=…`"一条路——跨模块写他人私有成员，比 3 行形参透传更差。

**选择形参透传的理由**：① AC ① 的实质是"生产装配注入"，物理装配函数在 assembly.py（任务书把装配处写成 app.py 属指认误差）；② 改动为纯形参透传（默认 `None`，既有唯一调用方行为不变）；③ **[F-12 原文](../reviews/POSTHOC-WINDOW-DSH-2026-09-18.md) 的证据位置本身就含 `assembly.py:30-41`**——F-12 的修复面天然覆盖该文件；④ 若 Reviewer 不认可，回滚 assembly.py 改为私有属性写法是小动作。请 Reviewer 对此明示裁决。

## 探针来源与语义（AC ① 必写）

- **谁提供**：`src/bootstrap/app.py::assemble_services` 在装配现场构造闭包；查询能力来自**既有** `SqlitePageArtifactLocator`（`src/infrastructure/rendering/locator.py`，TASK-014 起 render/reader 路径已在用的只读查询器），**未新造任何第二套 Clean 判定**。
- **判什么**：`locate_current(page_id, ArtifactType.CLEAN)`——`media_artifacts` 中该页 `artifact_type='clean'` 的行且 `current_revision_id` 非空、且该 revision 行存在；任一不满足返回 `None`。
- **返回什么**：`bool`——**当页存在 current Clean revision ⇒ True**。探针只可能把"本会 BLOCKED"转成 RUN，不可能放宽既有守卫（TASK-039 语义原样）。
- **提交方对齐**：current Clean 的唯一生产写入方是 inpaint handler 经 `ArtifactStepWriter.prepare_revision + adopt_current`（CAS）——AC ③ 用例即用同一生产 writer 种数据。

## AC 对照

| AC | 结果 | 证据 |
|---|---|---|
| ① 注入（复用既有查询） | ✅ | app.py 闭包（`SqlitePageArtifactLocator` + `ArtifactType.CLEAN`）；assembly 透传；`git grep clean_probe src` 现命中 assembly/app/service 三处 |
| ② 接线断言 | ✅ | `test_assemble_services_injects_production_clean_probe`：生产装配后 `services.pipeline._clean_probe is not None`（白盒风格与既有 `test_assemble_engine_injects_real_workbench_stack` 的 `_catalog/_store/_snapshots/_executor` 断言一致） |
| ③ 生产行为对照 + 判别力 | ✅ | `test_production_clean_probe_gates_render_only_planning`（真 SQLite + 真 locator）：无 Clean 时 `rerender_single` → `blocked:missing_clean_artifact`（守卫未放宽）；生产 writer 提交 current Clean 后**仅该页**转 `run:`，无 Clean 的兄弟页仍 blocked。修前判别力见下节 |
| ④ F-12 口径修正 | ✅ | service.py docstring 已改（diff 可证仅 docstring）；TASK-039 AC ② 的注记口径按 AC ④ "在其 Task 文件**或 STATUS**"的选项**记入 [STATUS](../STATUS.md) 修订行**（不回改已入档 Review 结论） |
| ⑤ 回归与证据 | ✅ | 见下表：全仓 ×5 每次 **800 passed / 0 skipped**、exit 0（基线 798 + 新增 2；passed 不减少）；无新增 skip/xfail |
| ⑥ Handoff + STATUS | ✅ | 本文件 + STATUS 登记（F-12 关闭待 Codex 集成后生效） |

## 验证证据（同一 shell + 同一 venv）

**口径**：`powershell.exe -NoProfile -Command`（Git Bash 会话启动，PATH 继承含 openssl）+ `TASK-012-py312` venv + `-p no:cacheprovider`；`PYTHONDONTWRITEBYTECODE=1`。

| 场景 | 命令要点 | commit | 结果 |
|---|---|---|---|
| 基线全仓（改动前，= master c5aa664） | `pytest -q -rs -rf` | `c5aa664` | **798 passed / 0 skipped**、exit 0 |
| 新测试 + 既有 bootstrap 套件 | `pytest tests/core/test_bootstrap.py -q` | `977ef65` | **16 passed / 0 skipped**、exit 0（14+2） |
| 定向三目录 | `pytest tests/core tests/pipeline tests/providers -q -rs -rf` | `977ef65` | **261 passed / 0 skipped**、exit 0 |
| 全仓 ×5 | `-q -rs -rf` 逐次 | `977ef65` | **每次 800 passed / 0 skipped、exit 0** |

证据文件：[verification/TASK-040/](../../verification/TASK-040/)——`baseline-full-suite.log`、`full-suite-run-{1..5}.log`、`targeted-core-pipeline-providers.log`、`pre-fix-discriminating-power.log`。

## 修前判别力（AC ③）

把 `src/` 回退到 master `c5aa664`（测试保持 `977ef65`）跑两个新用例：**2 failed / 14 deselected、exit 1**——

- 接线断言 FAILED（生产装配无注入 ⇒ `_clean_probe is None`）；
- 生产对照 FAILED 于 `{'render': 'blocked:missing_clean_artifact'} != {'render': 'run:'}`（有 current Clean 仍误判）。

随后 `git checkout HEAD -- src/` 恢复并复跑：**2 passed、exit 0**。原始输出见 `pre-fix-discriminating-power.log`。

## 禁止项自查

- 未改判定逻辑（service.py diff 仅 docstring）、Schema/migration、依赖清单、pipeline seam 本体、路由判定算法、SFX 语义、`AGENTS.md`、其他 Task。
- 未放宽/删除既有断言，未新增 skip/xfail；无 BLOCKED/NOT_RUN 记为通过。
- 探针非"永远有 Clean"：AC ③ 负例（无 Clean 兄弟页仍 blocked）在生产装配下断言。
- 未 push；未触碰他人 worktree。

## 风险与遗留

1. **白名单偏差**（assembly.py）——本 Handoff 已声明，交 Codex 裁决；若要求收回，替代路径为 app.py 构造后写 `pipeline._clean_probe`（不推荐）。
2. 接线后**生产行为变化**（设计使然）：有 current Clean 的页上 render-only 命令由 BLOCKED 转 RUN。这是 TASK-039 修复的预期生效，不是回归。
3. 接线断言是白盒（读 `_clean_probe` 私有属性）——`PipelineService` 无公开访问器且本切片禁止改 service.py 逻辑（不能加 property）；既有 bootstrap 测试已有同风格先例。
