---
task_id: TASK-003
author: DeepSeek Harness
recipient: Codex
base_commit: 9472df5c44a1bad24cda639201b0824a9ea5eecc
delivery_head: 9c6a73b8f87248d65e9b3865ef4d087b2c2fb21f
status: awaiting_review
supersedes: doc/handoffs/TASK-003-f620c69.md
---

# Handoff：TASK-003 第三次复审修订（R-015～R-018）

交付日期：2026-09-14（Asia/Shanghai）。本轮只处理 Codex 第三次 Review（commit `84795fb`，`doc/reviews/TASK-003-f620c69.md`，decision=`changes_requested`，P1×1 / P2×3）的 R-015～R-018；未实现功能、未改产品要求、未合并 master、未 push。Owner 为 DeepSeek Harness，Reviewer 为 Codex——**本 Handoff 不是自我批准**。

## 固定审查对象

- `base_commit = 9472df5c44a1bad24cda639201b0824a9ea5eecc`
- `reviewed_head = 9c6a73b8f87248d65e9b3865ef4d087b2c2fb21f`（**取代** `f620c69`）
- 被拒 head：`588383f`、`6caefe1`、`f620c69`；第三次 Review 报告：commit `84795fb` 的 `doc/reviews/TASK-003-f620c69.md`（位于 Reviewer 分支，不在本分支）
- 作者分支：`agent/deepseek/TASK-003-verification-spec`；工作区 `G:/CODEX/New Manga.worktrees/TASK-003-deepseek`

## 提交链

~~~text
9472df5 docs: authorize TASK-003 verification design          （基线）
588383f / 6caefe1 / f620c69                                    （前三次交付，分别被 R-001~R-010、R-011~R-014、R-015~R-018 拒绝）
9c6a73b docs(TASK-003): address re-review findings R-015..R-018 （本次内容交付 = reviewed_head）
~~~

本轮内容改动 5 个文件（`+40/−15`）：Fixture 清单、验收规范、TASK-003 正文、上一轮 Handoff 措辞、验证脚本。D02/D04/D11 的 F-08 状态边**未再触碰**（仍为 3 新增 / 0 删除）。

## Finding 处置

| Finding | 级别 | 处置 | 证据 |
|---|---|---|---|
| R-015 | P1 | **fixed** | Manifest §2 改为**明确 allowlist**：只接受 `generated / 自制（项目生成）`、`acquired / CC0`、`acquired / 用户授权` 三种取值；未知来源、明确未授权、`待定`、空值与自造类型（如 `pirated / unknown`）一律拒绝。脚本对**所有**库存行（不只 `available`）校验 allowlist。生成方式要求升级为：`generated` 必须同时给出**脚本路径与参数**（正则要求脚本扩展名 + 参数/seed 标记），`acquired` 必须同时给出**取得途径、版本与授权依据** |
| R-016 | P2 | **fixed** | 脚本新增固定 **20-ID 集合**（14 个 `FX-*` + 6 个 `DS-*`）：先拒绝重复 ID，再与冻结集合做完整比较；重复一个 ID 并删除另一个不再可能通过 |
| R-017 | P2 | **fixed** | `doc/tasks/TASK-003.md` 正文由"当前为 `ready`，Owner …改为 `in_progress`"改为"当前状态以 frontmatter 为准（`in_review`，等待 Codex 独立 Review）"，不再复制会过期的流程描述 |
| R-018 | P2 | **fixed** | `MANIFEST.md` 的"后续已授权 Task"改为"后续**获授权** Task"；同时修正规范 §10 与上一轮 Handoff 中的同义残留（`git grep '已授权'` 在 allowed_paths 内已 0 命中） |

## 验证证据

环境：Windows `10.0.26200`、PowerShell `7.6.6`、Git `2.52.0.windows.1`。

| 检查 | 命令 | 环境 / commit | 结果 |
|---|---|---|---|
| 交付验证脚本 | `pwsh -NoProfile -File ./verification/TASK-003/verify.ps1` | `9c6a73b`（干净工作区） | **PASS，退出码 0**，12 条输出 |
| 空白错误 | `git diff --check 9472df5 9c6a73b --` | 同上 | PASS，退出码 0 |
| 变更统计 | `git diff --stat 9472df5 9c6a73b` | 同上 | 11 路径，`+1122/−34`（含历次交付与元数据） |
| 工作区洁净 | `git status --short --branch` | 同上 | PASS，仅分支行 |
| F-08 增量 | `git diff 9472df5 9c6a73b -- D02 D04 D11` | 同上 | 3 新增、**0 删除** |
| 旧措辞残留 | `git grep -n '已授权'`（allowed_paths 内） | 同上 | 0 命中 |

脚本实际输出（12 条）：

~~~text
PASS: changed paths authorized; TASK-001/002 unchanged; other 24 tasks frozen
PASS: D08 unchanged; all 185 AC IDs, priorities and titles intact and unique
PASS: 24 unique ACG identifiers (10 topic + 7 global + 7 extension), rows unique in both tables
PASS: every ACG row carries exactly one legal result enum value
PASS: method spec keeps no result copy; result truth stays in D13 / commit-bound reports
PASS: fixture manifest fields/states, frozen 20-ID set, source allowlist, hash and method discipline (0 available)
PASS: AC-CAP-001..004 each route to an existing dataset (DS-E/DS-B2/DS-C/DS-D)
PASS: ACG-SMOKE documents the full D08 §53 flow including restart persistence
PASS: F-08 is exactly one added edge per file in D02/D04/D11, with no deletions
PASS: result enum, full performance protocol, UNAPPROVED_THRESHOLD and scope discipline intact
PASS: 532 local links and all code fences valid
PASS: git diff --check; owner-authored guards only, independent Review still required
~~~

### 负向检查（等价逻辑的合成输入，未修改被审文件）

| 场景 | 期望 | 实测 |
|---|---|---|
| 基线（合规 generated available + missing） | 接受 | `ACCEPT` ✓ |
| 未知来源 `unknown / x` | 拒绝 | `REJECT: source not allowlisted` ✓ |
| 未授权来源 `acquired / 未授权` | 拒绝 | `REJECT: source not allowlisted` ✓ |
| 许可待定 `acquired / 待定` | 拒绝 | `REJECT: source not allowlisted` ✓ |
| 自造类型 `pirated / unknown` | 拒绝 | `REJECT: source not allowlisted` ✓ |
| 重复 ID（同 ID 出现两次） | 拒绝 | `REJECT: duplicate fixture ID` ✓ |
| ID 集合缺项 | 拒绝 | `REJECT: fixture ID set mismatch` ✓ |
| `available` 但 Hash 非真 SHA256 | 拒绝 | `REJECT: no real SHA256` ✓ |
| `generated` available 缺脚本路径/参数 | 拒绝 | `REJECT: generated without script path/params` ✓ |
| `acquired` available 缺版本/授权依据 | 拒绝 | `REJECT: acquired without path/version/licence basis` ✓ |
| `acquired / CC0` 合规（含取得途径、版本、授权依据） | 接受 | `ACCEPT` ✓ |

**该脚本由 Owner 编写，是回归护栏，不构成独立批准。**

## 未完成项

| 项 | 状态 | 原因 |
|---|---|---|
| 产品 / 应用端到端测试 | `N/A` | 仓库无应用源码与可执行产物 |
| 性能与容量实测 | `NOT_RUN` | 无实现、无 ENV-B/ENV-D 基准数据 |
| 模型质量评估 | `BLOCKED` | 素材 20 项全部 `missing`、阈值 `UNAPPROVED_THRESHOLD` 未获批 |
| 素材生成与 Hash 登记 | `NOT_RUN` | 属后续获授权 Task；本轮只维护状态与防线 |
| Mermaid 渲染校验 | `NOT_RUN` | 仅围栏结构检查 |
| D08 §68～§76 的实际覆盖验证 | `NOT_RUN` | 标识与路由已建立；实际测试属 TASK-022/026/027 |
| 独立复审与集成 | 待 Codex | `integration_commit` 仍为空，Task 不得 done |

## 风险与遗留

- allowlist 目前只有三个取值；若后续需要引入新的授权形态（例如其它开放许可），必须先在本文件登记并同步验证脚本，不能靠自造字符串绕过。
- `available` 的脚本路径/参数与取得途径/版本/授权依据要求，当前只经合成样本验证（真实 `available` 数量为 0）；首个真实素材入库时需按该口径登记。
- 素材仍全部未取得；在素材与许可确认前，依赖真实素材的质量结论只能记 `BLOCKED`/`NOT_RUN`。

## 接收与复现

唯一 Git common directory：`G:/CODEX/New Manga/.git`；Owner worktree `G:/CODEX/New Manga.worktrees/TASK-003-deepseek`。

~~~powershell
git diff --stat 9472df5 9c6a73b
git diff --check 9472df5 9c6a73b --
pwsh -NoProfile -File ./verification/TASK-003/verify.ps1
~~~

复审需覆盖 R-015～R-018 的 disposition、来源 allowlist 是否真正拒绝未知/未授权/待定来源、20 个 Fixture ID 是否被完整固定且拒绝重复、`generated`/`acquired` 的生成方式要求是否可验证、Task 正文状态与 frontmatter 是否一致，以及 F-08 是否仍为"仅一条状态边且无删除"。P0/P1 未解决不得批准；P2 必须记录 disposition。
