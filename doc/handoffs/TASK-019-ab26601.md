---
task_id: TASK-019
author: Codex
recipient: DeepSeek Harness（独立 Review，非作者）
base_commit: 36242fb
delivery_head: ab26601
status: in_review
scope: 尾项切片（Standards S-1/S-2：路由策略与保护规则的分层修正）
integrated_baseline: 3755af9（TASK-019 主体，不因本切片失效）
---

# Handoff：TASK-019 尾项切片（Standards S-1 / S-2 分层修正）

## 交付结果

修订提交 **`ab26601`**（10 文件、`+235/−165`）。固定 base `36242fb`（代码基线）；分支起点 `0389eb4`（含 post-hoc Standards 附录的 master）。

**已集成基线 `3755af9` 不因本切片失效**：本切片是对其的**事后分层修正**，不撤销主体集成的任何结论。

| 变化 | 内容 |
|---|---|
| 新增 `src/application/translation/inpaint/route_catalog.py` | 路由**声明与门控**（`RouteRecord`/`ROUTE_TABLE`/`route_record`/`provider_id_for_route`/`route_gate`/`describe_routes`/`learned_routes_declared`）从 infrastructure 逐字迁入 |
| 迁移 `inpaint_router.py` → `src/application/translation/inpaint/router.py` | `git mv` 保留历史；唯一内容改动是把 `route_gate` 的导入目标改指新模块 |
| 新增 `src/application/translation/inpaint/protection.py` | 非目标像素保护**规则** `protected_pixel_violations` 迁入 application；适配器侧 `require_non_target_protection` 留在 infrastructure 改为从 application 导入（方向合法） |
| 收敛 `src/infrastructure/providers/inpaint_routes.py` | 只留光栅实现与 provider；删除已迁出的 158 行；改为导入 application 的规则与门控 |
| 改向 `step.py` / `handlers.py` / `runtime.py` | `step.py` **不再 import infrastructure**；infrastructure 侧改从 application 导入策略与声明（合法方向） |
| 测试 | `test_inpaint.py`、`test_handlers_pipeline.py` 导入路径同步；`test_ports_contract.py` **新增架构回归守卫**（扫描 `src/application/**` 是否 import infrastructure） |

## 动因（来自 post-hoc Standards 轴）

[`doc/reviews/TASK-019-standards-addendum.md`](../reviews/TASK-019-standards-addendum.md)：**S-1**——`src/application/translation/inpaint/step.py` 反向依赖 `infrastructure.providers.inpaint_router`/`inpaint_routes`，违反 `doc/02_TECHNICAL_ARCHITECTURE_.md` §架构方向（`QML/UI → Application → Domain/Ports → Infrastructure Adapters`，"禁止反向依赖"），且全仓 `src/application/**` 仅此一处；**S-2**（同根因）——路由策略与适配器同住 infrastructure。

## 验证证据

环境：Windows `10.0.26200`；`G:/CODEX/New Manga.task-envs/TASK-012-py312/Scripts/python.exe`（Python 3.12.3 / PySide6 6.11.2）；`PYTHONDONTWRITEBYTECODE=1`；CWD = 分支 worktree 根。

| # | 命令 / 步骤 | 退出码 | passed | skipped | 结果 |
|---|---|---:|---:|---:|---|
| 1 | `python -m pytest tests/providers -q -p no:cacheprovider -rs` | 0 | **111** | **0** | 110 → 111（+分层守卫），无减少 |
| 2 | `python -m pytest tests/pipeline tests/core tests/storage -q -p no:cacheprovider -rs` | 0 | **78** | **0** | 与修正前一致 |
| 3 | 全仓 `python -m pytest -q -p no:cacheprovider -rs`（5 次） | 0（4 次）/ 1（1 次） | **641** | **6** | 4 次 `641 passed, 6 skipped`；另 1 次 `1 failed, 640 passed, 6 skipped`（**用例名未捕获**）——见下 |
| 4 | **S-1 断言**：扫描 `src/application/**` 是否 `from/import infrastructure` | — | — | — | **0 命中**（修正前 `step.py` 2 处） |
| 5 | **S-2 断言**：`rg -n "inpaint_router" src tests` | — | — | — | **0 命中** |
| 6 | **判别力**：`git show 726baf5:src/application/translation/inpaint/step.py` 中的 infrastructure 导入 | — | — | — | 确有 2 处 → 新增守卫在旧代码上必然失败 |
| 7 | **独立装配复验**（作者脚本，`%TEMP%`）：`assemble_services` + `providers.status()` | 0 | — | — | 12 个 Provider；四条学习型路线仍 `not_ready/PROVIDER_NOT_IMPLEMENTED`；GPU `unavailable` |

**skip 原因**：命令 1、2 为 `0 skipped`；命令 3 的 6 项均为既有 `tests/network` 的 `openssl unavailable`。

**关于那次偶发失败（如实登记，不掩盖、不归因）**：全仓 5 次运行中 1 次 `1 failed`，用例名未被捕获，其后 4 次全绿；`tests/reading_export/test_qml_contract.py::test_reader_webtoon_swaps_in_vertical_viewer` 是仓库**已登记的低频顺序敏感 flaky**（单独复跑 8 次全过，只在全仓串跑时偶发），与本切片改动路径（`application/translation/inpaint/**`、`infrastructure/providers/**`、`tests/providers/**`）无关；本切片**不把它记为通过**。Reviewer 复跑若复现，请按同一口径登记。

## 行为不变性

纯搬迁 + 导入改向：`router.py` 仅改 1 行导入；迁出代码逐字保留（仅补 docstring）；`inpaint_routes.py` 只删已迁出的定义。证据：全仓 640 → 641（唯一增量是新增守卫），既有 640 项全部仍通过；装配复验的 Provider 状态集合与修正前完全一致。

## 边界与合规

- 仅改 TASK-019 允许路径内的文件（`src/application/translation/inpaint/**`、`src/infrastructure/providers/**`、`tests/providers/**`、`doc/tasks/TASK-019.md`、`doc/handoffs/TASK-019-*.md`、`verification/TASK-019/**`）。
- **未改**依赖清单、Schema、pipeline seam 本体、`AGENTS.md`、生产数据、其他 Task。
- 未 push、未合并 master。

## 状态

`doc/tasks/TASK-019.md` 置 **`in_review`**（本切片待独立 Review；主体集成结论与 `done` 记录保留），交 DeepSeek Harness 独立 Review。集成与最终收口由 Codex 在 Review 通过后执行。
